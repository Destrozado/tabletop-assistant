// app/composables/useGameEndCopy.ts
//
// STUB de la fase RED (plan 09-32, Task 2): exporta la forma final de
// `buildDiscardBody`/`buildEndGameBody` con un cuerpo intencionadamente
// incorrecto, solo para que `useGameEndCopy.test.ts` pueda importar el
// módulo y sus aserciones fallen por assertion real (no por "Cannot find
// module"). La implementación de verdad —y su comentario justificativo,
// citando `09-VERIFICATION.md` ronda 7— llega en la fase GREEN de este
// mismo commit-a-commit.
export function buildDiscardBody(_savedSummary: string): string {
  return ''
}

export function buildEndGameBody(_savedSummary: string): string {
  return ''
}
