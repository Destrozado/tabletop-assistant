---
phase: 08-valores-conocidos-dentro-del-paso
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - app/composables/useGameSession.ts
  - app/composables/__tests__/useGameSession.test.ts
  - app/components/StepScreen.vue
  - app/pages/[game]/index.vue
autonomous: false
requirements: [VAL-01, VAL-02, VAL-03, VAL-04, VAL-05, VAL-06]

must_haves:
  truths:
    - "VAL-01/D-08: con villano elegido, `setup.escenario.02` muestra la cifra entre paréntesis DENTRO del mismo `<p class=\"text-display\">` del texto, separada por un espacio, con el mismo estilo — sin `text-accent`, sin elemento nuevo, sin bloque propio"
    - "VAL-02/D-09: `setup.heroes.03`, `setup.manos.02` y `setup.manos.03` muestran bajo la frase una lista «Jugador N · Héroe» a la izquierda en `text-body` y el número a la derecha en `text-heading` negrita, con `max-w-[720px]` y `border-b border-accent/50`"
    - "D-09/D-32: las filas de esa lista NO son `<button>`, no tienen chevron `›`, ni `@click`, ni `aria-label` — no hay afordancia donde no hay acción"
    - "D-10: el bloque no lleva rótulo («VIDA INICIAL», «VALORES»…), a diferencia de `ELECCIÓN` y `Opciones`"
    - "D-11: con `playerCount: 1` la lista es de una fila y no se colapsa a paréntesis"
    - "D-12: el orden del `<template>` es frase grande → lista de valores → aviso `⚠` (el mulligan es el único marcado que además lleva `⚠`)"
    - "D-14: con selección parcial se pintan solo las filas conocidas, sin `'—'` ni renglón vacío"
    - "VAL-03/D-15: sin ninguna selección los cuatro pasos se pintan exactamente como hoy — el paréntesis no existe y el bloque de lista no está en el DOM (no está vacío: no está)"
    - "ARCHITECTURE §3/§5: `StepScreen.vue` y `app/pages/[game]/index.vue` siguen sin importar `~~/engine/*`; todo llega pre-resuelto desde `useGameSession.ts`"
    - "T-01-01: las dos superficies nuevas se pintan con interpolación `{{ }}`, nunca con HTML crudo"
    - "VAL-04/D-16: el `git diff` de toda la fase sobre `content/marvel-champions.json` sigue siendo cuatro líneas `\"value\"` añadidas y nada más"
    - "VAL-05/VAL-06/D-16: `npm test` verde con los 35 clips intactos y el gate de deriva de voz sin pedir regenerar ninguno; la locución sigue diciendo la frase genérica, sin el número"
  artifacts:
    - path: "app/composables/useGameSession.ts"
      provides: "`buildStepValueSuffix`/`buildStepValueCells` puros más las computeds `stepValueSuffix` y `stepValueRows` en el objeto devuelto"
      exports: ["buildStepValueSuffix", "buildStepValueCells", "StepValueCell"]
    - path: "app/components/StepScreen.vue"
      provides: "Prop `stepValueSuffix` interpolado en el `<p>` de `actionText` y bloque de lista sin afordancia"
      contains: "stepValueRows"
    - path: "app/pages/[game]/index.vue"
      provides: "Los dos valores nuevos desestructurados del composable y pasados como props"
      contains: ":step-value-rows"
    - path: "app/composables/__tests__/useGameSession.test.ts"
      provides: "Tests de las dos funciones puras nuevas (etiqueta, orden, sufijo, ausencia)"
  key_links:
    - from: "app/composables/useGameSession.ts"
      to: "engine/stepValues.ts"
      via: "import de `resolveStepValue`/`resolveStepValueRows`"
      pattern: "from '~~/engine/stepValues'"
    - from: "app/pages/[game]/index.vue"
      to: "app/components/StepScreen.vue"
      via: "props `:step-value-suffix` y `:step-value-rows`"
      pattern: ":step-value-(suffix|rows)"
    - from: "app/components/StepScreen.vue"
      to: "app/composables/useGameSession.ts"
      via: "el componente no importa nada del motor; recibe todo resuelto"
      pattern: "stepValueSuffix"
---

<objective>
Coser el resolutor del motor a la pantalla: dos computeds nuevas en `useGameSession.ts`, las
dos superficies de render en `StepScreen.vue` (el paréntesis de D-08 y la lista de D-09/D-10)
y el paso de props desde la página. Cierra la fase con la verificación mecánica de VAL-04 /
VAL-05 / VAL-06 y una lectura humana de legibilidad.

Purpose: es el único plan que el grupo ve. Todo lo demás de la fase existe para que estas dos
superficies puedan pintarse sin que ningún componente sepa qué paso es ni toque el motor.
Output: la cifra en pantalla, con `npm test` verde y el contenido intacto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md
@.planning/phases/08-valores-conocidos-dentro-del-paso/08-PATTERNS.md
@.planning/phases/08-valores-conocidos-dentro-del-paso/08-01-SUMMARY.md
@.planning/phases/08-valores-conocidos-dentro-del-paso/08-02-SUMMARY.md

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del código actual. -->

`engine/stepValues.ts` (plan 08-02):
  export interface StepValueRow { slot: number, heroId: string, heroName: string, playerName: string, value: number }
  export function resolveStepValue(kind: string | null | undefined, context: SessionContext, catalogue: CharacterCatalogue | null): number | null
  export function resolveStepValueRows(kind: string | null | undefined, context: SessionContext, catalogue: CharacterCatalogue | null): StepValueRow[]

`app/composables/useGameSession.ts` — precedentes a copiar:
  - visibilidad derivada del dato, línea ~192:
      const showsSelectionGrid = computed<boolean>(() => currentNode.value?.step.selection === 'characters')
    (igualdad estricta a propósito: el contenido llega como JSON crudo, la clave puede faltar)
  - separación función pura + computed fina, líneas 48-82 y 213-219:
      export function buildCounterCells(values: CounterState, slots: {…}[]): CounterCell[]
      const counterCells = computed<CounterCell[]>(() => { … getCatalogue(session.value.gameId) … })
  - etiqueta de jugador, dentro de `buildCounterCells`:
      const baseLabel = resolvePlayerLabel(i, slot.playerName)     // de './useHeroSearch'
      // resolvePlayerLabel(slotIndex, playerName) → playerName si no está vacío, si no `Jugador ${slotIndex + 1}`
  - literal de separador ya usado en el fichero: ' · ' (en `DEFEATED_SUFFIX = ' · SIN VIDA'`)
  - el objeto devuelto termina en: showsCounterBand, counterCells, incrementCounter, decrementCounter

`app/components/StepScreen.vue` — estado de hoy:
  - línea 5: `withDefaults(defineProps<{ … }>(), { selectionRows: null, duplicateWarningText: null })`
  - línea 57: `<p class="text-display font-bold text-primary-text">{{ actionText }}</p>`
  - líneas 68-97: bloque `selectionRows` (el molde a copiar y desnudar): wrapper
    `<div v-if="…" class="w-full flex flex-col items-center gap-sm">`, rótulo
    `<p class="text-label font-bold uppercase text-secondary-text">ELECCIÓN</p>`, rejilla
    `<div class="w-full max-w-[720px] grid grid-cols-1">`, y filas `<button type="button"
    class="w-full min-h-12 px-md py-sm flex items-center justify-between gap-md text-left
    text-body font-normal border-b border-accent/50 transition-transform duration-75
    active:brightness-95" :aria-label @click>` con `<span class="text-accent shrink-0">›</span>`.
  - línea 99: empieza el bloque `options`; líneas 128-138: los avisos `⚠` finales.
  - cabecera (líneas 2-4): «Componente tonto. Renderiza SIEMPRE con interpolación … prohibida
    la directiva de HTML crudo en toda la app (T-01-01)».

`app/pages/[game]/index.vue`:
  - líneas 57-80: desestructuración de `useGameSession()`, terminando en `incrementCounter,
    decrementCounter,`
  - líneas 699-712: el bloque `<StepScreen … />`, con `:selection-rows="selectionRows"` y
    `:duplicate-warning-text="duplicateWarningText"` como últimos props.

`app/assets/css/main.css:35-42` — escala fija: display 2.5rem / heading 1.75rem / body
1.25rem / label 1.125rem. No se estrena ningún tamaño.

`app/composables/__tests__/useGameSession.test.ts` — la suite existente NO monta nada: solo
prueba las funciones puras exportadas, en el proyecto `app-logic` (entorno node), con los
alias `~`/`~~` resueltos por `vitest.config.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Costura reactiva en `useGameSession.ts` (+ tests de las funciones puras)</name>
  <files>app/composables/useGameSession.ts, app/composables/__tests__/useGameSession.test.ts</files>
  <read_first>
    - app/composables/useGameSession.ts (fichero completo: cabecera de arquitectura, `buildCounterCells`, `showsSelectionGrid`, `counterCells`, y el objeto de retorno)
    - app/composables/useHeroSearch.ts (líneas 118-130: `resolvePlayerLabel`)
    - app/composables/__tests__/useGameSession.test.ts (fichero completo: estilo de la suite)
    - engine/stepValues.ts (las firmas creadas en el plan 08-02)
    - .planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md (D-08, D-09, D-14, D-15)
  </read_first>
  <action>
    En `app/composables/useGameSession.ts`:

    1. Importar `resolveStepValue` y `resolveStepValueRows` desde `~~/engine/stepValues`, y
       el tipo `StepValueRow` con `import type`, en el bloque de imports del motor
       (orden alfabético entre los demás `~~/engine/*`).

    2. Exportar `interface StepValueCell { key: string, label: string, value: number }` —
       la forma que `StepScreen.vue` recibirá, hermana de `CounterCell`.

    3. Exportar dos funciones PURAS (fuera de `useGameSession`, junto a `buildCounterCells`,
       para que la suite existente pueda probarlas sin montar nada):
       - `buildStepValueSuffix(value: number | null): string | null` — devuelve
         `` ` (${value})` `` (con UN espacio inicial, paréntesis normales, sin espacios
         dentro) cuando `value` es un número finito, y `null` en cualquier otro caso
         (incluido `null`, `undefined` y `NaN`). Es lo que hace que D-15 sea mecánico:
         sin cifra, no hay sufijo, y por tanto el `<p>` renderiza exactamente el mismo
         texto que hoy.
       - `buildStepValueCells(rows: StepValueRow[]): StepValueCell[]` — mapea cada fila a
         `key: \`valor-${row.slot}\``, `label: \`${resolvePlayerLabel(row.slot,
         row.playerName)} · ${row.heroName}\`` (mismo glifo separador ` · ` que ya usa
         `DEFEATED_SUFFIX` en este fichero) y `value: row.value`. NO introduce ningún
         fallback `'—'` (D-14: las filas sin valor ya vienen filtradas por el motor) y NO
         trunca la etiqueta en JS: el truncado es de CSS en el componente.
         El formato de clave `valor-{slot}` es deliberadamente distinto del `jugador-{i}`
         de la banda de contadores, porque estas filas no son pulsables y su clave no
         viaja en ningún evento.

    4. Añadir dos computeds dentro de `useGameSession`, junto a `counterCells`:
       - `stepValueSuffix = computed<string | null>(...)`: `null` si no hay sesión; si la
         hay, obtiene el catálogo con `getCatalogue(session.value.gameId)` y devuelve
         `buildStepValueSuffix(resolveStepValue(currentNode.value?.step.value,
         session.value.context, catalogue))`.
       - `stepValueRows = computed<StepValueCell[] | null>(...)`: `null` si no hay sesión;
         si la hay, construye las celdas con `buildStepValueCells(resolveStepValueRows(
         currentNode.value?.step.value, session.value.context, catalogue))` y devuelve
         `null` —no un array vacío— cuando no queda ninguna fila. `null` es lo que hace que
         el bloque no exista en el DOM (D-15), consistente con el valor por defecto de
         `selectionRows`.
       Ambas leen `currentNode.value?.step.value` y NO comparan contra ningún id de paso: la
       decisión de qué se pinta vive en el dato (D-01/TECH-04), igual que `showsSelectionGrid`.

    5. Añadir `stepValueSuffix` y `stepValueRows` al objeto devuelto, después de
       `counterCells`.

    Comentarios en español, en el registro del fichero, citando D-08 (el sufijo se
    interpola dentro de la frase grande, no es un bloque), D-14/D-15 (fila a fila; sin
    ninguna fila, `null`, no un array vacío) y la disciplina de componentes tontos.

    En `app/composables/__tests__/useGameSession.test.ts`: añadir un `describe` nuevo para
    las dos funciones puras, sin tocar el de `buildCounterCells`. Casos:
      - `buildStepValueSuffix(42)` es `' (42)'` exactamente (aserto de igualdad de cadena,
        espacio inicial incluido); `buildStepValueSuffix(0)` es `' (0)'`;
        `buildStepValueSuffix(null)` es `null` (VAL-03/D-15).
      - `buildStepValueCells([])` devuelve `[]`.
      - una fila sin `playerName` produce `label` «Jugador 1 · Spider-Man» (base 1 en la
        etiqueta, `slot` 0 en la clave) y `key` `'valor-0'`.
      - una fila con `playerName: 'Ana'` produce «Ana · Thor».
      - dos filas con `slot` 0 y 2 (selección parcial, D-14) conservan sus claves
        `valor-0` y `valor-2` y ninguna celda intermedia se inventa.
      - ninguna celda contiene el carácter `'—'` (D-14: ese marcador es de la banda de
        contadores, no de esta lista).
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/useGameSession.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from '~~/engine/stepValues'" app/composables/useGameSession.ts` devuelve `1`
    - `grep -n "export function buildStepValueSuffix" app/composables/useGameSession.ts` y `grep -n "export function buildStepValueCells" app/composables/useGameSession.ts` encuentran una línea cada uno
    - `grep -n "stepValueSuffix,\|stepValueRows," app/composables/useGameSession.ts` encuentra las dos entradas dentro del objeto de retorno
    - `grep -c "setup\.\|ronda\." app/composables/useGameSession.ts` devuelve `0` (ningún id de contenido cableado)
    - `grep -c "'—'" app/composables/useGameSession.ts` no aumenta respecto a antes del plan (el único uso sigue siendo el de `buildCounterCells`)
    - `npx vitest run app/composables/__tests__/useGameSession.test.ts` sale con código 0 y la suite incluye al menos 6 tests nuevos
    - `npx tsc --noEmit` no reporta errores
  </acceptance_criteria>
  <done>El composable expone `stepValueSuffix` (cadena `' (N)'` o `null`) y `stepValueRows` (celdas ya etiquetadas o `null`), ambos derivados de la clave del dato, con tests puros que fijan el formato y la ausencia.</done>
</task>

<task type="auto">
  <name>Task 2: Las dos superficies en `StepScreen.vue` y el paso de props en la página</name>
  <files>app/components/StepScreen.vue, app/pages/[game]/index.vue</files>
  <read_first>
    - app/components/StepScreen.vue (fichero completo: props, defaults, y el bloque `selectionRows` que es el molde)
    - app/pages/[game]/index.vue (líneas 55-85 de la desestructuración y 695-715 del bloque `<StepScreen>`)
    - app/assets/css/main.css (líneas 30-50: la escala tipográfica fija)
    - .planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md (D-08, D-09, D-10, D-11, D-12)
    - .planning/phases/08-valores-conocidos-dentro-del-paso/08-PATTERNS.md (sección `app/components/StepScreen.vue`)
  </read_first>
  <action>
    En `app/components/StepScreen.vue`:

    1. Dos props nuevos en `defineProps`, con la misma disciplina de forma en línea que
       `selectionRows` (nada de importar tipos del motor):
       - `stepValueSuffix?: string | null`
       - `stepValueRows?: { key: string, label: string, value: number }[] | null`
       Ambos con valor por defecto `null` en el objeto de `withDefaults`, junto a
       `selectionRows: null`.

    2. D-08 — el `<p>` de `actionText` (hoy la línea 57) pasa a interpolar también el
       sufijo, en el MISMO elemento y en el MISMO nodo de texto:
       `{{ actionText }}{{ stepValueSuffix ?? '' }}`. Sin `<span>` nuevo, sin
       `text-accent`, sin cambiar ni una clase del `<p>`. Cuando el sufijo es `null` el
       texto renderizado es idéntico al de hoy, carácter por carácter (VAL-03/D-15).

    3. D-09/D-10/D-12 — bloque nuevo, colocado DESPUÉS del `</div>` que cierra el bloque de
       `selectionRows` y ANTES del `v-if` del bloque `options`, de modo que el orden del
       template sea frase grande → lista de valores → avisos `⚠`. Estructura: wrapper
       `<div v-if="stepValueRows && stepValueRows.length" class="w-full flex flex-col
       items-center gap-sm">`, SIN el `<p>` de rótulo (D-10: este bloque no lleva
       «ELECCIÓN» ni ningún equivalente), rejilla `<div class="w-full max-w-[720px] grid
       grid-cols-1">`, y por cada fila un `<div v-for … :key="row.key" class="w-full
       min-h-12 px-md py-sm flex items-center justify-between gap-md text-left border-b
       border-accent/50">` que contiene dos `<span>`: el izquierdo
       `class="min-w-0 truncate text-body font-normal text-primary-text"` con
       `{{ row.label }}`, y el derecho `class="shrink-0 text-heading font-bold
       text-primary-text"` con `{{ row.value }}`.

       Regla dura de D-09/D-32, verificable a simple vista en el diff: la fila es un
       `<div>`, NUNCA un `<button>`; sin `type="button"`, sin `@click`, sin `:aria-label`,
       sin la clase `active:brightness-95`, sin `transition-transform`, y sin el
       `<span class="text-accent shrink-0">›</span>` final. No hay acción, así que no hay
       afordancia.

    4. Comentario encima del bloque, en el registro del resto del fichero: citar D-09 (copia
       la anatomía de fila de `selectionRows`), D-10 (sin rótulo, y por qué), D-11 (con un
       jugador es una lista de una fila, no un paréntesis), D-12 (por qué va aquí y no
       después de los avisos) y D-32 (por qué no es un `<button>` — para que nadie lo
       «unifique» con el bloque de arriba en una revisión futura).

    En `app/pages/[game]/index.vue`:

    5. Añadir `stepValueSuffix` y `stepValueRows` a la desestructuración de
       `useGameSession()`, después de `counterCells`. NO crear ningún `computed` local:
       llegan ya resueltos, igual que `counterCells` (D-04 rechaza expresamente recomputar
       lógica pura fuera del motor).

    6. Pasarlos al componente en el bloque `<StepScreen …>`:
       `:step-value-suffix="stepValueSuffix"` y `:step-value-rows="stepValueRows"`,
       colocados junto a `:selection-rows`. Ningún evento nuevo: esta lista no emite nada.

    Ni este componente ni esta página pueden importar `~~/engine/*` (ARCHITECTURE §3/§5).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "v-html" app/components/StepScreen.vue` devuelve `0` (T-01-01)
    - `grep -c "stepValueSuffix" app/components/StepScreen.vue` devuelve al menos 3 (prop, default, interpolación)
    - `grep -n '{{ actionText }}{{ stepValueSuffix ?? '"'"''"'"' }}' app/components/StepScreen.vue` encuentra la línea, y el `<p>` sigue teniendo exactamente `class="text-display font-bold text-primary-text"`
    - El bloque de `stepValueRows` no contiene `<button`, `@click`, `aria-label`, `›` ni `active:brightness-95`: `sed -n '/stepValueRows && stepValueRows.length/,/^      <\/div>/p' app/components/StepScreen.vue | grep -cE "button|@click|aria-label|›|active:brightness" ` devuelve `0`
    - Ese mismo bloque contiene `max-w-[720px]`, `border-b border-accent/50`, `text-body` y `text-heading font-bold`
    - `grep -c "ELECCIÓN\|VALORES\|VIDA INICIAL" ` en ese bloque devuelve `0` (D-10)
    - El bloque aparece en el fichero DESPUÉS de la línea de `actionText` y ANTES de la línea `v-if="options && options.length"`: comprobar con `grep -n` que el número de línea del `v-if` de `stepValueRows` está entre ambos
    - `grep -cE "~~/engine" app/components/StepScreen.vue app/pages/\[game\]/index.vue` devuelve `0` para `StepScreen.vue`
    - `grep -c ":step-value-suffix\|:step-value-rows" app/pages/\[game\]/index.vue` devuelve `2`
    - `grep -c "stepValueSuffix,\|stepValueRows," app/pages/\[game\]/index.vue` encuentra las dos en la desestructuración
    - `npm test` sale con código 0 y `npx tsc --noEmit` no reporta errores
    - `npm run generate` completa sin error (el build estático sigue funcionando)
  </acceptance_criteria>
  <done>Los cuatro pasos marcados pintan su cifra —paréntesis o lista— y el resto de la app queda igual; ningún componente importa el motor y el build estático sigue saliendo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación mecánica de VAL-04/05/06 y lectura humana de legibilidad</name>
  <files>.planning/phases/08-valores-conocidos-dentro-del-paso/08-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md (D-16 y el aviso del `<domain>` sobre «37 clips»: son 35)
    - .planning/ROADMAP.md (los 5 criterios de éxito de la Fase 8)
  </read_first>
  <action>
    Ejecutar primero, sin intervención humana, las 8 comprobaciones mecánicas listadas en
    `<what-built>` y pegar su salida literal en `08-03-SUMMARY.md`: son la evidencia de
    VAL-04, VAL-05 y VAL-06 y ninguna de ellas la hace una persona. Después levantar
    `npm run dev` y pedir al humano la lectura de legibilidad descrita en
    `<how-to-verify>`. No dar la fase por buena hasta recibir la señal de reanudación.
    Ojo con el criterio nº 5 del ROADMAP: dice «37 clips» y ese dato es heredado y ya
    erróneo — el recuento real y verificado es 35, y la comprobación válida es que el
    número de `.m4a` y el de entradas del manifiesto COINCIDAN y no hayan cambiado, no que
    valgan 37.
  </action>
  <what-built>
    ANTES de pedir nada al humano, ejecutar y pegar la salida de estas comprobaciones en el
    SUMMARY (son la evidencia de VAL-04, VAL-05 y VAL-06, y no las hace una persona):

    1. `git diff -U0 content/marvel-champions.json | grep '^+[^+]'` → exactamente 4 líneas,
       las cuatro con `"value":`.
    2. `git diff -U0 content/marvel-champions.json | grep -c '^-[^-]'` → `0`.
    3. `git diff content/marvel-champions.json | grep -c '"text"\|"speech"'` → `0`.
    4. `grep -c '"contentVersion": 13,' content/marvel-champions.json` → `1`.
    5. `ls public/audio/*.m4a | wc -l` y
       `node -e "console.log(Object.keys(JSON.parse(require('fs').readFileSync('scripts/voice/manifest.json','utf8')).entries).length)"`
       → el MISMO número en las dos (hoy 35; el criterio nº 5 del ROADMAP dice «37» y es un
       dato heredado ya erróneo — la comprobación válida es que ambos recuentos coincidan y
       que `git status --porcelain public/audio scripts/voice/manifest.json` esté vacío).
    6. `npx vitest run engine/__tests__/voice-drift.test.ts` → código 0, sin pedir
       regenerar ningún clip (VAL-05/VAL-06).
    7. `npm test` → código 0.
    8. `grep -rn "handSizeHero" app/ engine/stepValues.ts` → ninguna coincidencia en el
       camino de render de esta fase (D-05).

    Después, levantar `npm run dev` y dejar la app en un estado listo para mirar.
  </what-built>
  <how-to-verify>
    En la tablet o en el navegador a 1024x768, con una partida nueva de 3 jugadores en
    dificultad Normal:

    1. En «Decidid, como grupo…» elige un villano (p. ej. Rhino) y héroes para los tres
       jugadores (p. ej. Spider-Man, Thor, She-Hulk).
    2. Avanza hasta «Ajustad el dial de vida del villano al valor indicado…»: la frase debe
       terminar en « (42)», en el mismo tamaño y color que el resto de la frase, leyéndose
       como una sola oración a un brazo de distancia. No debe haber un bloque aparte ni un
       número de otro color.
    3. Avanza hasta «Ajustad vuestro dial de salud…»: debajo de la frase debe verse una
       lista de tres filas, «Jugador 1 · Spider-Man 10», «Jugador 2 · Thor 14»,
       «Jugador 3 · She-Hulk 15» — nombre a la izquierda pequeño, número a la derecha
       grande y en negrita, con una línea fina bajo cada fila. Sin rótulo encima, sin
       flechita `›`, y las filas NO deben poder pulsarse ni reaccionar al tacto.
    4. Avanza hasta «Robad cartas hasta completar vuestra mano inicial»: la lista debe decir
       6, 5 y 6 (mano de la cara Alter-Ego). Spider-Man tiene que poner 6, no 5.
    5. Paso siguiente (Mulligan): la misma lista, y el aviso `⚠ No barajéis las
       descartadas…` DEBAJO de la lista, nunca encima.
    6. Pulsa el botón de escucha/repetición de voz en cualquiera de esos cuatro pasos: la
       locución debe decir la frase genérica SIN ningún número.
    7. Empieza una partida nueva SIN elegir nada: esos mismos cuatro pasos deben verse
       exactamente como antes de esta fase — sin paréntesis, sin lista, sin renglón vacío
       ni hueco reservado bajo la frase.
    8. Con 4 jugadores y héroe elegido solo para el 1 y el 3: la lista debe traer dos filas
       («Jugador 1 …» y «Jugador 3 …»), sin ninguna fila con «—» ni en blanco.
  </how-to-verify>
  <resume-signal>Escribe «aprobado» o describe qué se ve distinto de lo anterior</resume-signal>
  <verify>
    <automated>npm test</automated>
    <human-check>Los 8 pasos de `<how-to-verify>` confirmados en pantalla</human-check>
  </verify>
  <done>Las 8 comprobaciones mecánicas están ejecutadas con su salida pegada en el SUMMARY, `npm test` está en verde con los clips de audio intactos, y el humano ha respondido «aprobado» o ha registrado las diferencias observadas.</done>
  <acceptance_criteria>
    - Las 8 comprobaciones mecánicas de `<what-built>` están ejecutadas y su salida literal pegada en `08-03-SUMMARY.md`
    - `npm test` en verde y `git status --porcelain public/audio scripts/voice/manifest.json` vacío
    - El recuento de `.m4a` y el de entradas del manifiesto coinciden (35 = 35) y ninguno ha cambiado en esta fase
    - El humano ha respondido «aprobado» o ha registrado las diferencias observadas
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Texto de contenido → DOM | Cualquier cadena de `content/marvel-champions.json` acaba dentro de un elemento de la pantalla. |
| `localStorage` (`selection`, `playerName`, `playerCount`) → etiqueta de fila | El nombre de jugador es entrada del usuario y además editable a mano desde DevTools. |
| Composable → componente | `StepScreen.vue` es tonto por contrato: si recibiera un tipo del motor, la disciplina se rompería sin que nada fallara. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering (XSS por contenido tratado como HTML) | `StepScreen.vue`, las dos superficies nuevas | mitigate | Solo interpolación `{{ }}` en el sufijo y en las dos celdas de fila; `grep -c v-html` sobre el fichero debe seguir devolviendo 0 (T-01-01, regla vigente de toda la app). |
| T-08-09 | Tampering | `playerName` manipulado en `localStorage` desbordando la fila | mitigate | `resolvePlayerSlots` ya recorta a `PLAYER_NAME_MAX_LENGTH` (14) y `resolvePlayerLabel` normaliza el vacío; la celda izquierda lleva `min-w-0 truncate` para que un nombre largo no empuje al número fuera de los 720px. |
| T-08-10 | Spoofing (la pantalla afirma una cifra que no es la de la carta) | `stepValueSuffix`/`stepValueRows` | mitigate | Las dos computeds no calculan nada: delegan en `engine/stepValues.ts`, que ya tiene fijadas por test la regla de D-13 y la corrección de D-05. La verificación humana (Task 3, pasos 2 y 4) contrasta 42 y 6 contra la carta real. |
| T-08-11 | Information disclosure | — | accept | La fase no añade red, almacenamiento nuevo ni datos personales: el único dato nuevo en pantalla sale de un catálogo público ya committeado. |
| T-08-SC | Tampering | instalaciones npm/pip/cargo | accept | Esta fase instala CERO paquetes. Si algo pareciera necesitar uno, detenerse y ejecutar el protocolo de legitimidad de paquetes; no está autorizado dentro de este plan. |
</threat_model>

<verification>
1. `npm test` en verde (motor + app-logic, incluido `voice-drift.test.ts`).
2. `npx tsc --noEmit` sin errores y `npm run generate` completa.
3. `git diff -U0 content/marvel-champions.json` = 4 líneas añadidas `"value":`, 0 eliminadas.
4. `grep -c v-html app/components/StepScreen.vue` = 0; `grep -c "~~/engine" app/components/StepScreen.vue` = 0.
5. Bloque de `stepValueRows` sin `<button>`, sin `@click`, sin `aria-label`, sin `›`, sin rótulo.
6. Firma humana del checkpoint (no bloqueante para dispositivo según el ROADMAP, pero sí requerida como lectura de legibilidad).
</verification>

<success_criteria>
- Con villano elegido, `setup.escenario.02` termina en « (42)» dentro de la misma frase grande.
- Con héroes elegidos, los tres pasos por jugador muestran su lista «Jugador N · Héroe → número», con la mano de la cara Alter-Ego.
- Sin selección, los cuatro pasos se ven exactamente como antes de la fase.
- Con selección parcial, solo las filas conocidas, sin marcador.
- `npm test` verde, 35 clips y 35 entradas de manifiesto intactos, contenido con solo cuatro líneas añadidas.
</success_criteria>

<output>
Create `.planning/phases/08-valores-conocidos-dentro-del-paso/08-03-SUMMARY.md` when done
</output>
