---
phase: 08-valores-conocidos-dentro-del-paso
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - engine/stepValues.ts
  - engine/__tests__/stepValues.test.ts
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]

must_haves:
  truths:
    - "D-04: el cálculo vive en un módulo nuevo del motor (`engine/stepValues.ts`) que REUTILIZA `computeInitialVillainHealth` y `computeInitialHeroHealth` de `engine/counters.ts` en vez de reimplementar la aritmética"
    - "D-13 (regla dura): `engine/stepValues.ts` no importa ni llama a `resolveCounterValues` en NINGUNA rama — con el villano ya a 38 de 42, el resolutor sigue devolviendo 42, la cifra impresa"
    - "D-05: la mano inicial sale de `hero.handSizeAlterEgo`, nunca de `handSizeHero` — Spider-Man da 6, no 5"
    - "D-14: con selección parcial se devuelven SOLO las filas cuyo héroe se conoce; no se emite ninguna fila con marcador `'—'` ni con `value: null`"
    - "D-15/VAL-03: sin ninguna selección, el resolutor de paréntesis devuelve `null` y el de filas devuelve un array vacío — no hay nada que pintar"
    - "D-11: `heroHealth` y `handSizeAlterEgo` producen SIEMPRE filas, también con `playerCount: 1`; `villainHealth` produce siempre un valor único, nunca filas"
    - "D-02: la forma de salida se deriva del propio miembro del enum; el módulo no consulta ningún segundo campo de «alcance» ni ningún id de paso"
    - "Contrato defensivo ya establecido (`resolvePlayerSlots`/`resolveCounters`): con `playerCount`, `selection` o `catalogue` manipulados el módulo nunca lanza, nunca devuelve `undefined` y nunca propaga `NaN`"
  artifacts:
    - path: "engine/stepValues.ts"
      provides: "`resolveStepValue` y `resolveStepValueRows`, puros, sin Vue ni DOM"
      exports: ["resolveStepValue", "resolveStepValueRows", "StepValueRow"]
      min_lines: 60
    - path: "engine/__tests__/stepValues.test.ts"
      provides: "Suite que fija D-04, D-05, D-13, D-14, D-15 y la batería defensiva"
      min_lines: 80
  key_links:
    - from: "engine/stepValues.ts"
      to: "engine/counters.ts"
      via: "import de las funciones de cálculo ya testeadas"
      pattern: "import \\{ computeInitialHeroHealth, computeInitialVillainHealth \\} from './counters'"
    - from: "engine/stepValues.ts"
      to: "engine/selection.ts"
      via: "normalización defensiva de huecos de jugador y villano"
      pattern: "resolvePlayerSlots|resolveVillainId"
---

<objective>
Escribir el módulo puro que resuelve, para un paso que declara `value`, la cifra a pintar:
un único número para `villainHealth` y una lista por jugador para `heroHealth` y
`handSizeAlterEgo`. Con sus tests propios.

Purpose: es el único sitio donde vive la aritmética de esta fase, y el único sitio donde se
puede fijar por test la regla dura de D-13 (siempre la cifra impresa del catálogo, jamás el
contador congelado) y la corrección de fidelidad de D-05 (`handSizeAlterEgo`, no
`handSizeHero`). Nada de esto podría fijarse si viviera como computed en el composable.
Output: `engine/stepValues.ts` + `engine/__tests__/stepValues.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md
@.planning/phases/08-valores-conocidos-dentro-del-paso/08-PATTERNS.md
@.planning/phases/08-valores-conocidos-dentro-del-paso/08-01-SUMMARY.md

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del código actual. -->

`engine/counters.ts` — lo que SÍ se reutiliza (D-04):
  export function computeInitialVillainHealth(
    villain: CatalogueVillain | null,
    playerCount: number,
    difficulty: Difficulty,
  ): number | null
  export function computeInitialHeroHealth(hero: CatalogueHero | null): number | null
Lo que NO se toca en ningún caso (D-13): `resolveCounterValues`, `resolveCounters`,
`incrementVillain`/`decrementVillain`/`incrementHero`/`decrementHero`.

`engine/selection.ts`:
  export function resolvePlayerSlots(context: SessionContext): { heroId: string | null, playerName: string }[]
  export function resolveVillainId(context: SessionContext): string | null
Ambas ya normalizan defensivamente: la longitud la manda `context.playerCount` (0 si no es
entero positivo), validan por TIPO, nunca lanzan, nunca devuelven `undefined` dentro del
array.

`engine/types.ts`:
  export type StepValueKind = 'villainHealth' | 'heroHealth' | 'handSizeAlterEgo'   // plan 08-01
  export interface CatalogueHero { id: string, name: string, alterEgo: string, health: number, handSizeHero: number, handSizeAlterEgo: number }
  export interface CatalogueVillain { id: string, name: string, stages: VillainStage[] }
  export interface CharacterCatalogue { heroes: CatalogueHero[], villains: CatalogueVillain[] }
  export interface SessionContext { playerCount, difficulty, selection, counters, … }

Patrón de búsqueda en el catálogo que ya usa `engine/counters.ts` (plantilla estructural,
NO se importa la función que lo contiene):
  `catalogue !== null ? catalogue.villains.find(v => v.id === villainId) ?? null : null`
  `catalogue !== null ? catalogue.heroes.find(h => h.id === slot.heroId) ?? null : null`

Cifras de referencia ya verificadas en `content/marvel-characters.json` (usar estas, no
inventar otras): Rhino etapa I = 14 `healthPerHero` → 42 con 3 jugadores en normal;
Kang etapa I = 12 normal / 15 experto `healthPerHero`; Thor `health` 14, `handSizeHero` 4,
`handSizeAlterEgo` 5; Spider-Man `health` 10, `handSizeHero` 5, `handSizeAlterEgo` 6;
She-Hulk `health` 15, `handSizeAlterEgo` 6.

Cómo cargan el catálogo real los tests hermanos (`engine/__tests__/counters.test.ts:1-38`):
`readFileSync` + `JSON.parse` sobre `new URL('../../content/marvel-characters.json',
import.meta.url)`, sin validador de esquema (eso ya lo cubre `catalogueSchema.test.ts`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear `engine/stepValues.ts`</name>
  <files>engine/stepValues.ts</files>
  <read_first>
    - engine/counters.ts (fichero completo: las dos funciones a reutilizar, el patrón de búsqueda en catálogo, y `resolveCounterValues` — para saber exactamente qué NO importar)
    - engine/selection.ts (líneas 1-90: contrato de normalización defensiva y firmas de `resolvePlayerSlots`/`resolveVillainId`)
    - engine/types.ts (`StepValueKind`, `CatalogueHero`, `CatalogueVillain`, `CharacterCatalogue`, `SessionContext`)
    - .planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md (D-04, D-05, D-11, D-13, D-14, D-15)
  </read_first>
  <action>
    Crear `engine/stepValues.ts`, módulo puro (cero imports de Vue/Nuxt/DOM, solo relativos
    a `./counters`, `./selection` y `./types`), con esta superficie pública exacta:

    - `export interface StepValueRow { slot: number, heroId: string, heroName: string,
      playerName: string, value: number }` — `slot` en base 0; `heroName` es
      `hero.name` del catálogo; `playerName` es el crudo del hueco (la etiqueta
      «Jugador N» la resuelve la capa de app con `resolvePlayerLabel`, no este módulo, que
      no puede importar de `app/`). El campo `value` es SIEMPRE un `number`: una fila con
      valor desconocido no existe (D-14).

    - `export function resolveStepValue(kind: string | null | undefined, context:
      SessionContext, catalogue: CharacterCatalogue | null): number | null` — devuelve la
      cifra única de mesa. Solo la rama `kind === 'villainHealth'` puede devolver un
      número: obtiene el id con `resolveVillainId(context)`, busca el villano en
      `catalogue.villains` con el patrón `find(...) ?? null` de `counters.ts`, y delega en
      `computeInitialVillainHealth(villain, context.playerCount, context.difficulty)`.
      Cualquier otro `kind` (incluidos `'heroHealth'`, `'handSizeAlterEgo'`, `undefined`,
      `null` o un literal desconocido) devuelve `null`.

    - `export function resolveStepValueRows(kind: string | null | undefined, context:
      SessionContext, catalogue: CharacterCatalogue | null): StepValueRow[]` — devuelve las
      filas por jugador. Solo `kind === 'heroHealth'` y `kind === 'handSizeAlterEgo'`
      producen filas; cualquier otro valor devuelve `[]`. Itera
      `resolvePlayerSlots(context)` con su índice, busca el héroe en `catalogue.heroes` por
      `slot.heroId` con el mismo patrón `find(...) ?? null`, y resuelve el número con
      `computeInitialHeroHealth(hero)` para `'heroHealth'` y con `hero.handSizeAlterEgo`
      para `'handSizeAlterEgo'` (campo plano del catálogo: no existe ni hace falta una
      función de cálculo para la mano). Descarta la fila —no la emite— cuando el héroe no
      se encuentra, cuando `catalogue` es `null` o cuando el número resuelto no pasa
      `Number.isFinite` (D-14: sin valor, sin fila; prohibido emitir `'—'`, `null` o un
      hueco reservado). No hay caso especial para `playerCount === 1` (D-11): una fila es
      una lista de una fila.

    La comparación de `kind` es de igualdad estricta contra cada literal, nunca un chequeo
    de veracidad ni un `switch` con `default` que caiga en una rama de render: el contenido
    llega al navegador como JSON crudo sin pasar por el validador de esquema, así que el
    campo puede estar ausente o traer cualquier cadena (mismo razonamiento que el comentario
    de `showsSelectionGrid` en `useGameSession.ts`).

    Contrato defensivo, calcado del de `resolvePlayerSlots`/`resolveCounters`: validar por
    TIPO, nunca lanzar, nunca devolver `undefined`, nunca propagar `NaN`. Un `playerCount`
    de 2.5, `'3'`, `NaN` o `null`, una `selection` de forma inesperada y un `catalogue` a
    `null` deben degradar a `null` / `[]`.

    Comentario de cabecera del fichero, en el registro de los de `counters.ts` y
    `selection.ts` (español, explica el contrato y sobre todo lo que el módulo NO hace):
    debe dejar escrita la regla dura de D-13 — este módulo pinta SIEMPRE la cifra impresa
    del catálogo y JAMÁS lee el contador congelado, por eso no importa
    `resolveCounterValues` en ninguna rama; el caso concreto es volver con `‹ ATRÁS` a
    `setup.escenario.02` con el villano a 38 de 42 y que el paso siga diciendo 42, porque
    el texto dice «ajustad el dial al valor indicado». Citar también D-04 (reutiliza, no
    reimplementa) y D-05/D-07 (`handSizeAlterEgo` porque el setup arranca con la cara de
    Alter-Ego boca arriba, RR v1.7 Apéndice II paso 1).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' engine/stepValues.ts | grep -c 'resolveCounterValues'` devuelve `0` (D-13: ni siquiera fuera de comentarios aparece la llamada; en comentarios sí se la nombra para explicar la prohibición)
    - `grep -c "from './counters'" engine/stepValues.ts` devuelve `1`, y esa línea importa exactamente `computeInitialHeroHealth` y `computeInitialVillainHealth`
    - `grep -c "handSizeHero" engine/stepValues.ts` devuelve `0`
    - `grep -c "'—'" engine/stepValues.ts` devuelve `0` (D-14: aquí no se usa el marcador de la banda de contadores)
    - `grep -cE "vue|nuxt|document|window" engine/stepValues.ts` devuelve `0`
    - `grep -n "export function resolveStepValue\b" engine/stepValues.ts` y `grep -n "export function resolveStepValueRows" engine/stepValues.ts` encuentran una línea cada uno
    - `grep -n "export interface StepValueRow" engine/stepValues.ts` encuentra una línea
    - `npx tsc --noEmit` no reporta errores
  </acceptance_criteria>
  <done>El módulo existe, exporta `StepValueRow`, `resolveStepValue` y `resolveStepValueRows`, reutiliza las dos funciones de `counters.ts`, no menciona `resolveCounterValues` en código, y compila.</done>
</task>

<task type="auto">
  <name>Task 2: Crear `engine/__tests__/stepValues.test.ts`</name>
  <files>engine/__tests__/stepValues.test.ts</files>
  <read_first>
    - engine/__tests__/counters.test.ts (fichero completo: carga de fixtures, cifras de referencia y la batería `normalización defensiva`)
    - engine/__tests__/selection.test.ts (estilo de fixtures de `SessionContext` manipulado)
    - engine/stepValues.ts (el módulo recién creado)
    - .planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md (D-05, D-13, D-14, D-15)
  </read_first>
  <action>
    Crear la suite del módulo, copiando el arranque de `counters.test.ts`: `readFileSync` +
    `JSON.parse` del catálogo real `content/marvel-characters.json` vía
    `fileURLToPath(new URL(...))`, y helpers locales para fabricar un `SessionContext`.
    Reutilizar las MISMAS cifras que ya fija `counters.test.ts` para que los números se
    contrasten entre suites en vez de inventar valores nuevos.

    Casos que la suite debe fijar, cada uno citando su decisión en el nombre del `it` o del
    `describe`:

    1. D-04 / VAL-01 — `resolveStepValue('villainHealth', ...)` con Rhino, 3 jugadores y
       dificultad normal devuelve 42 (la misma cifra que `computeInitialVillainHealth` en
       `counters.test.ts`); con Kang en experto devuelve la cifra de experto, no la normal.
    2. D-13 (el test que pinta la regla dura) — con `context.counters.villainHealth`
       sembrado a 38 y el mismo contexto de Rhino/3/normal, `resolveStepValue` sigue
       devolviendo 42. Añadir un segundo aserto equivalente para héroes: con una vida de
       héroe congelada distinta de la impresa, la fila sigue trayendo la impresa.
    3. D-05 / VAL-02 — `resolveStepValueRows('handSizeAlterEgo', ...)` con Spider-Man
       devuelve 6, y un aserto explícito de que 6 !== `spiderMan.handSizeHero` (5), con un
       comentario que cite el Apéndice II paso 1 del RR v1.7.
    4. VAL-02 — `resolveStepValueRows('heroHealth', ...)` con Thor / She-Hulk devuelve 14 y
       15, en el orden de los huecos, con `slot` en base 0 y `heroName` del catálogo.
    5. D-14 — con `playerCount: 4` y solo los huecos 0 y 2 con héroe, se devuelven
       EXACTAMENTE dos filas, con `slot` 0 y 2; ninguna fila tiene `value` nulo y el array
       no contiene ningún hueco.
    6. D-15 / VAL-03 — sin ninguna selección: `resolveStepValue('villainHealth', …)` es
       `null` y `resolveStepValueRows(kind, …)` es `[]` para los dos kinds por jugador.
    7. D-11 — con `playerCount: 1` y un héroe elegido, `resolveStepValueRows` devuelve una
       fila (no colapsa a valor único), y `resolveStepValue` sigue devolviendo `null` para
       esos dos kinds.
    8. D-02 — cruce de formas: `resolveStepValueRows('villainHealth', …)` es `[]` y
       `resolveStepValue('heroHealth', …)` / `resolveStepValue('handSizeAlterEgo', …)` son
       `null`. Un `kind` desconocido (`'threat'`), `undefined` y `null` dan `null` y `[]`.
    9. Batería defensiva, calcada de la de `resolveCounters`: `playerCount` a 2.5, `NaN`,
       `'3'`, 0 y negativo; `selection` a `null`, a un número y con `heroes` que no es un
       array; `heroId` apuntando a un héroe inexistente; `catalogue` a `null`. Cada caso
       asserta que NO lanza y que el resultado es `null` / `[]` (o filas filtradas), nunca
       `undefined` ni `NaN` dentro de una fila.
    10. D-13, guardarraíl estructural — un aserto de código fuente: leer
        `engine/stepValues.ts` con `readFileSync`, quitar las líneas de comentario
        (`^\s*//` y `^\s*\*`) y comprobar que la cadena `resolveCounterValues` no aparece
        en lo que queda. Es el test que impide que una futura «mejora» reintroduzca el
        contador congelado sin que nadie lo note.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/stepValues.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run engine/__tests__/stepValues.test.ts` sale con código 0 y ejecuta al menos 10 tests
    - `grep -c "D-13" engine/__tests__/stepValues.test.ts` devuelve al menos 2 (el caso de las 38/42 y el guardarraíl de código fuente)
    - `grep -c "handSizeHero" engine/__tests__/stepValues.test.ts` devuelve al menos 1 (el aserto de D-05 contrasta 6 contra 5)
    - `grep -c "resolveCounterValues" engine/__tests__/stepValues.test.ts` devuelve al menos 1 (solo dentro del guardarraíl de fuente)
    - Prueba de mutación manual, documentada en el SUMMARY: cambiar temporalmente `handSizeAlterEgo` por `handSizeHero` en `engine/stepValues.ts` hace fallar la suite; revertir la deja verde otra vez
    - `npm test` sale con código 0
  </acceptance_criteria>
  <done>La suite existe, cubre D-04, D-05, D-11, D-13, D-14, D-15 y la batería defensiva, incluye el guardarraíl de fuente contra `resolveCounterValues`, y `npm test` sigue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → `SessionContext` → `engine/stepValues.ts` | El estado de sesión es editable a mano desde DevTools: `playerCount`, `selection.heroes` y `counters` pueden llegar con cualquier forma. |
| `content/marvel-champions.json` (JSON crudo en el navegador) → `kind` | En ejecución el contenido no pasa por Zod: `step.value` puede ser una cadena arbitraria o estar ausente. |
| `content/marvel-characters.json` (generado por script contra API de terceros) → catálogo | Una regeneración podría traer campos ausentes o no numéricos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | `resolveStepValue` / `resolveStepValueRows` con `SessionContext` manipulado | mitigate | Toda la normalización se delega en `resolvePlayerSlots`/`resolveVillainId` (validan por tipo, nunca lanzan) más un filtro `Number.isFinite` antes de emitir una fila; batería defensiva explícita en la suite (caso 9). |
| T-08-05 | Tampering | `kind` procedente de JSON crudo | mitigate | Igualdad estricta contra los tres literales; cualquier otra cosa devuelve `null`/`[]`, nunca una rama de render por defecto. |
| T-08-06 | Denial of Service (pantalla inutilizable a media partida) | catálogo regenerado con cifras no numéricas | mitigate | `computeInitialVillainHealth` ya guarda con `Number.isFinite`; el módulo nuevo filtra la fila en vez de propagar `NaN` a la pantalla. |
| T-08-07 | Repudiation (la app afirma una cifra falsa) | mezcla de cifra impresa y contador congelado | mitigate | D-13: prohibición de `resolveCounterValues`, fijada por el guardarraíl de código fuente del caso 10 y por el test de 38→42. |
| T-08-SC | Tampering | instalaciones npm/pip/cargo | accept | Esta fase instala CERO paquetes. Si algo pareciera necesitar uno, detenerse y ejecutar el protocolo de legitimidad de paquetes; no está autorizado dentro de este plan. |
</threat_model>

<verification>
1. `npx vitest run engine/__tests__/stepValues.test.ts` en verde.
2. `npm test` en verde (ninguna suite existente se rompe).
3. `grep -v '^//' engine/stepValues.ts | grep -c resolveCounterValues` = 0.
4. Prueba de mutación de D-05 (handSizeAlterEgo → handSizeHero) rompe la suite.
</verification>

<success_criteria>
- `engine/stepValues.ts` resuelve las tres formas del enum reutilizando `engine/counters.ts`.
- Ningún camino del módulo lee un contador congelado.
- Con selección parcial se devuelven solo las filas conocidas; sin selección, nada.
- La suite propia cubre D-04, D-05, D-11, D-13, D-14, D-15 y entradas manipuladas.
</success_criteria>

<output>
Create `.planning/phases/08-valores-conocidos-dentro-del-paso/08-02-SUMMARY.md` when done
</output>
