---
phase: 08-valores-conocidos-dentro-del-paso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/types.ts
  - engine/schema.ts
  - content/marvel-champions.json
autonomous: true
requirements: [VAL-04, VAL-05, VAL-06]

must_haves:
  truths:
    - "D-01: los cuatro pasos declaran la clave EN EL DATO (`content/marvel-champions.json`); no existe ninguna tabla de ids de paso en `engine/` ni en `app/`, ni ninguna deducción a partir del texto del paso"
    - "D-02: la clave es un enum plano de tres valores (`villainHealth` | `heroHealth` | `handSizeAlterEgo`); no hay un segundo campo de «alcance» ni un objeto `{ kind, scope }`"
    - "D-03: exactamente cuatro pasos quedan marcados — `setup.escenario.02` → villainHealth, `setup.heroes.03` → heroHealth, `setup.manos.02` → handSizeAlterEgo, `setup.manos.03` → handSizeAlterEgo"
    - "D-05/D-07: el valor del enum para la mano inicial se llama `handSizeAlterEgo` (no `handSizeHero`, no `startingHandSize`, no `handSize`), dejando la conclusión de reglas visible en el propio contenido"
    - "D-06: `ronda.jugadores.02` NO lleva la clave `value` — sigue byte-idéntico"
    - "D-01: `contentVersion` sigue en 13; ninguna partida en curso se descarta"
    - "VAL-04/D-16: `git diff content/marvel-champions.json` añade exactamente 4 líneas, todas `\"value\":`, y no elimina ni modifica ninguna"
    - "VAL-05/VAL-06/D-16: `engine/__tests__/voice-drift.test.ts` sigue verde sin regenerar ningún clip; los 35 `.m4a` de `public/audio/` y las 35 entradas de `scripts/voice/manifest.json` quedan intactos"
    - "CR-01: `StepSchema` es `z.strictObject`; la clave nueva está declarada en `engine/schema.ts` y el contenido real sigue validando en CI"
  artifacts:
    - path: "engine/types.ts"
      provides: "`StepValueKind` exportado y `value?: StepValueKind` en `StepDefinition`, sin ningún import de zod"
      contains: "handSizeAlterEgo"
    - path: "engine/schema.ts"
      provides: "`value: z.enum([...]).optional()` dentro de `StepSchema`, hermana de `selection`"
      contains: "villainHealth"
    - path: "content/marvel-champions.json"
      provides: "Cuatro claves `\"value\"` en los cuatro pasos de D-03"
      contains: "\"value\": \"handSizeAlterEgo\""
  key_links:
    - from: "content/marvel-champions.json"
      to: "engine/schema.ts"
      via: "validación estricta en `engine/__tests__/content.test.ts`"
      pattern: "value: z\\.enum"
    - from: "engine/types.ts"
      to: "engine/schema.ts"
      via: "los tres literales del enum coinciden exactamente en ambos ficheros"
      pattern: "villainHealth.*heroHealth.*handSizeAlterEgo"
---

<objective>
Declarar el contrato del dato de la Fase 8: la clave opcional `value` en el esquema de paso,
su espejo en el tipo TypeScript que `app/` importa, y las cuatro apariciones de esa clave en
los cuatro pasos de preparación de D-03.

Purpose: sin la clave declarada en `engine/schema.ts`, el contenido deja de validar
(`z.strictObject`, CR-01); sin ella en `engine/types.ts`, la capa de app no puede leerla con
tipos. Este plan no pinta nada todavía — establece el dato del que todo lo demás se deriva.
Output: `engine/types.ts` y `engine/schema.ts` con la clave nueva, y cuatro líneas
`"value"` añadidas a `content/marvel-champions.json` sin tocar ni un carácter de ningún
`text` ni `speech`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md
@.planning/phases/08-valores-conocidos-dentro-del-paso/08-PATTERNS.md

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del código actual. -->
<!-- No hace falta explorar el repositorio: esto es el estado de hoy. -->

`engine/types.ts:43-60` — `StepDefinition` hoy (el precedente literal a copiar es `selection?: 'characters'`):
  export interface StepDefinition extends TextBlock {
    id: string
    title: string
    kind: 'step' | 'summary'
    selection?: 'characters'
    variants?: { difficulty?: Partial<Record<Difficulty, Partial<TextBlock>>> }
    citation?: Citation
  }
Cabecera del fichero (líneas 1-4): «Cero imports de Vue/Nuxt/DOM … este módulo se importa
desde `app/`». Nada de zod aquí.

`engine/schema.ts:68-87` — `StepSchema` hoy; `selection: z.enum(['characters']).optional()`
está en la línea 79, justo antes de `variants`. Todo objeto del fichero es `z.strictObject`.

`content/marvel-champions.json:5` — `"contentVersion": 13,` (NO se toca).

Anclas exactas de los cuatro pasos a modificar (número de línea de HOY, verificado):
  - línea 178: `"kind": "step",` dentro de `setup.escenario.02` (id en línea 176)
  - línea  47: `"kind": "step",` dentro de `setup.heroes.03`   (id en línea  45)
  - línea 305: `"kind": "step",` dentro de `setup.manos.02`    (id en línea 303)
  - línea 317: `"kind": "step",` dentro de `setup.manos.03`    (id en línea 315)
En los cuatro, la línea siguiente es hoy `"text": …` (en `setup.manos.03`, `"text"` y luego
`"warning"`). El precedente de colocación es `setup.heroes.01`, donde `"selection":
"characters",` va entre `"kind"` y `"text"`.

`content/marvel-champions.json:415-419` — `ronda.jugadores.02`, el paso que D-06 deja
explícitamente FUERA. No se toca.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declarar la clave `value` en el tipo y en el esquema</name>
  <files>engine/types.ts, engine/schema.ts</files>
  <read_first>
    - engine/types.ts (fichero completo: la cabecera de «cero zod» y el bloque `StepDefinition`)
    - engine/schema.ts (líneas 1-95: el comentario de `z.strictObject` y el bloque `StepSchema`)
    - .planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md (D-01, D-02, D-07)
  </read_first>
  <action>
    En `engine/types.ts`: exportar un alias de tipo nuevo
    `StepValueKind` con exactamente los tres miembros `'villainHealth' | 'heroHealth' |
    'handSizeAlterEgo'`, y añadir el campo opcional `value?: StepValueKind` a la interfaz
    `StepDefinition`, colocado inmediatamente después de `selection?: 'characters'` y antes
    de `variants?`. El alias se exporta (no se inlinea) porque `engine/stepValues.ts`
    (plan 08-02) y `app/composables/useGameSession.ts` (plan 08-03) lo van a nombrar.
    Este fichero NO importa zod ni nada de Vue/Nuxt/DOM — respetar su cabecera.

    Comentario a escribir encima del campo, en el mismo registro que el de `selection`
    (español, explicando la decisión y su porqué, no lo que hace el código): citar D-01
    (la clave vive EN EL DATO, nunca una tabla de ids en `engine/` ni en `app/` — misma
    disciplina de TECH-04/D-24 que ya impuso `selection`), D-02 (enum plano de tres
    valores; que se pinte paréntesis o lista se DERIVA del propio tipo, por eso no hay un
    segundo campo de «alcance» que pueda contradecir al primero) y D-07/D-05 (se llama
    `handSizeAlterEgo` nombrando el campo exacto del catálogo porque el setup arranca con
    la cara de Alter-Ego boca arriba — Rules Reference v1.7, Apéndice II paso 1—, y
    `handSizeHero` daría la cifra equivocada en los 23 héroes). Igual que `selection`, el
    campo va FUERA de `TextBlock`: no debe poder variar por dificultad.

    En `engine/schema.ts`: añadir dentro de `StepSchema` la clave
    `value: z.enum(['villainHealth', 'heroHealth', 'handSizeAlterEgo']).optional(),`
    inmediatamente después de la línea de `selection` y antes de `variants`. Los tres
    literales deben coincidir carácter por carácter con los del alias de `engine/types.ts`.
    NO añadir ninguna regla en `superRefine`: como `selection`, es un flag solitario sin
    campo dependiente que pueda quedar huérfano (el propio comentario de `selection` ya lo
    razona). NO tocar `GameDefinitionSchema.contentVersion`.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/schema.test.ts engine/__tests__/content.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handSizeAlterEgo" engine/types.ts` devuelve al menos 1 y `grep -c "^import\|from 'zod'" engine/types.ts | grep -q 0` sigue siendo cierto (el fichero no importa zod: `grep -c "zod" engine/types.ts` devuelve 0)
    - `grep -n "export type StepValueKind" engine/types.ts` encuentra exactamente una línea
    - `grep -n "value?: StepValueKind" engine/types.ts` encuentra exactamente una línea, dentro del bloque `StepDefinition`
    - `grep -n "value: z.enum(\['villainHealth', 'heroHealth', 'handSizeAlterEgo'\]).optional()" engine/schema.ts` encuentra exactamente una línea
    - `grep -c "superRefine" engine/schema.ts` devuelve el mismo número que antes del cambio (ninguna regla nueva)
    - `npx vitest run engine/__tests__/schema.test.ts engine/__tests__/content.test.ts` sale con código 0
    - `npx tsc --noEmit` (o `npx nuxt typecheck` si es el comando del proyecto) no reporta errores nuevos en `engine/`
  </acceptance_criteria>
  <done>`StepValueKind` existe y se exporta desde `engine/types.ts`, `StepDefinition.value` es opcional y del tipo del alias, `StepSchema` declara el mismo enum como opcional, y el contenido actual (todavía sin ninguna clave `value`) sigue validando.</done>
</task>

<task type="auto">
  <name>Task 2: Marcar los cuatro pasos de D-03 en el contenido, sin tocar texto ni voz</name>
  <files>content/marvel-champions.json</files>
  <read_first>
    - content/marvel-champions.json (líneas 1-10 para `contentVersion`; líneas 18-26 para el precedente `"selection": "characters"` de `setup.heroes.01`; líneas 43-52, 174-182, 301-310, 313-322 para los cuatro pasos a marcar; líneas 413-420 para `ronda.jugadores.02`, que NO se toca)
    - .planning/phases/08-valores-conocidos-dentro-del-paso/08-CONTEXT.md (D-03, D-06, D-16)
    - engine/schema.ts (el `StepSchema` recién modificado en la Task 1)
  </read_first>
  <action>
    Añadir exactamente CUATRO líneas nuevas a `content/marvel-champions.json`, cada una
    inmediatamente DESPUÉS de la línea `"kind": "step",` de su paso y con la misma sangría
    que ella (misma colocación que `"selection": "characters",` en `setup.heroes.01`):

      - en `setup.escenario.02` → `"value": "villainHealth",`
      - en `setup.heroes.03`   → `"value": "heroHealth",`
      - en `setup.manos.02`    → `"value": "handSizeAlterEgo",`
      - en `setup.manos.03`    → `"value": "handSizeAlterEgo",`  (el mulligan; su clave
        `"warning"` existente se queda exactamente donde está, sin reordenar)

    Reglas duras de esta edición:
      - NO modificar ni un carácter de ningún campo `text` ni `speech` de ningún paso
        (VAL-04). Ninguna línea existente puede aparecer como eliminada en el `git diff`.
      - NO bumpear `"contentVersion"`: se queda en 13 (D-01; precedente verificado de la
        Fase 6, que añadió `selection` sin bumpear). Bumpearlo descartaría la partida en
        curso de cualquiera que esté jugando.
      - NO marcar `ronda.jugadores.02` (D-06): el tamaño de mano de la ronda depende de la
        cara boca arriba de cada jugador, que la app no rastrea.
      - NO añadir la clave a ningún quinto paso, ni «de paso» a ningún otro fichero de
        contenido.
      - No reformatear, reordenar claves ni normalizar el JSON: el único cambio permitido
        son las cuatro inserciones.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `git diff -U0 content/marvel-champions.json | grep -c '^+[^+]'` devuelve exactamente `4`
    - `git diff -U0 content/marvel-champions.json | grep '^+[^+]' | grep -c '"value":'` devuelve exactamente `4`
    - `git diff -U0 content/marvel-champions.json | grep -c '^-[^-]'` devuelve exactamente `0` (ninguna línea eliminada ni modificada)
    - `git diff content/marvel-champions.json | grep -c '"text"\|"speech"'` devuelve `0`
    - `grep -c '"contentVersion": 13,' content/marvel-champions.json` devuelve `1`
    - `python3 -c "import json,sys; d=json.load(open('content/marvel-champions.json')); s=[st for ph in d['phases'] for st in ph['steps']] if 'phases' in d else None"` no lanza (el JSON sigue siendo parseable); alternativamente `node -e "JSON.parse(require('fs').readFileSync('content/marvel-champions.json','utf8'))"` sale con código 0
    - `grep -A 4 '"ronda.jugadores.02"' content/marvel-champions.json | grep -c '"value"'` devuelve `0`
    - `ls public/audio/*.m4a | wc -l` devuelve `35` y `node -e "console.log(Object.keys(JSON.parse(require('fs').readFileSync('scripts/voice/manifest.json','utf8')).entries).length)"` devuelve `35` — mismo número, y ambos sin cambios respecto a antes del plan (`git status --porcelain public/audio scripts/voice/manifest.json` vacío)
    - `npx vitest run engine/__tests__/voice-drift.test.ts` sale con código 0 sin pedir regenerar ningún clip
    - `npm test` sale con código 0
  </acceptance_criteria>
  <done>Los cuatro pasos de D-03 declaran su `value`, `ronda.jugadores.02` no, `contentVersion` sigue en 13, el gate de deriva de voz sigue verde con los 35 clips intactos, y el `git diff` del contenido son cuatro líneas añadidas y nada más.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `content/marvel-champions.json` → motor/app | El contenido se valida con Zod en Node (test/CI), pero llega al navegador como JSON crudo del bundle: en ejecución nadie garantiza la forma. |
| Repositorio → clip de audio pregenerado | Un cambio accidental en `speech` invalidaría silenciosamente un `.m4a` ya generado (la locución diría otra cosa que la pantalla). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `content/marvel-champions.json` → `engine/schema.ts` | mitigate | `StepSchema` es `z.strictObject`: una clave `value` no declarada, o con un literal fuera del enum, hace fallar `engine/__tests__/content.test.ts` en CI (fail loudly at build, CR-01). |
| T-08-02 | Tampering | campos `speech` ↔ `scripts/voice/manifest.json` | mitigate | `engine/__tests__/voice-drift.test.ts` hashea todos los `speech` contra el manifiesto; cualquier roce exige regenerar el clip y rompe la fase (D-16). Criterio de aceptación explícito: 0 líneas `speech` en el `git diff`. |
| T-08-03 | Tampering | `contentVersion` | mitigate | Criterio de aceptación fija `"contentVersion": 13`; un bump accidental descartaría sesiones en curso de usuarios reales. |
| T-08-SC | Tampering | instalaciones npm/pip/cargo | accept | Esta fase instala CERO paquetes: no hay superficie de cadena de suministro. Si algún paso necesitara un paquete nuevo, hay que DETENERSE y ejecutar el protocolo de legitimidad de paquetes antes de instalar; no está autorizado dentro de este plan. |
</threat_model>

<verification>
1. `npm test` en verde (incluye `content.test.ts`, `schema.test.ts` y `voice-drift.test.ts`).
2. `git diff -U0 content/marvel-champions.json` = 4 líneas añadidas, todas `"value":`, 0 eliminadas.
3. `git status --porcelain public/audio scripts/voice/manifest.json` vacío.
4. `grep '"value"' content/marvel-champions.json` devuelve exactamente 4 líneas, ninguna dentro de `ronda.*`.
</verification>

<success_criteria>
- `StepValueKind` exportado desde `engine/types.ts` y `value?: StepValueKind` en `StepDefinition`.
- `value: z.enum(['villainHealth', 'heroHealth', 'handSizeAlterEgo']).optional()` en `StepSchema`.
- Cuatro claves `"value"` en el contenido, en los pasos exactos de D-03 y en ningún otro.
- `contentVersion` en 13; 35 clips y 35 entradas de manifiesto intactos; `npm test` verde.
</success_criteria>

<output>
Create `.planning/phases/08-valores-conocidos-dentro-del-paso/08-01-SUMMARY.md` when done
</output>
