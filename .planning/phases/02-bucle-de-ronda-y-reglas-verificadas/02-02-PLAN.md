---
phase: 02-bucle-de-ronda-y-reglas-verificadas
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - engine/header.ts
  - engine/__tests__/header.test.ts
  - engine/toc.ts
  - engine/__tests__/toc.test.ts
  - app/composables/useGameSession.ts
  - app/components/AppHeader.vue
  - app/components/IndexOverlay.vue
  - app/pages/[game]/index.vue
autonomous: true
requirements: [FLOW-04, FLOW-07, FLOW-08]

must_haves:
  truths:
    - "Dentro del bucle, la cabecera muestra RONDA {n} · {Fase} · {i} de {total de esa fase} — «3 de 6» en la fase del villano, no «29 de 33»"
    - "Durante la preparación la cabecera sigue exactamente igual que en la Fase 1 (PREPARACIÓN · 8 de 23)"
    - "Estando en el bucle, el índice muestra primero los bloques de la ronda y al final la preparación, atenuada y separada por un rótulo"
    - "Dentro del bucle ningún paso lleva ✓; solo el paso actual lleva ●"
    - "Tocar una fila atenuada de la preparación sigue cerrando el índice y saltando allí"
    - "La barra de título del índice dice RONDA a secas, nunca RONDA 4 · Villano"
    - "Al reanudar una partida guardada en mitad del bucle, el aviso de partida guardada dice en qué ronda y fase se quedó"
    - "La cabecera nunca deja que la zona derecha (3 jug · Normal ≡) se encoja o se desplace por culpa de una etiqueta izquierda larga"
    - "D-26: FLOW-08 se cumple sin código nuevo — no hay botón «Volver a la ronda» ni se recuerda el punto de salida"
  artifacts:
    - path: "engine/header.ts"
      provides: "Derivación pura de sectionLabel/plainSectionTitle/position (D-22/D-23)"
      exports: ["describeHeader", "HeaderInfo"]
    - path: "engine/__tests__/header.test.ts"
      provides: "Cobertura Vitest de la derivación por fase vs. global"
    - path: "engine/toc.ts"
      provides: "Reordenado + atenuado + supresión de ✓ dentro del bucle (D-24/D-25)"
      contains: "dimmed"
  key_links:
    - from: "app/composables/useGameSession.ts"
      to: "engine/header.ts"
      via: "computed que envuelve describeHeader"
      pattern: "describeHeader"
    - from: "app/pages/[game]/index.vue"
      to: "app/components/IndexOverlay.vue"
      via: "prop title alimentado por plainSectionTitle, nunca por sectionLabel"
      pattern: ":title=\"plainSectionTitle\""
---

<objective>
Hacer que la cabecera y el índice digan la verdad dentro del bucle: contador relativo a la fase (`RONDA 4 · Villano · 3 de 6`), bloques de la ronda primero y preparación al final como zona de consulta, y `●` sin `✓` porque en un bucle «hecho y no vuelve» es falso.

Purpose: el plan `02-01` deja el bucle funcionando pero contando mal — la cabecera diría `RONDA · 29 de 33`, que es un número sin significado en un ciclo, y el índice seguiría marcando `✓` en pasos que van a repetirse en la ronda siguiente.
Output: una función pura nueva en `engine/`, la extensión de `engine/toc.ts`, y el cableado mínimo en composable, componentes y página.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTEXT.md
@.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-RESEARCH.md
@.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-PATTERNS.md
@.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-UI-SPEC.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-UI-SPEC.md
@engine/toc.ts
@app/composables/useGameSession.ts
</context>

<interfaces>
Contratos que el ejecutor necesita sin explorar el repo.

`engine/types.ts` — `RuntimeStepNode` ya lleva TODO lo necesario, no hay campo nuevo que añadir:
`{ step, sectionId, sectionTitle, sectionRepeats, phaseId, phaseTitle, breadcrumb, runtimeId }`
`sectionRepeats` lo estampa `engine/flatten.ts` desde `section.repeats` del dato autorado.

`engine/toc.ts` hoy exporta:
- `TocRow { id: string, label: string, mark: 'done' | 'current' | null }`
- `TocBlock { label: string, steps: TocRow[] }`
- `tableOfContents(sequence: RuntimeStepNode[], cursor: number): TocBlock[]`

`app/components/AppHeader.vue` props (NO cambian): `sectionLabel: string`, `position: { current, total } | null`, `sessionContext: string`. Su plantilla ya concatena `{{ sectionLabel }}` + `· {{ position.current }} de {{ position.total }}` — la cadena compuesta `RONDA 4 · Villano · 3 de 6` sale sola sin tocar la plantilla.

`app/components/IndexOverlay.vue` props hoy: `title: string`, `blocks: { label, steps: { id, label, mark }[] }[]`. Emite `jump-to` y `close`. Su `numberedBlocks` numera las filas 1..N de forma corrida en el orden en que llegan los bloques — tras el reordenado, la ronda queda 1..10 y la preparación 11..34, que es exactamente lo que pide la maqueta de `02-UI-SPEC.md` §Layout 3.

`app/pages/[game]/index.vue` hoy pasa `:title="sectionLabel"` al `IndexOverlay` (esto es lo que hay que corregir) y compone `savedSummary` a partir de `sectionLabel` + `position` + `sessionContextLabel`.

Estructura del contenido tras el plan `02-01`: `sequence.length === 34`; nodos 0-23 son `setup` (`sectionRepeats:false`, 23 `kind:'step'` + 1 `kind:'summary'`); nodos 24-27 son `ronda.jugadores` (4 pasos, `phaseTitle: 'Jugadores'`); nodos 28-33 son `ronda.villano` (6 pasos, `phaseTitle: 'Villano'`); `sectionTitle` de ambos es `'Ronda'`.
</interfaces>

<decisions_closed_here>
**DC-4 — La reanudación dentro del bucle (PITFALLS.md:179) se resuelve EN ESTA FASE, y sale gratis.**
`02-CONTEXT.md` dejó a criterio del planner si el aviso de partida guardada debía mencionar la ronda. La respuesta es sí, y no requiere código nuevo ni un campo persistido nuevo: `savedSummary` en `app/pages/[game]/index.vue` ya se compone SIEMPRE desde `sectionLabel` + `position` + `sessionContextLabel`. En cuanto `sectionLabel` pase a componer `RONDA {n} · {Fase}` (D-22), el `ResumePrompt` dirá por sí solo `RONDA 4 · Villano · 3 de 6 · 3 jug · Normal` en vez del engañoso `RONDA · 29 de 33`. Se convierte en criterio de aceptación verificable de la tarea 2, no en trabajo adicional.
Esto es también el motivo por el que la derivación va a una función pura compartida y no a dos `computed` separadas: cabecera, título del overlay y resumen de reanudación deben salir de la misma fuente para no desincronizarse.

**DC-5 — Ningún botón «Volver a la ronda» (D-26).** FLOW-08 se cumple sin código nuevo: `jumpTo()` ya no toca `round`, y el camino de vuelta al bucle es el propio índice, cuyos bloques de ronda quedan además los primeros de la lista tras D-24. No se añade estado de «desde dónde salté».
</decisions_closed_here>

<tasks>

<task type="auto">
  <name>Tarea 1: engine/header.ts — derivación pura de la cabecera (D-22/D-23)</name>
  <files>engine/header.ts, engine/__tests__/header.test.ts</files>
  <read_first>
    - `engine/toc.ts` (fichero completo — es el análogo exacto: función pura sobre `RuntimeStepNode[]`, comentario de cabecera de fichero, disciplina TECH-04 de no cablear contra ids)
    - `app/composables/useGameSession.ts` líneas 44-80 (los `computed` `sectionLabel` y `position` actuales, cuya lógica hay que portar; incluido el comentario WR-01 sobre el fallback `?? 'step'`)
    - `engine/types.ts` (`EngineSession`, `RuntimeStepNode`)
    - `engine/__tests__/toc.test.ts` (fichero completo — estructura de tests a replicar, incluida la prueba de pureza con `JSON.parse(JSON.stringify(...))`)
    - `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-RESEARCH.md` §Pattern 1 (el boceto y el aviso sobre `phaseId` vs `sectionId`)
  </read_first>
  <behavior>
    - Nodo de `setup` con `kind:'step'` en el índice 7: `sectionLabel === 'PREPARACIÓN'`, `plainSectionTitle === 'PREPARACIÓN'`, `position === { current: 8, total: 23 }` (idéntico al comportamiento actual de la Fase 1).
    - Nodo `setup.mesa-lista.01` (`kind:'summary'`): `position === null`, `sectionLabel === 'PREPARACIÓN'`.
    - Nodo `ronda.jugadores.03` con `round: 4`: `sectionLabel === 'RONDA 4 · Jugadores'`, `plainSectionTitle === 'RONDA'`, `position === { current: 3, total: 4 }`.
    - Nodo `ronda.villano.03` con `round: 4`: `sectionLabel === 'RONDA 4 · Villano'`, `position === { current: 3, total: 6 }` — NUNCA `{ current: 7, total: 10 }`.
    - Nodo `ronda.villano.06` con `round: 12`: `sectionLabel === 'RONDA 12 · Villano'`, `position === { current: 6, total: 6 }`.
    - `cursor` fuera de rango o secuencia vacía: devuelve `null`.
    - Pureza: dos llamadas con la misma sesión devuelven objetos iguales y no mutan `session.sequence`.
    - Sobre el fixture `tiny-game.json` (sección repetible `loop`, `sectionTitle: 'Bucle'`, `phaseTitle: 'Turno'`), un nodo del bucle con `round: 2` da `sectionLabel === 'BUCLE 2 · Turno'` — prueba de que nada está cableado contra `'ronda'` ni contra `'Villano'` (TECH-04).
  </behavior>
  <action>
    Crea `engine/header.ts`, fichero puro sin ningún import de Vue/Nuxt/DOM, exportando la interfaz `HeaderInfo { sectionLabel: string, plainSectionTitle: string, position: { current: number, total: number } | null }` y la función `describeHeader(session: EngineSession): HeaderInfo | null`.

    Reglas de derivación:
    - `plainSectionTitle` es SIEMPRE `node.sectionTitle.toUpperCase()`, en ambos tramos. Es la fuente del título del overlay del índice; existe justamente para que ese título nunca reutilice la etiqueta compuesta (Pitfall 4 de `02-RESEARCH.md`).
    - Si `node.sectionRepeats` es `true` (D-22): `sectionLabel` es `` `${node.sectionTitle.toUpperCase()} ${session.round} · ${node.phaseTitle}` ``. Ojo: `phaseTitle` va SIN `.toUpperCase()` — de ahí la convención Title Case autorada en `02-01`.
    - Si `node.sectionRepeats` es `false` (D-23): `sectionLabel` es `node.sectionTitle.toUpperCase()`, sin ronda ni fase. La asimetría entre tramos es deliberada: la preparación es lineal y tiene meta; el bucle no.
    - `position` es `null` cuando `(node.step.kind ?? 'step') === 'summary'`.
    - `position` dentro del tramo repetitivo se calcula filtrando la secuencia por `node.phaseId`, **NUNCA por `sectionId`**. La sección `ronda` contiene dos fases (4 + 6 = 10 pasos); filtrar por sección daría `3 de 10` en vez del `3 de 6` que exige la maqueta aprobada.
    - `position` fuera del tramo repetitivo se calcula sobre todos los nodos `kind:'step'` de la secuencia completa, exactamente como hoy — comportamiento sin cambios.
    - Replica el fallback `(node.step.kind ?? 'step')` en todos los filtros: es WR-01, el contenido llega al navegador como JSON crudo sin pasar por el `.default('step')` de Zod.
    - Escribe el comentario de cabecera de fichero siguiendo la convención de `engine/toc.ts`: qué deriva, de qué, y la invariante que preserva; nombra D-22, D-23, TECH-04 y la regla `phaseId`-no-`sectionId` por su nombre.

    Crea `engine/__tests__/header.test.ts` replicando la estructura de `engine/__tests__/toc.test.ts`: carga el fixture `tiny-game.json` con `flatten` + mapeo manual de `runtimeId`, carga el contenido real con `validateGameDefinition`, y cubre todos los casos de `<behavior>`. Construye las sesiones con `expand(...)` y sobrescribe `cursor`/`round` con propagación de objeto (`{ ...session, cursor, round }`), nunca mutando.

    No toques `app/` en esta tarea — el cableado es la tarea 2.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/header.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `engine/header.ts` existe y exporta `describeHeader` y `HeaderInfo`.
    - `grep -c "from 'vue'\|from 'nuxt\|document\.\|window\." engine/header.ts` devuelve 0.
    - `grep -c "'ronda'\|'setup'\|\"ronda\"\|\"setup\"\|Villano\|Jugadores" engine/header.ts` devuelve 0 (TECH-04: cero conocimiento de un juego concreto).
    - `grep -c "phaseId" engine/header.ts` es al menos 1 y `grep -c "sectionId" engine/header.ts` devuelve 0 dentro del cálculo de posición del tramo repetitivo.
    - `npx vitest run engine/__tests__/header.test.ts` pasa, incluyendo el caso `'BUCLE 2 · Turno'` sobre el fixture.
    - `npx vitest run` completo sigue en verde (esta tarea solo añade ficheros).
    - `git diff --name-only` para esta tarea lista exactamente `engine/header.ts` y `engine/__tests__/header.test.ts`.
  </acceptance_criteria>
  <done>La lógica de cabecera del bucle es una función pura testeada en Vitest, no un `computed` de Vue.</done>
</task>

<task type="auto">
  <name>Tarea 2: Cablear la cabecera del bucle y arreglar el layout de AppHeader</name>
  <files>app/composables/useGameSession.ts, app/components/AppHeader.vue, app/pages/[game]/index.vue</files>
  <read_first>
    - `app/composables/useGameSession.ts` (fichero completo — el patrón «computed que envuelve una función pura del motor» está justo encima, en `currentText` con `resolveText`)
    - `app/components/AppHeader.vue` (fichero completo — las dos `div` de las zonas izquierda y derecha)
    - `app/pages/[game]/index.vue` (fichero completo — el destructuring de `useGameSession()`, la composición de `savedSummary`, y el `:title="sectionLabel"` del `IndexOverlay`)
    - `engine/header.ts` tal como quedó en la tarea 1
    - `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-UI-SPEC.md` §Layout 2 (el bug de layout y su arreglo exacto de dos clases)
  </read_first>
  <action>
    En `app/composables/useGameSession.ts`: importa `describeHeader` desde `~~/engine/header` y sustituye los cuerpos inline de los `computed` `sectionLabel` y `position` por envoltorios finos sobre esa función, replicando el patrón exacto que ya usa `currentText` con `resolveText`. Añade un tercer `computed` exportado, `plainSectionTitle`, alimentado por el mismo `describeHeader` (una sola llamada derivada compartida, no tres llamadas independientes). Los tres devuelven el mismo valor por defecto que hoy cuando no hay sesión (`''`, `null`, `''`). Traslada al nuevo fichero `engine/header.ts` la parte del comentario WR-01 que explica el fallback `?? 'step'`, y deja en el composable una referencia a dónde vive ahora la lógica — el composable sigue siendo la única costura reactiva, y ningún componente importa `~~/engine/*`.

    En `app/components/AppHeader.vue`: añade `min-w-0 flex-1` a la `div` de la zona izquierda (la que hoy tiene `text-label font-bold text-primary-text truncate`) y `shrink-0` a la `div` de la zona derecha (la que hoy tiene `flex items-center gap-md`). Sin esto, `truncate` no recorta: en una fila `flex justify-between`, un hijo sin `min-w-0` no puede encogerse por debajo del ancho de su contenido, así que la etiqueta compuesta más larga empujaría o comprimiría la zona `3 jug · Normal ≡`, que el contrato D-11 prohíbe mover. No cambies props, ni emits, ni la interpolación de `position`: la cadena `RONDA 4 · Villano · 3 de 6` se compone sola con la plantilla actual.

    En `app/pages/[game]/index.vue`: añade `plainSectionTitle` al destructuring de `useGameSession()` y cambia el `IndexOverlay` para que reciba `:title="plainSectionTitle"` en lugar de `:title="sectionLabel"`. La barra de título del índice es un nombre de sección (`PREPARACIÓN`, `RONDA`), no una lectura de posición; dejar `sectionLabel` mostraría `RONDA 4 · Villano` como encabezado del overlay. No toques `savedSummary`: debe seguir componiéndose de `sectionLabel` + `position` + `sessionContextLabel` — es precisamente esa reutilización la que hace que el aviso de partida guardada empiece a decir la ronda (DC-4). No toques el bloque `watchDebounced` ni el comentario NO-OP del final del fichero.
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx nuxt typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describeHeader" app/composables/useGameSession.ts` es al menos 1.
    - `grep -c "plainSectionTitle" app/composables/useGameSession.ts` es al menos 1 y aparece en el objeto de retorno del composable.
    - `grep -c "~~/engine" app/components/AppHeader.vue app/components/IndexOverlay.vue app/components/StepScreen.vue` devuelve 0 en los tres.
    - `grep -q 'min-w-0 flex-1' app/components/AppHeader.vue` y `grep -q 'shrink-0' app/components/AppHeader.vue` salen ambos con código 0.
    - `grep -q ':title="plainSectionTitle"' app/pages/\[game\]/index.vue` sale con código 0; `grep -c ':title="sectionLabel"' app/pages/\[game\]/index.vue` devuelve 0.
    - Comprobación en `npm run dev` con emulación de tablet horizontal: estando en `ronda.villano.03` de la ronda 4, la cabecera muestra literalmente `RONDA 4 · Villano · 3 de 6`; estando en el octavo paso de la preparación muestra `PREPARACIÓN · 8 de 23`.
    - DC-4 verificado a mano: guardar la partida en un paso de la fase del villano en la ronda 3, recargar, y comprobar que el aviso de partida guardada dice `RONDA 3 · Villano · … · N jug · …` y no `RONDA · 29 de 33`.
    - La zona derecha de la cabecera (`3 jug · Normal` + `≡`) ocupa exactamente el mismo ancho en un paso de la preparación y en `RONDA 12 · Jugadores · 4 de 4`.
    - `npx vitest run` en verde y sin errores de tipos.
  </acceptance_criteria>
  <done>La cabecera dice la verdad en ambos tramos, la zona derecha no se mueve, y la reanudación dentro del bucle deja de ser engañosa.</done>
</task>

<task type="auto">
  <name>Tarea 3: Índice del bucle — reordenado, atenuado y ● sin ✓ (D-24/D-25)</name>
  <files>engine/toc.ts, engine/__tests__/toc.test.ts, app/components/IndexOverlay.vue, app/pages/[game]/index.vue</files>
  <read_first>
    - `engine/toc.ts` (fichero completo — el bucle de agrupación por `phaseId` consecutivo y la derivación de marcas)
    - `engine/__tests__/toc.test.ts` (fichero completo, ya actualizado por `02-01` a 9 bloques)
    - `app/components/IndexOverlay.vue` (fichero completo — tipo del prop `blocks`, `numberedBlocks`, la clase condicional de color por `mark`, la hairline `border-b border-background` de la barra de título)
    - `app/pages/[game]/index.vue` (el `computed` `blocks` que llama a `tableOfContents`)
    - `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-UI-SPEC.md` §Layout 3 (maqueta, rótulo divisor, regla de atenuado por token de color y no por opacidad)
  </read_first>
  <behavior>
    - Con el cursor en la preparación (fuera del bucle): la salida de `tableOfContents` es byte a byte la de hoy salvo por el campo nuevo, que vale `dimmed: false` en todos los bloques. Orden natural, sin divisor, sin atenuado. Todos los tests preexistentes siguen pasando.
    - Con el cursor dentro del bucle: los bloques de la sección repetible salen primero en su orden natural (`Jugadores`, `Villano`), y detrás los bloques no repetibles con `dimmed: true`.
    - Con el cursor dentro del bucle: ninguna fila perteneciente a la sección repetible lleva `mark: 'done'`; solo la del cursor lleva `'current'` y el resto `null`, incluidas las de pasos ya recorridos en esta misma ronda.
    - Con el cursor dentro del bucle: las filas de la preparación conservan su marca `'done'` derivada del cursor (están todas por detrás); lo que cambia es su color, no su marca.
    - Pureza conservada: dos llamadas iguales devuelven lo mismo y no mutan la secuencia.
  </behavior>
  <action>
    En `engine/toc.ts`: añade el campo `dimmed: boolean` a `TocBlock` (y a `WorkingBlock`). Calcula al principio de `tableOfContents` un `insideLoop` como `sequence[cursor]?.sectionRepeats === true`. Cambia la regla de marca para que, cuando `node.sectionRepeats` sea `true`, jamás emita `'done'`: solo `'current'` para `index === cursor` y `null` en cualquier otro caso (D-25 — el `✓` significa «hecho y no vuelve», falso dentro de un bucle; la red contra el «¿ya lo hicimos?» es la línea `⚠` de recuento de D-21, no una marca). Tras agrupar, si `insideLoop`, particiona los bloques en repetibles y no repetibles según el `sectionRepeats` de sus nodos y devuelve primero los repetibles y después los no repetibles con `dimmed: true`; si no, devuelve el orden natural con `dimmed: false` en todos.

    La condición de reordenado y atenuado debe leer únicamente `sectionRepeats`. Prohibido referenciar `'ronda'`, `'setup'` o cualquier id literal (TECH-04 y D-24 lo dicen explícitamente): el mismo código debe servir para Warhammer 40.000 sin tocarlo.

    En `engine/__tests__/toc.test.ts`: añade casos para todo lo listado en `<behavior>`, incluidos un caso con el cursor en un paso de la ronda ya «pasado» dentro de la ronda actual que verifica cero `'done'` en toda la sección repetible, y un caso sobre el fixture `tiny-game.json` que comprueba el reordenado con la sección `loop` — otra vez, prueba de que nada está cableado contra el contenido real.

    En `app/components/IndexOverlay.vue`: añade `dimmed: boolean` al tipo del prop `blocks`. Cuando `block.dimmed` sea `true`, fuerza el color de texto de todas sus filas a `text-secondary-text`, sobrescribiendo la rama actual que pinta `'done'` en `text-primary-text`, y aplica el mismo color al glifo `✓` de esas filas (hoy es `text-accent`). El atenuado se implementa cambiando de token de color, **nunca con opacidad**: bajar la opacidad del texto primario degradaría su ratio de contraste en una zona que sigue siendo pulsable, mientras que el token secundario `#9AA0AC` sobre el fondo mantiene 6.9:1 ya verificado. Inserta, justo antes del primer bloque con `dimmed: true`, un divisor: una hairline `border-t border-background` más un rótulo centrado en `text-label font-bold uppercase text-secondary-text` con el texto literal `PREPARACIÓN (CONSULTA)`. Ese divisor solo se renderiza si existe al menos un bloque atenuado. No cambies el comportamiento de `onRowClick`: tocar una fila atenuada debe seguir cerrando el overlay y saltando (FLOW-08); «atenuada» es tratamiento visual, no estado deshabilitado. No cambies `numberedBlocks`: la numeración corrida en el orden en que llegan los bloques ya produce la ronda como 1..10 y la preparación como 11..34, que es la maqueta aprobada.

    En `app/pages/[game]/index.vue`: nada que cambiar en la llamada a `tableOfContents` — el campo `dimmed` viaja dentro de `blocks`. Verifica únicamente que el tipado sigue cuadrando con el prop del componente.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/toc.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "dimmed" engine/toc.ts` es al menos 3; `grep -c "'ronda'\|\"ronda\"\|'setup'\|\"setup\"" engine/toc.ts` devuelve 0.
    - `grep -c "sectionRepeats" engine/toc.ts` es al menos 2.
    - `grep -q "PREPARACIÓN (CONSULTA)" app/components/IndexOverlay.vue` sale con código 0.
    - `grep -c "opacity-" app/components/IndexOverlay.vue` no aumenta respecto a la línea base (el atenuado es por token de color, no por opacidad).
    - `npx vitest run` en verde, incluidos todos los tests de `toc.test.ts` preexistentes sobre el caso fuera del bucle.
    - Comprobación en `npm run dev`: estando en `ronda.villano.03`, abrir el índice muestra la barra de título `RONDA`, después los bloques `JUGADORES` y `VILLANO` con las filas 1..10 y un único `●` en la fila 7, después la hairline con `PREPARACIÓN (CONSULTA)`, y después las filas 11..34 de la preparación en gris con sus `✓` también en gris. Ninguna fila 1..10 lleva `✓`.
    - Comprobación en `npm run dev`: tocar una fila gris de la preparación cierra el índice y salta a ese paso, y la cabecera sigue mostrando la misma ronda que antes del salto (FLOW-08).
    - Comprobación en `npm run dev`: estando en la preparación, abrir el índice se ve exactamente como en la Fase 1 — sin divisor, sin filas grises, orden natural.
    - `git diff --name-only` para esta tarea lista exactamente los cuatro ficheros declarados.
  </acceptance_criteria>
  <done>El índice se comporta como una zona de juego con una zona de consulta detrás, y ninguna marca miente sobre lo que va a volver a pasar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motor puro (`engine/`) → capa reactiva (`useGameSession.ts`) | Única costura; ningún componente importa `~~/engine/*` |
| `localStorage` → `engine/persistence.ts` | Dato bajo control del usuario/extensiones, ya validado en forma completa desde la Fase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Etiquetas de cabecera e índice pintadas desde datos autorados | mitigate | Interpolación `{{ }}` exclusivamente en `AppHeader.vue` e `IndexOverlay.vue`; prohibida la directiva de HTML crudo (T-01-01). Ninguna de las tres tareas introduce una plantilla nueva de renderizado |
| T-02-03 | Tampering | `zod` cruzando a `app/` | mitigate | `engine/header.ts` no importa `zod`; el composable solo importa `describeHeader`. Criterio de aceptación que verifica cero imports de Vue/Nuxt/DOM en el fichero nuevo |
| T-02-05 | Tampering | `localStorage` manipulado con un `round` arbitrario | accept | `round` solo alimenta una cadena de cabecera pintada como texto; un valor absurdo produce una etiqueta rara, nunca un acceso fuera de rango: `position` se deriva de `cursor`/`phaseId`, no de `round`. `isPersistedPosition` ya filtra formas inválidas desde la Fase 1 |
| T-02-06 | Denial of service | Etiqueta compuesta desbordando la cabecera y desplazando controles | mitigate | Arreglo de layout `min-w-0 flex-1` / `shrink-0` (tarea 2): la zona izquierda recorta con elipsis y la derecha nunca se encoge |
| T-02-SC | Tampering | Instalaciones de npm/pip/cargo | accept | Cero paquetes nuevos en esta fase; no hay superficie de cadena de suministro que auditar |
</threat_model>

<verification>
- `npx vitest run` en verde con los tests nuevos de `header.test.ts` y `toc.test.ts`.
- `npx nuxt typecheck` sin errores.
- Recorrido manual en emulación de tablet horizontal: preparación → mesa lista → ronda 1 completa → ronda 2, comprobando cabecera e índice en ambos tramos y el salto de vuelta desde la preparación.
</verification>

<success_criteria>
- `RONDA 4 · Villano · 3 de 6` en la cabecera dentro del bucle; `PREPARACIÓN · 8 de 23` sin cambios fuera de él.
- Índice reordenado con la preparación al final, atenuada, separada por `PREPARACIÓN (CONSULTA)` y todavía pulsable.
- Cero `✓` dentro del bucle; solo `●` en el paso actual.
- Título del overlay siempre el nombre plano de la sección.
- El aviso de partida guardada menciona la ronda y la fase (DC-4).
- Nada cableado contra los ids `ronda`/`setup` en `engine/` (TECH-04).
</success_criteria>

<output>
Crea `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-02-SUMMARY.md` al terminar.
</output>
