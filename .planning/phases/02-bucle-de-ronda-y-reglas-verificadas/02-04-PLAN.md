---
phase: 02-bucle-de-ronda-y-reglas-verificadas
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - .planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md
  - content/marvel-champions.json
  - engine/__tests__/content.test.ts
autonomous: false
requirements: [CONT-02, CONT-03, CONT-04, CONT-05, CONT-06, CONT-07, CONT-09, ADAPT-04]

must_haves:
  truths:
    - "Cada paso de la ronda tiene su cita comprobada contra la página real del Rules Reference v1.7, no contra el borrador"
    - "El usuario ha revisado a mano todo el contenido de la ronda contra el reglamento antes de darlo por definitivo"
    - "El usuario ha jugado una ronda completa de principio a fin en la app y confirma que el flujo es correcto"
    - "Las correcciones que salgan de esa revisión están aplicadas y bajo gate automático"
  artifacts:
    - path: ".planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md"
      provides: "Dossier de revisión: cada paso de la ronda con su texto, aviso, detalle y la cita contrastada"
      min_lines: 40
  key_links:
    - from: ".planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md"
      to: "content/marvel-champions.json"
      via: "una fila por id de paso, con el id literal como clave"
      pattern: "ronda\\.(jugadores|villano)\\.\\d{2}"
---

<objective>
Cerrar CONT-09: dar el contenido de la ronda por definitivo solo después de que el usuario lo haya contrastado a mano contra el Rules Reference v1.7 y haya jugado una ronda completa en la app.

Purpose: en la Fase 1, los tres errores de fidelidad reales —una regla fantasma sin respaldo, un paso que se comía la multiplicación por jugadores y una reescritura necesaria de «etapas de villano»— **no los cazó ninguna verificación automática**. Los cazó la revisión humana del usuario. El gate de esquema detecta contenido *malformado*, no contenido *incorrecto*. D-36 exige por eso una tarea explícita de revisión humana en el plan, no un paso implícito.
Output: dossier de revisión, veredicto del usuario y correcciones aplicadas con su gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTEXT.md
@.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-RESEARCH.md
@content/marvel-champions.json
@engine/__tests__/content.test.ts
</context>

<interfaces>
Fuentes de verdad locales, confirmadas presentes durante la investigación de esta fase:
- `~/Downloads/mc_rulesreference_v17-compressed.pdf` — Rules Reference v1.7, en inglés, 3,6 MB. **Manda en caso de conflicto.**
- `~/Downloads/Marvel-Champions_aprende_a_jugar.pdf` — «Aprende a jugar», 32 MB, solo para orden narrativo.
- `pdftotext` disponible en `/opt/homebrew/bin/pdftotext`; `pdftotext -layout` sobre el Rules Reference está validado. Los números de página son los impresos en el pie del documento, no el índice del visor.

Páginas relevantes ya localizadas por la investigación: Round Overview p.4 · Boost cards p.8 · End of Player Phase p.16 · Modes of Play (Experto vs. Heroico) p.28 · Player Deck p.32 · Villain Defeat p.44 · Villain Phase p.45 · Encounter Deck (entrada de glosario) · Stunned / Confused / Tough.

Contenido a revisar: la sección `ronda` de `content/marvel-champions.json` — 2 fases, 10 pasos `kind:'step'`, 8 con `warning`, 5 con `warningDetail`, todos con `speech` y `citation`.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Re-verificación automática y dossier de revisión</name>
  <files>.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md, content/marvel-champions.json</files>
  <read_first>
    - `content/marvel-champions.json`, sección `ronda` completa (los 10 pasos con todos sus campos)
    - `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-RESEARCH.md` §«Rules Reference quotations for content authoring» y §«State of the Art» (las cuatro correcciones confirmadas con su página)
    - `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTEXT.md` §D-31 (el principio que decide qué se escribe y qué no) y §canonical_refs
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-06-SUMMARY.md` (qué forma tuvieron los tres errores que la revisión humana cazó en la Fase 1 — sirve de guion de qué buscar)
  </read_first>
  <action>
    Extrae del Rules Reference las páginas relevantes con `pdftotext -layout` acotado por rangos (`-f`/`-l`) sobre `~/Downloads/mc_rulesreference_v17-compressed.pdf`, volcando la salida a un fichero temporal fuera del repo. Para cada uno de los 10 pasos de la sección `ronda`, contrasta tres cosas contra el texto extraído: que la página citada contiene efectivamente la regla que el paso enuncia; que el paso no enuncia ninguna regla que no aparezca en el reglamento (la «regla fantasma» fue uno de los tres errores de la Fase 1); y que el paso no omite una condición que el reglamento sí impone.

    Escribe `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md` como dossier pensado para que una persona lo revise deprisa junto al PDF: una tabla con una fila por paso, en orden de secuencia, con las columnas id, `title`, `text`, `warning`, `warningDetail`, `speech`, `citation.section`, `citation.page` y un veredicto automático por paso (contrastado / discrepancia / no localizable). Debajo, una sección por cada una de las cuatro correcciones confirmadas del borrador, indicando en qué paso concreto vive ahora la corrección y con qué campo se cumple: 6 pasos oficiales de la fase del villano y no 4; obligaciones «una o más por identidad» y no una por jugador; solo el villano y los esbirros con la palabra clave Villano roban carta de aumento; y el Modo Experto no altera la estructura de la fase del villano —eso es el Modo Heroico, un eje aparte y combinable, hoy en v2 como CONF-02—. Cierra el dossier con una lista de preguntas abiertas explícitas para el usuario, si las hay: cualquier punto donde el reglamento admita más de una lectura, o donde D-31 haga dudar entre recordar mirar y enunciar la consecuencia.

    Si encuentras una discrepancia clara y objetiva (una cita a una página equivocada, una regla enunciada que el reglamento no dice, una condición omitida), **corrígela en `content/marvel-champions.json` en esta misma tarea** y déjala anotada en el dossier como corrección ya aplicada. Si la discrepancia es de criterio y no de hecho, no la corrijas: llévala a la lista de preguntas abiertas para que la decida el usuario en la tarea 2.

    No reproduzcas en el dossier párrafos largos del Rules Reference: cita página y epígrafe y resume en una línea. El dossier vive en el repo y le aplica la misma restricción legal que a la app.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md &amp;&amp; grep -c "ronda\." .planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - El dossier existe y menciona los 10 ids de paso de la sección `ronda`, cada uno al menos una vez.
    - El dossier contiene una sección dedicada a cada una de las cuatro correcciones confirmadas, cada una apuntando al id de paso y al campo concreto donde vive.
    - Cada fila del dossier lleva un veredicto explícito; no queda ninguna celda de veredicto vacía.
    - Si el dossier declara alguna corrección como «ya aplicada», `git diff content/marvel-champions.json` muestra ese cambio y `npx vitest run` sigue en verde.
    - El dossier no contiene ningún bloque de más de dos líneas transcrito del Rules Reference.
    - `npx vitest run` en verde al terminar la tarea.
  </acceptance_criteria>
  <done>Existe un documento que permite revisar los 10 pasos contra el reglamento en una sentada, y las discrepancias objetivas ya están corregidas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 2: Revisión humana del contenido de la ronda y partida completa (D-36)</name>
  <files>content/marvel-champions.json (solo lectura durante la revisión), .planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md (solo lectura durante la revisión)</files>
  <read_first>
    - `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md` (el dossier producido por la tarea 1)
    - `content/marvel-champions.json`, sección `ronda`
    - `~/Downloads/mc_rulesreference_v17-compressed.pdf` (Rules Reference v1.7 — fuente de verdad)
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-06-SUMMARY.md` (los tres errores que la revisión humana cazó en la Fase 1)
  </read_first>
  <action>
    PAUSA OBLIGATORIA. No continúes al siguiente paso hasta obtener el veredicto del usuario. Esta tarea no la ejecuta el agente: la ejecuta la persona, y es el gate final de CONT-09 por decisión explícita D-36.

    Antes de presentar el checkpoint, deja el entorno listo para la revisión: arranca `npm run dev` y comprueba que la app carga, e indica al usuario la URL local y la ruta absoluta del dossier `02-CONTENT-REVIEW.md`. Después presenta el guion de `&lt;how-to-verify&gt;` tal cual, sin resumirlo ni reordenarlo, y espera.

    Cuando llegue la respuesta: si es «aprobado», anótalo literal y pasa a la tarea 3. Si son correcciones, transcríbelas literalmente, sin reinterpretarlas ni «mejorarlas», asociando cada una al id de paso al que afecta, y pasa a la tarea 3 con esa lista. Si alguna corrección es ambigua sobre qué dice el reglamento, pregunta antes de aplicarla en vez de decidir por tu cuenta: el criterio de fidelidad de reglas es del usuario, no del ejecutor.
  </action>
  <what-built>
    - La sección `ronda` de `content/marvel-champions.json`: 2 fases (Jugadores, 4 pasos; Villano, 6 pasos), 8 avisos, 5 consecuencias detalladas, los 10 pasos citados contra el Rules Reference v1.7 y con frase locutable.
    - El bucle funcionando de verdad: avanzar desde el último paso vuelve al primero de la fase de jugadores y sube la ronda; retroceder desde el primero baja la ronda; saltar no toca el contador.
    - Cabecera relativa a la fase dentro del bucle (`RONDA 4 · Villano · 3 de 6`) e índice reordenado con la preparación al final, atenuada y todavía pulsable.
    - Aviso `⚠` clicable con modal de consecuencia en los 5 pasos que la tienen, y sin afordancia en los que no.
    - Dossier de revisión en `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md` con la re-verificación automática ya hecha.
  </what-built>
  <how-to-verify>
    Esta revisión es obligatoria y no es un trámite: en la Fase 1, los tres errores de fidelidad reales los cazaste tú a mano, no la verificación automática.

    **Parte A — Revisión del contenido contra el reglamento (con el PDF delante).**
    1. Abre `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md` junto a `~/Downloads/mc_rulesreference_v17-compressed.pdf`.
    2. Recorre la tabla paso a paso y contrasta cada fila con la página citada. Busca en concreto los tres patrones de error de la Fase 1: una regla enunciada que el reglamento no dice en ninguna parte; un paso que se come una condición del reglamento; y una redacción que induce a hacer algo distinto de lo que la regla pide.
    3. Comprueba las cuatro correcciones confirmadas del borrador, una por una: la fase del villano tiene 6 pasos y en el orden del reglamento; en ninguna parte se dice «una obligación por jugador»; el detalle de «Los enemigos activan» dice que solo el villano y los esbirros con la palabra clave Villano roban carta de aumento; y ningún paso de la ronda cambia por elegir Experto.
    4. Responde a las preguntas abiertas del final del dossier, si las hay.

    **Parte B — Partida en la app (emulación de tablet horizontal, `npm run dev`).**
    5. Empieza una partida nueva, recorre la preparación hasta «Mesa lista» y pulsa EMPEZAR A JUGAR: debes entrar en `Jugad vuestros turnos…`, no quedarte clavado.
    6. Recorre la ronda 1 entera, los 10 pasos, leyendo cada texto como si lo estuvieras aplicando en la mesa. Juzga con el criterio de CONT-11: ¿cada paso es una acción física inequívoca, ni tan fina que sea un festival de toques ni tan gruesa que entierre el detalle olvidable?
    7. En el último paso pulsa SIGUIENTE: debes volver al primer paso de la fase de jugadores y la cabecera debe decir `RONDA 2 · Jugadores · 1 de 4`.
    8. Pulsa Atrás desde ahí: debes volver al último paso del villano con `RONDA 1`.
    9. Abre el índice (`≡`) estando en la fase del villano: los bloques de la ronda deben ir primero, con un solo `●` y ningún `✓`; la preparación debe ir al final, en gris, tras el rótulo `PREPARACIÓN (CONSULTA)`. Toca una fila gris de la preparación: debe saltar ahí sin cambiar el número de ronda.
    10. Vuelve al bucle desde el índice y toca los 5 avisos con consecuencia (`Atentos al dial del villano`, `Mazo agotado…`, `Terminan aquí los efectos…`, `Atentos a los Estados…`, `Encuentros agotado…`): cada uno abre su modal; ciérralo con `Entendido`, tocando fuera y con Escape. Comprueba que sigues en el mismo paso y la misma ronda.
    11. Comprueba que los avisos SIN consecuencia (`Sumad los iconos…`, `Una a cada jugador…`, `Resolved los «cuando termine…»`) no tienen borde, ni chevron, ni responden al toque.
    12. Recarga la página en mitad de la ronda 3: el aviso de partida guardada debe decir en qué ronda y fase te quedaste, no solo un número de paso.

    **Qué responder:** «aprobado» si el contenido es fiel y el flujo es correcto. Si no, enumera cada corrección con el id del paso al que afecta y qué dice el reglamento. Si dudas de la granularidad de algún paso (D-04/CONT-11), dilo también: es el criterio con el que se revisa, y partir o fusionar un paso es una corrección legítima.
  </how-to-verify>
  <resume-signal>Escribe «aprobado», o enumera las correcciones indicando el id del paso afectado</resume-signal>
  <verify>
    <automated>MISSING — verificación humana por diseño (D-36). El gate automático equivalente ya corrió en la tarea 1 (`npx vitest run` + dossier) y volverá a correr en la tarea 3; lo que esta tarea comprueba —fidelidad de reglas y granularidad en mesa— no es automatizable: en la Fase 1 los tres errores reales los cazó una persona, no el esquema.</automated>
    <human-check>El usuario responde «aprobado» o enumera correcciones con el id del paso afectado.</human-check>
  </verify>
  <acceptance_criteria>
    - Existe un veredicto explícito del usuario, en sus propias palabras, registrado antes de empezar la tarea 3.
    - El veredicto cubre las dos partes del guion: revisión del contenido contra el reglamento (parte A) y partida jugada en la app (parte B).
    - Si el veredicto contiene correcciones, cada una queda asociada a un id de paso concreto de la sección `ronda` antes de aplicarse.
    - Ninguna corrección se aplica durante esta tarea: esta tarea solo recoge el veredicto.
  </acceptance_criteria>
  <done>El usuario ha dado un veredicto explícito sobre la fidelidad del contenido de la ronda y sobre el flujo jugado en la app.</done>
</task>

<task type="auto">
  <name>Tarea 3: Aplicar las correcciones y blindarlas con gates</name>
  <files>content/marvel-champions.json, engine/__tests__/content.test.ts, .planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md</files>
  <read_first>
    - El veredicto literal del usuario en la tarea 2
    - `content/marvel-champions.json`, sección `ronda`
    - `engine/__tests__/content.test.ts` (los gates cableados por id añadidos en `02-01` y `02-03`)
    - `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTENT-REVIEW.md`
  </read_first>
  <action>
    Aplica en `content/marvel-champions.json` cada corrección que el usuario haya enumerado, respetando todos los presupuestos y convenciones vigentes: `text` ≤90, `warning` ≤60, `warningDetail` ≤320, `speech` ≤120 sin glifos, imperativo plural, sin mencionar el recuento real de jugadores, sin `variants.difficulty` en la ronda, títulos de fase en Title Case, sin el glifo `⚠` autorado, y cita obligatoria en todo paso `kind:'step'`.

    Si alguna corrección cambia el número de pasos, sus ids o el reparto de avisos, actualiza en el mismo cambio los recuentos y listas cableados de `engine/__tests__/content.test.ts` y `engine/__tests__/toc.test.ts` — es la convención de «gate que muerde»: el test se actualiza a conciencia, nunca se relaja para que deje de molestar. Si una corrección afecta a la estructura del bucle (número de pasos de una fase), revisa también las aserciones de `engine/__tests__/navigator.test.ts` y `engine/__tests__/header.test.ts` que dependan de índices o denominadores concretos.

    Por cada corrección de fidelidad aplicada, añade además una aserción nueva y cableada por id en `engine/__tests__/content.test.ts` que impida que el error vuelva: el mismo mecanismo con el que la Fase 1 blindó «una o más» frente a «una por jugador». Un error de reglas cazado por una persona debe quedar convertido en un test.

    Sube `contentVersion` en una unidad si el contenido cambia (PERS-03). No lo subas si el usuario aprobó sin correcciones.

    Actualiza el dossier `02-CONTENT-REVIEW.md` con una sección final que registre el veredicto del usuario, la lista de correcciones aplicadas y la fecha. Ese documento es la evidencia de que CONT-09 se cumplió con revisión humana, no solo con validación automática.

    Si el usuario aprobó sin correcciones, esta tarea se limita a registrar el veredicto en el dossier y a confirmar que la suite sigue verde.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Toda corrección enumerada por el usuario en la tarea 2 tiene un cambio correspondiente en `content/marvel-champions.json`, identificable por el id del paso.
    - Toda corrección de fidelidad aplicada tiene al menos una aserción nueva cableada por id en `engine/__tests__/content.test.ts`.
    - `npx vitest run` en verde, con un número de tests mayor o igual al del plan `02-03`.
    - `node -e "const g=require('./content/marvel-champions.json');const s=g.sections.flatMap(x=>x.phases).flatMap(p=>p.steps);for(const x of s){if(x.text.length>90)throw new Error('text '+x.id);if(x.warning&&x.warning.length>60)throw new Error('warning '+x.id);if(x.warningDetail&&x.warningDetail.length>320)throw new Error('detail '+x.id);if(x.speech&&x.speech.length>120)throw new Error('speech '+x.id)}console.log('ok')"` imprime `ok`.
    - `02-CONTENT-REVIEW.md` contiene una sección final con el veredicto literal del usuario y la lista de correcciones aplicadas.
    - Si hubo cambios de contenido, `contentVersion` es exactamente uno mayor que al empezar esta tarea.
  </acceptance_criteria>
  <done>El contenido de la ronda es definitivo, revisado por una persona contra el reglamento, y cada error cazado tiene su test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF del reglamento (local, solo lectura) → dossier en el repo | Se extrae texto para verificar; no se transcribe al producto |
| dossier revisado → contenido definitivo | El veredicto humano es el gate final de CONT-09 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information disclosure | Transcripción de texto con copyright al dossier o al contenido | mitigate | El dossier cita página y epígrafe y resume en una línea; criterio de aceptación que prohíbe bloques de más de dos líneas transcritos. La app mantiene redacción propia (D-31/CONT-10) |
| T-02-09 | Repudiation | «Contenido verificado» sin evidencia de quién lo verificó ni contra qué | mitigate | El dossier registra el veredicto literal del usuario, la fecha y la lista de correcciones; queda versionado en el repo junto al contenido que avala |
| T-02-10 | Tampering | Un error de reglas cazado a mano volviendo a colarse más adelante | mitigate | Cada corrección de fidelidad se convierte en una aserción cableada por id en `content.test.ts`, igual que se hizo en la Fase 1 con «una o más por identidad» |
| T-02-02 | Tampering | Corrección que rompe un presupuesto de texto o deja un paso sin cita | mitigate | Gates de esquema vigentes (`.max()` por campo, `citation` obligatoria en `kind:'step'`) más el comando `node -e` de comprobación de presupuestos en los criterios de aceptación |
| T-02-SC | Tampering | Instalaciones de npm/pip/cargo | accept | Cero paquetes nuevos; `pdftotext` es una herramienta local de verificación, no una dependencia de build ni de runtime |
</threat_model>

<verification>
- `npx vitest run` en verde.
- Veredicto explícito del usuario registrado en `02-CONTENT-REVIEW.md`.
- Los 12 requisitos de la fase quedan en estado verificable para el cierre de fase.
</verification>

<success_criteria>
- El contenido de la ronda está contrastado contra el Rules Reference v1.7 por una persona, con evidencia versionada.
- Las cuatro correcciones confirmadas del borrador están presentes, localizadas por id de paso y bajo gate automático.
- Una ronda completa se ha jugado de principio a fin en la app, con bucle, cabecera, índice y modal comprobados en mesa.
- Toda corrección salida de la revisión tiene su test.
</success_criteria>

<output>
Crea `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-04-SUMMARY.md` al terminar.
</output>
