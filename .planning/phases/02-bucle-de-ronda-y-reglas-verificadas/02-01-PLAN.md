---
phase: 02-bucle-de-ronda-y-reglas-verificadas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/marvel-champions.json
  - engine/schema.ts
  - engine/__tests__/schema.test.ts
  - engine/__tests__/navigator.test.ts
  - engine/__tests__/content.test.ts
  - engine/__tests__/toc.test.ts
autonomous: true
requirements: [FLOW-03, FLOW-04, FLOW-07, FLOW-08, CONT-02, CONT-03, CONT-04, CONT-05, CONT-06, CONT-07, CONT-09, ADAPT-04]

must_haves:
  truths:
    - "Desde «Mesa lista», pulsar EMPEZAR A JUGAR lleva al primer paso de la fase de los jugadores (hoy no lleva a ningún sitio)"
    - "El usuario recorre 4 pasos de la fase de los jugadores en el orden descartar → robar → enderezar"
    - "El usuario recorre 6 pasos de la fase del villano, en el orden oficial del Rules Reference v1.7"
    - "Al avanzar desde el último paso de la fase del villano, el usuario vuelve al primer paso de la fase de los jugadores y el contador de ronda sube en uno"
    - "Al retroceder desde el primer paso de la fase de los jugadores estando en la ronda 2 o posterior, el usuario vuelve al último paso de la fase del villano y el contador de ronda baja en uno"
    - "Saltar a un paso del bucle no toca el contador de ronda, y seguir avanzando desde ahí continúa por el bucle"
    - "Saltar a un paso de la preparación no toca el contador de ronda"
    - "Un fichero de contenido sin ninguna sección repeats:true hace fallar la validación en CI"
  artifacts:
    - path: "content/marvel-champions.json"
      provides: "Sección ronda (repeats:true) con 2 fases y 10 pasos citados"
      contains: "\"id\": \"ronda\""
    - path: "engine/schema.ts"
      provides: "Gate D-37: exactamente una sección repeats:true"
      contains: "repeating.length !== 1"
    - path: "engine/__tests__/navigator.test.ts"
      provides: "Cobertura de FLOW-03/04/07/08 contra el contenido real"
      contains: "marvel-champions.json"
  key_links:
    - from: "content/marvel-champions.json"
      to: "engine/expand.ts"
      via: "repeats:true en la sección ronda deriva loopStartIndex/loopEndIndex"
      pattern: "\"repeats\": true"
    - from: "engine/schema.ts"
      to: "engine/__tests__/content.test.ts"
      via: "validateGameDefinition(rawMarvelChampions) en el gate de CI"
      pattern: "validateGameDefinition"
---

## Phase Goal

**Nota de formato (a resolver fuera de este plan):** el `**Goal:**` de la Fase 2 en `ROADMAP.md` **no** está redactado en formato de historia de usuario (`As a … I want to … so that …`), igual que ocurrió en la Fase 1. No se inventa aquí una historia sustitutiva. Se reproduce el objetivo literal del roadmap y se deja constancia de la discrepancia para que el desarrollador decida si ejecutar `/gsd mvp-phase 2`.

> Un grupo puede jugar una partida completa de Marvel Champions de principio a fin: tras la preparación, la fase de los jugadores, la fase del villano (con sus 6 pasos oficiales) y el fin de ronda se encadenan en un bucle que vuelve solo al punto correcto, ronda tras ronda, con el contenido corregido y verificado contra los errores ya detectados en el borrador.

<objective>
Encender por primera vez la maquinaria de bucle que la Fase 1 dejó construida pero nunca ejecutada con datos reales, autorando la sección `ronda` del contenido de Marvel Champions (2 fases, 10 pasos, todos citados contra el Rules Reference v1.7) y endureciendo a la vez el gate de contenido en CI.

Este plan es la rodaja vertical completa: contenido → motor → pantalla. Al terminarlo, el grupo puede jugar ronda tras ronda de verdad en la tablet. La cabecera todavía mostrará el contador global de la preparación (`RONDA · 25 de 33`) en vez del relativo a la fase (`RONDA 4 · Villano · 3 de 6`) — eso es el plan `02-02`, y no impide jugar.

Purpose: FLOW-03/04/07/08 no son motor nuevo — `engine/navigator.ts` y `engine/expand.ts` ya son correctos y no se tocan. Lo que falta es el dato que los activa y las reglas correctas dentro de él.
Output: sección `ronda` en el JSON de contenido, gate D-37 endurecido, y cobertura automática del cierre de bucle contra el contenido real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTEXT.md
@.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-RESEARCH.md
@.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-PATTERNS.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-CONTEXT.md
@content/marvel-champions.json
@engine/schema.ts
@engine/expand.ts
@engine/navigator.ts
</context>

<interfaces>
Contratos ya existentes que el ejecutor NO debe redescubrir explorando el repo.

De `engine/types.ts`:
- `SectionDefinition { id, title, repeats: boolean, phases: PhaseDefinition[] }`
- `PhaseDefinition { id, title, summaryLabel?, steps: StepDefinition[] }`
- `StepDefinition extends TextBlock { id, title, kind: 'step'|'summary', variants?, citation? }`
- `TextBlock { text, warning?, speech? }`
- `Citation { source: 'rules-reference'|'learn-to-play', section: string, page?: number }`
- `EngineSession { gameId, contentVersion, sequence, cursor, round, context, loopStartIndex?, loopEndIndex? }`

De `engine/expand.ts` (NO TOCAR): deriva `loopStartIndex`/`loopEndIndex` del primer y último nodo de la única sección con `repeats:true`.

De `engine/navigator.ts` (NO TOCAR):
- `next(session)`: si `cursor === loopEndIndex` devuelve `{ cursor: loopStartIndex, round: round + 1 }`; si no, `cursor + 1` clampado.
- `prev(session)`: si `cursor === loopStartIndex` y `round > 1` devuelve `{ cursor: loopEndIndex, round: round - 1 }`; si no, `cursor - 1` clampado.
- `jumpTo(session, runtimeId)`: cambia `cursor`, nunca `round`; `runtimeId` desconocido devuelve la misma sesión.

Estado actual del contenido (`content/marvel-champions.json`): `contentVersion: 6`, 1 sección (`setup`, `repeats:false`), 7 fases, 24 pasos (23 `kind:'step'` + 1 `kind:'summary'`), 3 con `warning`, 2 con `variants.difficulty`, 0 con `speech`.

Índices resultantes tras este plan: `sequence.length === 34`, `loopStartIndex === 24`, `loopEndIndex === 33`.
</interfaces>

<decisions_closed_here>
Decisiones que `02-CONTEXT.md` delegó explícitamente al planner y que quedan CERRADAS aquí. No se re-litigan durante la ejecución.

**DC-1 — Campo `speech`: se autora AHORA, como política de fase, para todo el contenido de la ronda.**
Los 10 pasos `kind:'step'` de la sección `ronda` declaran `speech` no vacío. Razón: `ROADMAP.md` afirma que retro-adaptarlo es caro y el esquema lo lleva desde la Fase 1; escribir la frase en el mismo momento en que se redacta el paso cuesta casi nada, mientras que hacerlo en la Fase 3 significa 34 frases de golpe y a ciegas. La política es «todos, no solo los difíciles» porque «solo donde haga falta» es un juicio que un ejecutor puede resolver como «nunca».
Alcance acotado a conciencia: **los 23 pasos de la preparación siguen SIN `speech`** — retrofit de la preparación es trabajo de la Fase 3 (VOZ-01) y no entra aquí.
Contrato de redacción de `speech`: una sola frase, ≤120 caracteres, sin los glifos `⚠`, `×` ni `›`, y sin fórmulas escritas con símbolos (si el `text` muestra una fórmula, el `speech` la enuncia en palabras). Tope aplicado en `engine/schema.ts` con `.max(120)`.

**DC-2 — Prohibido tocar `engine/expand.ts` y `engine/navigator.ts`.**
`PITFALLS.md:63` recomienda un paso por jugador; **D-20 lo descarta a conciencia**. No se añade `perPlayer`, no se expande por jugador, el nº de pasos del bucle no depende del nº de jugadores. `02-PATTERNS.md` confirma que ambos ficheros están correctos y no necesitan cambio: no programar trabajo muerto sobre ellos.

**DC-3 — La sección `ronda` tiene exactamente dos fases (D-34).** No existe una tercera fase «Fin de ronda». CONT-04 (ficha de jugador inicial + cierre de ronda) se satisface dentro de `ronda.villano`, pasos 5 y 6.
</decisions_closed_here>

<content_spec>
Especificación literal del contenido a autorar. Los textos de abajo son el objetivo; el autor puede afinar la redacción **siempre que** siga cumpliendo los presupuestos (`text` ≤90, `warning` ≤60, `speech` ≤120), el registro imperativo-plural, y todas las aserciones de la tarea 3.

Sección: `id: "ronda"`, `title: "Ronda"`, `repeats: true`.

Fase 1: `id: "ronda.jugadores"`, `title: "Jugadores"` (**Title Case obligatorio**, no `"JUGADORES"` — ver aviso de convención abajo), `summaryLabel: "Fase de los jugadores completa"`.

| id | title | text | warning | speech | citation.section | page |
|----|-------|------|---------|--------|------------------|------|
| `ronda.jugadores.01` | Turnos de los jugadores | Jugad vuestros turnos en orden de jugador, uno tras otro. | Atentos al dial del villano | Jugad vuestros turnos en orden de jugador. | Player Phase / Player Turn; ver también Villain Defeat (p. 44) | 32 |
| `ronda.jugadores.02` | Descartar hasta el tamaño de mano | En orden de jugador, descartad hasta bajar al tamaño de vuestra mano. | — | En orden de jugador, descartad hasta bajar al tamaño de mano. | End of Player Phase, paso 1 | 16 |
| `ronda.jugadores.03` | Robar hasta el tamaño de mano | Robad a la vez hasta completar el tamaño de vuestra mano. | Mazo agotado: barajad el descarte y repartíos un encuentro | Robad a la vez hasta completar el tamaño de vuestra mano. | End of Player Phase, paso 2; ver también Player Deck, mazo agotado (p. 32) | 16 |
| `ronda.jugadores.04` | Enderezar | Enderezad a la vez todas vuestras cartas, incluidas las de encuentro agotadas. | Terminan aquí los efectos «hasta el final de la fase» | Enderezad todas vuestras cartas, incluidas las de encuentro agotadas. | End of Player Phase, pasos 3, 4 y 5 | 16 |

Fase 2: `id: "ronda.villano"`, `title: "Villano"` (**Title Case obligatorio**), `summaryLabel: "Fase del villano completa"`.

| id | title | text | warning | speech | citation.section | page |
|----|-------|------|---------|--------|------------------|------|
| `ronda.villano.01` | Colocar amenaza | Colocad amenaza en el esquema principal según su campo de aceleración. | Sumad los iconos y fichas de aceleración en juego | Colocad amenaza en el esquema principal según su campo de aceleración. | Villain Phase, paso 1 | 45 |
| `ronda.villano.02` | Los enemigos activan | El villano ataca a cada héroe y avanza el esquema contra quien esté en Alter-Ego. | Atentos a los Estados en los personajes | El villano ataca a cada héroe y avanza el esquema contra quien esté en Alter-Ego. | Villain Phase, paso 2; ver también Boost Cards (p. 8) y Stunned/Confused/Tough | 45 |
| `ronda.villano.03` | Repartir cartas de encuentro | Repartid una carta de encuentro a cada jugador, en orden de jugador. | Una a cada jugador, más una por cada icono de peligro | Repartid una carta de encuentro a cada jugador, en orden de jugador. | Villain Phase, paso 3 | 45 |
| `ronda.villano.04` | Revelar cartas de encuentro | Revelad las cartas de encuentro una a una, empezando por el jugador inicial. | Encuentros agotado: barajad y poned ficha de aceleración | Revelad las cartas de encuentro una a una, empezando por el jugador inicial. | Villain Phase, paso 4; ver también Encounter Deck, mazo agotado | 45 |
| `ronda.villano.05` | Pasar la ficha de jugador inicial | Pasad la ficha de jugador inicial al jugador de vuestra izquierda. | — | Pasad la ficha de jugador inicial al jugador de vuestra izquierda. | Villain Phase, paso 5 | 45 |
| `ronda.villano.06` | Fin de la fase del villano y de la ronda | Cerrad la fase y la ronda antes de empezar la siguiente. | Resolved los «cuando termine la fase o la ronda» | Cerrad la fase y la ronda antes de empezar la siguiente. | Villain Phase, paso 6; ver también Round Overview (p. 4) | 45 |

**Todos los `citation.source` son `"rules-reference"`.** Todos los pasos son `kind: "step"` (ninguno `summary`).

**Convención de mayúsculas (Pitfall 5, cerrada en `02-UI-SPEC.md` §Layout 2):** las fases de la preparación están autoradas en MAYÚSCULAS (`"HÉROES"`, `"ARCHIENEMIGOS"`) porque la cabecera actual no las usa. Las fases de la ronda **deben** ir en Title Case (`"Jugadores"`, `"Villano"`) porque `engine/header.ts` (plan `02-02`) las inyectará **sin** `.toUpperCase()` para componer `RONDA 4 · Villano`. El overlay del índice sí las pinta en mayúsculas por CSS (`uppercase` en su `<h2>`), así que no hay inconsistencia visual allí.

**Sin `variants.difficulty` en ningún paso de la ronda** — es el cuarto error confirmado del borrador: el Modo Experto **no** altera la estructura ni las cantidades de la fase del villano (eso es el Modo Heroico, un eje aparte y combinable, hoy en v2 como CONF-02).

**Los avisos NO llevan el glifo `⚠` autorado.** `StepScreen.vue` ya lo antepone en plantilla (`⚠ {{ warningText }}`). Autorar el glifo lo duplicaría en pantalla.

**Anclas de las reglas condicionales (D-29/D-30), ya reflejadas arriba:** cambio de fase del villano → `ronda.jugadores.01`; agotamiento del mazo de jugador → `ronda.jugadores.03`; Estados Aturdido/Confundido/Duro → `ronda.villano.02`; agotamiento del mazo de encuentros → `ronda.villano.04`.

**D-28 — los pasos 4 y 5 del fin de fase del reglamento NO son pasos.** Los efectos que terminan «hasta el final de la fase» y los que se resuelven «cuando/después de que termine la fase» viajan como línea `⚠` del paso `ronda.jugadores.04` (enderezar). Por eso la fase de los jugadores son 4 pasos y no 6, y por eso ese aviso existe: no es decorativo, es la única presencia de dos pasos oficiales del reglamento. Contrasta a conciencia con D-35, que sí convierte el paso 6 de la fase del villano en un paso real, porque ahí el criterio de éxito exige los 6 pasos oficiales y ese paso es la frontera visible del bucle.

**D-21 — la red contra olvidar a un jugador es la línea `⚠` de recuento**, no un paso por jugador. Vive en `ronda.villano.03` («Una a cada jugador, más una por cada icono de peligro»). Es el mecanismo que ya existía desde D-05: sin esquema nuevo y sin interfaz nueva.

**La consecuencia detallada de cada aviso (`warningDetail`) NO se autora en este plan** — es el plan `02-03`, junto con el campo de esquema que la valida. Incluido el tercer error confirmado del borrador (solo el villano y los esbirros con la palabra clave Villano roban carta de aumento), que vivirá en el `warningDetail` de `ronda.villano.02`.
</content_spec>

<tasks>

<task type="auto">
  <name>Tarea 1: Tests en rojo — cierre de bucle contra contenido real y gate D-37 invertido</name>
  <files>engine/__tests__/navigator.test.ts, engine/__tests__/schema.test.ts</files>
  <read_first>
    - `engine/__tests__/navigator.test.ts` (fichero completo — estructura `describe('flatten')`/`describe('expand')`/`describe('next')`/`describe('prev')`/`describe('jumpTo')`, y el helper `noLoopGame()`)
    - `engine/__tests__/schema.test.ts` (fichero completo — el helper `baseGame()` y los 12 casos existentes)
    - `engine/__tests__/content.test.ts` líneas 1-16 (patrón de carga del contenido real con `readFileSync` + `fileURLToPath` + `validateGameDefinition`)
    - `engine/navigator.ts` y `engine/expand.ts` (contratos exactos de `next`/`prev`/`jumpTo` y de los índices de bucle)
    - `engine/schema.ts` (el `superRefine` actual y su comentario `TODO(fase 2)`)
    - La sección `<content_spec>` de este plan (los ids exactos que las aserciones deben usar)
  </read_first>
  <behavior>
    Tests que deben quedar EN ROJO al terminar esta tarea (la tarea 2 los pone en verde). Es un ciclo RED deliberado dentro del plan, no un fallo.
    - navigator: `expand(marvelChampions, ctx)` produce `sequence.length === 34`, `loopStartIndex === 34 - 10`, `loopEndIndex === 33`.
    - navigator: `next()` con `cursor === 33` y `round === 1` devuelve `cursor === 24` y `round === 2` (FLOW-03 + FLOW-04).
    - navigator: `next()` con el cursor en el nodo `setup.mesa-lista.01` avanza a `ronda.jugadores.01` (deja de ser el NO-OP documentado en `app/pages/[game]/index.vue`).
    - navigator: `prev()` con `cursor === 24` y `round === 2` devuelve `cursor === 33` y `round === 1`.
    - navigator: `prev()` con `cursor === 24` y `round === 1` devuelve `cursor === 23` y `round === 1` (sale del bucle hacia la preparación sin decrementar).
    - navigator: `jumpTo('ronda.villano.03')` desde una sesión con `round === 4` deja `round === 4`, y dos `next()` seguidos recorren `ronda.villano.04` y `ronda.villano.05` (FLOW-07).
    - navigator: `jumpTo('setup.heroes.01')` desde `round === 4` deja `round === 4` (FLOW-08), y avanzar desde ahí hasta `loopEndIndex` incrementa a `round === 5`.
    - schema: `GameDefinitionSchema.parse(baseGame())` NO lanza (con el nuevo `baseGame()` que ya incluye una sección repetible).
    - schema: con CERO secciones `repeats:true` lanza (test invertido).
    - schema: con DOS secciones `repeats:true` lanza (se conserva).
  </behavior>
  <action>
    Reescribe el helper `baseGame()` de `engine/__tests__/schema.test.ts` para que devuelva DOS secciones: la actual `setup` (`repeats:false`) y una nueva `ronda` con `repeats:true`, `title:'Ronda'`, una fase `ronda.turno` con `title:'Jugadores'` y un paso `ronda.turno.01`. Esto es obligatorio, no cosmético: tras D-37, un `baseGame()` sin sección repetible haría que TODOS los tests negativos del fichero (`'lanza ZodError con un paso sin id'`, `'lanza ZodError con un text de 120 caracteres'`, etc.) pasaran por el motivo equivocado — lanzarían por la invariante de repetición y dejarían de comprobar lo que dicen comprobar.

    Invierte el test `'NO lanza con cero secciones repeats:true'` (líneas ~60-64): renómbralo a `'lanza ZodError con cero secciones repeats:true'`, pon `game.sections[1].repeats = false`, y cambia la aserción a `.toThrow()`. Esto es la Pitfall 2 de `02-RESEARCH.md`: dejarlo intacto enmascararía un endurecimiento que nunca ocurrió.

    Ajusta el test `'lanza ZodError con dos secciones repeats:true'` para que siga siendo válido con el nuevo `baseGame()` (basta con poner `game.sections[0].repeats = true`, que deja dos; no hace falta el `push` manual actual, pero mantenerlo también es válido siempre que el recuento siga siendo mayor que uno).

    En `engine/__tests__/navigator.test.ts`, añade la carga del contenido real replicando literalmente el patrón de `content.test.ts` líneas 9-11 (`readFileSync` + `fileURLToPath` sobre `../../content/marvel-champions.json` + `validateGameDefinition`) y añade un bloque `describe('bucle de ronda sobre el contenido real (FLOW-03/04/07/08)')` con los casos listados en `<behavior>`. Usa `expand(marvelChampions, context)` como base y deriva los índices de `session.loopStartIndex`/`session.loopEndIndex` en vez de teclear 24/33 a mano donde sea posible, salvo en la aserción que precisamente comprueba esos valores. Conserva intactos todos los `describe` existentes sobre `tiny-game.json` — cubren el caso sin bucle (`noLoopGame()`), que no pasa por Zod y por tanto no se ve afectado por D-37.

    No toques `engine/navigator.ts`, `engine/expand.ts` ni `engine/__tests__/fixtures/tiny-game.json`: los tres están confirmados como correctos (el fixture ya tiene una única sección `loop` con `repeats:true`).
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/navigator.test.ts engine/__tests__/schema.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "repeats.*true" engine/__tests__/schema.test.ts` devuelve al menos 2 (el `baseGame()` nuevo y el caso de dos secciones).
    - `grep -q "lanza ZodError con cero secciones" engine/__tests__/schema.test.ts` sale con código 0; `grep -q "NO lanza con cero secciones" engine/__tests__/schema.test.ts` sale con código distinto de 0.
    - `grep -q "marvel-champions.json" engine/__tests__/navigator.test.ts` sale con código 0.
    - `npx vitest run engine/__tests__/navigator.test.ts engine/__tests__/schema.test.ts` FALLA, y los fallos son exclusivamente: los casos nuevos de bucle sobre contenido real, y el caso de cero secciones repetibles. Ningún test preexistente sobre `tiny-game.json` falla.
    - `npx vitest run engine/__tests__/content.test.ts engine/__tests__/toc.test.ts engine/__tests__/resolve.test.ts engine/__tests__/persistence.test.ts` sigue en verde (esta tarea no toca contenido ni esquema).
  </acceptance_criteria>
  <done>Los tests que describen el bucle real y el gate endurecido existen y fallan por la razón correcta; el resto de la suite sigue verde.</done>
</task>

<task type="auto">
  <name>Tarea 2: Autorar la sección RONDA y endurecer el gate en el mismo cambio</name>
  <files>content/marvel-champions.json, engine/schema.ts</files>
  <read_first>
    - `content/marvel-champions.json` (fichero completo — forma exacta de sección/fase/paso, formato de `citation`, sangría y orden de claves)
    - `engine/schema.ts` (fichero completo — `TextBlockSchema`, `SectionSchema`, el `superRefine` con el `TODO(fase 2)`)
    - La sección `<content_spec>` de este plan (los 10 pasos, literal)
    - La sección `<decisions_closed_here>` de este plan (DC-1: política de `speech`)
    - `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-RESEARCH.md` §«Rules Reference quotations for content authoring» (las citas página a página que respaldan cada paso)
    - `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-CONTEXT.md` §D-27 a D-35 y §D-31 (el principio «Recordar mirar, no explicar lo que ya está impreso»)
  </read_first>
  <action>
    **Los dos ficheros de esta tarea deben cambiar juntos, en el mismo commit.** Endurecer el gate antes de que exista la sección `ronda` rompe `content.test.ts` en su primera línea de módulo (`validateGameDefinition(rawMarvelChampions)` lanzaría al importar), y autorar el contenido sin endurecer el gate deja el `TODO(fase 2)` abierto. Es la Pitfall 1 de `02-RESEARCH.md`.

    En `content/marvel-champions.json`: sube `contentVersion` de `6` a `7` (PERS-03: cualquier partida guardada contra la v6 debe caer en el desenlace `content-changed` y volver al inicio conservando jugadores y dificultad, en vez de reanudar en un índice desplazado). Añade la sección `ronda` **después** de la sección `setup` en el array `sections`, con la forma exacta y los 10 pasos de `<content_spec>`. Reutiliza literalmente la forma de `citation` ya usada en `setup` (`source`, `section`, `page`). Todos los pasos llevan `kind: "step"` explícito y `speech` no vacío (DC-1). Ningún paso lleva `variants`. Ningún `warning` incluye el glifo `⚠`.

    En `engine/schema.ts`, dos cambios y solo dos:
    1. En el `superRefine`, sustituye la condición `repeating.length > 1` por `repeating.length !== 1`, actualiza el mensaje de `ctx.addIssue` para que diga que se exige exactamente una sección (por ejemplo, en la línea de `Exactly one section must have repeats:true, found ${repeating.length}`), y elimina el comentario `TODO(fase 2)` sustituyéndolo por uno que registre D-37 y por qué la invariante es «exactamente una» (una sección sin bucle dejaría `loopEndIndex` indefinido y el juego llegaría a la mesa sin ronda, en silencio).
    2. En `TextBlockSchema`, añade `.max(120)` al campo `speech` existente y actualiza su comentario: deja de estar «reservado para Fase 3, sin usar aquí» — el contenido de la ronda lo usa desde ahora (DC-1), aunque el consumidor en tiempo de ejecución (TTS) siga siendo Fase 3.

    NO añadas aquí el campo `warningDetail` ni su regla de dependencia con `warning`: eso es el plan `02-03`. NO toques `engine/types.ts` ni `engine/resolve.ts` en esta tarea. NO toques `engine/expand.ts` ni `engine/navigator.ts` (DC-2). `zod` no cruza a `app/` (T-01-19): todos los cambios de esquema quedan dentro de `engine/schema.ts`.
  </action>
  <verify>
    <automated>node -e "const g=require('./content/marvel-champions.json');const r=g.sections.find(s=>s.id==='ronda');const st=r.phases.flatMap(p=>p.steps);if(g.contentVersion!==7)throw new Error('contentVersion');if(g.sections.filter(s=>s.repeats).length!==1)throw new Error('repeats');if(r.phases.length!==2)throw new Error('fases');if(r.phases[0].steps.length!==4||r.phases[1].steps.length!==6)throw new Error('pasos');for(const s of st){if(s.text.length>90||(s.warning&&s.warning.length>60)||!s.speech||s.speech.length>120||!s.citation||s.variants)throw new Error('paso '+s.id)}console.log('ok',st.length)" &amp;&amp; npx vitest run engine/__tests__/navigator.test.ts engine/__tests__/schema.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./content/marvel-champions.json').contentVersion)"` imprime `7`.
    - La sección `ronda` existe con `repeats: true`, `title: "Ronda"`, y es la ÚNICA con `repeats: true` en todo el fichero.
    - `ronda.phases` tiene exactamente 2 entradas, con `id` `ronda.jugadores` (4 pasos) y `ronda.villano` (6 pasos), y ambas declaran `summaryLabel` no vacío.
    - Los `title` de ambas fases son exactamente `"Jugadores"` y `"Villano"` — NO `"JUGADORES"`/`"VILLANO"`.
    - Los 10 pasos son `kind: "step"`, todos tienen `citation.source === "rules-reference"` y `citation.page` entero positivo; ninguno declara `variants`.
    - Los 10 pasos declaran `speech` no vacío de ≤120 caracteres, sin los caracteres `⚠`, `×` ni `›`.
    - Ningún `warning` de la ronda contiene el carácter `⚠`.
    - `grep -c "repeating.length !== 1" engine/schema.ts` devuelve 1; `grep -c "TODO(fase 2)" engine/schema.ts` devuelve 0.
    - `grep -c "z.string().max(120).optional()" engine/schema.ts` devuelve 1 (el campo `speech`).
    - `grep -rn "warningDetail" engine/schema.ts` no devuelve nada (es del plan 02-03).
    - `git diff --name-only` para esta tarea lista exactamente `content/marvel-champions.json` y `engine/schema.ts` — ni `expand.ts`, ni `navigator.ts`, ni el fixture `tiny-game.json`.
    - `npx vitest run engine/__tests__/navigator.test.ts engine/__tests__/schema.test.ts` pasa al 100%.
  </acceptance_criteria>
  <done>El bucle existe como dato, el gate exige exactamente una sección repetible, y los tests de motor de la tarea 1 están en verde.</done>
</task>

<task type="auto">
  <name>Tarea 3: Gates de contenido que muerden sobre la ronda</name>
  <files>engine/__tests__/content.test.ts, engine/__tests__/toc.test.ts</files>
  <read_first>
    - `engine/__tests__/content.test.ts` (fichero completo — el helper `allSteps()`, `PLAYER_COUNT_PATTERN`, y el patrón de «gate que muerde» de las líneas finales)
    - `engine/__tests__/toc.test.ts` (fichero completo — en particular el test que enumera los 7 bloques del contenido real)
    - `content/marvel-champions.json` tal como quedó tras la tarea 2
    - La sección `<content_spec>` de este plan
  </read_first>
  <action>
    Actualiza los recuentos cableados que la tarea 2 rompió por diseño (es la convención deliberada de «gate que muerde» de la Fase 1, no una regresión):
    - `'la sección setup aplanada produce exactamente 24 nodos: 23 kind step y 1 kind summary'` → 34 nodos totales, 33 `kind:'step'`, 1 `kind:'summary'`; renombra el test para que ya no diga «la sección setup».
    - `'exactamente 3 pasos declaran warning (D-05)'` → 11 ids, con la lista completa ordenada: los 3 de `setup` más `ronda.jugadores.01`, `ronda.jugadores.03`, `ronda.jugadores.04`, `ronda.villano.01`, `ronda.villano.02`, `ronda.villano.03`, `ronda.villano.04`, `ronda.villano.06`.
    - `'el número de nodos aplanados es idéntico con dificultad normal y con dificultad expert'` → `toBe(34)`.
    - `'toda fase con al menos un paso kind step declara summaryLabel no vacío'` → generaliza el bucle: hoy recorre solo `marvelChampions.sections[0].phases`; debe recorrer todas las secciones.
    - `'exactamente 2 pasos declaran variants.difficulty'` se mantiene con los mismos 2 ids de `setup` — ningún paso de la ronda declara variantes.
    - En `toc.test.ts`, el test `'sobre el contenido real de Marvel Champions con cursor 0 devuelve los 7 bloques en orden'` → 9 bloques, con `'Jugadores'` y `'Villano'` al final, en ese orden. Con `cursor 0` el nodo actual NO está en la sección repetible, así que el orden es el natural del documento (sin reordenado; el reordenado es del plan `02-02`).

    Añade estas aserciones nuevas, todas cableadas contra ids concretos (misma convención):
    - **Estructura del bucle (D-34/D-35, CONT-03):** existe una sección `ronda` con `repeats:true`; tiene exactamente 2 fases; `ronda.jugadores` tiene 4 pasos `kind:'step'` y `ronda.villano` exactamente 6 — el recuento oficial del Rules Reference, no los 4 del borrador. El último paso de la sección es `ronda.villano.06`.
    - **Convención Title Case (Pitfall 5):** `ronda.jugadores.title === 'Jugadores'` y `ronda.villano.title === 'Villano'`, y ninguno de los dos es igual a su propia versión en mayúsculas.
    - **Orden de fin de fase (CONT-02):** los ids de `ronda.jugadores` en orden son `01, 02, 03, 04`, y sus `text` casan respectivamente con `/descartad/i` (02), `/robad/i` (03) y `/enderezad/i` (04).
    - **CONT-04:** `ronda.villano.05.text` casa con `/ficha de jugador inicial/i`; `ronda.villano.06` existe como `kind:'step'`.
    - **CONT-05 (dos agotamientos distintos):** `ronda.jugadores.03.warning` casa con `/barajad el descarte/i` y NO con `/aceleración/i`; `ronda.villano.04.warning` casa con `/aceleración/i`. Los dos avisos son cadenas distintas.
    - **CONT-06:** `ronda.jugadores.01.warning` casa con `/dial del villano/i`.
    - **CONT-07:** `ronda.villano.02.warning` casa con `/estados/i`.
    - **ADAPT-04 (D-33):** `ronda.villano.02.text` casa a la vez con `/héroe/i` y con `/alter-ego/i` en la misma cadena, sin ningún campo `branches` en el paso.
    - **Error confirmado nº2:** ningún `text` ni `warning` de la ronda casa con `/una por jugador/i`.
    - **Error confirmado nº3 (negativo, el positivo llega en 02-03):** ningún `text` ni `warning` de la ronda casa con `/todos los esbirros/i`.
    - **Error confirmado nº4:** ningún paso de la ronda declara `variants.difficulty`, y ningún `text`/`warning` de la ronda casa con `/experto|heroico/i`.
    - **Política DC-1 de `speech`:** los 10 pasos de la ronda declaran `speech` no vacío de ≤120 caracteres, sin `⚠`, `×` ni `›`. Registra en el mismo test, con un comentario, que los pasos de `setup` siguen deliberadamente sin `speech` (Fase 3).
    - **Sin glifo autorado:** ningún `warning` del fichero contiene `⚠` (lo antepone `StepScreen.vue`).
    - **Gate D-37 que muerde:** sobre una copia en memoria del JSON real (mismo patrón que el test de los 120 caracteres, sin tocar el fichero), poner `repeats:false` en la sección `ronda` hace que `validateGameDefinition` lance.

    Todas las aserciones de contenido de la ronda deben leer los pasos por su `id` mediante un helper, no por índice de array, para que insertar un paso en el futuro rompa el recuento (deseado) pero no desplace silenciosamente las comprobaciones semánticas.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` termina con 0 fallos y con un número de tests estrictamente mayor que los 61 de la línea base.
    - `grep -c "ronda\." engine/__tests__/content.test.ts` devuelve al menos 15 (las aserciones nuevas están cableadas contra ids concretos, no contra recuentos genéricos).
    - `grep -q "'Jugadores'" engine/__tests__/toc.test.ts` y `grep -q "'Villano'" engine/__tests__/toc.test.ts` salen ambos con código 0.
    - Prueba de mordida manual reproducible: cambiar `ronda.villano` a 5 pasos en el JSON hace fallar `content.test.ts`; revertir lo devuelve a verde.
    - `git diff --name-only` para esta tarea lista exactamente `engine/__tests__/content.test.ts` y `engine/__tests__/toc.test.ts`.
  </acceptance_criteria>
  <done>Toda la suite en verde con el contenido de la ronda dentro, y cada corrección de reglas confirmada tiene su propia aserción cableada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autor de contenido → JSON versionado | El único «input» de esta fase es contenido escrito a mano y revisado como código; no hay entrada de usuario final |
| JSON de contenido → navegador de la tablet | El contenido llega crudo al cliente (sin pasar por Zod), y se pinta como texto |
| JSON de contenido → CI (Node/Vitest) | Único punto donde `zod` valida; es el gate que impide que llegue a la mesa contenido malformado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Texto de contenido pintado en `StepScreen.vue` | mitigate | Heredada de T-01-01: interpolación `{{ }}` exclusivamente, prohibida la directiva de HTML crudo. Este plan no toca componentes, así que la mitigación se conserva por no hacer nada |
| T-02-02 | Tampering | Contenido sin bucle llegando a la tablet en silencio | mitigate | D-37: `superRefine` exige exactamente una sección `repeats:true`; test «gate que muerde» en `content.test.ts` que lanza al poner `repeats:false` sobre una copia en memoria |
| T-02-03 | Tampering | `zod` cruzando a `app/` y llegando al bundle del navegador | mitigate | Todos los cambios de esquema confinados a `engine/schema.ts` (T-01-19); criterio de aceptación que verifica la lista de ficheros tocados |
| T-02-04 | Information disclosure | Reproducción de texto con copyright del Rules Reference | mitigate | Redacción propia imperativa y breve (D-31/CONT-10); la cita vive solo en datos (D-06), nunca en pantalla; ningún `text`/`warning` copia literalmente el reglamento |
| T-02-05 | Tampering | `localStorage` manipulado con una posición de la v6 del contenido | accept/mitigate | Ya mitigado en Fase 1 (`isPersistedPosition` + `contentChangedFallback`); este plan sube `contentVersion` a 7, que es precisamente lo que fuerza el desenlace `content-changed` en vez de reanudar en un índice desplazado |
| T-02-SC | Tampering | Instalaciones de npm/pip/cargo | accept | Esta fase instala CERO paquetes nuevos (`02-RESEARCH.md` §Package Legitimacy Audit: no aplica). No hay tarea de instalación, luego no hay checkpoint de legitimidad que superar |
</threat_model>

<verification>
- `npx vitest run` en verde, con más tests que los 61 de la línea base.
- `node -e "const {expand}=require('...')"` no aplica (ESM); la comprobación estructural del JSON se hace con el comando `node -e` de la tarea 2.
- Comprobación manual en `npm run dev`: desde «Mesa lista», pulsar EMPEZAR A JUGAR entra en `Jugad vuestros turnos…`; recorrer los 10 pasos y pulsar SIGUIENTE en el último vuelve al primero de la fase de jugadores. La cabecera todavía dirá `RONDA · 25 de 33` — es correcto para este plan; el contador relativo a la fase llega en `02-02`.
</verification>

<success_criteria>
- La sección `ronda` existe con 2 fases y 10 pasos, todos citados contra el Rules Reference v1.7 y todos con `speech`.
- `loopStartIndex === 24` y `loopEndIndex === 33` sobre el contenido real.
- FLOW-03, FLOW-04, FLOW-07 y FLOW-08 cubiertos por tests automáticos contra el contenido real, no solo contra el fixture.
- Un contenido sin sección repetible hace fallar la validación en CI.
- `engine/expand.ts`, `engine/navigator.ts` y `engine/__tests__/fixtures/tiny-game.json` sin modificar.
</success_criteria>

<output>
Crea `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-01-SUMMARY.md` al terminar.
</output>
