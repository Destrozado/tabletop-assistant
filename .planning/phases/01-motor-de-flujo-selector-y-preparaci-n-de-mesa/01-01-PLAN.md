---
phase: 01-motor-de-flujo-selector-y-preparaci-n-de-mesa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - nuxt.config.ts
  - tsconfig.json
  - vitest.config.ts
  - .gitignore
  - netlify.toml
  - .github/workflows/ci.yml
  - app/app.vue
  - app/assets/css/main.css
  - app/pages/[game]/index.vue
  - app/components/AppHeader.vue
  - app/components/StepScreen.vue
  - app/components/NavBand.vue
  - app/composables/useGameContent.ts
  - app/composables/useGameSession.ts
  - engine/types.ts
  - engine/schema.ts
  - engine/flatten.ts
  - engine/expand.ts
  - engine/resolve.ts
  - engine/navigator.ts
  - engine/__tests__/fixtures/tiny-game.json
  - engine/__tests__/navigator.test.ts
  - engine/__tests__/schema.test.ts
  - engine/__tests__/resolve.test.ts
  - engine/__tests__/content.test.ts
  - content/games-index.ts
  - content/marvel-champions.json
autonomous: true
requirements: [TECH-01, TECH-02, TECH-03, TECH-04, FLOW-01, FLOW-02, FLOW-05, CONT-08, UI-01, UI-02, UI-03, UI-04, UI-05]
user_setup: []

must_haves:
  truths:
    - "Un usuario abre `/marvel-champions` en Chrome (emulación tablet, horizontal) y ve el primer paso real de la preparación con su frase de acción grande sobre tema oscuro (UI-01, UI-05, D-19: criterios proxy objetivos, no validación física)"
    - "Al pulsar SIGUIENTE avanza al paso siguiente y al pulsar ‹ Atrás retrocede; SIGUIENTE es el control más prominente de la pantalla (FLOW-01, FLOW-02)"
    - "La cabecera muestra sección y posición a la izquierda y contexto de partida a la derecha, con el control ≡ en el extremo, y sobrevive sin rediseño al formato `RONDA 4 · Villano · 3 de 6` de la Fase 2 (FLOW-05, D-11)"
    - "Al girar el viewport a vertical la app se oculta por completo y aparece el aviso «Girad la tablet»; nada se reorganiza (UI-04)"
    - "`npx vitest run` prueba el cierre de bucle, el salto y el clamp del motor sobre un fixture con sección repetible, y rechaza contenido mal formado (TECH-02, TECH-03)"
    - "`npm run generate` produce salida estática desplegable sin servidor Node (TECH-01)"
    - "El motor no importa nada de Vue/Nuxt/DOM y el contenido es un fichero de datos versionado; añadir un juego es añadir un fichero (TECH-04)"
  artifacts:
    - path: "engine/navigator.ts"
      provides: "next/prev/jumpTo puros sobre EngineSession"
      exports: ["next", "prev", "jumpTo"]
    - path: "engine/schema.ts"
      provides: "GameDefinitionSchema Zod + validateGameDefinition (solo Node/CI)"
      exports: ["GameDefinitionSchema", "validateGameDefinition"]
    - path: "engine/types.ts"
      provides: "Contratos TypeScript del contenido y del runtime, con warning/speech/kind/citation"
      contains: "export interface StepDefinition"
    - path: "content/marvel-champions.json"
      provides: "Contenido real del bloque HÉROES (3 pasos citados), sección setup repeats:false"
      contains: "\"gameId\": \"marvel-champions\""
    - path: "app/assets/css/main.css"
      provides: "Tokens de diseño de 01-UI-SPEC.md como @theme de Tailwind v4"
      contains: "--color-background: #14161C"
    - path: "app/app.vue"
      provides: "Shell con meta viewport, #app-root y #orientation-guard"
      contains: "orientation-guard"
    - path: ".github/workflows/ci.yml"
      provides: "Gate de CI que ejecuta la suite de Vitest"
      contains: "vitest"
    - path: "netlify.toml"
      provides: "Configuración de despliegue preparada (D-17), no ejecutada"
      contains: "publish"
  key_links:
    - from: "app/composables/useGameSession.ts"
      to: "engine/navigator.ts"
      via: "import estático desde ~~/engine"
      pattern: "from '~~/engine/navigator'"
    - from: "app/composables/useGameContent.ts"
      to: "content/marvel-champions.json"
      via: "import estático en build (nunca fetch en runtime)"
      pattern: "import .*content/marvel-champions.json"
    - from: "engine/__tests__/content.test.ts"
      to: "engine/schema.ts"
      via: "validateGameDefinition sobre el contenido real"
      pattern: "validateGameDefinition"
---

## Phase Goal

**Como** grupo de jugadores con una tablet apoyada junto a la mesa, **quiero** elegir Marvel Champions, indicar jugadores y dificultad, y recorrer paso a paso toda la preparación de mesa con «Siguiente»/«Atrás», cabecera de orientación, índice de salto y progreso guardado, **para que** podamos montar la partida entera sin abrir el reglamento y sin saltarnos ningún paso.

> Reformulación literal del `**Goal:**` de la Fase 1 en ROADMAP.md al formato de historia de usuario exigido por MVP_MODE. No añade alcance.

## Rebanada de este plan (Esqueleto Andante)

Abro `/marvel-champions` en Chrome con emulación de tablet en horizontal, veo el primer paso **real** de la preparación (leído del JSON de contenido a través del cargador tipado), pulso SIGUIENTE y avanzo, pulso Atrás y retrocedo. Con `npm run generate` obtengo la salida estática desplegable. Esto prueba la tubería entera —scaffold, rutas, lectura real de contenido validado, motor puro, UI táctil, build— antes de añadir ninguna anchura.

<objective>
Levantar el esqueleto andante del proyecto: scaffold de Nuxt 4 con SSG, tema oscuro tablet-first de `01-UI-SPEC.md` traducido a tokens de Tailwind v4, guardia de orientación, motor de flujo puro en TypeScript con sus tests, esquema Zod validado en CI, y una pantalla de paso real navegable con SIGUIENTE/Atrás.

Purpose: probar de extremo a extremo la arquitectura completa (contenido → motor → composable → componentes → build estático) con la mínima anchura posible, para que las rebanadas siguientes solo añadan contenido y pantallas sin renegociar decisiones estructurales.
Output: proyecto Nuxt 4 funcional en la raíz del repo, `engine/` puro con tests verdes, `content/marvel-champions.json` con el bloque HÉROES real y citado, y `npm run generate` produciendo salida estática.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-CONTEXT.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-RESEARCH.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-UI-SPEC.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/SKELETON.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Contratos que este plan CREA y que los planes 02-06 consumen sin explorar el código. -->

`engine/types.ts` (creado en la tarea 2, ningún import de Vue/Nuxt/DOM):

- `type Difficulty = 'normal' | 'expert'`
- `interface Citation { source: 'rules-reference' | 'learn-to-play'; section: string; page?: number }`
- `interface TextBlock { text: string; warning?: string; speech?: string }`
- `interface StepDefinition extends TextBlock { id: string; title: string; kind: 'step' | 'summary'; variants?: { difficulty?: Partial<Record<Difficulty, Partial<TextBlock>>> }; citation?: Citation }`
- `interface PhaseDefinition { id: string; title: string; summaryLabel?: string; steps: StepDefinition[] }`
- `interface SectionDefinition { id: string; title: string; repeats: boolean; phases: PhaseDefinition[] }`
- `interface GameDefinition { gameId: string; title: string; locale: 'es'; contentVersion: number; sections: SectionDefinition[] }`
- `interface FlatStepNode { step: StepDefinition; sectionId: string; sectionTitle: string; sectionRepeats: boolean; phaseId: string; phaseTitle: string; breadcrumb: string }`
- `interface RuntimeStepNode extends FlatStepNode { runtimeId: string }`
- `interface SessionContext { playerCount: number; difficulty: Difficulty; [key: string]: unknown }`
- `interface EngineSession { gameId: string; contentVersion: number; sequence: RuntimeStepNode[]; cursor: number; round: number; context: SessionContext; loopStartIndex?: number; loopEndIndex?: number }`

Funciones puras exportadas:
- `engine/flatten.ts` → `flatten(game: GameDefinition): FlatStepNode[]`
- `engine/expand.ts` → `expand(game: GameDefinition, context: SessionContext): EngineSession`
- `engine/navigator.ts` → `next(s: EngineSession): EngineSession`, `prev(s: EngineSession): EngineSession`, `jumpTo(s: EngineSession, runtimeId: string): EngineSession`
- `engine/resolve.ts` → `resolveText(node: RuntimeStepNode, context: SessionContext): TextBlock`
- `engine/schema.ts` → `GameDefinitionSchema`, `validateGameDefinition(json: unknown): GameDefinition` — **solo Node/CI, nunca importado desde `app/`**

`app/composables/useGameSession.ts` expone: `session` (ref), `start(context)`, `next()`, `prev()`, `jumpTo(runtimeId)`, `currentNode` (computed), `currentText` (computed `TextBlock` ya resuelto), `sectionLabel` (computed string), `position` (computed `{current, total} | null`), `sessionContextLabel` (computed string, p. ej. `3 jug · Normal`).

Props de componentes (idénticos a `01-UI-SPEC.md` §Component Inventory):
- `AppHeader`: `sectionLabel: string`, `position: {current:number,total:number} | null`, `sessionContext: string`, emite `index-open`
- `StepScreen`: `actionText: string`, `warningText: string | null`
- `NavBand`: `nextLabel?: string` (por defecto `SIGUIENTE`), emite `back` y `next`
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Scaffold de Nuxt 4, tema oscuro tablet-first y gates de build</name>
  <files>package.json, package-lock.json, nuxt.config.ts, tsconfig.json, vitest.config.ts, .gitignore, netlify.toml, .github/workflows/ci.yml, app/app.vue, app/assets/css/main.css</files>
  <read_first>
    - `CLAUDE.md` — decisiones de stack vinculantes (`nuxt generate`, `@tailwindcss/vite`, sin Pinia, sin `@nuxt/content`, Netlify)
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-RESEARCH.md` — §Standard Stack (versiones exactas), §Instalación mínima de esta fase, §Pattern 1 (vitest projects), §Pattern 2 (tokens Tailwind v4 `@theme`), §Anti-Patterns (`--spacing` escalar), §Despliegue preparado (`netlify.toml`, `.gitignore`)
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-UI-SPEC.md` — §Spacing Scale, §Typography, §Color, §Orientation & Rotation Behavior (UI-04), §Copywriting Contract (textos del guardia de orientación)
    - `.planning/research/ARCHITECTURE.md` §4 — layout de Nuxt 4, por qué `engine/` va fuera de `srcDir`, alias `~~`
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/SKELETON.md` — decisiones arquitectónicas fijadas para todo el proyecto
  </read_first>
  <action>
Crea el proyecto Nuxt 4 **en la raíz del repo existente** (`/Users/vcompanyb/TableGameAssistant`), que hoy solo contiene `CLAUDE.md`, `.planning/` y `.git/`.

Procedimiento de scaffold: ejecuta `npm create nuxt@latest tga-scaffold` en un directorio temporal FUERA del repo (usa el scratchpad de sesión). Si el CLI admite flags no interactivos usa `--packageManager npm` y desactiva la inicialización de git; si pregunta, responde: gestor de paquetes `npm`, **no** inicializar git, **ningún** módulo oficial adicional. Después mueve todo el contenido generado (incluidos ficheros ocultos) a la raíz del repo, sin tocar `.git/`, `.planning/` ni `CLAUDE.md`, y borra el temporal. No uses `nuxi init` (ya no es el comando documentado, ver 01-RESEARCH §Comando de scaffold actualizado).

Dependencias exactas (versiones verificadas en 01-RESEARCH §Standard Stack; deja que npm resuelva el semver del scaffold para `nuxt`, que debe quedar en 4.5.x):
- runtime: `@vueuse/core`, `@vueuse/nuxt`, `tailwindcss`, `@tailwindcss/vite`
- dev: `zod`, `vitest`, `@nuxt/test-utils`
- **NO instales `@vite-pwa/nuxt` ni `@playwright/test`** — son Fase 4 y post-v1 respectivamente.

`nuxt.config.ts`: `ssr: true` (nunca `ssr: false`), `modules: ['@vueuse/nuxt']`, `css: ['~/assets/css/main.css']`, `vite.plugins` con el plugin `tailwindcss()` importado de `@tailwindcss/vite`, `nitro.prerender.routes` con `'/'` y `'/marvel-champions'`, y `app.head.meta` con el viewport exacto de 01-UI-SPEC: `width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no`. Fija `compatibilityDate` al día de ejecución.

Scripts de `package.json`: `dev` (`nuxt dev`), `build` (`nuxt build`), `generate` (`nuxt generate`), `test` (`vitest run`).

`vitest.config.ts`: usa `test.projects` (NO `vitest.workspace.ts`) con un único proyecto `engine`: `environment: 'node'`, `include: ['engine/**/*.test.ts']`. Deja comentado el proyecto `nuxt` futuro tal como muestra 01-RESEARCH §Pattern 1.

`app/assets/css/main.css`: `@import "tailwindcss";` seguido de un bloque `@theme` con **exactamente** los tokens de 01-UI-SPEC traducidos según 01-RESEARCH §Pattern 2 — colores `--color-background: #14161C`, `--color-surface: #1E212B`, `--color-primary-text: #F2F3F5`, `--color-secondary-text: #9AA0AC`, `--color-accent: #2F81F7`, `--color-warning: #FFB020`, `--color-destructive: #FF5C5C`, `--color-on-accent: #0B1220`; espaciados con namespace `--spacing-xs|sm|md|lg|xl|2xl|3xl` (4/8/16/24/32/48/64px) — **nunca** el escalar `--spacing` a secas (Pitfall 4); tipografía `--text-display: 2.5rem` (+`--text-display--line-height: 1.2`), `--text-heading: 1.75rem` (1.25), `--text-body: 1.25rem` (1.5), `--text-label: 1.125rem` (1.2). No añadas `--font-weight-*`: `font-bold`/`font-normal` de fábrica cubren los pesos 700/400 del contrato. Declara la fuente Inter autoalojada (woff2 en `public/fonts/`) con `@font-face` y fallback `system-ui, -apple-system, sans-serif`; si no puedes obtener el woff2 sin red, deja el `@font-face` comentado con un TODO y usa el fallback — no bloquees el plan por la fuente.

`app/app.vue`: dos hermanos siempre montados — `#app-root` con clase `portrait:hidden` que contiene `<NuxtPage />`, y `#orientation-guard` con clases `hidden portrait:flex`, fondo `bg-background`, centrado, con el título «Girad la tablet» (Heading, texto primario) y el cuerpo «Esta aplicación se usa en horizontal.» (Body, texto secundario). Sin control de cierre, sin JS, sin listeners: la visibilidad la decide solo la variante `portrait:` (UI-04).

`.gitignore` en la raíz con al menos: `node_modules/`, `.nuxt/`, `.output/`, `.data/`, `dist/`, `*.log`, `.DS_Store`.

`netlify.toml` en la raíz según 01-RESEARCH §Despliegue preparado: `[build] command = "npm run generate"`, `publish` apuntando al directorio de salida real (verifícalo con la build de esta misma tarea; la investigación asume `.output/public` como `[ASSUMED]` A1 — corrígelo si la build produce otro directorio y anótalo en el SUMMARY), cabeceras `Cache-Control: no-cache` para `/sw.js` y `/manifest.webmanifest` (aún no existen; se dejan preparadas para la Fase 4) y `Cache-Control: public, max-age=31536000, immutable` para `/_nuxt/*`. No despliegues nada (D-17).

`.github/workflows/ci.yml`: workflow `CI` disparado en `push` y `pull_request`, un job en `ubuntu-latest` con `actions/checkout`, `actions/setup-node` (Node 22, cache npm), `npm ci` y `npm run test`. Este es el gate de TECH-02: un fichero de contenido mal formado debe romper aquí.
  </action>
  <verify>
    <automated>cd /Users/vcompanyb/TableGameAssistant && npm run generate && test -f .output/public/index.html && echo GENERATE_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && grep -v '^\s*//' nuxt.config.ts | grep -c "ssr: false" | grep -q '^0$' && echo NO_SPA_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && grep -q -- "--spacing-lg: 24px" app/assets/css/main.css && ! grep -qE "^\s*--spacing:\s" app/assets/css/main.css && echo TOKENS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` existe en la raíz del repo y lista `nuxt` (4.5.x), `@vueuse/core`, `@vueuse/nuxt`, `tailwindcss`, `@tailwindcss/vite` en dependencias y `zod`, `vitest`, `@nuxt/test-utils` en devDependencies
    - `package.json` NO contiene `@vite-pwa/nuxt` ni `@playwright/test`
    - `npm run generate` termina con código 0 y produce `.output/public/index.html`
    - `nuxt.config.ts` contiene `ssr: true`, el plugin de `@tailwindcss/vite`, `nitro.prerender.routes` incluyendo `'/marvel-champions'`, y el meta viewport con `user-scalable=no`
    - `app/assets/css/main.css` contiene los 8 tokens `--color-*`, los 7 `--spacing-{xs,sm,md,lg,xl,2xl,3xl}` y los 4 `--text-{display,heading,body,label}` con sus `--line-height`; NO contiene una declaración `--spacing:` sin sufijo
    - `app/app.vue` contiene un elemento con `id="app-root"` y clase `portrait:hidden`, y otro con `id="orientation-guard"` y clases `hidden portrait:flex`, con los textos exactos «Girad la tablet» y «Esta aplicación se usa en horizontal.»
    - `vitest.config.ts` define `test.projects` con un proyecto llamado `engine`, `environment: 'node'` e `include` `engine/**/*.test.ts`; no existe `vitest.workspace.ts`
    - `.gitignore` contiene `node_modules/`, `.nuxt/` y `.output/`
    - `netlify.toml` contiene `command = "npm run generate"`, un `publish` que coincide con el directorio realmente producido por la build, y las tres reglas `[[headers]]`
    - `.github/workflows/ci.yml` existe y su job ejecuta `npm run test`
  </acceptance_criteria>
  <done>El repo contiene un proyecto Nuxt 4 que compila a estático, con el tema oscuro tablet-first disponible como utilidades de Tailwind, guardia de orientación funcionando por CSS, y los gates de CI y despliegue preparados sin desplegar nada.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Motor de flujo puro, esquema Zod validado en CI y primer bloque de contenido real</name>
  <files>engine/types.ts, engine/schema.ts, engine/flatten.ts, engine/expand.ts, engine/resolve.ts, engine/navigator.ts, engine/__tests__/fixtures/tiny-game.json, engine/__tests__/navigator.test.ts, engine/__tests__/schema.test.ts, engine/__tests__/resolve.test.ts, engine/__tests__/content.test.ts, content/games-index.ts, content/marvel-champions.json</files>
  <read_first>
    - `.planning/research/ARCHITECTURE.md` §1 (pseudocódigo next/prev/jumpTo), §2 (tipos TS y boceto Zod), §3 (frontera de pureza), §6 (versionado de contenido)
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-RESEARCH.md` — §Hallazgos y ajustes 1-5 (warning en vez de detail, kind summary, `repeating.length <= 1`, toc por phases, variantes de dificultad como texto), §Code Examples (schema ajustado), §Motor de flujo, §Estrategia de tests del motor, §Tabla de 21 pasos (filas 1-3 = bloque HÉROES)
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-UI-SPEC.md` §Action-sentence character budget (límites 90/60 caracteres)
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-CONTEXT.md` — D-05 (aviso), D-06 (cita solo en datos), D-07/D-08 (fórmulas genéricas, sin nº real de jugadores)
    - `vitest.config.ts` (creado en la tarea 1)
  </read_first>
  <behavior>
    - `next()` en el último índice de una secuencia sin sección repetible deja el cursor donde estaba (clamp) y no altera `round`
    - `next()` cuando `cursor === loopEndIndex` devuelve `cursor === loopStartIndex` y `round + 1`
    - `prev()` cuando `cursor === loopStartIndex` y `round > 1` devuelve `cursor === loopEndIndex` y `round - 1`
    - `prev()` en el índice 0 con `round === 1` deja el cursor en 0 y `round` en 1
    - `jumpTo()` a un `runtimeId` del tramo repetible mueve el cursor y NO cambia `round`; un `next()` posterior continúa correctamente desde el nuevo cursor y cierra el bucle en el sitio correcto
    - `expand()` sobre un juego sin sección `repeats:true` devuelve `loopStartIndex` y `loopEndIndex` `undefined` y una secuencia del mismo largo que el número de pasos autorados
    - `GameDefinitionSchema.parse()` lanza `ZodError` con: un paso sin `id`, dos secciones con `repeats:true`, un `text` de más de 90 caracteres, un `warning` de más de 60 caracteres, e ids duplicados
    - `resolveText()` sobre un paso con `variants.difficulty.expert.text` devuelve el texto experto cuando `context.difficulty === 'expert'` y el texto base cuando es `'normal'`
    - `validateGameDefinition(content/marvel-champions.json)` no lanza
  </behavior>
  <action>
Crea el directorio `engine/` en la RAÍZ del repo (fuera de `srcDir`, que en Nuxt 4 es `app/`), con cero imports de Vue, Nuxt o DOM. Se importa desde la app con el alias `~~/engine/...`.

`engine/types.ts`: exactamente los tipos listados en el bloque `<interfaces>` de este plan. Diferencias deliberadas respecto a `ARCHITECTURE.md` §2, todas justificadas en 01-RESEARCH §Hallazgos: el campo se llama `warning` (no `detail`, hallazgo #1, D-05); `StepDefinition` lleva `kind: 'step' | 'summary'` (hallazgo #2, D-03); `PhaseDefinition` lleva `summaryLabel?: string` (etiqueta de la lista de repaso de «mesa lista», para que ni el índice ni el checklist se teclee dos veces en componentes); **no existe `perPlayer`** — D-02 colapsa las acciones simultáneas en un paso plural y D-08 prohíbe mencionar el nº de jugadores, así que la expansión por jugador no tiene ningún caso de uso en este proyecto; **no existe `branches`** en esta fase (ADAPT-04 es Fase 2). `SessionContext` es un saco abierto (`[key: string]: unknown`) por la recomendación de extensibilidad de `ARCHITECTURE.md` §8.

`engine/schema.ts`: el esquema Zod de 01-RESEARCH §Code Examples, con estos ajustes concretos:
- `idPattern` debe admitir guiones dentro de cada segmento además de puntos (los ids autorados incluyen `setup.jugador-inicial` y `setup.mesa-lista`); el patrón de `ARCHITECTURE.md` no los admite y los rechazaría.
- `TextBlockSchema`: `text` con `.min(1).max(90)`, `warning` opcional `.max(60)`, `speech` opcional (campo reservado para la Fase 3, no se usa en esta fase).
- `StepSchema`: `kind` con `.default('step')`, `variants.difficulty.{normal,expert}` como `TextBlock` parcial opcional, `citation` opcional.
- `PhaseSchema`: `summaryLabel` opcional.
- `superRefine`: rechaza más de una sección con `repeats:true` (relajado a «cero o una» en esta fase, hallazgo #3) con el comentario `// TODO(fase 2): endurecer a === 1 cuando exista la sección "round"`, y rechaza ids duplicados en todo el fichero.
- Exporta `GameDefinitionSchema` y `validateGameDefinition`.
**Restricción dura:** `engine/schema.ts` es el único fichero que importa `zod` y **ningún fichero bajo `app/` puede importarlo** — `zod` es devDependency y no debe entrar nunca en el bundle del navegador.

`engine/flatten.ts`, `engine/expand.ts`, `engine/resolve.ts`, `engine/navigator.ts`: funciones puras con las firmas del bloque `<interfaces>`. `flatten()` recorre el árbol una vez y construye `breadcrumb` como `"<sectionTitle> › <phaseTitle>"`. `expand()` fija `runtimeId = step.id`, `cursor = 0`, `round = 1`, y calcula `loopStartIndex`/`loopEndIndex` como primer y último índice de la única sección con `repeats:true`, dejándolos `undefined` si no hay ninguna. `navigator.ts` reproduce literalmente el pseudocódigo de 01-RESEARCH §Motor de flujo (los guards `!== undefined` importan: sin sección repetible no debe entrar nunca en la rama de bucle). `resolveText()` fusiona `variants.difficulty[context.difficulty]` sobre el `TextBlock` base campo a campo (los campos ausentes en la variante conservan el valor base) y devuelve un `TextBlock` nuevo; no hace aritmética ni sustituye tokens numéricos (D-07/D-08: las fórmulas se muestran tal cual).

Tests (todos bajo `engine/__tests__/`, ejecutados por el proyecto `engine` de Vitest):
- `fixtures/tiny-game.json`: juego falso de 6 pasos con dos secciones, la segunda con `repeats:true` y 3 pasos. Es obligatorio, no opcional: el contenido real de esta fase no tiene sección repetible y es la única forma de probar el cierre de bucle exigido por TECH-03.
- `navigator.test.ts`, `expand.test.ts` (puedes fusionarlo en navigator.test.ts si queda más claro), `schema.test.ts`, `resolve.test.ts`, `content.test.ts` cubriendo cada punto del bloque `<behavior>` de esta tarea.
- `schema.test.ts` debe construir sus casos inválidos en el propio test (objetos literales mutando una base válida), no ficheros sueltos.

Contenido:
- `content/games-index.ts`: array exportado `games` con `{ id: 'marvel-champions', title: 'Marvel Champions', status: 'available' }` y `{ id: 'warhammer-40k', title: 'Warhammer 40.000', status: 'coming-soon' }`, tipado con un `GameIndexEntry` local.
- `content/marvel-champions.json`: `gameId: "marvel-champions"`, `title: "Marvel Champions: El Juego de Cartas"`, `locale: "es"`, `contentVersion: 1`, una única sección `{ id: "setup", title: "Preparación", repeats: false }` con una única fase `{ id: "setup.heroes", title: "HÉROES", summaryLabel: "Héroes elegidos y dial ajustado" }` y **exactamente los 3 pasos del bloque HÉROES de la tabla de 21 pasos de 01-RESEARCH** (ids `setup.heroes.01`, `.02`, `.03`), con su `title` corto para el índice, su `text` en imperativo plural ≤90 caracteres, y su `citation` con `source: "rules-reference"`, `section` y `page: 49`. Ninguno de los tres lleva `warning`. Los 18 pasos restantes y la pantalla «mesa lista» los autora el plan 01-03: no los adelantes aquí ni dejes secciones `round` de relleno (Pitfall 5).
  </action>
  <verify>
    <automated>cd /Users/vcompanyb/TableGameAssistant && npx vitest run --project engine</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && ! grep -rE "from ['\"](vue|nuxt|#app|#imports)" engine/ && echo ENGINE_PURE_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && ! grep -rn "zod" app/ && echo NO_ZOD_IN_CLIENT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project engine` termina con código 0 y ejecuta al menos 12 tests
    - `grep -rE "from ['\"](vue|nuxt|#app|#imports)" engine/` no devuelve ninguna línea
    - `grep -rn "zod" app/` no devuelve ninguna línea
    - `engine/navigator.ts` contiene `export function next(`, `export function prev(` y `export function jumpTo(`
    - `engine/schema.ts` contiene `export const GameDefinitionSchema`, `.max(90)`, `.max(60)` y el comentario `TODO(fase 2)`
    - `engine/types.ts` contiene `warning?: string` y `kind: 'step' | 'summary'`, y NO contiene `perPlayer` ni `detail?:`
    - `engine/__tests__/fixtures/tiny-game.json` contiene exactamente una sección con `"repeats": true`
    - `content/marvel-champions.json` tiene 3 pasos con ids `setup.heroes.01`, `setup.heroes.02`, `setup.heroes.03`, cada uno con un objeto `citation`, y `"contentVersion": 1`
    - `content/games-index.ts` contiene `status: 'coming-soon'` para `warhammer-40k`
    - Un test de `schema.test.ts` demuestra que un `text` de 120 caracteres hace fallar `GameDefinitionSchema.parse`
  </acceptance_criteria>
  <done>El motor puro pasa sus tests de cierre de bucle, salto, clamp y reanudación estructural; el esquema Zod rechaza contenido mal formado en CI; y existe contenido real de Marvel Champions (bloque HÉROES) que valida contra el esquema.</done>
</task>

<task type="auto">
  <name>Tarea 3: Pantalla de paso navegable — runner, composables y las tres bandas de la UI</name>
  <files>app/pages/[game]/index.vue, app/components/AppHeader.vue, app/components/StepScreen.vue, app/components/NavBand.vue, app/composables/useGameContent.ts, app/composables/useGameSession.ts</files>
  <read_first>
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-UI-SPEC.md` — §Layout (las tres bandas con sus alturas exactas), §Component Inventory (props y nombres accesibles), §Interaction & State Coverage (pressed, sin estados deshabilitados en Siguiente/Atrás), §Typography, §Color, §Copywriting Contract
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-CONTEXT.md` — D-11 (tres zonas de cabecera y su forma en Fase 2), D-12 (sin barra de progreso), D-05 (aviso solo si el paso tiene trampa)
    - `.planning/research/ARCHITECTURE.md` §3 y §5 — la capa de composables es la ÚNICA costura entre el motor puro y la reactividad de Vue; componentes tontos
    - `.planning/research/PITFALLS.md` Pitfall 7 — hidratación SSR de estado de navegador
    - `engine/types.ts`, `engine/navigator.ts`, `engine/expand.ts`, `engine/resolve.ts` (creados en la tarea 2)
    - `app/app.vue`, `app/assets/css/main.css` (creados en la tarea 1)
  </read_first>
  <action>
`app/composables/useGameContent.ts`: import **estático** de `~~/content/marvel-champions.json` y de `~~/content/games-index.ts` (nunca `fetch`/`useFetch`, Anti-Patrón 3 de ARCHITECTURE). Expone `getGame(gameId: string): GameDefinition | null` resolviendo contra un mapa literal por id, y `games` (el índice). Castea el JSON al tipo `GameDefinition` importado de `~~/engine/types`; **no importa `engine/schema.ts`** — la validación Zod es exclusivamente de build/CI (TECH-02).

`app/composables/useGameSession.ts`: la única costura reactiva. Mantiene un `ref<EngineSession | null>`, expone `start(gameId, context)` que llama a `expand()`, y `next()`/`prev()`/`jumpTo()` que reasignan el ref con el resultado de las funciones puras del motor. Computeds derivados:
- `currentNode` = `session.sequence[session.cursor]`
- `currentText` = `resolveText(currentNode, session.context)`
- `sectionLabel` = el `sectionTitle` del nodo actual en mayúsculas (en esta fase siempre `PREPARACIÓN`); esta computed es el punto de extensión de D-11 para la Fase 2 (`RONDA 4 · Villano`), así que no la llames `setupLabel` ni la codifiques contra el id `setup`
- `position` = `{ current, total }` calculados **solo sobre los nodos con `kind === 'step'`** (`current` = posición 1-based del nodo actual entre ellos), y `null` cuando el nodo actual tiene `kind === 'summary'`
- `sessionContextLabel` = `` `${playerCount} jug · ${difficulty === 'expert' ? 'Experto' : 'Normal'}` ``

`app/components/AppHeader.vue`: banda de 64px de alto fijo, `bg-surface`, tres zonas en flex — izquierda `sectionLabel` + `· {current} de {total}` (omite el sufijo de posición cuando `position` es `null`) en token Label/700 color texto primario; derecha `sessionContext` en Label color texto secundario; extremo derecho un botón icono `≡` de 48×48dp con `aria-label="Abrir índice"` que emite `index-open`. La zona derecha debe ser un contenedor flex que admita más de dos hijos (la Fase 3 insertará ahí el icono de silencio). Nada de barra de progreso (D-12).

`app/components/StepScreen.vue`: banda de contenido flexible, `bg-background`, bloque **centrado verticalmente** (no anclado arriba) y limitado a una columna de ~900-960px con padding lateral `2xl`. Dentro: la frase de acción en token Display/700 color texto primario, centrada; y —solo si `warningText` no es nulo— una línea de aviso a 24px (`lg`) por debajo, con el glifo `⚠` y el texto en token Body color `warning`. Renderiza siempre con interpolación `{{ }}`; **prohibido `v-html`** (ver `<threat_model>`).

`app/components/NavBand.vue`: banda de 96px de alto fijo, `bg-surface`, dos controles — «‹ Atrás» a la izquierda ocupando ~35% del ancho, sin relleno de color, etiqueta en token Label sobre `surface`; y el botón principal a la derecha ocupando ~65%, relleno `bg-accent`, etiqueta `nextLabel` (por defecto `SIGUIENTE`, con el chevron `›`) en token Label color `on-accent` (`#0B1220`, nunca blanco). Ambos ≥48px de alto real. Estado pressed en `mousedown`/`touchstart`: oscurecido del 4% y escala del 98%, liberado en `touchend`/`mouseup`. **Ningún estado deshabilitado en esta fase** (01-UI-SPEC §Disabled states).

`app/pages/[game]/index.vue`: lee el parámetro de ruta, obtiene el `GameDefinition` con `useGameContent().getGame()`, y si no existe muestra un mensaje neutro. Llama a `start()` con un contexto de sesión **provisional y explícitamente marcado** `{ playerCount: 2, difficulty: 'normal' }`, con un comentario `// STUB: sustituido por el mini-setup en el plan 01-02 (SETUP-01/02/03)`. Compone las tres bandas en un layout de columna a altura completa (`h-dvh`, `flex flex-col`, cabecera y banda de navegación con altura fija, contenido `flex-1`) y conecta los eventos `next`/`back` de `NavBand` a `useGameSession()`. El botón `≡` aún no abre nada (el índice llega en el plan 01-05): deja el handler vacío con un comentario que lo indique. Todo el bloque que dependa de estado de navegador se monta bajo un guard de cliente (`<ClientOnly>` o un `ref` fijado en `onMounted`) mostrando un estado neutro de carga mientras tanto — en esta tarea aún no hay `localStorage`, pero la estructura del guard debe existir ya para que el plan 01-04 no tenga que reorganizar la página (Pitfall 7).
  </action>
  <verify>
    <automated>cd /Users/vcompanyb/TableGameAssistant && npm run generate && test -f .output/public/marvel-champions/index.html && echo RUNNER_PRERENDER_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && ! grep -rn "v-html" app/ && echo NO_VHTML_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && grep -q 'aria-label="Abrir índice"' app/components/AppHeader.vue && echo A11Y_OK</automated>
    <human-check>Con `npm run dev`, en Chrome con emulación de tablet en horizontal, `/marvel-champions` muestra el paso 1 del bloque HÉROES; SIGUIENTE avanza a los pasos 2 y 3 y se queda en el 3; Atrás retrocede hasta el 1 y se queda ahí. Al rotar el emulador a vertical aparece «Girad la tablet» y la app desaparece. La consola no muestra warnings de hidratación.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run generate` termina con código 0 y produce `.output/public/marvel-champions/index.html`
    - `grep -rn "v-html" app/` no devuelve ninguna línea
    - `app/components/NavBand.vue` contiene `SIGUIENTE`, la clase de relleno `bg-accent` y una altura de banda de 96px (`h-24` o equivalente explícito)
    - `app/components/AppHeader.vue` contiene `aria-label="Abrir índice"` y una altura de banda de 64px (`h-16` o equivalente explícito)
    - `app/components/StepScreen.vue` renderiza la línea de aviso solo bajo `v-if` sobre `warningText` y usa la clase de color `text-warning`
    - `app/composables/useGameSession.ts` importa de `~~/engine/navigator` y de `~~/engine/expand`; `app/composables/useGameContent.ts` NO importa `~~/engine/schema`
    - `app/composables/useGameSession.ts` calcula `position.total` filtrando por `kind === 'step'` y devuelve `null` cuando el nodo actual es `kind === 'summary'`
    - `app/pages/[game]/index.vue` contiene el comentario `STUB` señalando el contexto de sesión provisional
    - Los controles Atrás y SIGUIENTE no declaran ningún atributo `disabled` en esta fase
  </acceptance_criteria>
  <done>Un usuario recorre con SIGUIENTE/Atrás los 3 pasos reales del bloque HÉROES en `/marvel-champions`, con la cabecera de tres zonas, el tema oscuro, los tamaños táctiles del contrato y el guardia de orientación funcionando; la ruta se prerenderiza a estático.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Esta fase **no tiene backend, ni autenticación, ni cuentas, ni llamadas de red en runtime**: el contenido se importa en build y se sirve como estático desde un CDN. No existe frontera cliente↔servidor de aplicación. Las fronteras reales son solo tres:

| Boundary | Description |
|----------|-------------|
| registro npm → máquina de build/repo | La instalación de dependencias trae código de terceros que se ejecuta en build y se empaqueta en el bundle |
| contenido JSON del repo → bundle del navegador | El texto de los pasos se interpola en el DOM; si se renderizara como HTML crudo sería ejecución de código no confiado |
| usuario/DevTools → `localStorage` del propio navegador | El usuario puede editar a mano su propio estado persistido (no hay más usuarios que él; no es escalada de privilegio, es corrupción de datos propios) |

Categorías ASVS explícitamente **no aplicables** por diseño del proyecto, no por omisión: V2 Autenticación, V3 Sesión de servidor, V4 Control de acceso, V6 Criptografía. No se rellenan con controles genéricos.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Interpolación del texto de contenido en `StepScreen.vue`/`AppHeader.vue` | mitigate | Prohibición dura de `v-html` en todo `app/`: `text`, `warning`, `title` y `citation` se renderizan siempre con `{{ }}`. Verificado por el gate `grep -rn "v-html" app/` en la tarea 3 |
| T-01-02 | Tampering | `content/marvel-champions.json` llegando malformado al bundle | mitigate | `GameDefinitionSchema` (Zod) ejecutado por Vitest en `.github/workflows/ci.yml`; un fichero inválido rompe CI antes de llegar a la tablet (TECH-02). Zod no se envía al cliente |
| T-01-SC | Tampering | Instalación de dependencias npm (tarea 1) | mitigate | `01-RESEARCH.md` §Package Legitimacy Audit ya ejecutó `slopcheck 0.6.1` sobre los 9 paquetes: **cero [SLOP]**. `nuxt` y `vitest` salieron `[SUS]` únicamente por similitud léxica con `next`/`vite`, y quedaron verificados de forma cruzada (repositorios oficiales `github.com/nuxt/nuxt` y `github.com/vitest-dev/vitest`, 9 y ~5 años de antigüedad, decenas de millones de descargas semanales, `scripts.postinstall` vacío en los 9). Se tratan como `[VERIFIED: npm registry]` sin checkpoint adicional por decisión explícita y razonada del investigador. **El ejecutor no debe instalar ningún paquete fuera de la lista de la tarea 1**; cualquier dependencia adicional que se descubra necesaria requiere parar y consultar |
| T-01-03 | Information Disclosure | Bundle estático publicado | accept | La app no maneja PII, credenciales ni secretos: todo su contenido son reglas de juego redactadas de nuevo y pensadas para ser públicas. No hay nada que filtrar |
| T-01-04 | Denial of Service | Servido del sitio | transfer | Sitio 100% estático servido por Netlify; la disponibilidad es responsabilidad del CDN y, tras la Fase 4, la caché offline elimina la dependencia de red durante la partida |
</threat_model>

<verification>
- `npx vitest run` termina con código 0 (proyecto `engine`).
- `npm run generate` termina con código 0 y produce `.output/public/index.html` y `.output/public/marvel-champions/index.html`.
- `grep -rE "from ['\"](vue|nuxt|#app|#imports)" engine/` no devuelve nada (frontera de pureza intacta).
- `grep -rn "zod" app/` no devuelve nada (Zod fuera del bundle de cliente).
- `grep -rn "v-html" app/` no devuelve nada.
- Comprobación manual en Chrome con emulación de tablet horizontal: los 3 pasos se recorren con SIGUIENTE/Atrás sin warnings de hidratación en consola; al rotar a vertical aparece el aviso «Girad la tablet».
</verification>

<success_criteria>
- Existe un proyecto Nuxt 4.5.x en la raíz del repo que genera salida estática con `nuxt generate` y sin `ssr: false`.
- El motor de flujo vive en `engine/`, es puro y sus tests cubren cierre de bucle, salto y clamp (TECH-03 parcial; la reanudación desactualizada llega en el plan 01-04).
- Un fichero de contenido mal formado hace fallar `npm run test`, que es lo que ejecuta el workflow de CI (TECH-02).
- El contenido es un fichero JSON versionado en el repo, sin backend (TECH-01), y el motor no conoce ninguna palabra de Marvel Champions (TECH-04).
- La pantalla de paso implementa las tres bandas, los tokens de color/tipografía/espaciado y los tamaños táctiles de `01-UI-SPEC.md` (UI-01 por criterios proxy según D-19, UI-02, UI-03, UI-05) y el guardia de orientación (UI-04).
- SIGUIENTE es el control más prominente y Atrás funciona (FLOW-01, FLOW-02); la cabecera muestra sección y posición (FLOW-05, base para D-11).
- El esquema del contenido ya lleva el campo `citation` (CONT-08 como requisito de datos, D-06) y el campo `speech` reservado para la Fase 3.
</success_criteria>

<output>
Crea `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-01-SUMMARY.md` al terminar.
Anota explícitamente en el SUMMARY: el directorio de publicación real confirmado por la build (resolución de la asunción A1 de 01-RESEARCH) y la versión exacta de Nuxt instalada.
</output>
