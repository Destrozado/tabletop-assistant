---
phase: 01-motor-de-flujo-selector-y-preparaci-n-de-mesa
plan: 08
type: execute
wave: 3
depends_on: ["01-07"]
files_modified:
  - app/composables/useGameContent.ts
  - app/composables/useGameSession.ts
  - app/components/AppHeader.vue
  - app/components/StepScreen.vue
  - app/components/NavBand.vue
  - app/pages/[game]/index.vue
autonomous: true
requirements: [FLOW-01, FLOW-02, FLOW-05, UI-01, UI-02, UI-03, UI-05]
user_setup: []

must_haves:
  truths:
    - "Un usuario abre `/marvel-champions` en Chrome (emulación tablet, horizontal) y ve el primer paso real de la preparación con su frase de acción grande sobre tema oscuro (UI-01, UI-05, D-19: criterios proxy objetivos, no validación física)"
    - "Al pulsar SIGUIENTE avanza al paso siguiente y al pulsar ‹ Atrás retrocede; SIGUIENTE es el control más prominente de la pantalla (FLOW-01, FLOW-02)"
    - "La cabecera muestra sección y posición a la izquierda y contexto de partida a la derecha, con el control ≡ en el extremo, y sobrevive sin rediseño al formato `RONDA 4 · Villano · 3 de 6` de la Fase 2, sin barra de progreso (FLOW-05, D-11, D-12)"
    - "Los controles Atrás y SIGUIENTE están en la banda inferior, al alcance del pulgar, con al menos 48px de alto real (UI-02, UI-03)"
    - "El contenido llega a la pantalla por import estático en build, sin fetch en runtime y sin que Zod entre en el bundle del navegador"
  artifacts:
    - path: "app/composables/useGameSession.ts"
      provides: "La única costura entre el motor puro y la reactividad de Vue"
      exports: ["useGameSession"]
    - path: "app/composables/useGameContent.ts"
      provides: "getGame(gameId) y games por import estático del contenido"
      exports: ["useGameContent"]
    - path: "app/components/AppHeader.vue"
      provides: "Banda superior de tres zonas con el control de índice"
      contains: "Abrir índice"
    - path: "app/components/StepScreen.vue"
      provides: "Frase de acción centrada y línea de aviso opcional"
      contains: "text-warning"
    - path: "app/components/NavBand.vue"
      provides: "Banda inferior con Atrás y el botón principal SIGUIENTE"
      contains: "SIGUIENTE"
    - path: "app/pages/[game]/index.vue"
      provides: "Runner: compone las tres bandas y cablea la navegación"
      contains: "useGameSession"
  key_links:
    - from: "app/composables/useGameSession.ts"
      to: "engine/navigator.ts"
      via: "import estático desde ~~/engine"
      pattern: "from '~~/engine/navigator'"
    - from: "app/composables/useGameContent.ts"
      to: "content/marvel-champions.json"
      via: "import estático en build (nunca fetch en runtime)"
      pattern: "import .*content/marvel-champions.json"
    - from: "app/pages/[game]/index.vue"
      to: "app/components/NavBand.vue"
      via: "eventos next/back conectados a useGameSession"
      pattern: "@next|@back"
---

## Phase Goal

**Como** grupo de jugadores con una tablet apoyada junto a la mesa, **quiero** elegir Marvel Champions, indicar jugadores y dificultad, y recorrer paso a paso toda la preparación de mesa con «Siguiente»/«Atrás», cabecera de orientación, índice de salto y progreso guardado, **para que** podamos montar la partida entera sin abrir el reglamento y sin saltarnos ningún paso.

## Orden de ejecución de la fase

Este es el **tercero y último de los planes que componen el Esqueleto Andante**: 01-01 (scaffold) → 01-07 (motor y contenido) → **01-08 (este, runner)**. Al terminar este plan el esqueleto está cerrado de extremo a extremo y empiezan las rebanadas de anchura (01-02 selector y mini-setup, 01-03 contenido completo). Los números 07 y 08 son posteriores en numeración pero anteriores en ejecución a 01-02..01-06; la verdad de la ejecución son `wave` y `depends_on`, nunca el número de fichero.

## Rebanada de este plan (Esqueleto Andante, parte 3 de 3)

Abro `/marvel-champions` en Chrome con emulación de tablet en horizontal, veo el primer paso **real** de la preparación (leído del JSON de contenido validado, a través del motor puro), pulso SIGUIENTE y avanzo, pulso Atrás y retrocedo. Con `npm run generate` la ruta se prerenderiza a estático desplegable. Con esto la tubería entera —contenido → esquema → motor → composable → componentes → build— queda probada antes de añadir ninguna anchura.

<objective>
Cerrar el esqueleto andante con la pantalla de paso navegable: los dos composables que son la única costura entre el motor puro y Vue, las tres bandas de la UI de `01-UI-SPEC.md` y la página runner que las compone.

Purpose: probar de extremo a extremo que el contenido validado llega a la pantalla y que SIGUIENTE/Atrás mueven el motor, con la mínima anchura posible, para que las rebanadas siguientes solo añadan pantallas y contenido sin renegociar decisiones estructurales.
Output: `/marvel-champions` recorriendo los 3 pasos reales del bloque HÉROES, prerenderizado a estático.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-CONTEXT.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-UI-SPEC.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-01-SUMMARY.md
@.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-07-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
Ya existentes (planes 01-01 y 01-07), consumidos aquí **sin explorar el código**:

Del plan 01-01:
- Alias `~~/engine/...` y `~~/content/...`; `~/...` dentro de `app/`.
- Utilidades de Tailwind: `bg-background`, `bg-surface`, `text-primary-text`, `text-secondary-text`, `bg-accent`, `text-on-accent`, `text-warning`, `text-destructive`, `p-xs|sm|md|lg|xl|2xl|3xl`, `text-display|heading|body|label`.
- `app/app.vue` ya monta `#app-root` (oculto en vertical) con `<NuxtPage />` y `#orientation-guard`: esta página no tiene que preocuparse por la orientación.

Del plan 01-07:
- `~~/engine/types` → `Difficulty`, `TextBlock`, `StepDefinition`, `PhaseDefinition`, `SectionDefinition`, `GameDefinition`, `FlatStepNode`, `RuntimeStepNode`, `SessionContext`, `EngineSession`
- `~~/engine/expand` → `expand(game, context): EngineSession`
- `~~/engine/navigator` → `next(s)`, `prev(s)`, `jumpTo(s, runtimeId)` — puras, devuelven sesión nueva
- `~~/engine/resolve` → `resolveText(node, context): TextBlock`
- `~~/engine/schema` → **prohibido importar desde `app/`** (Zod es devDependency, Node/CI only)
- `~~/content/marvel-champions.json` → `GameDefinition` con la sección `setup` y 3 pasos
- `~~/content/games-index.ts` → `games: { id, title, status }[]` (el rótulo visible es `title`, no `name`)

Contratos que este plan CREA y que consumen los planes 01-02, 01-04 y 01-05:

- `app/composables/useGameContent.ts` → `getGame(gameId: string): GameDefinition | null`, `games`
- `app/composables/useGameSession.ts` → `session` (ref), `start(gameId, context)`, `next()`, `prev()`, `jumpTo(runtimeId)`, `currentNode` (computed), `currentText` (computed `TextBlock` ya resuelto), `sectionLabel` (computed string), `position` (computed `{current, total} | null`), `sessionContextLabel` (computed string, p. ej. `3 jug · Normal`)
- Props de componentes (idénticos a `01-UI-SPEC.md` §Component Inventory):
  - `AppHeader`: `sectionLabel: string`, `position: {current:number,total:number} | null`, `sessionContext: string`, emite `index-open`
  - `StepScreen`: `actionText: string`, `warningText: string | null`
  - `NavBand`: `nextLabel?: string` (por defecto `SIGUIENTE`), emite `back` y `next`
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Los dos composables — la única costura entre el motor puro y Vue</name>
  <files>app/composables/useGameContent.ts, app/composables/useGameSession.ts</files>
  <read_first>
    - `.planning/research/ARCHITECTURE.md` §3 y §5 — la capa de composables es la ÚNICA costura entre el motor puro y la reactividad de Vue; los componentes son tontos; Anti-Patrón 3 (nada de `fetch`/`useFetch` para el contenido)
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-CONTEXT.md` — D-11 (las tres zonas de cabecera y su forma en la Fase 2), D-12 (sin barra de progreso), D-03 (el paso `summary` no cuenta como posición)
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-UI-SPEC.md` §Copywriting Contract (formato exacto de la posición y del contexto de sesión)
    - `engine/types.ts`, `engine/expand.ts`, `engine/navigator.ts`, `engine/resolve.ts` (creados en el plan 01-07)
  </read_first>
  <action>
`app/composables/useGameContent.ts`: import **estático** de `~~/content/marvel-champions.json` y de `~~/content/games-index.ts` (nunca `fetch`/`useFetch`, Anti-Patrón 3 de ARCHITECTURE — el contenido se conoce en build y debe funcionar sin red). Expone `getGame(gameId: string): GameDefinition | null` resolviendo contra un mapa literal por id, y `games` (el índice, con su campo `title`). Castea el JSON al tipo `GameDefinition` importado de `~~/engine/types`; **no importa `engine/schema.ts`** — la validación Zod es exclusivamente de build/CI (TECH-02) y `zod` no debe entrar en el bundle del navegador.

`app/composables/useGameSession.ts`: la única costura reactiva del proyecto. Mantiene un `ref<EngineSession | null>`, expone `start(gameId, context)` que llama a `expand()`, y `next()`/`prev()`/`jumpTo()` que **reasignan** el ref con el resultado de las funciones puras del motor (nunca mutan la sesión in situ: el motor devuelve objetos nuevos). Ningún componente puede importar `~~/engine/*` directamente: si algún componente lo necesita, falta una computed aquí.

Computeds derivadas:
- `currentNode` = `session.sequence[session.cursor]`
- `currentText` = `resolveText(currentNode, session.context)`
- `sectionLabel` = el `sectionTitle` del nodo actual en mayúsculas (en esta fase siempre `PREPARACIÓN`). Esta computed es el punto de extensión de D-11 para la Fase 2 (`RONDA 4 · Villano`), así que **no la llames `setupLabel`** ni la codifiques contra el id `setup`: derívala del nodo.
- `position` = `{ current, total }` calculados **solo sobre los nodos con `kind === 'step'`** (`current` = posición 1-based del nodo actual entre ellos), y `null` cuando el nodo actual tiene `kind === 'summary'` (D-03: la pantalla de repaso no es «el paso 22 de 22»)
- `sessionContextLabel` = `` `${playerCount} jug · ${difficulty === 'expert' ? 'Experto' : 'Normal'}` ``

Todas las computeds devuelven un valor neutro seguro cuando `session` es `null` (cadena vacía, `null`), sin lanzar: la página monta antes de que exista sesión.
  </action>
  <verify>
    <automated>cd /Users/vcompanyb/TableGameAssistant && npm run generate && echo BUILD_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && ! grep -rn "zod" app/ && echo NO_ZOD_IN_CLIENT_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && ! grep -rnE "useFetch|\\\$fetch|fetch\(" app/composables/ && echo NO_RUNTIME_FETCH_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && grep -q "~~/engine/navigator" app/composables/useGameSession.ts && grep -q "~~/engine/expand" app/composables/useGameSession.ts && grep -q "kind === 'step'" app/composables/useGameSession.ts && echo SEAM_OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/composables/useGameSession.ts` importa de `~~/engine/navigator`, `~~/engine/expand` y `~~/engine/resolve`, y expone `start`, `next`, `prev`, `jumpTo`, `currentNode`, `currentText`, `sectionLabel`, `position`, `sessionContextLabel`
    - `position.total` se calcula filtrando por `kind === 'step'` y `position` es `null` cuando el nodo actual es `kind === 'summary'`
    - `sectionLabel` se deriva del nodo actual y no contiene la cadena literal `'setup'` ni `'PREPARACIÓN'` codificada
    - `app/composables/useGameContent.ts` NO importa `~~/engine/schema` y no usa `fetch`, `$fetch` ni `useFetch`
    - `grep -rn "zod" app/` no devuelve ninguna línea
    - `npm run generate` termina con código 0
  </acceptance_criteria>
  <done>Existe una única costura tipada entre el motor puro y Vue: los componentes podrán ser tontos porque toda la derivación vive aquí.</done>
</task>

<task type="auto">
  <name>Tarea 2: Las tres bandas de la UI y la página runner</name>
  <files>app/components/AppHeader.vue, app/components/StepScreen.vue, app/components/NavBand.vue, app/pages/[game]/index.vue</files>
  <read_first>
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-UI-SPEC.md` — §Layout (las tres bandas con sus alturas exactas), §Component Inventory (props y nombres accesibles), §Interaction & State Coverage (pressed; sin estados deshabilitados en Siguiente/Atrás), §Typography, §Color, §Spacing Scale (suelo táctil), §Copywriting Contract
    - `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-CONTEXT.md` — D-11 (tres zonas de cabecera y su forma en Fase 2), D-12 (sin barra de progreso), D-05 (aviso solo si el paso tiene trampa), D-19 (UI-01 se comprueba por criterios proxy en esta fase)
    - `.planning/research/PITFALLS.md` Pitfall 7 — hidratación SSR de estado de navegador
    - `app/composables/useGameSession.ts` y `app/composables/useGameContent.ts` (tarea 1)
    - `app/assets/css/main.css` y `app/app.vue` (plan 01-01)
  </read_first>
  <action>
Los tres componentes son **tontos**: reciben props, emiten eventos, no importan nada de `~~/engine` ni de los composables.

`app/components/AppHeader.vue`: banda de 64px de alto fijo, `bg-surface`, tres zonas en flex — izquierda `sectionLabel` + `· {current} de {total}` (omite el sufijo de posición cuando `position` es `null`) en token Label/700 color texto primario; derecha `sessionContext` en Label color texto secundario; extremo derecho un botón icono `≡` de 48×48dp con `aria-label="Abrir índice"` que emite `index-open`. La zona derecha debe ser un contenedor flex que admita **más de dos hijos** (la Fase 3 insertará ahí el icono de silencio). Nada de barra de progreso (D-12).

`app/components/StepScreen.vue`: banda de contenido flexible, `bg-background`, bloque **centrado verticalmente** (no anclado arriba) y limitado a una columna de ~900-960px con padding lateral `2xl`. Dentro: la frase de acción en token Display/700 color texto primario, centrada; y —solo si `warningText` no es nulo— una línea de aviso a 24px (`lg`) por debajo, con el glifo `⚠` y el texto en token Body color `warning`. Renderiza siempre con interpolación `{{ }}`; **prohibido `v-html`** (ver `<threat_model>`).

`app/components/NavBand.vue`: banda de 96px de alto fijo, `bg-surface`, dos controles — «‹ Atrás» a la izquierda ocupando ~35% del ancho, sin relleno de color, etiqueta en token Label sobre `surface`; y el botón principal a la derecha ocupando ~65%, relleno `bg-accent`, etiqueta `nextLabel` (por defecto `SIGUIENTE`, con el chevron `›`) en token Label color `on-accent` (`#0B1220`, **nunca blanco**). Ambos ≥48px de alto real. Estado pressed en `mousedown`/`touchstart`: oscurecido del 4% y escala del 98%, liberado en `touchend`/`mouseup`. **Ningún estado deshabilitado en esta fase** (01-UI-SPEC §Disabled states): en el primer y el último paso los botones siguen activos y el motor hace clamp.

`app/pages/[game]/index.vue`: lee el parámetro de ruta, obtiene el `GameDefinition` con `useGameContent().getGame()`, y si no existe muestra un mensaje neutro (sin filtrar el id ni sugerir juegos). Llama a `start()` con un contexto de sesión **provisional y explícitamente marcado** `{ playerCount: 2, difficulty: 'normal' }`, con un comentario `// STUB: sustituido por el mini-setup en el plan 01-02 (SETUP-01/02/03)`. Compone las tres bandas en un layout de columna a altura completa (`h-dvh`, `flex flex-col`, cabecera y banda de navegación con altura fija, contenido `flex-1`) y conecta los eventos `next`/`back` de `NavBand` a `useGameSession()`. El botón `≡` aún no abre nada (el índice llega en el plan 01-05): deja el handler vacío con un comentario que lo indique.

Todo el bloque que dependa de estado de navegador se monta bajo un **guard de cliente** (`<ClientOnly>` o un `ref` fijado en `onMounted`) mostrando un estado neutro de carga mientras tanto — en esta tarea aún no hay `localStorage`, pero la estructura del guard debe existir ya para que los planes 01-02 y 01-04 no tengan que reorganizar la página (Pitfall 7).
  </action>
  <verify>
    <automated>cd /Users/vcompanyb/TableGameAssistant && npm run generate && test -f .output/public/marvel-champions/index.html && echo RUNNER_PRERENDER_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && ! grep -rn "v-html" app/ && echo NO_VHTML_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && grep -q 'aria-label="Abrir índice"' app/components/AppHeader.vue && grep -q "SIGUIENTE" app/components/NavBand.vue && grep -q "bg-accent" app/components/NavBand.vue && echo UI_CONTRACT_OK</automated>
    <automated>cd /Users/vcompanyb/TableGameAssistant && ! grep -rn "~~/engine" app/components/ && ! grep -rn "disabled" app/components/NavBand.vue && echo DUMB_COMPONENTS_OK</automated>
    <human-check>Con `npm run dev`, en Chrome con emulación de tablet en horizontal, `/marvel-champions` muestra el paso 1 del bloque HÉROES; SIGUIENTE avanza a los pasos 2 y 3 y se queda en el 3; Atrás retrocede hasta el 1 y se queda ahí. La cabecera muestra `PREPARACIÓN · 1 de 3` a la izquierda y `2 jug · Normal` a la derecha. Al rotar el emulador a vertical aparece «Girad la tablet» y la app desaparece. La consola no muestra warnings de hidratación.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run generate` termina con código 0 y produce `.output/public/marvel-champions/index.html`
    - `grep -rn "v-html" app/` no devuelve ninguna línea
    - `app/components/NavBand.vue` contiene `SIGUIENTE`, la clase de relleno `bg-accent`, la etiqueta en `text-on-accent` y una altura de banda de 96px (`h-24` o equivalente explícito)
    - `app/components/AppHeader.vue` contiene `aria-label="Abrir índice"` y una altura de banda de 64px (`h-16` o equivalente explícito)
    - `app/components/StepScreen.vue` renderiza la línea de aviso solo bajo `v-if` sobre `warningText` y usa la clase de color `text-warning`
    - Ningún fichero de `app/components/` importa de `~~/engine` (los componentes son tontos)
    - Los controles Atrás y SIGUIENTE no declaran ningún atributo `disabled` en esta fase
    - `app/pages/[game]/index.vue` contiene el comentario `STUB` señalando el contexto de sesión provisional y monta el contenido bajo un guard de cliente
  </acceptance_criteria>
  <done>Un usuario recorre con SIGUIENTE/Atrás los 3 pasos reales del bloque HÉROES en `/marvel-champions`, con la cabecera de tres zonas, el tema oscuro, los tamaños táctiles del contrato y el guardia de orientación funcionando; la ruta se prerenderiza a estático.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Sin cambios estructurales respecto a 01-01 y 01-07: sin backend, sin red en runtime, sin cuentas, sin almacenamiento todavía. Este plan cierra por el lado del renderizado la frontera que 01-07 abrió por el lado de los datos.

| Boundary | Description |
|----------|-------------|
| contenido JSON del repo → DOM del navegador | El texto de los pasos se interpola en el DOM; si se renderizara como HTML crudo sería ejecución de código no confiado (**frontera que cierra este plan**) |
| parámetro de ruta `/[game]` → resolución de contenido | El id de juego viene de la URL, es decir, del usuario |
| devDependencies (`zod`) → bundle de cliente | Verificado aquí por gate: ningún fichero de `app/` importa `zod` |

Categorías ASVS no aplicables por diseño, igual que en 01-01: V2, V3, V4, V6.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Interpolación del texto de contenido en `StepScreen.vue` y `AppHeader.vue` | mitigate | Prohibición dura de `v-html` en todo `app/`: `text`, `warning`, `title` y `citation` se renderizan siempre con `{{ }}`. Verificado por el gate `grep -rn "v-html" app/` en la tarea 2, y reverificado por los planes 01-02, 01-04 y 01-05 |
| T-01-06 | Spoofing | Ruta `/[game]` con un id de juego arbitrario en la URL | accept | `getGame()` devuelve `null` para cualquier id desconocido y la página muestra un mensaje neutro que no refleja el id recibido. No hay recurso protegido que suplantar ni datos que exponer en una app sin cuentas ni backend |
| T-01-19 | Information Disclosure | `zod` cruzando al bundle de cliente | mitigate | `useGameContent.ts` importa el JSON y `~~/engine/types`, nunca `~~/engine/schema`. Gate `grep -rn "zod" app/` en la tarea 1 |
| T-01-03 | Information Disclosure | Bundle estático publicado | accept | Sin PII, credenciales ni secretos. Sin cambios respecto a 01-01 |
</threat_model>

<verification>
- `npm run generate` termina con código 0 y produce `.output/public/index.html` y `.output/public/marvel-champions/index.html`.
- `grep -rn "zod" app/` no devuelve nada (Zod fuera del bundle de cliente).
- `grep -rn "v-html" app/` no devuelve nada.
- `grep -rn "~~/engine" app/components/` no devuelve nada (componentes tontos).
- Comprobación manual en Chrome con emulación de tablet horizontal: los 3 pasos se recorren con SIGUIENTE/Atrás sin warnings de hidratación en consola; al rotar a vertical aparece el aviso «Girad la tablet».
</verification>

<success_criteria>
- La pantalla de paso implementa las tres bandas, los tokens de color/tipografía/espaciado y los tamaños táctiles de `01-UI-SPEC.md` (UI-01 por criterios proxy según D-19, UI-02, UI-03, UI-05).
- SIGUIENTE es el control más prominente y Atrás funciona (FLOW-01, FLOW-02); la cabecera muestra sección y posición (FLOW-05, base para D-11).
- El Esqueleto Andante queda cerrado: scaffold + rutas + lectura real de contenido validado + motor + UI + build estático, sin ningún stub de datos.
</success_criteria>

<output>
Crea `.planning/phases/01-motor-de-flujo-selector-y-preparaci-n-de-mesa/01-08-SUMMARY.md` al terminar.
Anota explícitamente en el SUMMARY: que el Esqueleto Andante (planes 01-01 + 01-07 + 01-08) queda cerrado, y cualquier ajuste de layout que haya hecho falta respecto a las alturas de banda del contrato de UI.
</output>
