---
phase: 06-selecci-n-de-villano-h-roes-y-jugadores
plan: 07
type: execute
wave: 4
depends_on: [06-03, 06-06]
files_modified:
  - app/components/PlayerModal.vue
  - app/components/VillainPickerModal.vue
  - app/components/StepScreen.vue
autonomous: false
requirements: [SEL-01, SEL-02, SEL-03, SEL-04, SEL-05, SEL-06, SEL-07, SEL-08, SEL-09]

must_haves:
  truths:
    - "Un grupo puede elegir villano, héroe y nombre de cada jugador dentro del paso, y recargar la página conserva exactamente esa selección (SEL-08, criterio nº 4 del ROADMAP)"
    - "Un grupo que no toca ningún selector juega exactamente como en v1.7 (SEL-09, criterio nº 5 del ROADMAP)"
    - "Escribir un nombre con espacios dentro del modal NO avanza el paso (D-12, la trampa nombrada explícitamente en CONTEXT.md)"
    - "El aviso de héroe repetido se ve, no se puede tocar y no impide continuar (SEL-07/D-16/D-32)"
    - "`git diff` sobre `content/marvel-champions.json` no toca ni un carácter de ningún `text` ni `speech`, y los 35 clips de voz siguen intactos"
    - "El catálogo y los alias viajan dentro del bundle: con la red apagada, la selección sigue funcionando (CAT-06)"
    - "La fase no ha instalado ningún paquete ni ha añadido ningún entorno de test"
  artifacts:
    - path: ".planning/phases/06-selecci-n-de-villano-h-roes-y-jugadores/06-07-SUMMARY.md"
      provides: "Evidencia de los gates mecánicos y transcripción del veredicto humano"
      min_lines: 40
  key_links:
    - from: "verificación humana"
      to: "ROADMAP.md §Phase 6"
      via: "los 5 criterios de éxito comprobados uno a uno"
      pattern: "criterio"
---

<objective>
Cerrar la fase: pasar los gates mecánicos que demuestran lo que un test sí puede
demostrar, y llevar a cabo la **revisión humana en navegador** de todo lo que
estructuralmente no puede estar cubierto por tests en este proyecto.

Purpose: esta fase estrena tres superficies táctiles (una rejilla, dos modales) y el
primer campo de texto de toda la app, y `vitest.config.ts` no tiene entorno DOM
(`06-RESEARCH.md` Q7). Ser honestos sobre eso es parte del trabajo: el cableado de los
componentes **no está cubierto por ningún test automatizado** y se verifica a mano. El
ROADMAP ya lo anticipa y añade el límite: se verifica en viewport simulado (portátil o
móvil), **no** en la tablet de mesa, cuyo modelo y sistema operativo siguen sin
conocerse desde la Fase 1 — deuda heredada de v1.7 que esta fase tampoco resuelve y
que debe quedar anotada, no disimulada.

Output: `06-07-SUMMARY.md` con la evidencia de cada gate y la transcripción del
veredicto del usuario. Si la revisión detecta defectos, se corrigen aquí solo si son
de copy o de clase Tailwind; cualquier defecto de comportamiento se registra como gap
para `/gsd:plan-phase --gaps`, no se parchea a ojo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-selecci-n-de-villano-h-roes-y-jugadores/06-CONTEXT.md
@.planning/phases/06-selecci-n-de-villano-h-roes-y-jugadores/06-UI-SPEC.md
@.planning/phases/06-selecci-n-de-villano-h-roes-y-jugadores/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barrido de gates mecánicos y evidencia de que nada prohibido cambió</name>
  <files>—</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 6" (los 5 criterios de éxito, que son lo que estos gates y la Task 2 comprueban)
    - .planning/phases/06-selecci-n-de-villano-h-roes-y-jugadores/06-CONTEXT.md §D-02 (la consecuencia que el planner debía verificar explícitamente: ni un carácter de `text`/`speech`) y §D-19
    - engine/__tests__/voice-drift.test.ts (qué comprueba exactamente el gate de deriva de voz)
    - vitest.config.ts y package.json (para comprobar que no se añadió ni entorno de test ni dependencia)
  </read_first>
  <action>
Ejecutar los gates siguientes y **transcribir la salida real de cada uno** en el
SUMMARY (no un «pasó», sino la línea de salida relevante). Ninguno modifica ficheros.

1. **Suite completa:** `npm test`.
2. **Build de producción:** `npm run build`.
3. **Generación estática:** `npm run generate`.
4. **Diff del contenido (D-02, y el mismo criterio que la Fase 8 exigirá):**
   `git diff origin/main -- content/marvel-champions.json` (o contra el commit previo
   a la fase si `origin/main` no está disponible). Comprobar que la única diferencia es
   la línea `"selection": "characters"` y que `grep -E '^[-+].*"(text|speech)"'` sobre
   ese diff no devuelve nada.
5. **Gate de deriva de voz y recuento de clips:** `npx vitest run --project engine engine/__tests__/voice-drift.test.ts`
   y comprobar que `scripts/voice/manifest.json` sigue con 35 entradas y que
   `git status --porcelain public/audio scripts/voice` está vacío.
6. **Catálogo dentro del bundle (CAT-06, sin red):** tras `npm run generate`, buscar un
   id de héroe del catálogo dentro del JavaScript generado — p. ej.
   `grep -rl "captain-marvel" .output/public/_nuxt/ | head -3` debe devolver al menos
   un fichero. Es la prueba de que el catálogo viaja en el bundle y no se pide por red.
   Buscar también un alias español (p. ej. `Bruja Escarlata`) para confirmar que el
   mapa de D-05 viaja igual.
7. **Barridos de invariantes de la app:**
   - `grep -rn "v-html" app/` no devuelve nada (T-01-01).
   - `grep -rn "setup.heroes.01" app/` no devuelve nada (D-02/TECH-04).
   - `grep -rn "from 'zod'" app/` no devuelve nada (T-01-19).
   - `git diff --name-only engine/persistence.ts app/composables/usePersistedSession.ts app/composables/useStepShortcuts.ts` no devuelve nada (D-19/D-Q2).
   - `grep -n "formatVersion" engine/persistence.ts` sigue mostrando el mismo `!== 1`
     de siempre y `grep -c '"contentVersion": 13' content/marvel-champions.json`
     devuelve 1.
8. **Sin dependencias ni entornos nuevos:**
   `git diff --name-only package.json package-lock.json vitest.config.ts` no devuelve
   nada, y `grep -rn "jsdom\|happy-dom\|@vue/test-utils" package.json vitest.config.ts`
   no devuelve nada.

Si alguno falla, **parar y reportarlo** en vez de intentar arreglarlo por iniciativa
propia: un fallo en los gates 4, 5, 7 u 8 significa que algún plan anterior se salió
de su alcance, y eso se corrige donde se rompió.
  </action>
  <verify>
    <automated>npm test && npm run build && npx vitest run --project engine engine/__tests__/voice-drift.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` termina con exit code 0 y el SUMMARY transcribe la línea de recuento de tests
    - `npm run build` y `npm run generate` terminan con exit code 0
    - `git diff content/marvel-champions.json | grep -Ec '^[-+].*"(text|speech)"'` devuelve 0
    - `git diff -U0 content/marvel-champions.json | grep -c '^+[^+]'` devuelve 1
    - `node -e "const m=require('./scripts/voice/manifest.json');if(Object.keys(m.entries).length!==35)process.exit(1)"` termina con exit code 0
    - `git status --porcelain public/audio scripts/voice` no devuelve nada
    - `grep -rl "captain-marvel" .output/public/_nuxt/ | head -1` devuelve al menos un fichero
    - `grep -rn "v-html" app/` no devuelve nada
    - `grep -rn "setup.heroes.01" app/` no devuelve nada
    - `grep -rn "from 'zod'" app/` no devuelve nada
    - `git diff --name-only engine/persistence.ts app/composables/usePersistedSession.ts app/composables/useStepShortcuts.ts package.json package-lock.json vitest.config.ts` no devuelve nada
    - El SUMMARY incluye la salida literal (no parafraseada) de los gates 1, 4, 5 y 6
  </acceptance_criteria>
  <done>Los ocho gates mecánicos están pasados con su salida transcrita, y queda demostrado por diff que ni el texto, ni la voz, ni la persistencia, ni las dependencias se han tocado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Revisión humana en navegador de las tres superficies nuevas</name>
  <files>app/components/PlayerModal.vue, app/components/VillainPickerModal.vue, app/components/StepScreen.vue</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 6" (los 5 criterios de éxito y la nota de verificación humana: viewport simulado, no la tablet real, y no bloqueante para cerrar la fase)
    - .planning/phases/06-selecci-n-de-villano-h-roes-y-jugadores/06-UI-SPEC.md §Layout 1-4 y §"Copywriting Contract" (para contrastar lo que se ve con el contrato aprobado)
    - .planning/phases/06-selecci-n-de-villano-h-roes-y-jugadores/06-CONTEXT.md §D-03, §D-12, §D-16 (los tres comportamientos que más fácil es implementar mal sin que ningún test se entere)
  </read_first>
  <action>
Arrancar el servidor de desarrollo (`npm run dev`) y comprobar que responde en
`http://localhost:3000/marvel-champions`. Presentar al usuario las 13 comprobaciones
de `<how-to-verify>` numeradas, con el resultado esperado de cada una, y **detenerse a
esperar su veredicto**. No modificar ningún fichero antes de recibirlo; después, solo
si el defecto es de copy o de clase Tailwind, corregirlo en el fichero correspondiente
de `files_modified` y volver a pedir confirmación de esa comprobación concreta.
  </action>
  <what-built>
    La fase completa, funcionando en local: una rejilla de elección dentro del paso
    «Decidid, como grupo…», un modal de villano, un modal de jugador con campo de
    nombre y filtro de héroes, y la persistencia de todo ello dentro de la sesión.

    Lo que **no** está cubierto por ningún test automatizado y por eso se mira a mano:
    todo el cableado de componentes. `vitest.config.ts` solo tiene proyectos de entorno
    `node`, no hay jsdom ni `@vue/test-utils`, y esta fase decidió no inventar esa
    infraestructura (`06-RESEARCH.md` Q7). Los tests cubren el motor y las funciones
    puras; esta revisión cubre lo que se ve y lo que se toca.
  </what-built>
  <how-to-verify>
    Arranca `npm run dev` y abre `http://localhost:3000/marvel-champions` en un
    viewport apaisado de tablet (en DevTools, p. ej. 1024×768). Elige **3 jugadores /
    Normal** y avanza —o usa el índice `≡`— hasta el paso «Decidid, como grupo, qué
    villano vais a enfrentar y qué héroe llevará cada jugador».

    Recorre estas 13 comprobaciones y responde con el número de la que falle, si falla
    alguna:

    1. **Rejilla visible (SEL-01/SEL-03/D-03).** Bajo la frase grande se ve `ELECCIÓN`
       y cuatro filas: `Villano`, `Jugador 1`, `Jugador 2`, `Jugador 3`, todas con `—`
       a la derecha y un `›` azul. Una sola columna.
    2. **El texto del paso no ha encogido (D-04).** La frase se sigue viendo igual de
       grande que en cualquier otro paso.
    3. **SIGUIENTE no está bloqueado (SEL-09, criterio nº 5).** Sin tocar nada, pulsa
       `SIGUIENTE`: avanza sin aviso, sin confirmación y sin ningún hueco nuevo. Vuelve
       con `‹ ATRÁS`.
    4. **Modal de villano (SEL-02).** Toca la fila `Villano`: se abre un modal titulado
       `VILLANO` con `Sin elegir`, `Kang`, `Rhino`, `Ultron` en ese orden alfabético.
       Toca `Rhino`: se cierra solo y la fila pasa a decir `Rhino`. Vuelve a abrirlo:
       `Rhino` lleva un `✓`. Toca `Sin elegir`: la fila vuelve a `—`. Elige `Rhino` otra vez.
    5. **Modal de jugador y campo de nombre (SEL-04/SEL-06).** Toca `Jugador 1`. El
       modal se titula `JUGADOR 1`. **Ningún campo tiene el foco al abrir** (no salta
       el teclado en un dispositivo táctil). El campo `Nombre` muestra `Jugador 1`
       como texto editable.
    6. **La trampa de D-12 — la comprobación más importante de esta lista.** Con el
       modal abierto, borra el contenido del campo `Nombre` y escribe `Ana Maria`
       (con el espacio). El paso de detrás **no debe avanzar** ni una sola vez. Cierra
       el modal y pulsa la barra espaciadora: **ahora sí** debe avanzar (los atajos
       vuelven). Retrocede con `←`.
    7. **Filtro (SEL-05/D-08).** Abre `Jugador 1` otra vez y escribe en `Buscar héroe…`:
       `bruja` encuentra a Bruja Escarlata; bórralo y escribe `scarlet` (nombre inglés)
       — la encuentra; bórralo y escribe `wanda` (alter ego) — la encuentra; bórralo y
       escribe `BRÚJA` (mayúsculas y un acento de más) — la encuentra igual; bórralo y
       escribe `zzz` — sale `Ningún héroe coincide con «zzz»` y `Sin elegir` sigue
       visible arriba. Borra el filtro y elige `Thor`: guarda y cierra.
    8. **El nombre aparece en la rejilla.** La primera fila de jugador ahora se llama
       `Ana Maria` (no `Jugador 1`) y su valor es el nombre del héroe elegido.
    9. **Héroe repetido (SEL-07/D-16/D-32).** Abre `Jugador 2` y elige `Thor` también.
       Bajo la rejilla aparece una línea naranja `⚠ … llevan el mismo héroe`.
       **Intenta tocarla: no debe pasar absolutamente nada** — no tiene borde, no tiene
       chevron y no abre ningún modal. Y `SIGUIENTE` sigue avanzando sin quejarse.
    10. **El marcador `ya:` (D-16).** Abre `Jugador 3`. En la fila de `Thor` se ve
        `ya: Ana Maria y Jugador 2` en **gris**, nunca en naranja, y la fila **sigue
        siendo pulsable**. No la elijas; cierra con `Escape`.
    11. **Las tres vías de cierre y el foco (D-13).** Abre `Jugador 3` y ciérralo con
        `Escape`; ábrelo y ciérralo tocando la zona oscura de alrededor; ábrelo y
        ciérralo con `✕`. Las tres veces vuelves a la misma pantalla y nada se pierde.
        Tras cerrar, pulsa `Tab`: el foco debería estar de vuelta en la fila que
        abriste (se ve el recuadro de foco en esa fila).
    12. **Recargar conserva la selección (SEL-08, criterio nº 4).** Recarga la página
        (F5). Acepta continuar la partida guardada. Vuelve al paso de elección: el
        villano, los dos héroes y el nombre `Ana Maria` están exactamente como los
        dejaste.
    13. **El resto de la app no cambió (SEL-09) y la selección muere con la partida
        (D-14).** Avanza a cualquier otro paso: no hay rejilla ni hueco nuevo, se ve
        igual que siempre. Luego abre `≡` → `Partida terminada`, confirma, y empieza
        una partida nueva: los nombres vuelven a ser `Jugador 1`… y la rejilla vuelve a
        estar vacía.
    14. **Volver a la elección desde la ronda, sin código nuevo (D-18).** Empieza otra
        partida, elige villano y un héroe, y avanza hasta entrar en el bucle de ronda
        (la cabecera pasa a decir `RONDA 1 · …`). Avanza unos cuantos pasos más. Ahora
        abre `≡`: la preparación aparece al final, atenuada (D-24). Salta al paso
        «Decidid, como grupo…»: **la rejilla se ve con tu selección intacta**, puedes
        cambiar un héroe, y al volver con `≡` a la ronda **el número de ronda no ha
        cambiado**. Esta es la única comprobación de D-18, que es una decisión
        satisfecha *por inacción* (no se escribió código para ella): sin esta prueba
        nadie confirmaría que la inacción de verdad funciona.

    Extra opcional pero recomendable (2 minutos): en DevTools marca **Offline** y
    recarga. La app debe seguir funcionando y la lista de héroes debe seguir
    apareciendo — el catálogo y los alias viajan dentro del bundle, no se piden por red.

    **Límite honesto que debe quedar anotado:** todo esto se comprueba en un viewport
    simulado en portátil, no en la tablet de mesa, cuyo modelo y sistema operativo
    siguen sin conocerse desde la Fase 1. La comprobación pendiente en el dispositivo
    real —especialmente cómo se comporta el teclado en pantalla con el panel a
    `max-h-[80vh]` (`06-UI-SPEC.md` decisión 12)— se arrastra como deuda, igual que en
    v1.7. El ROADMAP dice que esta verificación **no es bloqueante** para cerrar la
    fase.
  </how-to-verify>
  <acceptance_criteria>
    - Las 14 comprobaciones se le presentan al usuario numeradas y con el resultado esperado explícito de cada una
    - El veredicto del usuario queda transcrito en el SUMMARY comprobación por comprobación (no un «todo bien» agregado)
    - Las comprobaciones 6, 9 y 12 —D-12, el `⚠` no pulsable y la persistencia tras recargar— se reportan de forma individual y explícita, porque son las tres que ningún test automatizado cubre y las tres que el CONTEXT.md señala como trampas
    - Cualquier defecto encontrado se clasifica en el SUMMARY como (a) copy o clase Tailwind, corregible en este plan, o (b) comportamiento, que se registra como gap para `/gsd:plan-phase --gaps` y NO se parchea aquí
    - El SUMMARY anota explícitamente que la verificación se hizo en viewport simulado y que el dispositivo real sigue pendiente (deuda heredada de v1.7)
  </acceptance_criteria>
  <verify>
    <human-check>Las 13 comprobaciones tienen veredicto individual del usuario, con las 6, 9 y 12 reportadas de forma explícita.</human-check>
  </verify>
  <done>El usuario ha recorrido las 13 comprobaciones en el navegador, su veredicto está transcrito una a una en el SUMMARY, y la deuda de dispositivo real queda anotada.</done>
  <resume-signal>Escribe «aprobado» si las 13 pasan, o el número de las que fallen con lo que viste en cada una.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lo que los tests demuestran ↔ lo que solo se ve | El cableado de componentes no tiene cobertura automatizada en este proyecto; la única barrera es esta revisión |
| entorno de verificación ↔ dispositivo real | Se verifica en viewport simulado; la tablet de mesa sigue sin identificarse desde la Fase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-23 | Tampering | flujo de partida con el modal abierto (D-12) | mitigate | Comprobación 6 de la Task 2: escribir un nombre con espacio con el modal abierto no puede avanzar el paso. Es el único punto donde esta protección se ejercita de verdad — la parte pura ya está testeada, el cableado no puede estarlo |
| T-06-24 | Repudiation | verificación de la fase | mitigate | El SUMMARY transcribe el veredicto comprobación por comprobación y la salida literal de los gates mecánicos, no un resumen; y anota explícitamente el límite del viewport simulado en vez de dejar entender que se probó en la tablet |
| T-06-25 | Information Disclosure | bundle generado | mitigate | Gate 6 de la Task 1: se comprueba que el catálogo y los alias están dentro del JavaScript generado y que ninguna ruta pide nada por red (CAT-06); el barrido de `catalogue-isolation` ya impide que el script generador o la API de MarvelCDB se cuelen en `app/` |
| T-06-26 | Denial of Service | teclado en pantalla sobre el panel `max-h-[80vh]` | accept | No se construye ninguna maquinaria de `visualViewport` en esta fase (`06-UI-SPEC.md` decisión 12): sin dispositivo real no hay evidencia de que haga falta, y el panel ya desplaza su propio contenido. Se acepta y se anota como deuda de dispositivo, igual que el resto de la deuda heredada de v1.7 |
| T-06-SC | Tampering | instalaciones npm/pip/cargo | accept | Gate 8 de la Task 1 comprueba explícitamente que `package.json`/`package-lock.json` no han cambiado en toda la fase |
</threat_model>

<verification>
1. Los ocho gates de la Task 1 pasan y su salida está transcrita en el SUMMARY.
2. Las 13 comprobaciones de la Task 2 tienen veredicto individual del usuario.
3. `git status --porcelain` tras el plan no lista nada salvo, como mucho, correcciones de copy o de clase Tailwind en los tres ficheros declarados en `files_modified`.
4. Los 5 criterios de éxito del ROADMAP §Phase 6 están cubiertos: nº 1 por la comprobación 4, nº 2 por las 5 y 7, nº 3 por las 8 y 9, nº 4 por la 12 y nº 5 por las 3 y 13.
5. El SUMMARY nombra la deuda pendiente de dispositivo real.
</verification>

<success_criteria>
- La suite completa, la build y la generación estática pasan, y el diff del contenido es una sola línea sin tocar `text` ni `speech`.
- Los 35 clips de voz siguen intactos y el gate de deriva sigue verde.
- El usuario ha recorrido las 13 comprobaciones en el navegador y su veredicto está transcrito una a una.
- La fase no ha añadido ni un paquete, ni un entorno de test, ni una línea a `engine/persistence.ts`, `usePersistedSession.ts` o `useStepShortcuts.ts`.
- La deuda de dispositivo real queda anotada, no disimulada.
</success_criteria>

<output>
Create `.planning/phases/06-selecci-n-de-villano-h-roes-y-jugadores/06-07-SUMMARY.md` when done
</output>
