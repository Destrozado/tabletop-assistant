---
phase: 09-hist-rico-y-estad-sticas
plan: 40
type: execute
wave: 33
depends_on: ["09-38", "09-39"]
gap_closure: true
files_modified:
  - .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HIST-04, HIST-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "La evaluación de riesgo de la marca en memoria distingue por escrito los DOS casos —la marca se pierde (advertencia perdida, aceptado) y la marca persiste cuando ya no es cierta (afirmación incorrecta, inaceptable)— y dice cuál de los dos era el que no contemplaba y qué mecanismo concreto lo hace imposible ahora"
    - "El camino de pérdida NUEVO que introduce la validación por huella —entrar, ver el aviso, salir sin elegir, volver— está escrito como coste conocido, no descubierto por la ronda siguiente"
    - "Ninguna evaluación de riesgo de este documento se justifica por «estaba fuera del alcance del plan»: la justificación se sostiene sin conocer los planes"
    - "`REQUIREMENTS.md` registra la novena cara y lo que el lote 09-37..09-40 hace con ella, sin dar HIST-06 por cerrado: la casilla sigue sin marcar y la fila de trazabilidad lo dice"
    - "La comprobación visual humana en tablet (DEV-02) sigue nombrada como ABIERTA en los dos documentos, y gana el punto de guion nuevo que este lote añade (el aviso de discrepancia dentro de la pantalla de contenido cambiado), sin que eso la dé por hecha"
    - "La anti-recurrencia queda escrita como propiedad comprobable, no como intención: el documento nombra los ficheros y las patas concretas que pondrían roja la suite ante la décima cara, para que la ronda siguiente pueda ir a verificarlo en vez de creerlo"
  artifacts:
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md"
      provides: "la evaluación de riesgo corregida de la marca y la nota de cierre de la ronda 8"
      contains: "(ronda 8)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "la nota de la ronda 8 y las filas de trazabilidad de HIST-04/HIST-06 sincronizadas"
      contains: "ronda 8"
  key_links:
    - from: ".planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md (sección de la marca)"
      to: "app/composables/useProgressMismatchMark.ts (validación por huella)"
      via: "la evaluación nombra el mecanismo concreto que hace imposible la persistencia incorrecta"
      pattern: "huella"
    - from: ".planning/REQUIREMENTS.md (nota de HIST-06)"
      to: "app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts"
      via: "la nota nombra el gate que cubre la clase de defecto, no una intención"
      pattern: "invariantesDeMarcaDeEstado"
  prohibitions:
    - "No se marca HIST-06 como `[x]` ni se escribe en ningún sitio que el hueco quede cerrado: solo una ronda de verificación independiente puede decir eso, y varias notas anteriores de estos mismos documentos dieron por cerrado lo que no lo estaba"
    - "No se escribe que la comprobación visual humana en tablet (DEV-02) esté hecha, resuelta o verificada"
    - "Ninguna entrada nueva se justifica por el alcance de un plan: la justificación tiene que ser una evaluación de riesgo que se sostenga sin conocer los planes"
    - "No se toca ningún fichero de código desde este plan"
    - "No se reescribe `REQUIREMENTS.md` entero: solo ediciones acotadas a las secciones nombradas, para no perder el resto del documento"
    - "No se afirma ninguna garantía absoluta sobre lo que la app «nunca» hace sin nombrar el mecanismo que lo sostiene y el fichero donde vive"
    - "No se modifica, renumera ni borra ninguno de los planes 09-01..09-36 ni sus SUMMARY"
---

<objective>
El tercer hallazgo de la ronda 8 no es de código: es de la documentación que el lote anterior
escribió para cerrar la ronda.

`deferred-items.md`, en la sección de la marca en memoria, evalúa el riesgo residual **solo** para
el caso en que la marca se PIERDE (recarga completa del navegador) y concluye por escrito que la
ausencia de marca no puede producir una afirmación incorrecta, y que lo único que se pierde en ese
caso es la advertencia. Esa evaluación no contempla el caso que la ronda 8 confirmó por trazado de
código: la marca SIGUE puesta y ya es falsa — lo contrario exacto de lo que el texto garantiza.

Es el mismo patrón que rondas anteriores encontraron en los motivos de las excepciones auditadas:
un texto que sella un hueco sin cubrir el caso que de verdad importa. Y es exactamente por qué la
corrección del código no basta: si el documento sigue diciendo que el riesgo está evaluado cuando
solo lo está a medias, la ronda siguiente parte de una base falsa.

Este plan corrige ese texto, registra el coste nuevo que el arreglo introduce (una advertencia que
se pierde en un camino más), y pone al día la trazabilidad **sin dar nada por cerrado**.

Purpose: que la ronda de verificación siguiente encuentre la documentación diciendo exactamente lo
que el árbol hace, incluidos sus costes, ni una palabra más.
Output: `deferred-items.md` y `REQUIREMENTS.md` al día, con HIST-06 y DEV-02 abiertos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-38-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-39-SUMMARY.md
</context>

<scope_boundary>
**DENTRO:** los dos ficheros de `files_modified`.

**FUERA:**
- Todo el código. Si al escribir la documentación apareciera que el código no hace lo que el
  SUMMARY del plan 09-38 dice, se anota como hallazgo en el SUMMARY de este plan y NO se escribe la
  afirmación; no se arregla el código desde aquí.
- `09-UI-SPEC.md`: el contrato de copy no cambia en este lote (el texto de
  `PROGRESS_MISMATCH_WARNING` es el mismo; lo que cambia es cuándo se muestra y dónde). Si al
  medirlo resultara que el contrato cita el aviso como exclusivo del modal de reanudación, se anota
  en el SUMMARY como deuda para el lote siguiente, con su ruta y su línea.
- `ROADMAP.md`: lo pone al día el planificador de este lote, no el ejecutor.
</scope_boundary>

<flagged_assumptions>
**Cobertura de bordes sin resolver.** Las 14 filas de la sonda determinista (HIST-01..HIST-09,
STAT-01..STAT-05) volvieron `unclassified`/`unresolved`. Ninguna se convierte en truth de respaldo
y ninguna se descarta; `deferred-items.md` ya tiene una entrada que lo registra como hecho y este
plan no la contradice.

**`09-UI-SPEC.md` no contiene ninguna sección `## UI Considerations`** (medido con
`grep -n "^## "`). No hay ítems que levantar y no se inventa ninguno.

**Sin medir todavía: si la garantía incorrecta está también en `REQUIREMENTS.md`.** Una búsqueda
previa no la encontró allí, pero la Task 2 obliga a medirlo con `grep` y a registrar el resultado
—cero coincidencias o las líneas exactas— en vez de darlo por hecho.
</flagged_assumptions>

<requirements_and_decisions_scope>
**Qué IDs re-aborda este lote y cuáles NO.** Los gaps de la ronda 8 tocan exactamente dos
requisitos de la fase:

- **HIST-06** (el histórico vive en localStorage y es la fuente de verdad, y ninguna afirmación de
  la interfaz sobre esos datos puede carecer de comprobación real) — **REABIERTO**, marcado `[ ]`
  en `REQUIREMENTS.md`, y este lote NO lo cierra: lo trabaja y deja la confirmación a una ronda de
  verificación independiente.
- **HIST-04** (el registro se construye a partir de una sesión que puede estar desactualizada) — la
  señal que avisaba de ese riesgo en el reintento es justo la que podía sobrevivir falsa.

Los otros doce IDs de la fase —HIST-01, HIST-02, HIST-03, HIST-05, HIST-07, HIST-08, HIST-09,
STAT-01, STAT-02, STAT-03, STAT-04 y STAT-05— **NO se re-abordan en este lote**, y la razón es
explícita: `09-VERIFICATION.md` (ronda 8) los verifica como SATISFECHOS por los planes 09-01..09-31,
sin reserva y sin cambios desde rondas anteriores. Reabrirlos aquí sería trabajo sin hallazgo que lo
motive. Ninguno de los cuatro planes de este lote toca el código que los satisface, y
`git diff` lo demuestra en cada uno.

**Decisiones de `09-CONTEXT.md` implicadas.** Este lote no revisa ninguna decisión: cambia cuándo y
dónde se muestra un aviso, nunca qué se decidió. Las decisiones que rozan el código tocado y que
siguen vigentes sin modificación son **D-03** (el aviso de resultado guardado nunca bloquea),
**D-43** (los CTA de reanudación locutan el paso recuperado) y **D-09**/**D-51** (dispara-y-olvida
de precarga y bloqueo de pantalla en esos mismos CTA): los tres comentarios que las citan en
`app/pages/[game]/index.vue` se conservan literalmente al añadir las llamadas nuevas.
</requirements_and_decisions_scope>

<artifacts_this_phase_produces>
| Símbolo | Tipo | Dónde |
|---|---|---|
| Sección «La marca de progreso que no coincide vive en memoria» reescrita | sección de documento | `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` |
| «Nota de cierre (ronda 8, planes 09-37..09-40)» | sección nueva de documento | ídem |
| Párrafo «Ronda 8» de la nota de cierre de hueco HIST-04/HIST-06 | párrafo nuevo | `.planning/REQUIREMENTS.md` |
| Filas de trazabilidad HIST-04 y HIST-06 actualizadas | filas de tabla | ídem |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: la evaluación de riesgo de la marca dice por fin el caso que importaba</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` — la sección «La marca de
      progreso que no coincide vive en memoria (ronda 7)» COMPLETA (~líneas 331-367): sus cuatro
      viñetas de evaluación de riesgo y su acción sugerida.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el tercer elemento de
      `gaps_remaining` y el tercer punto del `reason` del gap de SC3: describen literalmente qué
      afirma el texto actual y qué caso no cubre.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-38-SUMMARY.md` — qué hace exactamente la
      validación por huella y qué coste se registró: la documentación se escribe desde el SUMMARY
      del arreglo, contrastado contra el código, nunca de memoria.
    - `app/composables/useProgressMismatchMark.ts` — la cabecera tal como la deja el plan 09-38: el
      texto del documento y el del módulo tienen que decir lo mismo, y este es el original.
  </read_first>
  <action>
    1. **Reescribir la viñeta de evaluación de riesgo** de esa sección para que distinga
       explícitamente los DOS casos, cada uno con su nombre y su desenlace:
       - **Caso A, la marca se pierde.** Recarga completa del navegador, pestaña cerrada, o —nuevo
         desde el plan 09-38— cualquier reescritura del progreso que cambie su huella. Desenlace: el
         grupo no recibe una advertencia que le habría sido útil. Coste real, evaluado, aceptado; el
         peor desenlace sigue siendo el mismo que antes de todo este trabajo (un registro que podría
         llevar datos de otra partida), nunca uno peor.
       - **Caso B, la marca persiste cuando ya no es cierta.** Es el que la ronda 8 confirmó por
         trazado de código: continuar sobre el snapshot marcado, dejar que el autoguardado lo
         sustituya, salir sin terminar y volver a entrar. Desenlace: la app enseña una frase que en
         ese momento es falsa. **Inaceptable**, y por eso no se difiere: lo cierra el plan 09-38.
       Dejar escrito, con una frase explícita, que la versión anterior de esta evaluación solo
       contemplaba el caso A y afirmaba de él una garantía que se leía como si cubriera también el
       caso B — y que ese es exactamente el patrón que la propia fase persigue: un texto de cierre
       que no cubre el caso que de verdad importa.

    2. **Nombrar el mecanismo, no la intención**: decir que el caso B es imposible por construcción
       porque el lector de la marca compara la huella del progreso que había cuando se puso con la
       huella del progreso que hay al leer, y no devuelve nada si no coinciden; citar
       `app/composables/useProgressMismatchMark.ts` y `app/composables/useStoredProgress.ts` por
       ruta, y citar `app/composables/__tests__/useProgressMismatchMark.test.ts` como el fichero que
       lo fija por test. Sin esas rutas, la afirmación no se puede ir a comprobar y sería otra vez el
       mismo defecto.

    3. **Registrar el camino de pérdida NUEVO** como coste conocido, en su propia viñeta: como la
       huella cubre los siete campos de la posición persistida, `updatedAt` incluido, el propio
       autoguardado que dispara el montaje al restaurar la sesión ya cambia la huella. Consecuencia
       concreta: si el grupo entra, ve el aviso y sale sin elegir ni continuar, al volver a entrar ya
       no lo verá. Es caso A, se acepta, y se escribe aquí para que no lo descubra la ronda
       siguiente. Añadir la alternativa concreta por si algún día deja de ser aceptable (excluir
       `updatedAt` de la huella, al precio de que la garantía del caso B pase a depender otra vez de
       que todos los caminos de invalidación estén enumerados) y decir por qué hoy se prefiere la
       garantía a la cobertura.

    4. **Conservar** las viñetas que siguen siendo ciertas —por qué no se escribe en el
       almacenamiento del navegador, y qué cubre la navegación de cliente— sin tocarlas, y actualizar
       el encabezado de la sección para que diga que fue evaluada en la ronda 7 y **corregida** en la
       ronda 8.

    Todo en español, y ninguna afirmación absoluta sin el mecanismo y la ruta que la sostienen.
  </action>
  <verify>
    <automated>grep -c "no dice algo falso" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</automated>
    <fails_when>el número impreso por `grep -c` es distinto de 0 — la garantía incorrecta sigue en el documento (el código de salida 1 de `grep` con cero coincidencias es el resultado BUENO aquí)</fails_when>
    <automated>grep -c "lo que se pierde en ese caso es la advertencia, no la corrección" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</automated>
    <fails_when>el número impreso por `grep -c` es distinto de 0 (el código de salida 1 con cero coincidencias es el resultado BUENO aquí)</fails_when>
    <automated>grep -c "useProgressMismatchMark.ts" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</automated>
    <fails_when>el número impreso por `grep -c` es 0, o el código de salida es 1 — la evaluación no cita el fichero donde vive el mecanismo, así que no se puede ir a comprobar</fails_when>
  </verify>
  <acceptance_criteria>
    - Los dos casos aparecen nombrados y separados: `grep -c "Caso A" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1 y lo mismo para `Caso B`.
    - `grep -c "huella" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 4.
    - `grep -c "updatedAt" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1 (el camino de pérdida nuevo, con su causa concreta).
    - `grep -c "fuera del alcance de este plan" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` NO aumenta respecto al valor previo: registrar los dos recuentos en el SUMMARY.
    - `git diff --stat -- .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` muestra un diff acotado a esa sección más la nota de cierre de la Task 3; pegar el recuento de líneas en el SUMMARY.
    - `git diff --stat -- app/ engine/ package.json package-lock.json` vacío.
  </acceptance_criteria>
  <done>
    La evaluación de riesgo cubre los dos casos, nombra el mecanismo y las rutas que sostienen la
    garantía, y registra el coste nuevo antes de que nadie lo descubra.
  </done>
</task>

<task type="auto">
  <name>Task 2: la trazabilidad registra la novena cara sin dar nada por cerrado</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — las casillas HIST-01..HIST-09 (líneas ~74-84), la nota de cierre
      de hueco con sus párrafos por ronda (líneas ~86 en adelante, hasta el párrafo de la ronda 7), y
      las filas de trazabilidad de HIST-04 (línea ~332) y HIST-06 (línea ~334).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el gap de SC3 entero y la
      fila de HIST-06 de su tabla de cobertura de requisitos.
    - Los SUMMARY de los planes 09-37, 09-38 y 09-39 — todo lo que este párrafo afirme tiene que
      salir de ahí, contrastado contra el código, nunca de memoria.
  </read_first>
  <action>
    1. **Medir primero, escribir después.** Ejecutar sobre `.planning/REQUIREMENTS.md` la búsqueda de
       la garantía incorrecta que la ronda 8 encontró en `deferred-items.md` (la que concluye que la
       ausencia de la marca no puede producir ninguna afirmación incorrecta) y registrar en el
       SUMMARY el resultado exacto: cero coincidencias, o las líneas encontradas. Si aparece, se
       corrige con el mismo criterio de dos casos de la Task 1. Si no aparece, se escribe en el
       SUMMARY que se midió y no estaba — nunca se omite la medición.

    2. **Añadir un párrafo «Ronda 8»** a la nota de cierre de hueco, después del de la ronda 7, con
       exactamente esto y nada más adornado: la ronda 8 confirmó por trazado de código independiente
       una NOVENA cara del mismo patrón, esta vez dentro del propio mecanismo construido para cerrar
       la octava (la marca de discrepancia no se invalidaba al continuar, y su rama alternativa de
       montaje ni la pintaba ni la retiraba); el lote 09-37..09-40 la cierra por dos vías
       independientes —validación del referente en el momento de leer, e invalidación explícita en
       los dos caminos que faltaban— y añade un gate nuevo,
       `app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts`, que audita invariantes de
       ciclo de vida de estado de módulo, una clase que los cuatro gates de texto no podían cubrir;
       y las dos evasiones del propio mecanismo de excepciones auditadas quedan cerradas por el plan
       09-39. Terminar diciendo, con esas palabras, que **nada de esto da HIST-06 por cerrado**:
       queda pendiente de confirmación por una ronda de verificación independiente.

    3. **Sincronizar a mano las dos filas de trazabilidad**: HIST-04 y HIST-06 añaden los planes
       09-37..09-40 a su columna de fase/plan, y su texto refleja el estado real. HIST-06 sigue
       diciendo que está reabierto y pendiente de confirmación independiente. Ninguna otra fila se
       toca en esta tarea.

    4. **DEV-02**: comprobar que sigue apareciendo como PENDIENTE y añadir al guion de la
       comprobación el punto nuevo que este lote crea: el aviso de discrepancia mostrado ahora
       también dentro de la pantalla de contenido cambiado (`ContentChangedNotice.vue`), que nadie ha
       visto renderizado en un dispositivo físico. Añadir el punto al guion **no** es realizar la
       comprobación, y el texto tiene que decirlo.

    Ediciones acotadas, en español, sin reescribir el documento.
  </action>
  <verify>
    <automated>grep -c "^- \[ \] \*\*HIST-06" .planning/REQUIREMENTS.md</automated>
    <fails_when>el número impreso por `grep -c` es distinto de 1 — la casilla de HIST-06 se ha marcado o se ha perdido</fails_when>
    <automated>grep -c "ronda 8" .planning/REQUIREMENTS.md</automated>
    <fails_when>el número impreso por `grep -c` es 0, o el código de salida es 1 — la novena cara no ha quedado registrada</fails_when>
    <automated>grep -c "invariantesDeMarcaDeEstado" .planning/REQUIREMENTS.md</automated>
    <fails_when>el número impreso por `grep -c` es 0, o el código de salida es 1 — la nota no nombra el gate concreto, así que afirma una cobertura que no se puede ir a comprobar</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\- \[x\] \*\*HIST-06" .planning/REQUIREMENTS.md` devuelve 0.
    - La fila de trazabilidad de HIST-06 contiene `09-40` y sigue conteniendo la palabra `Reabierto`; pegar la fila entera en el SUMMARY.
    - La fila de trazabilidad de HIST-04 contiene `09-38`; pegar la fila entera en el SUMMARY.
    - `grep -ci "DEV-02" .planning/REQUIREMENTS.md` devuelve al menos 1 y ninguna de esas líneas contiene `hecha`, `resuelta`, `verificada` ni `completada`: pegar las líneas encontradas en el SUMMARY.
    - El SUMMARY registra el resultado de la medición del punto 1 (cero coincidencias, o las líneas encontradas y cómo se corrigieron).
    - `git diff --stat -- .planning/REQUIREMENTS.md` muestra un diff acotado; ninguna sección ajena a HIST-04/HIST-06/DEV-02 aparece modificada.
    - `git diff --stat -- app/ engine/` vacío.
  </acceptance_criteria>
  <done>
    La trazabilidad dice lo que pasó y lo que falta, nombra el gate por ruta, y no da por cerrado
    nada que una verificación independiente no haya confirmado.
  </done>
</task>

<task type="auto">
  <name>Task 3: nota de cierre de la ronda 8 — qué cierra este lote, qué sigue abierto y qué atraparía la décima cara</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` — las dos notas de cierre
      anteriores («Nota de cierre (ronda 6, plan 09-31)» y «Nota de cierre (ronda 7, planes
      09-32..09-36)»): son el modelo de forma, nombrando cada cosa una a una con su plan.
    - Los SUMMARY de los planes 09-37, 09-38 y 09-39.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — la tabla de
      anti-patrones encontrados: los dos WARNING que este lote NO toca
      (`UpdateBanner`/`HistorySavedNotice` compartiendo posición y capa, y la comprobación humana
      pendiente) tienen que seguir nombrados como abiertos.
  </read_first>
  <action>
    1. **Sección nueva «Nota de cierre (ronda 8, planes 09-37..09-40)»**, con la misma forma que las
       dos anteriores: cada hallazgo de la ronda 8 nombrado uno a uno, con el plan que lo cierra —
       CR-01 (la marca que no se invalidaba al continuar), WR-01 (la rama de contenido cambiado que
       calculaba el aviso sin pintarlo ni retirarlo), el hallazgo sobre esta misma documentación,
       WR-02 (respaldo sin relevancia de contenido), WR-03 (detector de circularidad tras una
       subcadena literal) e IN-01 (el umbral de longitud). Para cada uno: qué era, qué plan lo cierra
       y por qué vía.

    2. **Lo que sigue ABIERTO**, nombrado uno a uno para que nada se dé por cerrado por descuido: el
       solape `UpdateBanner`/`HistorySavedNotice` (ronda 4), los toques invisibles sobre la cabecera
       de `/historico` (ronda 4), la vía de recuperación de un blob permanentemente ilegible, la
       sonda de cobertura de bordes sin clasificar, y la comprobación visual humana en tablet
       (DEV-02) — esta última con su guion actualizado y con la frase explícita de que no se ha
       realizado.

    3. **Párrafo de anti-recurrencia, escrito como propiedad comprobable.** Qué tendría que pasar
       para que la DÉCIMA cara de este defecto la encuentre una ejecución de la suite y no otra
       ronda de revisión humana, nombrando los mecanismos concretos y sus rutas: (a) el
       descubrimiento de marcas de `invariantesDeMarcaDeEstado.test.ts` es un glob sobre
       `app/composables/**`, así que cualquier composable nuevo con estado de módulo mutable y una
       raíz vigilada entra solo, sin que nadie edite ninguna lista; (b) una vez dentro, tiene que
       demostrar que su lector valida el referente, que su fichero hermano de tests recorre el ciclo
       completo, y que todas las ramas de montaje que lo leen lo pintan; (c) toda excepción auditada
       de los dos gates tiene que citar un respaldo cuyo contenido tenga que ver con lo que afirma y
       nombrar algo comprobable; (d) toda condición de calidad de esas excepciones tiene que tener
       un caso sintético que la ponga roja, vigilado por su propio test de cobertura. Terminar con
       la advertencia honesta: esto cubre la clase de defecto que las nueve rondas han encontrado, y
       **no** es una promesa de que no haya una décima de otra clase — decir lo contrario sería, una
       vez más, una afirmación sin respaldo.

    Todo en español, con el mismo tono de las notas anteriores.
  </action>
  <verify>
    <automated>grep -c "Nota de cierre (ronda 8" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</automated>
    <fails_when>el número impreso por `grep -c` es distinto de 1</fails_when>
    <automated>grep -c "invariantesDeMarcaDeEstado" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</automated>
    <fails_when>el número impreso por `grep -c` es 0, o el código de salida es 1 — el párrafo de anti-recurrencia no nombra el mecanismo, así que no se puede ir a comprobar</fails_when>
    <automated>npm test</automated>
    <fails_when>salida distinta de 0, o cualquier línea «FAIL» (regresión rápida: la documentación no puede haber roto nada, y se ejecuta para dejar constancia del estado del árbol al cerrar el lote)</fails_when>
  </verify>
  <acceptance_criteria>
    - La nota nombra los seis hallazgos de la ronda 8 con su plan: `grep -c "09-38" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 2 y `grep -c "09-39" ...` devuelve al menos 2.
    - `grep -ciE "DEV-02" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 2 y ninguna de esas líneas contiene `hecha`, `resuelta` ni `verificada`: pegar las líneas en el SUMMARY.
    - El párrafo de anti-recurrencia nombra las cuatro propiedades (a-d) y al menos tres rutas de fichero: pegarlo entero en el SUMMARY.
    - `grep -c "no es una promesa" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1 (la advertencia honesta de cierre).
    - `npm test` sale 0; pegar la línea de resumen final.
    - `git diff --stat -- app/ engine/ package.json package-lock.json` vacío.
  </acceptance_criteria>
  <done>
    La ronda 8 queda cerrada por escrito con cada hallazgo nombrado, lo abierto sigue nombrado como
    abierto, y la propiedad anti-recurrencia está escrita de forma que la próxima ronda pueda ir a
    verificarla en el código en vez de creérsela.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentación → ronda de verificación siguiente | Lo que estos documentos afirman es la base sobre la que la ronda siguiente decide qué re-comprobar; una afirmación falsa aquí desvía esa ronda entera |
| SUMMARY de un plan → documentación | Un SUMMARY puede afirmar más de lo que su plan hizo; copiarlo sin contrastar contra el código propaga el error |
| Evaluación de riesgo escrita → decisión futura de dejar de mitigar | Una evaluación que solo cubre la mitad de los casos se lee como si los cubriera todos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-40-01 | Repudiation | Una evaluación de riesgo que garantiza por escrito algo que el mecanismo no sostiene (el hallazgo de la ronda 8 sobre esta misma documentación) | high | mitigate | La sección se reescribe con los dos casos nombrados y separados; tres `grep` negativos sobre las frases incorrectas como criterio de aceptación |
| T-09-40-02 | Repudiation | Dar HIST-06 por cerrado, o DEV-02 por hecha, sin verificación independiente | high | mitigate | Prohibiciones explícitas, `grep` sobre la casilla y sobre las líneas de DEV-02, y la frase de confirmación independiente repetida en la nota |
| T-09-40-03 | Spoofing | Citar un mecanismo o un fichero que no existe o que no hace lo que la nota dice | medium | mitigate | Todo mecanismo citado va con su ruta, y las rutas se comprueban por `grep` sobre el árbol antes de escribirlas |
| T-09-40-04 | Tampering | Reescribir `REQUIREMENTS.md` entero y perder secciones ajenas a esta fase | medium | mitigate | Instrucción explícita de ediciones acotadas y criterio de aceptación sobre el alcance del diff |
| T-09-40-SC | Tampering | Instalaciones de paquetes (npm) | low | accept | Este plan no toca código ni dependencias: `git diff --stat -- package.json package-lock.json app/ engine/` tiene que quedar vacío |
</threat_model>

<verification>
- `git diff --name-only` lista exactamente los dos ficheros de `files_modified`.
- `git diff --stat -- app/ engine/ package.json package-lock.json` vacío.
- `npm test` sale 0 y `npx tsc --noEmit` sale 0 (estado del árbol al cerrar el lote, registrado).
- Las tres frases que la ronda 8 señaló como garantía incorrecta ya no aparecen en
  `deferred-items.md`, y su sustitución nombra el mecanismo y las rutas.
- HIST-06 sigue `[ ]` y DEV-02 sigue nombrada como abierta en los dos documentos.
</verification>

<success_criteria>
- La evaluación de riesgo de la marca distingue por escrito la pérdida de la persistencia
  incorrecta, dice cuál no se contemplaba, y nombra el mecanismo y los ficheros que hacen imposible
  la segunda.
- El coste nuevo que introduce el arreglo está registrado antes de que nadie lo descubra.
- `REQUIREMENTS.md` registra la novena cara, las dos vías de cierre y el gate nuevo por ruta, sin
  marcar HIST-06 ni DEV-02.
- La nota de cierre de la ronda 8 nombra los seis hallazgos con su plan y lo que sigue abierto uno
  a uno.
- El párrafo de anti-recurrencia es verificable: nombra rutas y propiedades, y dice explícitamente
  qué NO garantiza.
</success_criteria>

<output>
Create `.planning/phases/09-hist-rico-y-estad-sticas/09-40-SUMMARY.md` when done
</output>
