---
phase: 09-hist-rico-y-estad-sticas
plan: 32
type: execute
wave: 25
depends_on: []
gap_closure: true
files_modified:
  - app/composables/useHistorySavedNotice.ts
  - app/composables/useGameEndCopy.ts
  - app/components/HistorySavedNotice.vue
  - app/pages/[game]/index.vue
  - app/composables/__tests__/useHistorySavedNotice.test.ts
  - app/composables/__tests__/useGameEndCopy.test.ts
  - app/composables/__tests__/avisoTrasRegistroFallido.test.ts
autonomous: true
requirements: [HIST-06, HIST-09]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`NOTICE_BODY['failure-stale']` no afirma ni anterioridad temporal, ni identidad de partida, ni diferencia de ronda: afirma exactamente lo que `esLaMismaPartida` establece —que lo guardado no es el punto en el que la partida terminó— y declara por escrito lo que la app NO sabe"
    - "La copy de fin de partida deja de prometer un reintento que dos de los cuatro estados del dispositivo contradicen; lo único que promete es lo que `preserveProgress = !historyRecorded` garantiza en las cuatro salidas"
    - "Toda la copy del diálogo de fin de partida y del descarte vive en un `.ts` con test puro, no en un literal de plantilla ni en un `computed` dentro del SFC — el fichero donde han vivido cinco de las ocho caras del defecto deja de contener afirmaciones sobre los datos guardados del grupo"
    - "El escenario canónico de la ronda 6 (disco en `cursor: 2`, partida que termina en `cursor: 3`, misma `round`) tiene un test que fija POR TEXTO que la copy no afirma nada sobre la ronda, y que deja escrito el contraejemplo: en ese escenario las dos rondas son iguales"
    - "Ningún componente `.vue` escribe a mano un literal de `NoticeVariant` para decidir cómo se pinta el aviso"
  artifacts:
    - path: "app/composables/useHistorySavedNotice.ts"
      provides: "la copy de `failure-stale` reescrita y `isSuccessVariant` como única vía de distinguir el tono del aviso"
      contains: "isSuccessVariant"
    - path: "app/composables/useGameEndCopy.ts"
      provides: "`buildEndGameBody` y `buildDiscardBody`, la copy del diálogo de fin de partida y del descarte como funciones puras"
      contains: "buildEndGameBody"
    - path: "app/composables/__tests__/useGameEndCopy.test.ts"
      provides: "el test puro que fija esa copy y la ausencia de la promesa de reintento"
      contains: "buildEndGameBody"
    - path: "app/composables/__tests__/avisoTrasRegistroFallido.test.ts"
      provides: "el test 5 con la aserción de texto que faltaba y el contraejemplo de la ronda escrito como aserción"
      contains: "no la ronda"
  key_links:
    - from: "app/composables/useStoredProgress.ts (esLaMismaPartida)"
      to: "app/composables/useHistorySavedNotice.ts (NOTICE_BODY['failure-stale'])"
      via: "la copy no afirma ningún hecho que la comparación booleana de seis campos no establezca"
      pattern: "no coincide con el punto en el que habéis terminado"
    - from: "app/composables/useHistorySavedNotice.ts (planGameEnd.preserveProgress)"
      to: "app/composables/useGameEndCopy.ts (buildEndGameBody)"
      via: "la única promesa del diálogo es la que `preserveProgress = !historyRecorded` garantiza en las cuatro salidas"
      pattern: "el progreso no se borrará"
  prohibitions:
    - "No se reintroduce ninguna instrucción de reintento en la variante `failure-stale`: el reintento desde un snapshot que no coincide es el gesto que puede escribir en el histórico —único dato irreconstruible de la app— datos de otra partida"
    - "No se afirma anterioridad temporal («una versión anterior», «el último autoguardado») sobre lo que hay en disco: `updatedAt` está excluido de `esLaMismaPartida` por escrito, así que no existe ningún orden temporal comprobado"
    - "No se afirma con certeza que registrar desde el snapshot guardaría datos distintos: `buildHistoryEntry` solo lee `round` y `context`, así que cuando la diferencia está únicamente en `runtimeId` la entrada resultante sería idéntica — la única forma honesta es la modal («podría»)"
    - "No se toca ni una línea de `engine/`: la comparación y la copy viven en `app/`"
    - "No se importa `firebase` ni `firestore` en ningún fichero de `app/` (criterio de éxito nº 5 del ROADMAP, sigue vigente)"
---

<objective>
Cerrar la **octava cara** del defecto de fondo de esta fase, en su forma exacta: la copy escrita
para cerrar la séptima cara afirma tres hechos que su respaldo no establece.

`NOTICE_BODY['failure-stale']` dice hoy «En el dispositivo solo queda **una versión anterior de
esta partida** —**no la ronda en la que habéis terminado**—». Detrás de esas palabras solo hay
`esLaMismaPartida`, una comparación booleana sobre seis campos que (a) excluye `updatedAt` por
escrito, así que no puede establecer ninguna anterioridad; (b) no identifica qué campo difirió,
así que no puede hablar de la ronda; y (c) en el escenario canónico que el propio plan 09-28
construyó como test de regresión (`avisoTrasRegistroFallido.test.ts` test 5), la diferencia real
está en `runtimeId` y **las dos rondas son idénticas** — la frase es literalmente falsa justo en
el escenario que se usa como prueba de que funciona.

En paralelo, `endGameBody` promete «la app conservará el progreso para que podáis reintentarlo»
**antes** de que la app haya leído el dispositivo. Es falsa en 2 de los 4 estados (`absent`: el
aviso posterior dice que no hay nada que reintentar; `stale`: el aviso posterior deliberadamente
no ordena el reintento). Y el motivo escrito que la protege en `AFIRMACIONES_AUDITADAS` afirma
que «es cierto en las cuatro salidas»: un motivo falso sellando un hueco.

Este plan hace tres cosas y ninguna más: reescribe la copy de `failure-stale` para que no afirme
más de lo comprobado, saca toda la copy del diálogo de fin de partida (y la del descarte) a un
`.ts` con test puro cuyo texto solo promete lo que `preserveProgress` garantiza, y pone en el
test 5 la aserción de texto que faltaba junto con el contraejemplo escrito.

Purpose: que ninguna frase que el grupo lea sobre sus propios datos afirme un hecho que el dato
que la respalda no establece.
Output: `failure-stale` reescrita, `app/composables/useGameEndCopy.ts` con su test puro,
`isSuccessVariant`, y el test 5 fijando el texto.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
</context>

<scope_boundary>
**DENTRO:** los siete ficheros de `files_modified`, y solo las superficies que este plan nombra.

**FUERA:**
- `app/composables/useStoredProgress.ts` → **no se toca**. Ver `<alternativa_rechazada>`: este
  plan rechaza de forma razonada convertir `esLaMismaPartida` en un resultado con motivo.
- `app/composables/__tests__/afirmacionesRespaldadas.test.ts` (el gate de clase) → es el plan
  **09-34**, que debe correr DESPUÉS para auditar el árbol ya corregido. **Excepción obligatoria y
  única:** si alguno de los cambios de este plan deja rojo cualquier gate existente, el arreglo
  MÍNIMO para dejar la suite en verde entra aquí y se anota literalmente en el SUMMARY (qué gate,
  qué línea, por qué).
- La marca en memoria del snapshot que no coincide y el aviso de `ResumePrompt` → es el plan
  **09-33**.
- `.planning/**` → es el plan **09-36**.
</scope_boundary>

<alternativa_rechazada>
`09-VERIFICATION.md` ofrece dos vías para el primer `missing:` de SC3: **(A)** retirar de
`failure-stale` las afirmaciones no respaldadas, o **(B)** hacer que `esLaMismaPartida` devuelva
qué campo difirió (`{ iguales: false, motivo: 'round' | 'runtimeId' | … }`) y elegir la copy a
partir de ese motivo, con un test por motivo.

**Este plan elige (A) y rechaza (B), por cuatro razones comprobables:**

1. **(B) no arregla la afirmación de anterioridad.** «Una versión anterior» necesita `updatedAt`,
   excluido de la comparación por escrito y a propósito (incluirlo haría `'stale'` a toda partida).
   Con motivo o sin él, esa frase hay que retirarla igual.
2. **Un `motivo` único sería a su vez una afirmación sin respaldo.** Varios campos pueden diferir
   a la vez; elegir uno como «el» motivo es inventar una prioridad que el dato no tiene.
3. **(B) multiplica por seis la superficie de afirmación** en una fase cuya clase de defecto
   —ocho caras en siete rondas— es exactamente el exceso de afirmación. Seis frases nuevas son
   seis ocasiones nuevas de escribir la novena cara.
4. **(B) no cambiaría la acción del grupo.** `buildHistoryEntry` (`engine/history.ts:51-107`) lee
   únicamente `session.context`, `session.round` y `session.gameId` — nunca `cursor`/`runtimeId`.
   En el escenario canónico (solo difiere `runtimeId`) el registro resultante sería **idéntico**,
   así que ni siquiera un motivo correcto autorizaría a decir «guardaría datos distintos» con
   certeza: la única forma honesta sigue siendo la modal («podría»), que es la que (A) usa.

Lo que (A) pierde frente a (B) —guía más concreta— no se pierde de verdad: la copy actual ya no
ordena ningún reintento, así que la acción que el grupo toma es la misma antes y después.
</alternativa_rechazada>

<flagged_assumptions>
**Cobertura de bordes sin resolver (no se descarta en silencio).** La sonda determinista de bordes
se ejecutó sobre los 14 IDs de requisito de la fase (HIST-01..HIST-09, STAT-01..STAT-05) y devolvió
las 14 filas como `unclassified` / `unresolved`. No se ha convertido ninguna en «truth» de respaldo:
quedan aquí registradas como suposición explícita del planificador. Consecuencia práctica para este
plan: sus `must_haves.truths` salen del análisis directo de `09-VERIFICATION.md`, no de la sonda.

**Sin `## UI Considerations` que levantar.** `09-UI-SPEC.md` existe pero no tiene esa sección
(sus encabezados son Design System, Spacing Scale, Typography, Color, Layout, Component Inventory,
Interaction & State Coverage, Copywriting Contract, Registry Safety, Decisions Made Without User
Input, Forward-Compatibility Notes, Checker Sign-Off). No hay consideraciones que trasladar a
`must_haves`; se deja constancia para que la ausencia no parezca un descarte silencioso.

**Comprobación humana pendiente, no automatizable aquí.** `09-VERIFICATION.md` registra un
`human_verification` (solape visual de `UpdateBanner` e `HistorySavedNotice` en tablet horizontal
real, bajo DEV-02). Este plan **no** la automatiza ni la da por hecha: sigue abierta.
</flagged_assumptions>

<artifacts_this_phase_produces>
Símbolos y ficheros NUEVOS que introduce este plan (no existen hoy en el árbol):

| Símbolo / fichero | Tipo | Dónde |
|---|---|---|
| `app/composables/useGameEndCopy.ts` | fichero nuevo | `app/composables/` |
| `buildEndGameBody(savedSummary: string): string` | función pura exportada | `app/composables/useGameEndCopy.ts` |
| `buildDiscardBody(savedSummary: string): string` | función pura exportada | `app/composables/useGameEndCopy.ts` |
| `isSuccessVariant(variant: NoticeVariant): boolean` | función pura exportada | `app/composables/useHistorySavedNotice.ts` |
| `app/composables/__tests__/useGameEndCopy.test.ts` | fichero nuevo | `app/composables/__tests__/` |
| `registrado` (renombre de la variable local `guardado`) | identificador local | `app/pages/[game]/index.vue` |
| `avisoLecturaNoComprobada` (renombre de `avisoProgresoNoComprobado`) | identificador local | `app/pages/[game]/index.vue` |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: el camino completo de `failure-stale`, de la lectura fallida al texto que lee el grupo</name>
  <files>app/composables/useHistorySavedNotice.ts, app/composables/__tests__/useHistorySavedNotice.test.ts, app/composables/__tests__/avisoTrasRegistroFallido.test.ts</files>
  <read_first>
    - `app/composables/useHistorySavedNotice.ts` — COMPLETO. Sobre todo el bloque de comentario de
      las líneas 126-158 (que explica cada variante) y `NOTICE_BODY` (líneas 159-165). Ese
      comentario describe `failure-stale` como «afirma exactamente lo que `esLaMismaPartida` ha
      comprobado»: es la frase que esta tarea tiene que **hacer verdad**, no repetir.
    - `app/composables/useStoredProgress.ts` líneas 103-177 — `esLaMismaPartida` (qué seis campos
      compara, y el comentario que excluye `updatedAt` por escrito) y la guarda de
      `readStoredProgress` que produce `'stale'`.
    - `engine/persistence.ts` líneas 9-37 — `PersistedPosition` y `toPersistedPosition`. Importa
      que `runtimeId` es el id del NODO de la secuencia (el paso), no un identificador de
      instancia de partida.
    - `engine/history.ts` líneas 44-120 — `buildHistoryEntry`. Confirma por lectura directa que
      solo lee `session.context`, `session.round` y `session.gameId`: es el hecho en el que se
      apoya la forma modal de la frase nueva.
    - `app/composables/__tests__/avisoTrasRegistroFallido.test.ts` — COMPLETO, con atención al
      test 5 (líneas 201-254) y a los dobles de `localStorage` de las líneas 44-100.
    - `app/composables/__tests__/useHistorySavedNotice.test.ts` — COMPLETO (la tabla de verdad de
      `resolveNoticeVariant` y las aserciones existentes sobre `failure-stale`).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el gap de SC3 entero,
      sobre todo su lista `missing:`.
  </read_first>
  <behavior>
    - Con el montaje EXACTO del test 5 (tres autoguardados con éxito en `cursor: 0/1/2`, cuota
      agotada después, cierre en `cursor: 3`): `readStoredProgress(tinyGame, partidaQueTermina)`
      sigue devolviendo `stored: 'stale'` y `planGameEnd(...).variant` sigue siendo
      `'failure-stale'` — este plan no cambia ninguna decisión, solo el texto.
    - En ese mismo montaje, `NOTICE_BODY[variante]` **no** contiene «no la ronda».
    - En ese mismo montaje, `NOTICE_BODY[variante]` **no** contiene «versión anterior».
    - En ese mismo montaje se puede afirmar por test que la ronda en disco y la ronda de la partida
      que termina son **iguales** — el contraejemplo que hace falsa la frase retirada.
    - En ese mismo montaje, `NOTICE_BODY[variante]` **sí** contiene la frase que sí está
      respaldada: «no coincide con el punto en el que habéis terminado».
    - Siguen valiendo las tres aserciones negativas que el test 5 ya tenía: el cuerpo no contiene
      «sigue guardada», ni «no se ha perdido», ni «Partida terminada».
    - `NOTICE_BODY['failure-stale']` no contiene ninguna forma de «reintentar».
    - Las otras cuatro entradas de `NOTICE_BODY` no cambian ni un carácter.
  </behavior>
  <action>
    1. **Reescribir `NOTICE_BODY['failure-stale']`** (hoy en la línea 162) con exactamente este
       texto, sin añadirle nada:

       `La partida no se ha registrado. En el dispositivo queda guardada una partida de este juego, pero no coincide con el punto en el que habéis terminado: la app no puede saber si es esta misma partida o es otra. Registrarla desde ahí podría guardar en el histórico datos que no son los de esta partida. Suele deberse al modo privado del navegador o a la memoria llena.`

       Cada oración tiene que poder señalarse a un hecho comprobado, y el comentario que precede a
       `NOTICE_BODY` tiene que decir cuál:
       - «queda guardada una partida de este juego» ← `readStoredProgress` llegó a `'stale'`, que
         exige `result.outcome !== 'fresh'`: hay una posición reanudable bajo
         `tga:progress:<gameId>`.
       - «no coincide con el punto en el que habéis terminado» ← `esLaMismaPartida` devolvió
         `false` sobre los seis campos de `PersistedPosition`. «El punto» es exactamente ese
         objeto; no se dice qué campo difiere porque la comparación no lo sabe.
       - «la app no puede saber si es esta misma partida o es otra» ← declaración explícita de
         ignorancia: `runtimeId` identifica un paso de la secuencia, no una instancia de partida.
       - «podría guardar … datos que no son los de esta partida» ← modal, no certeza:
         `buildHistoryEntry` solo lee `round` y `context`, así que cuando la única diferencia está
         en `runtimeId` el registro sería idéntico. Afirmarlo con certeza sería la novena cara.

    2. **Reescribir el bloque de comentario de `failure-stale`** (líneas 139-146) para que describa
       lo anterior y deje por escrito las DOS afirmaciones retiradas y por qué: la anterioridad
       temporal (imposible: `updatedAt` excluido de la comparación) y la diferencia de ronda
       (falsa en el escenario canónico del test 5, donde `round` vale 1 en disco y en la partida
       que termina porque `expand()` lo fija y ningún `{ ...base, cursor: n }` lo cambia). Citar
       `09-VERIFICATION.md` (ronda 7) como origen.

    3. **Tests en `useHistorySavedNotice.test.ts`** (añadir, sin tocar la intención de los
       existentes): que `NOTICE_BODY['failure-stale']` contiene la frase respaldada; que no
       contiene ninguna de las dos afirmaciones retiradas; que no contiene ninguna forma de
       «reintentar»; y que las otras cuatro entradas siguen siendo exactamente las de antes
       (comparar contra las cadenas literales actuales, copiadas al test).

    4. **Test 5 de `avisoTrasRegistroFallido.test.ts`** — la costura de extremo a extremo, que es
       lo que hace de esta tarea la rebanada vertical del plan. Sin cambiar el montaje:
       - Añadir la aserción de texto que faltaba: el cuerpo NO contiene «no la ronda».
       - Añadir la aserción positiva: el cuerpo SÍ contiene «no coincide con el punto en el que
         habéis terminado».
       - Añadir el **contraejemplo escrito**: leer la posición que quedó en disco (vía
         `usePersistedSession().readProgress('tiny-game')`) y la de la partida que termina (vía
         `toPersistedPosition({ ...base, cursor: 3 })`) y afirmar que sus `round` son **iguales**
         y sus `runtimeId` **distintos**. Es la prueba, dentro del propio test, de por qué la frase
         retirada era falsa aquí. Acompañarla de un comentario que lo diga con esas palabras.
       - Ampliar el título del test 5 para que nombre lo que ahora fija (que el texto no afirma
         nada sobre la ronda).

    Todo el código y los comentarios en español, igual que el resto de los ficheros.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/useHistorySavedNotice.test.ts app/composables/__tests__/avisoTrasRegistroFallido.test.ts</automated>
    <fails_when>salida distinta de 0, o la línea de resumen contiene «0 passed», o cualquier línea «FAIL»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "no coincide con el punto en el que habéis terminado" app/composables/useHistorySavedNotice.ts` devuelve 1.
    - `grep -c "toContain('no la ronda')" app/composables/__tests__/avisoTrasRegistroFallido.test.ts` devuelve 1 — la aserción de texto que el `missing:` de SC3 pide existe literalmente en el test 5.
    - `grep -c "toContain('versión anterior')" app/composables/__tests__/useHistorySavedNotice.test.ts` devuelve al menos 1.
    - `grep -v "^\s*//" app/composables/useHistorySavedNotice.ts | grep -c "reintentar"` devuelve 1 — la ÚNICA aparición fuera de comentarios es la de `failure-recoverable` (línea 161), que sí está respaldada; `failure-stale` no ordena ningún reintento.
    - El test 5 contiene una aserción que compara las dos `round` y las declara iguales: `grep -c "runtimeId" app/composables/__tests__/avisoTrasRegistroFallido.test.ts` devuelve al menos 1.
    - `npx vitest run app/composables/__tests__/useHistorySavedNotice.test.ts app/composables/__tests__/avisoTrasRegistroFallido.test.ts` sale 0. Registrar en el SUMMARY el número de tests antes y después de los dos ficheros.
    - `npm run typecheck` sale 0.
  </acceptance_criteria>
  <done>
    El camino completo —doble de `localStorage` que falla tarde → `readStoredProgress` con
    `esperada` → `planGameEnd` → `NOTICE_BODY`— produce un texto cuyas cuatro oraciones se pueden
    señalar, una a una, a un hecho comprobado, y el test que recorre ese camino lo fija por texto.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: la copy del diálogo de fin de partida sale del SFC a un `.ts` con test puro</name>
  <files>app/composables/useGameEndCopy.ts, app/composables/__tests__/useGameEndCopy.test.ts, app/pages/[game]/index.vue</files>
  <read_first>
    - `app/pages/[game]/index.vue` líneas 470-515 — `savedSummary`, `discardBody` y `endGameBody`
      con sus comentarios completos. El comentario de `endGameBody` (líneas 492-509) enumera rama
      por rama las cuatro salidas del diálogo: es el razonamiento que hay que conservar, corregido.
    - `app/pages/[game]/index.vue` líneas 572-605 — `finishGame(preserveProgress)` y la llamada
      `if (!preserveProgress) clear(gameId)`, que es lo que respalda la única promesa que la copy
      nueva puede hacer.
    - `app/composables/useHistorySavedNotice.ts` líneas 70-94 — `GameEndPlan`/`planGameEnd` y el
      comentario que explica por qué `preserveProgress` depende SOLO de `historyRecorded`.
    - `app/composables/useProgressMountPlan.ts` — COMPLETO. Es el precedente exacto del patrón que
      esta tarea aplica: copy como constante exportada de un `.ts`, con test puro, en vez de un
      literal en plantilla.
    - `app/composables/__tests__/useProgressMountPlan.test.ts` líneas 1-20 — la cabecera que
      explica por qué el test es puro (entorno `node`, sin jsdom ni contexto de Nuxt). El proyecto
      `app-logic` de `vitest.config.ts` no monta componentes: por eso la copy tiene que salir del
      `.vue` para poder tener test.
  </read_first>
  <behavior>
    - `buildEndGameBody('Rhino · 2 jug · Normal')` devuelve una cadena que contiene ese resumen
      interpolado entre paréntesis.
    - `buildEndGameBody(...)` NO contiene ninguna forma de «conservar» ni de «reintentar».
    - `buildEndGameBody(...)` contiene «el progreso no se borrará» — la única promesa que
      `preserveProgress = !historyRecorded` garantiza en las cuatro salidas del diálogo.
    - `buildDiscardBody('Rhino · 2 jug · Normal')` devuelve exactamente el texto que hoy produce
      `discardBody` con ese mismo argumento, carácter a carácter.
    - Ambas funciones son puras: dos llamadas con el mismo argumento devuelven la misma cadena y
      no tocan `window`, `localStorage` ni ningún estado de módulo.
  </behavior>
  <action>
    1. **Crear `app/composables/useGameEndCopy.ts`** con una cabecera de comentario que explique
       por qué existe: `09-VERIFICATION.md` (ronda 7) encontró que la promesa de reintento de
       `endGameBody` era falsa en 2 de los 4 estados del dispositivo y que el motivo escrito que la
       auditaba afirmaba lo contrario de lo verificado; la copy sobre datos persistidos del grupo
       vive desde ahora en un `.ts` con test puro —mismo criterio que `NOTICE_BODY` y
       `UNVERIFIED_PROGRESS_NOTICE`— porque el proyecto `app-logic` de Vitest no monta componentes
       y un literal dentro del SFC no puede tener test.

       Exportar dos funciones puras:
       - `buildDiscardBody(savedSummary: string): string` → exactamente el texto actual:
         `Se borrará el progreso guardado de la partida en curso (${savedSummary}). Esta acción no se puede deshacer.`
         Trasladar íntegro el comentario que hoy lo justifica (`onDiscardConfirm` llama a
         `clear(gameId)` sin condición).
       - `buildEndGameBody(savedSummary: string): string` → texto NUEVO:
         `El progreso guardado de esta partida (${savedSummary}) se borrará y volveréis a la pantalla de inicio. Si el registro en el histórico falla, el progreso no se borrará y la app os avisará.`

    2. **Escribir, junto a `buildEndGameBody`, la comprobación de veracidad rama por rama**, esta
       vez correcta — es la corrección del motivo falso que `09-VERIFICATION.md` señala:
       - «Salir sin registrar» → `onOutcomeDismiss` → `finishGame()` sin argumento →
         `preserveProgress` es `false` → se borra. Cierta.
       - Registro con éxito → `planGameEnd(true, stored).preserveProgress` es `false` → se borra.
         Cierta.
       - Registro fallido, en **cualquiera** de los cuatro estados del dispositivo →
         `planGameEnd(false, stored).preserveProgress` es `true` → no se borra, y
         `notifyHistorySaved(plan.variant)` se invoca siempre en esa misma rama. Cierta.
       Y dejar escrito, en el mismo comentario, **qué se ha retirado y por qué**: la promesa de
       poder reintentar no la respalda `preserveProgress` —que solo mira `historyRecorded`— sino el
       estado del dispositivo, que en ese punto del flujo todavía no se ha leído; es falsa cuando
       ese estado resulta ser `absent` (no queda nada que reintentar) o `stale` (el reintento es
       justo lo que no se debe hacer).

    3. **Crear `app/composables/__tests__/useGameEndCopy.test.ts`**, test puro (sin `window`, sin
       jsdom), con un caso por viñeta de `<behavior>`. Incluir explícitamente las aserciones
       negativas `expect(buildEndGameBody(resumen)).not.toContain('conservar')` y
       `expect(buildEndGameBody(resumen)).not.toContain('reintentar')`, con un comentario que
       nombre `09-VERIFICATION.md` ronda 7 como el motivo de que existan.

    4. **Cablear `app/pages/[game]/index.vue`:** importar las dos funciones y dejar los dos
       `computed` reducidos a la llamada
       (`const discardBody = computed(() => buildDiscardBody(savedSummary.value))`, ídem
       `endGameBody`). Sustituir el comentario largo de `endGameBody` por una línea que remita al
       `.ts` («la copy y su comprobación de veracidad viven en `useGameEndCopy.ts`, que sí tiene
       test puro»), sin duplicar el razonamiento en dos sitios.

    5. **Dos renombres en el mismo fichero, mismo diff, coste cero y valor real para el gate de
       clase del plan 09-34** (que pasa a vigilar raíces léxicas, no subcadenas, y ahí un
       identificador indistinguible de una frase es ruido que obliga a auditar código que no es
       copy):
       - la variable local de `onOutcomeRecorded` `guardado` → `registrado` (es el resultado de
         `record()`, es decir «registrado en el histórico»; la propia fase lleva ocho caras
         confundiendo «guardado» con «registrado», así que el nombre nuevo es además el correcto).
         Renombrar sus cuatro apariciones, incluida la de `planGameEnd(...)`.
       - el `ref` `avisoProgresoNoComprobado` → `avisoLecturaNoComprobada` (lo que no se ha podido
         comprobar es la LECTURA del dispositivo). Renombrar también en el `:unverified-progress-notice`
         de la plantilla; el nombre de la prop de `MiniSetupScreen` **no** cambia.

    Todo el código y los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/useGameEndCopy.test.ts && npm run typecheck</automated>
    <fails_when>salida distinta de 0, o la línea de resumen contiene «0 passed», o el typecheck imprime alguna línea «error TS»</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch app/composables/useGameEndCopy.ts` sale 0 tras `git add` — el fichero es fuente versionada, no un artefacto.
    - `grep -c "export function buildEndGameBody" app/composables/useGameEndCopy.ts` devuelve 1 y `grep -c "export function buildDiscardBody" app/composables/useGameEndCopy.ts` devuelve 1.
    - `grep -c "el progreso no se borrará y la app os avisará" app/composables/useGameEndCopy.ts` devuelve 1.
    - `grep -c "not.toContain('conservar')" app/composables/__tests__/useGameEndCopy.test.ts` devuelve 1 y `grep -c "not.toContain('reintentar')" app/composables/__tests__/useGameEndCopy.test.ts` devuelve 1.
    - `grep -c "buildEndGameBody(savedSummary.value)" 'app/pages/[game]/index.vue'` devuelve 1 y `grep -c "buildDiscardBody(savedSummary.value)" 'app/pages/[game]/index.vue'` devuelve 1 — los dos `computed` son una sola llamada.
    - <!-- planner-discipline-allow: avisoProgresoNoComprobado --> `grep -c "avisoLecturaNoComprobada" 'app/pages/[game]/index.vue'` devuelve al menos 3 (declaración, asignación en `onMounted`, uso en plantilla) y `grep -c "avisoProgresoNoComprobado" 'app/pages/[game]/index.vue'` devuelve 0.
    - `grep -c "registrado = record(session.value, outcome)" 'app/pages/[game]/index.vue'` devuelve 1 y `grep -c "planGameEnd(registrado, stored)" 'app/pages/[game]/index.vue'` devuelve 1 — el renombre llegó a las dos apariciones que importan. Registrar además en el SUMMARY la salida literal de `grep -v "^\s*//" 'app/pages/[game]/index.vue' | grep -nw "guardado"`, para que el recuento residual quede escrito en vez de supuesto.
    - `npx vitest run app/composables/__tests__/useGameEndCopy.test.ts` sale 0 con al menos 5 tests.
    - `npm test` sale 0 (ningún test previo de la app se rompe con los renombres).
  </acceptance_criteria>
  <reversibility rating="reversible">
    Mover copy de un SFC a un `.ts` y renombrar dos identificadores locales se deshace con un
    `git revert` de un solo commit; no hay migración de datos ni contrato externo.
  </reversibility>
  <done>
    La única promesa que el diálogo de fin de partida hace sobre los datos del grupo es la que
    `preserveProgress = !historyRecorded` garantiza en las cuatro salidas, y esa promesa tiene un
    test puro que la fija y dos aserciones negativas que impiden que la retirada vuelva.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ningún `.vue` decide el tono del aviso escribiendo a mano un literal de `NoticeVariant`</name>
  <files>app/composables/useHistorySavedNotice.ts, app/components/HistorySavedNotice.vue, app/composables/__tests__/useHistorySavedNotice.test.ts</files>
  <read_first>
    - `app/components/HistorySavedNotice.vue` — COMPLETO, con atención a la línea 84
      (`:class="variant === 'success' ? …"`), que es la única comparación de variante fuera del
      composable.
    - `app/composables/useHistorySavedNotice.ts` líneas 26-68 — el tipo `NoticeVariant`,
      `resolveAutoDismissMs` (que ya compara contra `'success'` dentro del composable, el
      precedente exacto de lo que esta tarea añade) y `resolveNoticeVariant`.
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` líneas 370-395 — el test WR-03 de
      Gate C y su `LITERALES_VARIANTE`, que el plan **09-34** ampliará con `'success'`. Esta tarea
      existe para que esa ampliación no tenga que abrir una excepción nueva.
  </read_first>
  <behavior>
    - `isSuccessVariant('success')` es `true`.
    - `isSuccessVariant(v)` es `false` para las cuatro variantes de fallo.
    - `HistorySavedNotice.vue` no contiene ningún literal de `NoticeVariant`.
    - El aviso sigue pintándose igual: primera letra en color de acento cuando la variante es la de
      éxito, y en color de aviso en las cuatro de fallo.
  </behavior>
  <action>
    1. **Exportar `isSuccessVariant(variant: NoticeVariant): boolean`** desde
       `app/composables/useHistorySavedNotice.ts`, junto a `resolveAutoDismissMs` (que ya hace esa
       misma comparación internamente; reutilizarla desde `resolveAutoDismissMs` para que la
       comparación literal exista en un solo sitio del fichero). Comentario de una línea: el
       componente no puede escribir el literal a mano, porque quien escribe una variante a mano es
       quien afirma sobre el dispositivo sin haber pasado por `readStoredProgress` — el gesto que
       Gate C persigue (WR-03) y que el plan 09-34 extiende también a `'success'`, el literal más
       peligroso de escribir a mano porque pinta el aviso de éxito sin haber pasado por `record()`.

    2. **`HistorySavedNotice.vue`:** importar `isSuccessVariant` y sustituir la comparación de la
       línea 84 por `:class="isSuccessVariant(variant) ? 'first-letter:text-accent' : 'first-letter:text-warning'"`.
       El componente sigue siendo tonto: no decide nada, solo pregunta. Como `variant` puede ser
       `null` mientras no hay aviso, mantener la guarda `v-if` que ya envuelve ese bloque, o
       adaptar la llamada al tipo real de la prop sin introducir ningún `as` — si el typecheck se
       queja, la solución correcta es estrechar el tipo en el `v-if`, nunca silenciarlo.

    3. **Tests en `useHistorySavedNotice.test.ts`:** un caso que recorra las cinco variantes y fije
       `isSuccessVariant` en cada una, y un caso que fije que `resolveAutoDismissMs` y
       `isSuccessVariant` coinciden (la duración corta es exactamente la de la variante de éxito).

    Todo el código y los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/useHistorySavedNotice.test.ts && npm run typecheck</automated>
    <fails_when>salida distinta de 0, o la línea de resumen contiene «0 passed», o el typecheck imprime alguna línea «error TS»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function isSuccessVariant" app/composables/useHistorySavedNotice.ts` devuelve 1.
    - `grep -c "isSuccessVariant(variant)" app/components/HistorySavedNotice.vue` devuelve 1.
    - <!-- planner-discipline-allow: 'success' --> `grep -v "^\s*//" app/components/HistorySavedNotice.vue | grep -c "'success'"` devuelve 0 — el componente ya no nombra ninguna variante (el `grep -v` evita que la prosa del comentario largo falsee el recuento).
    - `npx vitest run app/composables/__tests__/useHistorySavedNotice.test.ts` sale 0.
    - `npm run typecheck` sale 0 y `grep -c "ts-expect-error" app/components/HistorySavedNotice.vue` devuelve 0 — el typecheck pasa sin silenciar nada.
    - `npm test` sale 0.
  </acceptance_criteria>
  <done>
    El único fichero de la app que nombra una variante del aviso es el que la define, y el plan
    09-34 puede añadir `'success'` a Gate C sin abrir ninguna excepción nueva.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → `PersistedPosition` | La posición en disco viene de `JSON.parse` de `localStorage`: editable a mano en DevTools, escribible por otra pestaña o por una extensión |
| Copy de la app → grupo | Cada frase que la app enseña sobre los datos guardados del grupo es una afirmación que el grupo usará para decidir si reintenta el registro |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-32-01 | Spoofing | `NOTICE_BODY['failure-stale']` afirmando identidad/anterioridad de la partida en disco | high | mitigate | La frase nueva no afirma identidad ni orden temporal; declara explícitamente que la app no puede saberlo, y un test fija ambas ausencias por texto |
| T-09-32-02 | Tampering | El histórico (dato irreconstruible, D-13) recibiendo `round`/`context` de otra partida porque la copy indujo el reintento | high | mitigate | `failure-stale` no ordena ningún reintento y la forma modal advierte del riesgo; criterio de aceptación con `grep` sobre «reintentar» fuera de comentarios |
| T-09-32-03 | Repudiation | Un motivo escrito falso en la auditoría sellando un hueco con sello de verificado | medium | mitigate | La comprobación rama por rama de `endGameBody` se reescribe corregida junto a la función, y el plan 09-34 elimina la entrada auditada de `index.vue` en vez de corregir su prosa |
| T-09-32-04 | Elevation of privilege | Un `savedSummary` con contenido manipulado desde `localStorage` interpolado en la copy | low | accept | Vue interpola como texto, nunca como HTML; el riesgo se limita a un resumen con texto raro, sin ejecución — mismo trato que ya recibe `discardBody` |
| T-09-32-SC | Tampering | Instalaciones de paquetes (npm) | low | accept | Este plan no instala ni actualiza ninguna dependencia: `git diff -- package.json package-lock.json` debe quedar vacío |
</threat_model>

<verification>
- `npm test` en verde (los dos proyectos de Vitest).
- `npm run typecheck` en verde.
- `git diff -- package.json package-lock.json` vacío.
- `git diff --stat -- engine/` vacío: este plan no toca el motor.
- `grep -rEi "firestore|firebase" app/ engine/` no devuelve ninguna línea.
</verification>

<success_criteria>
- Las cuatro oraciones de `NOTICE_BODY['failure-stale']` se pueden señalar, una a una, a un hecho
  que `readStoredProgress`/`esLaMismaPartida` establecen, y las dos afirmaciones que no lo estaban
  han desaparecido del texto y están fijadas por aserción negativa.
- El diálogo de fin de partida no promete ningún reintento, y su única promesa está pinchada por
  un test puro en un `.ts`.
- El fichero `app/pages/[game]/index.vue` ya no contiene copy propia sobre los datos guardados del
  grupo: toda ha salido a `useGameEndCopy.ts`.
- Ningún `.vue` nombra un literal de `NoticeVariant`.
</success_criteria>

<output>
Create `.planning/phases/09-hist-rico-y-estad-sticas/09-32-SUMMARY.md` when done
</output>
