---
phase: 09-hist-rico-y-estad-sticas
plan: 31
type: execute
wave: 24
depends_on: ["09-28", "09-29", "09-30"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md
  - .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
  - .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
autonomous: true
requirements: [HIST-04, HIST-06, HIST-09]

must_haves:
  truths:
    - "`REQUIREMENTS.md` registra que la ronda 6 encontró una SÉPTIMA variante del mismo patrón y no da el hueco por cerrado: HIST-06 sigue `[ ]` hasta que una ronda de verificación lo confirme"
    - "El método de auditoría de afirmaciones incorpora la pregunta que la ronda 6 demostró que faltaba —si el dato mide el MISMO objeto del que la frase habla— y cierra la regla de exclusión que dejó pasar `endGameBody`"
    - "La fila de `HistorySavedNotice.vue` deja de estar marcada como respaldada por una lectura sin más: dice qué lectura, contra qué se compara y qué la respalda ahora"
    - "Lo que este lote deja sin cerrar está registrado con una evaluación de RIESGO que se sostiene sola, nunca con un motivo de alcance de plan"
    - "El contrato de copy de la fase describe el texto que la app enseña de verdad tras este lote, incluidas las dos frases nuevas"
    - "La comprobación visual humana en tablet (DEV-02) sigue nombrada como ABIERTA, en ningún sitio como hecha"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "el párrafo de la ronda 6 y la tabla de trazabilidad sincronizada a mano"
      contains: "ronda 6"
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md"
      provides: "Q6, la fila de HistorySavedNotice corregida y las filas nuevas del lote"
      contains: "Q6"
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md"
      provides: "el riesgo del autoguardado tras una lectura fallida, evaluado y aceptado por escrito"
      contains: "(ronda 6)"
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md"
      provides: "el contrato de copy con las frases nuevas y el aviso de la cuarta variante"
      contains: "failure-stale"
  key_links:
    - from: ".planning/REQUIREMENTS.md (nota de HIST-06)"
      to: "app/composables/useStoredProgress.ts (esLaMismaPartida)"
      via: "la nota nombra el código concreto que cierra la séptima cara, no una intención"
      pattern: "esLaMismaPartida"
---

<objective>
Poner la documentación de la fase al día con lo que el código hace de verdad, **después** de que
09-28, 09-29 y 09-30 estén verdes — nunca antes. Cuatro notas anteriores de `REQUIREMENTS.md`
dieron por cerrado lo que no lo estaba; escribir la quinta por adelantado sería repetir el defecto
que esta fase persigue, esta vez en la documentación.

Cuatro encargos concretos, todos ellos nombrados en el `missing:` de `09-VERIFICATION.md` o en sus
`artifacts:`:

1. **`REQUIREMENTS.md`** debe registrar que la ronda 6 encontró una **séptima variante** del mismo
   patrón —se cambió el respaldo de un booleano de escritura a una lectura real, correcto, pero esa
   lectura contestaba una pregunta distinta de la que el texto planteaba— sin dar el hueco por
   cerrado. HIST-06 sigue `[ ]`.
2. **`09-AUDIT-AFIRMACIONES-UI.md`** debe corregir la fila de `HistorySavedNotice.vue` que Q5 marcó
   como RESPALDADA sin llegar al fondo, y cerrar el agujero de método que la dejó pasar.
3. **`deferred-items.md`** debe registrar, con evaluación de riesgo, lo único que este lote deja
   abierto a sabiendas.
4. **`09-UI-SPEC.md`** debe describir la copy que la app enseña de verdad tras este lote.

Ojo, por escrito y con memoria del proyecto detrás: `requirements.mark-complete` del gsd-sdk deja
las filas de la tabla de trazabilidad obsoletas **en silencio**; la sincronización de esas filas es
a mano, siempre.

Purpose: que quien lea la documentación de la fase sepa en qué estado real está HIST-06 y por qué,
y que el método de auditoría no vuelva a dejar pasar la misma clase de defecto.
Output: los cuatro documentos al día, sin declarar ningún cierre que no le corresponde declarar a
un plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-28-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-29-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-30-SUMMARY.md
</context>

<scope_boundary>
**DENTRO:** los cuatro ficheros de `files_modified`. Ninguno es código.

**FUERA:**
- Cualquier fichero bajo `app/` o `engine/`. Si esta tarea necesitara tocar código, es que algo
  salió mal en 09-28/09-29/09-30 y hay que **decirlo en el SUMMARY**, no arreglarlo aquí a
  escondidas.
- Marcar HIST-06 como `[x]` → lo decide una ronda de verificación, nunca un plan. Esto ya salió mal
  una vez (ver `09-27-SUMMARY.md`).
- Marcar la fase como completa en `ROADMAP.md`.
- Las filas de trazabilidad de los requisitos ajenos a esta fase (`SEL-*`, `COMP-*`, `VOZ-*`, …) →
  siguen desincronizadas por el problema conocido de `requirements.mark-complete`, ya registrado en
  `deferred-items.md` desde el plan 09-03. Corregirlas a ciegas aquí es lo que esa entrada
  desaconseja: exige cotejar cada fila contra los `requirements-completed` de cada SUMMARY, y eso
  es un barrido propio.
- Rehacer el barrido de los 24 `.vue` de `09-AUDIT-AFIRMACIONES-UI.md` §2 entero. Solo se tocan las
  filas que este lote cambia (ver Task 2). Afirmar un barrido que no se ha hecho sería,
  literalmente, el defecto que ese documento audita.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: `REQUIREMENTS.md` registra la séptima variante sin declarar un cierre</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — el bloque de checkboxes `HIST-01..HIST-09` (~`:75-83`), la nota
      de cierre de hueco COMPLETA (~`:85-125`, incluidos los párrafos «rondas 2 y 3», «Ronda 4» y
      «Ronda 5» que el plan 09-27 dejó escritos) y la tabla de trazabilidad (~`:255-268`), en
      especial las filas HIST-04, HIST-06 y HIST-09.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — COMPLETO. El quinto punto
      de la lista `missing:` del Gap #1 es literalmente el encargo de esta tarea; la fila HIST-06 de
      su tabla «Requirements Coverage» y el bloque `human_verification:` fijan qué NO se puede dar
      por cerrado.
    - Los SUMMARY de `09-28`, `09-29` y `09-30` — qué se construyó de verdad, con qué nombres de
      fichero y de función. La nota tiene que nombrar código que existe; si un nombre cambió durante
      la ejecución, gana el nombre real, no el del plan.
    - La memoria del usuario `gsd-sdk-requirements-tabla-espanol.md` — la tabla de trazabilidad se
      sincroniza a mano; ninguna herramienta lo hace sola.
  </read_first>
  <action>
    1. **Checkbox de HIST-06:** comprobar que sigue `[ ]` y **dejarlo así**. Esta tarea no lo marca.
       Si por cualquier motivo apareciera `[x]`, devolverlo a `[ ]` y decirlo en el SUMMARY.

    2. **Añadir un párrafo «Ronda 6 (`09-VERIFICATION.md`, planes 09-28/09-29/09-30)»** al final de
       la nota de cierre de hueco, sin borrar ni reescribir los anteriores. Debe contener, como
       mínimo:

       - **El hallazgo, en una frase:** el cierre de la ronda 5 sustituyó el booleano de escritura
         por una lectura real —eso quedó genuinamente cerrado—, pero la lectura que lo sustituye
         contestaba «¿hay ALGUNA partida reanudable?» mientras la copy de `failure-recoverable`
         planteaba «¿sigue guardada ESTA partida?». Séptima cara del mismo patrón. Nombrar la
         consecuencia concreta: el reintento que esa frase ordenaba podía escribir en el
         histórico —el único dato irreconstruible de la app— la ronda y la selección de
         héroes/villano de un autoguardado anterior, sin ningún aviso.
       - **El agravante, sin suavizarlo:** el propio test de regresión que el plan 09-26 escribió
         para cerrar la ronda 5 (`avisoTrasRegistroFallido.test.ts`, test 5) reproducía ese
         escenario y **fijaba la respuesta defectuosa en verde como comportamiento esperado**.
       - **El segundo hallazgo, de proceso:** el gate de clase que 09-26 introdujo para «proteger la
         CLASE» inspeccionaba el 1,9% de la plantilla del único fichero donde han vivido cinco de
         las siete caras, por una regex perezosa que cortaba en el primer `</template>` anidado —
         la razón estructural por la que la séptima cara no se detectó sola.
       - **El cierre elegido**, nombrando código que existe: `readStoredProgress(game, esperada?)`
         y `esLaMismaPartida` (`app/composables/useStoredProgress.ts`), el cuarto valor `'stale'`,
         la variante `failure-stale` y la reescritura de `failure-unknown`
         (`app/composables/useHistorySavedNotice.ts`), `planProgressMount`
         (`app/composables/useProgressMountPlan.ts`) para que el montaje deje de colapsar `'absent'`
         con `'unknown'`, el gate reescrito con región vigilada completa, barrido de `.ts`,
         excepciones por frase y auto-verificación
         (`app/composables/__tests__/afirmacionesRespaldadas.test.ts`), y el arreglo de `endGameBody`
         (`app/pages/[game]/index.vue`), abierto desde la ronda 4 como IN-03/WR-04.
       - **Los WARNING de la ronda 5 que este lote SÍ cerró:** WR-05 (el comentario derogado de
         `HistorySavedNotice.vue` que instruía reintroducir el booleano), WR-06 (la ventana sin
         `try/catch` en `onOutcomeRecorded`) y WR-07 (`PLACEHOLDER_CONTEXT` mutable y compartido).
         Decirlo aquí es lo que impide que reaparezcan como «pendientes» sin serlo.
       - **La frase final que esta nota no puede omitir:** *este cierre no se da por bueno hasta que
         una ronda de verificación independiente lo confirme; cuatro notas anteriores de este mismo
         documento dieron por cerrado lo que no lo estaba.*

    3. **Tabla de trazabilidad**, a mano (la herramienta no lo hace):
       - Fila `HIST-06` → rango de planes `Fase 9 (09-13..09-31)`, estado
         `Reabierto en la ronda 5, séptima variante encontrada en la ronda 6 — ver nota de cierre de hueco abajo`.
       - Fila `HIST-04` → añadir `09-28` a su columna de planes y conservar el matiz de riesgo: el
         registro puede construirse a partir de un `session` desactualizado en la rama de reintento;
         ese camino es el que 09-28 cierra.
       - Fila `HIST-09` → actualizar el rango a `(09-13..09-31)`, conservando `Satisfecho`.
       - Ninguna otra fila se toca (ver `<scope_boundary>`).

    4. Comprobar que **DEV-02** sigue nombrando la comprobación humana en tablet como pendiente, y
       añadirle a su guion los dos puntos nuevos que este lote introduce: el aviso de lectura no
       comprobada en el mini-setup y el aviso de la variante `failure-stale` (20 s, texto largo).
       No marcar DEV-02 como hecho bajo ninguna circunstancia.

    Todo en español. No tocar ningún fichero de código.
  </action>
  <verify>
    <automated>grep -c "ronda 6" .planning/REQUIREMENTS.md; grep -c "esLaMismaPartida" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "HIST-06" .planning/REQUIREMENTS.md | grep -c "\[ \]"` devuelve 1 — el checkbox sigue sin marcar.
    - `grep -c "Ronda 6" .planning/REQUIREMENTS.md` devuelve al menos 1.
    - `grep -c "séptima" .planning/REQUIREMENTS.md` devuelve al menos 1.
    - `grep -c "esLaMismaPartida" .planning/REQUIREMENTS.md` devuelve al menos 1 y `grep -c "planProgressMount" .planning/REQUIREMENTS.md` devuelve al menos 1 — la nota nombra código que existe.
    - `grep -c "no se da por bueno hasta que" .planning/REQUIREMENTS.md` devuelve al menos 2 (la frase de la ronda 5 y la de la ronda 6).
    - `grep "^| HIST-06 |" .planning/REQUIREMENTS.md | grep -c "09-31"` devuelve 1 y `grep "^| HIST-09 |" .planning/REQUIREMENTS.md | grep -c "09-31"` devuelve 1.
    - `grep -c "DEV-02" .planning/REQUIREMENTS.md` devuelve al menos 1 y ninguna de sus líneas contiene `[x]` ni la palabra «completado». Verificarlo y dejarlo escrito en el SUMMARY.
    - `git diff --stat` de esta tarea toca exclusivamente `.planning/REQUIREMENTS.md`.
  </acceptance_criteria>
  <done>
    `REQUIREMENTS.md` dice la verdad sobre HIST-06, nombra el código concreto de la ronda 6 y no
    declara un cierre que no le corresponde declarar.
  </done>
</task>

<task type="auto">
  <name>Task 2: el método de auditoría gana la pregunta que faltaba y las filas afectadas se corrigen</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` — COMPLETO: §1
      «Método» con Q1-Q5, el criterio de inclusión, los tres veredictos y **la regla de exclusión
      por “acción futura”**; §2 «Inventario», en especial las filas de `HistorySavedNotice.vue`,
      `GameOutcomeDialog.vue`, `MiniSetupScreen.vue`, `ResumePrompt.vue` y `ContentChangedNotice.vue`;
      §3 «Defectos»; el «Addendum (ronda 5, plan 09-27)»; §4 «Recuento» y §5 «La regla que este
      barrido deja escrita para el futuro».
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el `artifacts:` del Gap #1
      que nombra este fichero («Q5 se aplicó parcialmente… se quedó en “viene de una lectura”, sin
      llegar a “¿de la lectura CORRECTA para la pregunta que el texto plantea?”») y la fila
      `⚠ INCOMPLETO` de la tabla de artefactos.
    - Los SUMMARY de `09-28`, `09-29` y `09-30` — qué frases existen ahora y qué las respalda.
  </read_first>
  <action>
    1. **§1 «Método» — añadir Q6:** *¿la lectura que respalda esta frase mide el MISMO objeto del
       que la frase habla?* Explicar de dónde nace, sin suavizarlo: Q5 («¿el dato que respalda esta
       frase responde a la MISMA pregunta que la frase plantea?») se añadió en la ronda 5
       precisamente para cazar este patrón, y en la ronda 6 se demostró que **se había aplicado a
       medias** sobre `HistorySavedNotice.vue`: se comprobó que el dato venía de una lectura real
       (que era el hallazgo de la ronda 5) y no que esa lectura fuera de **la misma partida** de la
       que el texto hablaba. Q5 distingue «escritura vs lectura»; Q6 distingue «alguna vs ésta».
       Dejar escrito que las dos hay que hacerlas, en ese orden, y que aprobar Q5 no exime de Q6.

    2. **§1 — cerrar la regla de exclusión por «acción futura».** Hoy `GameOutcomeDialog.vue` está
       marcado `NO APLICA` con este motivo literal: «describen… una acción FUTURA — “Se borrará el
       progreso guardado…” —, no un hecho ya comprobado; excluido por la regla de acción futura».
       Esa exclusión es lo que dejó vivir a `endGameBody` desde la ronda 4. Reescribir la regla: una
       promesa sobre una acción futura **sí** entra en el barrido cuando la app es quien ejecuta esa
       acción y tiene una rama en la que no la ejecuta; lo que queda fuera son las promesas sobre
       cosas que no dependen de la app (p. ej. que la pantalla siga encendida). Nombrar `endGameBody`
       como el ejemplar que esta regla dejó pasar durante tres rondas.

    3. **§2 — corregir la fila de `app/components/HistorySavedNotice.vue`.** Conservar el texto
       tachado que ya tiene (es el registro de cómo se llegó hasta aquí) y añadir el tramo de la
       ronda 6: el veredicto de la ronda 5 volvió a quedarse corto bajo Q6, porque
       `readStoredProgress(game)` solo podía contestar «¿hay alguna?»; tras el plan 09-28 la
       variante la decide `planGameEnd` a partir de un `StoredProgress` de **cuatro** valores
       producido por `readStoredProgress(game, esperada)`, que compara `runtimeId`/`round`/`context`
       de lo que hay en disco contra la sesión que acaba de terminar (`esLaMismaPartida`), y existe
       la variante `failure-stale` para el caso «hay algo, pero no es esto». Veredicto nuevo:
       `RESPALDADA bajo Q6 desde la ronda 6 (09-28) — pendiente de confirmación por verificación`.

    4. **§2 — corregir la fila de `app/components/GameOutcomeDialog.vue`:** deja de ser `NO APLICA`.
       Su `warningBody` es `endGameBody`, calculado en `app/pages/[game]/index.vue`, y era falso en
       la rama `preserveProgress: true`; el plan 09-30 lo reescribió para que sea cierto en las
       cuatro salidas del diálogo. Veredicto: `DEFECTO (rondas 4-6) → CORREGIDO en 09-30`, con el
       texto nuevo citado y la tabla de veracidad rama por rama resumida en una línea.

    5. **§2 — añadir dos filas nuevas**, una por cada superficie que este lote crea:
       - `app/components/MiniSetupScreen.vue` → deja de ser `NO APLICA`: ahora puede pintar
         `unverifiedProgressNotice`. Respaldo: `planProgressMount` solo emite esa frase con
         `stored === 'unknown'`, y la frase afirma exactamente eso (que la lectura falló), sin negar
         ni afirmar la existencia de una partida. Veredicto `RESPALDADA`.
       - `app/composables/useProgressMountPlan.ts` → la casa de esa copy, con su test puro
         (`useProgressMountPlan.test.ts`) y su tabla de verdad total sobre los cuatro valores.
         Veredicto `RESPALDADA`.

    6. **§3 y §4** — actualizar el recuento y añadir en «Defectos» la entrada de la ronda 6 (la
       séptima cara y el gate que no vigilaba), enlazando a `09-VERIFICATION.md`.

    7. **§5 — actualizar la regla para el futuro** con lo que el gate hace ahora de verdad: barre el
       fichero entero menos comentarios y `<style>`, incluye `<script setup>` y `app/**/*.ts`, tiene
       excepciones **por frase** con motivo escrito, y se autocomprueba con un SFC sintético de
       `<template>` anidado. La regla de método es: *ninguna frase nueva sobre los datos del grupo
       entra sin pasar Q1-Q6 y sin que el gate la vea; si el gate no la ve, el defecto está en el
       gate.*

    8. **No rehacer el barrido de los 24 `.vue`.** Decir por escrito que las demás filas se
       mantienen tal cual porque esos ficheros no han cambiado en este lote, y nombrar cuáles sí se
       han tocado.

    Todo en español. No tocar ningún fichero de código.
  </action>
  <verify>
    <automated>grep -c "Q6" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md; grep -c "failure-stale" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Q6" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 4 (enunciado en §1, justificación, fila de `HistorySavedNotice.vue`, regla de §5).
    - `grep -c "esLaMismaPartida" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 1.
    - `grep -c "acción FUTURA" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` — el valor puede seguir siendo ≥1 (la regla se reescribe, no se borra), pero la línea de `GameOutcomeDialog.vue` ya NO puede contener `NO APLICA`: comprobar con `grep "GameOutcomeDialog.vue" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md | grep -c "NO APLICA"` → 0.
    - `grep -c "useProgressMountPlan" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 1 y `grep -c "unverifiedProgressNotice" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 1.
    - `grep -c "regionVigilada" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 1 — §5 describe el gate que existe.
    - El SUMMARY lista qué filas de §2 se tocaron y cuáles no, y por qué.
    - `git diff --stat` de esta tarea no toca ningún fichero bajo `app/` ni `engine/`.
  </acceptance_criteria>
  <done>
    El método de auditoría incorpora la pregunta que la ronda 6 demostró que faltaba, la regla que
    dejó pasar `endGameBody` está cerrada, y las filas afectadas dicen lo que el código hace hoy.
  </done>
</task>

<task type="auto">
  <name>Task 3: registrar por escrito lo que queda abierto, y el contrato de copy al día</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md, .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` — COMPLETO. El formato de cada
      entrada (título, «Encontrado durante», «Descripción», «Por qué no se corrige aquí», «Acción
      sugerida») y, sobre todo, el dictamen que varias entradas citan: un aplazamiento justificado
      por ALCANCE DE PLAN **no es válido**; la justificación tiene que ser una evaluación de RIESGO.
      Fijarse también en las entradas `WR-04`/`WR-05` **(ronda 4)**, para que las nuevas lleven
      `(ronda 6)` en el título y no se confundan con ellas.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` — §8 «Save-result notice» y la
      sección «Copywriting Contract», en especial las filas «Save-result notice — failure (D-03)» y
      «Outcome dialog retained warning (D-02, unchanged text, only relocated)».
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-02 — la consecuencia nº 1 (el
      autoguardado que machaca una posición no comprobada), que es exactamente lo que esta tarea
      registra como riesgo evaluado.
    - Los SUMMARY de `09-28`, `09-29` y `09-30` — los textos EXACTOS que quedaron en el árbol. El
      contrato de copy debe citarlos literalmente, no de memoria: copiarlos del código.
  </read_first>
  <action>
    **A. `deferred-items.md`.**

    1. **Una entrada nueva:** `## WR-02 (ronda 6) — tras una lectura fallida del progreso, el primer autoguardado de la partida nueva sobrescribe lo que no se pudo leer`.
       - *Encontrado durante:* `09-REVIEW.md` (ronda 5, WR-02) y confirmado por `09-VERIFICATION.md`
         (ronda 6) como WARNING.
       - *Descripción:* con `stored === 'unknown'` al montar, la app enseña el mini-setup; el grupo
         empieza otra partida y el primer `watchDebounced` que consiga escribir machaca
         `tga:progress:<gameId>`, destruyendo una posición que quizá seguía ahí y que solo era
         ilegible en ese instante. El plan 09-29 cierra la mitad honesta —la app **avisa** antes,
         con `UNVERIFIED_PROGRESS_NOTICE`— pero no corta ni condiciona el autoguardado.
       - *Por qué no se corrige aquí (evaluación de RIESGO, no de alcance):* en las dos causas
         realistas de un fallo de lectura —modo privado y cuota— la **escritura falla igual**, así
         que no hay nada que destruir; el escenario dañino exige una lectura que falla y una
         escritura posterior que funciona, es decir un fallo transitorio. El radio de impacto es el
         PROGRESO de una partida (`tga:progress:<gameId>`), nunca el histórico (`tga:history`, otra
         clave, intacta por HIST-09), que es el único dato irreconstruible. Y el grupo ha recibido
         el aviso antes de empezar, así que la acción es informada. Cortar el autoguardado a cambio
         dejaría sin guardar una partida que el grupo sí ha empezado a propósito — un daño cierto
         para evitar uno improbable.
       - *Supuesto NO verificado (hallazgo del verificador de planes, ronda 6):* la frase «en las
         dos causas realistas la escritura falla igual» asume que las causas de un fallo de LECTURA
         coinciden con las de un fallo de ESCRITURA. Pero `usePersistedSession.ts` solo marca
         `read: 'failed'` cuando `getItem` lanza o no hay `window` — un disparador más estrecho y
         menos correlacionado con `QuotaExceededError` en `setItem` de lo que la justificación
         supone, así que «la lectura falla y la escritura posterior funciona» puede ser menos raro
         de lo que este texto sugiere. El diferimiento se mantiene (la mitigación de 09-29 sigue en
         pie y el radio sigue siendo el progreso, nunca `tga:history`), pero la entrada debe hacer
         constar por escrito que esta correlación está SUPUESTA y no comprobada contra el código,
         para que una ronda futura pueda revisarla sin tener que volver a deducirla.
       - *Acción sugerida:* si alguna vez se aborda, archivar el blob ilegible bajo
         `tga:progress:<gameId>:backup-<ts>` antes del primer `save()` de la partida nueva, en vez
         de condicionar el autoguardado.

    2. **Confirmar por escrito, en una nota al final del fichero, qué NO se difiere de la ronda 5:**
       WR-05 (comentario derogado en `HistorySavedNotice.vue`), WR-06 (`try/catch` en
       `onOutcomeRecorded`) y WR-07 (`PLACEHOLDER_CONTEXT` mutable) quedaron **cerrados** por el plan
       09-28, y WR-03/WR-04 (los huecos del gate) e IN-03/IN-05 por el plan 09-30. Nombrarlos uno a
       uno con su plan. Es lo que impide que reaparezcan como pendientes sin serlo, y lo que impide
       que se den por cerrados los que no lo están.

    3. **Comprobar que las entradas de la ronda 4** sobre el solapamiento `UpdateBanner`/
       `HistorySavedNotice` y sobre `pointer-events-none` siguen presentes y **abiertas**, y que la
       nota de la comprobación humana en tablet bajo DEV-02 sigue diciendo que está ABIERTA. No
       cerrarlas. Si el texto de alguna sugiere lo contrario, corregirlo.

    4. Si los SUMMARY de 09-28/09-29/09-30 registraron algún hallazgo nuevo no cerrado (p. ej. un
       ejemplar nuevo detectado por el gate ampliado), abrirle entrada propia con el mismo formato y
       con evaluación de riesgo. Si no hubo ninguno, decirlo explícitamente en el SUMMARY de este
       plan.

    **B. `09-UI-SPEC.md` — el contrato de copy describe lo que la app enseña.**

    5. En «Copywriting Contract», sustituir la fila «Save-result notice — failure (D-03)» por
       **cuatro filas**, una por variante (`failure-recoverable`, `failure-stale`,
       `failure-unrecoverable`, `failure-unknown`), con el texto **copiado literalmente** de
       `NOTICE_BODY` en `app/composables/useHistorySavedNotice.ts`. Añadir una nota de una línea que
       diga que este contrato refleja el estado tras la ronda 6 y que la copy original de esta fila
       (la de D-03, «El dispositivo no permitió escribir…») fue retirada en rondas anteriores por
       afirmar una causa que la app no podía comprobar.

    6. Sustituir la fila «Outcome dialog retained warning (D-02, unchanged text, only relocated)»
       por el texto nuevo de `endGameBody` (plan 09-30), copiado literalmente, con una nota: D-02
       exigía conservar el texto **tal cual**, y la ronda 6 demostró que ese texto era falso en la
       rama `preserveProgress: true`; se conserva la intención de D-02 (advertir antes de terminar)
       y se corrige el hecho.

    7. Añadir una fila nueva: «Mini-setup — lectura del progreso no comprobada (ronda 6)» con el
       texto literal de `UNVERIFIED_PROGRESS_NOTICE`, y una nota de que solo se muestra con
       `stored === 'unknown'`.

    8. En §8 «Save-result notice», añadir un párrafo que diga que las variantes son cuatro y por
       qué, enlazando a `useHistorySavedNotice.ts` como fuente de verdad del texto. No redibujar los
       diagramas ASCII.

    Todo en español. No tocar ningún fichero de código.
  </action>
  <verify>
    <automated>grep -cE "^## WR-[0-9]+ \(ronda 6\)" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md; grep -c "failure-stale" .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## WR-[0-9]+ \(ronda 6\)" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1.
    - La entrada WR-02 (ronda 6) de `deferred-items.md` hace constar el supuesto no verificado:
      `grep -c "SUPUESTA" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve
      al menos 1, y esa línea nombra la correlación entre fallo de lectura y fallo de escritura.
    - La entrada nueva NO contiene «fuera del perímetro de este plan», «fuera del alcance de este plan» ni «no entraba en el encargo». Comprobarlo con `grep -n "fuera del perímetro\|fuera del alcance\|no entraba en el encargo"` y verificar a mano que las coincidencias pertenecen todas a entradas ANTIGUAS. Dejarlo dicho en el SUMMARY, citando el dictamen de la ronda 4.
    - `grep -c "WR-05" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` y `grep -c "WR-06" …` devuelven al menos 1 cada uno, y la nota del punto A.2 existe: `grep -c "cerrados por el plan 09-28" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1.
    - `grep -c "DEV-02" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 2 y ninguna de esas líneas dice que la comprobación humana esté hecha.
    - `grep -c "failure-stale" .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` devuelve al menos 1 y `grep -c "failure-unknown" …` devuelve al menos 1.
    - `grep -c "al guardar la nueva podríais sustituirla" .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` devuelve 1 — la copy del mini-setup está en el contrato, copiada del código.
    - **Comprobación de fidelidad, obligatoria:** para cada texto copiado al contrato de copy, ejecutar un `grep -c` de la misma cadena contra el fichero de código que la define y confirmar que devuelve ≥1 en ambos sitios. Listar en el SUMMARY los pares (documento, fichero de código) comprobados. Un contrato de copy que no coincide con el código es, otra vez, una afirmación sin respaldo.
    - `git diff --stat` de esta tarea toca exclusivamente los dos ficheros de `files_modified`.
  </acceptance_criteria>
  <done>
    Lo que se deja sin cerrar está escrito con un motivo que aguanta una lectura crítica, lo cerrado
    está nombrado con su plan, y el contrato de copy coincide literalmente con lo que la app enseña.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentación → futuras rondas de planificación | Una nota que declara un cierre inexistente dirige el trabajo siguiente hacia otro sitio. Es el mecanismo por el que esta fase lleva seis rondas: cuatro notas anteriores dieron por cerrado lo que no lo estaba. |
| `requirements.mark-complete` (gsd-sdk) → tabla de trazabilidad | La herramienta deja las filas obsoletas **en silencio** (memoria del proyecto). Confiar en ella es confiar en una escritura cuyo resultado nadie ha leído. |
| Contrato de copy → código | Si el contrato y el código divergen, el contrato pasa a ser una afirmación sin respaldo sobre lo que la app dice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-31-01 | Repudiation (declarar un cierre no verificado) | La nota de HIST-06 en `.planning/REQUIREMENTS.md` | mitigate | Task 1: HIST-06 sigue `[ ]`, la nota registra el hallazgo y el cierre elegido pero termina con la frase que prohíbe darlo por bueno hasta una verificación independiente. Criterio: `grep -c "no se da por bueno hasta que"` ≥ 2. |
| T-09-31-02 | Tampering (desincronización silenciosa) | Tabla de trazabilidad de `REQUIREMENTS.md` frente a los `requirements-completed` reales | mitigate | Task 1 punto 3: sincronización **a mano** de las tres filas que este lote toca, con criterios `grep` sobre la fila concreta (no sobre todo el fichero, para que la prosa no falsee el recuento). El resto sigue registrado como deuda desde 09-03. |
| T-09-31-03 | Repudiation (aplazar sin evaluar) | `deferred-items.md` | mitigate | Task 3: la entrada nueva lleva evaluación de riesgo explícita (qué hace falta para alcanzarlo, qué pasa como mucho, por qué es asumible) y el criterio de aceptación prohíbe las tres fórmulas de alcance de plan que la ronda 4 invalidó. |
| T-09-31-04 | Spoofing (documento que no describe el producto) | Contrato de copy de `09-UI-SPEC.md` | mitigate | Task 3, comprobación de fidelidad: cada texto del contrato se verifica con `grep` contra el fichero de código que lo define, y los pares comprobados se listan en el SUMMARY. |
| T-09-31-05 | Repudiation (dar por hecha una verificación humana) | DEV-02, pendiente desde `09-22-SUMMARY.md` | mitigate | Tasks 1 y 3: DEV-02 sigue nombrado como ABIERTO en los dos documentos, y se le añaden al guion las dos superficies nuevas (aviso del mini-setup y variante `failure-stale`), en vez de cerrarlo. |
| T-09-31-06 | Tampering (código tocado desde un plan de documentación) | `app/`, `engine/` | mitigate | `<scope_boundary>` explícito + criterio de aceptación por tarea: `git diff --stat` no puede incluir ningún fichero de código. Si hiciera falta tocarlo, se reporta, no se arregla aquí. |
| T-09-31-SC | Tampering (cadena de suministro) | `npm` / `package.json` | n/a | Este plan **no instala ningún paquete** y no toca código. |
</threat_model>

<verification>
- `git diff --stat` del plan completo toca exclusivamente los cuatro ficheros de `.planning/`.
- `npx vitest run` y `npm run typecheck` siguen en verde: este plan no toca código, así que
  cualquier cambio de resultado es señal de que algo se coló.
- El SUMMARY deja escrito que la comprobación visual humana en tablet horizontal (DEV-02) sigue
  pendiente y que este lote no la cierra, y que HIST-06 sigue `[ ]` a la espera de la ronda 7.
</verification>

<success_criteria>
La documentación de la fase describe el estado real de HIST-06 tras la séptima variante, nombra el
código concreto que la aborda sin declarar un cierre que no le corresponde, incorpora al método de
auditoría la pregunta que faltaba (Q6) y cierra la regla de exclusión que dejó pasar `endGameBody`,
registra con evaluación de riesgo lo único que queda abierto a sabiendas, y el contrato de copy
coincide literalmente —comprobado con `grep`— con lo que la app enseña.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-31-SUMMARY.md` al terminar.
</output>
