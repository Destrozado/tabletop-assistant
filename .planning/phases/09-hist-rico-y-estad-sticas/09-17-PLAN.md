---
phase: 09-hist-rico-y-estad-sticas
plan: 17
type: execute
wave: 11
depends_on: ["09-13", "09-14", "09-15", "09-16"]
gap_closure: true
files_modified:
  - .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md
  - .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
  - .planning/REQUIREMENTS.md
  - app/composables/useGameHistory.ts
  - app/composables/useHeroSearch.ts
  - app/composables/usePersistedSession.ts
  - engine/history.ts
  - app/composables/__tests__/useGameHistory.test.ts
  - app/composables/__tests__/useHeroSearch.test.ts
  - app/composables/__tests__/usePersistedSession.test.ts
  - engine/__tests__/history.test.ts
autonomous: true
requirements: [HIST-04, HIST-06, HIST-07, HIST-08]

must_haves:
  truths:
    - "Existe un barrido ESCRITO y exhaustivo de las tres fronteras que llevan tres rondas fallando (`usePersistedSession.ts`, `useGameHistory.ts`, `engine/history.ts`): toda función de los tres ficheros tiene un veredicto explícito bajo los tres criterios, no solo las que alguien recordó mirar"
    - "Cada DEFECTO del barrido que vive en esos tres ficheros (o en un colaborador directo alcanzado por el mismo criterio) está cerrado y tiene su test de regresión — no se cierra un hallazgo y se deja el adyacente para la ronda siguiente"
    - "WR-01: una entrada con `heroId: undefined` deja de pintar «Ana · undefined» en la tarjeta del histórico; la capa de vista usa el mismo predicado que `engine/statistics.ts`"
    - "WR-06: `formatEntryDuration` no pinta «1 h 0 min», ni «0 min», ni duraciones negativas"
    - "Ningún `Record<string, …>` alcanzable desde dato de usuario en esta cadena resuelve por `Object.prototype`, ni siquiera los que hoy solo reciben claves del catálogo"
    - "Lo que el barrido encuentra y NO se cierra aquí queda escrito en `deferred-items.md` con fichero, línea, motivo y propuesta — nunca omitido en silencio"
    - "La tabla de trazabilidad de `REQUIREMENTS.md` deja de contradecir a la verificación en HIST-04 y HIST-06"
  artifacts:
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md"
      provides: "el barrido documentado que el verificador pide explícitamente para romper el bucle de rondas: inventario completo de funciones, tres preguntas por función, veredicto y recuento"
      contains: "Veredicto"
      min_lines: 80
    - path: "app/composables/useGameHistory.ts"
      provides: "buildHistoryCardView que normaliza los huecos de jugador en su propia frontera, con el mismo predicado que el motor de estadísticas"
      contains: "undefined"
    - path: "engine/history.ts"
      provides: "formatEntryDuration sin «1 h 0 min», sin «0 min» y sin negativos"
      contains: "durationMs < 0"
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md"
      provides: "registro explícito de los hallazgos no cerrados en este lote, con motivo y propuesta"
      contains: "WR-02"
  key_links:
    - from: "09-AUDIT-FRONTERAS.md"
      to: "los tests de regresión de la Task 3"
      via: "cada fila con veredicto DEFECTO tiene un test que la cubre, referenciado por su identificador BF-xx"
      pattern: "BF-\\d+"
    - from: "buildHistoryCardView (capa de vista)"
      to: "extractHeroIds (engine/statistics.ts)"
      via: "mismo predicado de «este hueco tiene héroe» en los dos lados, que es lo que WR-01 señala que hoy diverge"
      pattern: "undefined"
---

<objective>
Este plan existe por una petición explícita y nominal del verificador, escrita al final de
`09-VERIFICATION.md` (§«Patrón recurrente a señalar para la planificación del próximo cierre»)
y repetida en su resumen ejecutivo:

> «Esta es la tercera ronda consecutiva en la que la verificación (o el `09-REVIEW.md` que la
> precede) encuentra un defecto de la misma familia — una frontera de validación que se
> endurece por un lado del contrato lectura/escritura y deja sin cubrir un caso adyacente en el
> otro lado o en un módulo vecino. Antes de dar la fase por cerrada, valdría la pena una
> revisión específica y exhaustiva de TODAS las rutas de `usePersistedSession.ts` +
> `useGameHistory.ts` + `engine/history.ts` bajo el mismo criterio (¿qué pasa si esta operación
> falla a medias? ¿qué pasa si esta clave de objeto es un dato no confiable?), **en vez de
> cerrar un hallazgo a la vez**.»

Los cuatro planes anteriores de este lote (09-13 a 09-16) cierran hallazgos concretos. Este
cierra el PATRÓN: recorre las tres fronteras entera y sistemáticamente, deja el recorrido
escrito para que la re-verificación pueda comprobar la cobertura en vez de confiar en ella, y
cierra todo lo que encuentre en ese perímetro de una vez.

Propósito: las rondas 1, 2 y 3 se perdieron las tres por lo mismo. La ronda 1 endureció
`loadHistory`; la ronda 2 descubrió que `appendHistoryEntry` no compartía el predicado. La
ronda 2 endureció el predicado de forma; la ronda 3 descubrió que `readRaw` no distinguía «no
he podido leer». La ronda 3 endureció el mapa de nombres por un lado; el vecino
(`useGameContent`) tenía la misma línea. Un cuarto cierre puntual tiene, por inducción, una
probabilidad alta de dejar un cuarto caso adyacente. Un barrido con inventario y veredicto por
función, no.

Salida: `09-AUDIT-FRONTERAS.md` con el recorrido completo; los defectos que encuentre cerrados
y con test; y lo que quede fuera anotado con nombre, línea y motivo en `deferred-items.md`, no
omitido.
</objective>

<scope_boundary>
DENTRO de este plan:

- El barrido documentado de **toda** función exportada y módulo-privada de
  `app/composables/usePersistedSession.ts`, `app/composables/useGameHistory.ts` y
  `engine/history.ts`, más los colaboradores directos que los tres criterios alcancen
  (`app/composables/useHeroSearch.ts`, `engine/statistics.ts`, `engine/selection.ts`).
- El cierre de todo DEFECTO que el barrido encuentre en ese perímetro y cuyo arreglo sea
  local a esos ficheros. Como mínimo, los que ya están identificados y confirmados:
  **WR-01** (`useGameHistory.ts:113,126`), **WR-06** (`engine/history.ts:125-132`) y los mapas
  indexados de `useHeroSearch.ts` (`:66`, `:150-163`, `:173`), que `09-REVIEW.md` §CR-02 señala
  al pie como «mismo repaso conviene aquí».
- El registro en `deferred-items.md` de lo que el barrido encuentre y NO se cierre aquí, con
  fichero, línea, motivo del aplazamiento y propuesta concreta. Esto incluye los WARNING que
  los planes 09-13..09-16 han declarado explícitamente fuera de su alcance: **WR-02** (la vía
  de recuperación ante un blob ilegible permanente), **WR-04** (accesibilidad de
  `GameOutcomeDialog`), **WR-05 (b)** (la banda fuera de flujo) y **WR-07** (`sampleCaption`
  ignora la muestra de villanos), más los INFO de `09-REVIEW.md` que sean deuda real.
- La sincronización a mano de las filas HIST-04 y HIST-06 de `.planning/REQUIREMENTS.md`, que
  el verificador ha señalado como desincronizadas en las rondas 2 y 3 («`REQUIREMENTS.md` marca
  HIST-04 y HIST-06 como `[x]`/"Satisfecho" pese a que esta verificación encuentra, de nuevo,
  defectos reproducibles sobre esos mismos dos requisitos»), y que `deferred-items.md` ya
  registra como pendiente desde el plan 09-03.

FUERA de alcance:

- Los dos BLOCKER de la ronda 3 y los tres WARNING que cierran 09-13, 09-14, 09-15 y 09-16.
  Este plan corre DESPUÉS de los cuatro (ola 11) precisamente para auditar el código final, no
  un estado intermedio. Si el barrido encuentra que alguno no quedó bien cerrado, eso es un
  hallazgo del barrido y se arregla aquí.
- Cualquier defecto cuyo arreglo exija una superficie de interfaz nueva (pantalla, botón,
  estado que `09-UI-SPEC.md` no cubra): se registra en `deferred-items.md`, no se construye a
  ciegas en una ronda de cierre de huecos.
- Firestore/Firebase en cualquier forma (Fase 10). SC5 exige que sigan sin existir en `app/` ni
  `engine/`.
- Los ficheros `.vue` de pantalla (`historico.vue`, `estadisticas.vue`, `[game]/index.vue`,
  `GameOutcomeDialog.vue`, `HistorySavedNotice.vue`): quedan fuera del perímetro que el
  verificador nombra, y 09-16 ya toca los dos últimos. Si el barrido encuentra un defecto que
  solo se puede cerrar ahí, va a `deferred-items.md`.
</scope_boundary>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md

<interfaces>
Inventario COMPLETO de funciones del perímetro, extraído del repo. El barrido de la Task 1
debe cubrirlas todas; si alguna falta en la tabla final, el barrido está incompleto.

`app/composables/usePersistedSession.ts` (16 funciones, tras el cierre de 09-13):
`normalizeVoicePreference`, `isPersistedPosition`, `isHistoryPlayerEntry`, `isGameHistoryEntry`,
`readRaw`, `writeRaw`, `removeRaw`, `readEnvelope`, y las ocho que devuelve el composable:
`load`, `save`, `clear`, `loadVoicePreference`, `saveVoicePreference`, `loadHistory`,
`appendHistoryEntry`, `removeHistoryEntry`.

`app/composables/useGameHistory.ts` (4 funciones de módulo + el composable):
`resolveFrozenNames`, `buildHistoryCardView`, `toStatRowView`, `buildStatisticsView`, y dentro
de `useGameHistory()`: `record`, `reload`, `remove` y las tres computed `cardViews`,
`statisticsView`, `isEmpty`.

`engine/history.ts` (5 exportadas + el helper privado que añade 09-14):
`buildHistoryEntry`, `describeLossCause`, `formatEntryDate`, `formatEntryDuration`,
`sortEntriesByRecency`, `resolveFrozenHeroName`, más la constante exportada `MONTHS_ES`.

Colaboradores directos alcanzados por el criterio «clave de objeto con dato no confiable»:
- `app/composables/useHeroSearch.ts`: `resolveHeroSpanishName` (`:66`, `spanishHeroAliases[heroId]`),
  `buildTakenByMap` (`:147-163`, `labelsByHeroId` y `result`), `buildDuplicateWarningText`
  (`:173`, `slotIndexesByHeroId`), `resolvePlayerLabel`, `normalizeForSearch`.
- `engine/statistics.ts`: `buildRows` (usa `Map`, no objeto literal — candidato a veredicto OK,
  pero hay que escribirlo), `extractHeroIds` (`:98`, el predicado
  `p.heroId !== null && p.heroId !== undefined` que WR-01 dice que su gemelo de la vista NO
  tiene), `extractVillainId`, `aggregateStatistics`.
- `engine/selection.ts`: `resolvePlayerSlots`, `resolveVillainId`, `emptySelection`.

Defectos YA CONFIRMADOS por `09-REVIEW.md` que el barrido debe reproducir y cerrar:
- **WR-01** (`useGameHistory.ts:111-113` y `:126`): el filtro solo comprueba
  `player !== null && typeof player === 'object'`, y `hasAnyHero`/`playerLines` solo
  `player.heroId !== null`. Con `players: [{ playerName: 'Ana' }]` (sin `heroId`),
  `hasAnyHero` sale `true`, se entra por la rama del héroe y
  `` `${label} · ${player.heroName ?? player.heroId}` `` interpola `undefined`: la tarjeta pinta
  «Ana · undefined».
- **WR-06** (`engine/history.ts:125-132`): con exactamente 3 600 000 ms sale «1 h 0 min»;
  `Number.isFinite` deja pasar negativos (`-6 000 000` ⇒ «-2 h -40 min», porque
  `Math.floor(-100/60) === -2` y `-100 % 60 === -40`); y una partida de 20 s sale «0 min».
  Ningún test existente fija esas tres cadenas, así que cerrarlo no rompe nada
  (`engine/__tests__/history.test.ts:156-176` solo cubre 6 000 000, 120 000 y `null`).
- **Mapas de `useHeroSearch.ts`**: `resolveHeroSpanishName('constructor', 'X')` hace
  `alias.trim()` sobre la función `Object`, que no tiene `trim` — **lanza**, y este fichero
  alimenta `resolveFrozenNames`. Hoy no es alcanzable porque el `if (hero)` de
  `resolveFrozenNames` filtra contra el catálogo antes, pero es exactamente el tipo de
  «alcanzable el día que alguien quite el filtro» que este barrido existe para cerrar.

Arneses de test a reutilizar sin reescribir: `createFakeLocalStorage()`, `makeEntry`,
`makeSession` en los tres ficheros de test de `app/composables/__tests__/`; `baseContext`,
`baseSession`, `emptyNames`, `NOON_UTC_MS` en `engine/__tests__/history.test.ts`. Y
`app/composables/__tests__/useHeroSearch.test.ts`, que ya existe y tiene sus propios helpers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: El barrido escrito — inventario, tres preguntas por función y veredicto</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md</files>
  <read_first>
    - `app/composables/usePersistedSession.ts` COMPLETO, en su estado final tras 09-13.
    - `app/composables/useGameHistory.ts` COMPLETO, en su estado final tras 09-14.
    - `engine/history.ts` COMPLETO, en su estado final tras 09-14.
    - `app/composables/useHeroSearch.ts` COMPLETO, `engine/statistics.ts` COMPLETO y
      `engine/selection.ts` COMPLETO — los colaboradores directos del perímetro.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el párrafo «Patrón
      recurrente a señalar para la planificación del próximo cierre» (final del documento) y el
      resumen ejecutivo: son el encargo literal de esta tarea y hay que citarlos en la cabecera
      del documento que se crea.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` COMPLETO — las secciones
      Warnings e Info son el punto de partida, no el final: el barrido debe encontrar también
      lo que ese informe no miró.
    - Los cuatro SUMMARY de este lote (`09-13-SUMMARY.md` … `09-16-SUMMARY.md`) — para auditar
      el código tal como quedó, incluidas las decisiones que esos planes tomaron.
  </read_first>
  <action>
    Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md`. Es un
    documento de planificación, no código: no lleva `<task>` ni frontmatter de plan, sí una
    cabecera breve que cite el encargo del verificador palabra por palabra y nombre el
    perímetro auditado.

    **Método (sección 1).** Declarar las TRES preguntas que se hacen a cada función, y por qué
    son tres y no las dos que el verificador nombra: las dos suyas —«¿qué pasa si esta
    operación falla a medias?» y «¿qué pasa si esta clave de objeto es un dato no confiable?»—
    más una tercera derivada del patrón que las tres rondas comparten, «¿puede el valor que
    devuelve esta función afirmar algo que no ha ocurrido?», que es la pregunta que habría
    cazado el `true` de `appendHistoryEntry` sobre un histórico destruido y el
    `outcome: 'resumed'` sobre una configuración perdida.

    **Inventario y veredicto (sección 2).** Una tabla por fichero, una fila por función, con
    las columnas: `Función` | `Q1 fallo a medias` | `Q2 dato no confiable como clave/campo` |
    `Q3 afirma lo que no ocurrió` | `Veredicto`. El veredicto es exactamente uno de tres:
    `OK` (con la razón en una frase, no en blanco), `DEFECTO BF-xx` (con identificador
    correlativo) o `ACEPTADO` (con el motivo explícito y, si procede, el ID de WARNING/INFO de
    `09-REVIEW.md`). **Ninguna celda puede quedar vacía y ninguna función del inventario del
    bloque `<interfaces>` de este plan puede faltar** — si una pregunta no aplica a una
    función (p. ej. Q2 sobre `describeLossCause`, que no indexa nada), se escribe «no aplica:
    no indexa por dato externo», no se deja en blanco. Las celdas en blanco son precisamente
    cómo se pierde el caso adyacente.

    **Defectos (sección 3).** Por cada `BF-xx`: fichero y línea, qué falla exactamente, **con
    qué entrada concreta se reproduce** (valores reales, no descripciones), si es alcanzable
    hoy en producción o solo desde un llamador futuro, y el arreglo propuesto en una o dos
    frases. Si un defecto no se puede reproducir con valores concretos, no es un defecto: es
    una sospecha, y va en una lista aparte al final de la sección.

    **Recuento (sección 4).** Número de funciones auditadas, cuántas OK, cuántas DEFECTO,
    cuántas ACEPTADO, y la suma. El recuento es lo que permite a la re-verificación comprobar
    la COBERTURA del barrido en vez de confiar en ella.

    **Fuera de perímetro (sección 5).** Lo que el barrido detecte y no se pueda cerrar en los
    ficheros de este plan, con el motivo (necesita interfaz nueva / vive en una pantalla /
    pertenece a otra fase) y un puntero a la entrada correspondiente de `deferred-items.md`
    que creará la Task 3.

    Durante el barrido, apoyarse en comandos concretos además de la lectura —dejar constancia
    en el documento de cuáles se ejecutaron—: `grep -n "\[" ` sobre los tres ficheros para
    localizar TODO acceso indexado; `grep -rn "Record<string" app/ engine/` para localizar todo
    mapa por clave de cadena; `grep -n "??\|||" ` para localizar los valores por defecto que
    pueden estar tapando un `undefined` que en realidad es una función heredada; y
    `grep -n "return true\|return false\|outcome:" ` para localizar los retornos que afirman
    algo (pregunta Q3).

    NO modificar ningún fichero de código en esta tarea. El barrido primero, los arreglos
    después: si se mezclan, el documento acaba describiendo el código que se acaba de escribir
    en vez del que había, y pierde todo su valor como evidencia.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md && grep -c "| " .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md</automated>
  </verify>
  <acceptance_criteria>
    - El fichero `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` existe y tiene más de 80 líneas.
    - Contiene las cinco secciones: método, inventario y veredicto, defectos, recuento, fuera de perímetro.
    - Las tablas de inventario contienen una fila por cada una de las 16 funciones de `usePersistedSession.ts`: comprobable con `for f in normalizeVoicePreference isPersistedPosition isHistoryPlayerEntry isGameHistoryEntry readRaw writeRaw removeRaw readEnvelope load save clear loadVoicePreference saveVoicePreference loadHistory appendHistoryEntry removeHistoryEntry; do grep -q "$f" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md || echo "FALTA $f"; done` sin ninguna salida.
    - Lo mismo para `resolveFrozenNames`, `buildHistoryCardView`, `toStatRowView`, `buildStatisticsView`, `record`, `reload`, `remove`, `cardViews`, `statisticsView`, `isEmpty`.
    - Lo mismo para `buildHistoryEntry`, `describeLossCause`, `formatEntryDate`, `formatEntryDuration`, `sortEntriesByRecency`.
    - `grep -c "^| " .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` devuelve al menos 35 (filas de tabla).
    - `grep -cE "OK|DEFECTO|ACEPTADO" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` devuelve al menos 30.
    - `grep -c "BF-" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` devuelve al menos 3 — el barrido reproduce como mínimo WR-01, WR-06 y el de los mapas de `useHeroSearch.ts`; si encontrara menos de tres defectos, el barrido no ha mirado.
    - La sección de recuento contiene una cifra total de funciones auditadas y la suma de los tres veredictos coincide con ella.
    - `git status --short` no muestra ningún fichero de `app/` ni `engine/` modificado tras esta tarea.
  </acceptance_criteria>
  <done>
    El recorrido que el verificador pide existe por escrito, con veredicto explícito por
    función y las tres preguntas contestadas en todas las celdas, de modo que la
    re-verificación puede comprobar la cobertura del barrido en lugar de confiar en que se hizo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cerrar todos los defectos del barrido que viven en el perímetro, de una vez</name>
  <files>app/composables/useGameHistory.ts, engine/history.ts, app/composables/useHeroSearch.ts, app/composables/usePersistedSession.ts</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` recién creado por la
      Task 1 — la lista `BF-xx` es la lista de trabajo de esta tarea, y ninguna otra.
    - `app/composables/useGameHistory.ts` — `buildHistoryCardView` (`:93-161`), con atención al
      comentario CR-01 de `:106-113` (que ya explica por qué esta función desconfía de `entry`)
      y a las líneas del filtro y de `playerLines`.
    - `engine/statistics.ts:95-106` — `extractHeroIds`/`extractVillainId`, el predicado
      `p.heroId !== null && p.heroId !== undefined` que la capa de vista debe igualar (WR-01
      es literalmente «un módulo tiene la guarda y el gemelo la olvidó»).
    - `engine/history.ts` — `formatEntryDuration` (`:123-132`) y su comentario D-10/D-21, más
      `formatEntryDate` (`:117-121`) que es su hermano y ya degrada a «—» correctamente.
    - `engine/__tests__/history.test.ts:156-176` — el `describe` D-21, para confirmar con los
      propios ojos qué cadenas exactas están ya fijadas por test (6 000 000 ⇒ «1 h 40 min»,
      120 000 ⇒ «2 min», `null` ⇒ «—») y no romper ninguna.
    - `app/composables/useHeroSearch.ts` COMPLETO — `spanishHeroAliases`,
      `resolveHeroSpanishName` (`:66`), `buildTakenByMap` (`:147-163`) y
      `buildDuplicateWarningText` (`:173`).
    - `app/composables/useGameHistory.ts:54-71` (`resolveFrozenNames`) tal como lo dejó 09-14 —
      el patrón `Object.create(null)` que esta tarea replica en los mapas hermanos.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-01, §WR-06 y el párrafo
      final de §CR-02 («Mismo repaso conviene en cualquier otro `Record<string, …>` indexado
      por dato de usuario»).
  </read_first>
  <action>
    Cerrar **todos** los `BF-xx` que el barrido haya marcado como DEFECTO y cuyo arreglo sea
    local a los cuatro ficheros de esta tarea. Cada corrección lleva un comentario que cita su
    identificador `BF-xx` y, si lo tiene, su ID de `09-REVIEW.md`, para que la trazabilidad
    documento→código sea comprobable con un `grep`.

    Los tres siguientes están confirmados de antemano y son obligatorios; el resto sale del
    barrido:

    **1. WR-01 — la capa de vista normaliza los huecos de jugador en su propia frontera
    (`app/composables/useGameHistory.ts`, `buildHistoryCardView`).**
    Sustituir el `.filter` actual, que solo descarta `null` y no-objetos, por una normalización
    que produzca siempre huecos bien formados: `heroId` pasa a ser el valor solo si es
    `typeof === 'string'` y `null` en cualquier otro caso (incluido `undefined`, un número o
    una función); `heroName`, igual; `playerName`, la cadena si lo es y cadena vacía si no.
    Así `hasAnyHero` y la interpolación de `playerLines` dejan de poder ver `undefined`, y el
    predicado de esta capa queda alineado con el de `engine/statistics.ts:98`, que es
    exactamente lo que WR-01 señala que hoy diverge. Comentar citando `BF-xx`/WR-01 y la frase
    que fija la regla: *el módulo que pinta y el módulo que agrega deben decidir «este hueco
    tiene héroe» con el mismo criterio, o la pantalla y la estadística cuentan cosas distintas
    sobre la misma partida.*
    Mientras se está ahí, aplicar el mismo criterio a las dos cifras que esta función interpola
    en texto, `entry.playerCount` (`contextLine`) y `entry.round` (`roundAndDurationLine`):
    usar el valor solo si `Number.isFinite`, y en caso contrario `players.length` y `1`
    respectivamente — las mismas cifras que `buildHistoryEntry` produce cuando no se puede
    saber (cierre 09-12). Es defensa en profundidad: la frontera de almacenamiento ya los
    filtra, pero `buildHistoryCardView` es una función exportada y probada directamente, y su
    contrato no puede depender de que alguien la haya llamado bien.

    **2. WR-06 — `formatEntryDuration` (`engine/history.ts`).**
    Tres cambios en la misma función: devolver «—» también cuando `durationMs` sea menor que 0
    (hoy `Number.isFinite` deja pasar los negativos y la cadena sale con minutos negativos);
    acotar `totalMinutes` a un mínimo de 1 para que una partida de 20 segundos no se registre
    como «0 min» (la duración mínima que la copy puede afirmar sin mentir es «1 min», igual que
    `round` mínimo es 1 por el mismo razonamiento de 09-12); y devolver solo «N h» cuando los
    minutos sean exactamente 0, en vez de «N h 0 min». Las tres cadenas ya fijadas por el
    `describe` D-21 (`'1 h 40 min'`, `'2 min'`, `'—'`) deben seguir saliendo idénticas.
    Actualizar el comentario D-10/D-21 de la función citando `BF-xx`/WR-06.

    **3. Los mapas indexados de `app/composables/useHeroSearch.ts`.**
    Aplicar el mismo criterio que 09-14 aplicó a `heroNames`, en los cuatro sitios:
    `spanishHeroAliases` se lee con `Object.hasOwn` antes de usar el valor, y el valor solo se
    acepta si es `typeof === 'string'` (hoy `resolveHeroSpanishName('constructor', name)` hace
    `alias.trim()` sobre la función `Object`, que no tiene `trim`: **lanza**); y
    `labelsByHeroId`, `result` y `slotIndexesByHeroId` se construyen con `Object.create(null)`.
    Comentar en `resolveHeroSpanishName` que hoy sus claves vienen siempre del catálogo y que
    la guarda es preventiva: es un fichero que alimenta `resolveFrozenNames`, y el día que
    alguien quite el filtro contra el catálogo el defecto pasaría de latente a activo sin que
    nada avisara. Citar `BF-xx` y el párrafo final de §CR-02 de `09-REVIEW.md`.

    **4. Lo que el barrido añada.** Cerrar cada `BF-xx` restante siguiendo el mismo patrón:
    normalizar en la frontera, no relajar el validador de enfrente; comentar con el
    identificador; y no cambiar ninguna firma pública salvo que el propio defecto sea la firma.

    Si algún `BF-xx` del barrido NO se cierra aquí, su fila del documento debe pasar de
    `DEFECTO BF-xx` a `ACEPTADO` con el motivo escrito, y la Task 3 lo registra en
    `deferred-items.md`. Lo que no puede pasar es que un `BF-xx` quede sin cerrar y sin
    registrar.

    NO tocar `isGameHistoryEntry`/`isHistoryPlayerEntry` para «que pase» nada, NO tocar ningún
    fichero `.vue`, NO cambiar el contrato público de `appendHistoryEntry`/`record`, y NO
    introducir ninguna dependencia nueva.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -12 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BF-" app/composables/useGameHistory.ts engine/history.ts app/composables/useHeroSearch.ts` devuelve un total de al menos 3 — cada corrección cita su identificador del barrido.
    - `grep -v '^ *//' engine/history.ts | grep -c "durationMs < 0"` devuelve 1.
    - `grep -v '^ *//' engine/history.ts | grep -c "Math.max(1"` devuelve 1.
    - Comportamiento: `formatEntryDuration(3_600_000)` devuelve `'1 h'`; `formatEntryDuration(20_000)` devuelve `'1 min'`; `formatEntryDuration(-6_000_000)` devuelve `'—'`; y `formatEntryDuration(6_000_000)`, `formatEntryDuration(120_000)` y `formatEntryDuration(null)` siguen devolviendo `'1 h 40 min'`, `'2 min'` y `'—'`.
    - Comportamiento: `buildHistoryCardView` sobre una entrada con `players: [{ playerName: 'Ana' }]` (sin `heroId`) devuelve `noSelectionLine` no nulo o un `playerLines` que NO contiene la subcadena `'undefined'`; en ningún caso una línea con «Ana · undefined».
    - `grep -c "undefined" app/composables/useGameHistory.ts` devuelve al menos 1 (el predicado alineado con `engine/statistics.ts`).
    - `grep -v '^ *//' app/composables/useHeroSearch.ts | grep -c "Object.create(null)"` devuelve al menos 3.
    - `grep -v '^ *//' app/composables/useHeroSearch.ts | grep -c "Object.hasOwn"` devuelve al menos 1.
    - Comportamiento: `resolveHeroSpanishName('constructor', 'Fallback')` no lanza y devuelve `'Fallback'`.
    - Todo `BF-xx` de `09-AUDIT-FRONTERAS.md` aparece o bien citado en un comentario de código (cerrado) o bien con veredicto `ACEPTADO` en el propio documento: comprobable listando los identificadores del documento y buscándolos con `grep -r` en `app/` y `engine/`.
    - `npx vitest run` termina con código 0 y `npm run build` termina con código 0.
  </acceptance_criteria>
  <done>
    Todos los defectos que el barrido encontró dentro del perímetro están cerrados en el mismo
    lote, no uno por ronda: la capa de vista y el motor de estadísticas deciden igual, la
    duración no puede pintar una cifra imposible, y ningún mapa de esta cadena resuelve por
    `Object.prototype`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests de regresión por cada `BF-xx`, registro de lo aplazado y sincronización de la trazabilidad</name>
  <files>engine/__tests__/history.test.ts, app/composables/__tests__/useGameHistory.test.ts, app/composables/__tests__/useHeroSearch.test.ts, app/composables/__tests__/usePersistedSession.test.ts, .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` — la lista `BF-xx`
      con su estado final tras la Task 2: los cerrados necesitan test aquí, los `ACEPTADO`
      necesitan entrada en `deferred-items.md`.
    - `engine/__tests__/history.test.ts` — el `describe` D-21 (`:156-176`) es donde van los
      casos de `formatEntryDuration`, junto a los que ya existen y sin tocarlos.
    - `app/composables/__tests__/useGameHistory.test.ts` — el `describe('buildHistoryCardView')`
      (`:71-216`) y su `makeEntry` (`:34-55`), calcado del ejemplo de `09-UI-SPEC.md` §4.
    - `app/composables/__tests__/useHeroSearch.test.ts` — sus helpers y su estilo; los tests de
      los mapas van ahí.
    - `app/composables/__tests__/usePersistedSession.test.ts` — solo si el barrido encontró
      algún `BF-xx` en ese fichero; si no, no se toca.
    - `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` — el formato de entrada
      que ya usa el documento (título, «Encontrado durante», «Descripción», «Por qué no se
      corrige aquí», «Acción sugerida»): las entradas nuevas lo replican exactamente.
    - `.planning/REQUIREMENTS.md` — las filas HIST-04 y HIST-06 de la tabla de trazabilidad y
      sus checkboxes.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` §«Cobertura de
      requisitos» — la nota de trazabilidad reincidente que esta tarea cierra.
  </read_first>
  <action>
    **1. Un test de regresión por cada `BF-xx` cerrado en la Task 2.** Cada `it` cita su
    identificador en el nombre, y cada uno debe FALLAR contra el código previo a la Task 2.
    Como mínimo:

    - En `engine/__tests__/history.test.ts`, dentro del `describe` D-21 existente y sin tocar
      sus tests: `formatEntryDuration(3_600_000)` ⇒ `'1 h'`; `formatEntryDuration(7_200_000)`
      ⇒ `'2 h'`; `formatEntryDuration(20_000)` ⇒ `'1 min'`; `formatEntryDuration(0)` ⇒
      `'1 min'`; `formatEntryDuration(-6_000_000)` ⇒ `'—'`;
      `formatEntryDuration(Number.NEGATIVE_INFINITY)` ⇒ `'—'`. Y un anti-regresión que repite
      las tres cadenas ya fijadas para que quede explícito que no han cambiado.
    - En `app/composables/__tests__/useGameHistory.test.ts`, dentro del `describe` de
      `buildHistoryCardView`: una entrada con `players: [{ playerName: 'Ana' }]` produce una
      tarjeta en la que ninguna cadena contiene `'undefined'` (comprobarlo sobre
      `JSON.stringify(view)`, que cubre los diez campos de la vista de una vez y no solo el que
      se esperaba); lo mismo con `heroId: undefined` explícito, con `heroId: 7` y con
      `players: [{ heroId: 'thor', heroName: undefined, playerName: 'Ana' }]`. Y una entrada
      con `playerCount: Number.NaN` y `round: Number.NaN` produce una tarjeta sin `'NaN'` en
      ninguna cadena.
    - En `app/composables/__tests__/useHeroSearch.test.ts`: parametrizado sobre las ocho claves
      de `Object.prototype` (`constructor`, `toString`, `valueOf`, `hasOwnProperty`,
      `__proto__`, `isPrototypeOf`, `propertyIsEnumerable`, `toLocaleString`),
      `resolveHeroSpanishName(clave, 'Fallback')` no lanza y devuelve `'Fallback'`; y
      `buildTakenByMap` con un hueco cuyo `heroId` sea una de esas claves devuelve un mapa cuyo
      prototipo es `null` y que no contiene entradas heredadas.
    - Cualquier otro `BF-xx` cerrado, con su test en el fichero que le corresponda.

    **2. Registrar en `deferred-items.md` todo lo que no se cierra.** Una entrada por
    hallazgo, con el formato que el documento ya usa. Obligatorias, además de lo que salga del
    barrido con veredicto `ACEPTADO`:

    - **WR-02 (vía de recuperación ante un blob ilegible permanente).** Descripción: con un
      `tga:history` corrupto o de `formatVersion` desconocido, `appendHistoryEntry` devuelve
      `false` en todas las partidas futuras y `loadHistory()` devuelve `[]`, así que
      `/historico` dice «Todavía no hay partidas registradas» (hay, no se saben leer) y no
      existe ninguna acción en la interfaz para salir del bloqueo. Por qué no aquí: exige una
      superficie de interfaz que `09-UI-SPEC.md` no cubre. Acción sugerida: distinguir el caso
      en el contrato de `appendHistoryEntry`, dar copy propia al aviso y ofrecer en
      `/historico` una acción explícita de archivado (renombrar la clave a
      `tga:history:backup-<ts>` y empezar limpio, conservando el dato antiguo). Anotar que
      09-16 ya cerró la mitad del problema (el aviso ya no atribuye una causa falsa) y que lo
      pendiente es exclusivamente la salida del callejón.
    - **WR-04 (accesibilidad de `GameOutcomeDialog`).** Sin `Escape`, sin gestión de foco, sin
      `aria-labelledby`, y las cuatro acciones terminan la partida. Acción sugerida: copiar el
      bloque de `WarningDetailModal.vue`; decidir antes qué significa `Escape` frente a D-01.
    - **WR-05 (b) (la banda empuja fuera del viewport las pantallas `h-dvh`).** Afecta también
      a `UpdateBanner` y a las cuatro pantallas. Acción sugerida: sacar la banda del flujo
      (`fixed top-0 inset-x-0 z-40`) o envolver `<NuxtPage/>` en un contenedor
      `flex flex-col h-dvh` con `min-h-0`.
    - **WR-07 (`sampleCaption` declara la muestra de héroes e ignora la de villanos).** Acción
      sugerida: exponer `entriesWithVillain` en `StatisticsSummary` y declarar la muestra de
      cada tabla por separado.
    - **Los INFO de `09-REVIEW.md` que sean deuda real**, agrupados en una sola entrada con la
      lista de identificadores y una línea por cada uno: IN-04 (el histórico crece sin tope),
      IN-05 (ids de longitud variable), IN-07 (el fichero e2e aborta la suite en tiempo de
      import), IN-11 (`removeHistoryEntry` se traga el fallo de escritura), IN-12 (una entrada
      rechazada es invisible y no se puede borrar) e IN-13 (`Math.round` anuncia «100 %» sin
      pleno). Los demás son estilo y no hace falta registrarlos.

    **3. Sincronizar `.planning/REQUIREMENTS.md`.** Las filas HIST-04 y HIST-06 de la tabla de
    trazabilidad deben reflejar el estado real tras este lote: los planes que las cierran
    (añadiendo 09-13 a 09-17 a los que ya figuran) y un estado que no contradiga a la
    verificación. Es el problema conocido de `requirements.mark-complete` que la memoria del
    usuario documenta y que `deferred-items.md` registra como pendiente desde el plan 09-03: no
    revierte ni actualiza filas cuando aparece un hallazgo posterior, así que la sincronización
    es manual. NO marcar nada como satisfecho que la re-verificación tenga que desmentir: si al
    terminar este plan queda alguna duda sobre HIST-04 o HIST-06, la fila lo dice.

    **4. Prueba anti-tautología.** `git stash push -- app/composables/useGameHistory.ts
    engine/history.ts app/composables/useHeroSearch.ts app/composables/usePersistedSession.ts`
    (pathspec explícito), `npx vitest run` para confirmar que los tests nuevos FALLAN, y
    `git stash pop`. Dejar constancia en el SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` termina con código 0 y con estrictamente más tests que al terminar 09-16.
    - `grep -rc "BF-" engine/__tests__/history.test.ts app/composables/__tests__/useGameHistory.test.ts app/composables/__tests__/useHeroSearch.test.ts` devuelve un total de al menos 3.
    - `grep -c "formatEntryDuration(3_600_000)" engine/__tests__/history.test.ts` devuelve al menos 1 y el test afirma `'1 h'`.
    - `grep -c "JSON.stringify" app/composables/__tests__/useGameHistory.test.ts` devuelve al menos 1 — la comprobación de «ninguna cadena de la vista contiene `undefined`» se hace sobre la vista entera, no sobre un campo elegido a mano.
    - `grep -c "toLocaleString" app/composables/__tests__/useHeroSearch.test.ts` devuelve al menos 1 — la lista de ocho claves está completa.
    - `grep -c "WR-02" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1; lo mismo para `WR-04`, `WR-05` y `WR-07`.
    - `grep -c "IN-11" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1.
    - `grep -n "HIST-04\|HIST-06" .planning/REQUIREMENTS.md` muestra filas que mencionan al menos uno de los planes `09-13`…`09-17`.
    - `git diff --stat` no muestra líneas ELIMINADAS en los bloques de test preexistentes de los cuatro ficheros de test: solo adiciones e `import` ampliados.
    - Prueba anti-tautología ejecutada y anotada en el SUMMARY: con el `git stash push` de los cuatro ficheros de producción aplicado, `npx vitest run` FALLA; tras `git stash pop`, pasa.
    - `npm run build` termina con código 0.
  </acceptance_criteria>
  <done>
    Cada defecto del barrido tiene un test permanente que lo cubre, cada hallazgo no cerrado
    tiene una entrada con fichero, motivo y propuesta en `deferred-items.md`, y la tabla de
    trazabilidad deja de contradecir a la verificación en los dos requisitos que la ronda 3
    marcó como PARCIALES.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `window.localStorage` → las tres fronteras auditadas | **Entrada no fiable** por contenido (editable en DevTools, residuo de otra versión, escritura interrumpida) y por disponibilidad (`getItem`/`setItem` pueden lanzar). Es la única entrada externa de la app junto con el parámetro de ruta. |
| Dato no confiable usado como CLAVE de objeto | Frontera implícita y reincidente: `heroId`, `gameId` de la ruta y `villainId` llegan como cadenas arbitrarias y se usan para indexar mapas. Un objeto literal convierte ocho nombres heredados en «claves que existen». |
| Retorno de una función → afirmación de la interfaz | Un `boolean`/`outcome` que la interfaz traduce a una frase («✓ Partida registrada», «Partida guardada … CONTINUAR») es una afirmación sobre el mundo. Si el valor no puede sostenerla, la app miente sin que nada falle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-17-01 | Tampering (defecto adyacente no cubierto — el patrón de las tres rondas) | `usePersistedSession.ts` + `useGameHistory.ts` + `engine/history.ts` en conjunto | mitigate | Task 1: barrido con inventario completo, tres preguntas por función y veredicto obligatorio en toda celda; Task 2: cierre de todos los `BF-xx` del perímetro en un solo lote; Task 3: un test por `BF-xx`. Severidad `high`: es la amenaza que ha costado tres rondas. |
| T-09-17-02 | Tampering (contaminación por cadena de prototipos, latente) | `spanishHeroAliases`, `labelsByHeroId`, `slotIndexesByHeroId` en `useHeroSearch.ts` | mitigate | Task 2.3: `Object.hasOwn` en la lectura y `Object.create(null)` en la construcción, los cuatro sitios. Test parametrizado sobre las ocho claves. Severidad `medium`: hoy latente (las claves vienen del catálogo), pero `resolveHeroSpanishName` **lanza** si se activa, y es un fichero que alimenta la cadena del histórico. |
| T-09-17-03 | Repudiation (la pantalla y la estadística cuentan cosas distintas de la misma partida) | `buildHistoryCardView` vs `extractHeroIds` (WR-01) | mitigate | Task 2.1: mismo predicado en los dos lados, normalizando en la frontera de la vista. Tests sobre `JSON.stringify(view)` para cubrir los diez campos de una vez. Severidad `medium`. |
| T-09-17-04 | Repudiation (cifra imposible presentada como dato) | `formatEntryDuration` (WR-06) | mitigate | Task 2.2: «—» para negativos, mínimo «1 min», sin «N h 0 min». Seis casos nuevos en el `describe` D-21. Severidad `low`: cosmético, pero es una cifra que el grupo lee como hecho. |
| T-09-17-05 | Denial of Service (callejón sin salida ante un blob ilegible permanente) | `appendHistoryEntry` + `/historico` (WR-02) | transfer | Registrado en `deferred-items.md` con propuesta concreta (archivar a `tga:history:backup-<ts>`). Se aplaza porque exige interfaz nueva fuera de `09-UI-SPEC.md`; 09-16 ya cerró su mitad de diagnóstico falso. Aplazamiento EXPLÍCITO y escrito, no omisión. |
| T-09-17-06 | Denial of Service (cuota agotada por crecimiento sin tope) | `tga:history` sin poda (IN-04/IN-12) | transfer | Registrado en `deferred-items.md`. El síntoma del día que tope la cuota es un fallo de escritura, que con 09-13 y 09-16 ya es honesto y recuperable, no destructivo. |
| T-09-17-07 | Information disclosure | `tga:history` | accept | Nombres de jugador del propio grupo y resultados, solo en el dispositivo, sin red ni backend. Firestore no existe todavía en el repo (SC5/STAT-04). |
| T-09-17-08 | Spoofing / Elevation of privilege | — | accept | No aplican: la app no tiene identidades, sesiones autenticadas ni privilegios que escalar. |
| T-09-17-SC | Tampering (supply chain) | Instalaciones npm/pip/cargo | accept | Ninguna tarea de este plan instala ni actualiza paquetes: se modifican ficheros TypeScript y de test ya existentes y se crean dos documentos de planificación. Si durante la ejecución surgiera la necesidad de instalar algo, este plan queda bloqueado hasta que un checkpoint humano lo apruebe. |
</threat_model>

<verification>
1. `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` existe, supera las 80
   líneas y contiene una fila por cada función del inventario del bloque `<interfaces>` de este
   plan, sin celdas vacías.
2. `npx vitest run` — suite completa en código 0, con estrictamente más tests que al terminar
   09-16.
3. `npm run build` termina con código 0.
4. `grep -v '^ *//' engine/history.ts | grep -c "durationMs < 0"` devuelve 1.
5. `grep -v '^ *//' app/composables/useHeroSearch.ts | grep -c "Object.create(null)"` devuelve
   al menos 3.
6. Todo identificador `BF-xx` de `09-AUDIT-FRONTERAS.md` está o citado en un comentario de
   `app/`/`engine/` o marcado `ACEPTADO` en el propio documento — ninguno queda suelto.
7. `grep -c "WR-02" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al
   menos 1.
8. `grep -ric "firestore\|firebase" app/ engine/` devuelve 0 — SC5 y STAT-04 siguen
   trivialmente ciertos al cerrar la fase.
9. `grep -rn "TODO\|FIXME\|HACK\|XXX\|TBD" app/composables/ engine/ | grep -v "__tests__"`
   devuelve 0 resultados — mismo control que la verificación aplica sobre los ficheros clave.
10. Prueba anti-tautología con `git stash push` de los cuatro ficheros de producción ejecutada
    y su resultado anotado en el SUMMARY.
</verification>

<success_criteria>
- Existe un barrido escrito de las tres fronteras con veredicto explícito por función bajo las
  tres preguntas, y un recuento que permite comprobar su cobertura.
- Todo `BF-xx` marcado DEFECTO está cerrado con su test de regresión, o marcado `ACEPTADO` con
  motivo escrito y registrado en `deferred-items.md`.
- Una entrada del histórico con `heroId` ausente, `undefined` o de tipo equivocado no produce
  ninguna cadena con `'undefined'` ni `'NaN'` en la tarjeta.
- `formatEntryDuration` nunca devuelve «1 h 0 min», «0 min» ni una duración negativa, y sus
  tres cadenas ya fijadas por test siguen idénticas.
- Ningún `Record<string, …>` de la cadena del histórico, incluidos los hoy latentes de
  `useHeroSearch.ts`, resuelve por `Object.prototype`.
- `deferred-items.md` contiene, con fichero, línea, motivo y propuesta, WR-02, WR-04, WR-05 (b),
  WR-07 y los INFO que son deuda real.
- Las filas HIST-04 y HIST-06 de `REQUIREMENTS.md` ya no contradicen a la verificación.
- La suite completa sigue en verde y los tests nuevos fallan si se revierten los cambios de
  producción.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-17-SUMMARY.md` al terminar.
</output>
