---
phase: 09-hist-rico-y-estad-sticas
plan: 12
type: execute
wave: 8
depends_on: ["09-01", "09-09"]
gap_closure: true
files_modified:
  - engine/history.ts
  - engine/persistence.ts
  - app/composables/usePersistedSession.ts
  - engine/__tests__/history.test.ts
  - engine/__tests__/persistence.test.ts
  - app/composables/__tests__/usePersistedSession.test.ts
  - app/composables/__tests__/useGameHistory.test.ts
autonomous: true
requirements: [HIST-04, HIST-06]

must_haves:
  truths:
    - "SC2: una partida jugada desde una sesión reanudada con `context` parcial se registra con los 10 campos presentes y bien tipados — `difficulty`, `playerCount` y `round` se normalizan en el motor en vez de propagarse como hueco"
    - "SC3: `appendHistoryEntry` nunca devuelve `true` sobre una entrada que `loadHistory()` vaya a rechazar después — el predicado que decide qué se puede LEER decide también qué se puede ESCRIBIR"
    - "SC3: el ciclo completo `record()` → `loadHistory()` recupera la entrada también cuando la sesión de origen tenía `context: {}` y `round: NaN`"
    - "La rama `resumed` de `resume()` no propaga un `context` sin forma de `SessionContext` ni un `round` que no sea un entero ≥ 1 — mismo criterio que su rama hermana `contentChangedFallback` ya aplica"
    - "`isGameHistoryEntry` rechaza `round`, `playerCount` y `durationMs` con valor `NaN` o `Infinity` (WR-03), en vez de dejarlos llegar a la pantalla como «Hasta la ronda NaN»"
    - "Ninguna regresión: los cierres de la ronda anterior (CR-01/CR-02/CR-03) siguen en verde y la suite completa (26 ficheros / 665 tests de línea base) sigue pasando"
  artifacts:
    - path: "engine/history.ts"
      provides: "buildHistoryEntry con normalización defensiva de difficulty/playerCount/round, mismo criterio que ya aplica a durationMs"
      contains: "Number.isInteger"
    - path: "engine/persistence.ts"
      provides: "rama `resumed` de resume() que valida context con isValidContext y normaliza round"
      contains: "isValidContext(persisted.context)"
    - path: "app/composables/usePersistedSession.ts"
      provides: "appendHistoryEntry que se somete a isGameHistoryEntry antes de escribir; validador numérico por Number.isFinite"
      contains: "if (!isGameHistoryEntry(entry)) return false"
    - path: "app/composables/__tests__/useGameHistory.test.ts"
      provides: "test de ida y vuelta record() → loadHistory() desde un context: {} — la prueba de que la escritura y la lectura comparten contrato"
  key_links:
    - from: "appendHistoryEntry"
      to: "isGameHistoryEntry"
      via: "guarda de entrada antes de readEnvelope/writeRaw — cierra el círculo lectura=escritura"
      pattern: "isGameHistoryEntry\\(entry\\)"
    - from: "engine/persistence.ts (rama `resumed`)"
      to: "isValidContext"
      via: "el context persistido se valida igual que en contentChangedFallback antes de entrar en la sesión viva"
      pattern: "isValidContext\\(persisted\\.context\\)"
    - from: "buildHistoryEntry"
      to: "GameHistoryEntry bien tipada"
      via: "normalización de difficulty/playerCount/round en el punto de construcción"
      pattern: "Number\\.isInteger"
---

<objective>
Cerrar el único BLOCKER de `09-VERIFICATION.md` (2ª ronda), que se manifiesta sobre dos
criterios de éxito del ROADMAP (SC2 y SC3) desde una sola causa raíz: la frontera de
validación del histórico se endureció en la ronda anterior SOLO por el lado de LECTURA
(`isGameHistoryEntry`, usado por `loadHistory`), dejando el lado de ESCRITURA
(`appendHistoryEntry`, `usePersistedSession.ts:293-303`) persistiendo cualquier objeto sin
comprobar nada. El verificador lo reprodujo de forma independiente: una entrada con
`difficulty: undefined` / `playerCount: undefined` se escribe con éxito
(`appendHistoryEntry` devuelve `true`, el grupo lee «✓ Partida registrada») y `loadHistory()`
inmediatamente después devuelve `[]`.

Propósito: es el inverso exacto del contrato D-03, que existe precisamente porque el
histórico es el único dato de la app que el proyecto declara irreconstruible. D-03 protege
«que el grupo se entere si el dispositivo no dejó escribir»; aquí el dispositivo SÍ dejó
escribir, el dato se perdió igualmente, y encima con una confirmación positiva encima. Hasta
que esto no se cierre, HIST-04 y HIST-06 siguen siendo PARCIALES y la fase no puede darse por
buena.

Salida: el motor deja de propagar el hueco (normaliza en el punto de construcción y no
reanuda una sesión con `context` sin forma), la costura de almacenamiento deja de prometer un
éxito que no puede sostener, y cuatro ficheros de test fijan la invariante que faltaba —
*toda entrada que `buildHistoryEntry` produce sobrevive a un ciclo de ida y vuelta por
`localStorage`*.
</objective>

<scope_boundary>
DENTRO de este plan, y solo esto:

- Los tres `missing:` del gap SC2 de `09-VERIFICATION.md` (guarda en `appendHistoryEntry`,
  normalización en `buildHistoryEntry`, los dos tests nombrados). El gap SC3 declara
  explícitamente «igual que el gap de SC2 — es la misma causa raíz, doble impacto», así que
  no añade trabajo propio.
- La rama `resumed` de `resume()` en `engine/persistence.ts`, clasificada por el verificador
  como 🛑 BLOCKER que habilita al anterior: es el origen concreto del `context` parcial.
- **WR-03** de `09-REVIEW.md` (`typeof === 'number'` acepta `NaN`/`Infinity` en
  `round`/`playerCount`/`durationMs`): es exactamente el mismo trabajo de normalización
  defensiva sobre el mismo validador, con coste marginal nulo.

FUERA de alcance, anotado explícitamente para que la re-verificación no lo confunda con un
descuido — ninguno es un gap de verificación, todos son WARNING/INFO de `09-REVIEW.md`:

- **WR-01** (`useGameHistory.ts:113,126` usa `!== null` sin `!== undefined`): el propio
  informe de verificación lo declara «hoy inalcanzable en producción porque
  `isHistoryPlayerEntry` ya rechaza un `heroId` ausente antes de llegar aquí».
- **WR-02** (un blob `unreadable` bloquea el registro para siempre con un diagnóstico falso, y
  sin vía de recuperación en la interfaz). Consecuencia que este plan debe reconocer sin
  cerrarla: la Task 2 añade un segundo motivo por el que `appendHistoryEntry` puede devolver
  `false`, y el aviso de D-03 seguirá culpando al almacenamiento del dispositivo. Por eso la
  Task 1 va PRIMERO y la Task 3 exige la invariante de ida y vuelta: si el motor nunca produce
  una entrada inválida, esa nueva rama de `false` es defensa en profundidad inalcanzable en
  producción, no un camino que el grupo vaya a pisar. Corregir el copy del aviso es WR-02 y
  se decide aparte.
- **WR-05** (cancelación / `Escape` / trampa de foco en `GameOutcomeDialog.vue`), **WR-06**
  (`role="status"`/`aria-live` en `HistorySavedNotice.vue`), **WR-07** (cosmética de
  `formatEntryDuration`): ninguno vive en los tres ficheros de producción que este plan toca.

NO se toca: `loadHistory` (su contrato público no cambia ni una línea), `removeHistoryEntry`,
`readEnvelope`, `isHistoryPlayerEntry`, `load`/`save`/`clear`, ni ninguna pantalla `.vue`.
</scope_boundary>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-09-PLAN.md

<interfaces>
Extraído del repo (no hay que explorarlo para averiguarlo):

`engine/persistence.ts` (91 líneas):
- `interface PersistedPosition { formatVersion: 1; gameId: string; contentVersion: number;
  runtimeId: string; round: number; context: SessionContext; updatedAt: string }`
- `function isValidContext(value: unknown): value is SessionContext` (línea 45) — privada,
  exige objeto con `typeof playerCount === 'number'` y `typeof difficulty === 'string'`.
- `function contentChangedFallback(persisted, fresh)` (línea 62) — YA aplica
  `isValidContext(persisted.context) ? persisted.context : fresh.context`. Es el patrón a
  replicar.
- `resume()` (línea 67): tres ramas devuelven `contentChangedFallback`; la cuarta, el
  `return` final (líneas 85-88), devuelve
  `{ session: { ...fresh, cursor, round: persisted.round, context: persisted.context },
  outcome: 'resumed' }` — sin pasar por `isValidContext` y sin mirar `persisted.round`.

`engine/history.ts` — `buildHistoryEntry(session, outcome, now, names)`:
- `const { context, round, gameId } = session` (línea 32).
- `resolveVillainId(context)` y `resolvePlayerSlots(context)` ya normalizan por su cuenta.
- `durationMs` (líneas 50-57) es el CRITERIO DEFENSIVO DE REFERENCIA de este fichero:
  `typeof context.startedAt === 'number' && Number.isFinite(context.startedAt) && now >=
  context.startedAt ? now - context.startedAt : null`.
- El objeto devuelto copia SIN normalizar: `difficulty: context.difficulty` (línea 67),
  `playerCount: context.playerCount` (línea 68), `round` (línea 69).

`engine/selection.ts:47-69` — `resolvePlayerSlots(context)` fija la longitud del array con
`Number.isInteger(context.playerCount) && context.playerCount > 0 ? context.playerCount : 0`.
Este es el criterio exacto que la Task 1 debe reutilizar para `playerCount`, de modo que
`entry.playerCount === entry.players.length` pase a ser invariante.

`app/composables/usePersistedSession.ts` (340 líneas):
- `isPersistedPosition` (78-87): exige que `context` sea objeto no nulo, nada más.
- `isHistoryPlayerEntry` (96-102) e `isGameHistoryEntry` (117-133). Las tres comprobaciones
  numéricas actuales son `typeof candidate.round === 'number'`,
  `typeof candidate.playerCount === 'number'` y
  `(candidate.durationMs === null || typeof candidate.durationMs === 'number')`.
- `readEnvelope(): EnvelopeRead` con variantes `{ kind: 'empty' }` / `{ kind: 'ok', entries:
  unknown[] }` / `{ kind: 'unreadable' }`.
- `appendHistoryEntry(entry)` (líneas 293-303): hoy empieza directamente por
  `const read = readEnvelope()`; no hay ninguna guarda sobre `entry`.

`app/composables/useGameHistory.ts:240-244` — `record(session, outcome)` resuelve nombres
congelados, llama a `buildHistoryEntry(session, outcome, Date.now(), names)` y devuelve
`appendHistoryEntry(entry)` tal cual.

Arneses de test ya existentes, a reutilizar sin reescribir:
- `engine/__tests__/history.test.ts`: `baseContext(overrides)`, `baseSession(overrides)`,
  `emptyNames`, `NOON_UTC_MS`.
- `engine/__tests__/persistence.test.ts`: `basePersisted(overrides)`, `tinyGame`, `expand`.
- `app/composables/__tests__/usePersistedSession.test.ts`: `createFakeLocalStorage()`,
  `makeEntry(overrides)`, `describe('tga:history — clave independiente de la partida
  (D-13/HIST-09)')` (línea 187).
- `app/composables/__tests__/useGameHistory.test.ts`: `createFakeLocalStorage()`,
  `makeSession(contextOverrides, round)`, `describe('useGameHistory — ciclo
  record/reload/remove con localStorage falso')` (línea 373).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: El motor deja de propagar el hueco — normalización en `buildHistoryEntry` y rama `resumed` de `resume()` validada</name>
  <files>engine/history.ts, engine/persistence.ts</files>
  <read_first>
    - `engine/history.ts` completo — el fichero a modificar. Prestar atención a la cabecera
      (contrato de pureza: normalización defensiva, nunca muta, nunca lanza) y sobre todo al
      bloque `durationMs` (líneas 50-57) con su comentario `T-09-02`: es el criterio
      defensivo que este plan replica sobre los otros tres campos, no un criterio nuevo.
    - `engine/persistence.ts` completo (91 líneas) — `isValidContext` (línea 45),
      `contentChangedFallback` (línea 62, con su comentario que ya explica por qué valida) y
      el `return` final de `resume()` (líneas 85-88), que es el que falta por alinear.
    - `engine/selection.ts` líneas 47-69 — `resolvePlayerSlots`, de donde sale el criterio
      exacto de `playerCount` (`Number.isInteger(...) && ... > 0`).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el gap SC2 del
      frontmatter, en concreto el segundo `missing:` y la cadena de alcanzabilidad ya
      verificada línea por línea.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §CR-01 — la propuesta de fix
      concreta, que esta tarea endurece un punto más (entero positivo en vez de solo finito).
  </read_first>
  <action>
    **1. `engine/history.ts` — normalizar los tres campos que hoy se copian en crudo.**

    En el objeto que devuelve `buildHistoryEntry`, sustituir las tres asignaciones directas
    (`difficulty: context.difficulty`, `playerCount: context.playerCount`, `round`) por
    valores normalizados en el propio punto de construcción, con el mismo estilo de guarda
    explícita que el bloque `durationMs` que está justo encima:

    - `difficulty`: `'expert'` si y solo si `context.difficulty` es exactamente la cadena
      `'expert'`; `'normal'` en cualquier otro caso (ausente, `undefined`, `'imposible'`,
      numérico). Es el mismo valor por defecto con el que arranca el mini-setup, así que no
      inventa información: registra la única dificultad que se puede afirmar.
    - `playerCount`: `context.playerCount` si cumple
      `Number.isInteger(context.playerCount) && context.playerCount > 0`; en caso contrario,
      `players.length` — donde `players` es el array que esta misma función acaba de derivar
      de `resolvePlayerSlots(context)`. Esto REUTILIZA literalmente el criterio de
      `engine/selection.ts:50` y convierte en invariante que
      `entry.playerCount === entry.players.length`, que hoy no lo es.
    - `round`: `round` si cumple `Number.isInteger(round) && round >= 1`; en caso contrario,
      `1`. Un `round` de `NaN`, `2.5` o `-4` no es una ronda; `1` es la única cifra que la
      copia «Hasta la ronda N» puede afirmar sin mentir.

    Conservar intacto el comentario `D-09` que acompaña a `round` («tal cual, se lee "hasta la
    ronda N", nunca round - 1») y añadirle la coletilla de por qué ahora pasa por una guarda:
    la copia no cambia, lo que cambia es de dónde puede venir el número. Añadir un comentario
    de bloque sobre las tres asignaciones citando **CR-01 (ronda 2)** y explicando la regla
    que fija esta tarea en una frase: *el motor no propaga un hueco que la frontera de
    almacenamiento vaya a rechazar después — normaliza en origen, igual que ya hace con
    `durationMs`.*

    NO tocar `id`, `gameId`, `result`, `lossCause`, `villainId`, `villainName`, `players`,
    `durationMs` ni `recordedAt`: ninguno interviene en el gap y todos tienen ya su guarda o
    su normalizador aguas arriba.

    **2. `engine/persistence.ts` — alinear la rama `resumed` con su rama hermana.**

    En el `return` final de `resume()` (el que produce `outcome: 'resumed'`), dejar de
    propagar `persisted.context` y `persisted.round` en crudo:

    - `context`: aplicar exactamente la misma expresión que ya usa `contentChangedFallback`,
      es decir `isValidContext(persisted.context) ? persisted.context : fresh.context`. No
      crear un helper nuevo ni cambiar `isValidContext`: la asimetría entre las dos ramas es
      justamente el defecto, así que la corrección es usar la función que ya existe.
    - `round`: `persisted.round` si cumple `Number.isInteger(persisted.round) &&
      persisted.round >= 1`; en caso contrario, `fresh.round` (que `expand()` deja siempre en
      1). Motivo: `isPersistedPosition` en la capa de storage solo comprueba que la clave
      `round` exista, no su tipo.

    Añadir sobre el `return` un comentario que cite **CR-01 (ronda 2)** y nombre la
    consecuencia concreta: un `context` parcial adoptado aquí viajaba entero hasta
    `buildHistoryEntry` y de ahí a una entrada del histórico que `loadHistory()` rechazaba
    para siempre. Dejar explícito que esta rama y `contentChangedFallback` comparten criterio
    a propósito, para que nadie vuelva a endurecer solo una de las dos.

    NO modificar `isValidContext` (ni para exigir `'normal'`/`'expert'`, ni para descartar
    `NaN` en `playerCount`): la normalización final la hace `buildHistoryEntry` en la parte 1,
    y endurecer aquí convertiría sesiones hoy reanudables en reinicios — un daño mayor que el
    que se está reparando.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/history.test.ts engine/__tests__/persistence.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *//' engine/history.ts | grep -c "difficulty: context.difficulty,"` devuelve 0 (ya no se copia en crudo).
    - `grep -v '^ *//' engine/history.ts | grep -c "playerCount: context.playerCount,"` devuelve 0.
    - `grep -v '^ *//' engine/history.ts | grep -c "Number.isInteger"` devuelve al menos 2 (`playerCount` y `round`).
    - `grep -v '^ *//' engine/history.ts | grep -c "context.difficulty === 'expert'"` devuelve 1.
    - `grep -v '^ *//' engine/persistence.ts | grep -c "context: persisted.context"` devuelve 0.
    - `grep -v '^ *//' engine/persistence.ts | grep -c "isValidContext(persisted.context)"` devuelve 2 (`contentChangedFallback` y la rama `resumed`).
    - Comportamiento: `buildHistoryEntry({...session, context: {} as SessionContext, round: NaN}, 'won', now, emptyNames)` devuelve un objeto con `difficulty === 'normal'`, `playerCount === 0`, `round === 1`, `players` de longitud 0 y `durationMs === null`.
    - Comportamiento: `buildHistoryEntry` sobre una sesión normal (`context: { playerCount: 3, difficulty: 'expert' }`, `round: 7`) sigue devolviendo `difficulty === 'expert'`, `playerCount === 3`, `round === 7` — la normalización no altera el camino feliz.
    - `npx vitest run engine/__tests__/history.test.ts engine/__tests__/persistence.test.ts` termina con código 0 sin haber editado ningún test existente (los tests nuevos llegan en la Task 3).
  </acceptance_criteria>
  <done>
    Ninguna sesión, por parcial o manipulado que sea su `context`, produce ya una
    `GameHistoryEntry` con `difficulty`, `playerCount` o `round` ausentes, `undefined`, `NaN`
    o fuera de rango; y una sesión reanudada con `context: {}` en `localStorage` arranca con
    el `context` de la sesión fresca en vez de adoptar el hueco.
  </done>
</task>

<task type="auto">
  <name>Task 2: La escritura se somete al mismo predicado que la lectura — `appendHistoryEntry` y el validador numérico (CR-01 ronda 2 + WR-03)</name>
  <files>app/composables/usePersistedSession.ts</files>
  <read_first>
    - `app/composables/usePersistedSession.ts` completo — el fichero a modificar. En concreto:
      `isGameHistoryEntry` (117-133) y su comentario de bloque (103-116, que ya explica por qué
      esta frontera trata `localStorage` como entrada no fiable), `readEnvelope`,
      `appendHistoryEntry` (293-303) con su comentario D-03/CR-03, y `loadHistory` (272-276).
    - `engine/history.ts` YA CORREGIDO por la Task 1 — para confirmar que todo lo que el motor
      produce supera el validador que esta tarea va a aplicar en la escritura. Si algo no
      encaja, el fallo está en la Task 1, no aquí: esta guarda no debe relajarse para que pase.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §CR-01 (el fix de una línea)
      y §WR-03 (`Number.isFinite` sustituye a las tres comprobaciones `typeof`).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md` línea 87 y siguientes —
      el texto literal de D-03, que es el contrato que esta tarea restaura.
  </read_first>
  <action>
    Modificar `app/composables/usePersistedSession.ts`, solo estas dos cosas:

    **1. Guarda de entrada en `appendHistoryEntry`.** Como PRIMERA sentencia de la función,
    antes de `const read = readEnvelope()`, devolver `false` si `isGameHistoryEntry(entry)` es
    falso. La firma pública no cambia (`(entry: GameHistoryEntry) => boolean`), y el `false`
    viaja por el camino de D-03 que `useGameHistory.record()` y `HistorySavedNotice` ya saben
    pintar: un fallo honesto en vez de un éxito falso.

    Añadir sobre la guarda un comentario que cite **CR-01 (ronda 2)** y fije la regla en una
    frase, en el mismo estilo de cita-de-decisión que usa el resto del fichero: *el predicado
    que decide qué se puede LEER (`isGameHistoryEntry`, usado por `loadHistory`) debe decidir
    también qué se puede ESCRIBIR; una entrada que no vaya a superarlo no se «guarda», se
    pierde en silencio con un ✓ encima.* Anotar además, para quien lea esto en la Fase 10,
    que tras la Task 1 de este plan esta rama es defensa en profundidad inalcanzable desde
    `record()` — su valor es impedir que un llamador futuro (la sincronización a Firestore, un
    import) reintroduzca el hueco por otra puerta.

    NO tocar el resto del cuerpo: la lógica `readEnvelope()` → `unreadable` ⇒ `false` →
    envoltorio con `[entry, ...previas]` en crudo es el cierre de CR-03 y sigue siendo
    correcta palabra por palabra.

    **2. `isGameHistoryEntry`: los números se validan por finitud, no por `typeof` (WR-03).**
    `typeof NaN === 'number'` es `true`, así que hoy una entrada con `round: NaN` supera el
    validador y la tarjeta pinta «Hasta la ronda NaN». Sustituir las tres comprobaciones
    numéricas por `Number.isFinite`:

    - `typeof candidate.round === 'number'` → `Number.isFinite(candidate.round)`
    - `typeof candidate.playerCount === 'number'` → `Number.isFinite(candidate.playerCount)`
    - `(candidate.durationMs === null || typeof candidate.durationMs === 'number')` →
      `(candidate.durationMs === null || Number.isFinite(candidate.durationMs))`

    `Number.isFinite` ya implica `typeof === 'number'` (no coacciona su argumento), así que
    sustituye a las tres sin añadir ninguna comprobación extra. Es además el criterio que este
    mismo fichero ya consume desde el motor (`engine/history.ts`, guarda de `startedAt` y de
    `durationMs`): el estándar existe dentro de la propia fase y este validador simplemente no
    lo aplicaba. Ampliar el comentario de bloque que precede a `isGameHistoryEntry` con una
    línea citando WR-03.

    NO endurecer nada más en este validador: `recordedAt` sigue exigiéndose solo como `string`
    (rechazar una fecha no parseable escondería una partida completa que `formatEntryDate` ya
    degrada a «—», D-21), `playerCount === 0` sigue siendo aceptable (es lo que la Task 1
    produce honestamente cuando no se sabe cuántos jugaron: rechazarlo reintroduciría la
    pérdida silenciosa que este plan cierra), y no se añade la coherencia `result`/`lossCause`
    (es IN-09, fuera de alcance).
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/usePersistedSession.test.ts app/composables/__tests__/useGameHistory.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *//' app/composables/usePersistedSession.ts | grep -c "if (!isGameHistoryEntry(entry)) return false"` devuelve 1.
    - `grep -v '^ *//' app/composables/usePersistedSession.ts | grep -c "typeof candidate.round === 'number'"` devuelve 0.
    - `grep -v '^ *//' app/composables/usePersistedSession.ts | grep -c "typeof candidate.playerCount === 'number'"` devuelve 0.
    - `grep -v '^ *//' app/composables/usePersistedSession.ts | grep -c "Number.isFinite(candidate."` devuelve 3.
    - La guarda aparece ANTES de `readEnvelope()` dentro de `appendHistoryEntry`: en la salida de `grep -n "function appendHistoryEntry" -A 6 app/composables/usePersistedSession.ts`, la línea de `isGameHistoryEntry(entry)` tiene número menor que la de `readEnvelope()`.
    - Comportamiento: con `tga:history` ausente, `appendHistoryEntry(makeEntry({ difficulty: undefined as never }))` devuelve `false` y `fakeStorage.getItem('tga:history')` sigue siendo `null` (el espía `setItem` no se invocó con esa clave).
    - Comportamiento: con `tga:history` ausente, `appendHistoryEntry(makeEntry())` (entrada bien formada) sigue devolviendo `true` y la entrada se recupera con `loadHistory()` — el camino feliz no se degrada.
    - Comportamiento: una entrada con `round: NaN` no supera `loadHistory()`.
    - `npx vitest run app/composables/__tests__/usePersistedSession.test.ts app/composables/__tests__/useGameHistory.test.ts` termina con código 0 sin haber editado ningún test existente.
  </acceptance_criteria>
  <done>
    `appendHistoryEntry` no puede devolver `true` sobre una entrada que `loadHistory()` vaya a
    rechazar: el mismo predicado gobierna los dos extremos del circuito. Y `NaN`/`Infinity`
    dejan de cruzar la frontera en `round`, `playerCount` y `durationMs`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests de regresión — la invariante de ida y vuelta que nadie estaba comprobando</name>
  <files>engine/__tests__/history.test.ts, engine/__tests__/persistence.test.ts, app/composables/__tests__/usePersistedSession.test.ts, app/composables/__tests__/useGameHistory.test.ts</files>
  <read_first>
    - `engine/__tests__/history.test.ts` — reutilizar `baseContext`, `baseSession`,
      `emptyNames`, `NOON_UTC_MS`. El `describe('D-08/D-10: durationMs — reloj de pared sin
      tope, null cuando no se puede saber')` es el modelo de redacción a imitar: cada `it`
      nombra el caso hostil y afirma la cifra exacta.
    - `engine/__tests__/persistence.test.ts` — reutilizar `basePersisted(overrides)`,
      `tinyGame`, `expand`, `context`. Los tests de D-20 («`localStorage` manipulado») son el
      modelo de redacción para la rama `resumed`.
    - `app/composables/__tests__/usePersistedSession.test.ts` — reutilizar
      `createFakeLocalStorage()`, `makeEntry(overrides)` y el `beforeEach`/`afterEach` que
      monta y desmonta `globalThis.window`. Los tests nuevos van dentro del `describe`
      `'tga:history — clave independiente de la partida (D-13/HIST-09)'` (línea 187) o en un
      `describe` hermano con el mismo arnés.
    - `app/composables/__tests__/useGameHistory.test.ts` — reutilizar `makeSession`,
      `createFakeLocalStorage()` y el `describe('useGameHistory — ciclo record/reload/remove
      con localStorage falso')` (línea 373), cuyo único `it` ya ejercita `record`/`reload`/
      `remove` con el `window` de mentira.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el tercer `missing:`
      del gap SC2, que nombra literalmente dos de los tests de esta tarea.
    - `engine/history.ts`, `engine/persistence.ts` y `app/composables/usePersistedSession.ts`
      ya corregidos por las Tasks 1 y 2.
  </read_first>
  <action>
    Añadir los tests de regresión que hoy no existen. Cada uno debe FALLAR contra el código
    previo a este plan y PASAR tras él. Nombrar cada `it` citando el hallazgo que cubre
    («CR-01 ronda 2», «WR-03»), igual que los `describe` existentes citan su decisión D-xx. No
    editar ni borrar ningún test existente de los cuatro ficheros.

    **A. `engine/__tests__/history.test.ts`** — `describe` nuevo, «CR-01 (ronda 2):
    buildHistoryEntry normaliza en origen y nunca propaga un hueco»:

    1. Con `baseSession({ context: {} as SessionContext })`, la entrada devuelta tiene
       `difficulty === 'normal'`, `playerCount === 0` y `players` de longitud 0. Afirmar
       además que `Object.hasOwn(entry, 'difficulty')` y `Object.hasOwn(entry, 'playerCount')`
       son `true` — es justo lo que `JSON.stringify` eliminaba y lo que hacía desaparecer la
       partida.
    2. Con `context.difficulty` puesto a `'imposible' as never` y a `undefined as never`, la
       entrada devuelve `'normal'` en ambos casos; con `'expert'`, devuelve `'expert'`.
    3. Con `context.playerCount` puesto a `NaN`, a `-3`, a `2.5` y a `undefined`, la entrada
       devuelve `playerCount === entry.players.length` en los cuatro casos.
    4. Con `playerCount: 3` válido y `selection` con tres héroes, `playerCount === 3` y
       `players.length === 3`: la invariante `playerCount === players.length` se cumple
       también en el camino feliz, no solo en los degradados.
    5. Con `round` puesto a `NaN`, a `0`, a `-4` y a `2.5` (vía el segundo parámetro de
       `baseSession`), la entrada devuelve `round === 1`; con `round: 7`, devuelve `7`
       (el test D-09 existente ya cubre este último, no duplicarlo si ya afirma lo mismo).

    **B. `engine/__tests__/persistence.test.ts`** — `describe` nuevo, «CR-01 (ronda 2): la
    rama `resumed` valida el context igual que `content-changed`»:

    6. Con `basePersisted({ context: {} as SessionContext })` y un `runtimeId`/`contentVersion`
       que coinciden (para forzar la rama `resumed`), `resume()` devuelve
       `outcome === 'resumed'` y `session.context` es el `context` de la sesión FRESCA
       (`toEqual(fresh.context)`), no el `{}` persistido.
    7. Con `basePersisted({ context: { playerCount: 2, difficulty: 'expert' } })` y la misma
       coincidencia, `outcome === 'resumed'` y `session.context` sigue siendo EXACTAMENTE el
       persistido: la validación no degrada el camino feliz (este es el test anti-regresión
       del cambio).
    8. Con `basePersisted({ round: NaN as unknown as number })`, `session.round === 1`; con
       `round: 3`, `session.round === 3`.

    **C. `app/composables/__tests__/usePersistedSession.test.ts`** — el test que
    `09-VERIFICATION.md` nombra literalmente:

    9. «CR-01 (ronda 2): `appendHistoryEntry` de una entrada sin `difficulty` devuelve `false`
       y no escribe nada». Con `tga:history` ausente, llamar con
       `makeEntry({ difficulty: undefined as never })` y afirmar las tres cosas: el retorno es
       `false`, `fakeStorage.getItem('tga:history')` es `null`, y
       `fakeStorage.setItem` no fue invocado con la clave `'tga:history'` (el espía `vi.fn` ya
       lo permite; filtrar `mock.calls`).
    10. La misma prueba con `makeEntry({ playerCount: undefined as never })` y con
        `makeEntry({ round: NaN })`.
    11. «CR-01 (ronda 2): una entrada válida sigue escribiéndose» — con `tga:history` ausente,
        `appendHistoryEntry(makeEntry({ id: 'ok' }))` devuelve `true` y
        `loadHistory().map(e => e.id)` es `['ok']`. Sin esta aserción el cierre podría pasar
        con una guarda que rechace todo.
    12. «CR-01 (ronda 2): con entradas previas, una entrada inválida no las toca» — sembrar un
        envoltorio v1 válido, capturar la cadena sembrada ANTES de la llamada, llamar con una
        entrada inválida y afirmar con `toBe` que la cadena guardada es idéntica byte a byte.
    13. «WR-03: `round`/`playerCount`/`durationMs` con `NaN` o `Infinity` no superan
        `loadHistory()`». Sembrar un envoltorio v1 con `makeEntry({ id: 'buena' })` y varias
        entradas rotas (`round: NaN`, `playerCount: Infinity`, `durationMs: NaN`) y afirmar
        que `loadHistory().map(e => e.id)` es exactamente `['buena']`.

    **D. `app/composables/__tests__/useGameHistory.test.ts`** — el segundo test que
    `09-VERIFICATION.md` nombra literalmente, y el que de verdad cierra el círculo porque
    cruza los tres ficheros de producción:

    14. «CR-01 (ronda 2): una entrada construida desde `context: {}` sobrevive a un ciclo
        `record()` → `loadHistory()`». Dentro del `describe` del ciclo con `localStorage`
        falso, construir un `EngineSession` cuyo `context` sea `{} as SessionContext` y cuyo
        `round` sea `NaN`, llamar a `record(session, 'won')` y afirmar que devuelve `true`;
        después `reload()` y afirmar que `entries.value` tiene longitud 1 y que esa entrada
        tiene `difficulty === 'normal'`, `playerCount === 0`, `round === 1` y `players` de
        longitud 0. Este test es la invariante que faltaba: *lo que el motor construye,
        `localStorage` lo devuelve*. Contra el código previo a este plan, `record()` devolvía
        `true` y `reload()` dejaba `entries.value` vacío.
    15. La misma ida y vuelta con una sesión normal (`makeSession({ selection: { villainId:
        'rhino', heroes: [{ heroId: 'spider-man', playerName: 'Ana' }] } })`), afirmando que
        `playerCount === 1` y `round` es el de la sesión: el camino feliz sigue intacto.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` termina con código 0, con al menos 26 ficheros y estrictamente más de 665 tests (línea base reproducida por `09-VERIFICATION.md`).
    - `npx vitest run engine/__tests__/history.test.ts engine/__tests__/persistence.test.ts` termina con código 0.
    - `npx vitest run app/composables/__tests__/usePersistedSession.test.ts app/composables/__tests__/useGameHistory.test.ts` termina con código 0.
    - `grep -c "CR-01 (ronda 2)" engine/__tests__/history.test.ts engine/__tests__/persistence.test.ts app/composables/__tests__/usePersistedSession.test.ts app/composables/__tests__/useGameHistory.test.ts` devuelve un total de al menos 4 (uno por fichero como mínimo).
    - `grep -c "WR-03" app/composables/__tests__/usePersistedSession.test.ts` devuelve al menos 1.
    - Al menos un test de `usePersistedSession.test.ts` compara el blob con `toBe` sobre la cadena capturada antes de la escritura (intacto byte a byte), no con `toEqual` sobre el objeto parseado.
    - `git diff --stat` no muestra ninguna línea ELIMINADA en los bloques de test preexistentes de los cuatro ficheros: solo adiciones (los tests nuevos) y, como mucho, los `import` ampliados.
    - Prueba de que los tests son reales y no tautológicos: `git stash` de los cambios de producción de las Tasks 1 y 2 (dejando los tests), ejecutar `npx vitest run` y confirmar que los tests nuevos FALLAN; `git stash pop` y confirmar que vuelven a pasar. Dejar constancia del resultado en el SUMMARY.
  </acceptance_criteria>
  <done>
    Los dos escenarios que `09-VERIFICATION.md` reprodujo con un test desechable están
    cubiertos por tests permanentes en el repo, más la invariante de ida y vuelta
    `record()` → `loadHistory()` que ningún test cubría, y la suite completa sigue en verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Esta fase no tiene backend, ni autenticación, ni red, ni entrada de terceros: modelar
amenazas de red o de identidad aquí sería relleno. La única frontera de confianza real es
local, y este plan vive justo encima de ella.

| Boundary | Description |
|----------|-------------|
| `window.localStorage` → app (`tga:progress:*`) | El blob de progreso es **entrada no fiable**: build anterior con otra forma de `context`, edición manual en DevTools, escritura interrumpida por cuota. `resume()` decide qué parte de ese dato entra en la sesión viva. |
| `window.localStorage` → app (`tga:history`) | Mismo carácter de entrada no fiable. `isGameHistoryEntry` es el único filtro. |
| App → `window.localStorage` (`tga:history`) | Toda escritura destruye el valor anterior de la clave, y el histórico es el único dato de la app que el proyecto declara irreconstruible. Una escritura que no se puede volver a leer equivale a no haber escrito, pero con confirmación positiva. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12-01 | Tampering (pérdida de integridad producida por el propio código) | `appendHistoryEntry` en `app/composables/usePersistedSession.ts` | mitigate | Task 2: guarda `if (!isGameHistoryEntry(entry)) return false` como primera sentencia — la escritura se somete al mismo predicado que la lectura. Tests 9-12 de la Task 3, incluida la comparación byte a byte del blob previo. |
| T-09-12-02 | Tampering (propagación de dato sin forma desde storage al motor) | `resume()`, rama `resumed`, en `engine/persistence.ts` | mitigate | Task 1: `isValidContext(persisted.context) ? persisted.context : fresh.context` y `round` normalizado a entero ≥ 1, mismo criterio que `contentChangedFallback` ya aplica. Tests 6-8 de la Task 3. |
| T-09-12-03 | Tampering (type confusion numérica) | `isGameHistoryEntry` (`round`/`playerCount`/`durationMs`) | mitigate | Task 2 (WR-03): `Number.isFinite` sustituye a `typeof === 'number'`, que acepta `NaN`/`Infinity`. Test 13 de la Task 3. |
| T-09-12-04 | Repudiation (confirmación de éxito que el sistema no puede sostener) | Cadena `record()` → `appendHistoryEntry` → `HistorySavedNotice` | mitigate | Task 1 + Task 2 + test 14: el contrato de D-03 («si falla, se dice; no se inventa un éxito») vuelve a ser cierto porque el `true` implica ahora que `loadHistory()` recuperará la entrada. La invariante se prueba de extremo a extremo, no por inspección. |
| T-09-12-05 | Denial of Service (registro bloqueado con diagnóstico falso) | `appendHistoryEntry` devolviendo `false` por validación y no por `writeRaw` | accept | Es WR-02, fuera de alcance declarado. Aceptado porque tras la Task 1 el motor no puede producir una entrada inválida, así que esta rama es inalcanzable desde `record()`; su valor es defensa en profundidad ante un llamador futuro (Fase 10). El copy del aviso se decide junto con el resto de WR-02. |
| T-09-12-06 | Information disclosure | `tga:history` | accept | Nombres de jugador del propio grupo y resultados de partidas, solo en el dispositivo, sin red ni backend. No existe canal de salida que mitigar; Firestore no existe todavía en el repo (SC5). |
| T-09-12-07 | Spoofing / Elevation of privilege | — | accept | No aplican: la app no tiene identidades, sesiones autenticadas ni privilegios que escalar. Registrarlo como «no aplica» es más honesto que inventar una mitigación. |
| T-09-12-SC | Tampering (supply chain) | Instalaciones npm/pip/cargo | accept | Ninguna tarea de este plan instala ni actualiza paquetes: se modifican tres ficheros TypeScript y cuatro de test, todos ya existentes. El gate de legitimidad de paquetes no aplica. Si durante la ejecución surgiera la necesidad de instalar algo, este plan queda bloqueado hasta que un checkpoint humano lo apruebe. |
</threat_model>

<verification>
1. `npx vitest run` — la suite completa termina con código 0, 26 ficheros y estrictamente más
   de 665 tests. Ningún test preexistente editado para pasar.
2. `grep -v '^ *//' app/composables/usePersistedSession.ts | grep -c "if (!isGameHistoryEntry(entry)) return false"`
   devuelve 1.
3. `grep -v '^ *//' engine/persistence.ts | grep -c "isValidContext(persisted.context)"` devuelve 2.
4. `grep -v '^ *//' engine/history.ts | grep -c "Number.isInteger"` devuelve al menos 2.
5. `grep -v '^ *//' app/composables/usePersistedSession.ts | grep -c "Number.isFinite(candidate."` devuelve 3.
6. `grep -ric "firestore\|firebase" app/ engine/` devuelve 0 — SC5 y STAT-04 siguen
   trivialmente ciertos: la Fase 10 no se adelanta aquí.
7. `npm run build` termina con código 0.
8. `npx playwright test` (o `npm run e2e`) sigue en 36/36 si el entorno lo permite; si los
   clips de audio no están presentes localmente, anotarlo en el SUMMARY en vez de darlo por
   verde (ver IN-10 de `09-REVIEW.md`).
</verification>

<success_criteria>
- Una entrada con `difficulty` o `playerCount` ausentes NO se escribe: `appendHistoryEntry`
  devuelve `false` y `tga:history` no cambia ni un byte.
- `buildHistoryEntry` sobre `context: {}` y `round: NaN` produce una entrada completa
  (`difficulty: 'normal'`, `playerCount: 0`, `round: 1`) que sobrevive al ciclo
  `record()` → `loadHistory()`.
- `entry.playerCount === entry.players.length` es invariante para toda entrada que produce el
  motor, tanto en el camino feliz como en los degradados.
- La rama `resumed` de `resume()` no adopta un `context` sin forma de `SessionContext` ni un
  `round` que no sea entero ≥ 1, y sigue devolviendo el `context` persistido intacto cuando sí
  tiene forma.
- `round`, `playerCount` y `durationMs` con `NaN`/`Infinity` no cruzan `isGameHistoryEntry`.
- La suite completa sigue en verde con los cierres de la ronda anterior (CR-01/CR-02/CR-03)
  intactos, y los tests nuevos fallan si se revierten los cambios de producción.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-12-SUMMARY.md` al terminar.
</output>
