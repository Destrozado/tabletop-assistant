---
phase: 09-hist-rico-y-estad-sticas
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/composables/usePersistedSession.ts
  - app/composables/__tests__/usePersistedSession.test.ts
autonomous: true
requirements: [HIST-06, HIST-09, STAT-04]

must_haves:
  truths:
    - "D-13: el histórico vive en una única clave `tga:history` con envoltorio { formatVersion: 1, entries: [] }"
    - "D-13: la lectura valida entrada a entrada y descarta las inválidas sin tirar el resto; JSON corrupto o formatVersion distinto devuelven []"
    - "D-03: appendHistoryEntry devuelve boolean — rompe a propósito el patrón void del resto de esta capa porque writeRaw falla en silencio por diseño"
    - "HIST-09: clear(gameId) no toca `tga:history`, igual que hoy no toca `tga:voice-enabled`"
    - "D-14/STAT-04: la única fuente del histórico es localStorage; ni una línea menciona Firestore, Firebase ni sincronización"
    - "Borrar una entrada reasigna un array nuevo con .filter(), nunca hace splice in situ"
  artifacts:
    - path: "app/composables/usePersistedSession.ts"
      provides: "HISTORY_KEY, loadHistory, appendHistoryEntry (boolean), removeHistoryEntry"
      contains: "const HISTORY_KEY = 'tga:history'"
    - path: "app/composables/__tests__/usePersistedSession.test.ts"
      provides: "aislamiento de clear(), descarte por entrada y detección de escritura fallida"
  key_links:
    - from: "app/composables/usePersistedSession.ts"
      to: "window.localStorage"
      via: "readRaw / writeRaw / removeRaw ya existentes"
      pattern: "readRaw\\(HISTORY_KEY\\)"
---

<objective>
Extender la ÚNICA costura de `localStorage` de la app con la tercera clave independiente:
`tga:history`. Lectura defensiva entrada a entrada, escritura que informa de si logró
escribir, y borrado por id.

Propósito: HIST-06 (el histórico como fuente de verdad) y HIST-09 (que «Partida terminada»
nunca lo borre) se cierran mecánicamente aquí, siguiendo el precedente exacto de `VOICE_KEY`.
D-03 obliga además a que la escritura devuelva `boolean`, porque `writeRaw` falla en silencio
por diseño y el histórico es el único dato de la app que no se puede reconstruir.

Salida: `usePersistedSession.ts` extendido de forma aditiva y su test ampliado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-RESEARCH.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-PATTERNS.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-01-SUMMARY.md

<interfaces>
Estado actual de `app/composables/usePersistedSession.ts` (leído del repo):

- `const KEY_PREFIX = 'tga:progress:'`, `const VOICE_KEY = 'tga:voice-enabled'`,
  `function storageKey(gameId: string): string`.
- Ayudantes privados SSR-safe: `readRaw(key): string | undefined`,
  `writeRaw(key, value): void`, `removeRaw(key): void` — los tres detrás de
  `typeof window === 'undefined'` y con `try/catch` que trata cualquier fallo como ausencia.
- `isPersistedPosition(value: unknown): value is PersistedPosition` — validador privado de
  forma mínima que nunca lanza; es el criterio a imitar.
- `usePersistedSession()` devuelve `{ load, save, clear, loadVoicePreference,
  saveVoicePreference }`; `clear(gameId)` es literalmente `removeRaw(storageKey(gameId))`.

Del plan 09-01, en `engine/types.ts`: `GameHistoryEntry`, `HistoryPlayerEntry`, `LossCause`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: writeRaw informa del resultado + clave, envoltorio y lectura del histórico</name>
  <files>app/composables/usePersistedSession.ts</files>
  <read_first>
    - `app/composables/usePersistedSession.ts` (fichero a modificar; leer la cabecera WR-02
      completa, el comentario de D-46 sobre `VOICE_KEY`, `isPersistedPosition`, `readRaw`,
      `writeRaw`, `removeRaw` y `load`)
    - `engine/persistence.ts` (de dónde sale el patrón `formatVersion` que D-13 reutiliza a
      propósito)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-RESEARCH.md` §«Extensión de
      `usePersistedSession.ts`» (implementación de referencia)
  </read_first>
  <action>
    Modificar `app/composables/usePersistedSession.ts` de forma aditiva:

    1. Cambiar la firma de `writeRaw` de `(key: string, value: string): void` a
       `(key: string, value: string): boolean`: `false` cuando no hay `window` (SSR) y cuando
       el `try` captura una excepción; `true` cuando `setItem` no lanzó. Esto resuelve la Open
       Question 1 de `09-RESEARCH.md` — se elige propagar el resultado del `try` interno, NO
       comparar `readRaw` antes y después, porque la comparación produciría un falso negativo
       si el contenido escrito coincidiera con el anterior y además obliga a una lectura y un
       parseo de más. Ampliar el comentario existente del `catch` para explicar que el fallo
       sigue siendo silencioso para `save`/`saveVoicePreference` (VOZ-06/D-51: un fallo de
       almacenamiento nunca rompe la interacción) y que el único llamador que MIRA el
       resultado es `appendHistoryEntry` (D-03).
       `save` y `saveVoicePreference` no cambian ni una línea: siguen ignorando el valor
       devuelto.

    2. Añadir, junto a `VOICE_KEY` y con un comentario que cite D-13 y HIST-09 con el mismo
       razonamiento del comentario de D-46 ya presente:
       `const HISTORY_KEY = 'tga:history'` (SIN sufijo de `gameId`: es lo que hace que
       `clear(gameId)` no pueda borrarlo) y `const HISTORY_FORMAT_VERSION = 1`.

    3. Añadir la interfaz privada `HistoryEnvelope { formatVersion: 1; entries:
       GameHistoryEntry[] }` y el validador privado
       `isGameHistoryEntry(value: unknown): value is GameHistoryEntry`, calcado en criterio de
       `isPersistedPosition`: nunca lanza y comprueba forma mínima — `id` string, `gameId`
       string, `result` igual a `'won'` o `'lost'`, `recordedAt` string, `players` array,
       `round` número y `playerCount` número.

    4. Añadir dentro de `usePersistedSession()` la función `loadHistory():
       GameHistoryEntry[]`: lee `readRaw(HISTORY_KEY)`; sin dato devuelve `[]`; parsea dentro
       de `try/catch` y devuelve `[]` ante JSON corrupto; devuelve `[]` si
       `parsed.formatVersion !== HISTORY_FORMAT_VERSION` (D-13: punto de migración futuro) o
       si `parsed.entries` no es array; y en el caso bueno devuelve
       `parsed.entries.filter(isGameHistoryEntry)` — D-13: tres entradas con una rota
       devuelven las dos buenas, nunca se tira el array entero.

    Prohibido: tocar el cuerpo de `clear()`, derivar `HISTORY_KEY` de `storageKey()`, usar
    `useLocalStorage`/`useStorage` de VueUse (contradice la cabecera WR-02 del fichero), y
    mencionar Firestore/Firebase/sync.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/usePersistedSession.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const HISTORY_KEY = 'tga:history'" app/composables/usePersistedSession.ts` devuelve 1
    - `grep -c "function writeRaw(key: string, value: string): boolean" app/composables/usePersistedSession.ts` devuelve 1
    - `grep -c "function isGameHistoryEntry" app/composables/usePersistedSession.ts` devuelve 1
    - `grep -A 2 "function clear(gameId: string): void" app/composables/usePersistedSession.ts | grep -c "HISTORY_KEY"` devuelve 0
    - `grep -Ec "useLocalStorage|useStorage" app/composables/usePersistedSession.ts` devuelve 0
    - `npx vitest run app/composables/__tests__/usePersistedSession.test.ts` termina con código 0 (los tests existentes siguen en verde tras el cambio de firma de `writeRaw`)
  </acceptance_criteria>
  <done>La clave nueva existe, es independiente de `gameId`, la lectura descarta lo ilegible entrada a entrada y `writeRaw` ya puede informar de un fallo.</done>
</task>

<task type="auto">
  <name>Task 2: appendHistoryEntry (boolean) y removeHistoryEntry</name>
  <files>app/composables/usePersistedSession.ts</files>
  <read_first>
    - `app/composables/usePersistedSession.ts` (ya modificado en la Task 1)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md` D-03, D-13
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-PATTERNS.md` §«Reassignment, never
      in-place mutation»
  </read_first>
  <action>
    Añadir dentro de `usePersistedSession()`, después de `loadHistory`:

    - `appendHistoryEntry(entry: GameHistoryEntry): boolean` — lee el histórico actual con
      `loadHistory()`, construye un `HistoryEnvelope` nuevo con `entries: [entry,
      ...actuales]` (la entrada nueva PRIMERO, que es lo que hace que el orden almacenado ya
      sea de más reciente a más antigua, HIST-07) y devuelve directamente el booleano de
      `writeRaw(HISTORY_KEY, JSON.stringify(envelope))`.
      Comentario obligatorio: es la ÚNICA función de este fichero que no devuelve `void`, y es
      deliberado (D-03) — el histórico es el único dato de la app que no se puede reconstruir,
      así que el grupo tiene que enterarse si el dispositivo no dejó escribir (modo privado,
      cuota). Quien vaya a «homogeneizar» la firma a `void` debe leer D-03 primero.

    - `removeHistoryEntry(id: string): void` — lee con `loadHistory()`, construye un envoltorio
      nuevo con `entries: actuales.filter(e => e.id !== id)` y lo escribe. Reasignación
      completa, nunca `splice` in situ (disciplina de todo el motor). Devuelve `void`: un
      borrado que no llega a escribirse deja la entrada visible, que es un estado observable y
      recuperable, al contrario que una partida no registrada.

    Añadir `loadHistory`, `appendHistoryEntry` y `removeHistoryEntry` al objeto que devuelve
    `usePersistedSession()`, en el mismo estilo plano que las cinco funciones actuales.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/usePersistedSession.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function appendHistoryEntry(entry: GameHistoryEntry): boolean" app/composables/usePersistedSession.ts` devuelve 1
    - `grep -c "entries: \[entry, ...current\]" app/composables/usePersistedSession.ts` devuelve 1 (o la variante equivalente con la entrada nueva en primera posición)
    - `grep -c "\.splice(" app/composables/usePersistedSession.ts` devuelve 0
    - `grep -c "return { load, save, clear" app/composables/usePersistedSession.ts` devuelve 1 y esa línea (o el objeto que abre) incluye `loadHistory`, `appendHistoryEntry` y `removeHistoryEntry`
    - `npx vitest run app/composables/__tests__/usePersistedSession.test.ts` termina con código 0
  </acceptance_criteria>
  <done>La costura expone las tres operaciones del histórico, con la escritura informando de su resultado y el borrado reasignando un array nuevo.</done>
</task>

<task type="auto">
  <name>Task 3: tests de la clave tga:history</name>
  <files>app/composables/__tests__/usePersistedSession.test.ts</files>
  <read_first>
    - `app/composables/__tests__/usePersistedSession.test.ts` (fichero a modificar; reutilizar
      `createFakeLocalStorage` y el bloque
      `describe('tga:voice-enabled — clave independiente de la partida (D-46)')` y el test
      `D-46: clear() borra el progreso ... pero NUNCA la preferencia de voz` como plantilla
      literal)
    - `app/composables/usePersistedSession.ts` (ya modificado)
  </read_first>
  <action>
    Ampliar el test con un bloque nuevo
    `describe('tga:history — clave independiente de la partida (D-13/HIST-09)', ...)` y un
    ayudante local `makeEntry(overrides)` que construya un `GameHistoryEntry` válido.

    Casos obligatorios:
    - HIST-09: guardar progreso de partida, añadir una entrada al histórico, llamar a
      `clear(gameId)` y comprobar que `load(gameId)` es `null` mientras `loadHistory()` sigue
      devolviendo la entrada. Calcado del test de D-46 que ya existe para la voz.
    - `HISTORY_KEY` no comparte prefijo con `KEY_PREFIX` (mismo test de forma que el de
      `VOICE_KEY`).
    - Sin dato guardado, `loadHistory()` devuelve `[]`.
    - JSON corrupto en `tga:history` (escribir una cadena que no parsea con el
      `localStorage` falso) devuelve `[]` y no lanza.
    - Un envoltorio con `formatVersion: 2` devuelve `[]` (punto de migración de D-13).
    - Un envoltorio con tres entradas de las que una está rota (sin `id`, o con `result`
      inválido) devuelve exactamente las dos buenas.
    - Orden: dos `appendHistoryEntry` seguidos dejan la última entrada la primera del array.
    - D-03: con un `setItem` que lanza (`vi.fn(() => { throw new Error('quota') })`, simulando
      modo privado o cuota agotada), `appendHistoryEntry` devuelve `false` y no lanza; con el
      `localStorage` falso normal, devuelve `true`.
    - `removeHistoryEntry(id)` deja las demás entradas intactas y borrar un id inexistente no
      cambia nada ni lanza.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/usePersistedSession.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/composables/__tests__/usePersistedSession.test.ts` termina con código 0 y ejecuta al menos 9 tests más que antes del plan
    - `grep -c "HIST-09" app/composables/__tests__/usePersistedSession.test.ts` devuelve al menos 1
    - `grep -c "quota" app/composables/__tests__/usePersistedSession.test.ts` devuelve al menos 1
    - `npm test` termina con código 0
  </acceptance_criteria>
  <done>Un test mecánico prueba que `clear(gameId)` no puede borrar el histórico y que una escritura fallida se detecta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `window.localStorage` → app | Contenido íntegramente editable a mano desde DevTools: JSON corrupto, envoltorio de otra versión, entradas con campos ausentes o de tipo inesperado |
| SSR (prerender) → navegador | Durante `nuxt generate` no existe `window`; toda esta capa debe seguir devolviendo el valor por defecto sin tocar ningún global |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering | `tga:history` editado a mano | mitigate | `loadHistory` valida entrada a entrada con `isGameHistoryEntry` (mismo criterio que `isPersistedPosition`) y descarta lo ilegible sin lanzar ni tirar el resto |
| T-09-12 | Denial of service | Cuota agotada o modo privado impidiendo escribir | mitigate | `writeRaw` devuelve `false` y `appendHistoryEntry` lo propaga; el aviso de fallo del plan 09-02 lo hace visible en vez de fingir éxito (D-03) |
| T-09-13 | Repudiation | Pérdida silenciosa del histórico al terminar una partida | mitigate | `clear()` no se toca y `HISTORY_KEY` no deriva de `storageKey(gameId)`; test explícito de HIST-09 que lo comprueba mecánicamente |
| T-09-14 | Information disclosure | Datos de partida en claro en `localStorage` | accept | Mismo modelo que el resto de la app (progreso y preferencia de voz ya viven en claro); no hay PII más allá de nombres de pila que el propio grupo teclea en su dispositivo |
</threat_model>

<verification>
- `npm test` en verde.
- `grep -rEi "firestore|firebase|syncedToFirestore" app/composables/usePersistedSession.ts` no
  devuelve ninguna línea (D-14 / criterio de éxito nº 5 del ROADMAP).
- El cuerpo de `clear()` sigue siendo exactamente una línea con `removeRaw(storageKey(gameId))`.
</verification>

<success_criteria>
- `tga:history` es una tercera clave independiente que convive con las de progreso y voz.
- La lectura es defensiva entrada a entrada; la escritura informa de su resultado; el borrado
  reasigna.
- Un test prueba que «Partida terminada» (que llama a `clear`) no puede tocar el histórico.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-04-SUMMARY.md` al terminar.
</output>
