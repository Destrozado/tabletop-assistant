---
phase: 09-hist-rico-y-estad-sticas
plan: 21
type: execute
wave: 14
depends_on: ["09-20"]
gap_closure: true
files_modified:
  - app/components/HistorySavedNotice.vue
autonomous: true
requirements: [HIST-06, HIST-09]

must_haves:
  truths:
    - "SC3: la banda de aviso no contiene ni una sola frase escrita a mano sobre el estado de los datos del grupo — pinta exclusivamente el texto que `useHistorySavedNotice()` deriva de dos valores de retorno reales"
    - "SC3: con el registro fallido Y el progreso no asegurado, el grupo lee que no hay nada que reintentar, nunca que la partida sigue guardada"
    - "El aviso sigue siendo perceptible por un lector de pantalla: la región `aria-live` permanente que introdujo 09-16 (WR-05a) se conserva sin tocar, con el texto entrando y saliendo dentro de ella"
    - "09-UI-SPEC.md §8 intacto: misma forma de banda, mismo `✕` de `w-12 h-12` arriba a la derecha, sin `role=\"dialog\"`, sin velo, sin bloquear la entrada"
  artifacts:
    - path: "app/components/HistorySavedNotice.vue"
      provides: "banda que renderiza heading/body del composable, sin copy literal propia"
      contains: "useHistorySavedNotice"
  key_links:
    - from: "HistorySavedNotice.vue"
      to: "NOTICE_BODY del composable"
      via: "el computed `body` devuelto por useHistorySavedNotice()"
      pattern: "\\{\\{ body \\}\\}"
---

<objective>
Última pieza del **BLOCKER CR-01 (ronda 4)**: hoy `app/components/HistorySavedNotice.vue:55-57`
tiene escrita a mano la frase «La partida no se ha perdido: sigue guardada en el dispositivo…» y
la pinta para CUALQUIER fallo del registro. El plan 09-20 ya construyó las tres variantes, la
copy exportada y el cableado que produce los dos booleanos reales; este plan hace que el
componente deje de tener opinión propia sobre los datos del grupo y se limite a pintar lo que el
composable ha decidido.

Es la corrección que convierte el arreglo en real: mientras el texto siga siendo un literal en la
plantilla, el `v-else` lo pinta igual con las dos variantes de fallo y el grupo sigue leyendo una
promesa falsa en el caso no recuperable.

Purpose: que la superficie visible no pueda afirmar nada por su cuenta.
Output: `HistorySavedNotice.vue` reducido a `{{ heading }}` / `{{ body }}` + el `✕`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
@app/components/HistorySavedNotice.vue
@app/composables/useHistorySavedNotice.ts
</context>

<interfaces>
Ya disponible tras el plan 09-20 — no hace falta explorar el código para deducirlo:

```
useHistorySavedNotice(): {
  variant: ComputedRef<'success' | 'failure-recoverable' | 'failure-unrecoverable' | null>
  heading: ComputedRef<string | null>
  body:    ComputedRef<string | null>   // null en la variante 'success'
  dismiss: () => void
}
```

Colores del glifo de encabezado, que hoy vienen dados por la rama de la plantilla y que hay que
conservar: `first-letter:text-accent` en éxito (`✓`), `first-letter:text-warning` en las dos
variantes de fallo (`⚠`).
</interfaces>

<scope_boundary>
**DENTRO:** `app/components/HistorySavedNotice.vue`, y nada más.

**FUERA:**
- `app/composables/useHistorySavedNotice.ts` — lo dejó terminado el plan 09-20; aquí solo se
  consume. Si al ejecutar falta `heading` o `body`, NO se añaden aquí: significa que 09-20 no se
  completó y hay que detenerse y decirlo.
- `app/app.vue` y la maquetación de la banda (`fixed`/flujo, altura, el empujón de las pantallas
  `h-dvh`) → plan 09-22.
- La duración del autocierre (20 s) → no se toca en este plan.
- `app/pages/[game]/index.vue` → lo cableó el plan 09-20.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: la banda deja de tener copy propia y pinta lo que el composable decide</name>
  <files>app/components/HistorySavedNotice.vue</files>
  <read_first>
    - `app/components/HistorySavedNotice.vue` — el fichero a modificar, COMPLETO. Hay dos
      comentarios que NO se borran y que hay que leer antes de tocar nada: el de cabecera del
      `<script setup>` (por qué este componente llama a un composable en vez de recibir props, y
      por qué se monta en `app.vue`), y el comentario de 09-16 sobre la región viva permanente
      (WR-05a: «un lector de pantalla solo anuncia los cambios de una región `aria-live` que YA
      estaba en el DOM cuando el contenido cambió»). El tercer comentario —el de 09-16 sobre el
      texto de fallo, el que justifica la frase «la partida sigue en el dispositivo»— SÍ se
      sustituye, porque su razonamiento es exactamente el que CR-01 ronda 4 invalidó.
    - `app/composables/useHistorySavedNotice.ts` — ya con los cambios del plan 09-20:
      `NOTICE_HEADING`, `NOTICE_BODY` y lo que devuelve `useHistorySavedNotice()`.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §8 (líneas 399-437) — la forma
      exacta de la banda, el `✕` de `w-12 h-12` arriba a la derecha, «never blocks input — no
      overlay, no `role="dialog"`».
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — la fila
      `app/components/HistorySavedNotice.vue` de `artifacts:` («línea 56: afirma "sigue guardada
      en el dispositivo" como hecho, sin que ningún dato de retorno lo respalde») y el tercer
      punto de `missing:`.
    - `app/components/UpdateBanner.vue` — la banda hermana, para confirmar que las clases de
      contenedor que se conservan son idénticas a las suyas.
  </read_first>
  <action>
    Reescribir `app/components/HistorySavedNotice.vue` con estos cambios y ninguno más:

    **1. `<script setup>`:** desestructurar `heading` y `body` además de `variant` y `dismiss`
    de `useHistorySavedNotice()`.

    **2. Plantilla.** Conservar exactamente, sin tocar ni una clase:
    - el `<div role="status" aria-live="polite">` exterior, permanente y sin `v-if`, con su
      comentario de 09-16 íntegro;
    - el `<div v-if="variant !== null" class="bg-surface border-b border-background px-2xl py-lg flex items-start justify-between gap-md">`;
    - el `<button …>✕</button>` con `aria-label="Cerrar aviso"`, `w-12 h-12` y `@click="dismiss"`.

    Sustituir las dos ramas `v-if="variant === 'success'"` / `v-else` por **un solo bloque**
    `<div class="flex flex-col gap-sm">` que contenga:
    - un `<h2>` con las clases actuales del encabezado (`text-heading font-bold text-primary-text`)
      más un `:class` que aplique `first-letter:text-accent` cuando `variant === 'success'` y
      `first-letter:text-warning` en cualquier otro caso, e interpole `{{ heading }}`;
    - un `<p v-if="body" class="text-body font-normal text-secondary-text">` que interpole
      `{{ body }}`. El `v-if` sobre `body` es lo que hace que la variante de éxito siga siendo
      solo encabezado, tal como fija `09-UI-SPEC.md` §8.

    **3. Comentario nuevo en el sitio del viejo.** Donde hoy está el comentario de 09-16 que
    justifica la copy de fallo, escribir uno que explique el contrato nuevo: este componente ya
    no contiene ninguna afirmación sobre los datos del grupo; el texto lo decide
    `resolveNoticeVariant` a partir de dos valores de retorno reales (`record()` y `save()`), y
    vive en `NOTICE_HEADING`/`NOTICE_BODY` para que un test puro pueda comprobarlo. Citar CR-01
    ronda 4 de `09-VERIFICATION.md` y dejar escrita la regla en una frase: **ninguna frase que
    esta banda muestre puede escribirse aquí; si hace falta una nueva, se añade como variante en
    el composable, con el booleano que la respalde.**

    NO añadir props. NO añadir `role="alert"`, velo, `role="dialog"` ni nada que bloquee la
    entrada (`09-UI-SPEC.md` §8 lo prohíbe explícitamente). NO cambiar las clases de
    maquetación de la banda: el empujón sobre las pantallas `h-dvh` lo arregla el plan 09-22 y
    tocarlo aquí provocaría un conflicto entre los dos.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sigue guardada en el dispositivo" app/components/HistorySavedNotice.vue` devuelve exactamente 0 — la afirmación ya no está escrita a mano en la superficie.
    - `grep -c "Partida registrada" app/components/HistorySavedNotice.vue` devuelve 0 y `grep -c "No se pudo guardar la partida" app/components/HistorySavedNotice.vue` devuelve 0 — ninguna copy visible queda como literal en la plantilla.
    - `grep -c "{{ heading }}" app/components/HistorySavedNotice.vue` devuelve exactamente 1 y `grep -c "{{ body }}" app/components/HistorySavedNotice.vue` devuelve exactamente 1.
    - `grep -c 'aria-live="polite"' app/components/HistorySavedNotice.vue` devuelve exactamente 1 y `grep -c 'role="status"' app/components/HistorySavedNotice.vue` devuelve exactamente 1 — la región viva permanente de 09-16 (WR-05a) sigue intacta.
    - `grep -c 'v-if="variant !== null"' app/components/HistorySavedNotice.vue` devuelve exactamente 1 — el `v-if` sigue por DENTRO de la región viva, nunca sobre ella.
    - `grep -c 'aria-label="Cerrar aviso"' app/components/HistorySavedNotice.vue` devuelve exactamente 1 y su línea vecina conserva `w-12 h-12`.
    - `grep -c 'role="dialog"' app/components/HistorySavedNotice.vue` devuelve 0 y `grep -c "inset-0" app/components/HistorySavedNotice.vue` devuelve 0 — la banda sigue sin bloquear la entrada (`09-UI-SPEC.md` §8).
    - `grep -c "first-letter:text-accent" app/components/HistorySavedNotice.vue` devuelve exactamente 1 y `grep -c "first-letter:text-warning" app/components/HistorySavedNotice.vue` devuelve exactamente 1.
    - `grep -c "CR-01" app/components/HistorySavedNotice.vue` devuelve al menos 1 — el comentario nuevo cita el hallazgo que lo motiva.
    - `npm run build` termina con código 0.
    - `npx vitest run` termina con código 0, incluidos los 4 tests de `app/composables/__tests__/avisoTrasRegistroFallido.test.ts`.
  </acceptance_criteria>
  <done>
    El aviso que el grupo lee tras un registro fallido está derivado, palabra por palabra, de dos
    booleanos que la app ha comprobado. La banda no puede volver a prometer nada por su cuenta.
  </done>
</task>

</tasks>

<verification>
- `npm run build` y `npx vitest run` terminan ambos con código 0.
- `git diff --stat` muestra exactamente un fichero modificado: `app/components/HistorySavedNotice.vue`.
- Barrido de cierre del BLOCKER, que el SUMMARY debe citar con su salida literal:
  `grep -rn "sigue guardada" app/ | grep -v "useHistorySavedNotice.ts" | grep -v "__tests__"`
  no devuelve NINGUNA línea — la única aparición de la promesa en todo `app/` es la constante
  `NOTICE_BODY['failure-recoverable']`, que solo se alcanza con `progresoAsegurado === true`.
</verification>

<success_criteria>
El gap #1 de `09-VERIFICATION.md` queda cerrado de extremo a extremo: `save()` devuelve el
resultado real de la escritura (09-18), `onOutcomeRecorded` lo produce y lo comprueba (09-20), el
composable traduce los dos booleanos en una variante (09-20), un test de regresión lo fija
(09-20) y la superficie visible se limita a pintarlo (este plan).
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-21-SUMMARY.md` al terminar.
</output>
