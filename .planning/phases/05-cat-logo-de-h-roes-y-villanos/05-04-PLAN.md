---
phase: 05-cat-logo-de-h-roes-y-villanos
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - engine/types.ts
  - engine/catalogueSchema.ts
  - engine/__tests__/catalogueSchema.test.ts
  - scripts/catalogue/fetch-marvelcdb.mjs
  - content/marvel-characters.json
  - engine/__tests__/characters.test.ts
  - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md
autonomous: true
requirements: [CAT-01]
gap_closure: true
closes_gaps: ["CR-01"]

must_haves:
  truths:
    - "El catálogo committeado guarda, por cada uno de los 23 héroes, DOS tamaños de mano: handSizeHero (cara de héroe) y handSizeAlterEgo (cara de alter ego)"
    - "Ningún fichero del repo declara ya un campo handSize sin cualificar en el catálogo de personajes, ni en sus tipos ni en su esquema"
    - "Los valores de Spider-Man son handSizeHero 5 y handSizeAlterEgo 6, exactamente los que imprime el Rules Reference v1.7 (Apéndice III, ejemplo de carta de identidad)"
    - "Ningún comentario del repo afirma que el hand_size del lado héroe no sea un tamaño de mano real; el comentario de extracción cita el Rules Reference v1.7"
    - "El esquema Zod rechaza un héroe que traiga la clave legada handSize, así que un catálogo sin regenerar no puede pasar CI en silencio"
    - "Re-ejecutar npm run catalogue:generate sobre el mismo origen sigue produciendo un fichero byte a byte idéntico (D-10 intacto)"
  artifacts:
    - path: "engine/types.ts"
      provides: "CatalogueHero con handSizeHero y handSizeAlterEgo, y el comentario de cara activa citando el RR v1.7"
      contains: "handSizeHero"
    - path: "engine/catalogueSchema.ts"
      provides: "HeroSchema con los dos campos como z.number().int().positive() dentro del z.strictObject existente"
      contains: "handSizeAlterEgo"
    - path: "scripts/catalogue/fetch-marvelcdb.mjs"
      provides: "extractHero() lee y valida card.hand_size (héroe) y card.linked_card.hand_size (alter ego)"
      contains: "Apéndice III"
    - path: "content/marvel-characters.json"
      provides: "23 héroes con los dos tamaños de mano, regenerado por el script"
      contains: "handSizeHero"
  key_links:
    - from: "scripts/catalogue/fetch-marvelcdb.mjs"
      to: "content/marvel-characters.json"
      via: "extractHero devuelve handSizeHero desde card.hand_size"
      pattern: "handSizeHero"
    - from: "engine/catalogueSchema.ts"
      to: "engine/types.ts"
      via: "import type { CharacterCatalogue } y forma alineada de CatalogueHero"
      pattern: "handSize(Hero|AlterEgo)"
    - from: "engine/__tests__/characters.test.ts"
      to: "content/marvel-characters.json"
      via: "invariante de forma sobre los dos campos y ausencia del campo legado"
      pattern: "handSizeAlterEgo"
---

<objective>
Cerrar el gap CR-01 de `05-VERIFICATION.md`: el catálogo guarda un único `handSize` sin
cualificar, tomado siempre de la carta de alter ego, y descarta el tamaño de mano de la
cara de héroe — que el Rules Reference v1.7 define como un valor real, impreso y distinto
en cada cara. Peor todavía, el comentario D-09 del script deja escrito como hecho
autorizado que ese valor "no es el tamaño de mano real", listo para propagarse a la
Fase 7.

Purpose: `CLAUDE.md` §Constraints hace de la fidelidad contra el Rules Reference oficial
una puerta dura ("un asistente que guía mal es peor que no tener asistente"). El paso de
fin de fase de jugador de la Fase 7 corre en CADA ronda y necesita el tamaño de mano de
la cara ACTIVA; con un solo número guardado, a un Spider-Man en forma de héroe se le
diría "robad hasta 6" cuando su carta imprime 5. Recuperar el valor más tarde no es un
retoque de esquema: exige tocar el script y volver a descargar los 32 códigos.

Este plan aplica el fix a través de D-09, no contra D-09: la decisión del usuario ("nadie
edita el JSON a mano; un número equivocado significa que la regla de extracción está mal
y se corrige en el script") es exactamente el procedimiento que se sigue aquí. Lo que se
corrige es el anexo fáctico de D-09 ("caso concreto"), no la decisión.

Output: contrato (`engine/types.ts`, `engine/catalogueSchema.ts`) y generador
actualizados a dos campos por cara, `content/marvel-characters.json` regenerado con los
23 héroes, y una re-verificación contra el PDF oficial que reproduce la evidencia del
verificador y la deja en verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-VERIFICATION.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-REVIEW.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-02-SUMMARY.md

<interfaces>
<!-- Forma ACTUAL en el repo (la que este plan cambia) y forma OBJETIVO. -->

ACTUAL — `engine/types.ts` líneas 135-141:
  CatalogueHero { id: string; name: string; alterEgo: string; health: number; handSize: number }

ACTUAL — `engine/catalogueSchema.ts` líneas 45-51, dentro de `const HeroSchema = z.strictObject({...})`:
  id / name / alterEgo / health / handSize: z.number().int().positive()

OBJETIVO — `CatalogueHero` y `HeroSchema` con estos nombres exactos, en este orden:
  id, name, alterEgo, health, handSizeHero, handSizeAlterEgo
  (los dos nuevos: `z.number().int().positive()` en el esquema, `number` en el tipo)

Superficie del generador que se toca (`scripts/catalogue/fetch-marvelcdb.mjs`):
  function extractHero({ code, expectedName })   -- líneas ~169-199
  Puertas ya existentes que NO se tocan: card.name === expectedName, card.type_code === 'hero',
  presencia de card.linked_card, Number.isInteger(health) && health > 0.
  Comentario D-09 con la afirmación falsa: líneas 187-190.

Respuesta real de la API pública de MarvelCDB para un código de héroe
(`https://marvelcdb.com/api/public/card/01001a.json`), verificada en vivo durante la
planificación:
  card.name = "Spider-Man", card.type_code = "hero", card.hand_size = 5, card.health = 10
  card.linked_card.name = "Peter Parker", card.linked_card.hand_size = 6, card.linked_card.health = 10
  Es decir: el lado héroe SÍ trae su propio `hand_size`, distinto del de `linked_card`.

Fuente de autoridad (`reference/mc_rulesreference_v17-compressed.pdf`, fuera de control de
versiones; consultar con `pdftotext -layout`):
  - Apéndice III, anatomía de carta, punto 14: "Hand Size. The number of cards this
    card's controller resets their hand to each round." -> propiedad de la CARA.
  - Mismo apéndice, ejemplo impreso de Spider-Man: "HAND SIZE 5 / HIT POINTS 10" en
    IDENTITY (HERO) frente a "HAND SIZE 6 / HIT POINTS 10" en IDENTITY (ALTER-EGO).
  - Entrada "HAND SIZE": "Each player checks their hand size at the end of the player
    phase, either discarding down to or drawing up to the number of cards indicated by
    their hand size value." -> se comprueba el de la cara ACTIVA, cada ronda.
  - Apéndice II, Setup paso 1: cada jugador coloca su identidad con la cara de alter ego
    boca arriba, así que el número correcto DURANTE EL SETUP sigue siendo el de alter
    ego. El valor committeado hoy no está mal para el setup; lo que está mal es el nombre
    sin cualificar, la justificación falsa y el valor descartado.

Consumidores actuales de `handSize` en el repo (grep verificado durante la planificación;
no hay ninguno en `app/` — la Fase 6 aún no existe):
  scripts/catalogue/fetch-marvelcdb.mjs:180,184,185,187,196
  engine/catalogueSchema.ts:50
  engine/types.ts:132,140
  engine/__tests__/catalogueSchema.test.ts:11,74,76,102,164
  engine/__tests__/characters.test.ts:92,96,97
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato — CatalogueHero y HeroSchema pasan a dos tamaños de mano por cara</name>
  <files>engine/types.ts, engine/catalogueSchema.ts, engine/__tests__/catalogueSchema.test.ts</files>
  <read_first>
    - engine/types.ts líneas 112-155 (bloque completo "Catálogo de personajes (Fase 5)": el comentario de cabecera del bloque, VillainStage, CatalogueHero, CatalogueVillain y CharacterCatalogue — hace falta ver el registro exacto de los comentarios para escribir el nuevo en el mismo tono)
    - engine/catalogueSchema.ts completo (120 líneas: cabecera CR-01 sobre z.strictObject, characterIdPattern, slugifyCharacterName, los cuatro esquemas y el superRefine; el cambio va sólo dentro de HeroSchema, nada del superRefine se toca)
    - engine/__tests__/catalogueSchema.test.ts completo (182 líneas: baseCatalogue() en líneas 7-25 y los tres sitios donde se escribe un héroe literal — líneas 11, 102 y 164)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-VERIFICATION.md (gap CR-01, bloques reason y missing)
  </read_first>
  <action>
Cambiar el contrato del héroe de un campo a dos, con estos nombres exactos:
`handSizeHero` y `handSizeAlterEgo`. El campo `handSize` desaparece por completo del
repo — no se mantiene ningún alias ni campo de compatibilidad, porque no hay todavía
ningún consumidor (la Fase 6 aún no existe) y dejar vivo el nombre ambiguo es
precisamente lo que el gap CR-01 señala.

En `engine/types.ts`:
- En `CatalogueHero`, sustituir `handSize: number` por `handSizeHero: number` y
  `handSizeAlterEgo: number`, en ese orden, después de `health`.
- Reescribir el comentario de bloque que precede a la interfaz (líneas ~130-133). El
  texto actual afirma que `handSize` sale siempre de la carta de alter ego "donde
  `hand_size` es un modificador de habilidad (D-09)": esa afirmación es falsa y debe
  desaparecer. El comentario nuevo debe decir, en el mismo registro que el resto del
  fichero: que cada CARA de la carta de identidad imprime su propio tamaño de mano
  (Rules Reference v1.7, Apéndice III, anatomía de carta punto 14; ejemplo impreso de
  Spider-Man: héroe 5 / Peter Parker 6); que el chequeo de final de fase de jugador usa
  el de la cara ACTIVA, por eso hacen falta los dos; que el setup (Apéndice II, paso 1)
  arranca con la cara de alter ego boca arriba, así que el número del setup es
  `handSizeAlterEgo`; y que `health` sigue saliendo sólo de `linked_card` (eso no
  cambia). Mantener tal cual la referencia a D-07 sobre `name` y `alterEgo`.

En `engine/catalogueSchema.ts`:
- Dentro de `HeroSchema` (que sigue siendo `z.strictObject`, sin excepción), sustituir la
  línea `handSize: z.number().int().positive()` por `handSizeHero` y `handSizeAlterEgo`,
  ambos `z.number().int().positive()`.
- No tocar `characterIdPattern`, `slugifyCharacterName`, `VillainStageSchema`,
  `VillainSchema` ni ninguna rama del `superRefine`.
- Dejar anotada en un comentario de una línea junto a los dos campos la consecuencia
  buscada: al ser `z.strictObject`, un catálogo con la clave legada `handSize` falla con
  `Unrecognized key`, de modo que un fichero sin regenerar no puede colarse.

En `engine/__tests__/catalogueSchema.test.ts`:
- Actualizar los tres héroes literales (líneas 11, 102 y 164) para que lleven
  `handSizeHero` y `handSizeAlterEgo` en vez de `handSize`. Valores a usar: Spider-Man
  `handSizeHero: 5, handSizeAlterEgo: 6`; el Ms. Marvel de la línea 102 `handSizeHero: 5,
  handSizeAlterEgo: 6`.
- Sustituir el test `'lanza ZodError con handSize negativo'` (líneas 74-78) por dos tests
  equivalentes, uno por campo: `handSizeHero` a -1 lanza, `handSizeAlterEgo` a -1 lanza.
- Añadir un test de regresión explícito del gap, dentro del describe `'claves
  desconocidas rechazadas (CR-01 / CAT-04)'` o en uno propio junto a él: un héroe que
  traiga la clave legada `handSize` además de los dos campos nuevos hace lanzar el
  esquema. El nombre del test debe contener la subcadena `handSize` y la palabra
  `legada`, para que se lea en la salida de Vitest qué protege.

Aviso al ejecutor: al terminar esta tarea, `engine/__tests__/characters.test.ts` estará
en rojo a propósito — el catálogo del disco sigue con `handSize` y el esquema ya no lo
acepta. Además, por el defecto CR-02 que cierra el plan 05-05, Vitest reportará ese
fichero como "no tests" en vez de como fallo de aserción; ésa es la manifestación
esperada del otro gap y NO debe intentar arreglarse aquí. La Task 2 regenera el catálogo
y devuelve la suite a verde.
  </action>
  <verify>
    <automated>npx vitest run --project engine engine/__tests__/catalogueSchema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project engine engine/__tests__/catalogueSchema.test.ts` termina con exit code 0
    - `grep -c "handSizeHero" engine/types.ts` devuelve al menos 1 y `grep -c "handSizeAlterEgo" engine/types.ts` devuelve al menos 1
    - `grep -c "handSizeHero" engine/catalogueSchema.ts` devuelve al menos 1 y `grep -c "handSizeAlterEgo" engine/catalogueSchema.ts` devuelve al menos 1
    - `grep -c "handSize:" engine/types.ts` devuelve 0 y `grep -c "handSize:" engine/catalogueSchema.ts` devuelve 0 (ninguna declaración del campo legado sobrevive)
    - `grep -c "z.strictObject(" engine/catalogueSchema.ts` sigue devolviendo 4 (ningún objeto degradado a z.object)
    - `grep -ic "modificador de habilidad" engine/types.ts` devuelve 0
    - `grep -c "Apéndice III" engine/types.ts` devuelve al menos 1
    - En `engine/__tests__/catalogueSchema.test.ts` existe un test cuyo nombre contiene a la vez `handSize` y `legada`, y cuyo cuerpo afirma `.toThrow()`
  </acceptance_criteria>
  <done>CatalogueHero y HeroSchema declaran handSizeHero y handSizeAlterEgo; el campo handSize no existe en tipos ni esquema; el esquema estricto rechaza el campo legado y hay un test que lo demuestra; los comentarios citan el Rules Reference v1.7 en vez de la afirmación falsa.</done>
</task>

<task type="auto">
  <name>Task 2: Generador — extraer las dos caras, borrar la afirmación falsa y regenerar el catálogo</name>
  <files>scripts/catalogue/fetch-marvelcdb.mjs, content/marvel-characters.json, engine/__tests__/characters.test.ts</files>
  <read_first>
    - scripts/catalogue/fetch-marvelcdb.mjs completo (276 líneas: la cabecera D-06/D-09/D-10/CAT-04 y el procedimiento CAT-07 marcan el registro; extractHero en líneas 169-199 es lo que se toca; extractVillainStage, writeCatalogue y main no se tocan)
    - engine/__tests__/characters.test.ts líneas 80-105 (describe "invariantes de forma sobre héroes", donde vive la aserción sobre handSize en las líneas 92-99)
    - engine/catalogueSchema.ts tal como queda tras la Task 1 (la forma exacta que el fichero regenerado tiene que satisfacer)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md §"Corrección de cifras y reproducibilidad" (D-09 y D-10 completos: por qué el fix va en el script y por qué el fichero no lleva marca temporal)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-02-SUMMARY.md (el pacing de 1500 ms entre peticiones y el coste real de una ejecución completa: ~48 s para los 32 códigos)
  </read_first>
  <action>
En `extractHero()` de `scripts/catalogue/fetch-marvelcdb.mjs`:

- Mantener intactas las cuatro puertas ya existentes (nombre esperado, `type_code`
  `'hero'`, presencia de `linked_card`, `health` entero positivo).
- Leer el tamaño de mano de las DOS caras: de `card.hand_size` (cara de héroe) y de
  `card.linked_card.hand_size` (cara de alter ego). Nombrar las variables locales de
  forma que digan de qué cara vienen y renombrar la desestructuración actual
  `hand_size: handSize` para que deje de ser ambigua.
- Añadir una puerta de validación para el valor de la cara de héroe simétrica a la que ya
  existe para el de alter ego: entero y mayor que 0, con un `throw new Error` cuyo mensaje
  nombre explícitamente `hand_size del lado héroe` e incluya el código, el nombre de la
  carta y el valor recibido, siguiendo el patrón literal de los mensajes de error que ya
  hay en el fichero. Mantener la puerta existente del lado alter ego y ajustar su mensaje
  para que diga `linked_card.hand_size (lado alter ego)`.
- El objeto devuelto pasa a tener seis claves, en este orden exacto: `id`, `name`,
  `alterEgo`, `health`, `handSizeHero`, `handSizeAlterEgo`. Sigue siendo una lista blanca
  campo a campo (CAT-04); prohibido cualquier spread de `card`.
- Sustituir íntegramente el comentario de las líneas 187-190. El texto actual afirma que
  el `hand_size` del lado héroe "es un modificador de habilidad ... no el tamaño de mano
  real": es falso según el Rules Reference v1.7 y es la causa raíz del gap CR-01. El
  comentario nuevo debe (a) citar `Apéndice III` (anatomía de carta, punto 14) y la
  entrada `HAND SIZE` del RR v1.7 como fuente; (b) decir que cada cara imprime su propio
  tamaño de mano, con el ejemplo Spider-Man 5 (héroe) / Peter Parker 6 (alter ego); (c)
  decir que el chequeo de final de fase de jugador usa el de la cara activa, por eso se
  guardan los dos; (d) dejar claro que `health` sí sale siempre de `linked_card`; (e)
  advertir explícitamente que un `handSizeHero` bajo y llamativo —Iron Man expone 1— es el
  valor impreso real de esa cara y NO debe "corregirse": la habilidad de la carta que lo
  modifica se resuelve en la interfaz de la Fase 7, nunca en el catálogo; y (f) anotar que
  este comentario reemplaza al anexo "caso concreto" de D-09, cuya premisa fáctica quedó
  refutada por `05-VERIFICATION.md` (gap CR-01), mientras que la decisión D-09 en sí
  —nadie edita el JSON a mano, el fix va en el script— sigue vigente y es literalmente el
  procedimiento que se acaba de seguir.

Regenerar el catálogo con `npm run catalogue:generate`. Tarda ~48 s (32 peticiones con
1500 ms de pacing) y aborta sin escribir nada si cualquier código falla (D-05). No editar
`content/marvel-characters.json` a mano bajo ningún concepto: si un número sale mal, se
corrige la regla de extracción y se vuelve a ejecutar (D-09).

En `engine/__tests__/characters.test.ts`, actualizar el describe "invariantes de forma
sobre héroes":
- El test `'todo héroe tiene health y handSize enteros mayores que 0'` pasa a comprobar
  `health`, `handSizeHero` y `handSizeAlterEgo`, los tres como enteros mayores que 0, con
  mensajes de fallo que nombren el campo concreto y el `id` del héroe (mismo estilo que
  los mensajes actuales). Renombrar el test para que su nombre nombre los tres campos.
- Añadir un test de regresión del gap: ningún héroe del catálogo lleva la clave legada
  `handSize` — comprobarlo sobre `Object.keys(hero)`, con un mensaje que nombre el `id`
  del héroe infractor.
- No tocar nada más de ese fichero: su reestructuración por ámbito de módulo es el gap
  CR-02 y la cierra el plan 05-05.
  </action>
  <verify>
    <automated>npx vitest run --project engine</automated>
  </verify>
  <acceptance_criteria>
    - `npm run catalogue:generate` termina con exit code 0 e imprime `Escritos 23 héroes y 3 villanos en content/marvel-characters.json`
    - `grep -c handSizeHero content/marvel-characters.json` devuelve 23 y `grep -c handSizeAlterEgo content/marvel-characters.json` devuelve 23
    - `grep -c '"handSize":' content/marvel-characters.json` devuelve 0
    - `node -e "const c=require('./content/marvel-characters.json');const h=c.heroes.find(x=>x.id==='spider-man');console.log(h.handSizeHero,h.handSizeAlterEgo)"` imprime `5 6`
    - `node -e "const c=require('./content/marvel-characters.json');console.log(c.heroes.every(h=>Number.isInteger(h.handSizeHero)&&h.handSizeHero>0&&Number.isInteger(h.handSizeAlterEgo)&&h.handSizeAlterEgo>0&&!('handSize' in h)))"` imprime `true`
    - `node -e "const c=require('./content/marvel-characters.json');console.log(JSON.stringify(Object.keys(c.heroes[0])))"` imprime exactamente `["id","name","alterEgo","health","handSizeHero","handSizeAlterEgo"]`
    - `grep -c "\.\.\.card" scripts/catalogue/fetch-marvelcdb.mjs` sigue devolviendo 0 (lista blanca CAT-04 intacta)
    - `grep -Ec '"(text|real_text|flavor|traits|imagesrc|illustrator|url|octgn_id|generatedAt)"' content/marvel-characters.json` devuelve 0
    - `grep -ic "modificador de habilidad" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0
    - `grep -c "Apéndice III" scripts/catalogue/fetch-marvelcdb.mjs` devuelve al menos 1
    - `npx vitest run --project engine` termina con exit code 0 y reporta `Test Files 12 passed`
    - `npm run test` termina con exit code 0
  </acceptance_criteria>
  <done>El generador lee y valida el tamaño de mano de las dos caras, el comentario cita el Rules Reference v1.7 en vez de la afirmación refutada, el catálogo committeado trae los 23 héroes con los dos valores y ninguno con el campo legado, y la suite entera vuelve a verde.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verificación — contrastar contra el PDF oficial y confirmar que el determinismo D-10 sigue en pie</name>
  <files>.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md</files>
  <read_first>
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-VERIFICATION.md (gap CR-01: la evidencia exacta que hay que reproducir — el ejemplo impreso de Spider-Man y la entrada HAND SIZE)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md líneas 107-125 (D-09 completo, incluido el anexo "caso concreto" que este plan refuta, y D-10)
    - reference/README.md (por qué el PDF está fuera de control de versiones y cómo consultarlo)
    - content/marvel-characters.json regenerado en la Task 2 (es lo que se contrasta)
  </read_first>
  <action>
Reproducir, paso por paso, la evidencia con la que el verificador declaró el gap CR-01, y
dejar constancia de que ahora el catálogo la satisface.

1. Extraer el texto del Rules Reference: `pdftotext -layout
   reference/mc_rulesreference_v17-compressed.pdf /tmp/rr17.txt`. El PDF vive en el repo
   pero fuera de control de versiones (`reference/*.pdf` está en `.gitignore`) — si no
   estuviera presente en la máquina, PARAR y reportarlo como bloqueante en vez de dar la
   fidelidad por buena sin fuente. No copiar al repo ningún fragmento largo del PDF: la
   restricción legal de `CLAUDE.md` sigue vigente y en el SUMMARY van sólo las cifras y
   las citas cortas imprescindibles.

2. Localizar con grep sobre `/tmp/rr17.txt` las tres piezas de evidencia: la línea del
   ejemplo impreso que contiene `HAND SIZE 5 / HIT POINTS 10` (la misma línea contiene
   también `HAND SIZE 6 / HIT POINTS 10`, por el maquetado a dos columnas del apéndice);
   la línea de anatomía de carta que empieza por `14. Hand Size. The number of cards
   this card's`; y la entrada `HAND SIZE` cuya primera frase es `Each player checks their
   hand size at the end of the player`.

3. Contrastar los héroes muestreados. Imprimir del catálogo regenerado los pares
   (`handSizeHero`, `handSizeAlterEgo`) de estos cinco ids y comprobar que coinciden con
   los valores esperados: `spider-man` 5/6 (el único con confirmación directa e impresa
   en el PDF, y el que fija el criterio), `iron-man` 1/6, `thor` 4/5, `hulk` 4/5,
   `vision` 5/5. Los cuatro últimos son justo los que `05-02-SUMMARY.md` había registrado
   como "anomalías" sin reconocer que la anomalía era la regla real. Anotar en el SUMMARY,
   con honestidad sobre el nivel de confianza: sólo Spider-Man está impreso en el Rules
   Reference; los otros cuatro proceden de la API pública de MarvelCDB y su confirmación
   definitiva sería mirar el cartón físico. Anotar también, explícitamente, que el 1 de
   Iron Man es su tamaño de mano impreso de la cara de héroe y que la habilidad que lo
   aumenta se resuelve en la Fase 7, no aquí.

4. Confirmar que D-10 sigue en pie tras el cambio: calcular `shasum -a 256
   content/marvel-characters.json`, volver a ejecutar `npm run catalogue:generate` (otros
   ~48 s) y comprobar que el hash es idéntico. Registrar los dos hashes en el SUMMARY.

5. Corregir el anexo fáctico de D-09 en
   `.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md`. NO reescribir ni
   borrar la decisión D-09 ni su párrafo "caso concreto": es registro histórico de lo que
   el usuario decidió. Añadir inmediatamente debajo de ese párrafo un bloque nuevo que
   empiece por la cadena literal `**Corrección (gap CR-01 de 05-VERIFICATION.md):**` y
   que diga: que la premisa fáctica del "caso concreto" (el `hand_size` del lado héroe
   sería un modificador de habilidad y no un tamaño de mano) quedó refutada contra el
   Rules Reference v1.7 (Apéndice III punto 14 y entrada HAND SIZE; Spider-Man imprime 5
   en la cara de héroe y 6 en la de alter ego); que el catálogo pasa a guardar los dos
   valores como `handSizeHero` y `handSizeAlterEgo`; y que la decisión D-09 en sí sigue
   vigente y sin cambios, porque el fix se aplicó en el script y se regeneró el fichero,
   que es literalmente el procedimiento que D-09 exige.

No modificar ningún otro fichero en esta tarea.
  </action>
  <verify>
    <automated>node -e "const c=require('./content/marvel-characters.json');const g=id=>{const h=c.heroes.find(x=>x.id===id);return h.handSizeHero+'/'+h.handSizeAlterEgo};const got=['spider-man','iron-man','thor','hulk','vision'].map(g).join(' ');const want='5/6 1/6 4/5 4/5 5/5';if(got!==want){console.error('MISMATCH got='+got+' want='+want);process.exit(1)}console.log('OK '+got)"</automated>
  </verify>
  <acceptance_criteria>
    - `pdftotext -layout reference/mc_rulesreference_v17-compressed.pdf /tmp/rr17.txt` termina con exit code 0
    - `grep -c "HAND SIZE 5 / HIT POINTS 10" /tmp/rr17.txt` devuelve al menos 1, y esa misma línea contiene también `HAND SIZE 6 / HIT POINTS 10`
    - `grep -c "Each player checks their hand size at the end of the player" /tmp/rr17.txt` devuelve al menos 1
    - `grep -c "Hand Size. The number of cards this card" /tmp/rr17.txt` devuelve al menos 1
    - El comando `<automated>` de esta tarea imprime `OK 5/6 1/6 4/5 4/5 5/5` y termina con exit code 0
    - Dos ejecuciones consecutivas de `npm run catalogue:generate` producen el mismo `shasum -a 256 content/marvel-characters.json` (D-10), y los dos hashes quedan escritos en el SUMMARY
    - `grep -c "Corrección (gap CR-01 de 05-VERIFICATION.md)" .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md` devuelve 1
    - `grep -c "D-09" .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md` no ha disminuido respecto al valor previo a esta tarea (la decisión original sigue en su sitio, sólo se le añade un bloque)
    - `git diff --stat` no muestra ningún fichero modificado en esta tarea salvo `05-CONTEXT.md`
    - El SUMMARY registra las tres citas del RR localizadas, los cinco pares contrastados con su nivel de confianza y los dos hashes de determinismo
  </acceptance_criteria>
  <done>La evidencia del verificador está reproducida contra el PDF oficial y ahora la satisface el catálogo; los cinco héroes muestreados llevan los dos valores correctos; el determinismo D-10 está re-confirmado con hashes; y el anexo fáctico de D-09 queda corregido sin borrar la decisión del usuario.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marvelcdb.com -> `content/marvel-characters.json` | Única entrada no confiable de la fase: una respuesta HTTP de un tercero se convierte en un fichero de un repo público desplegado en Vercel. Cruza aquí durante `npm run catalogue:generate`, ejecutado a mano por el desarrollador. |
| `content/marvel-characters.json` -> bundle público | El fichero committeado viaja tal cual dentro del build; lo que entre aquí es público y permanente en el historial de git. |

Fuera de frontera: no hay servidor, ni sesión, ni autenticación, ni entrada de usuario en
esta fase. Modelar STRIDE completo aquí sería inventar superficie que no existe.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `extractHero()` en `scripts/catalogue/fetch-marvelcdb.mjs` | mitigate | El campo nuevo `handSizeHero` entra por la MISMA disciplina que el resto: puerta `Number.isInteger(x) && x > 0` con `throw` que aborta antes de escribir (D-05, todo-o-nada). Un valor no numérico, negativo o ausente de la API no puede llegar al fichero. |
| T-05-02 | Information disclosure | Proyección de carta a objeto de catálogo (CAT-04) | mitigate | El objeto devuelto sigue siendo lista blanca de seis claves nombradas una a una; prohibido el spread de `card`. Verificado por criterio de aceptación (`grep -c "\.\.\.card"` = 0) y por el grep de claves de copyright sobre el JSON regenerado. Segunda barrera: `z.strictObject` en los cuatro niveles del esquema. |
| T-05-03 | Tampering | `card.linked_card.name` -> `alterEgo` | accept | Sigue siendo el único valor escrito verbatim desde la red sin fijar (WR-03 del review). `05-VERIFICATION.md` lo clasificó como WARNING no bloqueante y este plan es cierre de gaps: pinearlo queda fuera de alcance a propósito, no por descuido. El riesgo real está acotado por el `expectedName` del lado héroe, que ya fija de qué carta se está hablando. |
| T-05-04 | Denial of service | `npm run catalogue:generate` con marvelcdb.com caído | accept | El script aborta con exit 1 sin escribir nada (D-05) y el fichero committeado sigue sirviendo; el build y CI nunca invocan el script (D-06). Una regeneración fallida es una molestia de desarrollo, nunca una caída de la app. |
| T-05-05 | Elevation of privilege | Ejecución del generador | accept | El script no usa `eval`, ni shell, ni procesos hijos, ni interpola datos de red en rutas de fichero; escribe en una ruta constante. Sin cambios en este plan. |
| T-05-SC | Tampering | npm / instalación de paquetes | n/a | Este plan no instala ningún paquete: no toca `package.json` ni `package-lock.json`. La puerta de legitimidad de paquetes no se activa. Si el ejecutor se viera tentado de añadir una dependencia, PARAR y replanificar. |
</threat_model>

<verification>
1. `npm run test` termina con exit code 0 (suite completa, no sólo el proyecto engine).
2. `grep -rn "handSize[^HA]" engine/ scripts/ content/ app/ --include=*.ts --include=*.mjs --include=*.json --include=*.vue` no devuelve ninguna declaración viva del campo legado (sólo, como mucho, el test de regresión que lo rechaza y los comentarios que explican por qué desapareció).
3. `git diff --stat` toca exactamente los siete ficheros de `files_modified`, ni uno más.
4. El catálogo regenerado sigue teniendo 23 héroes y 3 villanos, y los villanos no han cambiado ni un byte respecto al committeado (este plan no toca `extractVillainStage`).
</verification>

<success_criteria>
- El gap CR-01 de `05-VERIFICATION.md` queda cerrado: el catálogo guarda los dos tamaños de mano por héroe, con Spider-Man en 5/6 igual que el Rules Reference v1.7.
- Ningún comentario, tipo, esquema ni fichero de datos del repo sigue afirmando o implicando que el `hand_size` del lado héroe no sea un tamaño de mano real.
- La Fase 7 puede leer el valor de la cara activa sin volver a tocar el catálogo ni re-descargar nada.
- El determinismo D-10 y la lista blanca CAT-04 siguen demostrados tras el cambio.
- `npm run test` en verde y CI sin cambios de configuración.
</success_criteria>

<output>
Create `.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-04-SUMMARY.md` when done
</output>
