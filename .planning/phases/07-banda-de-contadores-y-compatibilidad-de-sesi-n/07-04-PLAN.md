---
phase: 07-banda-de-contadores-y-compatibilidad-de-sesi-n
plan: 04
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - app/composables/useGameSession.ts
  - app/composables/__tests__/useGameSession.test.ts
autonomous: true
requirements: [HP-01, HP-03, HP-05, HP-06, HP-07]

must_haves:
  truths:
    - "La banda se ve solo durante el bucle de rondas, y esa condición se deriva de `sectionRepeats === true`, nunca del id de contenido `ronda` (D-07, HP-01, TECH-04)"
    - "La banda recibe una celda de villano con etiqueta «VILLANO» más una celda por jugador, tantas como `playerCount` (D-04, HP-03)"
    - "La etiqueta de una celda de jugador es su nombre, y «Jugador N» cuando no lo ha puesto, reutilizando `resolvePlayerLabel` y `PLAYER_NAME_MAX_LENGTH`, nunca una segunda cifra ni un segundo texto por defecto (D-04)"
    - "Una celda sin valor conocido llega al componente como «—», el mismo carácter que la rejilla de la Fase 6 (D-12)"
    - "Un contador de héroe en 0 llega con la etiqueta «{nombre} · SIN VIDA» y marcado como derrotado; en 1 vuelve al nombre limpio (D-15, D-16, HP-06, HP-07)"
    - "La celda del villano nunca se marca como derrotada, ni cuando su valor es 0 (D-11)"
    - "«SIN VIDA» es un literal único en el código, sin concordancia de género que resolver (D-16)"
    - "Los mutadores expuestos son una sola sentencia de reasignación de `session.value`, sin ninguna escritura en propiedad anidada (D-20, HP-08)"
    - "Ningún componente importa `~~/engine/*`: la costura reactiva sigue siendo `useGameSession.ts` y solo ella (D-09 vía el patrón de componentes tontos)"
  artifacts:
    - path: "app/composables/useGameSession.ts"
      provides: "`showsCounterBand`, `counterCells`, `incrementCounter`, `decrementCounter` y la función pura `buildCounterCells`"
      exports: ["buildCounterCells", "useGameSession"]
    - path: "app/composables/__tests__/useGameSession.test.ts"
      provides: "Fijación del literal «SIN VIDA», del «—», del formato de claves y del fallback «Jugador N»"
      min_lines: 60
  key_links:
    - from: "app/composables/useGameSession.ts"
      to: "engine/counters.ts"
      via: "import con alias engineIncrement*/engineDecrement* + resolveCounterValues"
      pattern: "~~/engine/counters"
    - from: "app/composables/useGameSession.ts"
      to: "RuntimeStepNode.sectionRepeats"
      via: "computed showsCounterBand"
      pattern: "sectionRepeats === true"
    - from: "app/composables/useGameSession.ts"
      to: "app/composables/useHeroSearch.ts"
      via: "reutiliza resolvePlayerLabel para el fallback «Jugador N»"
      pattern: "resolvePlayerLabel"
---

<objective>
Montar la costura reactiva de los contadores: la condición de visibilidad, el modelo de
vista de las celdas y los dos mutadores que el componente va a emitir.

Purpose: `CounterBand.vue` (plan 05) tiene que poder ser tonto —sin un solo import de
`~~/engine/*`— y para eso todo tiene que llegarle resuelto: la etiqueta ya con su «Jugador N»
o su «· SIN VIDA», el valor ya como cadena o como «—», y la decisión de si la banda se pinta
o no. Ese trabajo es de este fichero, que es la única costura reactiva del proyecto. La parte
que se puede probar sin Vue —construir las celdas— se extrae como función pura exportada,
igual que `useStepShortcuts.ts` ya separa `shortcutsEnabled`/`isEditableTarget` de su
cableado, para que el literal «SIN VIDA» y el «—» queden fijados por un test.

Output: `app/composables/useGameSession.ts` ampliado y
`app/composables/__tests__/useGameSession.test.ts` nuevo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-UI-SPEC.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-PATTERNS.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-02-SUMMARY.md

<interfaces>
<!-- Contrato que este plan ESTABLECE. El plan 05 (CounterBand.vue + index.vue) lo consume tal cual. -->

Nuevas exportaciones de `app/composables/useGameSession.ts`:

- `export interface CounterCell { key: string, label: string, displayValue: string, defeated: boolean }`
  (misma forma que fija `07-UI-SPEC.md` §Component Inventory).
- `export function buildCounterCells(values: CounterState, slots: { heroId: string | null, playerName: string }[]): CounterCell[]`
  Función PURA, sin Vue, testeable a solas.

Nuevos valores devueltos por `useGameSession()`:
- `showsCounterBand: ComputedRef<boolean>`
- `counterCells: ComputedRef<CounterCell[]>`
- `incrementCounter(key: string): void`
- `decrementCounter(key: string): void`

Formato de claves (contrato con `CounterBand.vue`, ya fijado por `07-UI-SPEC.md`):
- celda del villano → `'villano'`
- celda del jugador de índice `i` → `` `jugador-${i}` `` (índice base 0)

Ya disponible de `engine/counters.ts` (plan 02):
`resolveCounterValues(context, catalogue)`, `incrementVillain(session, catalogue)`,
`decrementVillain(session, catalogue)`, `incrementHero(session, slot, catalogue)`,
`decrementHero(session, slot, catalogue)`.

Ya disponible en el fichero o en sus vecinos:
`resolvePlayerSlots`/`resolveVillainId` de `~~/engine/selection`;
`resolvePlayerLabel(slotIndex, playerName)` de `./useHeroSearch` (devuelve el nombre tal cual
o `Jugador ${slotIndex + 1}` si está vacío);
`useCharacterCatalogue().getCatalogue(gameId)` de `./useCharacterCatalogue`;
`currentNode` (ya computado en este fichero) con `sectionRepeats: boolean`;
`session.value.gameId`.

Caracteres exactos, sin sustitutos: `—` es EM DASH (U+2014), el mismo que ya usa
`selectionRows` en `app/pages/[game]/index.vue`. El separador de «SIN VIDA» es
espacio + MIDDLE DOT (U+00B7) + espacio, el mismo que usan las etiquetas compuestas de la app.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Añadir `buildCounterCells`, `showsCounterBand`, `counterCells` y los mutadores a `useGameSession.ts`</name>
  <files>app/composables/useGameSession.ts</files>
  <behavior>
    - `buildCounterCells({ villainHealth: null, heroHealth: [null, null] }, [{heroId:null,playerName:''},{heroId:null,playerName:''}])`
      devuelve 3 celdas: `{ key: 'villano', label: 'VILLANO', displayValue: '—', defeated: false }`,
      `{ key: 'jugador-0', label: 'Jugador 1', displayValue: '—', defeated: false }`,
      `{ key: 'jugador-1', label: 'Jugador 2', displayValue: '—', defeated: false }`.
    - Con `villainHealth: 42` y `heroHealth: [14]`, los `displayValue` son `'42'` y `'14'`
      (cadenas, no números) y ningún `defeated` es `true`.
    - Con `heroHealth: [0]` y `playerName: 'Ana'`, la celda es
      `{ key: 'jugador-0', label: 'Ana · SIN VIDA', displayValue: '0', defeated: true }`.
    - Con `heroHealth: [0]` y `playerName: ''`, la etiqueta es `'Jugador 1 · SIN VIDA'`.
    - Con `heroHealth: [1]`, la etiqueta vuelve a ser `'Ana'` limpia y `defeated` es `false`.
    - Con `villainHealth: 0`, la celda del villano sigue con `label: 'VILLANO'` y
      `defeated: false` — el villano NUNCA se marca (D-11).
    - El número de celdas es siempre `slots.length + 1`.
  </behavior>
  <read_first>
    - `app/composables/useGameSession.ts` — el fichero completo. En particular: el comentario
      de cabecera (la costura única entre motor y Vue), el bloque de imports (líneas ~13-19,
      con el patrón de alias `setHero as engineSetHero`), los tres wrappers
      `setVillain`/`setHero`/`setPlayerName` (líneas ~59-72, con su guarda
      `if (!session.value) return` y su ÚNICA sentencia de reasignación), la computed
      `showsSelectionGrid` (línea ~130) y las computeds `playerSlots`/`selectedVillainId`
      (líneas ~132-136), y el objeto `return` del final.
    - `engine/counters.ts` — las firmas reales tal como quedaron en el plan 02.
    - `app/composables/useHeroSearch.ts` — `resolvePlayerLabel` (líneas ~121-124) y el patrón
      de funciones puras exportadas junto a helpers de vista.
    - `app/composables/useCharacterCatalogue.ts` — `getCatalogue(gameId)` y por qué el
      catálogo es un import estático sin red.
    - `app/composables/useStepShortcuts.ts` — el precedente de «funciones puras exportadas al
      lado de su cableado» (`shortcutsEnabled`/`isEditableTarget` + `useStepShortcuts`), que
      es la razón por la que `buildCounterCells` se exporta en vez de quedarse dentro de la
      computed.
    - `engine/types.ts` — `RuntimeStepNode.sectionRepeats` (línea ~105) y `CounterState`.
    - `app/pages/[game]/index.vue` líneas ~374-410 (`selectionRows`) — de dónde sale el
      carácter `—` y el uso de `resolvePlayerLabel` para etiquetas de fila.
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-UI-SPEC.md`
      §Component Inventory y §Copywriting Contract — la forma de `cells` y los literales.
  </read_first>
  <action>
    Ampliar `app/composables/useGameSession.ts`. Cuatro adiciones, todas siguiendo patrones que
    ya están en el propio fichero.

    (1) Imports. Añadir un bloque desde `~~/engine/counters` con la MISMA convención de alias
    que ya usa el bloque de `~~/engine/selection`: `resolveCounterValues` sin alias, y
    `incrementVillain as engineIncrementVillain`, `decrementVillain as engineDecrementVillain`,
    `incrementHero as engineIncrementHero`, `decrementHero as engineDecrementHero`. Añadir
    `CounterState` al `import type` de `~~/engine/types`. Importar `resolvePlayerLabel` desde
    `./useHeroSearch` y `useCharacterCatalogue` desde `./useCharacterCatalogue`.

    (2) Función pura exportada a nivel de módulo (fuera de `useGameSession()`), con la interfaz
    `CounterCell` también exportada. `buildCounterCells(values, slots)` construye primero la
    celda del villano (`key: 'villano'`, `label: 'VILLANO'` fijo y en mayúsculas, `defeated`
    SIEMPRE `false` incluso con valor `0`, porque D-11 dice que la banda no reacciona a que el
    villano llegue a 0) y después una celda por entrada de `slots`, en orden, con
    `key` `` `jugador-${i}` ``. El `displayValue` de cualquier celda es `'—'` (EM DASH) cuando
    el valor es `null`, y `String(valor)` cuando es un número — incluido el `'0'`, que es un
    número normal y no un «sin valor». `defeated` de una celda de jugador es `valor === 0`
    (comparación estricta, nunca `!valor`, que colapsaría `null` con `0` y es exactamente lo
    que D-12 prohíbe). La etiqueta base es `resolvePlayerLabel(i, slots[i].playerName)` —
    reutilizada, nunca un segundo «Jugador N» teclado a mano — y cuando `defeated` es `true` se
    le añade el sufijo con el literal exacto `SIN VIDA` (mayúsculas, sin tilde, sin género,
    D-16) separado por espacio + MIDDLE DOT + espacio. Ese literal debe aparecer UNA sola vez
    en todo el código de la app, declarado como constante del módulo (nombre sugerido
    `DEFEATED_SUFFIX`) para que ninguna pantalla futura invente una segunda redacción.
    Comentario obligatorio citando D-15/D-16 y la nota de que HP-06 usa la palabra «derrotado»
    y que esta diferencia es deliberada.

    (3) Computeds dentro de `useGameSession()`, junto a `showsSelectionGrid`:
    `showsCounterBand` es una sola línea que lee `currentNode.value?.sectionRepeats === true`
    —igualdad estricta a propósito, mismo motivo que `showsSelectionGrid`— y nunca compara
    contra el id de contenido `'ronda'` (TECH-04, D-07). Comentario en el mismo tono que el de
    `showsSelectionGrid`, citando D-07 y dejando dicho que «durante la partida» de HP-01 se
    interpreta como «durante el bucle de rondas», y que por eso toda la preparación conserva el
    alto íntegro para el texto grande.
    `counterCells` devuelve `[]` sin sesión, y con sesión llama a `buildCounterCells` pasándole
    `resolveCounterValues(session.value.context, getCatalogue(session.value.gameId))` y
    `resolvePlayerSlots(session.value.context)`. La longitud la manda `playerCount` porque la
    manda `resolvePlayerSlots`/`resolveCounterValues`; este fichero no vuelve a derivarla.
    Instanciar `const { getCatalogue } = useCharacterCatalogue()` una sola vez dentro de
    `useGameSession()`, al lado de `useGameContent()`.

    (4) Mutadores expuestos: `incrementCounter(key)` y `decrementCounter(key)`. Guarda
    `if (!session.value) return` idéntica a la de `setHero`. Resuelven el catálogo con
    `getCatalogue(session.value.gameId)`. Si `key === 'villano'`, una ÚNICA sentencia
    `session.value = engineIncrementVillain(session.value, catalogue)` (o su `decrement`). Si
    `key` empieza por `'jugador-'`, extraen el índice con `Number.parseInt` sobre el resto de
    la clave y hacen una ÚNICA sentencia con `engineIncrementHero`/`engineDecrementHero` — sin
    validar el rango aquí, porque el motor ya devuelve la misma referencia con un `slot`
    inválido y duplicar la guarda sería una segunda fuente de verdad. Cualquier otra clave es
    un `return` silencioso. El formato de las claves vive solo en este fichero, que es también
    el que las produce en `buildCounterCells`; `index.vue` no debe conocerlo.
    Comentario citando D-20: son sentencias de reasignación de `session.value`, ninguna escribe
    en una propiedad anidada, y ahí está la razón de que HP-08 salga gratis con el
    `watchDebounced` no profundo.

    (5) Añadir `showsCounterBand`, `counterCells`, `incrementCounter` y `decrementCounter` al
    objeto `return`.

    Prohibiciones comprobables: sin `setInterval`/`setTimeout`/`requestAnimationFrame` (D-13);
    sin la cadena `'ronda'` en ninguna parte (TECH-04); sin escritura anidada sobre
    `session.value.context...`; sin un segundo `14` ni un segundo `'Jugador '` teclados a mano
    (se reutilizan `PLAYER_NAME_MAX_LENGTH` vía `resolvePlayerSlots` y `resolvePlayerLabel`).
  </action>
  <verify>
    <automated>npx vitest run --project app-logic; npx vitest run --project engine</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function buildCounterCells' app/composables/useGameSession.ts` devuelve 1.
    - `grep -c 'export interface CounterCell' app/composables/useGameSession.ts` devuelve 1.
    - `grep -c 'sectionRepeats === true' app/composables/useGameSession.ts` devuelve 1.
    - `grep -c "'ronda'" app/composables/useGameSession.ts` devuelve 0, y
      `grep -rc "'ronda'" app/ | grep -v ':0'` no devuelve ninguna línea (la app sigue sin
      cablear ese id en ningún sitio).
    - `grep -c 'SIN VIDA' app/composables/useGameSession.ts` devuelve exactamente 1 (literal
      único), y `grep -rc 'SIN VIDA' app/ | grep -v ':0'` señala solo este fichero.
    - `grep -c 'DERROTAD\|K.O.' app/composables/useGameSession.ts` devuelve 0.
    - `grep -c 'resolvePlayerLabel' app/composables/useGameSession.ts` es al menos 1.
    - `grep -c '~~/engine/counters' app/composables/useGameSession.ts` devuelve 1.
    - `grep -c "'jugador-" app/composables/useGameSession.ts` es al menos 2 (se produce la
      clave en `buildCounterCells` y se consume en los mutadores).
    - `grep -cE 'setInterval|setTimeout|requestAnimationFrame' app/composables/useGameSession.ts`
      devuelve 0.
    - `grep -cE 'session\.value\.context\.[a-zA-Z]+ *=|session\.value\.context\.counters' app/composables/useGameSession.ts`
      devuelve 0 salvo lecturas dentro de llamadas — verificado leyendo el diff: ninguna línea
      añadida asigna a una propiedad anidada de `session.value`.
    - `grep -c 'showsCounterBand' app/composables/useGameSession.ts` es al menos 2 (definición
      y `return`); igual para `counterCells`, `incrementCounter` y `decrementCounter`.
    - `npx vitest run` completo termina con código 0.
  </acceptance_criteria>
  <done>
    `useGameSession.ts` expone la condición de visibilidad derivada de `sectionRepeats`, el
    modelo de vista de las celdas con «—», «Jugador N» y «· SIN VIDA» ya resueltos, y dos
    mutadores de una sola sentencia de reasignación. Ningún componente necesita importar el
    motor.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fijar con test el literal «SIN VIDA», el «—», el formato de claves y el fallback «Jugador N»</name>
  <files>app/composables/__tests__/useGameSession.test.ts</files>
  <read_first>
    - `app/composables/useGameSession.ts` — la implementación real de `buildCounterCells` tras
      el Task 1.
    - `app/composables/__tests__/useHeroSearch.test.ts` — el analogo directo: cómo se testea una
      función pura exportada de un composable dentro del proyecto `app-logic` de Vitest,
      incluidos los imports con alias `~`/`~~`.
    - `vitest.config.ts` — confirmar que el proyecto `app-logic` incluye
      `app/**/*.test.ts`, corre en `environment: 'node'` y resuelve los alias `~`/`~~`, que es
      lo que permite importar este composable sin contexto de Nuxt.
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-UI-SPEC.md`
      §Copywriting Contract — los literales exactos que hay que assertar.
  </read_first>
  <action>
    Crear `app/composables/__tests__/useGameSession.test.ts` importando **solo**
    `buildCounterCells` (y el tipo `CounterCell` si hace falta) desde
    `~/composables/useGameSession`. No se testea el composable reactivo ni el cableado: mismo
    criterio que D-Q2 de `useStepShortcuts.ts`, donde el cableado queda fuera del test a
    propósito y solo se fijan las funciones puras.

    Un `describe('buildCounterCells (D-04/D-12/D-15/D-16)')` con estos `it`, uno por regla, con
    los valores literales del bloque `<behavior>` del Task 1:
    - «sin ningún valor conocido, todas las celdas pintan «—»» — comprobar además que el
      carácter es EM DASH y no un guion normal, comparando contra `'—'`.
    - «la celda del villano va primera, con etiqueta VILLANO fija».
    - «el número llega como cadena, incluido el 0».
    - «un jugador sin nombre se etiqueta Jugador N con N base 1».
    - «un contador de héroe en 0 añade el sufijo · SIN VIDA y marca defeated» — assertar la
      etiqueta completa `'Ana · SIN VIDA'` carácter a carácter, incluido el MIDDLE DOT
      (`'·'`), y que la palabra NO es «DERROTADO» ni «DERROTADA».
    - «en 1 la etiqueta vuelve al nombre limpio y defeated es false» (HP-07, reversibilidad).
    - «el villano con valor 0 no se marca como derrotado y conserva su etiqueta» (D-11).
    - «las claves son 'villano' y 'jugador-{índice base 0}'».
    - «hay siempre una celda más que jugadores», probado con 1, 2, 3 y 4 jugadores.
    - «`null` y `0` no se colapsan»: para el mismo slot, `null` da `'—'`/`defeated:false` y `0`
      da `'0'`/`defeated:true` — el par de aserciones que impide el fallo de D-12 (cuatro
      jugadores marcados «SIN VIDA» en la primera pantalla de una partida sin selección).
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/useGameSession.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/composables/__tests__/useGameSession.test.ts` termina con código 0 y
      con al menos 10 tests pasados.
    - `grep -c 'SIN VIDA' app/composables/__tests__/useGameSession.test.ts` es al menos 2.
    - `grep -c "u2014\|—" app/composables/__tests__/useGameSession.test.ts` es al menos 1.
    - `grep -c "jugador-0" app/composables/__tests__/useGameSession.test.ts` es al menos 1 y
      `grep -c "'villano'" app/composables/__tests__/useGameSession.test.ts` es al menos 1.
    - `grep -c 'Jugador 1\|Jugador 2' app/composables/__tests__/useGameSession.test.ts` es al
      menos 1.
    - `grep -c 'defeated' app/composables/__tests__/useGameSession.test.ts` es al menos 4.
    - `npx vitest run` completo termina con código 0 (ninguna suite preexistente rota).
    - Si el import de `~/composables/useGameSession` fallase por arrastrar contexto de Nuxt,
      documentar el fallo exacto en el SUMMARY antes de cambiar de enfoque; **no** convertir el
      test en un test del composable reactivo ni añadir `environment: 'nuxt'` a
      `vitest.config.ts` sin dejarlo escrito como desviación.
  </acceptance_criteria>
  <done>
    El literal «SIN VIDA», el EM DASH, el formato de claves, el fallback «Jugador N», la
    inmunidad del villano a la marca de derrotado y la separación estricta entre `null` y `0`
    están fijados por un test automático que corre en el proyecto `app-logic`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `SessionContext` (procedente de `localStorage`) → modelo de vista de las celdas | Este fichero es el último punto antes de la pantalla: cualquier `undefined`/`NaN` que llegue aquí se pinta. Toda la validación real ocurre antes, en `engine/counters.ts`, y este plan se limita a no deshacerla. |
| Nombre de jugador teclado por una persona → etiqueta de la banda | Texto arbitrario de hasta 14 caracteres que acaba renderizado. |
| Ninguna otra | Sin backend, sin red en ejecución, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | `counterCells` recibiendo un `context` corrupto | mitigate | `counterCells` no lee `context.counters` directamente jamás: pasa siempre por `resolveCounterValues` y `resolvePlayerSlots`, que ya normalizan por tipo y derivan la longitud de `playerCount`. Gate: `grep` de `session.value.context.counters` en el fichero devuelve 0. |
| T-07-14 | Tampering / Elevation of Privilege (XSS) | Nombre de jugador dentro de la etiqueta de la celda | mitigate | La etiqueta sale de este composable como cadena plana y `CounterBand.vue` la pintará por interpolación (`{{ }}`), nunca con `v-html` — convención T-01-01, ya verificada en la Fase 6 con `grep -rn "v-html" app/` vacío. Este plan no introduce ninguna vía de HTML crudo; el gate del `v-html` se ejecuta en el plan 05, donde nace el componente. |
| T-07-15 | Denial of Service (denegación de los datos propios) | Mutadores que muten en sitio | mitigate | Los dos mutadores expuestos son una sola sentencia de reasignación de `session.value`, calcada de `setVillain`/`setHero`. Una escritura anidada actualizaría la pantalla pero perdería los contadores al recargar (el `watchDebounced` no es profundo). Gate: ninguna línea añadida asigna a una propiedad anidada de `session.value`. |
| T-07-16 | Repudiation / Information Disclosure | — | accept | No aplica: no hay identidades ni datos sensibles; los nombres son motes de amigos elegidos por ellos mismos en su propio dispositivo. |
| T-07-SC | Tampering (cadena de suministro) | Dependencias | accept | Este plan no instala ni actualiza ningún paquete: usa `vue`, `~~/engine/*` y composables del propio repo, todos ya presentes. Sin superficie de cadena de suministro que auditar. |
</threat_model>

<verification>
- `npx vitest run` completo en verde (proyectos `engine` y `app-logic`).
- `grep -rn "~~/engine/" app/components/` no devuelve ninguna línea nueva: los componentes
  siguen siendo tontos (este plan no crea ninguno, y el plan 05 hereda el gate).
- `git diff --name-only` limitado a los dos ficheros de `files_modified`.
</verification>

<success_criteria>
- `showsCounterBand` deriva de `sectionRepeats === true` y no existe la cadena `'ronda'` en
  `app/`.
- `counterCells` entrega celdas con etiqueta y valor ya resueltos: «VILLANO», nombres o
  «Jugador N», «—» para lo desconocido y «· SIN VIDA» exactamente en 0.
- El villano nunca se marca como derrotado.
- Los mutadores expuestos reasignan `session.value` en una sola sentencia.
- El literal «SIN VIDA» aparece una única vez en el código y está fijado por un test.
</success_criteria>

<output>
Create `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-04-SUMMARY.md` when done
</output>
