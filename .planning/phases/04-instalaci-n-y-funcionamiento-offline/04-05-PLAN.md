---
phase: 04-instalaci-n-y-funcionamiento-offline
plan: 05
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - app/composables/useUpdatePrompt.ts
  - app/composables/__tests__/useUpdatePrompt.test.ts
  - app/components/UpdateBanner.vue
  - app/app.vue
  - e2e/update-banner.spec.ts
autonomous: true
requirements: [OFF-04]
must_haves:
  truths:
    - "Cuando hay una versión nueva publicada, aparece una banda que avisa y espera: la app nunca se recarga sola a mitad de ronda"
    - "La banda se puede descartar y no vuelve en toda la sesión (D-01)"
    - "Pulsar 'Actualizar' aplica la versión nueva al momento y la partida se reanuda en el mismo paso (D-02)"
    - "La banda aparece en cualquier pantalla, no solo en la de juego: una versión nueva puede detectarse también en el selector"
    - "La banda no es modal y no tapa ni bloquea el botón SIGUIENTE"
  artifacts:
    - path: "app/composables/useUpdatePrompt.ts"
      provides: "Decisión de visibilidad de la banda como función pura testeable + cableado con $pwa"
      exports: ["shouldShowUpdateBanner", "useUpdatePrompt"]
    - path: "app/components/UpdateBanner.vue"
      provides: "Banda descartable con botones Actualizar y Cerrar"
      min_lines: 25
    - path: "app/composables/__tests__/useUpdatePrompt.test.ts"
      provides: "Tests de la lógica de visibilidad, incluido el tri-estado"
    - path: "e2e/update-banner.spec.ts"
      provides: "Prueba de que la app nunca se recarga sola"
  key_links:
    - from: "app/app.vue"
      to: "app/components/UpdateBanner.vue"
      via: "montaje global dentro de #app-root, envuelto en ClientOnly"
      pattern: "UpdateBanner"
    - from: "app/components/UpdateBanner.vue"
      to: "$pwa.needRefresh / $pwa.updateServiceWorker"
      via: "useUpdatePrompt()"
      pattern: "useUpdatePrompt"
---

## Phase Goal

Cita literal de `.planning/ROADMAP.md` (Fase 4, línea `**Goal**`):

> Un grupo puede instalar la app en la tablet, abrirla a pantalla completa, y jugar
> una partida entera aunque la wifi se caiga a mitad de partida; cuando se publica
> una versión nueva, la app espera la decisión del usuario en vez de recargarse sola.

Rebanada que cierra este plan: **cuando se publica una versión nueva, la app
espera la decisión del grupo en vez de recargarse sola** (OFF-04).

<objective>
Dar superficie visible al `registerType: 'prompt'` que el plan 04-01 configuró:
una banda que avisa de que hay versión nueva, se puede ignorar, y solo aplica la
actualización cuando el grupo lo decide.

Propósito: sin esta banda, `prompt` significa "nunca se actualiza". Con ella,
significa "se actualiza cuando el grupo quiere", que es lo que pide OFF-04.
Salida: un composable con la decisión aislada y testeada, un componente tonto
calcado del precedente del repo, y su montaje global.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-instalaci-n-y-funcionamiento-offline/04-CONTEXT.md
@.planning/phases/04-instalaci-n-y-funcionamiento-offline/04-RESEARCH.md
@.planning/phases/04-instalaci-n-y-funcionamiento-offline/04-PATTERNS.md
@app/components/VoiceUnavailableNotice.vue
@app/composables/useVoiceAnnouncer.ts
@app/composables/usePreloadedAudio.ts
@app/app.vue
</context>

<interfaces>
<!-- Contrato que este plan crea. La Task 1 lo define, la Task 2 lo consume. -->

// app/composables/useUpdatePrompt.ts
export function shouldShowUpdateBanner(
  needRefresh: boolean | null | undefined,
  dismissed: boolean,
): boolean

export function useUpdatePrompt(): {
  showUpdateBanner: ComputedRef<boolean>
  dismissUpdate: () => void
  applyUpdate: () => void
}

<!-- API del módulo PWA que se consume (04-RESEARCH.md §Code Examples): -->
const { $pwa } = useNuxtApp()
$pwa?.needRefresh            // Ref<boolean> — hay un service worker esperando
$pwa?.updateServiceWorker(true)  // skipWaiting + clients.claim + recarga inmediata

<!-- Precedente exacto a copiar (04-PATTERNS.md): -->
app/composables/useVoiceAnnouncer.ts:391-395  -> ref de descarte de SESIÓN, nunca persistido
app/components/VoiceUnavailableNotice.vue     -> banda no modal, un solo emit `dismiss`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aislar la decisión de mostrar la banda en una función pura y cablearla con $pwa</name>

  <read_first>
    - `app/composables/useVoiceAnnouncer.ts` líneas 380-400 (el patrón `noticeDismissed` / `showVoiceUnavailableNotice`: descarte como estado de SESIÓN, nunca persistido — es literalmente D-01 ya implementado para otra banda)
    - `app/composables/usePreloadedAudio.ts` líneas 1-40 y 100-110 (convenciones: cabecera de comentarios explicando el porqué, `import { ref } from 'vue'` explícito y no auto-import, tri-estado `boolean | null`, guardia `typeof window === 'undefined'`)
    - `app/composables/__tests__/usePreloadedAudio.test.ts` (molde de los tests: nombres numerados en español, `vi.resetModules()` + `await import(...)` dinámico cuando hay estado de módulo, mocks manuales de globales del navegador)
    - `.planning/phases/04-instalaci-n-y-funcionamiento-offline/04-RESEARCH.md` §Pattern 2 y §Code Examples
    - `.planning/phases/04-instalaci-n-y-funcionamiento-offline/04-PATTERNS.md` §El try/catch de las APIs del navegador
    - `vitest.config.ts` (confirmar que el glob `app/**/*.test.ts` recoge el fichero de test nuevo sin tocar la configuración)
  </read_first>

  <files>app/composables/useUpdatePrompt.ts, app/composables/__tests__/useUpdatePrompt.test.ts</files>

  <behavior>
    - `shouldShowUpdateBanner(true, false)` devuelve `true` — hay versión nueva y no se ha descartado.
    - `shouldShowUpdateBanner(true, true)` devuelve `false` — descartada, no vuelve (D-01).
    - `shouldShowUpdateBanner(false, false)` devuelve `false` — no hay versión nueva.
    - `shouldShowUpdateBanner(null, false)` devuelve `false` — todavía no se sabe; el tri-estado nunca pinta la banda "por si acaso".
    - `shouldShowUpdateBanner(undefined, false)` devuelve `false` — `$pwa` puede no estar inyectado (prerender, SW no soportado) y eso jamás debe romper ni pintar nada.
    - Tras llamar a `dismissUpdate()`, `showUpdateBanner` es `false` aunque `needRefresh` siga siendo `true`, y sigue siendo `false` si `needRefresh` vuelve a ponerse a `true` en la misma sesión (D-01).
    - `applyUpdate()` no lanza nunca, aunque `$pwa` sea `undefined` o `updateServiceWorker` reviente.
  </behavior>

  <action>
Crear `app/composables/useUpdatePrompt.ts` siguiendo las convenciones del repo:
cabecera de comentarios que explique qué es y por qué, e imports explícitos
(`import { computed, ref } from 'vue'`, e `import type { ComputedRef } from 'vue'`
si hace falta) — nada de confiar en los auto-imports, ningún composable del repo
lo hace.

Exportar dos cosas:

1. `shouldShowUpdateBanner(needRefresh, dismissed)`: función PURA a nivel de
   módulo, sin dependencia alguna de Nuxt ni del navegador, que acepta
   `boolean | null | undefined` como primer argumento. Es la que se testea. El
   tri-estado sigue el precedente de `audioAvailable` en `usePreloadedAudio.ts`:
   `null`/`undefined` significan "aún no se sabe" y nunca pintan la banda.

2. `useUpdatePrompt()`: el cableado. Llama a `useNuxtApp()` para obtener `$pwa`,
   mantiene un `ref` local `dismissed` inicializado a `false` (estado de SESIÓN,
   nunca persistido, nunca pasa por `usePersistedSession` ni añade ninguna clave
   de `localStorage` — exactamente como `noticeDismissed` en
   `useVoiceAnnouncer.ts` líneas 391-395, citando D-01 en el comentario),
   y devuelve `showUpdateBanner` (un `computed` que delega en la función pura),
   `dismissUpdate()` y `applyUpdate()`.

   `applyUpdate()` llama a `$pwa?.updateServiceWorker(true)` dentro de un
   `try { ... } catch { }` con el bloque `catch` vacío de lógica y un comentario
   citando D-02 y D-03: es la convención transversal del repo para APIs del
   navegador (ver `usePersistedSession.ts` líneas 85-92 y `usePreloadedAudio.ts`
   líneas 65-71). Sin `console.error`, sin re-lanzar, sin error visible. Que una
   actualización falle nunca puede romper `next()`/`prev()` a mitad de partida.

   `useNuxtApp()` solo puede llamarse dentro de `useUpdatePrompt()`, nunca en el
   cuerpo del módulo: si se llamara al importar, el fichero no sería importable
   desde un test de Vitest en entorno `node` sin contexto de Nuxt.

Escribir los tests en `app/composables/__tests__/useUpdatePrompt.test.ts`
cubriendo los siete casos del bloque `<behavior>`, con nombres numerados y
descriptivos en español, como en `usePreloadedAudio.test.ts`. Los cinco primeros
prueban la función pura directamente. Para los dos últimos, montar un doble de
`$pwa` y verificar el comportamiento del descarte y del `try/catch`; si probar
`useUpdatePrompt()` completo exigiera un contexto de Nuxt real, es aceptable
limitar el test a la función pura y al `ref` de descarte, y dejar constancia en
el SUMMARY de qué quedó cubierto por Vitest y qué queda para el checkpoint
humano del plan 04-06. Lo que NO es aceptable es dejar sin test la función pura.

No tocar `vitest.config.ts`: el glob `app/**/*.test.ts` ya recoge este fichero.
  </action>

  <verify>
    <automated>npm run test</automated>
  </verify>

  <acceptance_criteria>
    - `app/composables/useUpdatePrompt.ts` exporta `shouldShowUpdateBanner` y `useUpdatePrompt`.
    - Los cinco casos de la función pura del bloque `<behavior>` están cubiertos por tests con nombre propio y pasan.
    - `npm run test` termina con código 0 y el recuento total de tests es mayor que antes de este plan (278 hoy).
    - `grep -c "useNuxtApp" app/composables/useUpdatePrompt.ts` devuelve 1, y esa llamada está dentro del cuerpo de `useUpdatePrompt()`, no en el ámbito del módulo (el fichero de test lo importa sin contexto de Nuxt y no falla).
    - `grep -c "localStorage\|usePersistedSession" app/composables/useUpdatePrompt.ts` devuelve 0: el descarte es estado de sesión, nunca persistido (D-01).
    - `app/composables/useUpdatePrompt.ts` contiene un `catch` cuyo cuerpo no ejecuta nada salvo un comentario que cita una decisión, y `grep -c "console\." app/composables/useUpdatePrompt.ts` devuelve 0.
    - `git diff --name-only` no incluye `vitest.config.ts`.
  </acceptance_criteria>

  <done>La regla "cuándo se muestra la banda" está aislada, testeada y no depende de Nuxt, y aplicar la actualización nunca puede lanzar.</done>
</task>

<task type="auto">
  <name>Task 2: La banda, su montaje global y la prueba de que la app nunca se recarga sola</name>

  <read_first>
    - `app/components/VoiceUnavailableNotice.vue` (31 líneas, el precedente exacto: componente tonto, sin props, clases de Tailwind del tema, botón de cierre con `aria-label="Cerrar aviso"`)
    - `app/app.vue` (25 líneas, único componente raíz compartido por las dos rutas; hoy es puro markup sin `<script setup>`; el `#app-root` con `portrait:hidden` y el overlay de orientación NO se tocan)
    - `app/pages/[game]/index.vue` líneas 440-465 (cómo se monta `VoiceUnavailableNotice` bajo `AppHeader` y por qué la banda de actualización NO va ahí sino en `app.vue`)
    - `app/composables/useUpdatePrompt.ts` (recién creado en la Task 1)
    - `e2e/pwa-install.spec.ts` y `playwright.config.ts` (ayudas de espera del service worker ya escritas en el plan 04-03)
    - `.planning/phases/04-instalaci-n-y-funcionamiento-offline/04-PATTERNS.md` §`app/app.vue` — árbol de componentes raíz
  </read_first>

  <files>app/components/UpdateBanner.vue, app/app.vue, e2e/update-banner.spec.ts</files>

  <action>
Crear `app/components/UpdateBanner.vue` calcando la forma de
`VoiceUnavailableNotice.vue`: `<script setup lang="ts">` que llama a
`useUpdatePrompt()` y expone `showUpdateBanner`, `dismissUpdate` y
`applyUpdate`; plantilla con `v-if="showUpdateBanner"` sobre un `div` con las
mismas clases de banda del precedente (`bg-surface`, `border-b border-background`,
`px-2xl py-lg`, `flex items-start justify-between gap-md`).

Contenido de la banda: un `h2` con clase `text-heading font-bold text-primary-text`
y el texto "Nueva versión disponible"; un `p` con clase
`text-body font-normal text-secondary-text` explicando en una frase que se puede
seguir jugando y aplicarla cuando queráis, y que la partida se reanuda en el
mismo paso; un botón "Actualizar" con el estilo de acción del tema
(`bg-accent text-on-accent`, altura mínima de 48px para el objetivo táctil) que
llama a `applyUpdate`; y un botón de cierre "✕" de 48x48 con
`aria-label="Cerrar aviso"` que llama a `dismissUpdate`, igual que el
precedente.

La banda NO es modal, no lleva overlay, no atrapa el foco y no puede tapar ni
desplazar el botón SIGUIENTE de `NavBand.vue`: es una banda en el flujo normal,
en la parte superior.

Montarla en `app/app.vue` dentro de `#app-root`, justo antes de `<NuxtPage />`.
Ese fichero es hoy puro markup sin `<script setup>` — no hace falta añadirle
uno, porque toda la lógica vive dentro del componente (misma forma "componente
tonto" que ya siguen `VoiceUnavailableNotice` y `ContentChangedNotice`).
Envolver `<UpdateBanner />` en `<ClientOnly>` para evitar el parpadeo de
hidratación: es el patrón que ya usa `app/pages/[game]/index.vue` a nivel de
plantilla. No tocar el overlay `#orientation-guard` ni la clase
`portrait:hidden` de `#app-root` (D-08: no hay trabajo responsive en esta fase).

Se monta en `app.vue` y no en la pantalla de juego porque una versión nueva
puede detectarse en cualquier pantalla, incluido el selector, y `app.vue` es el
único punto compartido por las dos rutas (no existe `app/layouts/` en este
proyecto). Comentarlo en el fichero.

Escribir `e2e/update-banner.spec.ts` con lo que Playwright SÍ puede demostrar de
verdad, sin inventar afordancias de producción solo para el test:
1. Con el service worker registrado y sin versión nueva publicada, la banda NO
   aparece: `page.getByText('Nueva versión disponible')` no está visible.
2. **La app no se recarga sola.** Marcar la página con un valor en `window`
   (que cualquier recarga borraría), esperar al menos 10 segundos con el service
   worker activo y comprobar que el marcador sigue ahí. Esta es la garantía dura
   de OFF-04 y la razón por la que `registerType: 'autoUpdate'` está prohibido.
3. **No hay cambio de controlador espontáneo:** registrar un listener de
   `controllerchange` sobre `navigator.serviceWorker` y comprobar que no se
   dispara en esos mismos segundos.
4. `registration.waiting` es `null` mientras no haya build nueva.

La aparición real de la banda con una versión nueva publicada NO se simula aquí:
requiere dos builds distintas servidas al mismo origen. Se verifica en el
checkpoint humano del plan 04-06, contra un despliegue real. Dejarlo escrito
como comentario en la cabecera del spec para que nadie lo lea como un olvido, y
NO añadir a producción ningún parámetro de query, flag de depuración ni "modo
demo" para forzar la banda en las pruebas.
  </action>

  <verify>
    <automated>npm run test && npx playwright test --reporter=list</automated>
  </verify>

  <acceptance_criteria>
    - `npx playwright test` termina con código 0 con los tres ficheros spec en verde (`pwa-install`, `offline-flow`, `update-banner`).
    - El test "la app no se recarga sola" mantiene el marcador de `window` tras al menos 10 segundos con el service worker activo, y el listener de `controllerchange` no se dispara.
    - `grep -c "UpdateBanner" app/app.vue` devuelve al menos 1, y la etiqueta está dentro de `#app-root` y antes de `<NuxtPage />`.
    - `app/app.vue` conserva sin cambios el `div#orientation-guard`, la clase `portrait:hidden` de `#app-root` y los dos textos "Girad la tablet" / "Esta aplicación se usa en horizontal" (`git diff app/app.vue` no muestra líneas eliminadas en ese bloque).
    - `app/components/UpdateBanner.vue` contiene un botón con `aria-label="Cerrar aviso"` y un botón con el texto "Actualizar", y no contiene `fixed inset-0`, `role="dialog"` ni ningún overlay (no es modal).
    - `grep -rc "pwa-update\|updateDemo\|debugBanner" app/ | grep -v ':0' | wc -l` devuelve 0: no se ha añadido ninguna afordancia de test al código de producción.
    - `npm run test` (Vitest) sigue en verde.
  </acceptance_criteria>

  <done>La banda existe, aparece en cualquier pantalla, se descarta para toda la sesión, y está demostrado automáticamente que la app no se recarga sola.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service worker en espera → app en ejecución | Aplicar la actualización sustituye el código que se está ejecutando en mitad de una partida |
| interfaz → decisión del usuario | La banda es la única superficie por la que el grupo decide cuándo cambia la versión |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Denial of Service (percibido) | Recarga sorpresa a mitad de ronda | mitigate | `registerType: 'prompt'` (04-01) más esta banda: la actualización solo se aplica llamando a `applyUpdate()` desde un click del usuario. Verificado por el test "la app no se recarga sola" y por la ausencia de `controllerchange` espontáneo |
| T-04-14 | Denial of Service | Un fallo de `updateServiceWorker` rompiendo la pantalla de juego | mitigate | `try/catch` de cuerpo vacío con comentario citando D-02, siguiendo la convención transversal del repo; `applyUpdate()` no lanza nunca (cubierto por el bloque `<behavior>`) |
| T-04-15 | Tampering | Una afordancia de depuración en producción para forzar la banda podría usarse para mostrar avisos falsos | mitigate | Prohibida explícitamente en la acción y verificada por un gate `grep`: la aparición real de la banda se verifica contra un despliegue real en el checkpoint humano del plan 04-06 |
| T-04-16 | Information Disclosure | Estado de descarte de la banda | accept | Es un booleano en memoria, nunca persistido y sin ningún dato asociado (D-01) |
</threat_model>

<verification>
1. La función pura de visibilidad está cubierta por Vitest, incluidos `null` y `undefined`.
2. La banda se monta en `app.vue` y por tanto aparece en las dos rutas.
3. La banda no es modal y no interfiere con `NavBand`.
4. Playwright demuestra que no hay recarga automática ni cambio de controlador espontáneo.
5. No hay ninguna afordancia de test en el código de producción.
</verification>

<success_criteria>
- OFF-04 queda cubierto por código: la app avisa y espera.
- La confirmación de que la banda APARECE de verdad ante una versión nueva publicada queda para el checkpoint humano del plan 04-06, contra un despliegue real; está declarado, no olvidado.
</success_criteria>

<output>
Crear `.planning/phases/04-instalaci-n-y-funcionamiento-offline/04-05-SUMMARY.md` al terminar.
Registrar: qué quedó cubierto por Vitest y qué quedó explícitamente para el
checkpoint humano, y el texto final de la banda.
</output>
