---
phase: 10-respaldo-en-firestore
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - firestore.rules
  - firebase.json
  - .firebaserc
  - .env.example
  - e2e/firestore-rules-contract.spec.ts
autonomous: false
requirements: [SYNC-06, SYNC-07]

user_setup:
  - service: firebase
    why: "El respaldo escribe en un proyecto de Firestore real; las reglas se despliegan a mano (D-15) y la auth anónima se habilita en la consola (SYNC-06)"
    env_vars:
      - name: NUXT_PUBLIC_FIREBASE_API_KEY
        source: "Consola de Firebase → Configuración del proyecto → Tus apps → App web → Configuración del SDK"
      - name: NUXT_PUBLIC_FIREBASE_AUTH_DOMAIN
        source: "Mismo sitio"
      - name: NUXT_PUBLIC_FIREBASE_PROJECT_ID
        source: "Mismo sitio"
      - name: NUXT_PUBLIC_FIREBASE_APP_ID
        source: "Mismo sitio"
    dashboard_config:
      - task: "Crear el proyecto de Firebase y una base de datos de Cloud Firestore en modo producción"
        location: "Consola de Firebase → Compilación → Firestore Database"
      - task: "Habilitar el proveedor de inicio de sesión Anónimo"
        location: "Consola de Firebase → Compilación → Authentication → Sign-in method → Anónimo"
      - task: "Desplegar el fichero de reglas committeado con `npx firebase-tools deploy --only firestore:rules`"
        location: "Terminal local, tras `npx firebase-tools login`"
      - task: "Declarar las cuatro variables NUXT_PUBLIC_FIREBASE_* como variables de entorno de BUILD en Vercel y redesplegar"
        location: "Vercel → Project → Settings → Environment Variables"

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SYNC-07/D-11: las reglas desplegadas permiten `create` y nada más — `read`, `update` y `delete` están denegados de forma explícita"
    - "SYNC-07/D-09: las reglas exigen `hasOnly()` sobre exactamente la lista blanca de la proyección más `uid` y `createdAt`, con comprobación de tipo por campo y topes de tamaño"
    - "D-12: una escritura con `createdAt` que no sea la hora del servidor se rechaza, y una con `uid` distinto del autenticado también"
    - "D-15: el fichero de reglas que se revisa es el mismo fichero committeado del que sale el despliegue — no hay una copia en la consola que pueda divergir"
    - "D-14: sin `.env` local la sincronización es no-op, así que `npm run dev` y los tests nunca escriben en el respaldo real"
    - "El despliegue real de las reglas queda revisado por una persona ANTES de que ninguna variable NUXT_PUBLIC_FIREBASE_* se rellene en ningún sitio"
  artifacts:
    - path: "firestore.rules"
      provides: "reglas create-only con validación de forma (D-09/D-11/D-12) y las instrucciones de despliegue manual en su cabecera"
      contains: "allow create"
    - path: "firebase.json"
      provides: "configuración mínima para `firebase deploy --only firestore:rules`"
      contains: "firestore"
    - path: ".firebaserc"
      provides: "id del proyecto por defecto (no es un secreto, D-13)"
    - path: ".env.example"
      provides: "los cuatro nombres de variable NUXT_PUBLIC_FIREBASE_*, todos vacíos"
      contains: "NUXT_PUBLIC_FIREBASE_PROJECT_ID"
    - path: "e2e/firestore-rules-contract.spec.ts"
      provides: "gate que compara la lista de hasOnly() de las reglas con SYNC_PAYLOAD_FIELDS"
  key_links:
    - from: "firestore.rules"
      to: "engine/sync.ts"
      via: "la lista de hasOnly() es SYNC_PAYLOAD_FIELDS más uid y createdAt"
      pattern: "hasOnly"
    - from: "e2e/firestore-rules-contract.spec.ts"
      to: "engine/sync.ts"
      via: "importa SYNC_PAYLOAD_FIELDS y lo compara con el texto de las reglas"
      pattern: "SYNC_PAYLOAD_FIELDS"
---

<objective>
Escribir, committear y dejar revisadas por una persona las reglas de seguridad que son la ÚNICA
barrera real de esta fase, y atar mecánicamente su lista de campos a la proyección que el plan
10-01 dejó en `engine/sync.ts`.

Propósito: el repo es público. Con la configuración web visible en el bundle, lo único que impide
que un desconocido lea las partidas del grupo, las borre o las corrompa son estas reglas. El
ROADMAP exige por eso una verificación humana concreta: revisar el fichero REALMENTE DESPLEGADO
antes de la primera escritura real, no después de que «ya funcione». Y el gate de contrato existe
porque una lista blanca que vive en dos ficheros diverge en silencio: el día que alguien añada un
campo a la proyección sin añadirlo a las reglas, la escritura se rechazará en producción y nadie
se enterará (nada lee de la nube).

Salida: `firestore.rules`, `firebase.json`, `.firebaserc` y `.env.example` committeados, un gate
de contrato en `e2e/`, y la revisión humana del despliegue real hecha y registrada.

Los supuestos señalados de la sonda de casos límite (10 filas) viven en la sección
`<probe_assumptions>` de `10-01-PLAN.md`; las prohibiciones de fase viven en el
`must_haves.prohibitions` de ese mismo plan. No se duplican aquí.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-respaldo-en-firestore/10-CONTEXT.md
@.planning/phases/10-respaldo-en-firestore/10-RESEARCH.md
@.planning/phases/10-respaldo-en-firestore/COVERAGE.md
@.planning/phases/10-respaldo-en-firestore/10-01-SUMMARY.md
</context>

<scope_boundary>
Este plan NO toca `nuxt.config.ts`, ni ningún fichero de `app/`, `engine/` o `content/`. No añade
`firebase-tools` a `package.json` bajo ningún concepto (D-15). No monta el emulador de Firestore ni
tests automatizados de reglas (D-14 los descarta por escrito). No crea un segundo proyecto de
Firebase. No añade App Check.

`PITFALLS.md:104` (la investigación de hito) recomienda `allow read: if true`. Queda EXPRESAMENTE
SUPERADO por D-11 y no debe reintroducirse: el argumento de esa recomendación era que «las
estadísticas necesitan leer», y la Fase 9 cerró las estadísticas leyendo exclusivamente
`localStorage` (STAT-04).
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: `firestore.rules`, `firebase.json` y `.firebaserc` committeados</name>
  <reversibility rating="reversible">Un fichero de reglas se redespliega en un minuto; equivocarse aquí no deja rastro en los datos.</reversibility>
  <files>firestore.rules, firebase.json, .firebaserc</files>
  <read_first>
    - `.planning/phases/10-respaldo-en-firestore/10-RESEARCH.md` §Code Examples — el bloque de reglas completo ya redactado y citado contra la documentación oficial, y los mínimos de `firebase.json`/`.firebaserc`. Es la especificación concreta de D-09/D-11/D-12, no una plantilla a adaptar
    - `engine/sync.ts` (creado en 10-01) — `SYNC_PAYLOAD_FIELDS`, la lista exacta de campos y su orden
    - `engine/types.ts` líneas 185-213 — los tipos de cada campo, que las reglas replican
    - `content/marvel-champions.json` línea 7 — `maxPlayers`, para entender de dónde sale el tope de tamaño de `players`
    - `.planning/phases/10-respaldo-en-firestore/10-CONTEXT.md` §D-11 y §D-15
  </read_first>
  <action>
    Crear `firestore.rules` en la raíz del repo, con `rules_version = '2'` y un único bloque
    `match /databases/{database}/documents { match /history/{docId} { … } }`.

    Contenido exacto de las condiciones:
    - `allow read, update, delete: if false;` — denegación explícita de las tres, escrita a
      propósito aunque la denegación por defecto ya las cubriría: el fichero es lo que una persona
      va a revisar en la Task 3, y una denegación que se lee es mejor que una que se deduce.
    - `allow create: if` con la conjunción de: `request.resource.data.keys().hasOnly([...])` sobre
      las catorce claves (las doce de `SYNC_PAYLOAD_FIELDS` —`id`, `gameId`, `result`, `lossCause`,
      `villainId`, `villainName`, `players`, `difficulty`, `playerCount`, `round`, `durationMs`,
      `recordedAt`— más `uid` y `createdAt`); `hasAll([...])` sobre las diez que nunca pueden ser
      nulas (`id`, `gameId`, `result`, `players`, `difficulty`, `playerCount`, `round`,
      `recordedAt`, `uid`, `createdAt`); `id` y `gameId` de tipo string; `result` dentro de
      `['won', 'lost']`; `lossCause` nulo o dentro de `['mainSchemeCompleted', 'heroesEliminated']`;
      `villainId` y `villainName` nulos o string; `players` de tipo list con
      `players.size() <= 8`; `difficulty` dentro de `['normal', 'expert']`; `playerCount` entero y
      mayor que 0; `round` entero y mayor que 0; `durationMs` nulo o entero; `recordedAt` string con
      `recordedAt.size() <= 64`; `request.resource.data.uid == request.auth.uid` (D-12, propiedad);
      y `request.resource.data.createdAt == request.time` (D-12, nadie puede antedatar).

    En la cabecera del fichero, en comentarios, dejar escritas cuatro cosas:
    (1) que la lista de `hasOnly()` es `SYNC_PAYLOAD_FIELDS` de `engine/sync.ts` más `uid` y
    `createdAt`, y que `e2e/firestore-rules-contract.spec.ts` lo comprueba mecánicamente;
    (2) la limitación aceptada a propósito: el lenguaje de reglas no puede iterar `players[]` para
    validar la forma de cada objeto, así que un documento con un `players` de elementos basura
    pasaría la regla — se acota por tamaño y se acepta, porque el peor caso es basura parcialmente
    inválida en un campo anidado de un documento que nadie lee;
    (3) el comando de despliegue de D-15, literal:
    `npx firebase-tools deploy --only firestore:rules`, precedido de `npx firebase-tools login`;
    (4) que este fichero es la única fuente: no existe ni debe existir una copia editada en la
    consola de Firebase, porque podrían divergir sin que nada avisara.

    Crear `firebase.json` con únicamente `{ "firestore": { "rules": "firestore.rules" } }`. No
    declarar `hosting` (la app se despliega en Vercel) ni `firestore.indexes` (la colección es de
    solo escritura, no hay ninguna consulta que indexar).

    Crear `.firebaserc` con `{ "projects": { "default": "<id-del-proyecto>" } }`. Si el id real del
    proyecto todavía no existe, dejar el marcador de posición literal `<firebase-project-id>` y
    nombrarlo en el `10-02-SUMMARY.md` como lo que la Task 3 tiene que sustituir — nunca inventar un
    id plausible.
  </action>
  <verify>
    <automated>test -f firestore.rules &amp;&amp; test -f firebase.json &amp;&amp; test -f .firebaserc &amp;&amp; git check-ignore -q firestore.rules firebase.json .firebaserc; echo "ignored=$?"</automated>
    <fails_when>alguno de los tres `test -f` sale distinto de cero, o la línea impresa dice `ignored=0` (que significaría que `.gitignore` está excluyendo ficheros que deben viajar en el repo)</fails_when>
    <automated>grep -c 'allow create' firestore.rules; grep -c 'hasOnly' firestore.rules; grep -c 'request.time' firestore.rules</automated>
    <fails_when>cualquiera de los tres recuentos impresos es 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "allow read, update, delete: if false" firestore.rules` devuelve 1
    - `grep -c "allow read: if true" firestore.rules` devuelve 0 — la recomendación superada de `PITFALLS.md:104` no está presente
    - `grep -c "request.resource.data.uid == request.auth.uid" firestore.rules` devuelve 1
    - `grep -c "request.resource.data.createdAt == request.time" firestore.rules` devuelve 1
    - `grep -c "players.size()" firestore.rules` devuelve 1
    - `grep -c "match /" firestore.rules` devuelve exactamente 2 — solo `databases/{database}/documents` e `history/{docId}`; ninguna otra colección tiene regla, así que cualquier otra ruta queda denegada por defecto
    - `grep -c "npx firebase-tools deploy --only firestore:rules" firestore.rules` es mayor que 0
    - `grep -c 'firebase-tools' package.json` sigue devolviendo 0
    - `firebase.json` no contiene ninguna clave `hosting`
  </acceptance_criteria>
  <done>
    Los tres ficheros existen en la raíz, están versionados, y el fichero de reglas contiene la
    denegación explícita de lectura/actualización/borrado, la lista blanca completa, las dos
    condiciones de D-12 y las instrucciones de despliegue manual en su cabecera.
  </done>
</task>

<task type="auto">
  <name>Task 2: `.env.example` y el gate de contrato entre las reglas y la proyección</name>
  <files>.env.example, e2e/firestore-rules-contract.spec.ts</files>
  <read_first>
    - `.gitignore` líneas 1-13 — `.env` y `.env.*` están excluidos con `!.env.example` reincluido: `.env.example` SÍ viaja en el repo y `.env` nunca
    - `engine/sync.ts` — `SYNC_PAYLOAD_FIELDS`, que este gate importa
    - `firestore.rules` — la lista de `hasOnly()` que este gate lee como texto
    - `e2e/offline-flow.spec.ts` líneas 12-24 — el patrón del repo para leer artefactos reales desde una spec de Playwright con `node:fs`/`node:path`, que es el árbol donde esos módulos sí tienen tipos
    - `.planning/phases/10-respaldo-en-firestore/10-RESEARCH.md` §Pitfall 3 — por qué un gate que depende de leer ficheros del repo/build vive en `e2e/` y no en `engine/__tests__/` ni en `app/composables/__tests__/`
    - `playwright.config.ts` — `testDir: './e2e'` y el `webServer` que construye antes de correr
  </read_first>
  <action>
    Crear `.env.example` con las cuatro variables de D-13, todas con valor vacío:
    `NUXT_PUBLIC_FIREBASE_API_KEY=`, `NUXT_PUBLIC_FIREBASE_AUTH_DOMAIN=`,
    `NUXT_PUBLIC_FIREBASE_PROJECT_ID=` y `NUXT_PUBLIC_FIREBASE_APP_ID=`. En comentarios encima:
    que estas claves web de Firebase no son un secreto (la seguridad la dan `firestore.rules`),
    pero que el repo es PÚBLICO y por eso no se committean con valor — cualquiera que clonara el
    repo escribiría en el respaldo del grupo al terminar una partida; que sin ellas la
    sincronización es no-op y eso es lo que mantiene limpios `npm run dev` y los tests (D-14); y que
    en Vercel deben declararse como variables de entorno de BUILD, porque con `nuxt generate` los
    valores se hornean en el build y cambiarlas sin redesplegar no tiene efecto.

    Crear `e2e/firestore-rules-contract.spec.ts`: una spec de Playwright que importa
    `SYNC_PAYLOAD_FIELDS` desde `../engine/sync` (importación relativa; `engine/` no usa alias) y
    lee `firestore.rules` con `readFileSync` desde `process.cwd()`. Extrae del texto de las reglas
    la lista literal del `hasOnly([...])` (por ejemplo, recortando entre `hasOnly([` y el `])` que
    lo cierra y quedándose con las cadenas entrecomilladas) y afirma tres cosas:
    (1) el conjunto extraído es exactamente `SYNC_PAYLOAD_FIELDS` más `uid` y `createdAt`, sin
    sobrantes ni faltantes, comparando conjuntos ordenados para que el orden no importe;
    (2) la extracción encontró al menos una clave — si el recorte devuelve una lista vacía el test
    falla con un mensaje que diga que el formato del fichero de reglas cambió, para que un gate roto
    nunca pase en verde por no haber comprobado nada;
    (3) el texto de las reglas contiene la denegación explícita de lectura, actualización y borrado.
    Los mensajes de las aserciones deben nombrar el campo concreto que sobra o que falta, no decir
    solo que las listas difieren.
  </action>
  <verify>
    <automated>npx playwright test e2e/firestore-rules-contract.spec.ts --reporter=list 2>&amp;1 | tail -10</automated>
    <fails_when>salida distinta de cero, o cualquier línea con `failed` o con un `✘` en las últimas 10 líneas, o `no tests found`</fails_when>
    <automated>git check-ignore -q .env.example; echo "ignored=$?"</automated>
    <fails_when>la línea impresa dice `ignored=0`, que significaría que `.env.example` está excluido del repo y el ejemplo no llegaría a nadie</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'NUXT_PUBLIC_FIREBASE_' .env.example` devuelve exactamente 4
    - `grep -Ec '^NUXT_PUBLIC_FIREBASE_[A-Z_]+=.+' .env.example` devuelve 0 — ninguna variable del ejemplo trae valor
    - `git ls-files -- .env.example` devuelve una línea tras el commit, y `git ls-files -- .env` no devuelve ninguna
    - El gate falla de verdad si se manipula: quitar un campo de la lista de `hasOnly()` en `firestore.rules` pone el test en rojo (comprobarlo ejecutando la mutación, y revertirla acto seguido; dejar constancia en el `10-02-SUMMARY.md` de que se ejecutó)
    - El gate falla también si el recorte no encuentra ninguna clave, con un mensaje que nombra el cambio de formato
  </acceptance_criteria>
  <done>
    `.env.example` documenta las cuatro variables sin valor, y el gate de contrato compara de verdad
    las dos listas y se ha visto ponerse rojo ante una mutación real.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Revisión humana de las reglas REALMENTE DESPLEGADAS, antes de la primera escritura real</name>
  <precondition>Existe un proyecto de Firebase con Cloud Firestore creado y `npx firebase-tools login` completado en la máquina del usuario (pasos 1 y 3 de `user_setup`)</precondition>
  <read_first>
    - `firestore.rules` — el fichero committeado, que es el objeto de la revisión
    - `.planning/ROADMAP.md` §Phase 10 «Verificación humana» — la verificación (b), textualmente
    - `.planning/phases/10-respaldo-en-firestore/10-CONTEXT.md` §D-11, §D-14 y §D-15
    - `.planning/phases/10-respaldo-en-firestore/10-RESEARCH.md` §Code Examples — la limitación documentada sobre `players[]`, que la revisión debe aceptar conscientemente y no descubrir después
  </read_first>
  <action>
    Presentar al usuario el guion de abajo y ESPERAR su veredicto. Antes de presentarlo, sustituir
    en `.firebaserc` el marcador de posición por el id real del proyecto si la Task 1 lo dejó
    pendiente.

    Regla dura de ordenación, que es la razón de ser de este checkpoint: NINGUNA variable
    `NUXT_PUBLIC_FIREBASE_*` puede rellenarse en ningún sitio —ni en un `.env` local, ni en Vercel—
    hasta que este checkpoint esté aprobado. Mientras no lo esté, la guarda de D-13 mantiene la
    sincronización en no-op y no puede ocurrir ninguna escritura real. Recoger el veredicto por
    bloque y, si un bloque falla, registrarlo textualmente como hueco en vez de reinterpretarlo.
  </action>
  <acceptance_criteria>
    - El usuario responde explícitamente sobre los bloques A, B y C
    - Queda registrado en `10-02-SUMMARY.md` qué texto de reglas estaba desplegado en la consola en el momento de la revisión y si coincidía carácter a carácter con el fichero committeado
    - Queda registrado que el usuario acepta por escrito la limitación de validación de `players[]`
    - `.firebaserc` contiene el id real del proyecto, no un marcador de posición
    - Queda registrado que ninguna variable `NUXT_PUBLIC_FIREBASE_*` estaba rellenada antes de la aprobación
  </acceptance_criteria>
  <what-built>
    El fichero `firestore.rules` está committeado en la raíz del repo, con reglas que permiten
    únicamente crear documentos en `history/{id}` con forma validada: catorce claves exactas, tipo
    por campo, tope de 8 jugadores, `uid` que debe coincidir con el usuario autenticado y
    `createdAt` que debe ser la hora del servidor. Leer, actualizar y borrar están denegados de
    forma explícita. Un gate automatizado comprueba que la lista de campos de las reglas y la de la
    proyección del cliente no puedan divergir. Nada ha escrito todavía en el proyecto real: sin las
    variables de entorno, la app es un no-op.
  </what-built>
  <how-to-verify>
    **A — Desplegar y comparar (lo que el ROADMAP pide literalmente)**

    1. `npx firebase-tools login` y después `npx firebase-tools deploy --only firestore:rules`
       desde la raíz del repo.
    2. Abrir la consola de Firebase → Firestore Database → pestaña **Reglas**.
    3. Comparar el texto que muestra la consola con `firestore.rules` del repo. Deben ser el mismo
       texto. Si difieren en algo —aunque sea una línea— el despliegue no salió del fichero
       committeado y eso es exactamente el fallo que esta revisión existe para detectar.

    **B — Leer las reglas desplegadas y confirmar que dicen lo que se cree**

    Sobre el texto que muestra la CONSOLA, no sobre el del repo, confirmar:

    1. `allow read` está en `false`. Nadie puede leer las partidas del grupo desde la app ni desde
       un cliente ajeno. La lectura la haces tú desde la consola, que se salta las reglas por ser
       propietario del proyecto.
    2. `allow update` y `allow delete` están en `false`. Borrar una partida en el dispositivo NO la
       borra del respaldo, y eso es deliberado: un respaldo que se borra solo cuando borras el
       original no es un respaldo.
    3. La lista de `hasOnly()` tiene exactamente catorce nombres y ninguno de ellos es un campo que
       no quieras que salga del dispositivo.
    4. Existe la línea que exige que `createdAt` sea la hora del servidor.
    5. No hay ningún otro bloque `match` aparte de `history/{docId}`.

    **C — Aceptar por escrito lo que estas reglas NO cubren**

    El lenguaje de reglas de Firestore no puede recorrer la lista `players` para validar la forma de
    cada objeto. Un documento con `players: [{ basura: 'x' }]` pasaría la regla mientras respete el
    tope de 8 elementos. Cerrarlo del todo exigiría una condición por índice hasta el tope, ilegible
    y frágil. El peor caso es basura parcialmente inválida en un campo anidado de un documento que
    ninguna pantalla lee jamás. Confirmar que se acepta así, a sabiendas.

    Confirmar además, en Authentication → Sign-in method, que el proveedor **Anónimo** está
    habilitado (SYNC-06). Si no lo está, la sincronización cae en el mismo camino que cualquier otro
    fallo: no-op silencioso y entradas que siguen pendientes.
  </how-to-verify>
  <resume-signal>Escribe «aprobado» si A, B y C están conformes, o describe qué bloque falla y con qué texto exacto.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente anónimo cualquiera → colección `history/` | Cualquiera con la configuración del bundle público puede autenticarse anónimamente e intentar escribir. Las reglas son la única barrera. |
| fichero committeado → reglas evaluadas por Google | Si el despliegue no sale del fichero del repo, lo que protege el proyecto no es lo que nadie ha revisado. |

## STRIDE Threat Register

ASVS L1, bloqueo en `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-06 | Information Disclosure | regla `read` de `history/{docId}` | high | mitigate | `allow read: if false` explícito. Ninguna pantalla lee de la nube (STAT-04 cerró las estadísticas contra `localStorage`), así que la denegación no rompe nada. `PITFALLS.md:104` queda superado y su reintroducción está prohibida por escrito. |
| T-10-07 | Tampering | reglas `update`/`delete` de `history/{docId}` | high | mitigate | `allow update, delete: if false` explícito: un hostil puede añadir basura, nunca corromper ni borrar las partidas reales del grupo. |
| T-10-08 | Tampering | regla `create`, documento malformado o sobredimensionado | high | mitigate | `hasOnly()` sobre catorce claves + `hasAll()` sobre las diez obligatorias + tipo por campo + `players.size() <= 8` + `recordedAt.size() <= 64`. |
| T-10-09 | Tampering | `players[]` anidado | medium | accept | El lenguaje de reglas no puede validar la forma de cada objeto de una lista. Se acota por tamaño y se acepta explícitamente: el peor caso es basura parcialmente inválida en un campo anidado de un documento write-only que nadie lee. Aceptación firmada en el bloque C del checkpoint de la Task 3. |
| T-10-10 | Denial of Service | cuota del plan gratuito (20.000 escrituras/día) | medium | accept | Aceptado en D-08 y en `PITFALLS.md` §5: el respaldo deja de sincronizar hasta el reinicio diario; `localStorage`, que es la fuente de verdad, no se ve afectado y jugar sigue igual. Si dejara de ser tolerable, la palanca siguiente es App Check, ya registrada como idea diferida. |
| T-10-11 | Repudiation | divergencia entre el fichero committeado y las reglas desplegadas | high | mitigate | D-15: despliegue desde el fichero committeado con `npx firebase-tools deploy --only firestore:rules`, y comparación carácter a carácter contra la consola en el bloque A del checkpoint humano bloqueante. |
</threat_model>

<artifacts_produced>
## Artefactos que este plan produce

**Ficheros nuevos**
- `firestore.rules`
- `firebase.json`
- `.firebaserc`
- `.env.example`
- `e2e/firestore-rules-contract.spec.ts`

**Símbolos exportados nuevos**
- Ninguno. Este plan no añade código de aplicación.

**Claves de configuración nuevas**
- Ninguna en el código. Documenta en `.env.example` las cuatro variables de entorno
  `NUXT_PUBLIC_FIREBASE_API_KEY`, `NUXT_PUBLIC_FIREBASE_AUTH_DOMAIN`,
  `NUXT_PUBLIC_FIREBASE_PROJECT_ID` y `NUXT_PUBLIC_FIREBASE_APP_ID` que el plan 10-01 declaró en
  `runtimeConfig.public`.

**Scripts nuevos de npm**
- Ninguno. `firebase-tools` se invoca con `npx` y nunca se instala (D-15).
</artifacts_produced>

<verification>
- `npx playwright test e2e/firestore-rules-contract.spec.ts --reporter=list` en verde.
- `grep -c 'firebase-tools' package.json` devuelve 0.
- `git ls-files -- .env` no devuelve nada; `git ls-files -- .env.example` devuelve una línea.
- El veredicto humano de los bloques A, B y C queda escrito en `10-02-SUMMARY.md`.
</verification>

<success_criteria>
- Las reglas desplegadas coinciden carácter a carácter con el fichero committeado, comprobado por
  una persona mirando la consola.
- Las reglas permiten crear y nada más, con forma validada y topes de tamaño.
- El gate de contrato se ha visto ponerse rojo ante una mutación real de la lista de campos.
- Ninguna variable `NUXT_PUBLIC_FIREBASE_*` se rellenó antes de la aprobación del checkpoint.
</success_criteria>

<output>
Crear `.planning/phases/10-respaldo-en-firestore/10-02-SUMMARY.md` al terminar.
</output>
