---
phase: 03.1-voz-pregenerada-en-espa-ol-con-gemini-tts
plan: 06
type: execute
wave: 5
depends_on: [03.1-05]
files_modified:
  - public/voice-probe.html
  - public/audio/_probe/
autonomous: false
requirements: [VOZ-07, VOZ-08]

must_haves:
  truths:
    - "En la tablet real, la locución que suena en cada paso es el clip de Rasalgethi y el control de silencio la calla y la reactiva (criterio 1)"
    - "Con la red caída antes de empezar, la app habla con la voz del sistema, sin banda ni aviso, y SIGUIENTE nunca se bloquea (criterio 2)"
    - "Con la red caída DESPUÉS de empezar, la locución sigue siendo la pregenerada durante toda la partida (criterio 3)"
    - "El despliegue no contiene ni la clave, ni el manifiesto, ni los artefactos de prueba de la fase"
  artifacts:
    - path: "public/voice-probe.html"
      provides: "ELIMINADO — artefacto temporal del checkpoint D-02"
    - path: "public/audio/_probe/"
      provides: "ELIMINADO — clips de prueba de estilo"
  key_links:
    - from: ".output/public/audio"
      to: "las 37 frases speech"
      via: "nuxt generate copia public/ tal cual"
      pattern: "\\.m4a"
---

<objective>
Limpiar los artefactos temporales de la fase, comprobar que el build de despliegue es correcto y
no filtra nada, y someter los cuatro criterios de éxito del ROADMAP a una prueba humana
bloqueante en la tablet real.

Purpose: la Fase 3 se aprobó con este mismo patrón (plan 03-05) y fue precisamente la escucha
humana la que detectó que la voz del sistema era inaceptable — el motivo de que exista esta fase.
El respaldo silencioso de D-07 es tan bueno enmascarando fallos que solo una escucha real
distingue "suena Rasalgethi" de "suena el respaldo y nadie se ha dado cuenta".

Output: repo limpio, build verificado y veredicto humano sobre los 4 criterios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-CONTEXT.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-01-SUMMARY.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-05-SUMMARY.md

<interfaces>
<!-- Datos concretos que la verificación necesita. -->

Criterios de éxito de la Fase 03.1 (ROADMAP.md):
1. Al llegar a cada paso, la locución que suena es el audio pregenerado con la voz Rasalgethi,
   no la síntesis del dispositivo, y el control de silencio de la Fase 3 la calla y la reactiva
   igual que antes.
2. Si un audio falta, no carga o la red se cae, la app recurre a `speechSynthesis` en silencio
   —sin banda ni aviso— y el botón SIGUIENTE nunca se bloquea.
3. Los 37 audios quedan disponibles en local al empezar la partida, de modo que una caída de
   wifi a mitad de partida no degrada la locución al respaldo.
4. Si alguien cambia una frase `speech` sin regenerar su audio, CI falla y nombra los ids
   afectados y el comando exacto para regenerarlos.  (Ya cubierto por CI en el plan 03.1-03.)

Despliegue: Vercel, auto-deploy al hacer push a `main`.
URL de producción: https://tabletop-assistant.vercel.app/
Artefactos temporales a borrar: `public/voice-probe.html` y `public/audio/_probe/` (creados en
el plan 03.1-01 para el checkpoint de estilo D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Borrar los artefactos de prueba y verificar el build de despliegue</name>
  <files>public/voice-probe.html, public/audio/_probe/</files>
  <read_first>
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-01-SUMMARY.md` — qué artefactos temporales se crearon y con qué nombres
    - `.gitignore` — bloque de secretos, para confirmar qué NO debe aparecer nunca en la salida
    - `nuxt.config.ts` — `nitro.routeRules`, incluida la regla de `/audio/**` añadida en el plan 03.1-04
  </read_first>
  <action>
1. Borrar `public/voice-probe.html` y el directorio `public/audio/_probe/` completo (con sus 3
   clips). Eran artefactos de un solo uso del checkpoint D-02 y no deben quedar desplegados.
2. Ejecutar `npm run test` y `npx nuxt generate` y comprobar sobre `.output/public`:
   - hay exactamente 37 ficheros `.m4a` en `.output/public/audio` y ningún subdirectorio `_probe`
   - no existe `.output/public/voice-probe.html`
   - no aparece por ninguna parte de `.output/` el fichero `manifest.json` de `scripts/voice/`
     (vive fuera de `public/` a propósito y no tiene consumidor en el cliente)
   - no aparece la cadena `GEMINI_API_KEY` ni el valor de la clave en ningún fichero de la salida
3. Ejecutar una build con el preset de Vercel (`NITRO_PRESET=vercel npx nuxt build`) y comprobar
   que `.vercel/output/config.json` contiene la regla de cabeceras de `/audio/**` junto a las ya
   existentes — es la comprobación que CLAUDE.md exige para cualquier cambio de cabeceras.
4. Hacer push a `main` para que Vercel despliegue la versión que el usuario va a probar, y
   confirmar que `https://tabletop-assistant.vercel.app/audio/setup.heroes.01.m4a` responde 200
   y que `https://tabletop-assistant.vercel.app/voice-probe.html` responde 404.
  </action>
  <verify>
    <automated>npm run test && npx nuxt generate && ls .output/public/audio/*.m4a | wc -l | tr -d ' ' | grep -x 37</automated>
  </verify>
  <acceptance_criteria>
    - `test -e public/voice-probe.html` sale distinto de 0 y `test -d public/audio/_probe` sale distinto de 0
    - `ls .output/public/audio/*.m4a | wc -l` devuelve 37 y `test -d .output/public/audio/_probe` sale distinto de 0
    - `grep -rl "GEMINI_API_KEY" .output/ | wc -l` devuelve 0
    - `find .output -name 'manifest.json' -path '*voice*' | wc -l` devuelve 0
    - `NITRO_PRESET=vercel npx nuxt build` sale 0 y `grep -c "/audio/" .vercel/output/config.json` devuelve 1 o más
    - `curl -s -o /dev/null -w "%{http_code}" https://tabletop-assistant.vercel.app/audio/setup.heroes.01.m4a` devuelve 200
    - `curl -s -o /dev/null -w "%{http_code}" https://tabletop-assistant.vercel.app/voice-probe.html` devuelve 404
    - `npm run test` sale 0
  </acceptance_criteria>
  <done>El repo no arrastra artefactos de prueba, el despliegue sirve los 37 clips con las cabeceras correctas, y no publica ni la clave ni el manifiesto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Prueba humana bloqueante en la tablet real — los 4 criterios del ROADMAP</name>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 03.1 — los 4 criterios de éxito, textualmente
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-05-SUMMARY.md` — qué se cableó exactamente y qué comprobaciones manuales ya se hicieron en escritorio
    - `.planning/phases/03-locuci-n-por-voz-y-pantalla-siempre-encendida/03-05-PLAN.md` — el patrón de prueba humana bloqueante que aprobó la Fase 3
  </read_first>
  <action>Confirmar primero que el despliegue de producción ya sirve la versión de esta fase (Task 1 hizo push y comprobó el 200 del clip y el 404 de la página de prueba). Presentar entonces el guion de verificación de abajo, bloque a bloque, y ESPERAR el veredicto del usuario. No dar la fase por verificada con una aprobación parcial: si un bloque falla, recogerlo textualmente como hueco para el cierre de fase en vez de reinterpretarlo.</action>
  <acceptance_criteria>
    - El usuario responde explícitamente sobre los bloques A, B y C
    - Queda registrado qué voz sonó en cada bloque (pregenerada frente a voz del sistema) y si apareció alguna banda o aviso
    - Queda registrado si el botón SIGUIENTE respondió con normalidad en los tres bloques
    - El veredicto por bloque queda escrito en `03.1-06-SUMMARY.md`
  </acceptance_criteria>
  <what-built>
    Las 37 frases locutables ahora suenan con la voz Rasalgethi generada por Gemini TTS y servida
    como audio estático desde el repo. Los clips se descargan en segundo plano al empezar la
    partida, así que una caída de wifi a mitad de partida ya no degrada la locución. Si un clip
    falta o no carga, habla la voz del sistema de la Fase 3 sin decir nada ni bloquear el flujo.
    Un test de CI impide mergear un audio que ya no corresponde a su frase.
  </what-built>
  <how-to-verify>
    Todo en la TABLET que se usa en la mesa, sobre `https://tabletop-assistant.vercel.app/`
    (hacer un refresco fuerte la primera vez para descartar una versión anterior en caché).

    **A — Criterio 1: suena Rasalgethi y el silencio funciona**
    1. Con wifi, elige Marvel Champions, indica jugadores y dificultad y entra en la partida.
    2. Pulsa SIGUIENTE varias veces. ¿La voz que suena es la MISMA que escuchaste en la prueba
       de estilo, o vuelve a ser la voz del sistema (la que te pareció inaceptable)?
    3. Toca el control de silencio de la cabecera a mitad de una frase: tiene que callarse al
       instante. Vuelve a activarlo y pulsa SIGUIENTE: tiene que volver a hablar.
    4. Recarga la página y comprueba que sigue silenciada/activada según cómo la dejaste.

    **B — Criterio 3: la wifi se cae A MITAD de partida**
    5. Sigue en la misma partida. Activa el MODO AVIÓN en la tablet.
    6. Pulsa SIGUIENTE cinco o seis veces más. ¿Sigue sonando la misma voz buena? (Debe seguir:
       los clips ya están descargados.) ¿En algún momento se bloqueó el botón?

    **C — Criterio 2: sin red DESDE EL PRINCIPIO (el respaldo)**
    7. Sigue en modo avión. Cierra la pestaña, borra los datos del sitio desde el navegador
       (Ajustes → datos de sitios) para vaciar la caché de audio, y vuelve a abrir la app.
    8. Empieza una partida nueva y pulsa SIGUIENTE varias veces. Aquí SÍ es correcto que suene
       la voz fea del sistema (o que no suene nada si el dispositivo no la tiene).
       Lo que hay que comprobar es que:
       - no aparece NINGUNA banda, aviso ni mensaje de error,
       - el botón SIGUIENTE responde con normalidad y el texto se lee igual,
       - la app no se queda colgada esperando nada.
    9. Desactiva el modo avión, empieza otra partida y confirma que la voz buena vuelve.

    **D — Criterio 4** ya está verificado por CI (el test falla nombrando ids y comando); no
    requiere nada por tu parte.

    **E — Dato pendiente desde la Fase 1**
    10. Si no quedó registrado en la prueba de estilo: modelo y versión de sistema operativo de
        la tablet (Ajustes → Información del dispositivo).
  </how-to-verify>
  <done>El usuario ha dado su veredicto sobre los bloques A, B y C, y ese veredicto queda registrado literalmente en el SUMMARY.</done>
  <resume-signal>Escribe "aprobado" si A, B y C se comportan como se describe. Si algo falla, di en qué punto numerado y qué pasó exactamente (qué voz sonó, si apareció alguna banda, si el botón dejó de responder). Añade el modelo/SO de la tablet si aún no se había registrado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → despliegue público en Vercel | Todo lo commiteado bajo `public/` queda accesible en internet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-20 | Information Disclosure | Salida de build (`.output/public`) | mitigate | Comprobado con grep que no contiene `GEMINI_API_KEY` ni el manifiesto de huellas; los únicos añadidos son los 37 `.m4a` con frases ya públicas |
| T-03.1-21 | Information Disclosure | Artefactos de prueba desplegados (`voice-probe.html`, `_probe/`) | mitigate | Se borran en la Task 1 y se verifica con un 404 real contra la URL de producción |
| T-03.1-22 | Tampering | Cabeceras de `/audio/**` perdidas al cambiar de preset de despliegue | mitigate | Se verifica sobre `.vercel/output/config.json` generado, no sobre el fuente — la comprobación que CLAUDE.md §Hosting exige |
| T-03.1-SC | Tampering | npm installs | mitigate | Cero instalaciones en toda la fase. `package.json` solo ganó dos entradas de `scripts` en el plan 03.1-01 |
</threat_model>

<verification>
- `npm run test` sale 0
- `npx nuxt generate` produce 37 `.m4a` y ningún artefacto de prueba
- La URL de producción sirve los clips (200) y ya no sirve la página de prueba (404)
- El usuario aprueba explícitamente los bloques A, B y C
</verification>

<success_criteria>
- Los 4 criterios de éxito de la Fase 03.1 del ROADMAP quedan verificados: 1, 2 y 3 por el
  usuario en la tablet real; 4 por CI
- El repo y el despliegue quedan sin artefactos temporales ni filtraciones
</success_criteria>

<output>
Create `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-06-SUMMARY.md` when done.
Registrar en el SUMMARY el veredicto del usuario por bloque (A/B/C), cualquier corrección
pedida, y el modelo/SO de la tablet si por fin queda registrado (cierra un bloqueante abierto
desde la Fase 1).
</output>
