---
phase: 03.1-voz-pregenerada-en-espa-ol-con-gemini-tts
verified: 2026-08-31T15:33:31Z
status: human_needed
score: 4/4 roadmap success criteria technically implemented and automation-verified; 2 of 4 still lack real-device confirmation on the target hardware (tablet)
overrides_applied: 0
re_verification: null
human_verification:
  - test: "Modelo y versión de Android de la tablet de mesa"
    expected: "Un modelo y versión de SO concretos, registrados en STATE.md/CONTEXT, para poder afirmar en el futuro qué se ha probado realmente"
    why_human: "Bloqueante abierto desde la Fase 1, preguntado explícitamente dos veces, nunca respondido; ningún grep puede producir este dato"
  - test: "Ejecutar el guion completo de 03.1-06-PLAN.md en la TABLET real de mesa (no un móvil): criterios 1, 2 y 3 del ROADMAP"
    expected: "La locución suena con Rasalgethi, el control de silencio la calla/reactiva y esa reactivación sobrevive a un recargo; sin red desde el arranque el sistema toma el relevo en silencio sin colgar SIGUIENTE; con red caída a mitad de partida el audio sigue viniendo del clip pregenerado"
    why_human: "Toda la evidencia actual es de un móvil Android, no de la tablet objetivo; el propio plan (03.1-06-PLAN.md must_haves) exige 'en la tablet real' explícitamente"
  - test: "Control de silencio: cortar un clip pregenerado a mitad de frase pulsando el mute, reactivar, pulsar SIGUIENTE y confirmar que vuelve a sonar; recargar la página y confirmar que el estado de silencio persiste"
    expected: "El clip se corta al instante al silenciar, el siguiente SIGUIENTE con la voz reactivada suena con normalidad, y tras recargar la preferencia de silencio sigue siendo la misma"
    why_human: "El guion de 03.1-06-PLAN.md lo pedía explícitamente (bloque A, pasos 3-4); el usuario no reportó sobre ello — sigue sin ejercitarse con audio pregenerado real, solo cubierto por unit tests puros (resolveVoiceState/resolveEffectiveAvailability)"
  - test: "Camino de respaldo D-07 desde el arranque: borrar datos del sitio, activar modo avión, empezar partida nueva y confirmar que habla la voz del sistema sin banda, sin error y sin colgar SIGUIENTE"
    expected: "La app narra con speechSynthesis desde el primer paso, sin ningún aviso visible ni consola de error, y el botón SIGUIENTE permanece operativo en todo momento"
    why_human: "Nunca ejecutado en ningún dispositivo; solo cubierto por `useVoiceAnnouncer.test.ts` (resolveEffectiveAvailability, D-07/D-08) y por revisión de código — mismo nivel de evidencia que VOZ-05/06 en la Fase 3, pero explícitamente pedido de nuevo por 03.1-06-PLAN.md para esta fase concreta"
---

# Phase 03.1: Voz pregenerada en español con Gemini TTS — Verification Report

**Phase Goal (ROADMAP.md):** El grupo escucha cada paso con una voz natural en español, idéntica en cualquier dispositivo, porque las 37 frases `speech` se generan una sola vez con Gemini TTS y se sirven como audio estático versionado en el repo; la voz del sistema construida en la Fase 3 queda intacta como respaldo silencioso, y un gate de CI hace imposible mergear un audio que ya no corresponde a su frase.

**Verified:** 2026-08-31T15:33:31Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | La locución que suena en cada paso es el audio pregenerado (Rasalgethi), no la síntesis del dispositivo; el control de silencio de la Fase 3 la calla y reactiva igual que antes | ⚠️ PARTIAL — human_needed | Código: `useVoiceAnnouncer.ts` inserta el clip pregenerado como fuente primaria (`getObjectUrl`/`staticUrlFor`) y reutiliza sin cambios el `resolveVoiceState` de la Fase 3 para la preferencia de silencio (verificado por lectura, `app/composables/useVoiceAnnouncer.ts:259-337`). Humano: el usuario confirmó en Android real que la regresión `audio-corta-y-reinicia` (corte a ~1s + reinicio con voz del sistema) **no se reproduce** avanzando rápido — señal indirecta de estabilidad de la fuente pregenerada — pero **no ejerció el control de silencio** con audio pregenerado sonando (mute a mitad de frase, reactivar, recargar). Ver `03.1-06-SUMMARY.md` "Huecos explícitos" #3. |
| 2 | Si un audio falta, no carga o la red se cae, la app recurre a `speechSynthesis` en silencio (sin banda, sin aviso) y SIGUIENTE nunca se bloquea | ⚠️ PARTIAL — human_needed | Código + unit tests: `resolveEffectiveAvailability`, `composeAudioFallback` en `app/composables/useVoiceAnnouncer.ts`, cubiertos por 11 tests en `app/composables/__tests__/useVoiceAnnouncer.test.ts` (verificado: `describe('resolveEffectiveAvailability (plan 03.1-05, D-07/D-08...')`). `grep -c "console\."` en la capa de voz/precarga = 0, consistente con "sin aviso". **El camino "sin red desde el arranque" nunca se probó en un dispositivo real** — solo unitario/código. |
| 3 | Los 37 audios quedan disponibles en local al empezar la partida; una caída de wifi a mitad de partida no degrada la locución al respaldo | ✓ VERIFIED | Código: `usePreloadedAudio.ts` — `prefetchAll` llamado desde `app/pages/[game]/index.vue:51` al confirmar el arranque de partida; Cache Storage (`voice-audio-v1`) como respaldo de lectura. Datos: `ls public/audio/*.m4a \| wc -l` → **37** (27 `setup.*` + 10 `ronda.*`, ningún fichero de 0 bytes — verificado con `find ... -size 0` → vacío). Humano: confirmado en Android real contra producción, cita verbatim: *"tras poner el modo avion se sigue pudiendo avanzar y retroceder y suenan los audios"*. |
| 4 | Si alguien cambia una frase `speech` sin regenerar su audio, CI falla y nombra los ids afectados y el comando exacto para regenerarlos | ✓ VERIFIED (re-probado por este verificador, no solo por el SUMMARY) | Mutación real de `setup.heroes.01` en `content/marvel-champions.json` (`+ " MUTADO PARA VERIFICACION."`), `npm run test` falló con: `Error: Audio desactualizado para: setup.heroes.01` / `Ejecuta: npm run voice:generate -- setup.heroes.01`. Revertido con `git checkout -- content/marvel-champions.json`; `npm run test` volvió a 293/293 en verde; `git status --porcelain content/` vacío tras revertir. |

**Score:** 2/4 criterios cerrados sin reservas por evidencia de dispositivo + automatización; 2/4 técnicamente implementados y probados a nivel de código/unitario pero pendientes de confirmación humana en el dispositivo objetivo.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `public/audio/*.m4a` (37 clips) | 37 clips de producción, ninguno vacío | ✓ VERIFIED | `ls public/audio/*.m4a \| wc -l` → 37 (10 `ronda.*` + 27 `setup.*`); `find public/audio -maxdepth 1 -name '*.m4a' -size 0` → sin resultados |
| `scripts/voice/manifest.json` | 37 entradas, huellas al día, voz/estilo declarados | ✓ VERIFIED | `grep -c '"hash"'` → 37; `manifest.voice` = `Rasalgethi`, `manifest.model` = `gemini-2.5-flash-preview-tts`, `manifest.style` = `plano-agil` |
| `engine/audio.ts` (`collectAudioIds`/`collectSpeechEntries`) | Única fuente TS del catálogo de 37 ids | ✓ VERIFIED | Comparación programática: `collectAudioIds(content)` (37 ids) == claves de `manifest.entries` (37 ids) en **ambas direcciones** — cero diferencias en cualquier sentido (verificado con script Node ejecutado por este verificador, no citado del SUMMARY) |
| `engine/__tests__/voice-drift.test.ts` | Gate de deriva D-04/D-05/D-11, 8 tests | ✓ VERIFIED | 8 tests presentes y en verde en `npm run test`; gate probado a morder de verdad (ver Truth #4) |
| `app/composables/usePreloadedAudio.ts` | Capa de precarga y Cache Storage, D-05/D-09; no debe eliminarse (dos capas deliberadamente solapadas con el SW de Fase 4) | ✓ VERIFIED — presente y wired | Existe, exporta `prefetchAll`/`getObjectUrl`/`staticUrlFor`/`audioAvailable`; importado y usado en `app/pages/[game]/index.vue:21,51` y en `app/composables/useVoiceAnnouncer.ts:19,265` |
| `app/composables/useVoiceAnnouncer.ts` | Costura de reproducción: audio pregenerado primario, `speechSynthesis` como respaldo | ✓ VERIFIED | `spokenLine`, `stopAudio`, `composeAudioFallback`, `scheduleAudioWatchdog`, `hasAudioStarted` presentes y ejercitados por 11 tests puros en `useVoiceAnnouncer.test.ts` |
| `scripts/voice/generate.mjs` + `fingerprint.mjs` + `styles.mjs` | Cadena de generación local, NO wired a build/CI | ✓ VERIFIED | `npm run voice:generate` = `node scripts/voice/generate.mjs`, script independiente; **cero** apariciones de `voice:generate`/`generate.mjs` en `.github/workflows/`; no aparece en `build`, `generate` ni `postinstall` de `package.json` |
| `public/voice-probe.html`, `public/audio/_probe/` | Artefactos de prueba de estilo — deben estar BORRADOS | ✓ VERIFIED — ausentes | `ls public/voice-probe.html` → No such file; `ls public/audio/_probe` → No such file; `find public -iname '*probe*'` → sin resultados |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `app/pages/[game]/index.vue` (arranque de partida) | `usePreloadedAudio().prefetchAll()` | llamada directa en `onConfirm`/arranque | ✓ WIRED | `grep` confirma import (línea 21) y llamada (línea 51) |
| `useVoiceAnnouncer.ts` (`spokenLine`) | `usePreloadedAudio().getObjectUrl()`/`staticUrlFor()` | lectura síncrona dentro del gesto de usuario | ✓ WIRED | Import línea 19, uso línea 265, comentarios explícitos sobre por qué es síncrono (Pitfall 1 de 03.1-RESEARCH.md) |
| `content/marvel-champions.json` (frase `speech`) | `scripts/voice/manifest.json` (huella) | `engine/__tests__/voice-drift.test.ts` (gate D-04) | ✓ WIRED — probado mordiendo de verdad | Ver Truth #4; mensaje de error nombra id + comando exacto |
| CI (`.github/workflows/*.yml`) | `npm run test` (incluye el gate de deriva) | `run: npm run test` en `ubuntu-latest` | ✓ WIRED | El job de test no invoca `voice:generate`, no necesita clave API ni herramientas del sistema — confirmado por lectura del workflow |
| `nuxt.config.ts` `routeRules['/audio/**']` | cabeceras de caché de los clips | `headers: { 'cache-control': ... }` | ✓ WIRED | Presente en `nuxt.config.ts:47-49`; consistente con D-09 (descarga en segundo plano al empezar partida, sin depender de caché larga del host) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| VOZ-07 | 03.1-02/03/04/05/06 | La locución suena con voz natural en español en cualquier dispositivo, sin depender de voces de síntesis instaladas | ? NEEDS HUMAN (REQUIREMENTS.md: Pending) | Implementación técnica completa y automáticamente verificada (37/37 clips, wiring correcto, gate de deriva). El propio equipo decidió explícitamente no cerrarlo hasta verificación en la **tablet real** (`03.1-04-SUMMARY.md`, `03.1-05-SUMMARY.md`, `03.1-06-SUMMARY.md`), y esa condición literal sigue sin cumplirse: la prueba humana se hizo en un móvil Android, no en la tablet de mesa, cuyo modelo/SO sigue sin registrarse desde la Fase 1. Este verificador mantiene el estado Pending — ver juicio razonado más abajo. |
| VOZ-08 | 03.1-05/06 | Si la locución grabada no está disponible, la app recurre a la voz del sistema sin avisar y sin interrumpir el flujo | ? NEEDS HUMAN (REQUIREMENTS.md: Pending) | Lógica de respaldo silencioso implementada y cubierta por unit tests (`resolveEffectiveAvailability`, `composeAudioFallback`); el camino "sin red desde el arranque" (el escenario más exigente de D-07) nunca se ejercitó en un dispositivo real, solo el escenario "red cae a mitad de partida" (que es más bien evidencia de VOZ-07/criterio 3, no del camino de respaldo puro). Mismo razonamiento que VOZ-07: se mantiene Pending. |

**Juicio razonado sobre el listón de VOZ-07/VOZ-08 (pedido explícitamente en el contexto de esta verificación):**
El propio `03.1-06-PLAN.md` fijó como *must_have* la frase "En la tablet real..." para el criterio 1, y el origen entero de esta fase fue una prueba humana en dispositivo real (Fase 3) que detectó que la voz del sistema era inaceptable — es decir, el proyecto ya tiene precedente de que la escucha en el dispositivo real importa más que la revisión de código para esta capa concreta. La evidencia de esta sesión es genuina y valiosa (Android real, producción, con caída de red real), pero es un dispositivo distinto al de mesa, y dos de los cuatro sub-guiones pedidos (mute con audio real, D-07 desde cero) no se ejecutaron en ningún dispositivo. Dado que el bloqueante "modelo/SO de la tablet" lleva abierto desde la Fase 1 sin respuesta pese a preguntarse dos veces, **este verificador coincide con el criterio ya aplicado por el equipo**: no elevar VOZ-07/VOZ-08 a completado. Se registran como `human_needed`, no como fallo — la implementación no tiene ninguna señal de estar rota; falta la confirmación en el hardware de destino.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/composables/useVoiceAnnouncer.ts` | 441 | Comentario con la palabra "TODO" | ℹ️ Info (falso positivo) | Es la palabra española "todo" ("TODO síncrono hasta la llamada"), no un marcador de deuda técnica en inglés. No requiere seguimiento. |
| `app/pages/[game]/index.vue` | 96 | "Context placeholder" en comentario | ℹ️ Info (falso positivo, código preexistente de Fase 1) | Describe un objeto de contexto estructural temporal reemplazado por `resume()` antes de renderizar; no es código de esta fase ni un stub visible al usuario. |

Sin marcadores `TBD`/`FIXME`/`XXX` sin referenciar en ningún fichero clave de la fase (`useVoiceAnnouncer.ts`, `usePreloadedAudio.ts`, `engine/audio.ts`, `voice-drift.test.ts`, `scripts/voice/generate.mjs`, `scripts/voice/manifest.json`). No hay debt marker gate que activar.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Suite completa en verde | `npm run test` | `Test Files 14 passed (14)`, `Tests 293 passed (293)` | ✓ PASS |
| Manifest ⇔ catálogo del motor coinciden exactamente | Script Node ad hoc (`collectAudioIds` vs `Object.keys(manifest.entries)`) | `only in engine ids: []`, `only in manifest: []`, ambos 37 | ✓ PASS |
| Gate de deriva muerde de verdad | Mutación real de `content/marvel-champions.json` + `npm run test` | `Error: Audio desactualizado para: setup.heroes.01` / `Ejecuta: npm run voice:generate -- setup.heroes.01`; 1 test falla, 292 pasan | ✓ PASS |
| Árbol limpio tras revertir | `git checkout -- content/marvel-champions.json` + `git status --porcelain content/` + `npm run test` | Salida vacía de `git status`; `293 passed (293)` | ✓ PASS |
| Artefactos de prueba de estilo ausentes | `ls public/voice-probe.html`, `ls public/audio/_probe`, `find public -iname '*probe*'` | Ambos "No such file", find vacío | ✓ PASS |
| `voice:generate` no wired a build/CI | `grep` en `.github/workflows/`, `package.json` scripts `build`/`generate`/`postinstall` | Cero coincidencias fuera del propio script `voice:generate`/`voice:probe` | ✓ PASS |
| Clave API no filtrada | `grep -rn GEMINI_API_KEY` fuera de `node_modules`; `git ls-files \| grep '\.env$'` | Solo referencias al nombre de la variable en `scripts/voice/generate.mjs`; ningún `.env` versionado | ✓ PASS |
| Ficheros de audio sin bytes cero | `find public/audio -maxdepth 1 -name '*.m4a' -size 0` | Sin resultados | ✓ PASS |

### Probe Execution

No aplica — esta fase no define scripts `scripts/*/tests/probe-*.sh`; su verificación runtime es el gate de Vitest (`voice-drift.test.ts`), ya ejecutado y probado a morder arriba.

### Human Verification Required

Ver la sección `human_verification` en el frontmatter (4 ítems). Resumen:

1. **Modelo/SO de la tablet de mesa** — bloqueante heredado desde la Fase 1, nunca respondido pese a preguntarse dos veces.
2. **Guion completo de 03.1-06 en la tablet real** (no un móvil) — criterios 1, 2 y 3 del ROADMAP.
3. **Control de silencio con audio pregenerado real** — cortar a mitad de frase, reactivar, confirmar que el estado sobrevive a un recargo.
4. **Camino D-07 sin red desde el arranque** — borrar datos del sitio en modo avión, empezar partida nueva, confirmar respaldo silencioso sin colgar SIGUIENTE.

### Gaps Summary

No hay gaps de **implementación**: todo el código, los 37 activos de audio, el gate de CI y el wiring extremo a extremo existen, son sustantivos (no stubs) y están correctamente conectados — cada afirmación se re-derivó con comandos reales en esta sesión (recuento de ficheros, comparación programática de ids, mutación real del contenido para probar que el gate muerde, greps de ausencia de artefactos/CI/API key), no se aceptó ninguna cifra citada en los SUMMARY sin repetir la comprobación.

Los huecos que existen son exclusivamente de **evidencia en el dispositivo objetivo**, y los propios SUMMARY del equipo ya los documentaron con honestidad en vez de ocultarlos (`03.1-06-SUMMARY.md` "Huecos explícitos"): la prueba humana fue en un móvil Android, no en la tablet de mesa; el control de silencio no se ejercitó con audio real; el camino de respaldo sin red desde el arranque no se probó en ningún dispositivo; y el modelo/SO de la tablet sigue sin registrarse pese a preguntarse dos veces. Este verificador coincide con la decisión del equipo de mantener VOZ-07/VOZ-08 como Pending en `REQUIREMENTS.md` en vez de cerrarlos con evidencia parcial, y no encuentra motivo para relajar ese listón: el origen mismo de esta fase fue una escucha real en dispositivo que detectó un problema que la revisión de código nunca habría visto.

**Nota adicional para el registro:** el bloqueo de cuota que detuvo `03.1-03` en sesión 1 se resolvió activando facturación en el proyecto de Google AI Studio, no esperando el reinicio diario del free tier como se documentó originalmente en `STATE.md`; el primer reintento tras activar facturación aún falló con `429 "prepayment credits are depleted"` porque el saldo no se había propagado. Esto ya está corregido en `03.1-03-SUMMARY.md` y se reafirma aquí para que no se repita la asunción incorrecta en investigación futura.

---

*Verified: 2026-08-31T15:33:31Z*
*Verifier: Claude (gsd-verifier)*
