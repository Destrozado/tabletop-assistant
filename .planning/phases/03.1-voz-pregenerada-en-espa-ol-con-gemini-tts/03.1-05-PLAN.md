---
phase: 03.1-voz-pregenerada-en-espa-ol-con-gemini-tts
plan: 05
type: execute
wave: 4
depends_on: [03.1-02, 03.1-04]
files_modified:
  - app/composables/useGameSession.ts
  - app/composables/useVoiceAnnouncer.ts
  - app/composables/__tests__/useVoiceAnnouncer.test.ts
  - app/pages/[game]/index.vue
autonomous: true
requirements: [VOZ-07, VOZ-08]

must_haves:
  truths:
    - "Al llegar a cada paso suena el clip pregenerado con la voz Rasalgethi, no la síntesis del dispositivo (criterio 1)"
    - "El control de silencio de la Fase 3 calla y reactiva el audio pregenerado exactamente igual que callaba la síntesis (criterio 1)"
    - "Si el audio falta, no carga o la red se cae, habla `speechSynthesis` sin banda, sin aviso y sin bloquear SIGUIENTE (criterio 2, D-07/D-08)"
    - "En un dispositivo SIN voz española, el audio pregenerado suena igualmente y la banda de la Fase 3 no aparece (VOZ-07: no depender de las voces del sistema)"
    - "La decisión audio-o-respaldo es totalmente síncrona dentro del gesto del usuario: ni un solo `await` entre el toque y `.play()` (Pitfall 1)"
    - "Al navegar, la locución en curso se corta antes de empezar la nueva: nunca se encolan ni se solapan las dos fuentes (VOZ-04)"
  artifacts:
    - path: "app/composables/useGameSession.ts"
      provides: "currentAudioId: la costura reactiva que traduce nodo+dificultad a id de audio"
      contains: "currentAudioId"
    - path: "app/composables/useVoiceAnnouncer.ts"
      provides: "announce() con rama de audio pregenerado primero y el cuerpo íntegro de la Fase 3 como respaldo"
      exports: ["useVoiceAnnouncer", "shouldAnnounce", "resolveEffectiveAvailability", "scheduleAudioWatchdog"]
    - path: "app/composables/__tests__/useVoiceAnnouncer.test.ts"
      provides: "Tests de las funciones puras nuevas, sin romper ninguno de los existentes de la Fase 3"
  key_links:
    - from: "app/composables/useGameSession.ts"
      to: "engine/resolve.ts"
      via: "resolveAudioId(currentNode, session.context) dentro de una computed"
      pattern: "resolveAudioId"
    - from: "app/composables/useVoiceAnnouncer.ts"
      to: "app/composables/usePreloadedAudio.ts"
      via: "getObjectUrl(id) síncrono dentro de announce()"
      pattern: "getObjectUrl\\("
    - from: "app/composables/useVoiceAnnouncer.ts"
      to: "HTMLAudioElement"
      via: "audioEl.src = url; audioEl.play().catch(...)"
      pattern: "\\.play\\(\\)"
---

<objective>
Insertar el audio pregenerado como fuente PRIMARIA de locución dentro de `announce()`, dejando
todo el camino de la Fase 3 intacto por debajo como respaldo silencioso.

Purpose: son los criterios 1 y 2 del ROADMAP. La Fase 3 no se deprecia: se reordena de primaria
a secundaria. El riesgo real de este plan no es una API nueva, es dejar colar un `await` entre el
toque y `.play()` — eso silenciaría el audio en iPad y el respaldo lo enmascararía tan bien que
nadie lo notaría hasta la prueba humana.

Output: una computed nueva en la costura del motor, la rama de audio en el anunciador con su
watchdog, y los tests puros que lo respaldan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-CONTEXT.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-RESEARCH.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-PATTERNS.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-04-SUMMARY.md
@app/composables/useVoiceAnnouncer.ts
@app/composables/useGameSession.ts

<interfaces>
<!-- Contratos ya existentes; no explorar el repo para deducirlos. -->

engine/resolve.ts (plan 03.1-02):
  resolveAudioId(node: RuntimeStepNode, context: SessionContext): string | null

app/composables/usePreloadedAudio.ts (plan 03.1-04):
  usePreloadedAudio(): {
    prefetchAll(ids: string[]): Promise<void>
    getObjectUrl(id: string): string | undefined     // SÍNCRONA
    staticUrlFor(id: string): string                 // `/audio/${id}.m4a`
    audioAvailable: Ref<boolean | null>              // null = precarga en vuelo
  }

app/composables/useVoiceAnnouncer.ts (Fase 3, estado actual — se AMPLÍA, no se sustituye):
  useVoiceAnnouncer(currentNode: ComputedRef<RuntimeStepNode|null>, currentText: ComputedRef<TextBlock>)
  export resolveVoiceState(prefEnabled: boolean, available: boolean|null): VoiceState
  export shouldAnnounce({ kind, state, line, isSupported }): boolean
  export hasSpanishVoice(voices): boolean
  export detectSpanishVoice(synth, onResult, timeoutMs = 2000): () => void
  export scheduleSpeakWatchdog(speak, getStatus, delayMs = 200): () => void   // G-01, solo para speechSynthesis
  devuelve { voiceState, announce, toggle, silence, available, isSupported, showVoiceUnavailableNotice, dismissNotice }

  Cuerpo actual de announce(): lee kind -> shouldAnnounce -> clearPendingRetry() ->
  try { speak() } catch { return } -> cancelWatchdog = scheduleSpeakWatchdog(speak, () => status.value)

app/pages/[game]/index.vue:
  const { ... currentNode, currentText, ... } = useGameSession()
  const { voiceState, announce, toggle: toggleVoice, showVoiceUnavailableNotice, dismissNotice } = useVoiceAnnouncer(currentNode, currentText)
  onNext/onBack/onIndexJumpTo llaman announce() como sentencia plana dentro del manejador (D-42)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: currentAudioId, la costura reactiva entre motor y voz</name>
  <files>app/composables/useGameSession.ts, app/pages/[game]/index.vue</files>
  <read_first>
    - `app/composables/useGameSession.ts` — fichero completo: su comentario de cabecera dice que es la ÚNICA costura reactiva con `~~/engine/*` y que si un componente necesita algo del motor es que falta una computed aquí; ver también la forma de `currentText`
    - `app/composables/useVoiceAnnouncer.ts` (líneas 1-12 y 151-160) — su comentario de cabecera prohíbe importar valores de `~~/engine/*` (solo `import type`), que es exactamente por lo que el id llega como parámetro ya calculado
    - `engine/resolve.ts` — firma de `resolveAudioId`
    - `app/pages/[game]/index.vue` (líneas 24-50) — destructuración de `useGameSession` y llamada a `useVoiceAnnouncer`
  </read_first>
  <action>
**app/composables/useGameSession.ts** — añadir, justo debajo de `currentText` y con el mismo
estilo, una computed `currentAudioId`:
`computed<string | null>(() => (!session.value || !currentNode.value) ? null : resolveAudioId(currentNode.value, session.value.context))`,
importando `resolveAudioId` del mismo `~~/engine/resolve` de donde ya viene `resolveText`.
Añadirla al objeto devuelto. Comentario encima explicando que la voz no puede calcular el id por
su cuenta: `useVoiceAnnouncer` tiene prohibido importar valores del motor (su propio comentario
de cabecera), así que la traducción nodo+dificultad → id de audio vive aquí, al lado de
`currentText`, y las dos no pueden desincronizarse porque salen de la misma rama de variante.

**app/pages/[game]/index.vue** — destructurar `currentAudioId` de `useGameSession()` y pasarlo
como TERCER argumento a `useVoiceAnnouncer(currentNode, currentText, currentAudioId)`. Nada más
cambia en esta página: `onNext`/`onBack`/`onIndexJumpTo` siguen llamando a `announce()` como
sentencia plana; toda la lógica nueva vive dentro del composable.
  </action>
  <verify>
    <automated>npx vitest run --project app-logic && npx nuxt generate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "currentAudioId" app/composables/useGameSession.ts` devuelve 3 o más (comentario/computed/objeto devuelto)
    - `grep -c "resolveAudioId" app/composables/useGameSession.ts` devuelve 2 (import y uso)
    - `grep -c "useVoiceAnnouncer(currentNode, currentText, currentAudioId)" "app/pages/[game]/index.vue"` devuelve 1
    - `grep -c "resolveAudioId" app/composables/useVoiceAnnouncer.ts` devuelve 0 (la voz sigue sin importar valores del motor)
    - `npx nuxt generate` sale 0
    - `npm run test` sale 0
  </acceptance_criteria>
  <done>El id del audio del paso actual llega al anunciador ya calculado, por la misma costura por la que ya llega el texto.</done>
</task>

<task type="auto">
  <name>Task 2: Funciones puras nuevas del anunciador y sus tests</name>
  <files>app/composables/useVoiceAnnouncer.ts, app/composables/__tests__/useVoiceAnnouncer.test.ts</files>
  <read_first>
    - `app/composables/useVoiceAnnouncer.ts` — fichero completo: `shouldAnnounce` (líneas 24-37), `resolveVoiceState` (16-22) y `scheduleSpeakWatchdog` (98-149) son el modelo exacto de "función pura extraída para poder testearla en entorno node sin montar el composable"
    - `app/composables/__tests__/useVoiceAnnouncer.test.ts` — fichero completo: convenciones de nombres en español con id de decisión, uso de temporizadores falsos para el watchdog, y el hecho de que el composable NO se monta en estos tests
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-RESEARCH.md` §Pattern 2 y §Pitfall 1
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-CONTEXT.md` — D-07 y D-08
  </read_first>
  <action>
Ampliar `app/composables/useVoiceAnnouncer.ts` con tres piezas puras exportadas, ANTES de tocar
`announce()` (eso es la Task 3). Cada una con su comentario-contrato citando la decisión.

1. `shouldAnnounce` — añadir a su objeto de entrada un campo OPCIONAL `hasAudio?: boolean`
   (por defecto `false`, para que las llamadas y los tests existentes de la Fase 3 sigan valiendo
   sin cambios). Nueva regla:
   `kind === 'step' && state === 'on' && line.length > 0 && (hasAudio === true || isSupported === true)`.
   Comentario: VOZ-07 exige que la locución no dependa de las voces instaladas en el sistema — si
   hay clip pregenerado, que `speechSynthesis` no esté soportado deja de ser motivo para callar.

2. `resolveEffectiveAvailability(audioAvailable: boolean | null, spanishVoiceAvailable: boolean | null): boolean | null`
   — nueva. Regla: si `audioAvailable === true` → `true`; si `audioAvailable === null` → `null`
   (la precarga aún no ha resuelto: se mantiene el optimismo de D-47 y NO se pinta la banda); si
   `audioAvailable === false` → devolver `spanishVoiceAvailable` tal cual. Comentario obligatorio
   explicando las dos consecuencias: (a) con audio pregenerado disponible, el control de silencio
   NUNCA queda en `unavailable`, así que el usuario siempre puede callar y reactivar la voz
   (criterio 1); (b) la banda de "sin voz en español" de la Fase 3 solo aparece cuando fallan
   LAS DOS fuentes, que es exactamente el caso para el que se diseñó — anunciarla mientras suena
   Rasalgethi sería mentir y además violaría D-07 (nada de bandas).

3. `scheduleAudioWatchdog(hasStarted: () => boolean, onStall: () => void, delayMs = 1200): () => void`
   — nueva, calcada en forma a `scheduleSpeakWatchdog` (devuelve función de cancelación,
   `setTimeout`, `try/catch` alrededor del callback). Si pasado el retardo `hasStarted()` sigue
   siendo `false`, invoca `onStall()`. Comentario: cubre el caso en que `.play()` ni resuelve ni
   rechaza (clip que no llega por una red presente pero inútil); sin esto ese paso se quedaría
   MUDO, peor que caer al respaldo. Debe decir también que el respaldo disparado desde aquí ya no
   está dentro del gesto y por tanto puede ser descartado en iOS: es una mejora oportunista, no
   una garantía, y jamás debe bloquear nada.

**Tests** — ampliar `app/composables/__tests__/useVoiceAnnouncer.test.ts` SIN modificar ni borrar
ningún test existente (son el contrato verificado de la Fase 3):
- `shouldAnnounce` con `hasAudio: true` e `isSupported: false` → `true` (VOZ-07).
- `shouldAnnounce` con `hasAudio: true` y `state: 'muted'` → `false` (el silencio manda también
  sobre el audio pregenerado).
- `shouldAnnounce` con `hasAudio: true` y `kind: 'summary'` → `false` (D-40 sigue vigente).
- `shouldAnnounce` sin pasar `hasAudio` se comporta exactamente como antes (regresión de la Fase 3).
- `resolveEffectiveAvailability`: los 9 pares posibles de (`true|false|null`, `true|false|null`),
  con especial atención a `(false, false) → false` (única combinación que enciende la banda) y a
  `(null, false) → null` (no parpadea la banda mientras la precarga está en vuelo).
- `scheduleAudioWatchdog` con temporizadores falsos: llama a `onStall` si `hasStarted()` es
  `false` al vencer; no lo llama si es `true`; no lo llama si se canceló antes; y un `onStall`
  que lanza no propaga la excepción.
  </action>
  <verify>
    <automated>npx vitest run --project app-logic app/composables/__tests__/useVoiceAnnouncer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project app-logic app/composables/__tests__/useVoiceAnnouncer.test.ts` sale 0 y ejecuta al menos 15 tests más que antes de este plan
    - `grep -c "export function resolveEffectiveAvailability\|export function scheduleAudioWatchdog" app/composables/useVoiceAnnouncer.ts` devuelve 2
    - `grep -c "hasAudio" app/composables/useVoiceAnnouncer.ts` devuelve 2 o más
    - `git diff app/composables/__tests__/useVoiceAnnouncer.test.ts | grep -c "^-" ` devuelve 0 salvo la línea de cabecera del propio diff (ningún test de la Fase 3 borrado ni modificado)
    - `npm run test` sale 0
  </acceptance_criteria>
  <done>Las tres reglas nuevas (cuándo hablar con audio, cuándo hay voz de verdad, y qué hacer si el clip no arranca) son funciones puras probadas antes de cablearse a nada.</done>
</task>

<task type="auto">
  <name>Task 3: Rama de audio pregenerado dentro de announce(), con el respaldo intacto debajo</name>
  <files>app/composables/useVoiceAnnouncer.ts</files>
  <read_first>
    - `app/composables/useVoiceAnnouncer.ts` — fichero completo, en especial `announce()` (líneas 219-251), `silence()` (253-265), `toggle()` (267-275), el watcher de visibilidad (282-285), `clearPendingRetry()` (172-175) y los `tryOnScopeDispose` (177, 204)
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-RESEARCH.md` §Pattern 2 — el esqueleto exacto de la rama de audio
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-PATTERNS.md` §`app/composables/useVoiceAnnouncer.ts` — qué se preserva sin tocar (guardas, `clearPendingRetry` incondicional, watchdog G-01 atado SOLO al respaldo)
    - `CLAUDE.md` §"Text-to-Speech: what's real vs assumed" — la regla del gesto síncrono, que aquí se extiende a `<audio>.play()`
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-04-SUMMARY.md` — firma real de `usePreloadedAudio`
  </read_first>
  <action>
Modificar `useVoiceAnnouncer` (diff, no reescritura). El cuerpo actual de `announce()` —guardas,
`try { speak() } catch { return }` y `scheduleSpeakWatchdog`— se extrae TAL CUAL a una función
interna `speakFallback()`; ni una línea de esa lógica cambia (es el arreglo G-01 ya verificado
en dispositivo real).

1. Firma: tercer parámetro `currentAudioId: ComputedRef<string | null>`. Importar
   `usePreloadedAudio` de `./usePreloadedAudio` (mismo patrón que ya se usa con
   `usePersistedSession`) y destructurar `getObjectUrl`, `staticUrlFor` y `audioAvailable`.
2. Elemento `<audio>` único y reutilizado (no un pool: solo suena una locución a la vez, VOZ-04).
   `let audioEl: HTMLAudioElement | null = null`, creado en `onMounted` con
   `new Audio()` y `preload = 'auto'`; nunca en el cuerpo del setup (esta página prerrenderiza).
   Mantener una bandera `audioStarted` puesta a `true` por el listener `playing` y a `false` en
   cada intento nuevo; es lo que lee `scheduleAudioWatchdog`. Registrar el desmontaje con
   `tryOnScopeDispose` (pausar, vaciar `src`, quitar el listener) — nunca `onUnmounted`, misma
   convención que el resto del fichero.
3. `stopAudio()`: función interna síncrona que pausa el elemento (dentro de try/catch) y cancela
   el watchdog de audio pendiente. Se invoca al principio de `announce()` y desde `silence()`.
4. Nuevo `announce()`, TODO síncrono hasta la llamada a `.play()` — prohibido cualquier `await`
   en el camino:
   - `const audioId = currentAudioId.value`
   - `const objectUrl = audioId ? getObjectUrl(audioId) : undefined`  (lectura síncrona de un Map)
   - `const audioSrc = objectUrl ?? (audioId ? staticUrlFor(audioId) : undefined)` — segundo
     escalón: si la precarga todavía no tiene el blob pero hay red, la URL estática directa
     suena mucho mejor que caer a la voz del sistema. Comentario explicándolo.
   - `willAnnounce = shouldAnnounce({ kind, state: voiceState.value, line: spokenLine.value, isSupported: isSupported.value, hasAudio: audioSrc !== undefined })`
   - `clearPendingRetry()` y `stopAudio()` SIEMPRE, antes del `if (!willAnnounce) return`
     (VOZ-04: navegar corta lo que hubiera sonando, decida lo que decida la guarda).
   - si `audioSrc`: cortar cualquier síntesis en curso llamando a `stop()` dentro de try/catch
     (la fuente cambia de motor y no pueden solaparse), asignar `audioEl.src = audioSrc`, poner
     `audioEl.currentTime = 0` dentro de try/catch (necesario para re-locutar el mismo paso),
     `audioStarted = false`, llamar a `audioEl.play().catch(() => speakFallback())` (D-07:
     rechazo = ausencia, en silencio) y armar
     `cancelAudioWatchdog = scheduleAudioWatchdog(() => audioStarted, speakFallback)`. `return`.
   - si no hay `audioSrc` (o `audioEl` es null porque aún no montó): `speakFallback()`.
   - Cero `console.*` en todo el camino (D-07: sin ruido en consola).
5. `silence()`: añadir `stopAudio()` al principio, antes de lo que ya hace. D-45 exige corte
   inmediato de la locución, venga de donde venga. `toggle()` y el watcher de visibilidad no
   cambian: ya pasan por `silence()`.
6. Disponibilidad efectiva: `available` deja de alimentar directamente a `voiceState`. Renombrar
   el ref interno de la detección de la Fase 3 a `spanishVoiceAvailable` (su lógica y su carrera
   acotada NO se tocan) y definir
   `const available = computed(() => resolveEffectiveAvailability(audioAvailable.value, spanishVoiceAvailable.value))`.
   `voiceState` y `showVoiceUnavailableNotice` siguen leyendo `available` exactamente igual que
   antes, así que el control de 3 estados de la cabecera y la banda descartable siguen sin
   tocarse. El objeto devuelto mantiene la misma forma (`available` sigue exportándose).
7. Actualizar el comentario de cabecera del fichero: la voz sigue sin ser motor y sigue sin
   importar valores de `~~/engine/*`; ahora tiene DOS fuentes, audio pregenerado primero y
   `speechSynthesis` de respaldo, y el watchdog G-01 sigue atado exclusivamente al respaldo.
  </action>
  <verify>
    <automated>npx vitest run && npx nuxt generate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function speakFallback" app/composables/useVoiceAnnouncer.ts` devuelve 1
    - `grep -c "scheduleSpeakWatchdog" app/composables/useVoiceAnnouncer.ts` devuelve 2 o más y esa llamada queda DENTRO de `speakFallback` (el watchdog G-01 no toca la rama de audio)
    - `sed -n '/function announce/,/^  }/p' app/composables/useVoiceAnnouncer.ts | grep -c "await"` devuelve 0
    - `sed -n '/function announce/,/^  }/p' app/composables/useVoiceAnnouncer.ts | grep -c "\.play()"` devuelve 1
    - `grep -c "console\." app/composables/useVoiceAnnouncer.ts` devuelve 0
    - `grep -c "resolveEffectiveAvailability(audioAvailable" app/composables/useVoiceAnnouncer.ts` devuelve 1
    - `grep -n "function silence" -A3 app/composables/useVoiceAnnouncer.ts | grep -c "stopAudio"` devuelve 1
    - `grep -c "tryOnScopeDispose" app/composables/useVoiceAnnouncer.ts` devuelve 3 o más (el nuevo recurso de audio también se desecha por scope, no por onUnmounted)
    - `npm run test` sale 0 con TODOS los tests de la Fase 3 aún en verde
    - `npx nuxt generate` sale 0
    - Comprobación manual en `npm run dev`: al pulsar SIGUIENTE suena el clip pregenerado; al pulsar SIGUIENTE otra vez a mitad de frase, la anterior se corta y arranca la nueva sin solaparse; con el control de silencio en «callado» no suena nada; en las DevTools con la red en «Offline» y la caché ya poblada sigue sonando el clip
  </acceptance_criteria>
  <done>El paso actual suena con el clip pregenerado, el silencio lo calla igual que antes, y cualquier fallo cae al camino de la Fase 3 sin banda, sin log y sin bloquear SIGUIENTE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gesto del usuario → API de reproducción del navegador | La política de autoplay concede permiso solo dentro del mismo tick del evento |
| id de audio → `audioEl.src` | El id acaba siendo una URL que el navegador va a cargar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-16 | Tampering | `audioEl.src` construido a partir de un id | mitigate | El id viene de `resolveAudioId`, derivado de contenido validado por Zod y verificado contra `/^[a-z0-9]+(\.[a-z0-9-]+)*$/` en el gate del plan 03.1-02; las únicas entradas del usuario (nº de jugadores, dificultad) pertenecen a conjuntos cerrados. Nunca se concatena texto libre a la URL |
| T-03.1-17 | Denial of Service | Locución que ni suena ni falla (clip que no llega por red presente pero inútil) | mitigate | `scheduleAudioWatchdog` cae al respaldo pasados 1200 ms sin `playing`; el botón SIGUIENTE nunca depende de la locución |
| T-03.1-18 | Information Disclosure | Fugas por consola en el camino de fallo | mitigate | D-07 exige silencio total: criterio de aceptación `grep -c "console\." useVoiceAnnouncer.ts` == 0 |
| T-03.1-19 | Repudiation | Regresión silenciosa del arreglo G-01 de la Fase 3 | mitigate | El cuerpo de la Fase 3 se extrae literalmente a `speakFallback()` y sus tests no se tocan; criterio de aceptación: cero líneas borradas en el fichero de tests existente |
| T-03.1-SC | Tampering | npm installs | mitigate | Cero instalaciones: `HTMLAudioElement` es nativo. `package.json` no se toca |
</threat_model>

<verification>
- `npm run test` sale 0, con todos los tests de la Fase 3 intactos y en verde
- `npx nuxt generate` sale 0
- No hay ni un `await` entre la entrada de `announce()` y la llamada a `.play()`
- Comprobación manual en `npm run dev`: suena el clip, se corta al navegar, el silencio lo calla, y con la red en Offline sigue sonando desde caché
</verification>

<success_criteria>
- La fuente primaria de locución es el clip pregenerado; `speechSynthesis` solo actúa de respaldo
- El control de silencio de la Fase 3 gobierna las dos fuentes por igual
- Un dispositivo sin voz española escucha igualmente el audio pregenerado y no ve ninguna banda
- Ningún fallo de audio produce banda, log ni bloqueo de SIGUIENTE
</success_criteria>

<output>
Create `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-05-SUMMARY.md` when done.
</output>
