---
phase: 03.1-voz-pregenerada-en-espa-ol-con-gemini-tts
plan: 03
type: execute
wave: 2
depends_on: [03.1-01, 03.1-02]
files_modified:
  - scripts/voice/styles.mjs
  - scripts/voice/manifest.json
  - public/audio/
  - engine/__tests__/voice-drift.test.ts
autonomous: true
requirements: [VOZ-07]

must_haves:
  truths:
    - "Las 37 frases `speech` tienen su clip generado con la voz Rasalgethi y el estilo que eligió el usuario, versionado en el repo (D-01/D-02)"
    - "Si alguien cambia una frase `speech` sin regenerar su audio, CI falla y nombra los ids afectados y el comando exacto para regenerarlos (D-04/D-05)"
    - "Si alguien cambia el estilo de locución sin regenerar, CI falla pidiendo `--force` (D-11)"
    - "El gate de CI nunca llama a la API de Gemini ni a `afconvert`: corre en ubuntu-latest y solo compara huellas (D-06)"
    - "Las dos variantes de dificultad de un mismo paso suenan distintas entre sí (Pitfall 4)"
  artifacts:
    - path: "scripts/voice/manifest.json"
      provides: "Mapa de huellas: voz, modelo, estilo y 37 entradas {hash, bytes, generatedAt}"
      contains: "Rasalgethi"
    - path: "public/audio/"
      provides: "37 clips .m4a servidos como estáticos, uno por cada cadena speech"
    - path: "engine/__tests__/voice-drift.test.ts"
      provides: "Gate de deriva contenido↔audio (D-04/D-05), con test de que el propio gate muerde"
      contains: "npm run voice:generate"
  key_links:
    - from: "engine/__tests__/voice-drift.test.ts"
      to: "scripts/voice/manifest.json"
      via: "readFileSync + fileURLToPath (mismo patrón que content.test.ts)"
      pattern: "manifest\\.json"
    - from: "engine/__tests__/voice-drift.test.ts"
      to: "engine/audio.ts"
      via: "collectSpeechEntries — el conjunto de claves del manifiesto debe coincidir exactamente"
      pattern: "collectSpeechEntries"
    - from: "engine/__tests__/voice-drift.test.ts"
      to: "scripts/voice/fingerprint.mjs"
      via: "misma función de huella que usó el generador"
      pattern: "fingerprint"
---

<objective>
Generar los 37 clips definitivos con el estilo que el usuario eligió y blindar el resultado
con el gate de CI que hace imposible mergear un audio que ya no corresponde a su frase.

Purpose: es el criterio 4 del ROADMAP y la razón por la que la fase puede confiarse a futuro.
D-05 es explícito: cuando el gate falla, tiene que decir QUÉ ids y QUÉ comando ejecutar, no
solo que algo no cuadra.

Output: `public/audio/*.m4a` (37), `scripts/voice/manifest.json` y
`engine/__tests__/voice-drift.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-CONTEXT.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-RESEARCH.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-PATTERNS.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-01-SUMMARY.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-02-SUMMARY.md

<interfaces>
<!-- Contratos producidos por los dos planes de la ola 1. -->

scripts/voice/generate.mjs (plan 03.1-01):
  npm run voice:generate                 -> solo las frases cuya huella cambió (D-10)
  npm run voice:generate -- <id> [<id>]  -> solo esos ids
  npm run voice:generate -- --force      -> las 37 (D-11)
  npm run voice:generate -- --delay=<ms> -> retardo entre llamadas (por defecto 1500)

scripts/voice/styles.mjs (plan 03.1-01):
  STYLES = { plano: '', pausado: '...', monitor: '...' }
  ACTIVE_STYLE = 'plano'   // <- este plan lo sustituye por la eleccion del usuario
  PROBE_PHRASE = 'Buscad el conjunto de Archienemigo (Nemesis) de vuestra identidad.'

scripts/voice/fingerprint.mjs (plan 03.1-01):
  fingerprint(speech: string): string   // sha256 hex truncado a 16, sin normalizar

scripts/voice/manifest.json (lo escribe SOLO el generador, forma fija):
  { voice, model, style, generatedAt, entries: { "<id>": { hash, bytes, generatedAt } } }

engine/audio.ts (plan 03.1-02):
  interface SpeechEntry { id: string, speech: string }
  collectSpeechEntries(game: GameDefinition): SpeechEntry[]   // 37 entradas
  collectAudioIds(game: GameDefinition): string[]

engine/__tests__/content.test.ts — patron de carga a replicar literalmente:
  const contentPath = fileURLToPath(new URL('../../content/marvel-champions.json', import.meta.url))
  const game = validateGameDefinition(JSON.parse(readFileSync(contentPath, 'utf-8')))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fijar el estilo elegido y generar los 37 clips definitivos</name>
  <files>scripts/voice/styles.mjs, scripts/voice/manifest.json, public/audio/</files>
  <read_first>
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-01-SUMMARY.md` — el estilo que eligió el usuario en el checkpoint D-02 y si hubo error de reproducción en la tablet
    - `scripts/voice/styles.mjs` — dónde vive `ACTIVE_STYLE` y su comentario actual
    - `scripts/voice/generate.mjs` — modos de invocación, política de reintentos y punto exacto donde consolida el manifiesto
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-RESEARCH.md` §Pitfall 2 (cuota) y §Open Questions 1
  </read_first>
  <action>
1. Fijar en `scripts/voice/styles.mjs` la constante `ACTIVE_STYLE` con el valor exacto que el
   usuario eligió en el checkpoint del plan 03.1-01 (`plano`, `pausado` o `monitor`), y
   actualizar el comentario para dejar registrado que fue una elección humana (D-02) y su fecha.
   Si el SUMMARY del plan 03.1-01 no contiene una elección explícita, PARAR y reportarlo: el
   lote no puede lanzarse sin ella.
2. Antes del lote, comprobar el tier real de la API del proyecto en
   `https://aistudio.google.com/rate-limit`. Si impone del orden de 3 peticiones por minuto,
   invocar con `--delay=21000`; si no, el retardo por defecto sirve.
3. Ejecutar `npm run voice:generate -- --force` (con el `--delay` que corresponda). `--force`
   es obligatorio aquí: el estilo activo acaba de cambiar respecto al que se usó en las
   pruebas (D-11).
4. Si la ejecución se corta por cuota (429 tras agotar los reintentos): NO inventar ficheros,
   NO reducir el número de frases, NO cambiar de voz ni de modelo. Relanzar
   `npm run voice:generate` (sin `--force`: por diferencia de huella ya solo recoge lo que
   falta) cuando la cuota se recupere. Si no es posible terminar en esta sesión, dejar el
   trabajo consolidado, reportarlo en el SUMMARY y NO dar la tarea por hecha.
5. Comprobaciones tras el lote: 37 `.m4a` en `public/audio/`, 37 entradas en `manifest.json`,
   `voice` igual a `Rasalgethi`, `style` igual al `ACTIVE_STYLE` fijado, ningún fichero por
   debajo de 10 KB y total por debajo de 1,5 MB (la estimación medida es 600-700 KB; un total
   muy por encima delata un bitrate o contenedor equivocados).
6. Escuchar con `afplay` al menos tres clips: uno de los 33 base y las DOS variantes del mismo
   paso (`.normal` y `.expert`). Confirmar que suenan en español, con la voz elegida, sin
   recortes al principio ni al final, y que las dos variantes NO dicen lo mismo — ese es
   exactamente el fallo que el respaldo silencioso enmascararía (Pitfall 4).
7. Confirmar que `public/audio/_probe/` sigue con sus 3 clips de prueba intactos y que el lote
   no los ha tocado ni los ha incluido en el manifiesto.
  </action>
  <verify>
    <automated>ls public/audio/*.m4a | wc -l | tr -d ' ' | grep -x 37</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/audio/*.m4a | wc -l` devuelve 37 (los 3 de `_probe/` no cuentan: están en un subdirectorio)
    - `grep -c '"hash":' scripts/voice/manifest.json` devuelve 37
    - `grep -c 'Rasalgethi' scripts/voice/manifest.json` devuelve 1 o más
    - `grep -c "ACTIVE_STYLE = 'plano'" scripts/voice/styles.mjs` devuelve 0 salvo que el usuario haya elegido exactamente `plano`; en cualquier caso el valor de `ACTIVE_STYLE` coincide con el campo `style` del manifiesto
    - `find public/audio -maxdepth 1 -name '*.m4a' -size -10k | wc -l` devuelve 0
    - `du -sk public/audio | cut -f1` devuelve menos de 1500
    - `ls public/audio/setup.encuentros.03.normal.m4a public/audio/setup.encuentros.03.expert.m4a public/audio/setup.escenario.04.normal.m4a public/audio/setup.escenario.04.expert.m4a` sale 0 (las 4 variantes de dificultad existen)
    - `cmp -s public/audio/setup.encuentros.03.normal.m4a public/audio/setup.encuentros.03.expert.m4a` sale distinto de 0 (los dos clips de variante NO son el mismo fichero)
    - `ls public/audio/_probe/*.m4a | wc -l` devuelve 3
    - `git status --porcelain public/audio | grep -c '^??\|^ M'` refleja 37 ficheros nuevos listos para commitear
  </acceptance_criteria>
  <done>Los 37 clips definitivos existen en el repo con la voz y el estilo aprobados, el manifiesto los describe, y una escucha real confirma que las variantes de dificultad no están duplicadas.</done>
</task>

<task type="auto">
  <name>Task 2: Gate de deriva contenido ↔ audio en CI (D-04/D-05)</name>
  <files>engine/__tests__/voice-drift.test.ts</files>
  <read_first>
    - `engine/__tests__/content.test.ts` — patrón de carga (`readFileSync` + `fileURLToPath` + `validateGameDefinition`), helpers locales que lanzan `Error` con el id dentro del mensaje, y los tests "el gate muerde" (`expect(() => ...).toThrow()` sobre una copia mutada en memoria)
    - `engine/audio.ts` — `collectSpeechEntries` / `collectAudioIds`
    - `scripts/voice/fingerprint.mjs` — la función de huella que usó el generador
    - `scripts/voice/manifest.json` — la forma real recién escrita
    - `.github/workflows/ci.yml` — CI corre `npm run test` en `ubuntu-latest`, sin `afconvert` y sin clave de API
    - `vitest.config.ts` — el proyecto `engine` recoge `engine/**/*.test.ts` sin tocar configuración
  </read_first>
  <action>
Crear `engine/__tests__/voice-drift.test.ts`. Cabecera con comentario que declare que este
fichero es SOLO LECTOR: nunca regenera, nunca invoca `afconvert`, nunca llama a la API
(D-06 — CI corre en ubuntu-latest, donde `afconvert` no existe).

Cargar tres cosas con el patrón de `content.test.ts`:
- el contenido validado (`validateGameDefinition`),
- `scripts/voice/manifest.json` vía `fileURLToPath(new URL('../../scripts/voice/manifest.json', import.meta.url))`,
- `fingerprint` y `ACTIVE_STYLE` importados de `../../scripts/voice/fingerprint.mjs` y
  `../../scripts/voice/styles.mjs`. Si Vitest se queja del tipado al importar `.mjs` desde un
  `.ts`, la corrección aceptada es añadir un `scripts/voice/fingerprint.d.ts` /
  `styles.d.ts` mínimo junto al `.mjs` — NO duplicar la función de huella dentro del test
  (una segunda definición es exactamente la deriva que este gate existe para evitar).

Helper local puro `findStaleAudio(entries, manifestEntries)` que devuelve las entradas cuyo
`manifestEntries[id]?.hash !== fingerprint(speech)` (incluye las ausentes). Y otro helper
`staleMessage(stale)` que construye el mensaje de D-05: una primera línea
`Audio desactualizado para: <ids separados por coma>` y una segunda línea
`Ejecuta: npm run voice:generate -- <ids separados por espacio>`. Tenerlos como funciones
locales permite usarlos tanto en el test real como en el test de "el gate muerde".

Tests (nombres en español citando D-04/D-05, al estilo del fichero análogo):
1. `D-04: cada frase speech tiene un audio con la huella al día` — `findStaleAudio` sobre el
   contenido y el manifiesto reales devuelve lista vacía; si no, lanzar `Error(staleMessage(stale))`
   para que el fallo de CI ya traiga los ids y el comando.
2. `D-05: el mensaje de fallo nombra los ids y el comando exacto` — sobre una copia en memoria
   con una frase mutada, comprobar que el mensaje contiene el id afectado y la subcadena
   `npm run voice:generate -- ` seguida de ese id.
3. `el gate muerde` — mutar en memoria el `speech` de un paso cualquiera (obtenido por
   recorrido, nunca por id tecleado) y comprobar que `findStaleAudio` devuelve exactamente esa
   entrada. Nunca escribir en el fichero real.
4. `el manifiesto cubre exactamente el catálogo del motor` — el conjunto de claves de
   `manifest.entries` es idéntico al conjunto de `collectAudioIds(game)`, comparando en las dos
   direcciones y nombrando en el mensaje de fallo las claves sobrantes y las que faltan. Este es
   el test que caza que el recorrido en JS plano de `generate.mjs` se separe del de
   `engine/audio.ts`.
5. `hay exactamente 37 entradas` — `Object.keys(manifest.entries)` tiene longitud 37.
6. `D-11: el estilo del manifiesto coincide con ACTIVE_STYLE` — si no, mensaje que diga
   `Ejecuta: npm run voice:generate -- --force`.
7. `cada entrada del manifiesto tiene su fichero de audio` — para cada id,
   `existsSync(public/audio/<id>.m4a)` y `statSync(...).size > 10000`. Mensaje de fallo con los
   ids sin fichero.
8. `metadatos de generación` — `manifest.voice` es `Rasalgethi` (D-01) y `manifest.model` no
   está vacío.

Prohibido en este fichero: `fetch`, `child_process`, `afconvert`, cualquier escritura a disco.
  </action>
  <verify>
    <automated>npx vitest run --project engine engine/__tests__/voice-drift.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project engine engine/__tests__/voice-drift.test.ts` sale 0 con al menos 8 tests
    - `grep -c "npm run voice:generate" engine/__tests__/voice-drift.test.ts` devuelve 2 o más (mensaje por id y mensaje `--force`)
    - `grep -Ec "fetch\(|child_process|afconvert|writeFileSync" engine/__tests__/voice-drift.test.ts` devuelve 0
    - `grep -c "collectSpeechEntries\|collectAudioIds" engine/__tests__/voice-drift.test.ts` devuelve 1 o más
    - `grep -c "setup\." engine/__tests__/voice-drift.test.ts` devuelve 0 (ningún id tecleado)
    - Prueba manual de que el gate muerde de verdad: editar temporalmente una cadena `speech` de `content/marvel-champions.json`, ejecutar `npm run test` y confirmar que falla nombrando ese id y el comando; revertir la edición con `git checkout -- content/marvel-champions.json` y confirmar que `npm run test` vuelve a salir 0. Dejar constancia en el SUMMARY
    - `npm run test` sale 0 con el árbol limpio
  </acceptance_criteria>
  <done>CI falla, nombrando ids y comando, ante cualquier cambio de frase sin regenerar, ante un cambio de estilo sin `--force`, ante un id sobrante o ausente en el manifiesto, y ante un audio prometido pero no commiteado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → despliegue público | Los 37 `.m4a` y el propio manifiesto quedan versionados; los `.m4a` se sirven públicamente desde Vercel |
| CI (ubuntu-latest) → herramientas locales | El gate corre en un entorno sin `afconvert` y sin `GEMINI_API_KEY`: no puede depender de ninguno de los dos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-08 | Information Disclosure | Los 37 `.m4a` publicados | accept | Contienen exactamente las 37 frases `speech`, ya públicas en `content/marvel-champions.json` dentro de un repo público. Cero PII, cero secretos. Se verifica escuchando muestras en la Task 1 |
| T-03.1-09 | Information Disclosure | `scripts/voice/manifest.json` | accept | Solo ids, huellas sha256 de cadenas ya públicas, tamaños y fechas. Aun así se mantiene FUERA de `public/` (no tiene consumidor en el cliente y no debe acabar en el manifiesto de precacheo de la Fase 4) |
| T-03.1-10 | Tampering | Manifiesto editado a mano, sin pasar por el generador (Pitfall 3) | mitigate | El gate compara huella-contra-contenido Y fichero-contra-entrada Y estilo-contra-`ACTIVE_STYLE`: un manifiesto retocado a mano no puede cuadrar las tres cosas sin que exista de verdad el audio correspondiente |
| T-03.1-11 | Elevation of Privilege | Gate de CI ejecutando herramientas locales o de red | mitigate | Prohibición explícita y verificada con grep: sin `fetch(`, sin `child_process`, sin `afconvert`, sin escrituras. D-06 queda garantizado por construcción, no por confianza |
| T-03.1-SC | Tampering | npm installs | mitigate | Este plan no instala nada: solo genera activos y añade un test. `package.json` no se modifica |
</threat_model>

<verification>
- `ls public/audio/*.m4a | wc -l` devuelve 37
- `npm run test` sale 0 con el árbol limpio, y falla nombrando ids y comando con una frase `speech` alterada
- Ni el test ni CI invocan a `afconvert` ni a la API de Gemini
- Escucha manual: los clips suenan en español con la voz Rasalgethi y las variantes de dificultad son distintas entre sí
</verification>

<success_criteria>
- 37 clips versionados, generados con Rasalgethi y el estilo aprobado por el usuario
- Manifiesto con 37 huellas, voz, modelo y estilo
- Un cambio de frase sin regenerar rompe CI con el mensaje de D-05
- Un cambio de estilo sin `--force` rompe CI con el mensaje correspondiente
</success_criteria>

<output>
Create `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-03-SUMMARY.md` when done.
Registrar en el SUMMARY: estilo fijado, si hubo cortes por cuota y cuántos reintentos, tamaño
total de `public/audio/`, y el resultado de la prueba manual de que el gate muerde.
</output>
