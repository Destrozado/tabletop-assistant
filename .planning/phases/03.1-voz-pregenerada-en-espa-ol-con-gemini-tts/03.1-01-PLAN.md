---
phase: 03.1-voz-pregenerada-en-espa-ol-con-gemini-tts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/voice/wav.mjs
  - scripts/voice/fingerprint.mjs
  - scripts/voice/styles.mjs
  - scripts/voice/generate.mjs
  - package.json
  - public/voice-probe.html
  - public/audio/_probe/estilo-plano.m4a
  - public/audio/_probe/estilo-pausado.m4a
  - public/audio/_probe/estilo-monitor.m4a
autonomous: false
requirements: [VOZ-07]

must_haves:
  truths:
    - "El desarrollador puede regenerar el audio de una frase concreta con `npm run voice:generate -- <id>` sin tocar los otros 36 (D-10)"
    - "El desarrollador puede regenerar las 37 con `npm run voice:generate -- --force` (D-11)"
    - "El usuario puede escuchar 2-3 variantes de estilo de la MISMA frase antes de que se genere ningún lote (D-02)"
    - "El usuario puede comprobar, en la tablet real, que un <audio>.play() disparado dentro de un click no es rechazado por la política de autoplay (riesgo A1 de RESEARCH.md)"
    - "Ningún fichero bajo app/ ni engine/ conoce la API de Gemini ni la clave"
  artifacts:
    - path: "scripts/voice/generate.mjs"
      provides: "Generador local: Gemini TTS (voz Rasalgethi) -> PCM -> WAV -> AAC/M4A, incremental por huella, reanudable ante 429"
      contains: "X-goog-api-key"
    - path: "scripts/voice/wav.mjs"
      provides: "Cabecera RIFF/WAVE canónica de 44 bytes sobre PCM crudo 24kHz/16-bit/mono"
      exports: ["wrapPcmAsWav"]
    - path: "scripts/voice/fingerprint.mjs"
      provides: "Huella sha256 truncada a 16 hex de una cadena speech (D-03)"
      exports: ["fingerprint"]
    - path: "scripts/voice/styles.mjs"
      provides: "Catálogo de estilos de locución (D-02) y el estilo activo, única fuente de verdad compartida por el generador y el gate de CI"
      exports: ["STYLES", "ACTIVE_STYLE", "PROBE_PHRASE"]
    - path: "public/voice-probe.html"
      provides: "Página estática de un solo uso para escuchar las 3 variantes en la tablet y ver el nombre del error si play() es rechazado"
    - path: "package.json"
      provides: "Entradas voice:generate y voice:probe, sin ninguna dependencia npm nueva"
      contains: "voice:generate"
  key_links:
    - from: "scripts/voice/generate.mjs"
      to: "content/marvel-champions.json"
      via: "readFileSync + recorrido genérico sections->phases->steps"
      pattern: "marvel-champions\\.json"
    - from: "scripts/voice/generate.mjs"
      to: "afconvert"
      via: "execFileSync con argumentos en array (nunca shell)"
      pattern: "execFileSync\\('afconvert'"
    - from: "public/voice-probe.html"
      to: "public/audio/_probe/*.m4a"
      via: "audio.src + play() síncrono dentro del manejador de click"
      pattern: "\\.play\\(\\)"
---

<objective>
Construir la cadena de generación local de audio (Gemini TTS -> PCM -> WAV -> AAC/M4A) y
dejar al usuario escuchando 2-3 variantes de estilo de UNA sola frase para que elija,
antes de que se genere ningún lote.

Purpose: D-02 es un gate duro — el lote de 37 no puede lanzarse hasta que el usuario haya
elegido el estilo. Además, esta misma escucha resuelve de paso el riesgo abierto A1 de
RESEARCH.md (¿`<audio>.play()` dentro de un gesto funciona en la tablet real?), que hasta
hoy solo está verificado leyendo código.

Output: `scripts/voice/` (4 ficheros, convención NUEVA en este repo — no hay precedente de
script propio), dos entradas npm, 3 clips de prueba y una página estática de escucha.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-CONTEXT.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-FINDINGS.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-RESEARCH.md
@.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-PATTERNS.md

<interfaces>
<!-- Contratos que el ejecutor necesita sin explorar el repo. -->

Forma real del contenido a locutar (content/marvel-champions.json, contentVersion 11,
NO se modifica en esta fase). Recuento verificado programáticamente: 33 pasos con
`speech` propio + 4 `speech` dentro de `variants.difficulty` = 37 cadenas.

- Paso base:            { id, title, kind: 'step'|'summary', text, speech?, warning?, ... }
- Variante:             step.variants.difficulty.normal.speech / .expert.speech
- Los 2 ÚNICOS pasos con variantes son `setup.encuentros.03` y `setup.escenario.04`,
  y ambos declaran `speech` en las dos dificultades. NO los teclees: derívalos recorriendo
  el JSON (convención "recorrido genérico, nunca lista de ids" de engine/__tests__/content.test.ts).

Identificador de audio (misma regla que implementa el plan 03.1-02 en engine/):
- entrada base:     id = step.id                      (cuando step.speech !== undefined)
- entrada variante: id = `${step.id}.${difficulty}`   (cuando variants.difficulty[d].speech !== undefined)

API de Gemini TTS (verificada con llamadas reales, 03.1-FINDINGS.md):
- POST https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-tts:generateContent
- Cabecera: `X-goog-api-key: <GEMINI_API_KEY>`  (NUNCA como parámetro `?key=` en la URL)
- Cuerpo: contents[0].parts[0].text = prefijo de estilo + frase;
  generationConfig.responseModalities = ["AUDIO"];
  generationConfig.speechConfig.voiceConfig.prebuiltVoiceConfig.voiceName = "Rasalgethi"
- Respuesta: candidates[0].content.parts[0].inlineData.data (base64),
  mimeType `audio/L16;codec=pcm;rate=24000` — PCM CRUDO, sin contenedor.
- Idioma autodetectado: no se declara el español en ninguna parte del cuerpo.

Conversión (verificada, macOS, sin ffmpeg):
  afconvert -f m4af -d aac -b 64000 entrada.wav salida.m4a
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cadena de generación local (scripts/voice/) y entradas npm</name>
  <files>scripts/voice/wav.mjs, scripts/voice/fingerprint.mjs, scripts/voice/styles.mjs, scripts/voice/generate.mjs, package.json</files>
  <read_first>
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-FINDINGS.md` — endpoint, modelo, forma del cuerpo, salida PCM cruda, comando afconvert (todo verificado con llamadas reales; no reinvestigar)
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-RESEARCH.md` §Code Examples — cabecera WAV campo a campo, invocación de afconvert, función fingerprint
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-PATTERNS.md` §No Analog Found — este repo NO tiene precedente de `scripts/`; aquí se establece la convención, no se sigue ninguna
    - `engine/__tests__/content.test.ts` (líneas 1-40) — convención de recorrido genérico `allSteps()` que el script debe replicar en JS plano
    - `package.json` — estado actual: 7 dependencies, 3 devDependencies, 6 scripts; ninguno delega en un fichero del repo
    - `.gitignore` — confirmar que `.env` está listado (bloque "Secretos y credenciales")
  </read_first>
  <action>
Crear cuatro ficheros ESM en `scripts/voice/` (Node puro, sin TypeScript, sin build). Cada
uno abre con un comentario de cabecera al estilo del repo (comentario-como-contrato: qué es,
qué decisión satisface, qué NO debe hacer).

**scripts/voice/wav.mjs** — exporta `wrapPcmAsWav(pcm, sampleRate = 24000, bitsPerSample = 16, channels = 1)`
que devuelve un Buffer = cabecera RIFF/WAVE canónica de 44 bytes + el PCM. Campos y offsets
exactamente los de RESEARCH.md §Code Examples (ChunkSize = 36 + pcm.length, AudioFormat 1,
Subchunk1Size 16, byteRate = sampleRate * channels * bitsPerSample/8). No escribir un parser
WAV genérico: el caso es fijo (mono, 16 bits, 24 kHz).

**scripts/voice/fingerprint.mjs** — exporta `fingerprint(speech)` =
`createHash('sha256').update(speech, 'utf-8').digest('hex').slice(0, 16)` con `node:crypto`.
Sin `trim()`, sin normalizar, sin lower-case: cualquier cambio, por mínimo que sea, debe
marcar el audio como obsoleto (anti-patrón explícito de RESEARCH.md).

**scripts/voice/styles.mjs** — exporta:
- `STYLES`: objeto con exactamente tres claves y su prefijo de prompt (cadena vacía = sin
  instrucción):
  - `plano`: `''`
  - `pausado`: `'Di lo siguiente con un tono pausado e instructivo, como quien explica una regla de un juego de mesa: '`
  - `monitor`: `'Di lo siguiente con un tono cercano y animado, como un monitor de juegos que guía a un grupo de amigos: '`
- `ACTIVE_STYLE`: de momento `'plano'`, con un comentario que diga literalmente que este
  valor lo fija el plan 03.1-03 con la elección del usuario (D-02) y que cambiarlo obliga a
  regenerar con `--force` (D-11).
- `PROBE_PHRASE`: la frase de referencia usada para comparar voces —
  `'Buscad el conjunto de Archienemigo (Nemesis) de vuestra identidad.'` (es el `speech`
  real de `setup.archienemigos.01`).

**scripts/voice/generate.mjs** — CLI invocado a mano por el desarrollador. Comentario de
cabecera que declare D-06: este script NUNCA se invoca desde el build, ni desde CI, ni
desde Vercel; requiere macOS (`afconvert`) y `GEMINI_API_KEY`.

Comportamiento:
1. Carga de entorno: `process.loadEnvFile('.env')` dentro de try/catch (si falla, seguir con
   `process.env` tal cual). Leer `GEMINI_API_KEY`; si falta, imprimir un mensaje que explique
   dónde ponerla y salir con código 1 — sin imprimir jamás el valor.
2. Comprobar que `afconvert` existe (`execFileSync('afconvert', ['-h'])` en try/catch, o
   `which afconvert`); si no, salir con código 1 explicando que la generación solo corre en macOS.
3. Constantes: `MODEL = 'gemini-2.5-flash-preview-tts'`, `VOICE = 'Rasalgethi'` (D-01),
   `ENDPOINT` construido con MODEL, `AUDIO_DIR = 'public/audio'`,
   `MANIFEST_PATH = 'scripts/voice/manifest.json'`.
4. Args con `process.argv.slice(2)`: ids posicionales (subconjunto a regenerar), `--force`
   (todas, D-11), `--probe` (modo prueba de estilo, ver Task 2), `--delay=<ms>` (por defecto
   1500). Un id posicional desconocido aborta listando los ids válidos.
5. `collectSpeechEntries()` en JS plano: lee `content/marvel-champions.json` con `readFileSync`
   y recorre `sections -> phases -> steps` devolviendo `{ id, speech }[]` con la regla de
   `<interfaces>` (base + variantes). Comentario obligatorio: esta función DUPLICA a
   `collectSpeechEntries` de `engine/audio.ts` porque un `.mjs` de Node no puede importar TS;
   la divergencia la caza el gate de CI del plan 03.1-03, que compara el conjunto de claves
   del manifiesto con el de la función TS.
6. Validar cada id contra `/^[a-z0-9]+(\.[a-z0-9-]+)*$/` antes de usarlo como nombre de
   fichero; un id que no cuadre aborta (no se construyen rutas con datos sin validar).
7. Selección incremental (D-10): saltar la entrada si
   `manifest.entries[id]?.hash === fingerprint(speech)` Y existe `public/audio/<id>.m4a`.
   `--force` ignora ambas comprobaciones. Imprimir al arrancar cuántas se van a generar de
   cuántas totales.
8. Por clip, en bucle SECUENCIAL (nunca en paralelo) con `await sleep(delay)` entre llamadas:
   POST al ENDPOINT con la cabecera `X-goog-api-key` y el cuerpo de `<interfaces>`, usando
   `STYLES[ACTIVE_STYLE] + speech` como texto; decodificar
   `candidates[0].content.parts[0].inlineData.data` de base64 a Buffer; `wrapPcmAsWav`;
   escribir un `.wav` temporal en `os.tmpdir()`; `execFileSync('afconvert', ['-f','m4af','-d','aac','-b','64000', wavPath, m4aPath])`
   (argumentos en array, nunca interpolación de shell); borrar el `.wav`.
9. Reanudabilidad (obligatoria, Open Question 1 de RESEARCH.md): escribir el `.m4a` y
   ACTUALIZAR `manifest.json` después de CADA clip, no al final. Un corte por cuota deja el
   trabajo hecho consolidado.
10. Reintentos: ante HTTP 429 o 5xx, reintentar hasta 3 veces con espera 5s / 15s / 45s. Si
    se agotan, imprimir cuántas se completaron de cuántas y el comando exacto para continuar
    (`npm run voice:generate`), y salir con código 1. Los mensajes de error imprimen el status
    y como mucho los primeros 200 caracteres del cuerpo de la respuesta — nunca la clave, nunca
    la cabecera de la petición.
11. Forma del manifiesto (contrato que el gate del plan 03.1-03 leerá):
    `{ voice, model, style, generatedAt, entries: { "<id>": { hash, bytes, generatedAt } } }`,
    claves de `entries` ordenadas alfabéticamente, indentado de 2 espacios, salto de línea final.
    El script es el ÚNICO escritor de este fichero (Pitfall 3 de RESEARCH.md); dejarlo dicho
    en el comentario de cabecera.

**package.json** — añadir dos entradas al bloque `scripts`, sin tocar `dependencies` ni
`devDependencies` (cero dependencias nuevas es un requisito duro de RESEARCH.md):
`"voice:generate": "node scripts/voice/generate.mjs"` y
`"voice:probe": "node scripts/voice/generate.mjs --probe"`.
  </action>
  <verify>
    <automated>node --check scripts/voice/wav.mjs && node --check scripts/voice/fingerprint.mjs && node --check scripts/voice/styles.mjs && node --check scripts/voice/generate.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` sale 0 para los cuatro ficheros de `scripts/voice/`
    - `node -e "import('./scripts/voice/wav.mjs').then(m=>{const b=m.wrapPcmAsWav(Buffer.alloc(10));console.log(b.length===54, b.toString('ascii',0,4)==='RIFF', b.toString('ascii',8,12)==='WAVE')})"` imprime `true true true`
    - `node -e "import('./scripts/voice/fingerprint.mjs').then(m=>console.log(m.fingerprint('hola').length===16, m.fingerprint('hola')!==m.fingerprint('Hola')))"` imprime `true true`
    - `node -e "import('./scripts/voice/styles.mjs').then(m=>console.log(Object.keys(m.STYLES).join(','), m.ACTIVE_STYLE))"` imprime `plano,pausado,monitor plano`
    - `node -e "const p=require('./package.json');console.log(Object.keys(p.dependencies).length===7, Object.keys(p.devDependencies).length===3, !!p.scripts['voice:generate'], !!p.scripts['voice:probe'])"` imprime `true true true true`
    - `grep -c "X-goog-api-key" scripts/voice/generate.mjs` devuelve 1 o más y `grep -c "key=" scripts/voice/generate.mjs` devuelve 0 (la clave nunca viaja en la URL)
    - `grep -n "GEMINI_API_KEY" scripts/voice/generate.mjs | grep -c "console"` devuelve 0 (la clave nunca se imprime)
    - `grep -c "execFileSync('afconvert'" scripts/voice/generate.mjs` devuelve 1 o más, y `grep -c "execSync\|exec(" scripts/voice/generate.mjs` devuelve 0 (sin shell)
    - `grep -rn "generativelanguage\|GEMINI_API_KEY" app/ engine/ nuxt.config.ts | wc -l` devuelve 0
    - `git check-ignore -v .env` sale 0
    - `grep -c "setup.encuentros.03\|setup.escenario.04" scripts/voice/generate.mjs` devuelve 0 (ids derivados por recorrido, nunca tecleados)
    - `npm run test` sigue saliendo 0
  </acceptance_criteria>
  <done>La cadena de generación existe, es sintácticamente válida, no filtra la clave, no ejecuta shell, no añade dependencias y no toca nada bajo `app/` ni `engine/`.</done>
</task>

<task type="auto">
  <name>Task 2: Tres variantes de estilo de una sola frase + página estática de escucha</name>
  <files>public/audio/_probe/estilo-plano.m4a, public/audio/_probe/estilo-pausado.m4a, public/audio/_probe/estilo-monitor.m4a, public/voice-probe.html, scripts/voice/generate.mjs</files>
  <read_first>
    - `scripts/voice/generate.mjs` (el que acaba de escribirse en la Task 1) — para añadirle el modo `--probe` de forma coherente con el resto del flujo
    - `scripts/voice/styles.mjs` — `STYLES` y `PROBE_PHRASE`
    - `CLAUDE.md` §"Text-to-Speech: what's real vs assumed" — la regla del gesto de usuario de iOS, que esta página existe para poner a prueba
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-RESEARCH.md` §Pattern 2 y §Assumptions Log A1 — qué exactamente hay que demostrar en el dispositivo
  </read_first>
  <action>
Completar el modo `--probe` de `scripts/voice/generate.mjs`: para cada clave de `STYLES`,
generar `PROBE_PHRASE` con la voz Rasalgethi y ese prefijo, y escribir
`public/audio/_probe/estilo-<clave>.m4a`. El modo `--probe` NO lee ni escribe
`manifest.json` (son clips desechables, no forman parte del contrato de deriva) y no
consulta huellas.

Ejecutar `npm run voice:probe` y dejar los 3 `.m4a` versionados.

Crear `public/voice-probe.html`: HTML estático suelto (sin Vue, sin Nuxt, sin build — Vercel
y `nuxt dev` lo sirven tal cual desde `public/`), en español, fondo oscuro y tipografía
grande al estilo tablet. Contenido:
- Un `<h1>` "Prueba de estilo de voz" y una línea con la frase locutada, para que se lea
  mientras suena.
- Tres botones grandes: "A · en plano", "B · pausado e instructivo", "C · monitor de juego",
  apuntando respectivamente a `/audio/_probe/estilo-plano.m4a`, `estilo-pausado.m4a`,
  `estilo-monitor.m4a`.
- UN solo elemento `<audio>` reutilizado (mismo modelo mental que la app: solo suena una
  locución a la vez). El manejador de click debe hacer, DE FORMA SÍNCRONA y sin ningún
  `await` ni `setTimeout` de por medio: `audio.pause()`, asignar `audio.src`, y
  `audio.play()`. El `.catch(...)` de la promesa de `play()` escribe `err.name` y
  `err.message` en un `<p id="estado">` visible — ese texto es justamente el dato que el
  usuario tiene que reportar (si aparece `NotAllowedError`, la asunción A1 de RESEARCH.md
  es falsa y hay que replantear el camino de audio antes de construir la integración).
- Un comentario HTML en la cabecera del fichero explicando que es un artefacto temporal de
  la fase 03.1 y que el plan 03.1-06 lo borra.

No añadir ninguna lógica extra (ni selector de voz, ni bucle, ni analítica).
  </action>
  <verify>
    <automated>ls public/audio/_probe/*.m4a | wc -l | tr -d ' ' | grep -x 3 && grep -c "await" public/voice-probe.html | grep -x 0</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/audio/_probe/*.m4a | wc -l` devuelve 3, con nombres `estilo-plano.m4a`, `estilo-pausado.m4a`, `estilo-monitor.m4a`
    - `find public/audio/_probe -name '*.m4a' -size -10k | wc -l` devuelve 0 (ningún clip truncado o vacío)
    - `afinfo public/audio/_probe/estilo-plano.m4a | grep -c 'estimated duration'` devuelve 1 (contenedor M4A válido y legible)
    - `grep -c "await" public/voice-probe.html` devuelve 0 y `grep -c "setTimeout" public/voice-probe.html` devuelve 0
    - `grep -c "\.play()" public/voice-probe.html` devuelve 1 o más, y `grep -c "err.name\|error.name" public/voice-probe.html` devuelve 1 o más
    - `grep -c "estilo-plano\|estilo-pausado\|estilo-monitor" public/voice-probe.html` devuelve 3 o más
    - `grep -c "probe" scripts/voice/generate.mjs` devuelve 1 o más y `grep -n "probe" -A5 scripts/voice/generate.mjs | grep -c "manifest"` devuelve 0 (el modo probe no toca el manifiesto)
  </acceptance_criteria>
  <done>Existen 3 clips de la misma frase con los 3 estilos y una página estática que los reproduce dentro del gesto y muestra el nombre del error si el navegador lo rechaza.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: El usuario elige el estilo en la tablet real (D-02) — BLOQUEA el lote de 37</name>
  <read_first>
    - `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-CONTEXT.md` §D-02 — por qué este checkpoint existe y qué se decide exactamente
    - `public/voice-probe.html` — la página que el usuario va a abrir, para poder explicarle qué verá
    - `scripts/voice/styles.mjs` — los tres prefijos de estilo, para nombrarlos correctamente al usuario
  </read_first>
  <action>Presentar el checkpoint al usuario con las instrucciones de abajo y ESPERAR su respuesta. No generar el lote de 37 bajo ninguna circunstancia: D-02 lo prohíbe hasta que haya una elección de estilo explícita. Si el usuario pide probar otros prefijos, editar `STYLES` en `scripts/voice/styles.mjs`, relanzar `npm run voice:probe` y volver a presentar el checkpoint.</action>
  <acceptance_criteria>
    - El usuario ha respondido con uno de estos tres valores exactos: `plano`, `pausado` o `monitor`
    - El usuario ha indicado si apareció algún error de reproducción en la línea de estado (en particular `NotAllowedError`)
    - La elección, el resultado de reproducción y el modelo/SO de la tablet quedan escritos en `03.1-01-SUMMARY.md`
    - `ls public/audio/*.m4a 2>/dev/null | wc -l` sigue devolviendo 0 (el lote de 37 NO se ha generado en este plan)
  </acceptance_criteria>
  <what-built>
    Tres locuciones de la misma frase ("Buscad el conjunto de Archienemigo (Nemesis) de
    vuestra identidad.") con la voz Rasalgethi y tres estilos distintos, más una página
    estática que las reproduce desde un toque real en la tablet.
  </what-built>
  <how-to-verify>
    1. Servir la página. Dos opciones, la que resulte más cómoda:
       - `npm run dev -- --host` y, desde la tablet conectada a la misma wifi, abrir
         `http://<IP-del-portátil>:3000/voice-probe.html`
       - o hacer push a `main` (auto-despliegue en Vercel) y abrir
         `https://tabletop-assistant.vercel.app/voice-probe.html`
    2. En la TABLET que se usa en la mesa (no en el portátil), pulsar los tres botones A, B y C.
    3. Escuchar las tres a volumen de partida, a un brazo de distancia.
    4. Responder tres cosas:
       a) **Qué estilo se queda**: `plano`, `pausado` o `monitor`.
       b) **Si en la línea de estado apareció algún error** al pulsar (sobre todo
          `NotAllowedError`). Si no apareció nada y sonó, perfecto: eso confirma que
          `<audio>.play()` dentro de un toque funciona en este dispositivo.
       c) **Modelo y sistema operativo de la tablet** (Ajustes → Información). Es un
          bloqueante abierto desde la Fase 1 y esta es la ocasión de cerrarlo.
    5. Si NINGUNO de los tres estilos convence, dilo: se pueden probar otros prefijos sin
       tocar nada más que `scripts/voice/styles.mjs` y volver a lanzar `npm run voice:probe`.
  </how-to-verify>
  <done>El usuario ha elegido uno de los tres estilos y se sabe si `<audio>.play()` dentro de un gesto funciona en la tablet real.</done>
  <resume-signal>Responde con el estilo elegido (`plano` / `pausado` / `monitor`), si hubo error de reproducción, y el modelo/SO de la tablet. Ese estilo es el que el plan 03.1-03 fijará en `ACTIVE_STYLE` antes de generar las 37.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| máquina del desarrollador → API de Gemini | La `GEMINI_API_KEY` sale de `.env` y cruza la red en una cabecera; es el único secreto de todo el proyecto |
| repo → despliegue público | Todo lo que se commitea bajo `public/` se sirve a cualquiera que visite la URL de Vercel |
| script Node → sistema de ficheros / `afconvert` | Los ids del contenido se convierten en rutas de fichero y en argumentos de un proceso externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Information Disclosure | `GEMINI_API_KEY` en `scripts/voice/generate.mjs` | mitigate | La clave se lee con `process.loadEnvFile('.env')` (fichero ya en `.gitignore`, verificado con `git check-ignore`), viaja SOLO en la cabecera `X-goog-api-key` (nunca como `?key=` en una URL que pueda acabar en un log), y ningún `console.*` la imprime. Criterio de aceptación con grep en la Task 1 |
| T-03.1-02 | Information Disclosure | Bundle del cliente | mitigate | `scripts/` nunca es importado desde `app/` ni `engine/`; verificado con `grep -rn "generativelanguage\|GEMINI_API_KEY" app/ engine/ nuxt.config.ts` == 0 |
| T-03.1-03 | Tampering | Invocación de `afconvert` | mitigate | `execFileSync` con argumentos en array (sin shell) y validación previa de cada id contra `/^[a-z0-9]+(\.[a-z0-9-]+)*$/` — imposible construir una ruta con `..` o inyectar un argumento desde el contenido |
| T-03.1-04 | Information Disclosure | `public/voice-probe.html` y `public/audio/_probe/*` | accept | Se despliegan públicamente durante la fase; contienen solo una frase de reglas ya pública en el repo. El plan 03.1-06 los borra al cerrar la fase |
| T-03.1-05 | Denial of Service | Cuota de la API de Gemini | mitigate | Bucle secuencial con retardo configurable, reintento acotado con backoff ante 429/5xx y consolidación del manifiesto tras cada clip: una interrupción por cuota nunca corrompe el estado ni obliga a repetir trabajo pagado |
| T-03.1-SC | Tampering | npm installs | mitigate | Esta fase NO instala ningún paquete (RESEARCH.md §Package Legitimacy Audit: no aplica). Criterio de aceptación duro: `dependencies` sigue con 7 entradas y `devDependencies` con 3. Cualquier dependencia nueva es una desviación que hay que reportar, no añadir en silencio |
</threat_model>

<verification>
- `node --check` sobre los cuatro ficheros de `scripts/voice/` sale 0
- `npm run test` sigue en verde (esta fase no toca todavía ningún test)
- 3 clips de prueba existen, son M4A válidos y no están truncados
- `grep -rn "generativelanguage\|GEMINI_API_KEY" app/ engine/ nuxt.config.ts` no devuelve nada
- El usuario ha elegido un estilo y ha reportado si hubo error de reproducción en la tablet
</verification>

<success_criteria>
- `npm run voice:generate -- <id>` y `npm run voice:generate -- --force` están disponibles y documentados en el propio script
- El generador es reanudable: escribe fichero + manifiesto tras cada clip y sobrevive a un 429
- El usuario ha escuchado 3 estilos de la misma frase en la tablet real y ha elegido uno
- Se conoce si `<audio>.play()` dentro de un gesto es aceptado por el navegador de la tablet
- Cero dependencias npm nuevas
</success_criteria>

<output>
Create `.planning/phases/03.1-voz-pregenerada-en-espa-ol-con-gemini-tts/03.1-01-SUMMARY.md` when done.
Registrar explícitamente en el SUMMARY: (1) el estilo elegido por el usuario, (2) si apareció
`NotAllowedError` u otro error al reproducir en la tablet, (3) el modelo/SO de la tablet.
El plan 03.1-03 lee esos tres datos.
</output>
