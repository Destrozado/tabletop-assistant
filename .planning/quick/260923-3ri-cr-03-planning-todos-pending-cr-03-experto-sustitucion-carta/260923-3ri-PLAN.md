---
phase: quick-260923-3ri
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/voice/convert.mjs
  - scripts/voice/generate.mjs
  - engine/__tests__/voice-convert.test.ts
  - content/marvel-champions.json
  - public/audio/setup.escenario.04.expert.m4a
  - scripts/voice/manifest.json
  - .planning/todos/pending/cr-03-experto-sustitucion-cartas-rhino-ultron.md
  - .planning/todos/completed/cr-03-experto-sustitucion-cartas-rhino-ultron.md
  - .planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md
files_deleted:
  - .planning/todos/pending/cr-03-experto-sustitucion-cartas-rhino-ultron.md
autonomous: true
tags: [marvel-champions, rules-fidelity, voice-generation, content, cr-03]

must_haves:
  truths:
    - "En dificultad Experto, el paso setup.escenario.04 dice en pantalla y en voz una instrucción cierta para Rhino, Ultron y Kang: usar las cartas de villano numeradas que indica el escenario para el modo Experto (paráfrasis de RR v1.7 p. 28), sin mandar al grupo a buscar cartas «del modo Experto» que la caja de Rhino/Ultron no trae"
    - "La locución de ese paso en Experto suena con un clip recién generado cuya huella coincide con el speech nuevo (gate de deriva en verde) y ningún otro clip ni ninguna otra entrada del manifiesto cambian"
    - "El generador de voz funciona en esta máquina Linux/WSL usando ffmpeg, y en macOS sigue usando afconvert con exactamente los mismos argumentos que antes"
    - "Una partida guardada a medias sigue siendo válida tras el cambio: ids, secuencia y contentVersion 14 intactos, y los recuentos de 35 clips no cambian"
    - "CR-03 queda cerrado con su resolución razonada, y la duda de reglas que destapa (qué combinación de etapas usan Rhino/Ultron en Experto) queda capturada como todo pendiente por verificar, no perdida"
  artifacts:
    - path: "scripts/voice/convert.mjs"
      provides: "Constructor puro del comando WAV->M4A por conversor (afconvert | ffmpeg), sin efectos"
      exports: ["SUPPORTED_CONVERTERS", "buildM4aCommand"]
    - path: "scripts/voice/generate.mjs"
      provides: "Detección de conversor (afconvert primero, ffmpeg de respaldo) y conversión vía buildM4aCommand"
    - path: "engine/__tests__/voice-convert.test.ts"
      provides: "Test puro (sin procesos ni ficheros) de los argumentos de cada conversor"
    - path: "content/marvel-champions.json"
      provides: "Variante expert de setup.escenario.04 reescrita (text + speech)"
      contains: "que indica el escenario para el modo Experto"
    - path: "public/audio/setup.escenario.04.expert.m4a"
      provides: "Clip regenerado del speech nuevo (AAC LC, 24 kHz, mono, marca M4A)"
    - path: "scripts/voice/manifest.json"
      provides: "35 entradas; solo setup.escenario.04.expert con huella nueva"
    - path: ".planning/todos/completed/cr-03-experto-sustitucion-cartas-rhino-ultron.md"
      provides: "Todo CR-03 cerrado con sección de resolución"
    - path: ".planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md"
      provides: "Duda de reglas abierta, capturada para verificar contra el PDF"
  key_links:
    - from: "content/marvel-champions.json (variants.difficulty.expert.speech de setup.escenario.04)"
      to: "scripts/voice/manifest.json (entries[setup.escenario.04.expert].hash)"
      via: "npm run voice:generate -- setup.escenario.04.expert (fingerprint.mjs)"
      pattern: "setup\\.escenario\\.04\\.expert"
    - from: "scripts/voice/generate.mjs"
      to: "scripts/voice/convert.mjs"
      via: "import { SUPPORTED_CONVERTERS, buildM4aCommand }"
      pattern: "buildM4aCommand"
    - from: "scripts/voice/manifest.json"
      to: "public/audio/setup.escenario.04.expert.m4a"
      via: "un .m4a por entrada (voice-drift.test.ts: cada entrada tiene su fichero > 10000 bytes)"
      pattern: "setup\\.escenario\\.04\\.expert"
---

<objective>
Cerrar CR-03: la variante `expert` del paso `setup.escenario.04` de `content/marvel-champions.json` ordena hoy, sin condición, sustituir las cartas de villano numeradas «por las del modo Experto de este escenario». Solo Kang trae cartas de villano de modo Experto (set `exp_kang`); para Rhino y Ultron el grupo busca unas cartas que no existen, justo antes de ajustar el dial de vida. Se reescribe esa única línea (text + speech) con una redacción cierta para los tres villanos, se regenera **solo** su clip de voz con `scripts/voice/generate.mjs` y se cierra el todo.

Purpose: CLAUDE.md §Constraints — «un asistente que guía mal es peor que no tener asistente». PROJECT.md lo marca como el punto de deuda más grave abierto.

Output: generador de voz capaz de correr en esta máquina (ffmpeg como respaldo de afconvert), variante `expert` reescrita, 1 clip regenerado + manifiesto al día, todo CR-03 en `.planning/todos/completed/` y un todo nuevo con la duda de reglas que queda abierta.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/cr-03-experto-sustitucion-cartas-rhino-ultron.md
@scripts/voice/generate.mjs
@scripts/voice/wav.mjs
@scripts/voice/fingerprint.mjs
@engine/__tests__/voice-drift.test.ts

## Hechos verificados al planificar (2026-09-23, no los re-derives)

**Bloqueo de entorno que este plan resuelve.** `scripts/voice/generate.mjs` (sección 2) aborta con `process.exit(1)` si `which afconvert` falla, ANTES de cualquier llamada de red. Esta máquina es Linux/WSL2: `afconvert` no existe; `/usr/bin/ffmpeg` y `/usr/bin/ffprobe` sí, con el codificador `aac`. Todas las regeneraciones anteriores (03.1-03, 260831-pym, 260901-jg1, 260902-0oz) se hicieron en macOS. Sin el cambio de la Task 1, el ítem es imposible aquí.

**Conversión ffmpeg comprobada.** Un WAV de `wrapPcmAsWav` (24000 Hz, mono, 16 bit) convertido con los argumentos ffmpeg de la Task 1 da exactamente las mismas 5 propiedades que el clip actual hecho con afconvert (`public/audio/setup.escenario.04.expert.m4a`): `codec_name=aac`, `profile=LC`, `sample_rate=24000`, `channels=1`, `TAG:major_brand=M4A `. El gate `ffprobe … | LC_ALL=C sort | tr '\n' '|'` da exactamente `TAG:major_brand=M4A |channels=1|codec_name=aac|profile=LC|sample_rate=24000|` en ambos.

**Clave de API.** `GEMINI_API_KEY` está exportada en el entorno del shell; no hay `.env` en el checkout (y un worktree tampoco lo tendría). `generate.mjs` intenta `process.loadEnvFile('.env')` y, si falla, sigue con `process.env`: no hace falta crear ningún `.env`. Nunca imprimas, copies ni leas el valor de la clave; compruébala solo con `[ -n "$GEMINI_API_KEY" ]`.

**Línea base de tests.** `npx vitest run` en `develop` (633ef20): 36 ficheros, 1129 tests, todo verde, ~1,3 s. `npm run typecheck` verde en ~3 s. CI (`.github/workflows/ci.yml`) corre typecheck + test + playwright. Si el worktree no trae `node_modules`, `npm ci` (lockfile existente, ningún paquete nuevo) antes de la Task 1.

**Commit de referencia de los gates de diff: `633ef20`** (HEAD de `develop` al empezar el lote; ningún otro ítem del lote toca `content/`, `public/audio/`, `scripts/voice/` ni `.planning/PROJECT.md`). Los `<verify>` comparan contra él y no contra `HEAD`, para que den el mismo veredicto antes y después de commitear.

**Contratos que la redacción nueva debe respetar** (todos existentes, no los toques):
- `engine/schema.ts:33` — `text` máx. 90 caracteres; `engine/schema.ts:65` — `speech` máx. 120.
- `engine/__tests__/content.test.ts:290-299` — `findDifficultySwapStep` localiza el paso de sustitución porque su `variants.difficulty.expert.text` casa con `/cartas de villano/i`, y exige exactamente un candidato; `:315` exige que sea `setup.escenario.04`.
- `engine/__tests__/content.test.ts:244-252` — los textos normal/expert de `setup.escenario.04` deben diferir entre sí y del texto base.
- `engine/__tests__/content.test.ts:605` — `contentVersion` exactamente 14.
- Recuentos de clips: `engine/__tests__/audio-ids.test.ts:24` y `engine/__tests__/voice-drift.test.ts:107` exigen 35. El conjunto de ids no cambia (se reescribe el speech de un id existente), así que **ningún test de recuento se edita**.

**Fuente de reglas (RR v1.7).** El PDF no está en el repo (`reference/*.pdf` en `.gitignore`, ver `reference/README.md`) ni en esta máquina. Las citas ya contrastadas contra el PDF por fases anteriores son la base:
- p. 28, Modes of Play — Expert Mode: «follow the content and setup instructions for the chosen scenario, using the listed expert mode villain stages, and add the Expert encounter set to the encounter deck» (registrada en `.planning/phases/02-bucle-de-ronda-y-reglas-verificadas/02-RESEARCH.md:432`, y en `03-SPEECH-REVIEW.md:109` y `08-04-PLAN.md:85-86`).
- p. 48, Appendix I — Encounter Decks: «Expert mode … uses a different combination of villain stages and adds the expert encounter set» (registrada en `.planning/phases/03-locuci-n-por-voz-y-pantalla-siempre-encendida/03-SPEECH-REVIEW.md:117`).
- La `citation` actual del paso (p. 28, con referencias a p. 48 y p. 45) ya cubre la redacción nueva: no se toca.

## Decisiones del planificador (discreción de Claude; documéntalas en el SUMMARY)

- **DQ-1 — Redacción.** Nuevo par de la variante `expert`, literal:
  - text (83 car.): «Usad las cartas de villano numeradas (etapas) que indica el escenario para Experto.»
  - speech (82 car.): «Usad las cartas de villano numeradas que indica el escenario para el modo Experto.»
  Es la paráfrasis directa de RR p. 28 («using the listed expert mode villain stages»), y es cierta para los tres: para Kang, lo que el escenario indica en Experto son las cartas del set `exp_kang`; para Rhino y Ultron, lo que su escenario indique para Experto, sin afirmar que existan cartas «de modo Experto». Mantiene el patrón de la variante `normal` («(etapas)» solo en `text`, no en `speech`). **Se descarta** la redacción que sugería el propio todo («Si este escenario trae cartas de villano de modo Experto, sustituidlas ahora»): implica que para Rhino/Ultron no cambia nada en las cartas de villano, y RR p. 28/p. 48 no respaldan eso (Experto usa «the listed expert mode villain stages» / «a different combination of villain stages»). La redacción elegida es cierta tanto si el escenario de Rhino/Ultron indica otras etapas en Experto como si no.
- **DQ-2 — Respaldo ffmpeg en el generador** (reversible). Se prefiere a un «afconvert» falso en el PATH (truco invisible que la próxima regeneración en Linux volvería a necesitar) y a abortar el ítem. En macOS el comportamiento no cambia: afconvert sigue teniendo prioridad y con argumentos idénticos byte a byte. `generate.mjs` sigue siendo el ÚNICO escritor de `scripts/voice/manifest.json`.
- **DQ-3 — `contentVersion` se queda en 14.** Mismo razonamiento que 260902-0oz: ni ids ni secuencia cambian y el texto se resuelve siempre fresco del contenido; subirlo solo tiraría una partida en curso a cambio de nada.
- **DQ-4 — El todo se cierra en `.planning/todos/completed/`** (convención de las herramientas GSD: `check-todos.md` y `close_phase_todos` de `execute-phase.md` mueven de `pending/` a `completed/`). El directorio aún no existe: créalo.
- **DQ-5 — No se tocan `.planning/PROJECT.md` ni `.planning/STATE.md`.** Son ficheros compartidos por los ítems paralelos del lote; la anotación de CR-03 como resuelto en PROJECT.md («Deuda conocida de la Fase 8», punto 1) la hace el coordinador tras la fusión. Menciónalo en el SUMMARY.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: El generador de voz corre en Linux (ffmpeg de respaldo), sin cambiar macOS</name>
  <files>scripts/voice/convert.mjs, scripts/voice/generate.mjs, engine/__tests__/voice-convert.test.ts</files>
  <behavior>
    - buildM4aCommand('afconvert', W, M) devuelve command 'afconvert' y exactamente los argumentos históricos de generate.mjs:188: -f m4af -d aac -b 64000, luego W, luego M (8 elementos, mismo orden)
    - buildM4aCommand('ffmpeg', W, M) devuelve command 'ffmpeg'; sus argumentos contienen los pares adyacentes -c:a aac, -b:a 64k y -f ipod, contienen -y, W va inmediatamente después de -i, y M es el último elemento
    - buildM4aCommand con un conversor fuera de la lista lanza un Error cuyo mensaje nombra el conversor recibido
    - SUPPORTED_CONVERTERS es exactamente ['afconvert', 'ffmpeg'] (orden de preferencia: macOS conserva afconvert)
  </behavior>
  <action>
    Implementa DQ-2 en orden TDD (RED -> GREEN).

    RED: crea `engine/__tests__/voice-convert.test.ts` con los 4 casos de `<behavior>`, importando `SUPPORTED_CONVERTERS` y `buildM4aCommand` desde `../../scripts/voice/convert.mjs` (mismo estilo de import relativo a `.mjs` que `voice-drift.test.ts` usa con `fingerprint.mjs`). Cabecera en comentario, en español, en la línea de la de `voice-drift.test.ts`: test puro, solo compara arrays en memoria, nunca lanza procesos, nunca toca disco ni red (CI corre en ubuntu-latest sin garantía de ffmpeg y sin clave). Usa rutas ficticias como `/tmp/x.wav` y `/tmp/x.m4a`. Ejecuta el test: debe fallar porque el módulo no existe. Commit del RED.

    GREEN: crea `scripts/voice/convert.mjs`, módulo puro sin efectos al importarse y sin importar ningún módulo de Node (ni de procesos ni de ficheros): solo exporta la constante `SUPPORTED_CONVERTERS` y la función `buildM4aCommand(converter, wavPath, m4aPath)` que devuelve `{ command, args }`. Rama afconvert: los mismos 6 flags + 2 rutas que hoy están en la línea 188 de `generate.mjs`. Rama ffmpeg, en este orden: `-hide_banner`, `-loglevel`, `error`, `-y`, `-i`, wavPath, `-c:a`, `aac`, `-b:a`, `64k`, `-f`, `ipod`, m4aPath (el WAV ya viene a 24000 Hz mono de `wrapPcmAsWav`: no remuestrees ni fuerces canales). Cualquier otro valor lanza Error nombrando el conversor. JSDoc de tipos en ambos exports (mismo estilo que `fingerprint.mjs`) y una cabecera «Qué es / Qué decisión satisface / Qué NO debe hacer» como las de sus vecinos, citando 260923-3ri.

    Después modifica `scripts/voice/generate.mjs`, sin tocar nada más que esto:
    (a) la sección 2 deja de exigir afconvert: recorre `SUPPORTED_CONVERTERS` en orden con el mismo `execFileSync('which', [nombre], { stdio: 'ignore' })` de hoy y se queda con el primero que exista en una constante de módulo (p. ej. `CONVERTER`); si no hay ninguno, `console.error` en español diciendo que hace falta `afconvert` (macOS) o `ffmpeg` (Linux/WSL) y `process.exit(1)`, igual que ahora. Imprime una línea `Conversor de audio: <nombre>` con `console.log`. Renombra el título de la sección en consecuencia.
    (b) `convertPcmToM4a` pide `{ command, args }` a `buildM4aCommand(CONVERTER, tmpWavPath, m4aPath)` y llama a `execFileSync(command, args)` — siempre con array de argumentos, nunca shell. El `try/finally` que borra el WAV temporal se conserva tal cual. El título del bloque 8 deja de decir «vía afconvert».
    (c) actualiza la cabecera del fichero: donde dice que requiere macOS por `afconvert`, pasa a decir que usa `afconvert` en macOS y `ffmpeg` como respaldo en Linux/WSL (260923-3ri), y que la clave puede venir de `.env` o del entorno. El párrafo D-06 (nunca desde build/CI/Vercel) y el de «ÚNICO escritor de manifest.json» quedan intactos.
    NO cambies `MODEL`, `VOICE`, `ENDPOINT`, `RETRY_BACKOFFS_MS`, la poda de huérfanos, el cálculo de huellas, el modo `--probe`, ni el orden «primero la clave, luego el conversor». Commit del GREEN.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/voice-convert.test.ts && ! grep -Eq "^[[:space:]]*import .*node:" scripts/voice/convert.mjs && grep -q "buildM4aCommand" scripts/voice/generate.mjs && node --check scripts/voice/generate.mjs && D=$(mktemp -d) && node --input-type=module -e "import { buildM4aCommand } from './scripts/voice/convert.mjs'; import { wrapPcmAsWav } from './scripts/voice/wav.mjs'; import { writeFileSync } from 'node:fs'; import { execFileSync } from 'node:child_process'; const pcm = Buffer.alloc(48000); for (let i = 0; i < 24000; i++) pcm.writeInt16LE(Math.round(8000 * Math.sin(i / 10)), i * 2); writeFileSync(process.argv[1] + '/t.wav', wrapPcmAsWav(pcm)); const { command, args } = buildM4aCommand('ffmpeg', process.argv[1] + '/t.wav', process.argv[1] + '/t.m4a'); execFileSync(command, args);" "$D" && test "$(ffprobe -v error -show_entries stream=codec_name,profile,sample_rate,channels -show_entries format_tags=major_brand -of default=nw=1 "$D/t.m4a" | LC_ALL=C sort | tr '\n' '|')" = "TAG:major_brand=M4A |channels=1|codec_name=aac|profile=LC|sample_rate=24000|" && rm -rf "$D" && GEMINI_API_KEY=offline-dry-run node scripts/voice/generate.mjs 2>&1 | grep -Eq "Conversor de audio: (afconvert|ffmpeg)" && test -z "$(git status --porcelain -- scripts/voice/manifest.json public/audio)" && npx vitest run && npm run typecheck</automated>
  </verify>
  <done>
    `convert.mjs` existe, es puro y sus 4 casos de test pasan; `generate.mjs` elige afconvert si existe y ffmpeg si no, y convierte vía `buildM4aCommand`; la conversión real con ffmpeg de un WAV sintético da AAC LC 24 kHz mono con marca M4A; una ejecución en seco del generador (clave ficticia, contenido aún sin cambiar, así que 0 clips obsoletos y ninguna llamada de red) imprime el conversor, dice que no hay nada que generar y no modifica ni el manifiesto ni ningún `.m4a`; suite completa y typecheck en verde. Dos commits (RED y GREEN).
  </done>
</task>

<task type="auto">
  <name>Task 2: Reescribir la variante expert de setup.escenario.04 y regenerar solo su clip</name>
  <files>content/marvel-champions.json, public/audio/setup.escenario.04.expert.m4a, scripts/voice/manifest.json</files>
  <precondition>`[ -n "$GEMINI_API_KEY" ]` es verdadero, `which ffmpeg || which afconvert` encuentra un conversor, y la Task 1 está commiteada (el generador ya no exige afconvert).</precondition>
  <action>
    Implementa DQ-1 y DQ-3; esta es la única tarea que gasta cuota de la API (1 petición).

    Paso 1 — contenido. En `content/marvel-champions.json`, paso `setup.escenario.04`, cambia SOLO los dos valores de `variants.difficulty.expert`: `text` pasa a ser exactamente la frase text de DQ-1 y `speech` exactamente la frase speech de DQ-1 (copia literal del bloque de contexto, con sus tildes y el punto final). No toques el `text`/`speech` base, la variante `normal`, `title`, `citation`, ningún id, el orden de pasos ni `contentVersion` (sigue en 14). El diff debe quedar en 2 líneas cambiadas.

    Paso 2 — comprobación en rojo antes de gastar cuota. Ejecuta `npx vitest run engine/__tests__/voice-drift.test.ts`. Debe fallar exactamente 1 test (D-04) y su mensaje debe nombrar como único id obsoleto `setup.escenario.04.expert`. Ejecuta también `npx vitest run engine/__tests__/content.test.ts engine/__tests__/audio-ids.test.ts`: todo verde (regex `/cartas de villano/i`, textos distintos, presupuestos 90/120, 35 entradas). Si el drift nombra cualquier otro id, o falla cualquier otro test, PARA (ver regla de parada).

    Paso 3 — regenerar un solo clip, desde la raíz del worktree: `npm run voice:generate -- setup.escenario.04.expert`. Pasa siempre el id explícito; nunca `--force` (regeneraría 35 clips con dinero real) ni `--probe`. La salida esperada incluye `Conversor de audio: …`, `Generando 1 de 35 clips totales` y `Hecho: 1/1 clips generados.`; si dice cualquier número distinto de 1 de 35, PARA. El propio script reintenta 429/5xx (5 s, 15 s, 45 s); no lo relances en bucle: como mucho un segundo intento si el primero falló por red.

    Paso 4 — comprobaciones antes del commit (son el `<verify>`, todas contra `633ef20`): el diff del contenido es de 2 líneas, el único `.m4a` cambiado es el de este id, el manifiesto sigue con 35 entradas y solo cambia la de este id (más el `generatedAt` de cabecera), con huella igual a `fingerprint` del speech nuevo, `voice`/`model`/`style` intactos; el clip nuevo pasa el mismo gate ffprobe de 5 propiedades que los de afconvert y dura entre 2 y 12 s; suite completa en verde. No edites `manifest.json` a mano en ningún caso: solo lo escribe `generate.mjs`.

    Regla de parada (restricción del coordinador: nunca enviar texto y clip desalineados). Si el generador sale con error (clave rechazada, HTTP 4xx, cuota agotada, fallo de red, fallo del conversor), o cualquier comprobación de los pasos 2 o 4 falla: restaura los tres ficheros de esta tarea a HEAD con `git checkout -- content/marvel-champions.json scripts/voice/manifest.json public/audio/setup.escenario.04.expert.m4a`, NO commitees nada de esta tarea, NO ejecutes la Task 3 (el todo se queda en `pending/`) y termina informando del bloqueo con el mensaje de error del generador (estado HTTP y cuerpo recortado que ya imprime; jamás la clave). El commit de la Task 1 se conserva: es válido por sí solo. No entregues un arreglo solo de `text` como alternativa.

    Si todo pasa, un commit con los tres ficheros. En el SUMMARY deja anotado, como comprobación humana NO bloqueante: escuchar el clip `setup.escenario.04.expert` en la tablet en la próxima partida (es el primer clip codificado con ffmpeg y no con afconvert).
  </action>
  <verify>
    <automated>node -e 'const c=require("./content/marvel-champions.json");const s=c.sections.flatMap(x=>x.phases).flatMap(p=>p.steps).find(x=>x.id==="setup.escenario.04");const e=s.variants.difficulty.expert;const T="Usad las cartas de villano numeradas (etapas) que indica el escenario para Experto.";const S="Usad las cartas de villano numeradas que indica el escenario para el modo Experto.";if(e.text!==T||e.speech!==S)throw new Error("variante expert distinta de la especificada");if(T.length>90||S.length>120)throw new Error("presupuesto de longitud");if(c.contentVersion!==14)throw new Error("contentVersion debe seguir en 14");console.log("CONTENT-OK")' && test "$(git diff 633ef20 --numstat -- content/marvel-champions.json)" = "$(printf '2\t2\tcontent/marvel-champions.json')" && test "$(git diff 633ef20 --name-only -- public/audio)" = "public/audio/setup.escenario.04.expert.m4a" && node --input-type=module -e 'import { readFileSync } from "node:fs"; import { execFileSync } from "node:child_process"; import { fingerprint } from "./scripts/voice/fingerprint.mjs"; const id = "setup.escenario.04.expert"; const S = "Usad las cartas de villano numeradas que indica el escenario para el modo Experto."; const now = JSON.parse(readFileSync("scripts/voice/manifest.json", "utf-8")); const before = JSON.parse(execFileSync("git", ["show", "633ef20:scripts/voice/manifest.json"], { encoding: "utf-8" })); if (Object.keys(now.entries).length !== 35 || Object.keys(before.entries).length !== 35) throw new Error("el manifiesto debe seguir con 35 entradas"); for (const k of Object.keys(before.entries)) { if (k !== id && JSON.stringify(now.entries[k]) !== JSON.stringify(before.entries[k])) throw new Error("entrada tocada sin motivo: " + k) } for (const f of ["voice", "model", "style"]) { if (now[f] !== before[f]) throw new Error("metadato cambiado: " + f) } if (now.entries[id].hash !== fingerprint(S)) throw new Error("huella no coincide con el speech nuevo"); console.log("MANIFEST-OK")' && test "$(ffprobe -v error -show_entries stream=codec_name,profile,sample_rate,channels -show_entries format_tags=major_brand -of default=nw=1 public/audio/setup.escenario.04.expert.m4a | LC_ALL=C sort | tr '\n' '|')" = "TAG:major_brand=M4A |channels=1|codec_name=aac|profile=LC|sample_rate=24000|" && ffprobe -v error -show_entries format=duration -of csv=p=0 public/audio/setup.escenario.04.expert.m4a | awk '{ if ($1 > 2 && $1 < 12) print "DUR-OK " $1; else { print "DUR-BAD " $1; exit 1 } }' && npx vitest run</automated>
  </verify>
  <done>
    La variante `expert` de `setup.escenario.04` dice literalmente las frases de DQ-1; el clip `public/audio/setup.escenario.04.expert.m4a` es nuevo (AAC LC 24 kHz mono, 2-12 s) y su huella en el manifiesto coincide con el speech nuevo; ninguna otra entrada del manifiesto ni ningún otro `.m4a` ha cambiado; 35 entradas; `contentVersion` 14; suite completa en verde (incluido `voice-drift.test.ts`). O bien, si algo falló: los tres ficheros restaurados a HEAD, sin commit de esta tarea, y el bloqueo informado.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cerrar el todo CR-03 y capturar la duda de reglas que queda abierta</name>
  <files>.planning/todos/pending/cr-03-experto-sustitucion-cartas-rhino-ultron.md, .planning/todos/completed/cr-03-experto-sustitucion-cartas-rhino-ultron.md, .planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md</files>
  <precondition>El commit de la Task 2 existe (texto y clip alineados). Si la Task 2 terminó por la regla de parada, esta tarea NO se ejecuta.</precondition>
  <action>
    Implementa DQ-4.

    (a) Crea `.planning/todos/completed/` y mueve con `git mv` el todo `cr-03-experto-sustitucion-cartas-rhino-ultron.md` desde `pending/`. En su frontmatter cambia `status` a `completed` y añade `resolved: 2026-09-23` y `resolved_by: quick-260923-3ri` (el resto del frontmatter y todo el cuerpo original quedan intactos). Añade al final una sección `## Resolución (quick 260923-3ri, 2026-09-23)` en español con: la ruta elegida (reescribir la variante a una redacción cierta para los tres villanos: un clip nuevo, no condicionar por villano); el texto anterior y el nuevo par text/speech; la justificación con la cita corta de RR v1.7 p. 28 tal como está registrada en `02-RESEARCH.md:432` (cita breve, uso privado); por qué se descartó la redacción condicional que proponía este mismo todo (DQ-1); que `contentVersion` sigue en 14 (DQ-3); que el clip se regeneró con `generate.mjs` usando el respaldo ffmpeg de la Task 1; que los recuentos de 35 clips no cambiaron; y un enlace al todo nuevo del punto (b). Añade también una nota de que la afirmación del apartado «Qué pasa» («lo único que cambia es que se añade el set de encuentros Experto») queda sin confirmar y se traslada al todo nuevo.

    (b) Crea `.planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md` con el mismo formato de frontmatter que el todo CR-03 (`id` igual al nombre sin extensión, `created: 2026-09-23`, `source: quick 260923-3ri (análisis de CR-03)`, `severity: high`, `area: fidelidad de reglas`, `status: pending`). Cuerpo en español, redactado como pregunta POR VERIFICAR, nunca como hecho: RR v1.7 p. 28 dice que en Experto se usan «the listed expert mode villain stages» y p. 48 que Experto usa «a different combination of villain stages»; el repo asume en cambio que para Rhino y Ultron las etapas no cambian en Experto (`engine/types.ts` en el comentario de `VillainStage` sobre la ausencia de `expert`, `scripts/catalogue/fetch-marvelcdb.mjs` en el comentario de `VILLAIN_STAGE_CARDS`, y `engine/__tests__/characters.test.ts` en el test «Rhino y Ultron: ninguna etapa lleva la clave expert»), y `engine/counters.ts` (`computeInitialVillainHealth`, D-11) precarga siempre la etapa I. Pregunta concreta: ¿la hoja de preparación del escenario de Rhino y de Ultron (Core Set) indica para Experto una combinación de etapas distinta de la estándar (por ejemplo, empezar en la etapa II)? Consecuencias SI se confirma: la cifra del dial en Experto para Rhino/Ultron sería la de la etapa I del catálogo y no la de la etapa con la que se empieza (hoy en el catálogo: Rhino 14 frente a 15 por héroe; Ultron 17 frente a 22 por héroe), y la variante `normal` («tal como vienen con el escenario») podría quedarse corta si el modo estándar no usa las tres etapas. Cómo verificarlo: el PDF de RR v1.7 (p. 28 y p. 48, con `pdftotext -layout -f N -l N`, ver `reference/README.md`) y el reglamento o la hoja de escenario físicos del Core Set. Termina con «No tocar el motor ni el catálogo hasta confirmarlo con el reglamento.»

    No toques `.planning/PROJECT.md` ni `.planning/STATE.md` (DQ-5). Un commit con los tres cambios de rutas.
  </action>
  <verify>
    <automated>test -f .planning/todos/completed/cr-03-experto-sustitucion-cartas-rhino-ultron.md && test ! -e .planning/todos/pending/cr-03-experto-sustitucion-cartas-rhino-ultron.md && grep -q "^status: completed$" .planning/todos/completed/cr-03-experto-sustitucion-cartas-rhino-ultron.md && grep -q "^resolved_by: quick-260923-3ri$" .planning/todos/completed/cr-03-experto-sustitucion-cartas-rhino-ultron.md && grep -q "## Resolución (quick 260923-3ri" .planning/todos/completed/cr-03-experto-sustitucion-cartas-rhino-ultron.md && grep -q "que indica el escenario para el modo Experto" .planning/todos/completed/cr-03-experto-sustitucion-cartas-rhino-ultron.md && test -f .planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md && grep -q "^status: pending$" .planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md && grep -q "computeInitialVillainHealth" .planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md && git ls-files --error-unmatch .planning/todos/completed/cr-03-experto-sustitucion-cartas-rhino-ultron.md .planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md && test -z "$(git ls-files -- .planning/todos/pending/cr-03-experto-sustitucion-cartas-rhino-ultron.md)" && git diff --quiet 633ef20 -- .planning/PROJECT.md</automated>
  </verify>
  <done>
    El todo CR-03 vive en `.planning/todos/completed/` con `status: completed`, `resolved_by: quick-260923-3ri` y una sección de resolución razonada; `pending/` ya no lo contiene; existe el todo nuevo por verificar sobre la combinación de etapas en Experto para Rhino/Ultron; PROJECT.md y STATE.md sin tocar; commit hecho.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> API de Gemini TTS | Una única petición HTTPS saliente con la clave en la cabecera `X-goog-api-key`; la respuesta (audio en base64) se escribe en disco |
| entorno del shell -> ficheros versionados | La clave vive solo en el entorno; nada de lo que se commitea puede contenerla |
| generate.mjs -> binario del sistema (afconvert/ffmpeg) | Se lanza un proceso externo con rutas derivadas de ids del contenido |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3ri-01 | Information disclosure | GEMINI_API_KEY en salida, SUMMARY o commits | high | mitigate | La presencia se comprueba solo con `[ -n "$GEMINI_API_KEY" ]`; prohibido `echo`/`env`/`printenv`/leer `.env`; `generate.mjs` nunca imprime la clave (solo estado HTTP y 200 caracteres del cuerpo de error); el dry-run de la Task 1 usa una clave ficticia; el informe de bloqueo de la Task 2 copia solo el mensaje del generador |
| T-3ri-02 | Tampering / Elevation | `buildM4aCommand` + `execFileSync` en generate.mjs | medium | mitigate | Conversor elegido de una lista cerrada (`SUPPORTED_CONVERTERS`), valor desconocido lanza; `execFileSync` siempre con array de argumentos, nunca shell; los ids pasan por `assertValidId` (`ID_PATTERN`) antes de convertirse en nombre de fichero (sin cambios) |
| T-3ri-03 | Tampering (integridad del contenido) | texto y clip desalineados en producción | medium | mitigate | Gate `voice-drift.test.ts` (D-04) en CI + comprobación en rojo previa (solo 1 id obsoleto) + regla de parada de la Task 2 que restaura los tres ficheros a HEAD y no commitea |
| T-3ri-04 | Tampering | `scripts/voice/manifest.json` editado a mano | low | mitigate | `generate.mjs` sigue siendo el único escritor; el verify compara el manifiesto contra `HEAD` entrada a entrada (solo cambia la del id regenerado) |
| T-3ri-05 | Denial of service (coste) | cuota/saldo de la API de Gemini | low | mitigate | Una sola petición: id explícito, prohibido `--force` y `--probe`, como mucho un reintento manual; el script ya acota 429/5xx a 3 reintentos |
| T-3ri-SC | Tampering | instalación de paquetes | low | accept | El plan no instala ningún paquete npm/pip/cargo; ffmpeg es un binario del sistema ya presente (`/usr/bin/ffmpeg`). Si faltara, se PARA en vez de instalar nada |
</threat_model>

<verification>
- `npx vitest run` verde: los 1129 tests de la línea base + los nuevos de `voice-convert.test.ts`, incluidos `voice-drift.test.ts` (huellas al día, 35 entradas, cada entrada con su `.m4a` > 10000 bytes) y `content.test.ts` (paso de sustitución localizado por `/cartas de villano/i`, textos distintos, `contentVersion` 14).
- `npm run typecheck` verde.
- `git diff 633ef20 --stat` (o contra la base del worktree) muestra solo los ficheros de `files_modified`: 3 de la Task 1, 3 de la Task 2, los movimientos de todo de la Task 3.
- Fidelidad de reglas: la frase nueva es la paráfrasis de RR v1.7 p. 28 («using the listed expert mode villain stages»), cierta para Rhino, Ultron y Kang; la `citation` del paso (p. 28) la sigue respaldando.
</verification>

<success_criteria>
- En Experto, `setup.escenario.04` ya no manda buscar cartas «del modo Experto» para Rhino/Ultron; dice en pantalla y en voz que se usen las cartas de villano numeradas que indica el escenario para Experto.
- Exactamente un clip regenerado (`setup.escenario.04.expert`) y su entrada de manifiesto; los otros 34 intactos.
- El generador de voz funciona en Linux/WSL con ffmpeg y en macOS sin cambios.
- Todo CR-03 cerrado en `completed/` con resolución; duda de reglas abierta capturada en `pending/`.
- Si la regeneración no fue posible: nada de la Task 2/3 commiteado y el bloqueo informado (nunca texto sin su clip).
</success_criteria>

<output>
Create `.planning/quick/260923-3ri-cr-03-planning-todos-pending-cr-03-experto-sustitucion-carta/260923-3ri-SUMMARY.md` when done
</output>
