---
phase: quick-260902-0oz
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/marvel-champions.json
  - scripts/voice/manifest.json
  - public/audio/setup.encuentros.01.m4a
  - public/audio/setup.escenario.01.m4a
  - public/audio/setup.escenario.06.m4a
  - public/audio/setup.escenario.07.m4a
  - public/audio/setup.escenario.08.m4a
autonomous: true
requirements: [CONT-fidelidad-terminologia]
tags: [marvel-champions, rules-fidelity, voice-generation, content]

must_haves:
  truths:
    - "El jugador lee «Plan Principal» en los 5 pasos que hablan de la carta/mazo de plan principal, y puede localizar esa carta física por su nombre en español"
    - "Ningún paso dice ya «carta de escenario» ni «mazo de escenario principal»"
    - "Los usos legítimos de «escenario» (el escenario como conjunto: título de sección, cartas de etapa, ids) siguen intactos"
    - "Cada frase `speech` cambiada suena con su clip pregenerado al día (el gate de deriva pasa en verde)"
    - "Una partida guardada en curso sigue siendo válida tras el cambio (ids y secuencia no cambian)"
  artifacts:
    - path: "content/marvel-champions.json"
      provides: "5 pasos renombrados a «Plan Principal» en title/text/speech"
      contains: "Plan Principal"
    - path: "scripts/voice/manifest.json"
      provides: "35 entradas, huellas al día de los 5 ids regenerados"
  key_links:
    - from: "content/marvel-champions.json (campo speech)"
      to: "scripts/voice/manifest.json (fingerprint)"
      via: "npm run voice:generate -- <ids>"
      pattern: "fingerprint"
    - from: "scripts/voice/manifest.json"
      to: "public/audio/*.m4a"
      via: "un .m4a por entrada del manifiesto"
      pattern: "setup\\.escenario\\.0[1678]"
---

<objective>
Renombrar como **Plan Principal** las 5 referencias del contenido de preparación de Marvel Champions que hoy llaman «carta de escenario», «mazo de escenario principal» o «el escenario» a lo que en las cartas físicas en español se llama literalmente **Plan Principal**.

Purpose: el usuario se quedó bloqueado en el paso de reunir conjuntos de encuentro porque buscaba una «carta de escenario» que no existe con ese nombre en la caja — la lista «Contenido:» está impresa en la cara 1A del Plan Principal. Un asistente que nombra mal la carta que hay que buscar es peor que no tener asistente.

Output: `content/marvel-champions.json` con 5 pasos reescritos (title/text/speech), 5 clips de audio regenerados y `scripts/voice/manifest.json` con las huellas al día. Sin cambios de ids, de estructura ni de `contentVersion`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260901-jg1-fusionar-pasos-de-apartar-el-conjunto-de/260901-jg1-SUMMARY.md

Fichero a editar: `content/marvel-champions.json`
Reglas oficiales en local: `reference/mc_rulesreference_v17-compressed.pdf` (no versionado; ver `~/.claude/projects/-Users-vcompanyb-TableGameAssistant/memory/rules-reference-pdf.md`)

<interfaces>
Topes duros de `engine/schema.ts` (StepSchema) — verificados en la planificación:
- `text`: `z.string().min(1).max(90)`
- `speech`: `z.string().max(120).optional()`
- `warning`: `z.string().max(60).optional()`
- `title`: `z.string().min(1)` — sin tope; el título más largo del fichero hoy mide 51 caracteres ("Resolver Preparación y Cuando se revela del villano"), así que 44 se queda dentro de lo ya probado en la cabecera de tablet.

CLI del generador de voz (`scripts/voice/generate.mjs`, líneas 75-80): acepta varios ids sueltos como argumentos posicionales; `--force` regenera sin mirar huellas, `--delay=<ms>` (por defecto 1500) espacia las llamadas. `runBatch()` poda por su cuenta las entradas huérfanas del manifiesto.
</interfaces>
</context>

<rules_basis>
El orquestador ya contrastó estas citas contra el Rules Reference v1.7, Apéndice II (p.49). **No hace falta rederivarlas**; sí conviene reconfirmarlas barato antes de tocar el JSON (`pdftotext -layout reference/mc_rulesreference_v17-compressed.pdf - | grep -n -i "main scheme"`), igual que hizo 260901-jg1:

- **Paso 8:** "Select Scenario. Select a scenario and put its **villain deck and main scheme deck** into play..." → legitima «mazo de villano y mazo de Plan Principal» en `setup.escenario.01`.
- **Paso 10:** "Create the Encounter Deck. Shuffle the encounter sets **listed on side 1A of the main scheme card**..." → legitima «indicados en el Plan Principal, cara 1A» en `setup.encuentros.01`. Este es el paso que bloqueó al usuario.
- **Paso 12a:** "Resolve any 'Setup' abilities **on main scheme card 1A**." → `setup.escenario.06`.
- **Paso 12b:** "**Flip the main scheme card to side 1B** and resolve any 'When Revealed' abilities on that side." → `setup.escenario.07` (nótese: la cara es **1B**, no «B» a secas como dice hoy el texto) y `setup.escenario.08`.

Término oficial en español confirmado por el usuario contra sus cartas físicas: **«Plan Principal»** (p.ej. Rino 1A, bloque "Contenido:").
</rules_basis>

<tasks>

<task type="auto">
  <name>Task 1: Renombrar a «Plan Principal» los 5 pasos afectados</name>
  <files>content/marvel-champions.json</files>
  <action>
Reconfirma primero las cuatro citas de `<rules_basis>` con `pdftotext -layout` + grep (barato, ~1 min) y déjalo anotado en el SUMMARY. Si alguna no casa, PARA y reporta antes de editar.

Aplica exactamente las sustituciones del `<content_spec>` de abajo. Son ediciones quirúrgicas de `title`, `text` y `speech`. **NO** toques ningún `id`, `kind`, `citation`, `warning`, ni el orden o número de pasos.

Decisión ya tomada, **no la revisites**: `contentVersion` se queda en **13**, no sube. `resume()` (`engine/persistence.ts:78`) descarta una partida guardada en cuanto `contentVersion` difiere; aquí ni los ids ni la secuencia cambian, y el texto se resuelve siempre fresco del contenido, así que subirlo solo serviría para tirar la partida de alguien a media mesa a cambio de nada. Documenta este razonamiento en el SUMMARY. Corolario: `engine/__tests__/content.test.ts:537` (`contentVersion es exactamente 13`) no se toca.

Guardarraíl de alcance — **no** renombres los usos legítimos de «escenario» que significan el escenario como conjunto, todos ellos verificados en la planificación y que deben sobrevivir intactos:
- `"section": "Apéndice II, pasos 1 y 8 (decisión de escenario adelantada)"` (~línea 27)
- el id `setup.escenario`, su `title` `"ESCENARIO DEL VILLANO"` y su `summaryLabel` `"Escenario del villano preparado"`
- todos los ids `setup.escenario.NN`
- las variantes de dificultad de `setup.escenario.04` ("...tal como vienen con el escenario", "...del modo Experto de este escenario") — ahí «escenario» sí significa la caja/escenario entero
- los `citation.section` de los pasos tocados (ya mencionan «Main Scheme» y «cara 1B» donde toca; se quedan como están)

Tras editar, deja constancia del RED esperado ejecutando `npx vitest run` (si falla con `TSCONFIG_ERROR`, ejecuta antes `npx nuxt prepare` — worktree fresco sin `.nuxt/`, precedente de 260901-jg1). El patrón esperado es: **solo** `voice-drift.test.ts` en rojo, quejándose de 5 huellas desactualizadas (`setup.encuentros.01`, `setup.escenario.01`, `.06`, `.07`, `.08`); **cero** huérfanos y **cero** descuadres de recuento, porque ningún id cambia. Si aparece algún otro fichero de test en rojo, o algún fallo de recuento/huérfano, PARA: significa que se ha tocado estructura sin querer.
  </action>
  <verify>
    <automated>node -e "const c=require('./content/marvel-champions.json');const s=JSON.stringify(c);if(/carta de escenario|mazo de escenario principal/.test(s))throw new Error('quedan referencias antiguas');if(!/ESCENARIO DEL VILLANO/.test(s))throw new Error('se ha perdido el titulo de seccion legitimo');if(c.contentVersion!==13)throw new Error('contentVersion no debe cambiar');const all=c.sections.flatMap(x=>x.phases).flatMap(p=>p.steps);const ids=['setup.encuentros.01','setup.escenario.01','setup.escenario.06','setup.escenario.07','setup.escenario.08'];for(const id of ids){const st=all.find(x=>x.id===id);if(!st)throw new Error('falta '+id);if(!/Plan Principal/.test(st.text+st.speech+st.title))throw new Error(id+' no menciona Plan Principal');if(st.text.length>90)throw new Error(id+' text '+st.text.length+'>90');if(st.speech.length>120)throw new Error(id+' speech >120');}console.log('OK 5 pasos renombrados, sin residuos, contentVersion 13');"</automated>
    <automated>npx vitest run engine/__tests__/schema.test.ts engine/__tests__/content.test.ts engine/__tests__/navigator.test.ts engine/__tests__/header.test.ts engine/__tests__/audio-ids.test.ts</automated>
  </verify>
  <done>Las 5 sustituciones aplicadas literalmente según `<content_spec>`; `npx vitest run` deja en rojo **únicamente** las 5 huellas de `voice-drift.test.ts` y todo lo demás en verde; `contentVersion` sigue en 13; las citas reconfirmadas contra el PDF.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerar los 5 clips de audio y cerrar la suite en verde</name>
  <files>scripts/voice/manifest.json, public/audio/setup.encuentros.01.m4a, public/audio/setup.escenario.01.m4a, public/audio/setup.escenario.06.m4a, public/audio/setup.escenario.07.m4a, public/audio/setup.escenario.08.m4a</files>
  <action>
Prerrequisito: `.env` con `GEMINI_API_KEY` debe existir en el directorio de trabajo. Si es un worktree fresco no lo tendrá (gitignored, no se propaga entre worktrees) — cópialo desde el checkout principal con `cp /Users/vcompanyb/TableGameAssistant/.env .`, sin imprimir nunca su contenido, y confirma con `git status --short .env` que sigue sin trackear. Precedente literal en 260901-jg1.

Ejecuta la regeneración de los 5 clips cuyo `speech` cambió, **sin `--force`** (así el propio generador confirma por huella que son exactamente esos 5 y ni uno más):

`npm run voice:generate -- setup.encuentros.01 setup.escenario.01 setup.escenario.06 setup.escenario.07 setup.escenario.08`

Genera dinero real; si la API devuelve 429 o error de crédito, PARA y repórtalo en vez de reintentar en bucle.

Comprobaciones posteriores:
- `ls public/audio/*.m4a | wc -l` debe seguir dando **35** (no se crea ni se borra ningún clip: ningún id cambió, así que no debe aparecer ningún huérfano podado).
- `scripts/voice/manifest.json` debe seguir con **35** entradas; su único diff aceptable es hash/bytes/generatedAt de los 5 ids regenerados más el `generatedAt` de cabecera. **Cero ediciones manuales** del manifiesto.
- Ningún `.m4a` nuevo de 0 bytes ni por debajo del umbral de 10 KB del gate.

No se puede verificar de oído en un entorno headless. Anota en el SUMMARY que queda **pendiente de oído humano en la próxima partida** que Gemini pronuncie bien «Plan Principal» y sobre todo «cara 1A» / «cara 1B» (riesgo real de que lea «uno A» de forma rara) — mismo tratamiento que dio 260901-jg1 a «Archienemigo».

Commits, en la línea de los de 260901-jg1:
- `fix(260902-0oz): renombrar carta y mazo de escenario como Plan Principal` (contenido)
- `feat(260902-0oz): regenerar los 5 clips de Plan Principal y cerrar la suite en verde` (audio + manifiesto)
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>test "$(ls public/audio/*.m4a | wc -l | tr -d ' ')" = "35" && node -e "const m=require('./scripts/voice/manifest.json');const e=m.entries??m;const n=Array.isArray(e)?e.length:Object.keys(e).length;if(n!==35)throw new Error('manifiesto con '+n+' entradas, se esperaban 35');console.log('OK 35 clips / 35 entradas');"</automated>
  </verify>
  <done>`npm test` en verde al 100% (incluido `voice-drift.test.ts`); 35 `.m4a` y 35 entradas de manifiesto, igual que antes; los 5 clips regenerados pesan bien por encima de 10 KB; dos commits creados; pendiente auditivo documentado en el SUMMARY.</done>
</task>

</tasks>

<content_spec>
Sustituciones literales. Izquierda = valor actual en `content/marvel-champions.json`, derecha = valor nuevo. Longitudes ya medidas contra los topes de `engine/schema.ts`.

### 1. `setup.encuentros.01` (~líneas 83-93) — el paso que bloqueó al usuario

| Campo | Nuevo valor | Long. |
|---|---|---|
| `title` | `Reunir conjuntos de encuentro` | 29 |
| `text` | `Reunid los conjuntos de encuentro indicados en el Plan Principal, cara 1A.` | 74/90 |
| `speech` | `Reunid los conjuntos de encuentro que indique el Plan Principal, cara 1A.` | 73/120 |

`citation` (Apéndice II, paso 10, p.49) se queda igual.

### 2. `setup.escenario.01` (~líneas 163-173)

| Campo | Nuevo valor | Long. |
|---|---|---|
| `title` | `Colocar mazos de villano y Plan Principal` | 41 |
| `text` | `Colocad el mazo de villano y el mazo de Plan Principal en el centro de la mesa.` | 79/90 |
| `speech` | `Colocad el mazo de villano y el de Plan Principal en el centro de la mesa.` | 74/120 |

`citation` (Apéndice II, paso 8, p.49) se queda igual.

### 3. `setup.escenario.06` (~líneas 235-245)

| Campo | Nuevo valor | Long. |
|---|---|---|
| `title` | `Resolver Preparación del Plan Principal (1A)` | 44 |
| `text` | `Resolved cualquier habilidad de Preparación en el Plan Principal, cara 1A.` | 74/90 |
| `speech` | `Resolved la habilidad de Preparación del Plan Principal, cara 1A.` | 65/120 |

`citation` (Apéndice II, paso 12a, p.49) se queda igual.

### 4. `setup.escenario.07` (~líneas 247-257) — además corrige «cara B» → «cara 1B»

| Campo | Nuevo valor | Long. |
|---|---|---|
| `title` | `Voltear a cara 1B y colocar amenaza` | 35 |
| `text` | `Voltead el Plan Principal a su cara 1B y colocad la amenaza inicial indicada.` | 77/90 |
| `speech` | `Voltead el Plan Principal a su cara 1B y colocad la amenaza inicial indicada.` | 77/120 |

`citation` ya dice «paso 12b (voltear a cara 1B)» — se queda igual; el texto pasa a coincidir con ella, que hoy no lo hacía.

### 5. `setup.escenario.08` (~líneas 259-269)

| Campo | Nuevo valor | Long. |
|---|---|---|
| `title` | `Resolver Cuando se revela del Plan Principal` | 44 |
| `text` | `Resolved cualquier habilidad de Cuando se revela en esa cara del Plan Principal.` | 80/90 |
| `speech` | `Resolved la habilidad de Cuando se revela de esa cara del Plan Principal.` | 73/120 |

`citation` (Apéndice II, paso 12b + "Main Scheme" p.27) se queda igual.
</content_spec>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → cliente | El JSON de contenido se prerenderiza al bundle; no hay entrada de usuario que lo atraviese |
| desarrollador → API de Gemini TTS | `scripts/voice/generate.mjs` envía las frases `speech` a una API externa con `GEMINI_API_KEY` desde `.env` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0oz-01 | Information Disclosure | `.env` con `GEMINI_API_KEY` copiado entre worktrees | mitigate | Nunca imprimir el contenido de `.env`; confirmar con `git status --short .env` que sigue sin trackear antes de commitear |
| T-0oz-02 | Tampering | `content/marvel-champions.json` | accept | Contenido estático versionado, validado por Zod (`schema.test.ts`) en CI; sin superficie de red ni entrada de usuario |
| T-0oz-03 | Tampering | Deriva texto↔audio (el clip dice una cosa, la pantalla otra) | mitigate | `voice-drift.test.ts` compara huella de `speech` contra manifiesto; la Tarea 1 exige ver el RED de las 5 huellas antes de regenerar |
| T-0oz-SC | Tampering | Instalaciones npm/pip/cargo | mitigate | N/A — este plan no instala ningún paquete nuevo |
</threat_model>

<verification>
1. `npm test` — 14 ficheros, todo en verde (294 tests, el recuento no debe cambiar: no se añade ni se quita ningún paso).
2. `grep -c "Plan Principal" content/marvel-champions.json` → al menos 5 apariciones distribuidas por los 5 pasos.
3. `grep -c "carta de escenario\|mazo de escenario principal" content/marvel-champions.json` → 0.
4. `grep -c "ESCENARIO DEL VILLANO" content/marvel-champions.json` → 1 (los usos legítimos sobreviven).
5. `ls public/audio/*.m4a | wc -l` → 35, sin ficheros de 0 bytes.
6. `git diff --stat` sobre `content/marvel-champions.json` — solo líneas de `title`/`text`/`speech`, ninguna de `id`, `kind`, `citation` ni `contentVersion`.
</verification>

<success_criteria>
- Los 5 pasos dicen «Plan Principal» en pantalla y en voz; el jugador puede buscar esa carta por su nombre real en español.
- `setup.escenario.07` dice «cara 1B», coherente con su propia cita y con el Rules Reference.
- Cero referencias residuales a «carta de escenario» / «mazo de escenario principal».
- Los usos legítimos de «escenario» intactos (sección, cartas de etapa, ids, citas).
- `contentVersion` sigue en 13 y ninguna partida guardada queda invalidada; la decisión está razonada en el SUMMARY.
- Suite completa en verde, 35 clips y 35 entradas de manifiesto, sin ediciones manuales del manifiesto.
</success_criteria>

<output>
Crear `.planning/quick/260902-0oz-renombrar-las-referencias-a-la-carta-y-m/260902-0oz-SUMMARY.md` al terminar.
</output>
</content>
</invoke>
