---
phase: quick-260923-3rn
plan: "01"
type: execute
wave: 1
depends_on: ["260923-3rl", "260923-3rm"]
files_modified:
  - app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts
  - .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
  - .planning/ROADMAP.md
autonomous: true
tags: [phase-9, gate, invariantes, tests, mutation-proof]

must_haves:
  truths:
    - "Si `onResumeContinue` de `app/pages/[game]/index.vue` deja de llamar a `clearProgressMismatch(gameId)`, `npx vitest run` se pone rojo (Pata 6) — demostrado por mutación ejecutada en disco y revertida"
    - "Si `onContentChangedAcknowledge` de `app/pages/[game]/index.vue` deja de llamar a `clearProgressMismatch(gameId)`, `npx vitest run` se pone rojo (Pata 6) — demostrado por mutación ejecutada en disco y revertida"
    - "Un lector cuyo ÚNICO parámetro es de tipo genérico con coma (`Record<string, string>`) ya no pasa la Pata 1 como si exigiera testigo, y una llamada de un solo argumento con `Map<string, string>` dentro ya no pasa la Pata 2"
    - "Una marca de estado de módulo declarada con anotación de tipo explícita (`const X: Map<string, string> = new Map()`, incluida una anotación que contiene `=>`) entra sola en el descubrimiento de `marcasDeEstadoDeModuloDe`"
    - "Cada uno de los dos arreglos de parsing tiene una mutación ejecutada y revertida (volver temporalmente al código viejo) que pone rojos sus casos sintéticos"
    - "`app/pages/[game]/index.vue` queda sin cambios netos; la suite completa, `npx tsc --noEmit` y `npm run typecheck` salen en verde"
    - "`deferred-items.md` registra el cierre de los tres puntos con el id 260923-3rn y el ROADMAP deja de listarlos como deuda abierta"
  artifacts:
    - path: "app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts"
      provides: "Pata 6 (retiradas explícitas documentadas por el retirador) + `argumentosDeNivelSuperior` con profundidad angular + `PATRON_ESTADO_DE_MODULO_MUTABLE` con anotación de tipo opcional"
      contains: "funcionesSinRetiradaDe"
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md"
      provides: "Cierre de hallazgo 3 (ronda 8 / 09-38) y de los dos huecos de parsing, con las mutaciones registradas"
      contains: "260923-3rn"
  key_links:
    - from: "invariantesDeMarcaDeEstado.test.ts (Pata 6)"
      to: "app/composables/useProgressMismatchMark.ts (bloque de comentario sobre `export function clearProgressMismatch`)"
      via: "llamantesDocumentadosDelRetirador"
      pattern: "llamantesDocumentadosDelRetirador"
    - from: "invariantesDeMarcaDeEstado.test.ts (Pata 6)"
      to: "app/pages/[game]/index.vue (cuerpos de onResumeContinue / onContentChangedAcknowledge)"
      via: "consumidoresDeLaMarca + cuerpoDeFuncionDeclarada"
      pattern: "cuerpoDeFuncionDeclarada"
    - from: "argumentosDeNivelSuperior"
      to: "contratoDeLaMarcaDe (Pata 1) / lecturasSinTestigoDe (Pata 2)"
      via: "separación de parámetros y argumentos de nivel superior"
      pattern: "argumentosDeNivelSuperior"
---

<objective>
Cerrar la deuda no bloqueante que la ronda 10 de `09-VERIFICATION.md` (frontmatter `advisory`) y el ROADMAP (entrada de la Fase 9) dejaron sobre el gate de invariantes de ciclo de vida `app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts`:

1. **La segunda defensa sin test** (`09-REVIEW.md` WR-03; `deferred-items.md`, sección «Ronda 8 / plan 09-38», hallazgo 3): hoy, si se borra `clearProgressMismatch(gameId)` de `onResumeContinue` o de `onContentChangedAcknowledge` (`app/pages/[game]/index.vue`), no se pone rojo ningún test. Se añade una **Pata 6** estructural que lo pone rojo.
2. **Hueco de parsing 1** (`09-REVIEW.md` WR-01): `argumentosDeNivelSuperior` separa por comas dentro de `<...>`, así que un tipo genérico infla la aridad y da un falso verde en las Patas 1 y 2.
3. **Hueco de parsing 2** (`09-REVIEW.md` WR-02): `PATRON_ESTADO_DE_MODULO_MUTABLE` rechaza `const X: Tipo = ...`, así que una marca con anotación de tipo explícita se salta el descubrimiento.

Cada cierre se demuestra con una **mutación ejecutada y revertida**, y su salida literal queda en el SUMMARY.

Purpose: a este gate se le exige que la décima cara del defecto de la Fase 9 la encuentre una ejecución de la suite y no otra ronda de revisión humana. Hoy tiene dos agujeros de falso verde y una defensa documentada como «independiente» que puede desaparecer sin que nada lo note.

Output: el gate con la Pata 6 y los dos arreglos de parsing, `deferred-items.md` con el cierre registrado y el ROADMAP al día. `app/pages/[game]/index.vue` queda sin cambios netos.

Decisión a criterio del planificador: la vía es una **pata nueva en el gate**, no montar la página. Montar `app/pages/[game]/index.vue` exigiría instalar `@vue/test-utils` y `happy-dom`, que hoy no están en `node_modules`, y pasar al entorno `nuxt` de Vitest, que `vitest.config.ts` evita por escrito. Encima, la página arrastra wake lock, audio, contenido, ruta y `ClientOnly`. Eso supone un coste y un riesgo que no se justifican para demostrar una sola llamada. Además, la pata es lo que `deferred-items.md` sugiere literalmente como alternativa: «exigir, para cada rama de invalidación explícita documentada en el propio comentario del `retirador`, una llamada real».
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts
@app/composables/__tests__/vocabularioDeAfirmaciones.ts
@app/composables/useProgressMismatchMark.ts

Lecturas acotadas (NO leer enteras):
- `app/pages/[game]/index.vue`: `onResumeContinue` (~l.533-555), `onContentChangedAcknowledge` (~l.780-798), la llamada `readProgressMismatchWarning(gameId, informe.huella)` (~l.208) y el import de `~/composables/useProgressMismatchMark` (~l.42-46). Localizarlos con grep por nombre.
- `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` l.43-111 (WR-01, WR-02, WR-03).
- `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` l.518-590 (sección «Ronda 8 / plan 09-38», hallazgo 3).

**Los números de línea son orientativos, medidos en 633ef20.** Este ítem depende de 260923-3rl y 260923-3rm (los dos tocan `deferred-items.md`; 3rm además `index.vue`, y los dos tienen que mantener verde este gate). Hay que releer los ficheros tal como estén tras su merge y localizar todo por identificador, nunca por número de línea.

<interfaces>
Utilidades que ya existen en `invariantesDeMarcaDeEstado.test.ts` y se reutilizan tal cual (no reimplementarlas):
- `indiceDeCierre(region: string, aperturaIndex: number): number` — empareja `( [ {` con su cierre. Solo cuenta paréntesis, corchetes y llaves.
- `argumentosDeNivelSuperior(contenido: string): string[]` — separa por comas de nivel superior. La Task 2 la cambia.
- `llamadasA(region: string, nombre: string): LlamadaEncontrada[]` con `{ inicio, fin, argumentos }`. Excluye `function nombre(`.
- `contenidoPorRuta(ficheros, ruta)`, `soloComposables(ficheros)`.
- `marcasDeEstadoDeModuloDe(ficheros): string[]`, `MARCAS_CON_REFERENTE_NO_PERSISTENTE`.
- `contratoDeLaMarcaDe(fuente): { ponedor, retirador, lector, aridadDelLector, segundoParametroOpcional }`. Sobre el árbol real da `markProgressMismatch` / `clearProgressMismatch` / `readProgressMismatchWarning`.
- `extraerFuncionesExportadas(fuente)` → cuerpo y tipo de retorno. Salta el tipo de retorno hasta el primer `{`.
Del módulo compartido `./vocabularioDeAfirmaciones`: `regionVigilada` (quita comentarios y `<style>`), `rutaRelativa`, `ficherosVueDelArbol`, `ficherosTsDelArbol` (globs `import.meta.glob` con `?raw`). No usar `node:fs`/`node:url`: `app/**` pasa por `npm run typecheck` sin `@types/node`.

Hechos medidos en 633ef20 con un prototipo del planificador:
- El bloque de comentario `//` justo encima de `export function clearProgressMismatch(` cita entre acentos graves `Map.delete`, `false`, `Set.delete`, `onOutcomeRecorded`, `onDiscardConfirm`, `finishGame`, `onResumeContinue` y `onContentChangedAcknowledge`.
- El único consumidor fuera de `__tests__` que importa `useProgressMismatchMark` es `app/pages/[game]/index.vue`.
- Las cinco funciones están declaradas como `function nombre(` en `index.vue` y todas llaman a `clearProgressMismatch(gameId)` en su cuerpo. La única llamada al lector es `readProgressMismatchWarning(gameId, informe.huella)`, con clave `gameId`.
- Con la regex nueva de la Task 2, el descubrimiento sobre el árbol real sigue siendo exactamente `useHistorySavedNotice.ts` y `useProgressMismatchMark.ts`. Los únicos `const` tipados de módulo en `app/composables/` son literales de objeto (`cataloguesById`, `gamesById`) y no casan.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Pata 6 de extremo a extremo — toda retirada explícita que documenta `clearProgressMismatch` existe de verdad en el cuerpo de su función (WR-03 / hallazgo 3)</name>
  <files>app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts</files>
  <precondition>`app/pages/[game]/index.vue` declara `function onResumeContinue(` y `function onContentChangedAcknowledge(`, y cada cuerpo contiene `clearProgressMismatch(gameId)`. Es cierto en 633ef20; hay que comprobarlo de nuevo tras el merge de 3rl/3rm con `grep -n "function onResumeContinue(\|function onContentChangedAcknowledge(\|clearProgressMismatch(gameId)" 'app/pages/[game]/index.vue'`. Si falta alguna, parar e informar: ese estado es una regresión de un hermano, no algo que se arregle aquí.</precondition>
  <behavior>
    - Real: `llamantesDocumentadosDelRetirador(<useProgressMismatchMark.ts real>, 'clearProgressMismatch')` contiene `onResumeContinue` y `onContentChangedAcknowledge` (los dos de WR-03).
    - Sintético: un bloque `//` sobre `export function retirar(` que cita `` `Map.delete` ``, `` `false` ``, `` `retirar` ``, `` `onA` `` y `` `onB` `` da exactamente `['onA', 'onB']`. Quedan fuera los nombres con punto, los literales y el propio retirador.
    - Sintético: una declaración del retirador sin comentario encima da `[]`.
    - Real: `consumidoresDeLaMarca('app/composables/useProgressMismatchMark.ts', …árbol…)` tiene exactamente la clave `app/pages/[game]/index.vue`.
    - Real: `funcionesSinRetiradaDe(<consumidores reales>, <llamantes reales>, 'clearProgressMismatch', 'readProgressMismatchWarning')` da `[]`.
    - Mutación PERMANENTE en memoria, un `it.each` sobre `['onResumeContinue', 'onContentChangedAcknowledge']`: se copia `regionVigilada` del `index.vue` real y se quitan de esa copia las llamadas al retirador SOLO dentro del cuerpo de esa función (los offsets salen de `cuerpoDeFuncionDeclarada` + `llamadasA`). El test exige que la copia difiera del original, para que la mutación haya ocurrido de verdad, y que el resultado sea exactamente `[{ nombre, motivo: 'sin-llamada' }]`.
    - Sintético: la llamada existe en OTRA función del mismo fichero pero no en `onA` → `[{ nombre: 'onA', motivo: 'sin-llamada' }]`. Demuestra que se mira el cuerpo, no el fichero entero.
    - Sintético: la llamada solo aparece en un comentario `//` dentro del cuerpo → `sin-llamada`.
    - Sintético: la llamada va DESPUÉS de `pedir().catch(() => {})` dentro del cuerpo → `[]`. Demuestra que ni las llaves internas ni `=>` truncan el cuerpo.
    - Sintético: el cuerpo llama `retirar(otroId)` mientras el lector del mismo fichero se llama con `gameId` → `clave-distinta`.
    - Sintético: el nombre documentado no está declarado en ningún consumidor → `sin-declarar`.
    - Invariante (`it.each` sobre `marcasNoAuditadas`, igual que las Patas 1-4): el retirador no es null, los llamantes documentados no están vacíos, los consumidores no están vacíos y `funcionesSinRetiradaDe` da `[]`.
  </behavior>
  <action>
Añadir la Pata 6 a `invariantesDeMarcaDeEstado.test.ts`. Va después del bloque de la Pata 5 y antes del bloque «Cierre de cobertura — las cuatro condiciones de calidad heredadas». Lleva cuatro funciones puras exportadas y sus `describe`, con el mismo estilo que las patas existentes: función pura, self-test sobre el árbol real, casos sintéticos y un `it.each` invariante sobre `marcasNoAuditadas`.

1. `llamantesDocumentadosDelRetirador(fuenteDeLaMarca: string, retirador: string): string[]`. Trabaja sobre la fuente CRUDA, nunca sobre `regionVigilada`, porque el contrato vive precisamente en el comentario.
   - Localizar la línea que empieza por `export function <retirador>(` y recoger el bloque contiguo de líneas que, sin espacios iniciales, empiezan por `//` justo encima.
   - Extraer los tramos entre acentos graves de una sola línea y quedarse con los que son identificadores desnudos (patrón de identificador JS, sin punto).
   - Descartar `true`, `false`, `null`, `undefined` y el propio nombre del retirador. Deduplicar en orden de aparición.
   - Sin declaración o sin comentario, devolver `[]`.
2. `consumidoresDeLaMarca(marca: string, ficheros: Record<string, string>): Record<string, string>`.
   - Recibe como `ficheros` la unión de `ficherosVueDelArbol` y `ficherosTsDelArbol`.
   - Devuelve, con clave `rutaRelativa`, los ficheros que no están bajo `/__tests__/`, no son la propia marca e importan desde un especificador que termina en `/<nombre del módulo sin .ts>` (comillas simples o dobles).
   - Descubierto, no tecleado: si una función deja de existir o se va a otro fichero que no importa la marca, el resultado cae del lado rojo (`sin-declarar`).
3. `cuerpoDeFuncionDeclarada(region: string, nombre: string): { inicio: number, fin: number, cuerpo: string } | null`.
   - Localiza `function <nombre>(`, exportada o no, con `\b` delante.
   - Salta los parámetros con `indiceDeCierre`, avanza hasta el primer `{` (mismo salto de tipo de retorno que `extraerFuncionesExportadas`) y delimita el cuerpo con `indiceDeCierre`.
   - `inicio` y `fin` son offsets del contenido del cuerpo dentro de `region`, sin las llaves. Si la función no está declarada, devuelve null.
   - Si alguien la reescribe como `const nombre = () => {…}`, devuelve null y la pata cae del lado rojo. Documentarlo en el comentario.
4. `funcionesSinRetiradaDe(consumidores: Record<string, string>, llamantes: string[], retirador: string, lector: string | null): Array<{ nombre: string, motivo: 'sin-declarar' | 'sin-llamada' | 'clave-distinta' }>`.
   - Aplica `regionVigilada` a cada consumidor, para que una llamada comentada no cuente.
   - Para cada nombre, evalúa TODA declaración que encuentre en los consumidores. Si no aparece en ninguno, `sin-declarar`.
   - La clave del consumidor es el primer argumento de la primera llamada al lector en ese mismo consumidor, vía `llamadasA`.
   - Si el cuerpo no tiene ninguna llamada al retirador, `sin-llamada`. Si tiene llamadas pero ninguna con primer argumento textualmente igual a la clave, `clave-distinta`. Si el consumidor no llama al lector, basta cualquier llamada con al menos un argumento.
   - Resultado ordenado por nombre y sin duplicados.
5. Los `describe` de la Pata 6 contienen los casos del `<behavior>`. El `it.each` invariante recorre `marcasNoAuditadas` y, ante un fallo, lanza un `Error` que nombra el fichero consumidor, la función y el motivo. El texto explica que la función está documentada en el comentario del retirador como punto de retirada explícita (la segunda defensa de CR-01/WR-01, `09-REVIEW.md` WR-03) y que no llamarlo deja esa defensa sin efecto sin que nada lo note.
6. Comentarios:
   - La cabecera del fichero pasa de «Cinco patas» a «Seis patas», con una viñeta de la Pata 6 (quick 260923-3rn).
   - El comentario anti-recurrencia de la Pata 5 menciona que la Pata 6 también es un `it.each` sobre `marcasNoAuditadas`.
   - El comentario de la Pata 6 declara su límite: demuestra que la llamada está presente en el cuerpo, no que sea alcanzable (una llamada bajo `if (false)` pasaría). Esa garantía no se apoya en la pata, porque la defensa primaria es la validación de huella de `useProgressMismatchMark.ts`.
7. NO editar `app/pages/[game]/index.vue` ni `app/composables/useProgressMismatchMark.ts`: la pata se escribe CONTRA el árbol tal como está.
8. Mutaciones EN DISCO, ejecutadas y revertidas, una detrás de otra, con la pata ya verde:
   - M1: borrar solo la línea `clearProgressMismatch(gameId)` de `onResumeContinue`. Ejecutar `npx vitest run app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` y comprobar que sale ROJO. Copiar al SUMMARY los títulos literales de los `it` que fallan y el mensaje del `Error`. Revertir con `git checkout -- 'app/pages/[game]/index.vue'` y volver a ejecutar hasta verde.
   - M2: lo mismo con `onContentChangedAcknowledge`.
   - Al terminar, `git diff --quiet HEAD -- 'app/pages/[game]/index.vue'` tiene que salir con 0.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts && git diff --quiet HEAD -- 'app/pages/[game]/index.vue'</automated>
  </verify>
  <done>
    - La Pata 6 está en verde sobre el árbol real.
    - Los casos sintéticos y los dos `it.each` de mutación en memoria están en verde.
    - M1 y M2 se ejecutaron en disco: cada una puso rojo el fichero de test y se revirtió. Su salida literal está en el SUMMARY.
    - `index.vue` no tiene cambios netos.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cerrar los dos huecos de parsing del gate — `<`/`>` en `argumentosDeNivelSuperior` (WR-01) y `const` con anotación de tipo en el descubrimiento (WR-02)</name>
  <files>app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts</files>
  <behavior>
    - (a) `contratoDeLaMarcaDe` sobre una marca sintética (`new Set`/`new Map` a columna 0) cuyo lector es `export function leer(claves: Record<string, string>): string | null` con `.has(`/`.get(` en el cuerpo → `aridadDelLector === 1`. Hoy da 2, que es el falso verde.
    - (b) Lector `leer(id: string, testigo: Record<string, string>)` → `aridadDelLector === 2`. Hoy da 3.
    - (c) `lecturasSinTestigoDe('lector(x as Map<string, string>)', 'lector')` → `['lector(x as Map<string, string>)']`. Hoy da `[]`, falso verde.
    - (d) `lecturasSinTestigoDe('lector(gameId, x as Map<string, string>)', 'lector')` → `[]`.
    - (e) `=>` no cierra nada: parámetros `m: Map<string, () => void>` → aridad 1 (hoy 2); parámetros `cb: () => void, testigo: string` → aridad 2; `lecturasSinTestigoDe('lector(gameId, (a: string) => a)', 'lector')` → `[]`.
    - (f) Una comparación con espacios no se toca: `lecturasSinTestigoDe('lector(a > b, testigo)', 'lector')` → `[]`.
    - (g) `marcasDeEstadoDeModuloDe` sobre `const marcas: Map<string, string> = new Map()` a columna 0 más una raíz vigilada en código (p. ej. una función que devuelve `'progreso'`) → descubre el fichero. Hoy da `[]`, que es la evasión.
    - (h) Igual con `const aviso: Ref<string | null> = ref(null)` → descubierto.
    - (i) Igual con `const m: Map<string, () => void> = new Map()` → descubierto. Es el caso que separa este arreglo de la regex literal que propone la review.
    - (j) `const x: number = 5` más una raíz → NO descubierto. La anotación de tipo no convierte cualquier `const` en estado de módulo.
    - Sin cambios: los self-tests reales de las Patas 1 y 2 y el test de descubrimiento sobre el árbol real (exactamente dos ficheros) siguen en verde sin tocarlos.
  </behavior>
  <action>
Primero RED:
- Añadir los casos (a)-(f) a los `describe` existentes «Pata 1 — contratoDeLaMarcaDe» y «Pata 2 — lecturasSinTestigoDe», y (g)-(j) a «Descubrimiento — marcasDeEstadoDeModuloDe». Los títulos citan `09-REVIEW.md` WR-01 o WR-02 y «caso sintético».
- Ejecutar el fichero y copiar al SUMMARY qué casos salen rojos contra el gate sin arreglar. Se esperan rojos (a), (b), (c), (e-primer caso), (g), (h) e (i).

Después GREEN:

Hueco 1 (WR-01). Cambiar SOLO `argumentosDeNivelSuperior`:
- Además de la profundidad de `( [ {`, lleva una profundidad angular.
- Un `<` la abre solo si el carácter inmediatamente anterior es de identificador (letra, dígito, `_` o `$`). Es el caso de `Record<`, `Map<`, `ref<` o `f<`, y deja fuera `a < b` con espacios.
- Un `>` la cierra solo si la profundidad angular es mayor que 0 y el carácter anterior no es `=`, así que `=>` nunca cierra nada. La profundidad angular nunca baja de 0.
- Solo se separa por coma cuando las dos profundidades valen 0.

NO añadir `<`/`>` a `indiceDeCierre`, aunque sea el fragmento que propone la review:
- `indiceDeCierre` también delimita CUERPOS de función (`extraerFuncionesExportadas`, y `cuerpoDeFuncionDeclarada` de la Task 1), donde `=>`, `>=` y las comparaciones son constantes. Contarlos truncaría los cuerpos: `.catch(() => {})` en `onResumeContinue` basta.
- Emparejar paréntesis ya es correcto en presencia de genéricos, porque un `<…>` nunca contiene paréntesis desequilibrados.

Documentarlo en el comentario de las dos funciones, junto con el residuo conocido: una comparación sin espacios como `a<b, c>d` INFRA-cuenta, y eso cae del lado rojo (menos argumentos hace que las Patas 1/2 lo marquen), nunca del verde.

Hueco 2 (WR-02). Cambiar `PATRON_ESTADO_DE_MODULO_MUTABLE`:
- La rama `const` acepta una anotación de tipo opcional entre el identificador y el `=`: dos puntos seguidos de cualquier cosa perezosa en la MISMA línea, hasta un `=` que vaya seguido de `new Set`, `new Map`, `ref<` o `ref(`.
- La rama `let` y el ancla de columna 0 no cambian.
- NO usar la regex literal de la review (el opcional de dos puntos seguido de «todo lo que no sea `=`»): una anotación con `=>` para en la flecha y vuelve a evadir el descubrimiento. Es el caso (i).

Actualizar los comentarios que enumeran las formas descubiertas: el de encima del patrón (su afirmación de que una marca DÉCIMA «entra sola» pasa a ser cierta para `const` con tipo) y el comentario anti-recurrencia de la Pata 5.

Mutaciones ejecutadas y revertidas, con el fichero ya en verde:
- M3: devolver temporalmente el bucle de `argumentosDeNivelSuperior` a su forma vieja (sin profundidad angular). Ejecutar el fichero y confirmar que (a), (b), (c) y (e-primer caso) salen ROJOS. Revertir y confirmar verde.
- M4: devolver temporalmente `PATRON_ESTADO_DE_MODULO_MUTABLE` a su forma vieja. Confirmar que (g), (h) e (i) salen ROJOS. Revertir y confirmar verde.
- M4b: sustituirlo temporalmente por la regex literal de la review. Confirmar que (i) sale ROJO y (g)/(h) en verde. Eso demuestra por qué no se adoptó. Revertir.
- Copiar al SUMMARY la salida literal de cada mutación: títulos que fallan y recuento.

Si tras el merge de 3rl/3rm el descubrimiento sobre el árbol real encuentra un fichero NUEVO por culpa de la regex ampliada (hoy no ocurre), es el hueco cerrándose como debe. NO debilitar la regex:
- Si es un aviso transitorio del estilo de `useHistorySavedNotice.ts`, añadirlo a `MARCAS_CON_REFERENTE_NO_PERSISTENTE` con un `motivo` que nombre un identificador comprobable y un `respaldo` que pase `respaldoRespaldaA`, y actualizar la lista fijada del test de descubrimiento.
- Si en cambio exige cambios de producción fuera de `files_modified`, parar e informar.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts && npm run typecheck</automated>
  </verify>
  <done>
    - Los casos (a)-(j) están en verde con el gate arreglado, y los que se esperaba ver en rojo se vieron en rojo antes del arreglo.
    - M3, M4 y M4b se ejecutaron y revirtieron. Su salida literal está en el SUMMARY.
    - `indiceDeCierre` queda sin cambios de lógica. Los self-tests reales y el descubrimiento sobre el árbol real siguen en verde.
    - `npm run typecheck` está en verde.
  </done>
</task>

<task type="auto">
  <name>Task 3: Registrar el cierre en `deferred-items.md` y en el ROADMAP, y pasar los gates completos</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md, .planning/ROADMAP.md</files>
  <action>
1. En `deferred-items.md`, sección «## Ronda 8 / plan 09-38 — …», insertar justo después del párrafo «**Acción sugerida:**» del hallazgo 3 (el que termina en «nunca la AUSENCIA de una llamada de retirada esperada.») un párrafo **«Actualización (quick 260923-3rn) — CERRADO:»**, con el mismo estilo que los «Actualización … — CERRADO» que ya tiene el fichero. Debe decir:
   - que se eligió la pata nueva y no el montaje de página, y por qué, en una frase (dependencias no instaladas y entorno `nuxt` evitado por `vitest.config.ts`);
   - qué exige la Pata 6: los llamantes documentados en el comentario de `clearProgressMismatch`, sus cuerpos reales en los consumidores descubiertos por import, y la misma clave que el lector;
   - las mutaciones M1/M2 ejecutadas y revertidas, con los títulos literales de los tests que se pusieron rojos (tomados del SUMMARY de la Task 1);
   - el límite declarado: prueba presencia en el cuerpo, no alcanzabilidad.
2. Justo a continuación, y antes del `---` que precede a «## Nota de cierre (ronda 8, planes 09-37..09-40)», añadir una sección nueva: **«## Huecos de parsing del gate de invariantes (`09-REVIEW.md` WR-01/WR-02; advisory de `09-VERIFICATION.md` ronda 10) — CERRADO (quick 260923-3rn)»**. Contenido:
   - los dos huecos, en una línea cada uno;
   - el arreglo de cada uno;
   - por qué `indiceDeCierre` no se tocó (los cuerpos con `=>`);
   - por qué no se usó la regex literal de la review (una anotación con `=>`);
   - las mutaciones M3/M4/M4b con su salida literal;
   - un bloque **«Observado al cerrar y NO cerrado aquí (fuera del alcance de este ítem; sin instancia real hoy)»** con tres viñetas: (i) `export const`/`export let` a columna 0 no entran en el descubrimiento; (ii) los contenedores que el patrón no enumera (`shallowRef`, `reactive`, `WeakMap`, arrays u objetos literales) tampoco; (iii) una coma dentro de un literal de cadena o de plantilla sigue SOBRE-contando argumentos en `argumentosDeNivelSuperior`, así que `lector('a, b')` cuenta 2. Es la misma dirección peligrosa (verde por accidente) que WR-01, y queda como candidato a una quick posterior.
   No escribir que esas tres estén cerradas.
3. En `.planning/ROADMAP.md`, con un `Edit` acotado a la línea de «**Phase 9: Histórico y estadísticas**» (nunca `Write` del fichero entero), completar la frase final «Queda registrado como deuda no bloqueante: dos huecos de parsing del propio gate nuevo y la falta de test directo sobre las dos llamadas explícitas a `clearProgressMismatch`». Hay que añadir que las tres quedaron cerradas después por la quick 260923-3rn: la Pata 6 del gate y los dos arreglos de parsing, cada uno demostrado por mutación ejecutada y revertida (ver `deferred-items.md`). El resto de la línea queda intacto.
4. NO editar `09-VERIFICATION.md`: es el acta histórica de la ronda 10. Hay que dejar constancia en el SUMMARY de que este ítem (igual que 3rl/3rm) cambia ficheros de su `covered_files` (`invariantesDeMarcaDeEstado.test.ts`, `deferred-items.md`), así que su `covered_digest` queda desfasado por diseño. Volver a sellarlo es una decisión del usuario al cerrar el lote, con el mismo patrón que el commit 633ef20. Tampoco tocar STATE.md: lo actualiza el flujo de quick.
5. Gates completos:
   - `npx vitest run`: 0 fallos. El recuento es la base tras el merge de 3rl/3rm más los tests nuevos de este ítem; anotar la cifra real en el SUMMARY.
   - `npx tsc --noEmit`: exit 0. El `tsconfig.json` raíz tiene `"files": []`, así que por sí solo no comprueba nada.
   - `npm run typecheck` (`nuxt typecheck`, que es el que comprueba `app/**` de verdad): exit 0.
   - Confirmar con `git diff --name-only HEAD` que los únicos ficheros cambiados son los tres de `files_modified` (más el PLAN/SUMMARY de esta quick) y que `app/pages/[game]/index.vue` no aparece.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run typecheck && grep -c "260923-3rn" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md && grep -c "260923-3rn" .planning/ROADMAP.md && git diff --quiet HEAD -- 'app/pages/[game]/index.vue'</automated>
  </verify>
  <done>
    - `deferred-items.md` marca CERRADO el hallazgo 3 y tiene la sección nueva de los dos huecos de parsing, con las mutaciones y las tres observaciones abiertas.
    - La línea de la Fase 9 del ROADMAP registra el cierre.
    - Suite completa, `tsc --noEmit` y `npm run typecheck` están en verde.
    - Ningún fichero fuera de `files_modified` está cambiado, e `index.vue` está intacto.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate de tests → CI/despliegue | El gate decide si una regresión del ciclo de vida de la marca llega a la mesa. Un falso verde del gate equivale a una garantía sin respaldo. No hay entrada de usuario ni cambio de runtime: todo el cambio vive en `__tests__/` y en documentación. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3rn-01 | Tampering | `argumentosDeNivelSuperior` (heurística angular) | medium | mitigate | La heurística solo puede INFRA-contar, y eso cae del lado rojo. Los casos (a)-(f) fijan las dos direcciones y M3 demuestra que vuelven a rojo sin el arreglo. |
| T-3rn-02 | Tampering | Mutación en disco de `app/pages/[game]/index.vue` (M1/M2) que se olvida sin revertir | medium | mitigate | Se revierte con `git checkout --`, la verificación de Task 1 y Task 3 exige `git diff --quiet HEAD -- 'app/pages/[game]/index.vue'`, y la propia Pata 6 se pondría roja si la línea faltara. |
| T-3rn-03 | Repudiation | Pata 6: llamada presente pero inalcanzable (código muerto) | low | accept | Es un límite declarado por escrito en el comentario de la pata. La garantía de SC3 no depende de ella, porque la defensa primaria es la validación de huella (`useProgressMismatchMark.ts`). |
| T-3rn-04 | Tampering | Regex de descubrimiento ampliada que descubre de más | low | mitigate | El caso (j) fija que un `const` tipado sin `new Set`/`new Map`/`ref` no entra, y el test de descubrimiento sobre el árbol real sigue fijado a los dos ficheros. |
</threat_model>

<verification>
- `npx vitest run` con 0 fallos, `npx tsc --noEmit` con exit 0 y `npm run typecheck` con exit 0.
- Las cinco mutaciones (M1, M2, M3, M4, M4b) y el RED previo de la Task 2 están ejecutados, cada uno con su salida roja literal en el SUMMARY. Las cinco mutaciones están revertidas.
- `git diff --quiet HEAD -- 'app/pages/[game]/index.vue'` sale con 0.
- `deferred-items.md` y `.planning/ROADMAP.md` contienen `260923-3rn`.
</verification>

<success_criteria>
- Quitar `clearProgressMismatch(gameId)` de `onResumeContinue` o de `onContentChangedAcknowledge` pone roja la suite, demostrado en disco y además fijado de forma permanente por la mutación en memoria de la Pata 6.
- Un tipo genérico con coma ya no infla la aridad en las Patas 1/2, y una marca `const X: Tipo = new Map()` / `ref(...)` ya no se salta el descubrimiento. Cada arreglo tiene su mutación ejecutada y revertida.
- La deuda queda registrada como cerrada donde estaba registrada como abierta, y las evasiones adyacentes observadas quedan escritas como abiertas, no como cerradas.
</success_criteria>

<output>
Crear `.planning/quick/260923-3rn-phase-9-gate-follow-ups-add-a-test-that-fails-if-onresumecon/260923-3rn-SUMMARY.md` al terminar. Debe incluir:
- la salida literal (títulos de test en rojo y recuentos) de las mutaciones RED de la Task 2 y de M1, M2, M3, M4 y M4b;
- el recuento final de la suite;
- la nota sobre el `covered_digest` de `09-VERIFICATION.md`.
</output>
