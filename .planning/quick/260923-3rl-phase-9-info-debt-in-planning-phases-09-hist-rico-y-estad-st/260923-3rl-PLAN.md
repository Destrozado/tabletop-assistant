---
phase: quick-260923-3rl
plan: "01"
type: execute
wave: 1
depends_on: ["260923-3rm"]
files_modified:
  - app/composables/usePersistedSession.ts
  - app/composables/__tests__/usePersistedSession.test.ts
  - app/composables/useGameHistory.ts
  - app/composables/__tests__/useGameHistory.test.ts
  - app/pages/historico.vue
  - engine/history.ts
  - engine/__tests__/history.test.ts
  - engine/statistics.ts
  - engine/__tests__/statistics.test.ts
  - e2e/offline-flow.spec.ts
  - .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
autonomous: true
requirements: [IN-04, IN-05, IN-07, IN-11, IN-12, IN-13]
tags: [historico, estadisticas, deuda-fase-9, localStorage, e2e]

must_haves:
  truths:
    - "Si el navegador no deja escribir al borrar una partida en /historico, el grupo ve un aviso en español «⚠ No se pudo borrar la partida» y la lista no cambia (IN-11)"
    - "Registrar una partida nunca deja más de 500 entradas en tga:history; la que sale es la más antigua (la última del array) (IN-04)"
    - "Una entrada que isGameHistoryEntry rechaza desaparece del disco en el siguiente registro con éxito; un envoltorio de formatVersion desconocido o JSON corrupto sigue sin sobrescribirse nunca (IN-12, CR-03 intacto)"
    - "appendHistoryEntry nunca escribe tga:history:synced: la poda de marcas sigue viviendo solo en useHistorySync.ts (D-04), así que un id podado no rompe la lista de marcas ni se vuelve a subir"
    - "Los ids nuevos del histórico son UUID v4 de crypto.randomUUID; sin crypto.randomUUID (contexto no seguro) buildHistoryEntry no lanza y el sufijo nunca queda vacío (IN-05)"
    - "/estadisticas nunca pinta «100 %» sin pleno exacto ni «0 %» con al menos una victoria (IN-13)"
    - "e2e/offline-flow.spec.ts carga y lista sus 5 tests aunque public/audio/ no tenga ningún .m4a; solo el test que necesita audio se marca como skipped en ese caso (IN-07)"
    - "Las seis entradas IN-04/IN-05/IN-07/IN-11/IN-12/IN-13 de deferred-items.md quedan anotadas CERRADO con la quick 260923-3rl y el test que lo fija"
  artifacts:
    - path: "app/composables/usePersistedSession.ts"
      provides: "removeHistoryEntry con resultado booleano; appendHistoryEntry que filtra por isGameHistoryEntry y poda a HISTORY_MAX_ENTRIES"
      contains: "HISTORY_MAX_ENTRIES"
    - path: "app/composables/useGameHistory.ts"
      provides: "remove(id) devuelve boolean; copy DELETE_FAILED_HEADING/DELETE_FAILED_BODY"
      contains: "DELETE_FAILED_HEADING"
    - path: "app/pages/historico.vue"
      provides: "aviso en línea de borrado fallido, región viva permanente"
      contains: "role=\"status\""
    - path: "engine/history.ts"
      provides: "id de entrada vía crypto.randomUUID con respaldo sin lanzar"
      contains: "randomUUID"
    - path: "engine/statistics.ts"
      provides: "winPercentage: 100 solo con pleno, 0 solo sin victorias"
      contains: "winPercentage"
    - path: "e2e/offline-flow.spec.ts"
      provides: "búsqueda perezosa del clip + test.skip en el test que la necesita"
      contains: "test.skip("
  key_links:
    - from: "app/pages/historico.vue (onConfirmarBorrado)"
      to: "useGameHistory().remove(id)"
      via: "el booleano devuelto decide si se pinta el aviso de borrado fallido"
      pattern: "remove\\("
    - from: "app/composables/useGameHistory.ts (remove)"
      to: "usePersistedSession().removeHistoryEntry(id)"
      via: "propaga el resultado de writeRaw en vez de descartarlo"
      pattern: "removeHistoryEntry\\("
    - from: "app/composables/usePersistedSession.ts (appendHistoryEntry)"
      to: "tga:history"
      via: "filtrado por isGameHistoryEntry + slice a HISTORY_MAX_ENTRIES antes de writeRaw"
      pattern: "HISTORY_MAX_ENTRIES"
    - from: "app/composables/useHistorySync.ts (syncPending, D-04)"
      to: "tga:history:synced"
      via: "la poda existente por readHistory() retira el id que appendHistoryEntry ya sacó de tga:history (sin cambios en este plan)"
      pattern: "currentHistoryIds"
    - from: "engine/statistics.ts (buildRows)"
      to: "winPercentage"
      via: "pct de cada fila, que también ordena la cascada D-24"
      pattern: "winPercentage\\("
---

<objective>
Cerrar las seis INFO de deuda real de la Fase 9 que `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` lista en «INFO de `09-REVIEW.md` que son deuda real»: IN-04 + IN-12 (tope/poda del histórico y descarte en disco de entradas que `isGameHistoryEntry` rechaza), IN-05 (id por `crypto.randomUUID`), IN-07 (el `throw` de módulo de `e2e/offline-flow.spec.ts` pasa a `test.skip` en el único test que necesita audio), IN-11 (`removeHistoryEntry` informa del fallo de escritura y `/historico` lo avisa) e IN-13 (`pct` nunca anuncia un extremo que el recuento no respalda). Al final, cada entrada queda anotada CERRADO con el id de esta quick.

Estado comprobado al planificar (base 633ef20, los números de línea del fichero de diferidos ya no coinciden): las seis siguen abiertas. IN-04/IN-12 → `appendHistoryEntry` en `app/composables/usePersistedSession.ts:484-505` antepone la entrada sobre `read.entries` en crudo, sin tope; IN-11 → `removeHistoryEntry` en `:521-539` devuelve `void` e ignora `writeRaw`; IN-05 → `engine/history.ts:110`; IN-13 → `engine/statistics.ts:72`; IN-07 → `e2e/offline-flow.spec.ts:22-28` (además del `throw` explícito, el `readdirSync` de módulo también aborta la suite con ENOENT si falta `public/audio/`, comprobado ejecutándolo).

Purpose: un histórico que crece sin tope, entradas basura que nadie puede ver ni borrar, un borrado que falla en silencio y un «100 %» que desmiente a su propio «199 de 200» son justo la clase de afirmación sin respaldo que la Fase 9 persiguió durante nueve rondas. Las guardas de la Fase 9 (`afirmacionesRespaldadas`, `invariantesDeMarcaDeEstado`) y la contabilidad de la Fase 10 (`tga:history:synced`, poda D-04) tienen que seguir en verde e intactas.

Output: los once ficheros de `files_modified`, con tests RED→GREEN para cada comportamiento nuevo, `npm test` y `npm run typecheck` en verde, y la spec de Playwright offline ejecutada completa una vez.

Orden respecto a 260923-3rm (dependencia declarada): 3rm cambia contratos que este plan toca por dentro (WR-02 puede convertir el retorno de `appendHistoryEntry` en un resultado discriminado y añadir una salida para el histórico ilegible en `/historico`; WR-07 puede añadir `entriesWithVillain` a `engine/statistics.ts`; WR-04/WR-05 cambian avisos y quizá el relleno de `historico.vue`). Este plan solo cambia qué entradas se escriben, el retorno de `removeHistoryEntry`, el cálculo de `pct` y un aviso en línea, así que se aplica limpio encima. Cada tarea empieza releyendo sus ficheros y conserva lo que 3rm haya dejado.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md

Código a tocar (releer antes de cada tarea — 260923-3rm puede haberlo cambiado):
@app/composables/usePersistedSession.ts
@app/composables/useGameHistory.ts
@app/pages/historico.vue
@engine/history.ts
@engine/statistics.ts
@e2e/offline-flow.spec.ts

Contabilidad de la Fase 10 que NO se toca pero que la poda debe respetar:
@app/composables/useHistorySync.ts
(syncPending: la poda D-04 filtra tga:history:synced por los ids de readHistory() al final de cada flush; flush() calcula pendientes como loadHistory() menos los ya marcados. Test que lo fija: app/composables/__tests__/useHistorySync.test.ts, «un id marcado cuya entrada ya no está en tga:history desaparece de la lista tras el flush (D-04, poda perezosa)».)

Gates de la Fase 9 que deben seguir en verde (léelos por encima antes de escribir cualquier copy o identificador nuevo en app/):
@app/composables/__tests__/vocabularioDeAfirmaciones.ts
@app/composables/__tests__/afirmacionesRespaldadas.test.ts
@app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts

Reglas de esos gates que afectan a este plan, destiladas:
- Gate A barre TODO `app/**/*.vue` y `app/**/*.ts` (fuera de `__tests__/`) sin comentarios ni `<style>`, código incluido: cualquier cadena O IDENTIFICADOR que contenga una raíz de RAICES_SOBRE_LOS_DATOS_DEL_GRUPO (conserv, guardad, guardar, dispositivo, progreso, reintent, «no se ha perdido», «ya no está», «no encontraréis», «se borrar») exige una entrada auditada. Este plan no añade ninguna: ni la copy nueva ni ningún nombre nuevo (p. ej. nada como una variable llamada con «guardad…», «conserv…» o «reintent…») contiene esas raíces.
- Gate C (primer test, que NO quita comentarios): fuera de los cinco ficheros autorizados, ningún `.ts`/`.vue` de app/ puede contener entre comillas simples los literales de StoredProgress (resumable, stale, absent, unknown) — ni siquiera en un comentario. `useGameHistory.ts` y `historico.vue` NO están autorizados; `usePersistedSession.ts` sí. Gate C (tercer test): ningún fichero salvo `useHistorySavedNotice.ts` escribe entre comillas un literal de NoticeVariant (incluido success).
- invariantesDeMarcaDeEstado: cualquier `let` o `const X = new Set/new Map/ref(` a nivel de módulo en `app/composables/**` se convierte en una marca auditada. Este plan no añade estado de módulo mutable: el estado del aviso vive en un `ref` dentro del `<script setup>` de la página.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): borrado fallido visible de extremo a extremo — removeHistoryEntry → remove() → aviso en /historico (IN-11)</name>
  <files>app/composables/usePersistedSession.ts, app/composables/__tests__/usePersistedSession.test.ts, app/composables/useGameHistory.ts, app/composables/__tests__/useGameHistory.test.ts, app/pages/historico.vue</files>
  <behavior>
    - usePersistedSession: con una entrada presente y escritura normal, removeHistoryEntry(id) devuelve true y el envoltorio persistido ya no la contiene.
    - Con setItem lanzando al reescribir tga:history, removeHistoryEntry(id) devuelve false, no lanza, y la cadena de tga:history sigue idéntica byte a byte.
    - Con un envoltorio ilegible (formatVersion 2, JSON corrupto, getItem que lanza, o sin window) devuelve false y no llama a setItem para tga:history — se añade la aserción de retorno a los tests CR-03/CR-01 (ronda 3)/SSR que ya existen para removeHistoryEntry, sin cambiar lo que ya afirman.
    - Con tga:history ausente, o con un id que no está en el envoltorio, devuelve true y NO llama a setItem para tga:history (no hay nada que borrar ni nada ha fallado).
    - WR-08 intacto: con dos entradas que comparten id se elimina como máximo una; y el test existente «CR-03: una entrada ilegible sobrevive en disco a un removeHistoryEntry de otra entrada» sigue en verde sin tocarlo.
    - useGameHistory: remove(id) devuelve true en el camino feliz (el test de ciclo existente sigue pasando); con el setItem del localStorage falso lanzando justo antes de remove(id), remove devuelve false y entries.value sigue conteniendo esa id tras la recarga interna.
    - useGameHistory exporta DELETE_FAILED_HEADING con valor exacto «⚠ No se pudo borrar la partida» y DELETE_FAILED_BODY con valor exacto «La app no ha conseguido leer o escribir el histórico en este navegador, así que no ha cambiado nada. Podéis volver a intentarlo más tarde.» — un test fija las dos cadenas literalmente.
  </behavior>
  <action>
Primero relee los cinco ficheros: si 260923-3rm ya cambió removeHistoryEntry, el retorno de appendHistoryEntry, o añadió a historico.vue una salida para el histórico ilegible o relleno para las bandas, conserva todo eso y adapta lo de abajo encima (si removeHistoryEntry ya devuelve un resultado discriminado, deriva de él el booleano «¿ha quedado borrada?» en vez de reemplazarlo).

RED primero: escribe los tests de behavior en usePersistedSession.test.ts (describe «tga:history — clave independiente de la partida», mismo arnés createFakeLocalStorage/makeEntry) y en useGameHistory.test.ts (describe «useGameHistory — ciclo record/reload/remove con localStorage falso» y un it aparte para las dos constantes de copy), ejecútalos y confirma que fallan por la razón esperada (retorno undefined / export ausente).

GREEN, capa de almacenamiento (usePersistedSession.ts): removeHistoryEntry pasa a devolver boolean. Ante lectura ilegible del envoltorio devuelve false sin escribir; ante envoltorio vacío devuelve true sin escribir; con envoltorio legible conserva exactamente el filtrado actual de «como máximo una coincidencia por id» sobre las entradas EN CRUDO (WR-08 y la supervivencia en disco de entradas ilegibles no cambian en esta tarea); si no hubo coincidencia devuelve true sin escribir; si la hubo, devuelve lo que devuelva writeRaw. Añádelo a la firma del return del composable sin cambiar nada más de ese objeto. Reescribe el comentario de removeHistoryEntry (hoy dice que devuelve void y por qué) para que diga qué significa true/false y por qué: IN-11, un borrado que no se escribe dejaba la tarjeta visible sin explicación. Actualiza la lista «Quién MIRA el resultado» del comentario de writeRaw para incluir removeHistoryEntry.

GREEN, composable (useGameHistory.ts): remove(id) devuelve boolean — guarda el resultado de removeHistoryEntry, recarga igual que hoy y devuelve ese resultado. Exporta DELETE_FAILED_HEADING y DELETE_FAILED_BODY como constantes de módulo string con los dos textos EXACTOS de behavior (copy como dato comprobable fuera del componente, mismo precedente que emptyTitle/emptyBody de buildStatisticsView). Esa copy solo afirma lo que false garantiza: con lectura ilegible no se intentó escribir y con setItem lanzando el almacenamiento no cambia, así que en los dos casos es cierto que «no ha cambiado nada»; no nombra causas que la app no ha comprobado. No contiene ninguna raíz vigilada.

GREEN, página (historico.vue): importa las dos constantes desde el composable y añade un ref booleano local (p. ej. borradoFallido; ningún identificador nuevo con raíces vigiladas). onConfirmarBorrado asigna a ese ref la negación de lo que devuelve remove(id) y cierra el diálogo como hoy; onAbrirBorrado lo pone a false (un intento nuevo retira el aviso anterior). Pinta el aviso EN EL FLUJO, justo debajo del header y antes del main, dentro de un contenedor shrink-0 con role="status" y aria-live="polite" que se renderiza SIEMPRE (sin v-if en la región viva; el v-if va en el contenido de dentro — mismo motivo documentado en HistorySavedNotice.vue desde 09-16: un lector de pantalla solo anuncia cambios de una región que ya estaba en el DOM). Contenido: mismo lenguaje visual que HistorySavedNotice.vue (bg-surface, border-b border-background, px-2xl py-lg, h2 text-heading font-bold text-primary-text con first-letter:text-warning para el ⚠, p text-body font-normal text-secondary-text) y un botón ✕ de 48×48 (w-12 h-12) con aria-label «Cerrar aviso» que pone el ref a false. NO lo hagas fixed ni superpuesto: al estar dentro del flex-col h-dvh, el main flex-1 overflow-y-auto encoge en vez de empujar la pantalla fuera del viewport (evita la clase de WR-05(b)) y no tapa los botones del header (evita la clase de WR-05 ronda 4). Sin autocierre por temporizador: se retira con ✕ o con el siguiente intento de borrado.

Decisión propia (discreción, documentar en SUMMARY): la propuesta original de IN-11 era reutilizar HistorySavedNotice; no se hace porque sus variantes son exclusivamente desenlaces de fin de partida producidos por planGameEnd (Gate C prohíbe escribir un NoticeVariant fuera de useHistorySavedNotice.ts) y porque 260923-3rm está reordenando precisamente esa banda.

Prohibido en useGameHistory.ts y historico.vue: literales entre comillas simples de StoredProgress o de NoticeVariant (Gate C, incluso en comentarios para StoredProgress), y cualquier let/new Set/new Map/ref( a nivel de módulo en useGameHistory.ts.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/usePersistedSession.test.ts app/composables/__tests__/useGameHistory.test.ts app/composables/__tests__/afirmacionesRespaldadas.test.ts app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts && npm run typecheck</automated>
  </verify>
  <done>
    - Los tests nuevos de behavior fallaron primero (RED registrado en el SUMMARY con el mensaje real) y ahora pasan; los tests previos de removeHistoryEntry (WR-08, CR-03, CR-01 ronda 3, SSR) siguen en verde.
    - `grep -c "DELETE_FAILED_HEADING" app/pages/historico.vue` ≥ 1 y `grep -c 'role="status"' app/pages/historico.vue` ≥ 1; el aviso no usa `fixed`.
    - afirmacionesRespaldadas.test.ts e invariantesDeMarcaDeEstado.test.ts en verde sin añadir ninguna entrada auditada nueva; `npm run typecheck` sale con 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tope y limpieza del histórico al registrar (IN-04 + IN-12) e id por crypto.randomUUID (IN-05)</name>
  <reversibility rating="costly">Una vez desplegado, appendHistoryEntry borra en los dispositivos del grupo las entradas que isGameHistoryEntry rechaza y la más antigua al pasar de 500; revertir el código no las recupera. Lo pidió el usuario explícitamente, las entradas rechazadas no se pueden ver, borrar ni sincronizar, y el tope queda a años del volumen real — se señala, no se bloquea.</reversibility>
  <files>app/composables/usePersistedSession.ts, app/composables/__tests__/usePersistedSession.test.ts, engine/history.ts, engine/__tests__/history.test.ts</files>
  <behavior>
    - HISTORY_MAX_ENTRIES se exporta desde usePersistedSession.ts y vale 500.
    - IN-12: con tga:history sembrado como formatVersion 1 con [buena-a, un objeto { id: 'rota' }, buena-b], appendHistoryEntry(nueva) devuelve el éxito de siempre y el JSON persistido contiene exactamente los ids [nueva, buena-a, buena-b] en ese orden, sin 'rota'.
    - IN-04: con 500 entradas válidas sembradas (e-0 la primera = la más reciente … e-499 la última), appendHistoryEntry(nueva) deja exactamente 500 entradas, la primera es nueva, e-498 es la última y e-499 ya no está.
    - IN-04: por debajo del tope no se poda nada (3 sembradas + 1 = 4).
    - D-04: con tga:history:synced sembrado con ['e-499'] y 500 entradas, appendHistoryEntry(nueva) no llama a setItem con la clave tga:history:synced y la cadena de esa clave sigue idéntica; readHistory() ya no devuelve e-499.
    - CR-03 intacto: los tests existentes con formatVersion 2 y con JSON no parseable siguen devolviendo el fallo de siempre con el blob idéntico byte a byte; y el test existente de «una entrada inválida no toca el blob» (CR-01 ronda 2) sigue igual, porque la guarda de la entrada nueva se evalúa antes de leer nada.
    - IN-05: con crypto.randomUUID disponible (Node 24 lo expone), buildHistoryEntry(...).id cumple /^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/ y dos entradas construidas con el mismo now tienen ids distintos.
    - IN-05: con globalThis.crypto sustituido por undefined (vi.stubGlobal) y Math.random forzado a 0, buildHistoryEntry no lanza y su id es exactamente `${NOON_UTC_MS}-0000000000` (el sufijo vacío que IN-05 describía queda cerrado).
  </behavior>
  <action>
Relee usePersistedSession.ts y engine/history.ts antes de tocarlos (260923-3rm y la Task 1 ya los han cambiado). Si appendHistoryEntry ya no devuelve boolean (WR-02 de 3rm), conserva su contrato de retorno tal cual: este plan solo cambia QUÉ array de entradas se escribe, nunca cuándo se aborta ni qué se devuelve. Ajusta las aserciones de retorno de los tests nuevos al contrato vigente.

RED primero: añade los tests de behavior (usePersistedSession.test.ts dentro del describe «tga:history — clave independiente de la partida»; history.test.ts sustituyendo el describe «id: aleatorio, con la forma esperada» y su test del regex antiguo — IN-05 pide expresamente actualizarlo). En history.test.ts importa además vi y afterEach desde vitest y restaura con vi.unstubAllGlobals() y vi.restoreAllMocks() en afterEach. Confirma que fallan por la razón esperada.

GREEN, IN-04 + IN-12 (usePersistedSession.ts): declara y exporta HISTORY_MAX_ENTRIES = 500 junto a HISTORY_FORMAT_VERSION (constante primitiva: no es estado de módulo mutable). En appendHistoryEntry, sin mover ni relajar sus dos guardas actuales (la entrada nueva debe pasar isGameHistoryEntry antes de leer; un envoltorio ilegible aborta sin escribir — CR-03/CR-01 ronda 3), las entradas previas pasan a filtrarse por isGameHistoryEntry antes de anteponer la nueva, y el array resultante se corta a los primeros HISTORY_MAX_ENTRIES elementos (el array se persiste de más reciente a más antigua porque cada registro antepone, así que lo que sale es lo más antiguo). No toques removeHistoryEntry en esta tarea: sigue conservando en crudo lo que no reconoce (el test CR-03 que lo fija no se cambia) — el único punto de mantenimiento es el registro, que ocurre una vez por partida. No leas ni escribas SYNCED_KEY desde appendHistoryEntry: la poda de marcas es de useHistorySync.ts (decisión 10-03, D-04) y ya retira en el siguiente flush cualquier id que no esté en readHistory(); como las pendientes se calculan desde loadHistory(), un id podado no puede volver a subirse. Nombres nuevos sin raíces vigiladas (Gate A barre el código de este fichero).

Reescribe con honestidad los comentarios que este cambio deja falsos, sin tocar su historia: (1) el bloque de appendHistoryEntry que hoy dice que las previas se conservan EN CRUDO y que lo que no valida se filtra «NUNCA de cara al DISCO» — ahora debe decir que IN-12 invierte eso para el registro, por qué (una entrada rechazada no se ve, no se puede borrar desde una tarjeta, no se sube a Firestore y ocupa cuota para siempre) y que removeHistoryEntry sigue siendo conservador; (2) la frase «la ÚNICA función de este fichero que no devuelve void», ya falsa (save, y desde la Task 1 removeHistoryEntry, también devuelven boolean); (3) el comentario de readEnvelope sobre quién filtra, para que diga que la lectura no filtra y que cada escritura decide; (4) junto a HISTORY_FORMAT_VERSION, una regla explícita: todo cambio de forma de GameHistoryEntry que una build anterior rechazaría DEBE subir HISTORY_FORMAT_VERSION, porque el envoltorio de versión desconocida es lo único que impide que una build antigua (rollback) descarte en su siguiente registro las entradas escritas por una más nueva; (5) el tope y su coste aceptado: una entrada que salga por el tope sin haberse subido nunca se pierde también del respaldo, asumido porque 500 partidas son años al ritmo del grupo (comentario de D-08 en useHistorySync.ts: decenas al año) y record() lanza flush() justo después de cada registro con éxito.

GREEN, IN-05 (engine/history.ts): el id deja de construirse con el sufijo base36 de Math.random. Extrae una función privada pequeña que recibe now: si globalThis.crypto?.randomUUID es una función, devuelve globalThis.crypto.randomUUID() invocado COMO MÉTODO de globalThis.crypto (una referencia suelta lanza «Illegal invocation» en navegador); si no (contexto no seguro, p. ej. un servidor de desarrollo abierto por IP de la LAN en la tablet), devuelve now, un guion y diez caracteres base36 de Math.random rellenados a la derecha con '0' hasta diez, de modo que el sufijo nunca queda vacío y buildHistoryEntry sigue sin lanzar (contrato de la cabecera del fichero). Decisión propia a documentar en el SUMMARY: UUID puro sin prefijo de instante (el orden lo da recordedAt vía sortEntriesByRecency, nunca el id; firestore.rules solo exige que id sea string y el id local es el id del documento, D-03 de la Fase 10, así que un UUID es un id de documento válido); los ids antiguos en localStorage y en Firestore no se migran. Actualiza el comentario del id en consecuencia; nada de esto toca Vue, DOM ni app/.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/usePersistedSession.test.ts engine/__tests__/history.test.ts app/composables/__tests__/useGameHistory.test.ts app/composables/__tests__/useHistorySync.test.ts app/composables/__tests__/afirmacionesRespaldadas.test.ts app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts && npm run typecheck</automated>
  </verify>
  <done>
    - RED de los tests nuevos registrado en el SUMMARY; ahora pasan junto con todos los tests previos de usePersistedSession/useGameHistory/useHistorySync/history (incluidos los CR-03 de formatVersion 2 y JSON corrupto y el D-04 de useHistorySync.test.ts, sin editarlos).
    - `grep -c "HISTORY_MAX_ENTRIES" app/composables/usePersistedSession.ts` ≥ 2 y `grep -c "randomUUID" engine/history.ts` ≥ 1.
    - `grep -c "SYNCED_KEY" app/composables/usePersistedSession.ts` no cambia respecto al valor de antes de esta tarea (appendHistoryEntry no gana ninguna referencia a esa clave).
    - Los dos gates de la Fase 9 y `npm run typecheck` en verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: % de victorias sin extremos falsos (IN-13), spec offline que no aborta sin audio (IN-07) y cierre en deferred-items.md</name>
  <files>engine/statistics.ts, engine/__tests__/statistics.test.ts, e2e/offline-flow.spec.ts, .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</files>
  <behavior>
    - winPercentage(199, 200) === 99; (200, 200) === 100; (1, 1) === 100; (1, 201) === 1; (0, 5) === 0; (1, 2) === 50; (2, 3) === 67; (3, 4) === 75; (0, 0) === 0.
    - aggregateStatistics con un héroe a 199 victorias de 200 y otro a 1 de 1: el primero sale con pct 99 y queda por debajo del segundo en la cascada D-24.
    - Los tests existentes de statistics.test.ts (D-25 «1 de 1» con pct 100, cascada D-24, CR-02, no-mutación) siguen en verde sin editarlos.
    - e2e: listando desde un directorio temporal con public/audio/ vacío, y desde otro sin public/audio/, Playwright lista «Total: 5 tests in 1 file» (hoy lista 0 porque el módulo aborta; comprobado al planificar).
  </behavior>
  <action>
IN-13 (engine/statistics.ts), RED primero en statistics.test.ts con los casos de behavior; luego GREEN: exporta una función pura winPercentage(wins, played) y úsala en buildRows en lugar del Math.round directo. Regla: 0 si played no es positivo; 100 solo si wins es igual o mayor que played; 0 solo si wins no es positivo; en cualquier otro caso el redondeo de siempre acotado al intervalo 1..99. Decisión propia a documentar en el SUMMARY y en el comentario: se acota también el extremo bajo (1 de 201 ya no pinta «0 %»), porque es el mismo defecto en la misma línea y la propia cabecera de engine/history.ts registra como antipatrón de esta fase endurecer un lado del contrato y dejar el vecino sin revisar. Conserva lo que 260923-3rm haya añadido a este fichero (p. ej. entriesWithVillain de WR-07). useGameHistory.ts no necesita cambios: ya pinta row.pct tal cual.

IN-07 (e2e/offline-flow.spec.ts): antes de editar, ejecuta la comprobación de listado del bloque verify y anota en el SUMMARY que hoy lista 0 tests (la línea de error real). Después elimina toda resolución del clip a nivel de módulo — tanto el readdirSync como el error lanzado cuando no hay .m4a, que hoy abortan la suite entera, incluidos los tests de /historico y /estadisticas que no dependen del audio. Sustitúyelo por una función de ayuda que devuelve el id del primer .m4a de public/audio/ (sin recursión, así public/audio/_probe/ sigue excluido como hoy) o null, envolviendo la lectura del directorio en try/catch para que un directorio ausente también dé null. Solo el primer test (el que comprueba en su paso 7 que un clip se sirve sin red) la invoca, al principio de su cuerpo, seguido de test.skip(condición de id nulo, motivo en español que diga que no hay ningún clip .m4a en public/audio/ y que el paso 7 no se puede comprobar en este checkout); el resto del test usa ese id donde hoy usa la constante de módulo. Actualiza el comentario de cabecera y el de la derivación del id: el skip solo ocurre en un checkout sin clips, se informa como skipped (nunca como passed), y en CI los clips están versionados, así que allí el test se ejecuta entero — no relaja ninguna aserción, que es lo que la cabecera del fichero prohíbe.

deferred-items.md: en la sección «INFO de 09-REVIEW.md que son deuda real (IN-04, IN-05, IN-07, IN-11, IN-12, IN-13)», añade a CADA una de las seis viñetas una línea de continuación sangrada que empiece por «**CERRADO (quick 260923-3rl):**» y diga en una frase qué se hizo y qué test lo fija: IN-04 (tope HISTORY_MAX_ENTRIES = 500 en appendHistoryEntry, usePersistedSession.test.ts), IN-05 (crypto.randomUUID con respaldo sin lanzar, engine/__tests__/history.test.ts), IN-07 (test.skip en el test del paso 7, comprobación de listado sin public/audio), IN-11 (removeHistoryEntry y remove() devuelven boolean, aviso en línea en /historico en vez de HistorySavedNotice y por qué; usePersistedSession.test.ts y useGameHistory.test.ts), IN-12 (appendHistoryEntry descarta del disco lo que isGameHistoryEntry rechaza, removeHistoryEntry sigue conservador, regla de subir HISTORY_FORMAT_VERSION; usePersistedSession.test.ts), IN-13 (winPercentage, acotado también el 0 %; engine/__tests__/statistics.test.ts). Corrige de paso las rutas con número de línea desfasado que citan esas viñetas si lo haces sin reescribir su texto original. NO toques la mención a IN-05 de la «Nota de cierre (ronda 6)»: es otro hallazgo distinto (normalización de comillas del gate, ronda 5), ya cerrado por 09-30. No edites ninguna sección WR (son de 260923-3rm). En ninguna anotación afirmes que se hizo la comprobación humana en tablet (DEV-02 sigue abierta).
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/statistics.test.ts app/composables/__tests__/useGameHistory.test.ts && ROOT=$(git rev-parse --show-toplevel) && T1=$(mktemp -d) && T2=$(mktemp -d) && mkdir -p "$T1/public/audio" && (cd "$T1" && "$ROOT/node_modules/.bin/playwright" test --config "$ROOT/playwright.config.ts" offline-flow --list) | grep -q "Total: 5 tests in 1 file" && (cd "$T2" && "$ROOT/node_modules/.bin/playwright" test --config "$ROOT/playwright.config.ts" offline-flow --list) | grep -q "Total: 5 tests in 1 file" && npx playwright test e2e/offline-flow.spec.ts --list | grep -q "Total: 5 tests in 1 file" && [ "$(grep -v '^\s*//' e2e/offline-flow.spec.ts | grep -c 'throw new Error')" = "0" ] && [ "$(grep -c 'CERRADO (quick 260923-3rl)' .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md)" -ge 6 ]</automated>
  </verify>
  <done>
    - RED de statistics.test.ts registrado; ahora pasa con todos los tests previos de estadísticas.
    - Listado de Playwright: 0 tests antes del cambio (anotado), 5 después, desde los dos directorios temporales y desde la raíz.
    - La spec completa ejecutada una vez desde la raíz con `npx playwright test e2e/offline-flow.spec.ts` (levanta `npm run generate` + preview por su webServer; darle hasta 10 min; Chromium está instalado en este entorno): 5 passed, 0 skipped — en este checkout los clips existen, así que el test del paso 7 NO debe salir como skipped. Resultado literal en el SUMMARY.
    - Seis anotaciones «CERRADO (quick 260923-3rl)» en la sección IN de deferred-items.md y ninguna sección WR modificada por esta tarea (`git diff` de ese fichero solo toca la sección IN).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| window.localStorage → app | tga:history y tga:history:synced son entrada no fiable (edición manual, build futura o pasada, escritura interrumpida); la app decide qué reescribe a partir de ella |
| navegador (Web Crypto) → engine | crypto.randomUUID puede faltar fuera de contexto seguro; el motor no puede lanzar por ello |
| app → Firestore (sin cambios) | la subida sigue siendo create-only; este plan solo cambia qué ids existen en local |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3rl-01 | Tampering | appendHistoryEntry (descarte IN-12) | medium | mitigate | Solo se descartan entradas dentro de un envoltorio formatVersion 1 legible; un envoltorio de versión desconocida o JSON corrupto sigue abortando sin escribir (tests CR-03 existentes, sin editar). Regla escrita junto a HISTORY_FORMAT_VERSION: todo cambio de forma que una build anterior rechazaría sube la versión, para que un rollback nunca descarte entradas de una build más nueva. |
| T-3rl-02 | Denial of Service | tga:history (cuota de localStorage) | medium | mitigate | Tope HISTORY_MAX_ENTRIES = 500 aplicado en cada registro (IN-04), fijado por test con 500 entradas sembradas. |
| T-3rl-03 | Repudiation | pérdida del respaldo de la entrada más antigua | low | accept | Una entrada que salga por el tope sin haberse subido nunca se pierde también en Firestore; 500 partidas son años al ritmo del grupo y record() lanza flush() tras cada registro con éxito. Documentado en el comentario del tope. |
| T-3rl-04 | Tampering | tga:history:synced (contabilidad D-04) | low | mitigate | appendHistoryEntry nunca lee ni escribe SYNCED_KEY (test que cuenta setItem de esa clave); la poda existente de useHistorySync.ts retira el id podado en el siguiente flush y las pendientes salen de loadHistory(), así que no hay resubida; las reglas create-only hacen inocuo cualquier reintento. |
| T-3rl-05 | Information Disclosure | copy del aviso de borrado fallido | low | mitigate | La copy solo afirma lo que false garantiza (almacenamiento sin cambios), sin raíces vigiladas; afirmacionesRespaldadas.test.ts en verde sin excepción auditada nueva. |
| T-3rl-06 | Denial of Service | buildHistoryEntry sin crypto.randomUUID | low | mitigate | Respaldo que nunca lanza y nunca deja el sufijo vacío, fijado por test con globalThis.crypto sustituido y Math.random forzado a 0. |
| T-3rl-07 | Spoofing | predictibilidad del id | low | accept | El id no es un secreto ni un token: las reglas de Firestore son create-only y un id repetido solo produce permission-denied. randomUUID usa el CSPRNG del navegador; el respaldo con Math.random solo aplica fuera de contexto seguro, nunca en producción (Vercel sirve HTTPS). |
</threat_model>

<verification>
Desde la raíz del repo (o del worktree), en este orden:
1. `npm test` — suite completa de Vitest (proyectos engine y app-logic) en verde, sin ningún test previo editado salvo el regex de id de engine/__tests__/history.test.ts (IN-05 lo pide) y las aserciones de retorno añadidas a los tests existentes de removeHistoryEntry.
2. `npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` — los dos gates de la Fase 9 en verde, sin ninguna entrada nueva en AFIRMACIONES_AUDITADAS ni en las excepciones del gate de invariantes.
3. `npm run typecheck` — sale con 0 (vue-tsc sobre app/, que arrastra engine/).
4. `npx playwright test e2e/offline-flow.spec.ts` — 5 passed, 0 skipped.
5. `git diff --stat` solo muestra los once ficheros de files_modified.
</verification>

<success_criteria>
- IN-04, IN-05, IN-07, IN-11, IN-12 e IN-13 cerrados en código con test que lo fija, y anotados CERRADO (quick 260923-3rl) en deferred-items.md.
- Borrar una partida cuando el navegador no deja escribir muestra el aviso en /historico; el camino feliz del borrado no cambia.
- tga:history nunca supera 500 entradas ni conserva entradas que isGameHistoryEntry rechaza tras un registro; CR-03 (no machacar un envoltorio ilegible) y D-04 (la poda de marcas vive en useHistorySync.ts) intactos.
- `npm test`, `npm run typecheck`, los gates de la Fase 9 y la spec offline en verde.
</success_criteria>

<output>
Crear `.planning/quick/260923-3rl-phase-9-info-debt-in-planning-phases-09-hist-rico-y-estad-st/260923-3rl-SUMMARY.md` al terminar, con: el RED real de cada tarea, el listado de Playwright antes (0) y después (5), el resultado literal de la spec completa, las tres decisiones propias (aviso en línea en vez de HistorySavedNotice; UUID puro; acotado también del 0 %), y qué encontró ya cambiado por 260923-3rm y cómo lo conservó.
</output>
