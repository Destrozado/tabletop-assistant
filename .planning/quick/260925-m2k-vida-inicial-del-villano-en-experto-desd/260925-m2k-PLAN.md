---
phase: quick-260925-m2k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/catalogue/fetch-marvelcdb.mjs
  - engine/types.ts
  - engine/catalogueSchema.ts
  - content/marvel-characters.json
  - engine/__tests__/catalogueSchema.test.ts
  - engine/__tests__/characters.test.ts
  - engine/counters.ts
  - engine/__tests__/counters.test.ts
  - engine/__tests__/stepValues.test.ts
  - content/marvel-champions.json
  - .planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md
  - .planning/todos/completed/experto-etapas-de-villano-rhino-ultron-por-verificar.md
autonomous: true
requirements: [QUICK-260925-m2k]

must_haves:
  truths:
    - "En Experto con 2 jugadores, el dial de vida inicial de Rhino marca 30 (etapa II, 15/jug) y el de Ultron 44 (etapa II, 22/jug)"
    - "En Normal con 2 jugadores, Rhino sigue en 28 y Ultron en 34 (etapa I, sin cambios)"
    - "Kang no cambia: normal 3 jug = 36, experto 3 jug = 45 (etapa I con cifras exp_kang)"
    - "El script del catálogo aborta sin escribir nada si la etapa inicial de Experto anotada a mano no coincide con el texto de la carta 1A del plan principal"
    - "El paso setup.escenario.04 en Experto dice que las cartas las marca el plan 1A y que en la caja básica son las etapas II y III, sin tocar el audio pregenerado"
  artifacts:
    - path: "scripts/catalogue/fetch-marvelcdb.mjs"
      provides: "VILLAIN_SCENARIOS (dato a mano expertStartStage + código de plan 1A) y comprobación cruzada contra la carta"
      contains: "expertStartStage"
    - path: "content/marvel-characters.json"
      provides: "expertStartStage en cada villano (rhino 2, kang 1, ultron 2)"
      contains: "expertStartStage"
    - path: "engine/counters.ts"
      provides: "computeInitialVillainHealth arranca en la etapa de Experto"
      contains: "expertStartStage"
  key_links:
    - from: "engine/counters.ts computeInitialVillainHealth"
      to: "CatalogueVillain.expertStartStage"
      via: "selección de etapa cuando difficulty === 'expert'"
      pattern: "expertStartStage"
    - from: "engine/catalogueSchema.ts VillainSchema"
      to: "engine/types.ts CatalogueVillain"
      via: "mismo campo opcional en los dos"
      pattern: "expertStartStage"
---

<objective>
Corregir la vida inicial del villano en modo Experto: Rhino y Ultron (Core Set) empiezan en la etapa II en Experto (RR v1.7 p.28 «using the listed expert mode villain stages» + cara 1A del plan principal: «(Rhino (II) and Rhino (III) instead for expert mode.)»), pero hoy `computeInitialVillainHealth` precarga siempre la etapa I. Confirmado por el usuario con las cartas físicas y por la API de MarvelCDB (decisiones cerradas, no reabrir).

Purpose: fidelidad de reglas — un dial que marca 28 cuando la carta en la mesa dice 30 es guiar mal (CLAUDE.md §Constraints).
Output: dato a mano `expertStartStage` por villano en el script (verificado contra la carta 1A), catálogo regenerado, motor corregido, tests, comentarios y texto del paso de Experto actualizados, y el todo movido a completed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@.planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md
@scripts/catalogue/fetch-marvelcdb.mjs
@engine/counters.ts

<interfaces>
Estado actual (extraído del código; no hace falta explorar más):

engine/types.ts (l.~266-300 comentario + VillainStage; l.~326 CatalogueVillain):
  export interface VillainStage { stage: number; health: number; healthPerHero: boolean; healthPerGroup: boolean; expert?: { health: number; healthPerHero: boolean; healthPerGroup: boolean } }
  export interface CatalogueVillain { id: string; name: string; stages: VillainStage[] }
  El bloque de comentario sobre VillainStage (l.~276-278) afirma: «Su ausencia es un hecho del dominio ... el modo Experto de ese escenario no sustituye las cartas de villano numeradas (caso de Rhino y Ultron, villanos del Core Set).» — FALSO, hay que corregirlo.

engine/catalogueSchema.ts: `VillainSchema = z.strictObject({ id, name, stages: z.array(VillainStageSchema).min(1) })`; superRefine al final del CharacterCatalogueSchema con bucle DC-01 (etapas consecutivas desde 1). Es strictObject: una clave nueva en el JSON sin añadirla al esquema hace fallar characters.test.ts.

engine/counters.ts computeInitialVillainHealth(villain, playerCount, difficulty): hoy usa `villain.stages[0]` y, si difficulty==='expert' y la etapa tiene `expert`, usa esas cifras. Guardas: villain null -> null; sin etapa -> null; playerCount no entero positivo -> null; health no finito -> null. Nunca lanza. Consumidores: engine/stepValues.ts resolveStepValue y resolveCounterValues (no cambian de firma).

Catálogo actual (content/marvel-characters.json, villanos en orden rhino, kang, ultron):
  rhino etapas 14/15/16 perHero true; kang 12/18/20 (true/false/true) con expert 15/22/25 (true/false/true); ultron 17/22/27 perHero true.

Cartas 1A verificadas hoy con la API (`GET https://marvelcdb.com/api/public/card/{code}.json`):
  01097a name "The Break-In!", type_code main_scheme, card_set_code rhino, text contiene «(Rhino (II) and Rhino (III) instead for expert mode.)»
  01137a name "The Crimson Cowl", type_code main_scheme, card_set_code ultron, text contiene «<i>(Ultron (II) and Ultron (III) instead for expert mode.)</i>»
  11007a name "Kang's Arrival", type_code main_scheme, card_set_code kang, text SIN ninguna línea «instead for expert mode» (su Experto va por el set exp_kang, ya modelado con `expert` por etapa).

content/marvel-champions.json, paso setup.escenario.04, variants.difficulty.expert:
  text: "Usad las cartas de villano numeradas (etapas) que indica el escenario para Experto."
  speech: "Usad las cartas de villano numeradas que indica el escenario para el modo Experto."
  `speech` está hasheado en scripts/voice/manifest.json (entrada setup.escenario.04.expert, hash f0d1e2a9a62b7a3f) y lo vigila engine/__tests__/voice-drift.test.ts -> NO se toca `speech` (regenerar audio es de pago). `text` tiene tope de 90 caracteres (TextBlockSchema) y engine/__tests__/content.test.ts localiza el paso de sustitución con /cartas de villano/i sobre variants.difficulty.expert.text y exige que el texto experto difiera del normal y del base.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dato a mano expertStartStage en el script, comprobado contra la carta 1A, esquema/tipos y catálogo regenerado</name>
  <files>scripts/catalogue/fetch-marvelcdb.mjs, engine/types.ts, engine/catalogueSchema.ts, content/marvel-characters.json, engine/__tests__/catalogueSchema.test.ts, engine/__tests__/characters.test.ts</files>
  <behavior>
    - catalogueSchema: acepta un villano con `expertStartStage: 2` y 3 etapas; acepta un villano SIN `expertStartStage` (baseCatalogue actual sigue válido); lanza ZodError con `expertStartStage: 0`, `1.5`, `'2'` y con `expertStartStage: 4` en un villano de 3 etapas (superRefine: debe ser <= número de etapas).
    - characters.test (catálogo real validado): rhino.expertStartStage === 2, ultron.expertStartStage === 2, kang.expertStartStage === 1.
    - characters.test: el test «Rhino y Ultron: ninguna etapa lleva la clave expert» se reescribe con la semántica correcta: Rhino y Ultron no tienen set de villano Experto alternativo (sus etapas no llevan `expert`), y su Experto se modela con `expertStartStage: 2` (usan sus propias etapas II y III). El test de Kang 15/22/25 no cambia.
  </behavior>
  <action>
Tipos y esquema (mantener los dos en sincronía): en engine/types.ts añadir a `CatalogueVillain` el campo opcional `expertStartStage?: number` con comentario: etapa (1..n) con la que empieza la partida en modo Experto según la cara 1A del plan principal («listed expert mode villain stages», RR v1.7 p.28); ausente = 1. Reescribir el párrafo falso del bloque de VillainStage (l.~276-278): la ausencia de `expert` significa que el escenario no trae un set de villano Experto con cifras propias (Rhino, Ultron), NO que el Experto no cambie las cartas: Rhino y Ultron usan en Experto sus etapas II y III, y eso se modela con `expertStartStage` en el villano, no con `expert` en la etapa. En engine/catalogueSchema.ts añadir a `VillainSchema` `expertStartStage: z.number().int().positive().optional()` (después de `stages`) y, en el superRefine, un bucle que añada issue si `expertStartStage` existe y es mayor que `villain.stages.length`, con mensaje en inglés al estilo de los existentes. Añadir los casos del behavior a catalogueSchema.test.ts (describe nuevo `expertStartStage`).

Script (fuente de verdad = dato a mano; la carta es comprobación cruzada, per decisión del usuario): en scripts/catalogue/fetch-marvelcdb.mjs añadir una constante `VILLAIN_SCENARIOS` (junto a VILLAIN_STAGE_CARDS) con una fila por villano, en el mismo orden rhino, kang, ultron: `{ villainName, mainSchemeCode, expectedMainSchemeName, expectedSetCode, expertStartStage }` — Rhino `01097a` "The Break-In!" `rhino` 2; Kang `11007a` "Kang's Arrival" `kang` 1; Ultron `01137a` "The Crimson Cowl" `ultron` 2. Comentario encima: por qué es dato a mano (el texto libre es frágil), qué significa, que Kang arranca en 1 con las cifras `expert` de exp_kang, y que Klaw sigue fuera (D-02). Añadir una función `checkExpertStartStage(row)` que: descarga la carta con `fetchCard` (mismo pacing); exige `type_code === 'main_scheme'`, `name === expectedMainSchemeName` y `card_set_code === expectedSetCode` (lanza con mensaje en español como el resto); quita etiquetas HTML del `text` (`replace(/<[^>]+>/g, '')`) y busca con una regex del tipo `/\(\s*(.+?) \((I{1,3})\) and \1 \((I{1,3})\) instead for expert mode\.?\s*\)/` (la retro-referencia exige el mismo nombre en las dos etapas). Reglas: si `expertStartStage === 1`, la carta NO debe contener `instead for expert mode` (si lo contiene, lanza: el dato a mano está desfasado); si `expertStartStage > 1`, debe haber coincidencia, el nombre capturado debe ser `villainName` y `STAGE_MAP[captura etapa]` debe ser igual a `expertStartStage`, si no lanza citando el valor a mano y el fragmento leído. No devuelve texto de carta: nada del `text` entra al catálogo (CAT-04). En `main()`: antes de escribir, validar que el conjunto de `villainName` de VILLAIN_SCENARIOS y de VILLAIN_STAGE_CARDS coincide exactamente (lanza si falta o sobra alguno) y que `expertStartStage` es entero entre 1 y el número de etapas declaradas del villano; ejecutar `checkExpertStartStage` para cada fila; al construir el objeto villano, añadir `expertStartStage` DESPUÉS de `stages` (orden de claves determinista, D-10) para todos los villanos (también Kang = 1, para que el dato sea explícito). Todo sigue siendo en memoria antes de `writeCatalogue` (D-05). Actualizar comentarios desfasados: cabecera CAT-04 (l.~33-35: el villano ahora lleva también `expertStartStage`), paso 1 de CAT-07 (l.~41-48: quitar «caso de Rhino y Ultron: villanos del Core Set sin set alternativo» como si no cambiaran; añadir que un villano nuevo necesita su fila en VILLAIN_SCENARIOS), párrafo l.~133-137 («no sustituye sus cartas numeradas (Rhino, Ultron)» es falso: reescribir como en types.ts), y el recuento de peticiones de `main()` (35 -> 38, ~53 s -> ~57 s).

Regenerar: ejecutar `npm run catalogue:generate` (red disponible). El diff de content/marvel-characters.json debe limitarse a tres líneas `"expertStartStage": N` (rhino 2, kang 1, ultron 2) más las comas; si cambia cualquier cifra, parar e informar. Si la red fallara de forma persistente, editar el JSON a mano de forma idéntica a lo que emitiría el script (clave tras `stages`) e indicarlo en el SUMMARY (excepción consciente a D-09). Después, actualizar characters.test.ts según el behavior (reemplazar el test de l.~248-262 y su comentario).

Commit: `fix(catalogo): etapa inicial de Experto por villano, comprobada contra la carta 1A del plan principal` (con la línea Co-Authored-By indicada por el entorno).
  </action>
  <verify>
    <automated>cd /Users/vcompanyb/TableGameAssistant && npx vitest run engine/__tests__/catalogueSchema.test.ts engine/__tests__/characters.test.ts && test "$(grep -c '"expertStartStage"' content/marvel-characters.json)" = 3</automated>
  </verify>
  <done>El script tiene VILLAIN_SCENARIOS y comprueba cada carta 1A (aborta si no coinciden); el catálogo trae rhino 2 / kang 1 / ultron 2 y ninguna cifra ha cambiado; tipos y esquema aceptan el campo opcional y rechazan valores fuera de rango; ya no quedan comentarios que digan que Rhino/Ultron no cambian en Experto; los tests del catálogo pasan; commit hecho.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeInitialVillainHealth arranca en la etapa de Experto, tests del motor y texto del paso setup.escenario.04</name>
  <files>engine/counters.ts, engine/__tests__/counters.test.ts, engine/__tests__/stepValues.test.ts, content/marvel-champions.json</files>
  <behavior>
    - computeInitialVillainHealth(rhino, 2, 'expert') === 30; (ultron, 2, 'expert') === 44; (rhino, 2, 'normal') === 28; (ultron, 2, 'normal') === 34.
    - Kang sin cambios: (kang, 3, 'normal') === 36, (kang, 3, 'expert') === 45; se mantiene el test D-11 «nunca 18 ni 54».
    - El test actual «Ultron ... experto también 51 (sin cifras propias de experto)» (counters.test.ts l.~56-58) pasa a: normal 3 jug = 51, experto 3 jug = 66 (etapa II, 22 x 3).
    - Defensivo (con villanos fabricados en el test, sin tocar el catálogo real): expertStartStage ausente en Experto -> usa la etapa 1 (comportamiento anterior); expertStartStage fuera de rango (p. ej. 5 con 3 etapas), 0, 1.5 o no numérico -> null, sin lanzar; en Normal se ignora expertStartStage aunque sea inválido (sigue saliendo la etapa 1).
    - stepValues.test: un caso nuevo de Rhino o Ultron en Experto a través de resolveStepValue('villainHealth', ...) que devuelva la cifra de la etapa II (p. ej. Ultron 3 jug = 66), usando el mismo helper `contextWith` del caso de Kang (l.~44).
  </behavior>
  <action>
En engine/counters.ts, reescribir `computeInitialVillainHealth` manteniendo firma y todas las guardas actuales (villain null -> null; playerCount no entero positivo -> null; health no finito -> null; nunca lanza, nunca NaN): calcular `startStage` = 1 en Normal; en Experto, `villain.expertStartStage ?? 1`. Si `startStage` no es entero positivo, devolver null. Localizar la etapa con `villain.stages.find(s => s.stage === startStage)` (el esquema garantiza etapas consecutivas desde 1, pero el `find` por número no depende de ello); si no existe, null. Las cifras son las de `expert` de esa etapa cuando difficulty es 'expert' y la etapa lo trae (caso Kang, etapa 1 con exp_kang), si no las planas de la etapa. Sustituir el comentario D-11 de encima: la precarga es la etapa con la que EMPIEZA la partida — la I en Normal y la que marca `expertStartStage` en Experto (Rhino/Ultron: II, según la cara 1A del plan principal y RR v1.7 p.28 «listed expert mode villain stages»; Kang: I con cifras exp_kang) —; la banda sigue sin saber de cambios de etapa durante la partida; una etapa inexistente o un `expertStartStage` inválido devuelve null en vez de adivinar. Actualizar los tests según el behavior (counters.test.ts: casos nuevos en el describe «precarga (D-09/D-11/HP-05)»; los villanos fabricados se construyen como `CatalogueVillain` con spread del de Rhino real y el `expertStartStage` a probar, casteando con `as any` para los valores no numéricos).

En content/marvel-champions.json, paso `setup.escenario.04`, cambiar SOLO `variants.difficulty.expert.text` a un texto genérico para todos los villanos que diga que las cartas las marca el plan principal 1A y que en la caja básica son las etapas II y III. Propuesta (83 caracteres): «Usad las cartas de villano del plan 1A para Experto (caja básica: etapas II y III).». Límites: como mucho 90 caracteres (TextBlockSchema), debe contener «cartas de villano» (content.test.ts lo usa para encontrar el paso) y debe seguir siendo distinto del texto normal y del base. NO tocar `speech` (ni del paso ni de la variante): está hasheado en scripts/voice/manifest.json y cambiarlo obligaría a regenerar audio de pago; la locución genérica actual («...que indica el escenario para el modo Experto») sigue siendo correcta. El esquema no permite texto por villano; no inventar ese mecanismo. No tocar la variante normal.

Pasar la suite completa y el typecheck (package.json tiene `typecheck: nuxt typecheck`; si falla por motivos del entorno ajenos a este cambio, anotarlo en el SUMMARY). Buscar comentarios desfasados que queden: `grep -rn "no sustituye\|sin set alternativo\|experto también" engine app scripts` no debe devolver nada que diga que Rhino/Ultron no cambian en Experto.

Commit: `fix(experto): la vida inicial del villano sale de la etapa con la que empieza Experto` (con la línea Co-Authored-By indicada por el entorno).

Por último, sin commitear (los documentos los commitea el orquestador): `git mv .planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md .planning/todos/completed/` y, en el fichero movido, cambiar la línea `status: pending` del frontmatter por `status: completed` y añadir `completed: 2026-09-25` y `resolved_by: quick 260925-m2k`.
  </action>
  <verify>
    <automated>cd /Users/vcompanyb/TableGameAssistant && npx vitest run && npm run typecheck && test -f .planning/todos/completed/experto-etapas-de-villano-rhino-ultron-por-verificar.md && ! test -f .planning/todos/pending/experto-etapas-de-villano-rhino-ultron-por-verificar.md</automated>
  </verify>
  <done>Rhino 2 jug Experto = 30, Ultron 2 jug Experto = 44, Normal sin cambios (28/34), Kang sin cambios (36/45); guardas defensivas cubiertas por tests; el texto experto de setup.escenario.04 explica el plan 1A y las etapas II y III sin que cambie ningún `speech` (voice-drift en verde); la suite completa y el typecheck pasan; commit hecho; el todo está movido a completed con status completed y sin commitear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MarvelCDB API -> script del catálogo | Datos de terceros entran al JSON committeado (solo a mano, nunca en build) |
| localStorage -> motor | Sin cambios; el catálogo es contenido del repo, no entrada del usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-m2k-01 | Tampering | checkExpertStartStage (fetch-marvelcdb.mjs) | mitigate | El dato a mano manda; la carta solo se usa para comparar, y cualquier discrepancia aborta sin escribir (D-05). No se copia texto de carta al catálogo (CAT-04); el esquema strictObject rechaza claves inesperadas |
| T-m2k-02 | Denial of service | computeInitialVillainHealth | mitigate | Un expertStartStage malformado o fuera de rango devuelve null (el dial queda sin valor) en vez de lanzar o dar NaN; tests defensivos lo cubren |
| T-m2k-03 | Information disclosure | content/marvel-characters.json | accept | Solo números y nombres; el campo nuevo es un entero, sin texto con copyright |
</threat_model>

<verification>
- `npx vitest run` en verde (incluidos characters, catalogueSchema, counters, stepValues, content y voice-drift).
- `npm run typecheck` en verde.
- `git diff` de content/marvel-characters.json: solo las tres claves `expertStartStage`.
- `grep -rn "expertStartStage" engine/types.ts engine/catalogueSchema.ts engine/counters.ts scripts/catalogue/fetch-marvelcdb.mjs` encuentra el campo en los cuatro.
</verification>

<success_criteria>
- En Experto el dial de vida inicial de Rhino y Ultron muestra la etapa II x jugadores; en Normal y para Kang no cambia nada.
- El script falla con un error claro si el dato a mano deja de coincidir con la carta 1A.
- Ningún comentario ni test dice ya que Rhino/Ultron no cambian en Experto.
- No se regenera ni invalida ningún audio.
</success_criteria>

<output>
Crear `.planning/quick/260925-m2k-vida-inicial-del-villano-en-experto-desd/260925-m2k-SUMMARY.md` al terminar.
</output>
