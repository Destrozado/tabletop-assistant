---
phase: quick-260923-3rm
plan: "01"
type: execute
wave: 1
depends_on: ["260923-3rl", "260923-3rn"]
files_modified:
  - app/app.vue
  - app/components/UpdateBanner.vue
  - app/components/HistorySavedNotice.vue
  - app/components/GameSelectorScreen.vue
  - app/components/MiniSetupScreen.vue
  - app/components/MesaListaScreen.vue
  - app/components/GameOutcomeDialog.vue
  - app/pages/historico.vue
  - app/pages/estadisticas.vue
  - app/pages/[game]/index.vue
  - app/composables/useDialogFocusTrap.ts
  - app/composables/useGameHistory.ts
  - app/composables/usePersistedSession.ts
  - app/composables/useStoredProgress.ts
  - app/composables/useProgressMountPlan.ts
  - app/composables/__tests__/pilaDeAvisos.test.ts
  - app/composables/__tests__/useDialogFocusTrap.test.ts
  - app/composables/__tests__/useGameHistory.test.ts
  - app/composables/__tests__/usePersistedSession.test.ts
  - app/composables/__tests__/useStoredProgress.test.ts
  - app/composables/__tests__/useProgressMountPlan.test.ts
  - app/composables/__tests__/afirmacionesRespaldadas.test.ts
  - engine/types.ts
  - engine/history.ts
  - engine/statistics.ts
  - engine/__tests__/history.test.ts
  - engine/__tests__/statistics.test.ts
  - e2e/notice-stack.spec.ts
  - e2e/game-outcome-dialog.spec.ts
  - e2e/unreadable-storage.spec.ts
  - .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
autonomous: true
tags: [phase-9-debt, warnings, layout, a11y, localStorage, history, statistics]

must_haves:
  truths:
    - "Con UpdateBanner y HistorySavedNotice visibles a la vez, las dos bandas se apilan en una sola franja en flujo encima de la pantalla: ninguna tapa a la otra, ninguna tapa un control de la pantalla y ningún toque atraviesa una banda hacia un botón oculto (WR-04 r4, WR-05 r4)"
    - "Mientras hay una banda visible, la pantalla de debajo pierde exactamente esa altura en vez de sumarla: ninguna pantalla h-dvh queda empujada fuera del viewport (WR-05(b) sigue cerrado con el mecanismo nuevo)"
    - "Con GameOutcomeDialog abierto, Tab y Shift+Tab nunca sacan el foco del diálogo; Escape sigue sin cerrarlo (decisión de 09-UI-SPEC §Layout 2); la salida por teclado es «Salir sin registrar» + Enter (WR-02 r4); al cerrar nunca se llama focus() sobre un nodo desprendido (WR-03 r4)"
    - "Un reintento de registro tras un fallo registra la duración y la fecha del PRIMER «Partida terminada» en esa misma posición, no las del día del reintento (WR-06 r4, D-08 intacto: reloj de pared sin tope desde startedAt)"
    - "/historico con un tga:history ilegible ya no dice «Todavía no hay partidas registradas»: explica el estado y, si el blob se leyó pero no se sabe interpretar, ofrece apartarlo; tras apartarlo el blob vive byte a byte en tga:history:backup-<ts>, tga:history queda ausente y las partidas nuevas vuelven a registrarse (WR-02)"
    - "Tras una lectura fallida del progreso al montar, el primer autoguardado de la partida nueva copia antes el blob anterior a tga:progress:<gameId>:backup-<ts>, y mientras la clave siga sin poder leerse no escribe encima; el aviso del mini-setup dice exactamente eso (WR-02 r6)"
    - "/estadisticas enseña una leyenda de muestra por tabla: la de héroes cuenta entradas con héroe, la de villanos cuenta entradas con villano (WR-07)"
    - "Los gates de la Fase 9 siguen en verde: npx vitest run (incluidos afirmacionesRespaldadas, invariantesDeMarcaDeEstado, avisoTrasRegistroFallido) y npm run typecheck"
  artifacts:
    - path: "app/app.vue"
      provides: "#app-root como columna h-dvh; franja de avisos shrink-0 en flujo; envoltorio de página flex-1 min-h-0"
      contains: "flex-1 min-h-0"
    - path: "app/composables/__tests__/pilaDeAvisos.test.ts"
      provides: "gate estructural de WR-04 r4 / WR-05 r4 / WR-05(b)"
    - path: "app/composables/useDialogFocusTrap.ts"
      provides: "nextTrappedIndex, resolveRestoreTarget, useDialogFocusTrap"
      exports: ["nextTrappedIndex", "resolveRestoreTarget", "useDialogFocusTrap"]
    - path: "engine/history.ts"
      provides: "freezeEndInstant + instante de referencia congelado en buildHistoryEntry"
      contains: "freezeEndInstant"
    - path: "app/composables/usePersistedSession.ts"
      provides: "readHistoryState, archiveUnreadableHistory, backupProgressBeforeOverwrite, createOverwriteGuard"
      contains: "createOverwriteGuard"
    - path: "engine/statistics.ts"
      provides: "StatisticsSummary.entriesWithVillain"
      contains: "entriesWithVillain"
    - path: "e2e/unreadable-storage.spec.ts"
      provides: "prueba en navegador real de WR-02 (apartar histórico) y WR-02 r6 (copia antes de sobrescribir)"
  key_links:
    - from: "app/app.vue (franja de avisos)"
      to: "UpdateBanner.vue / HistorySavedNotice.vue"
      via: "hermanas en flujo dentro de un único contenedor shrink-0, sin posicionamiento fijo"
      pattern: "shrink-0"
    - from: "app/components/GameOutcomeDialog.vue"
      to: "app/composables/useDialogFocusTrap.ts"
      via: "useDialogFocusTrap(panel)"
      pattern: "useDialogFocusTrap\\(panel\\)"
    - from: "app/pages/[game]/index.vue onOutcomeRecorded"
      to: "engine/history.ts freezeEndInstant"
      via: "stampEndOfGame (useGameHistory.ts) antes de record()"
      pattern: "stampEndOfGame"
    - from: "app/pages/[game]/index.vue (watchDebounced, pagehide, onOutcomeRecorded)"
      to: "usePersistedSession.ts createOverwriteGuard"
      via: "progressWriter.save(...) armado cuando planProgressMount devuelve unverifiedNotice"
      pattern: "progressWriter\\.save"
    - from: "app/pages/historico.vue"
      to: "usePersistedSession.ts archiveUnreadableHistory"
      via: "useGameHistory().archiveUnreadable() tras ConfirmDialog"
      pattern: "archiveUnreadable"
    - from: "app/composables/useStoredProgress.ts esLaMismaPartida"
      to: "SessionContext.endedAt"
      via: "comparación de context sin endedAt (el sello de fin no es posición)"
      pattern: "endedAt"
---

<objective>
Cerrar los WARNING de la Fase 9 que siguen abiertos en
`.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md`, después de haber comprobado
contra el código actual (HEAD `633ef20` + los merges de los siblings 260923-3rl y 260923-3rn)
cuáles siguen abiertos de verdad.

Resultado de la comprobación hecha al planificar (el ejecutor la repite en la Task 1, paso 0):

| Entrada de deferred-items.md | Estado en el código actual | Qué hace este plan |
|---|---|---|
| WR-04 (`GameOutcomeDialog` sin Escape/foco, ronda 1) | CERRADO por 09-19 (`aria-labelledby`, foco al panel, Escape deliberadamente NO cierra — 09-UI-SPEC §Layout 2) | No se reabre. Nota de re-comprobación (Task 2). |
| WR-05 (b) (pantallas `h-dvh` empujadas por los avisos) | CERRADO por 09-22 (bandas `fixed`) | El mecanismo se sustituye en la Task 1; la propiedad se conserva y queda fijada por test. Nota de re-comprobación. |
| WR-04 (ronda 4) solape `UpdateBanner`/`HistorySavedNotice` | ABIERTO (ambas `fixed top-0 inset-x-0 z-40`) | Task 1 |
| WR-05 (ronda 4) toques invisibles sobre la cabecera de `/historico` | ABIERTO (contenedor con eventos de puntero desactivados) | Task 1 |
| WR-02 + WR-03 (ronda 4) trampa y restauración de foco | ABIERTOS (sin trampa; restauración sobre nodo desprendido) | Task 2 |
| WR-06 (ronda 4) duración inflada en el reintento | ABIERTO (`durationMs = now - startedAt` con `now` del reintento) | Task 2 |
| WR-02 (primera entrada) envoltorio `unreadable` permanente sin salida en la interfaz | ABIERTO (`readHistory()` de la Fase 10 distingue el caso, pero ninguna pantalla lo usa ni ofrece salida) | Task 3 |
| WR-02 (ronda 6) el primer autoguardado sobrescribe un progreso que no se pudo leer | ABIERTO (solo el aviso de 09-29) | Task 3 |
| WR-07 `sampleCaption` ignora la muestra de villanos | ABIERTO (`entriesWithHeroes` solo) | Task 3 |

Ningún WARNING queda sin tratar: los dos ya cerrados se re-comprueban y anotan, los siete abiertos se cierran.

Purpose: son los últimos WARNING reales de la Fase 9 (tres de ellos tocan datos irreconstruibles o
reconstruibles del grupo); el proyecto exige que la app nunca afirme sin respaldo ni destruya lo que
no ha podido leer.

Output: franja de avisos en flujo, trampa de foco en el diálogo de fin, instante de fin congelado,
salida explícita para un histórico ilegible, copia de seguridad antes de sobrescribir un progreso no
leído, leyenda de muestra por tabla, tests de Vitest + tres specs de Playwright, y cada entrada de
`deferred-items.md` anotada con el quick id.

Nota de forma: sin tarea tracer — son siete arreglos independientes sobre una arquitectura ya
probada (equivalente a `--no-tracer`); cada tarea es de punta a punta para sus WARNING. Las tareas
tocan más de 5 ficheros cada una porque el techo es de 3 tareas; buena parte son sustituciones
mecánicas de una clase o tests.

Dependencias entre items del lote (por qué `depends_on`):
- 260923-3rl edita `usePersistedSession.ts` (`appendHistoryEntry`, `removeHistoryEntry`), `engine/history.ts` (id de `buildHistoryEntry`), `engine/statistics.ts` (`pct`), probablemente `useGameHistory.ts`/`historico.vue`/`HistorySavedNotice.vue` (IN-11) y `deferred-items.md`. Este plan edita esos mismos ficheros: va DESPUÉS y trabaja sobre lo que 3rl deje.
- 260923-3rn añade tests que ejercen `app/pages/[game]/index.vue` y endurece `invariantesDeMarcaDeEstado.test.ts`. Este plan cambia `index.vue` y `usePersistedSession()`: va DESPUÉS para que sus gates nuevos validen estos cambios (y para adaptar cualquier doble de `usePersistedSession` que 3rn haya creado).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW-ronda4.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md

Reglas de los gates de la Fase 9 que TODA edición de este plan tiene que respetar (medidas al planificar):

1. Gate A (`app/composables/__tests__/afirmacionesRespaldadas.test.ts` + `vocabularioDeAfirmaciones.ts`):
   barre TODO `app/**/*.vue` y `app/**/*.ts` fuera de `__tests__/`, quitando solo comentarios de
   bloque, comentarios HTML y comentarios de línea que EMPIECEN la línea (un comentario `//` al final
   de una línea de código NO se quita). Falla si el código/copy contiene una raíz de
   `RAICES_SOBRE_LOS_DATOS_DEL_GRUPO` (conserv, guardad, guardar, dispositivo, progreso, reintent,
   «no se ha perdido», «ya no está», «no encontraréis», «se borrar») sin entrada en
   `AFIRMACIONES_AUDITADAS` para ese fichero y esa raíz. `app/pages/[game]/index.vue` NO puede tener
   ninguna entrada auditada (hay un test que lo exige): en `index.vue`, `usePersistedSession.ts`,
   `useGameHistory.ts`, `historico.vue`, `estadisticas.vue` y `useDialogFocusTrap.ts` ningún
   identificador ni cadena nueva puede contener esas raíces — usar identificadores en inglés
   (`progressWriter`, no nada con «progreso»; `archive…`, nunca «conserv…»/«guardar…»).
2. Gate C: los literales entrecomillados de `StoredProgress` (los cuatro valores de ese tipo, con
   comillas simples, dobles o invertidas) solo pueden aparecer —incluso en COMENTARIOS— en los cinco
   ficheros de `FICHEROS_QUE_PUEDEN_NOMBRAR_EL_ESTADO_DEL_DISPOSITIVO`; los literales de
   `NoticeVariant` solo en `useHistorySavedNotice.ts`. No escribir ninguno de ellos en
   `useGameHistory.ts`, `historico.vue`, `estadisticas.vue`, `useDialogFocusTrap.ts` ni en los
   componentes de aviso.
3. Gate de invariantes (`invariantesDeMarcaDeEstado.test.ts`): su test de descubrimiento exige que
   en `app/composables/` (sin `__tests__/`) SOLO `useHistorySavedNotice.ts` y
   `useProgressMismatchMark.ts` tengan estado de módulo mutable a columna 0 (`let X`,
   `const X = ref(`, `const X = new Set/Map`) + alguna raíz vigilada. No añadir estado de módulo a
   columna 0 en ningún composable; el estado nuevo (p. ej. el flag del guardián de escritura) vive en
   un cierre dentro de una función.
4. Tests en entorno `node` (sin DOM): la lógica nueva se extrae a funciones puras testeables; el
   almacenamiento se simula como ya hacen `usePersistedSession.test.ts`/`useGameHistory.test.ts`
   (un `window` falso en `globalThis` con un `localStorage` falso).
5. Playwright (`playwright.config.ts`) levanta `npm run generate && npx nuxi preview` en el puerto 4173
   y REUTILIZA un servidor ya levantado fuera de CI: antes de cada ejecución comprobar que no hay nada
   escuchando en 4173, o se probaría una build vieja.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Franja de avisos en flujo — WR-04 (ronda 4), WR-05 (ronda 4), re-comprobación de WR-05 (b)</name>
  <files>app/app.vue, app/components/UpdateBanner.vue, app/components/HistorySavedNotice.vue, app/components/GameSelectorScreen.vue, app/components/MiniSetupScreen.vue, app/components/MesaListaScreen.vue, app/pages/historico.vue, app/pages/estadisticas.vue, app/pages/[game]/index.vue, app/composables/__tests__/pilaDeAvisos.test.ts, e2e/notice-stack.spec.ts, .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</files>
  <read_first>app/app.vue, app/components/UpdateBanner.vue, app/components/HistorySavedNotice.vue, app/pages/historico.vue (cabecera h-16), app/composables/__tests__/vocabularioDeAfirmaciones.ts (quitarComentarios, ficherosVueDelArbol), e2e/offline-flow.spec.ts (camino real de fin de partida: «Abrir índice» → «Partida terminada» → «GANADA»), e2e/counter-band-height.spec.ts (medidas de main que no deben cambiar)</read_first>
  <action>
Paso 0 — re-comprobación (sin editar nada): confirmar con grep y lectura que, sobre el árbol ya mergeado con 3rl/3rn, siguen abiertos los siete WARNING de la tabla del objetivo y siguen cerrados WR-04 (09-19) y WR-05 (b) (09-22). Si alguno ya estuviera cerrado por un sibling, NO se rehace: se anota como tal en el SUMMARY y en deferred-items.md y se salta su parte de la tarea correspondiente.

Diseño (sustituye el mecanismo de 09-22; lo que se conserva es su propiedad): las dos bandas dejan de estar posicionadas y pasan a apilarse EN FLUJO en una franja propia, encima de la página; la página ocupa el resto de la altura. Así ninguna tapa nada (WR-05 r4), no se solapan entre sí (WR-04 r4) y la pantalla resta la altura de la banda en vez de sumarla (WR-05 (b)).

1. `app/app.vue`: `#app-root` gana `class="h-dvh flex flex-col"`. Dentro, en este orden exacto: un `<div class="shrink-0 flex flex-col">` que contiene `<ClientOnly>` con `<UpdateBanner />` y después `<HistorySavedNotice />` (en ese orden, solos, sin nada más dentro del ClientOnly); y después un `<div class="flex-1 min-h-0">` que contiene solo `<NuxtPage />`. `NuxtPwaManifest` se queda donde está. Reescribir los comentarios del fichero para explicar la franja (quick 260923-3rm, WR-04/WR-05 ronda 4): orden de prioridad explícito (versión nueva arriba, aviso de registro debajo), ninguna banda cubre a otra ni a la pantalla, y los diálogos de pantalla completa (capas 40/50, posicionados) siguen pintando encima de la franja sin depender del orden del DOM.
2. `UpdateBanner.vue` y `HistorySavedNotice.vue`: quitar del contenedor visible las cuatro utilidades de posicionamiento/capa/puntero que añadió 09-22 (`fixed`, `top-0`, `inset-x-0`, `z-40`, y la que desactiva los eventos de puntero) y quitar de cada botón la utilidad que los reactivaba. El resto de clases, la copy, la lógica y la región viva permanente `role="status" aria-live="polite"` de `HistorySavedNotice` quedan igual. Reescribir el comentario grande de cada fichero: ya no hablar de capas ni de orden de montaje frente a `ResumePrompt`/`ContentChangedNotice`; explicar que la banda es un bloque en flujo dentro de la franja de `app/app.vue` y por qué (WR-04/WR-05 ronda 4). Si 3rl ha añadido una variante o copy a `HistorySavedNotice`, conservarla intacta.
3. Pantallas: sustituir la utilidad `h-dvh` por `h-full` en la raíz de `GameSelectorScreen.vue`, `MiniSetupScreen.vue`, `MesaListaScreen.vue`, `app/pages/historico.vue`, `app/pages/estadisticas.vue` y en las cinco apariciones de `app/pages/[game]/index.vue` (id desconocido, fallback de ClientOnly, «Cargando…», envoltorio de `ResumePrompt`, pantalla de juego). No tocar ningún `max-h-[..dvh]` de los diálogos. Tras esto, `h-dvh` solo aparece como clase en `#app-root`.
4. Crear `app/composables/__tests__/pilaDeAvisos.test.ts` (gate estructural, Vitest node, `import.meta.glob` vía `ficherosVueDelArbol` y `quitarComentarios` de `./vocabularioDeAfirmaciones` — nunca `node:fs`). Sobre el contenido con comentarios quitados: (a) ni `UpdateBanner.vue` ni `HistorySavedNotice.vue` contienen como clase ninguna de `fixed`, `inset-x-0`, `z-40` ni las dos utilidades de eventos de puntero (regex por token, `\b` a ambos lados); (b) en `app/app.vue` hay un único bloque que casa `<div class="…shrink-0…">`, espacios, `<ClientOnly>`, `<UpdateBanner />`, `<HistorySavedNotice />`, `</ClientOnly>`, `</div>` (tolerar espacios en blanco entre etiquetas), y después un elemento cuyas clases incluyen `flex-1` y `min-h-0` que envuelve directamente a `<NuxtPage />`; el elemento `id="app-root"` lleva `h-dvh` y `flex-col`; (c) ningún `.vue` bajo `/app/components/` ni `/app/pages/` usa `h-dvh` como clase (WR-05 (b): las pantallas toman su altura del envoltorio). Mensajes de error en español citando WR-04/WR-05 ronda 4 y WR-05 (b). Incluir un caso sintético por aserción (cadena inventada que la pone roja) para que el gate no pueda pasar en vacío.
5. Crear `e2e/notice-stack.spec.ts` (viewport por defecto 1280x800), un test: ir a `/`, «Marvel Champions», mini-setup 2 jugadores + Normal, «EMPEZAR PREPARACIÓN ›», «Abrir índice», «Partida terminada», «GANADA»; en `/` el encabezado «✓ Partida registrada» es visible y `document.elementFromPoint` en el centro de ese encabezado devuelve un nodo contenido en `[role="status"]` (un toque sobre el aviso cae en el aviso, WR-05 r4); pulsar el botón «Histórico» del selector (navegación de cliente, el aviso sigue visible dentro de sus 6 s); en `/historico`: el `boundingBox` del botón «Estadísticas ›» empieza en o por debajo del borde inferior del aviso (no tapado), `elementFromPoint` en su centro devuelve ese botón (o un descendiente), y `document.documentElement.scrollHeight <= window.innerHeight` (nada empujado fuera, WR-05 (b)); pulsar el `✕` del aviso («Cerrar aviso») y comprobar que la cabecera de `/historico` pasa a empezar en y = 0. No simular la banda de versión nueva (prohibido por T-04-15, ver cabecera de `e2e/update-banner.spec.ts`): esa mitad la cubre el gate estructural del punto 4.
6. `deferred-items.md`: bajo la entrada «WR-04 (ronda 4)» y bajo «WR-05 (ronda 4)» añadir un párrafo `**Actualización (quick 260923-3rm) — CERRADO:**` con qué cambió (franja en flujo en `app/app.vue`, sin posicionamiento ni puntero en las bandas, pantallas `h-full`) y la evidencia (`pilaDeAvisos.test.ts`, `e2e/notice-stack.spec.ts`, comandos y resultado reales). Bajo «WR-05 (b)» añadir `**Re-comprobado (quick 260923-3rm):**` diciendo que el cierre de 09-22 sigue siendo cierto con otro mecanismo (la banda resta altura en vez de flotar) y que ahora lo fija `pilaDeAvisos.test.ts` (c). No retocar las notas de cierre históricas de rondas anteriores.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/tabletop-assistant && npx vitest run app/composables/__tests__/pilaDeAvisos.test.ts && npx vitest run && npm run typecheck && (! curl -sf http://localhost:4173 >/dev/null) && npx playwright test e2e/notice-stack.spec.ts e2e/counter-band-height.spec.ts e2e/portrait-usable.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pilaDeAvisos.test.ts` existe, pasa, y cada una de sus tres aserciones tiene un caso sintético que la pone roja.
    - `grep -c 'flex-1 min-h-0' app/app.vue` devuelve al menos 1 y `grep -c 'shrink-0 flex flex-col' app/app.vue` devuelve al menos 1.
    - `npx vitest run` y `npm run typecheck` salen con código 0 (Gate A, Gate C y el gate de invariantes en verde).
    - Las tres specs de Playwright del verify pasan contra una build recién generada.
    - deferred-items.md tiene «Actualización (quick 260923-3rm) — CERRADO» bajo WR-04 (ronda 4) y WR-05 (ronda 4), y «Re-comprobado (quick 260923-3rm)» bajo WR-05 (b).
  </acceptance_criteria>
  <done>Las dos bandas se apilan en flujo encima de la página, no tapan ni se tapan, ningún toque las atraviesa, ninguna pantalla se sale del viewport, y un test estructural más una spec de navegador real lo fijan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fin de partida — trampa y restauración de foco (WR-02/WR-03 ronda 4) e instante de fin congelado (WR-06 ronda 4)</name>
  <files>app/composables/useDialogFocusTrap.ts, app/composables/__tests__/useDialogFocusTrap.test.ts, app/components/GameOutcomeDialog.vue, engine/types.ts, engine/history.ts, engine/__tests__/history.test.ts, app/composables/useGameHistory.ts, app/composables/useStoredProgress.ts, app/composables/__tests__/useStoredProgress.test.ts, app/pages/[game]/index.vue, e2e/game-outcome-dialog.spec.ts, .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</files>
  <read_first>app/components/GameOutcomeDialog.vue (comentarios de reconciliación D-01/D-02 y Escape), app/components/WarningDetailModal.vue (patrón de alta/baja de listener), engine/history.ts (buildHistoryEntry: durationMs, recordedAt, id — el id lo puede haber cambiado 3rl, no tocarlo), engine/types.ts (SessionContext, comentario D-07 de startedAt), app/composables/useStoredProgress.ts (esLaMismaPartida, huellaDelProgreso), app/pages/[game]/index.vue (onOutcomeRecorded y su comentario D-U4), .planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md (D-07, D-08)</read_first>
  <behavior>
    - nextTrappedIndex(-1, 4, false) === 0; nextTrappedIndex(-1, 4, true) === 3; nextTrappedIndex(3, 4, false) === 0; nextTrappedIndex(0, 4, true) === 3; nextTrappedIndex(1, 4, false) === 2; nextTrappedIndex(2, 4, true) === 1; nextTrappedIndex(-1, 0, false) === null.
    - resolveRestoreTarget(null) === null; resolveRestoreTarget({ isConnected: false }) === null; resolveRestoreTarget(x) === x cuando x.isConnected === true.
    - freezeEndInstant(sesión sin sello, T) devuelve una sesión NUEVA con context.endedAt = { at: T, runtimeId: runtimeId del nodo actual, round: session.round } y no muta la entrada.
    - freezeEndInstant aplicado dos veces en la misma posición conserva el PRIMER at (misma referencia devuelta en la segunda llamada).
    - freezeEndInstant tras mover el cursor (otro runtimeId) o cambiar round sustituye el sello por uno nuevo con el now nuevo.
    - freezeEndInstant con cursor fuera de rango o now no finito devuelve la sesión sin cambios.
    - buildHistoryEntry con sello válido en la posición actual y now = sello + 24 h: durationMs === sello - startedAt y recordedAt === new Date(sello).toISOString().
    - buildHistoryEntry con sello de otra posición (runtimeId o round distintos): durationMs === now - startedAt y recordedAt de now (D-08 tal cual).
    - buildHistoryEntry con sello corrupto (at NaN, at > now, at < startedAt, endedAt no objeto, runtimeId no cadena) lo ignora y nunca lanza.
    - Los tests existentes de durationMs (sin sello) siguen en verde sin tocarlos.
    - esLaMismaPartida(A, B) es true cuando A y B solo difieren en context.endedAt; huellaDelProgreso(A) !== huellaDelProgreso(B) en ese mismo caso.
  </behavior>
  <action>
WR-02/WR-03 (ronda 4):
1. Crear `app/composables/useDialogFocusTrap.ts` (imports explícitos de `vue`, sin estado de módulo, sin raíces vigiladas en código): exportar la función pura `nextTrappedIndex(currentIndex, count, backwards)` con la tabla del `<behavior>` (índice fuera de la lista = -1: hacia delante va al primero, hacia atrás al último; ciclo en los extremos; `null` si no hay ningún enfocable); exportar la función pura genérica `resolveRestoreTarget(previous)` sobre `{ isConnected: boolean } | null`; exportar `useDialogFocusTrap(container: Ref<HTMLElement | null>)` que en `onMounted` guarda `document.activeElement` (solo si es `HTMLElement`), enfoca `container.value` y registra un `keydown` en `window`; el manejador, solo para la tecla `Tab`, obtiene los `button:not([disabled])` de `container.value`, calcula el índice del `document.activeElement` entre ellos, pide `nextTrappedIndex(índice, total, event.shiftKey)` y, si no es `null`, hace `preventDefault()` y enfoca ese botón — también cuando el foco estaba FUERA del diálogo (p. ej. tras tocar el velo), que es el caso que cierra WR-02; en `onUnmounted` retira el listener y llama `focus()` solo sobre lo que devuelva `resolveRestoreTarget` (WR-03: nunca sobre un nodo desprendido). Comentario de cabecera: por qué existe (WR-02/WR-03 de `09-REVIEW-ronda4.md`), que el foco inicial va al contenedor y no a un botón (D-02), y que en las cuatro salidas reales el abridor (el botón de `IndexOverlay`) se desmonta en el mismo flush y la página navega a `/`, así que la restauración no hace nada en ellas A PROPÓSITO y solo protege a un futuro cierre no terminal.
2. Crear `app/composables/__tests__/useDialogFocusTrap.test.ts` con los casos del `<behavior>` de las dos funciones puras (RED primero, luego GREEN).
3. `GameOutcomeDialog.vue`: sustituir su `previouslyFocused`/`onMounted`/`onUnmounted` por `useDialogFocusTrap(panel)`; el panel sigue con `tabindex="-1"`. Mantener sin cambios la decisión de Escape y el velo (WR-04 ya cerrado por 09-19, 09-UI-SPEC §Layout 2) y actualizar la NOTA DE RECONCILIACIÓN de foco para decir que ahora el foco queda atrapado (WR-02 r4) y que la restauración está guardada por `isConnected` (WR-03 r4). Confirmar leyendo `shortcutsEnabled` en `useStepShortcuts.ts` que los atajos siguen apagados con `awaitingEndConfirm`.
4. Crear `e2e/game-outcome-dialog.spec.ts`: empezar partida (2 jugadores, Normal), «Abrir índice», «Partida terminada»; el encabezado «¿Cómo terminó la partida?» es visible; pulsar `Tab` 12 veces y después `Shift+Tab` 12 veces comprobando tras CADA pulsación que `document.activeElement` está dentro de `[aria-labelledby="game-outcome-heading"]` y es un `BUTTON`; pulsar `Escape` y comprobar que el diálogo sigue visible; enfocar «Salir sin registrar», pulsar `Enter` y comprobar que vuelve el selector (botón «Marvel Champions» visible).

WR-06 (ronda 4) — congelar el instante del desenlace, respetando D-08 (reloj de pared sin tope desde `startedAt`, sin acumular tiempo activo ni descontar pausas) y D-07 (nunca se reescribe `startedAt`):
5. `engine/types.ts`: exportar `interface FrozenEndInstant { at: number, runtimeId: string, round: number }` y añadir a `SessionContext` el campo opcional `endedAt?: FrozenEndInstant`, con comentario: campo ADITIVO igual que `startedAt`/`selection`/`counters` (sin bump de `formatVersion` ni `contentVersion`, `engine/persistence.ts` no se toca), sellado la primera vez que el grupo pulsa un resultado en esa posición, y solo válido mientras la sesión siga en esa misma posición.
6. `engine/history.ts`: exportar `freezeEndInstant(session, now)` con el contrato del `<behavior>` (pura, nunca muta, nunca lanza; conserva el sello más antiguo en la misma posición). En `buildHistoryEntry`, calcular un instante de referencia: el `at` del sello si es un objeto con `at` finito, `runtimeId` cadena igual al del nodo actual (`session.sequence[session.cursor]`), `round` igual a `session.round`, `at <= now` y (si `startedAt` es finito) `at >= startedAt`; si no, `now`. Usar ese instante para `durationMs` (mismas guardas de hoy) y para `recordedAt` (la fecha de la tarjeta es la del desenlace, como pedía 09-REVIEW-ronda4 WR-06). NO cambiar cómo se genera `id` (lo posee 3rl, IN-05). Actualizar el comentario de D-08 y el de `recordedAt` en `engine/types.ts` (`GameHistoryEntry`) para describir esto.
7. `engine/__tests__/history.test.ts`: tests del `<behavior>` para `freezeEndInstant` y `buildHistoryEntry` (RED → GREEN).
8. `app/composables/useStoredProgress.ts`: `esLaMismaPartida` compara `context` SIN `endedAt` (el sello de fin no es posición de partida: sin esto, un reintento cuyo guardado falla y deja en disco el autoguardado previo de la MISMA posición pasaría a `'stale'` sin serlo). `huellaDelProgreso` NO cambia (sigue cubriendo todo). Actualizar el comentario de la lista de campos comparados. Tests en `useStoredProgress.test.ts`: los dos casos del `<behavior>`. No añadir ninguna raíz vigilada nueva al código de este fichero.
9. `app/composables/useGameHistory.ts`: añadir al objeto que devuelve `useGameHistory()` la función `stampEndOfGame(session)` que devuelve `freezeEndInstant(session, Date.now())` (el reloj real se lee aquí y solo aquí, como dice la cabecera del fichero).
10. `app/pages/[game]/index.vue`: destructurar `stampEndOfGame` junto a `record`; en `onOutcomeRecorded`, dentro de `if (session.value && game)` y ANTES del `try` que llama a `record`, reasignar `session.value = stampEndOfGame(session.value)`, de modo que `record`, el guardado del fallo y `readStoredProgress(game, session.value)` vean la misma sesión sellada. Actualizar el comentario del orden D-U4 (silence → sellado → record → guardado → lectura → aviso → finishGame). Identificadores sin raíces vigiladas.
11. `deferred-items.md`: bajo «WR-02 (ronda 4)» (que incluye WR-03) y bajo «WR-06 (ronda 4)» añadir `**Actualización (quick 260923-3rm) — CERRADO:**` con qué cambió y la evidencia real (tests + spec + comandos). Registrar en WR-06 los dos residuos conocidos, sin esconderlos: (a) si el guardado del fallo tampoco se escribe, el reintento no lleva sello y mide como antes; (b) tras una reanudación con «el contenido ha cambiado» la posición cambia y el sello deja de aplicar (D-08 tal cual). Bajo la primera entrada «WR-04» añadir `**Re-comprobado (quick 260923-3rm):**` — sigue cerrado; Escape sigue sin cerrar a propósito; la trampa de foco nueva lo complementa.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/tabletop-assistant && npx vitest run app/composables/__tests__/useDialogFocusTrap.test.ts engine/__tests__/history.test.ts app/composables/__tests__/useStoredProgress.test.ts && npx vitest run && npm run typecheck && (! curl -sf http://localhost:4173 >/dev/null) && npx playwright test e2e/game-outcome-dialog.spec.ts e2e/offline-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Todos los casos del `<behavior>` existen como tests y pasan; los tests previos de `durationMs` siguen sin tocar y en verde.
    - `grep -c 'useDialogFocusTrap(panel)' app/components/GameOutcomeDialog.vue` devuelve 1; `grep -c 'stampEndOfGame' app/pages/[game]/index.vue` devuelve al menos 2 (destructuración + uso).
    - `npx vitest run` (incluidos afirmacionesRespaldadas, invariantesDeMarcaDeEstado y avisoTrasRegistroFallido) y `npm run typecheck` salen con 0.
    - `e2e/game-outcome-dialog.spec.ts` y `e2e/offline-flow.spec.ts` pasan contra una build recién generada.
    - deferred-items.md: «CERRADO (quick 260923-3rm)» bajo WR-02 (ronda 4) y WR-06 (ronda 4), con los dos residuos de WR-06 escritos; «Re-comprobado» bajo WR-04.
  </acceptance_criteria>
  <done>El foco no puede salir de GameOutcomeDialog, la restauración nunca apunta a un nodo desprendido, y un reintento de registro mide la partida hasta su desenlace real — con tests puros, una spec de navegador y D-07/D-08 intactos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Nunca pisar lo que no se ha podido leer — WR-02 (histórico ilegible), WR-02 (ronda 6, progreso) — y leyenda por tabla (WR-07)</name>
  <files>app/composables/usePersistedSession.ts, app/composables/__tests__/usePersistedSession.test.ts, app/composables/useGameHistory.ts, app/composables/__tests__/useGameHistory.test.ts, app/pages/historico.vue, app/pages/estadisticas.vue, engine/statistics.ts, engine/__tests__/statistics.test.ts, app/composables/useProgressMountPlan.ts, app/composables/__tests__/useProgressMountPlan.test.ts, app/composables/__tests__/afirmacionesRespaldadas.test.ts, app/pages/[game]/index.vue, e2e/unreadable-storage.spec.ts, .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</files>
  <read_first>app/composables/usePersistedSession.ts (readRaw, writeRaw, removeRaw, readEnvelope, readHistory/HistoryRead que consume useHistorySync.ts, appendHistoryEntry tal como lo dejó 3rl), app/composables/useHistorySync.ts (SOLO leer: no se toca; confirmar que solo mira `kind` de HistoryRead y que no enumera claves con prefijo), app/composables/useGameHistory.ts (reload, remove tal como lo dejó 3rl, buildStatisticsView), engine/statistics.ts (extractVillainId, entriesWithHeroes), app/composables/useProgressMountPlan.ts (UNVERIFIED_PROGRESS_NOTICE), app/composables/__tests__/afirmacionesRespaldadas.test.ts (entradas de useProgressMountPlan.ts), app/pages/[game]/index.vue (los tres llamadores de save: watchDebounced, pagehide, onOutcomeRecorded)</read_first>
  <behavior>
    - readHistoryState(): clave ausente → { kind: 'ok', entries: [] }; envoltorio válido → ok con las entradas válidas; JSON corrupto, formatVersion distinto o entries no array → { kind: 'uninterpretable' }; getItem que lanza → { kind: 'read-failed' }.
    - readHistory() (contrato de useHistorySync.ts) sigue devolviendo exactamente { kind: 'unreadable' } en los dos casos ilegibles.
    - archiveUnreadableHistory(now) con blob corrupto → 'archived'; `tga:history:backup-<now>` contiene el blob byte a byte; `tga:history` queda ausente; `tga:history:synced` intacta; un appendHistoryEntry posterior devuelve true.
    - archiveUnreadableHistory(now) con envoltorio válido o clave ausente → 'not-needed' sin ninguna escritura ni borrado.
    - archiveUnreadableHistory(now) con getItem que lanza → 'failed' sin setItem ni removeItem.
    - archiveUnreadableHistory(now) con setItem del backup que lanza → 'failed' y `tga:history` conserva el blob.
    - backupProgressBeforeOverwrite(gameId, now): clave ausente → true sin escribir; blob presente → copia exacta en `tga:progress:<gameId>:backup-<now>` y true; getItem que lanza → false; setItem del backup que lanza → false; nunca escribe `tga:progress:<gameId>`.
    - createOverwriteGuard(clock) sin armar: save escribe como save().
    - armado + clave con blob X: la primera save crea el backup con X y después escribe la posición nueva; la segunda save no crea otro backup.
    - armado + getItem que lanza: save devuelve false, no escribe la clave principal y sigue armado; cuando la lectura vuelve a funcionar, la siguiente save hace backup y escribe.
    - armado + setItem del backup que lanza: save devuelve false y la clave principal conserva X.
    - aggregateStatistics: una entrada con villano y sin héroes cuenta en entriesWithVillain y no en entriesWithHeroes; una con héroes y sin villano, al revés.
    - buildStatisticsView con 12 entradas, 10 con héroes y 12 con villano: heroSampleCaption === '12 partidas registradas · 10 con héroes anotados' y villainSampleCaption === null; con 1 entrada sin villano: villainSampleCaption === '1 partida registrada · 0 con villano anotado'.
    - useGameHistory() con blob corrupto: tras reload(), unreadableView.canArchive === true, entries vacío; archiveUnreadable() → 'archived' y después unreadableView === null e isEmpty === true. Con getItem que lanza: unreadableView.canArchive === false.
  </behavior>
  <action>
WR-02 (histórico ilegible sin salida):
1. `usePersistedSession.ts`: extraer la interpretación del blob a una función interna (JSON → formatVersion → entries) que usen tanto `readEnvelope` como el archivado, para que no puedan divergir; la variante ilegible interna de `EnvelopeRead` gana `reason: 'read-failed' | 'uninterpretable'` (ilegible de `readRaw` → `read-failed`; el resto → `uninterpretable`). Exportar `type HistoryState` y `readHistoryState()` con el `<behavior>`, y reescribir `readHistory()` como envoltorio de `readHistoryState()` que colapsa los dos casos ilegibles en `{ kind: 'unreadable' }` (tipo `HistoryRead` sin cambios: `useHistorySync.ts` no se toca). Añadir y devolver `archiveUnreadableHistory(now: number): 'archived' | 'not-needed' | 'failed'`: vuelve a leer la clave en crudo en ese instante; solo actúa si hay valor y la interpretación dice `uninterpretable`; escribe la copia exacta en `tga:history:backup-<now>`, la relee y exige igualdad; SOLO entonces retira `tga:history`, y devuelve `'archived'` únicamente si una relectura final confirma la clave ausente. Nunca escribe `tga:history` ni toca `tga:history:synced` (la siguiente partida crea el envoltorio por la vía legítima `'empty'` de `appendHistoryEntry`). Comentario: por qué (WR-02), por qué la copia se verifica antes de retirar nada (CR-03: nunca destruir lo que no se sabe leer), y el efecto conocido sobre la poda perezosa D-04 de `useHistorySync.ts` (con histórico vacío deja de haber ids que conservar; D-03 hace inocua cualquier resubida futura).
2. `useGameHistory.ts` (partiendo de lo que dejó 3rl en `reload`/`remove`): `reload()` pasa a leer `readHistoryState()`; guardar el estado en un ref `historyReadState` (`'ok' | 'read-failed' | 'uninterpretable'`) y las entradas como hoy. Exportar la función pura `buildUnreadableHistoryView(state)` que devuelve `{ title, body, statisticsBody, canArchive }` y las constantes de copy de abajo, y exponer desde `useGameHistory()` el computed `unreadableView` (`null` cuando el estado es `'ok'`), `archiveUnreadable()` (llama a `archiveUnreadableHistory(Date.now())`, después `reload()`, devuelve el resultado) y la función pura `archiveResultMessage(result)` (texto de éxito, de fallo, o `null` para `'not-needed'`). Copy EXACTA (sin raíces vigiladas; no la cambies sin volver a pasar Gate A):
   - `uninterpretable` — título «No se puede leer el histórico»; cuerpo «Hay un histórico anterior que la app no sabe interpretar. Mientras siga ahí, no se muestra ninguna partida y no se pueden registrar partidas nuevas. Podéis apartarlo como copia y empezar un histórico nuevo: la copia se queda aparte, sin tocar.»; `canArchive: true`.
   - `read-failed` — título «No se ha podido leer el histórico»; cuerpo «La app no ha conseguido leerlo en este momento, así que no puede saber qué partidas hay registradas. Puede deberse al modo privado del navegador. Volved a abrir esta pantalla más tarde.»; `canArchive: false`.
   - `statisticsBody` (ambos) «Las estadísticas salen del histórico, y ahora mismo la app no puede leerlo. En «Histórico» se explica qué podéis hacer.»
   - Botón «Apartarlo y empezar uno nuevo»; diálogo: título «¿Apartar el histórico que no se puede leer?», cuerpo «La app hará una copia aparte y empezará un histórico vacío. Esa copia no se muestra en ninguna pantalla.», confirmar «Sí, apartarlo», cancelar «Cancelar».
   - Resultado: éxito «Histórico apartado: la lista empieza de nuevo vacía.»; fallo «No se pudo apartar el histórico. Podéis intentarlo de nuevo más tarde.»
3. `app/pages/historico.vue`: dentro de `v-if="cargado"`, si `unreadableView` no es `null` pintar su título (h2) y cuerpo (mismas clases que el estado vacío) y, si `canArchive`, el botón que abre un `ConfirmDialog` (`:destructive="false"`, textos de las constantes); al confirmar, `archiveUnreadable()` y pintar `archiveResultMessage(resultado)` en una línea `role="status"`; si no, la lista o el estado vacío de siempre. Sin copy literal en la plantilla (todo viene del composable). Conservar lo que 3rl haya añadido (aviso de borrado fallido, IN-11).
4. `app/pages/estadisticas.vue`: si `unreadableView` no es `null`, pintar su título y `statisticsBody` en lugar de tablas y estado vacío.

WR-02 (ronda 6, progreso no leído que se sobrescribe):
5. `usePersistedSession.ts`: añadir y devolver `backupProgressBeforeOverwrite(gameId, now): boolean` con el `<behavior>` (copia exacta del crudo en `tga:progress:<gameId>:backup-<now>` releída y comparada; ausente → true; ilegible o copia fallida → false; nunca escribe la clave principal) y `createOverwriteGuard(clock = () => Date.now())` que devuelve `{ arm(), isArmed(), save(session) }`: el flag vive en un cierre DENTRO de la función (nunca a columna 0); `save` armado llama primero a `backupProgressBeforeOverwrite(session.gameId, clock())` y, si devuelve false, retorna false sin escribir y sigue armado; si devuelve true, desarma y delega en el `save` de siempre. Comentario: WR-02 ronda 6; el guardián ya no depende del supuesto no verificado de que lectura y escritura fallan juntas — si no se puede leer, no se escribe encima.
6. `app/pages/[game]/index.vue`: destructurar `createOverwriteGuard` (quitar `save` si queda sin uso), crear `const progressWriter = createOverwriteGuard()` en el cuerpo del setup; en `onMounted`, justo tras calcular `plan`, armarlo cuando `plan.unverifiedNotice !== null`; sustituir los TRES llamadores de guardado (callback de `watchDebounced`, `pagehide`, y el guardado tras registro fallido en `onOutcomeRecorded`) por `progressWriter.save(...)`. Actualizar los comentarios que describen esos llamadores. Identificadores sin raíces vigiladas. Si 3rn dejó un test de página con un doble de `usePersistedSession`, extender el doble con `createOverwriteGuard`.
7. `useProgressMountPlan.ts`: el aviso actual promete algo que deja de ser cierto; sustituir `UNVERIFIED_PROGRESS_NOTICE` por EXACTAMENTE: «No hemos podido comprobar si este dispositivo tiene una partida guardada de este juego. Podéis empezar una nueva: antes de guardar la nueva, la app aparta una copia de la que hubiera, y mientras no consiga leerla no guarda encima.» (solo usa las tres raíces ya auditadas para este fichero). En `useProgressMountPlan.test.ts` añadir que el aviso ya no contiene «podríais sustituirla». En `afirmacionesRespaldadas.test.ts`, reescribir SOLO el `motivo` y el `respaldo` de la entrada `raiz: 'guardar'` de `app/composables/useProgressMountPlan.ts` para que describan la frase nueva y citen `createOverwriteGuard`/`backupProgressBeforeOverwrite`, con `respaldo: 'app/composables/__tests__/usePersistedSession.test.ts'` (≥ 40 caracteres, nombra un identificador camelCase que aparece en ese test); revisar que el motivo de la raíz `guardad` siga siendo verdad. No añadir ninguna otra entrada auditada.

WR-07 (leyenda por tabla):
8. `engine/statistics.ts` (partiendo del `pct` que dejó 3rl): `StatisticsSummary` gana `entriesWithVillain`, contado con el MISMO predicado que `extractVillainId` (el que alimenta `villainRows`). `engine/__tests__/statistics.test.ts`: casos del `<behavior>`.
9. `useGameHistory.ts` `buildStatisticsView`: sustituir `sampleCaption` por `heroSampleCaption` (copy de 09-UI-SPEC sin cambiar una coma: «{N} partida(s) registrada(s) · {M} con héroes anotados», solo si N ≠ M) y `villainSampleCaption` (misma forma, «· {M} con villano anotado», solo si N ≠ M); actualizar la interfaz `StatisticsView` y los tests de `useGameHistory.test.ts` que hoy miran `sampleCaption`. `estadisticas.vue`: quitar el pie compartido y pintar cada leyenda dentro de su bloque de tabla, encima de su h2, con las mismas clases del pie actual.

E2E y anotaciones:
10. Crear `e2e/unreadable-storage.spec.ts` con dos tests. (a) Histórico: en `/`, `localStorage.setItem('tga:history', '{roto')`, recargar, ir a `/historico`: visible «No se puede leer el histórico» (y NO «Todavía no hay partidas registradas»); «Apartarlo y empezar uno nuevo» → «Sí, apartarlo» → visible «Histórico apartado: la lista empieza de nuevo vacía.»; `localStorage.getItem('tga:history')` es `null` y existe una clave que empieza por `tga:history:backup-` con valor `{roto`. (b) Progreso: `page.addInitScript` que envuelve `Storage.prototype.getItem` para lanzar solo con la clave `tga:progress:marvel-champions` mientras `sessionStorage.getItem('tga-fail-read') === '1'`; ir a `/`, escribir `localStorage['tga:progress:marvel-champions'] = 'BLOB-ANTERIOR'` y activar el flag, recargar, «Marvel Champions»: visible el texto nuevo del aviso («No hemos podido comprobar si este dispositivo…»); desactivar el flag, 2 jugadores + Normal + «EMPEZAR PREPARACIÓN ›», esperar al menos 1 s (debounce de 300 ms): existe `tga:progress:marvel-champions:backup-*` con `BLOB-ANTERIOR` y la clave principal es JSON con `formatVersion`. Variante en el mismo test o un tercero: con el flag SIEMPRE activo, tras empezar partida y esperar, la clave principal sigue siendo `BLOB-ANTERIOR` (leída desactivando el flag justo antes de comprobar) y no hay backup.
11. `deferred-items.md`: bajo la primera entrada «WR-02», bajo «WR-02 (ronda 6)» y bajo «WR-07» añadir `**Actualización (quick 260923-3rm) — CERRADO:**` con cambio y evidencia real; en WR-02 (ronda 6) dejar escrito que el «Supuesto NO verificado» deja de sostener nada (el guardián no escribe mientras no pueda leer) y que las copias se acumulan en la cuota solo una vez por episodio de lectura fallida (riesgo aceptado). Al final del fichero añadir `## Nota de cierre (quick 260923-3rm)`: lista uno a uno los siete WARNING cerrados y los dos re-comprobados, con su tarea; y declara como PENDIENTE —no hecha— la comprobación humana en la tablet real de las superficies visuales nuevas (franja de avisos en flujo con banda de versión nueva real, pantalla de histórico ilegible y su diálogo, estado ilegible de estadísticas, foco del diálogo de fin con teclado físico), sin editar `REQUIREMENTS.md`. Conservar intactas las anotaciones que 3rl haya añadido al fichero.
  </action>
  <verify>
    <automated>cd /home/vcompanyb/tabletop-assistant && npx vitest run app/composables/__tests__/usePersistedSession.test.ts app/composables/__tests__/useGameHistory.test.ts engine/__tests__/statistics.test.ts app/composables/__tests__/useProgressMountPlan.test.ts app/composables/__tests__/afirmacionesRespaldadas.test.ts app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts app/composables/__tests__/useHistorySync.test.ts && npx vitest run && npm run typecheck && (! curl -sf http://localhost:4173 >/dev/null) && npx playwright test e2e/unreadable-storage.spec.ts e2e/offline-flow.spec.ts e2e/update-banner.spec.ts e2e/notice-stack.spec.ts e2e/game-outcome-dialog.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Todos los casos del `<behavior>` existen como tests y pasan; `useHistorySync.test.ts` sigue en verde sin tocar `useHistorySync.ts`.
    - `grep -c 'createOverwriteGuard' app/composables/usePersistedSession.ts` ≥ 1 y `grep -c 'progressWriter.save' app/pages/[game]/index.vue` ≥ 3.
    - `grep -c 'entriesWithVillain' engine/statistics.ts` ≥ 1 y `grep -c 'villainSampleCaption' app/pages/estadisticas.vue` ≥ 1.
    - `npx vitest run` y `npm run typecheck` salen con 0 (Gate A con la copy nueva, Gate C, gate de invariantes con el mismo descubrimiento de siempre).
    - Las cinco specs de Playwright del verify pasan contra una build recién generada.
    - deferred-items.md: «CERRADO (quick 260923-3rm)» bajo WR-02, WR-02 (ronda 6) y WR-07, y la sección «Nota de cierre (quick 260923-3rm)» con la comprobación humana declarada como pendiente.
  </acceptance_criteria>
  <done>Un histórico ilegible tiene explicación y salida verificada en la interfaz, un progreso no leído ya no se sobrescribe sin copia, cada tabla de estadísticas describe su propia muestra, y todas las entradas del lote quedan anotadas con el quick id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app | `tga:history`, `tga:progress:<gameId>` y su `context.endedAt` son entrada NO fiable (build pasada/futura, edición manual, escritura interrumpida) |
| teclado → diálogo de fin | un `Tab`/`Enter` con foco fuera del diálogo actuaría sobre controles invisibles de la partida |
| app → Firestore (Fase 10) | fuera de alcance: `useHistorySync.ts` no se toca; archivar solo vacía la vista local |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3rm-01 | Tampering | `archiveUnreadableHistory` | high | mitigate | Relee e interpreta el crudo en el instante del clic; solo actúa sobre `uninterpretable`; copia verbatim a `tga:history:backup-<now>`, relee y compara, y solo entonces retira `tga:history`; nunca reescribe el envoltorio (CR-03 intacto). Fijado por los tests de fallo de `usePersistedSession.test.ts` y por `e2e/unreadable-storage.spec.ts` (a). |
| T-3rm-02 | Tampering | `createOverwriteGuard` / autoguardado | high | mitigate | Tras lectura fallida al montar, ninguna escritura de `tga:progress:<gameId>` ocurre sin copia previa verificada; si la clave sigue sin leerse, no se escribe. Tests armado/ilegible/copia fallida + `e2e/unreadable-storage.spec.ts` (b). |
| T-3rm-03 | Tampering | `buildHistoryEntry` / `context.endedAt` | medium | mitigate | El sello solo se usa si `at` es finito, `<= now`, `>= startedAt` y coincide con runtimeId y round actuales; cualquier otra forma se ignora sin lanzar (tests de sello corrupto). |
| T-3rm-04 | Elevation of privilege (acción no pretendida) | `GameOutcomeDialog` | medium | mitigate | `useDialogFocusTrap` cicla Tab/Shift+Tab dentro del diálogo incluso con foco previo fuera; `e2e/game-outcome-dialog.spec.ts` lo comprueba tras cada pulsación. |
| T-3rm-05 | Information disclosure | copias `…:backup-<ts>` | low | accept | Solo en el propio navegador del grupo, nunca se leen ni suben; mismo nivel de exposición que la clave original. |
| T-3rm-06 | Denial of service | cuota de localStorage | low | accept | Una copia por episodio de lectura fallida / archivado explícito; tamaño de un único blob; registrado en deferred-items.md. |
| T-3rm-07 | Repudiation (copy sin respaldo) | copy nueva de `/historico`, `/estadisticas` y del mini-setup | medium | mitigate | Cada frase describe una lectura o un resultado verificado (`readHistoryState`, relectura de la copia, `createOverwriteGuard`); Gate A/C en verde; la entrada auditada `guardar` de `useProgressMountPlan.ts` pasa a citar el test que ejerce el guardián. |
</threat_model>

<verification>
- `npx vitest run` — suite completa en verde (base medida al planificar: 36 ficheros, 1129 tests, ~1,3 s; tras 3rl/3rn y este plan el número crece, no se compara contra una cifra fija).
- `npm run typecheck` — exit 0.
- Sin servidor en el puerto 4173: `npx playwright test e2e/notice-stack.spec.ts e2e/game-outcome-dialog.spec.ts e2e/unreadable-storage.spec.ts e2e/offline-flow.spec.ts e2e/update-banner.spec.ts e2e/counter-band-height.spec.ts e2e/portrait-usable.spec.ts` — todo en verde.
- `git diff --stat` no incluye `app/composables/useHistorySync.ts`, `nuxt.config.ts` ni `content/*.json` (ficheros de otros items del lote).
- deferred-items.md: cada uno de los siete WARNING abiertos lleva «CERRADO (quick 260923-3rm)», WR-04 y WR-05 (b) llevan «Re-comprobado (quick 260923-3rm)», y la nota de cierre final declara pendiente la comprobación humana en tablet.
</verification>

<success_criteria>
- Los siete WARNING abiertos de la Fase 9 (WR-04 r4, WR-05 r4, WR-02 r4 + WR-03 r4, WR-06 r4, WR-02, WR-02 r6, WR-07) quedan cerrados en código con test que los fija; WR-04 y WR-05 (b) siguen cerrados y re-comprobados.
- Ningún gate de la Fase 9 se relaja: no se añade ninguna entrada auditada nueva a `AFIRMACIONES_AUDITADAS` (solo se reescribe el motivo/respaldo de una existente), el descubrimiento del gate de invariantes no cambia, y `index.vue` sigue sin raíces vigiladas.
- No se tocan `useHistorySync.ts`, `nuxt.config.ts` ni el contenido JSON.
- La comprobación humana en tablet de las superficies nuevas queda escrita como PENDIENTE, nunca como hecha.
</success_criteria>

<output>
Create `.planning/quick/260923-3rm-phase-9-open-warnings-in-planning-phases-09-hist-rico-y-esta/260923-3rm-SUMMARY.md` when done
</output>
