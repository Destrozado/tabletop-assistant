---
phase: 07-banda-de-contadores-y-compatibilidad-de-sesi-n
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - engine/__tests__/persistence.test.ts
  - app/composables/useStepShortcuts.ts
  - app/composables/__tests__/useStepShortcuts.test.ts
autonomous: true
requirements: [COMP-01, COMP-02, HP-09]

must_haves:
  truths:
    - "Una partida guardada por la v1.7 ya desplegada (`formatVersion: 1`, `context` con solo `playerCount` y `difficulty`) se reanuda como 'resumed' y no se descarta ni se corrompe (D-21, COMP-01)"
    - "Con esa sesión reanudada, todos los resolvedores de la fase devuelven valores definidos y la banda pintaría «—» en todas las celdas, sin ningún `undefined` ni `NaN` llegando a la interfaz (D-21, COMP-02)"
    - "Un `counters` manipulado a mano en `localStorage` (tipos erróneos, longitud desajustada, `NaN`, negativos) se normaliza sin lanzar (D-21, COMP-02)"
    - "`formatVersion` sigue en 1 y `engine/persistence.ts` no se ha modificado (D-19)"
    - "El esfuerzo de compatibilidad se detiene exactamente en D-21: no se añade migración, ni bump, ni defensa adicional de partidas a medias (D-22)"
    - "Los contadores no se operan con teclado, y eso está escrito como decisión en `useStepShortcuts.ts`, no dejado como descuido (D-17, HP-09)"
    - "Espacio / Enter / ← se comportan idénticos a v1.7: `shortcutsEnabled` no gana ni una rama, y el test que fija `BUTTON -> false` sigue siendo único, sin duplicado (D-17, HP-09)"
  artifacts:
    - path: "engine/__tests__/persistence.test.ts"
      provides: "Bloque describe D-21 con la sesión de forma v1.7 construida a mano"
      contains: "D-21"
    - path: "app/composables/useStepShortcuts.ts"
      provides: "La decisión D-17 escrita junto a `isEditableTarget`"
      contains: "D-17"
  key_links:
    - from: "engine/__tests__/persistence.test.ts"
      to: "engine/counters.ts"
      via: "importa resolveCounters/resolveCounterValues para probar la sesión reanudada"
      pattern: "from '\\.\\./counters'"
    - from: "app/composables/useStepShortcuts.ts"
      to: "app/composables/__tests__/useStepShortcuts.test.ts"
      via: "el test BUTTON -> false ya existente queda referenciado como el que fija D-17"
      pattern: "D-17"
---

<objective>
Cerrar el criterio de éxito nº 5 de la fase con el test que el roadmap pide expresamente, y
dejar por escrito la decisión de que los contadores son táctiles y no de teclado.

Purpose: el roadmap dice que el caso «partida guardada por la v1.7» **no lo cubre** el gate
`contentVersion`/`formatVersion` y que hay que probarlo aparte. Este plan es ese aparte. Y va
junto a la nota de D-17 porque las dos tareas responden a la misma pregunta —«¿esto está así
a propósito?»— en dos ficheros distintos, y ninguna toca lógica: una añade un test, la otra
añade comentarios. Es deliberadamente el plan más pequeño de la fase (D-22: el grupo no deja
partidas a medias, así que la compatibilidad se demuestra, no se sobre-ingeniería).

Output: bloque `describe('D-21…')` en `engine/__tests__/persistence.test.ts`, y comentarios
de decisión en `app/composables/useStepShortcuts.ts` y en su test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-PATTERNS.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-02-SUMMARY.md

<interfaces>
<!-- Lo que este plan consume. Nada nuevo se exporta aquí. -->

De `engine/counters.ts` (plan 02):
- `resolveCounters(context: SessionContext): CounterState`
- `resolveCounterValues(context: SessionContext, catalogue: CharacterCatalogue | null): CounterState`

Ya presentes en `engine/__tests__/persistence.test.ts` y reutilizables sin importar nada más:
`readFileSync`, `fileURLToPath`, `describe`/`expect`/`it`, `expand`, `next`, `resume`,
`toPersistedPosition`, `PersistedPosition`, `validateGameDefinition`, `resolvePlayerSlots`,
`resolveVillainId`, y el fixture `marvelChampions` cargado desde
`../../content/marvel-champions.json`.

Ya presente en `app/composables/useStepShortcuts.ts`:
- `shortcutsEnabled(state: ShortcutState): boolean` — NO se toca, ni una rama nueva.
- `isEditableTarget(target: { tagName?: string, isContentEditable?: boolean } | null): boolean`
  — excluye `INPUT`/`TEXTAREA`/`SELECT`/`contenteditable`, y NO excluye `BUTTON`.

Ya presente en `app/composables/__tests__/useStepShortcuts.test.ts` (≈líneas 163-165), y que
por tanto **no hay que volver a escribir**:
`it('BUTTON -> false (no es editable; el doble avance lo resuelve preventDefault, D-Q1)', …)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir el bloque D-21 — una sesión con forma de v1.7 se reanuda y resuelve sin `undefined` ni `NaN`</name>
  <files>engine/__tests__/persistence.test.ts</files>
  <read_first>
    - `engine/__tests__/persistence.test.ts` — el fichero completo, y en particular sus
      imports de cabecera (líneas 1-24, incluido cómo se carga `marvel-champions.json`) y el
      bloque `describe('D-20: resume() de una sesión persistida sin `selection` (Fase 6)')`
      (≈líneas 141-200) con sus dos `it`. El bloque D-21 es su hermano ampliado un campo más:
      mismo estilo, mismas aserciones, mismo tono de comentario.
    - `engine/persistence.ts` — `resume()`, `isValidContext` y el gate
      `formatVersion`/`contentVersion`, para poder afirmar con propiedad por qué esta forma se
      reanuda. **Leer, no modificar.**
    - `engine/counters.ts` — las firmas reales de `resolveCounters` y `resolveCounterValues`
      tal como quedaron en el plan 02.
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md`
      §D-19, §D-21, §D-22.
    - `.planning/ROADMAP.md` §«Phase 7» criterio de éxito nº 5 — la frase que exige este test.
  </read_first>
  <action>
    En `engine/__tests__/persistence.test.ts`, añadir un `describe` NUEVO y adyacente al de
    D-20, titulado en el mismo estilo:
    `'D-21: resume() de una sesión persistida con forma de v1.7 (sin selection, sin counters)'`.

    Único import nuevo del fichero: `resolveCounters` y `resolveCounterValues` desde
    `../counters`. Además, cargar el catálogo real igual que el fichero ya carga
    `marvel-champions.json`: `readFileSync` + `fileURLToPath` sobre
    `../../content/marvel-characters.json`, casteado a `CharacterCatalogue` (sin validador de
    esquema; eso ya lo cubre `engine/__tests__/catalogueSchema.test.ts`).

    Primer `it` — «una partida de v1.7 se reanuda y la banda pintaría «—» en todas las celdas»:
    construir a mano una `PersistedPosition` con `formatVersion: 1`,
    `gameId: 'marvel-champions'`, `contentVersion: marvelChampions.contentVersion`,
    `runtimeId: fresh.sequence[0].runtimeId`, `round: 1`,
    `context: { playerCount: 3, difficulty: 'normal' }` (forma v1.7 literal: **sin**
    `selection` y **sin** `counters`) y un `updatedAt` ISO fijo.
    Aserciones: `outcome` es `'resumed'` (ni `'fresh'` ni `'content-changed'`);
    `session.context.counters` es `undefined`;
    `resolveCounters(session.context)` es exactamente
    `{ villainHealth: null, heroHealth: [null, null, null] }`;
    `resolveCounterValues(session.context, catalogue)` es lo mismo (sin villano ni héroes
    elegidos no hay nada que calcular, y eso es el estado NORMAL de SEL-09, no un fallo);
    y la aserción por la que existe el test: ningún valor devuelto es `undefined` ni `NaN` —
    escribirla explícitamente sobre el array (`every(v => v === null || Number.isInteger(v))`)
    y sobre `villainHealth`, no dejarla implícita en el `toEqual`.
    Comentario en el `it` dejando dicho, como hace el bloque D-20, que este caso NO lo cubre
    el gate `contentVersion`/`formatVersion` y que por eso se prueba aparte.

    Segundo `it` — «`localStorage` manipulado: un `counters` corrupto se normaliza sin lanzar»:
    misma `PersistedPosition` pero con
    `context.counters` deliberadamente roto en un solo objeto: `villainHealth: 'muchos'`
    (cadena), y `heroHealth` como array de cuatro entradas con `playerCount: 3` conteniendo
    `42`, `NaN`, `'12'` y `-7`. Aserciones: `resume` no lanza
    (`expect(() => resume(persisted, fresh)).not.toThrow()`); `resolveCounters` devuelve
    exactamente 3 entradas; `villainHealth` es `null`; ninguna entrada es `NaN`, `undefined`
    ni negativa; y la cuarta entrada persistida se descarta porque la longitud la manda
    `playerCount`, nunca `counters.heroHealth.length`.

    No tocar `engine/persistence.ts`, no tocar `formatVersion`, no escribir ninguna migración
    ni ninguna función de upgrade de formato: D-19 y D-22 cierran esa puerta a propósito, y
    bumpear o migrar es justo lo que descartaría la partida real que hay ahora mismo en la
    tablet del grupo.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/persistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run engine/__tests__/persistence.test.ts` termina con código 0, y el
      recuento de tests del fichero ha crecido en exactamente 2 respecto al estado anterior.
    - `grep -c "D-21" engine/__tests__/persistence.test.ts` es al menos 1.
    - `grep -c "from '../counters'" engine/__tests__/persistence.test.ts` devuelve 1.
    - `grep -c 'marvel-characters.json' engine/__tests__/persistence.test.ts` devuelve 1.
    - `grep -c 'not.toThrow' engine/__tests__/persistence.test.ts` es al menos 1.
    - `grep -cE 'Number.isNaN|Number.isInteger' engine/__tests__/persistence.test.ts` es al
      menos 1.
    - `git diff --name-only` NO incluye `engine/persistence.ts`.
    - `git diff engine/__tests__/persistence.test.ts | grep '^-' | grep -v '^---'` está vacío
      (el bloque D-21 se AÑADE; no se borra ni se reescribe nada del bloque D-20 existente).
    - `npx vitest run --project engine` termina con código 0.
  </acceptance_criteria>
  <done>
    Existe un test que construye a mano una `PersistedPosition` con la forma de la v1.7
    desplegada, demuestra que `resume()` la devuelve utilizable, y demuestra que los
    resolvedores de esta fase la convierten en «—» en todas las celdas sin propagar
    `undefined` ni `NaN` — con `engine/persistence.ts` intacto y `formatVersion` en 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Escribir D-17 como decisión en `useStepShortcuts.ts` y referenciarla en el test que ya la fija</name>
  <files>app/composables/useStepShortcuts.ts, app/composables/__tests__/useStepShortcuts.test.ts</files>
  <read_first>
    - `app/composables/__tests__/useStepShortcuts.test.ts` — **leerlo ANTES de escribir nada**.
      Contiene ya (≈líneas 163-165) el test
      `it('BUTTON -> false (no es editable; el doble avance lo resuelve preventDefault, D-Q1)', …)`,
      que es exactamente el comportamiento que D-17 quiere fijado. Confirmar de primera mano
      que sigue ahí antes de decidir qué hacer (ver las dos ramas en `<action>`).
    - `app/composables/useStepShortcuts.ts` — el fichero completo, y en particular el bloque
      de comentario sobre `isEditableTarget` (≈líneas 114-123, el estilo «decisión escrita»
      de D-Q1 que hay que imitar) y `shortcutsEnabled` (≈líneas 100-112), que NO se toca.
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md` §D-17
      y §«Deferred Ideas» (operar los contadores con teclado no es un diferido con fecha: es
      la decisión de que la app es táctil).
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-RESEARCH.md`
      §Open Questions nº 2 — la recomendación de NO duplicar el test.
  </read_first>
  <action>
    Dos ediciones, ambas de solo comentario. Cero cambios de lógica en los dos ficheros.

    (a) En `app/composables/useStepShortcuts.ts`, ampliar el bloque de comentario que ya
    precede a `isEditableTarget` con la línea de D-17, en el mismo registro que la nota de
    D-Q1 que ya está ahí: los `▼`/`▲` de `CounterBand.vue` son elementos `<button>`, por tanto
    NO quedan excluidos por esta guarda, por tanto con el foco en una flecha Espacio sigue
    avanzando el paso exactamente igual que en v1.7 — y eso es **la decisión** (los contadores
    se operan con el dedo, no con el teclado), no un descuido que nadie vio. Dejar dicho
    también que `shortcutsEnabled` no gana ninguna rama por esta fase (HP-09 al pie de la
    letra) y que quien vaya a «arreglarlo» añadiendo una guarda para botones debe leer D-17
    primero.
    Prohibido: añadir `BUTTON` a la lista de tags excluidos, añadir un parámetro, una rama o
    un campo de estado nuevo, o tocar el cableado de `keydown`.

    (b) En `app/composables/__tests__/useStepShortcuts.test.ts`, **no escribir un test nuevo**.
    El comportamiento ya está fijado por el `it('BUTTON -> false …')` existente. Añadir
    únicamente una línea de comentario inmediatamente encima de ese `it`, indicando que este
    es el test que fija D-17 de la Fase 7 (los `▼`/`▲` de la banda de contadores son
    `<button>`) además del D-Q1 que ya citaba — un cruce de trazabilidad, no una duplicación.
    Si al leer el fichero ese `it` **no** estuviera (por haber cambiado desde que se hizo la
    investigación), entonces —y solo entonces— añadirlo con el mismo nombre y la misma única
    aserción `expect(isEditableTarget({ tagName: 'BUTTON' })).toBe(false)`, citando D-Q1 y
    D-17. En ningún caso debe quedar más de una aserción con ese `tagName: 'BUTTON'` en todo
    el fichero.
  </action>
  <verify>
    <automated>npx vitest run --project app-logic</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project app-logic` termina con código 0.
    - `grep -c 'D-17' app/composables/useStepShortcuts.ts` es al menos 1.
    - `grep -c 'D-17' app/composables/__tests__/useStepShortcuts.test.ts` es al menos 1.
    - `grep -c "tagName: 'BUTTON'" app/composables/__tests__/useStepShortcuts.test.ts` devuelve
      exactamente 1 (no se ha duplicado el test).
    - Cero cambios de lógica en el composable:
      `git diff -U0 app/composables/useStepShortcuts.ts | grep '^+' | grep -v '^+++' | grep -vE '^\+\s*(//|$)'`
      está vacío (toda línea añadida es un comentario o una línea en blanco).
    - `git diff app/composables/useStepShortcuts.ts | grep -c "'BUTTON'"` devuelve 0 (no se ha
      añadido `BUTTON` a la lista de tags excluidos).
    - `git diff app/composables/useStepShortcuts.ts | grep -c 'shortcutsEnabled'` devuelve 0.
    - Cero cambios de lógica en el test:
      `git diff -U0 app/composables/__tests__/useStepShortcuts.test.ts | grep '^+' | grep -v '^+++' | grep -vE '^\+\s*(//|$)'`
      está vacío, salvo en la rama excepcional en la que el `it('BUTTON -> false …')` no
      existiera y haya habido que añadirlo (documentarlo en el SUMMARY si ocurre).
  </acceptance_criteria>
  <done>
    `useStepShortcuts.ts` explica por escrito que los contadores no se operan con teclado y
    por qué no hace falta ninguna guarda nueva; el test que fija ese comportamiento existe una
    sola vez y referencia D-17; y `shortcutsEnabled` está intacto.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → `resume()` → `SessionContext` | La única frontera de la app, y el objeto concreto de este plan: la `PersistedPosition` que la v1.7 desplegada dejó escrita en la tablet real del grupo. |
| Ninguna otra | App estática, sin backend, sin auth, sin red en ejecución. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Denial of Service (denegación de los datos propios) | La partida en curso guardada en la tablet del grupo | mitigate | **El riesgo de mayor severidad realista de la fase.** Se cierra por demostración, no por confianza: el bloque D-21 construye a mano la forma exacta de la v1.7 y asserta que `resume()` devuelve `'resumed'`. Gates: `engine/persistence.ts` no aparece en `git diff --name-only`, y `git diff` del test no borra ninguna línea existente. |
| T-07-10 | Tampering | `context.counters` manipulado desde DevTools | mitigate | El segundo `it` inyecta un `counters` con cadena, `NaN`, número negativo y longitud desajustada, y asserta normalización sin lanzar, sin negativos y con longitud derivada de `playerCount`. Es la prueba de la mitigación que implementó el plan 02, ejercitada a través de `resume()` como en producción. |
| T-07-11 | Tampering | Añadir `BUTTON` a `isEditableTarget` «por seguridad» | mitigate | Cambiaría el comportamiento de Espacio/Enter/← respecto a v1.7 y rompería HP-09 sin que ningún test rojo lo delatase. Se bloquea con un gate de diff: toda línea añadida al composable debe ser un comentario. |
| T-07-12 | Spoofing / Information Disclosure | — | accept | No aplica: no hay identidades ni datos sensibles. Los contadores son puntos de vida de una partida entre amigos en un dispositivo compartido a propósito. |
| T-07-SC | Tampering (cadena de suministro) | Dependencias | accept | Este plan no instala ni actualiza ningún paquete: solo añade un `describe` y comentarios. Sin superficie de cadena de suministro que auditar. |
</threat_model>

<verification>
- `npx vitest run` completo en verde (proyectos `engine` y `app-logic`).
- `git diff --name-only` limitado a los tres ficheros de `files_modified`.
- `engine/persistence.ts`, `engine/schema.ts` y `formatVersion` intactos.
</verification>

<success_criteria>
- Existe el test que el roadmap exige para el criterio nº 5, construido a mano con la forma
  de la v1.7 y con aserciones explícitas de ausencia de `undefined`/`NaN`.
- Un `counters` corrupto de `localStorage` se normaliza sin lanzar, con longitud mandada por
  `playerCount`.
- La decisión de que los contadores no se operan con teclado está escrita en el código, con
  el test que la fija referenciado una sola vez y `shortcutsEnabled` sin una rama nueva.
</success_criteria>

<output>
Create `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-03-SUMMARY.md` when done
</output>
