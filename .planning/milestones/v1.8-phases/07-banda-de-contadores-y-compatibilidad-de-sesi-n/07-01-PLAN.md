---
phase: 07-banda-de-contadores-y-compatibilidad-de-sesi-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/counter-band-height.spec.ts
  - engine/types.ts
autonomous: true
requirements: [HP-02, COMP-01]

must_haves:
  truths:
    - "El presupuesto de altura de la banda (96px fijos = 12,5% de 768px) queda medido contra el viewport apaisado objetivo ANTES de que exista `CounterBand.vue` (D-01, D-06, criterio de éxito nº 1)"
    - "Ningún paso de la partida necesita más alto que el que queda tras reservar los 96px de la banda, así que la banda no obliga a reducir el texto grande del paso (D-01)"
    - "El tamaño del texto grande del paso queda registrado como 40px de referencia antes de la banda, para poder demostrar después que la banda no lo redujo (D-01, HP-02)"
    - "El presupuesto del ~15% se mide solo en apaisado; el viewport estrecho no entra en esa medición (D-06)"
    - "`SessionContext` admite `counters` como campo OPCIONAL y aditivo, y `formatVersion` sigue valiendo 1 (D-19)"
    - "«Sin tocar» y «cero» son dos estados de tipo distintos en el modelo de datos: `null` y `0`, nunca colapsados (D-09, D-12)"
  artifacts:
    - path: "e2e/counter-band-height.spec.ts"
      provides: "Medición del presupuesto de altura en el viewport objetivo, ejecutable antes y después de construir la banda"
      contains: "RESERVED_BAND_HEIGHT"
    - path: "engine/types.ts"
      provides: "`CounterState` y `SessionContext.counters?`"
      contains: "counters?: CounterState"
  key_links:
    - from: "engine/types.ts"
      to: "SessionContext"
      via: "campo opcional aditivo junto a `selection?`"
      pattern: "counters\\?: CounterState"
    - from: "e2e/counter-band-height.spec.ts"
      to: "viewport 1024x768"
      via: "test.use({ viewport })"
      pattern: "width: 1024"
---

<objective>
Cerrar el criterio de éxito nº 1 de la fase por el único orden que lo hace verdad: **medir
el presupuesto de altura contra el viewport objetivo antes de que el componente exista**, y
fijar el contrato de datos (`CounterState` + `SessionContext.counters?`) que el resto de la
fase consume.

Purpose: D-01 y D-06 ya DECIDIERON el presupuesto (96px fijos, `h-24 shrink-0`, 12,5% de
768px). Lo que falta no es decidir: es **demostrar con una medición real** que 96px caben
sin encoger el texto grande del paso, y hacerlo antes de construir la banda, para que la
medición no pueda convertirse en una justificación a posteriori. El campo de tipos va en
esta misma ola porque `engine/counters.ts` (plan 02) y `useGameSession.ts` (plan 04) lo
importan, y definirlo primero evita que dos ficheros inventen dos formas distintas.

Output: `e2e/counter-band-height.spec.ts` (nuevo, verde antes de existir la banda) y
`engine/types.ts` ampliado con `CounterState` + `counters?`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-UI-SPEC.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-PATTERNS.md

<interfaces>
<!-- Contratos que este plan ESTABLECE y que los planes 02, 04, 05 y 06 consumen tal cual. -->

Forma exacta que debe quedar exportada desde `engine/types.ts`:

- `export interface CounterState { villainHealth: number | null, heroHealth: (number | null)[] }`
- `SessionContext` gana `counters?: CounterState`, colocado inmediatamente DESPUÉS de
  `selection?: HeroSelection` y ANTES del index signature `[key: string]: unknown`.

Tipos ya existentes en `engine/types.ts` que este plan NO toca y que el plan 02 consume:
`Difficulty`, `VillainStage` (`stage`, `health`, `healthPerHero`, `healthPerGroup`,
`expert?`), `CatalogueHero` (`id`, `name`, `alterEgo`, `health`, …), `CatalogueVillain`
(`id`, `name`, `stages`), `CharacterCatalogue` (`gameId`, `heroes`, `villains`),
`RuntimeStepNode.sectionRepeats: boolean`, `EngineSession.context: SessionContext`.

Alturas fijas ya vigentes en el DOM (leídas de los componentes, no supuestas):
`AppHeader` → `<header class="h-16 shrink-0 …">` = 64px;
`NavBand` → `<footer class="h-24 shrink-0 …">` = 96px;
`StepScreen` → `<main class="flex-1 bg-background … overflow-y-auto">`, único `flex-1`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medir el presupuesto de altura en el viewport objetivo, antes de construir la banda</name>
  <files>e2e/counter-band-height.spec.ts</files>
  <read_first>
    - `e2e/portrait-usable.spec.ts` — analogo exacto: `test.use({ viewport })` por describe,
      limpieza defensiva del `localStorage` cuando aparece «Partida guardada», recorrido
      selector → mini-setup → pantalla de juego, y medición vía `page.evaluate`.
    - `playwright.config.ts` — `baseURL` http://localhost:4173, `webServer` con
      `npm run generate && npx nuxi preview` (timeout 180s), `workers: 1`.
    - `e2e/offline-flow.spec.ts` — patrón de bucle de clics sobre `SIGUIENTE ›` comparando
      el texto del paso entre iteraciones.
    - `app/components/AppHeader.vue`, `app/components/NavBand.vue`,
      `app/components/StepScreen.vue` — las tres raíces del DOM que se van a medir
      (`<header class="h-16 shrink-0">`, `<footer class="h-24 shrink-0">`,
      `<main class="flex-1 … overflow-y-auto">`).
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md`
      §D-01/D-06 y `07-UI-SPEC.md` §Spacing Scale — las cifras del presupuesto.
  </read_first>
  <action>
    Crear `e2e/counter-band-height.spec.ts`. Es el registro ejecutable del presupuesto de
    D-01/D-06 y **debe pasar en verde ahora, cuando `CounterBand.vue` todavía no existe** —
    no asserta la presencia de la banda, sino que el hueco reservado para ella cabe.

    Comentario de cabecera obligatorio (estilo de `e2e/portrait-usable.spec.ts`), citando
    D-01, D-06 y el criterio de éxito nº 1, y dejando escrito que esta spec se escribió y se
    ejecutó ANTES de construir el componente. Incluir también la salvedad honesta que
    `07-RESEARCH.md` §Assumptions Log A1 y `STATE.md` §Blockers ya recogen: 1024×768 es el
    apaisado ASUMIDO porque el modelo y SO de la tablet de mesa siguen sin identificarse.

    Constantes en el módulo, con estos nombres exactos:
    `TARGET_VIEWPORT = { width: 1024, height: 768 }`, `RESERVED_BAND_HEIGHT = 96`,
    `HP_02_CEILING_RATIO = 0.15`, `HEADER_HEIGHT = 64`, `NAVBAND_HEIGHT = 96`.

    Un solo `test.describe` con `test.use({ viewport: TARGET_VIEWPORT })` y dos `test`:

    (a) «el presupuesto de 96px cumple HP-02 en el viewport objetivo»: aserción aritmética
    pura `RESERVED_BAND_HEIGHT / TARGET_VIEWPORT.height` menor o igual que
    `HP_02_CEILING_RATIO`, con mensaje de fallo que imprima el porcentaje calculado (12,5%).
    No necesita navegador pero se deja dentro del describe para que la cifra viva junto a la
    medición real.

    (b) «reservar 96px no obliga a encoger el texto grande de ningún paso»: `page.goto('/')`;
    descartar defensivamente una partida guardada previa con el patrón `.or()`/`isVisible()`
    de `portrait-usable.spec.ts` (localStorage compartido entre specs, `workers: 1`); entrar
    con el botón «Marvel Champions»; mini-setup con 3 jugadores y «Normal»; pulsar
    «EMPEZAR PREPARACIÓN ›».
    Medir con `boundingBox()` que `page.locator('header')` mide `HEADER_HEIGHT` y
    `page.locator('footer')` mide `NAVBAND_HEIGHT` — son las alturas de las que se deriva el
    hueco disponible, así que se comprueban en vez de asumirse.
    Calcular `available = TARGET_VIEWPORT.height - HEADER_HEIGHT - NAVBAND_HEIGHT - RESERVED_BAND_HEIGHT`
    (512px) y recorrer los pasos pulsando «SIGUIENTE ›» en un bucle acotado (máximo 40
    iteraciones, cortando cuando el botón deja de estar visible o cuando aparece
    «EMPEZAR A JUGAR ›»), registrando en cada paso el `scrollHeight` de `main` vía
    `page.evaluate`. Assertar que el máximo observado es menor o igual que `available`, con
    un mensaje de fallo que imprima el máximo medido y en qué paso se dio.
    Assertar además que `getComputedStyle` de `main p.text-display` devuelve exactamente
    `'40px'` de `fontSize` en el primer paso — es la línea base con la que el plan 06
    demostrará que la banda no redujo el texto grande (criterio de éxito nº 1). Dejarlo
    comentado como tal en el propio test.

    No añadir ningún selector, `data-testid` ni aserción sobre `CounterBand`: este fichero se
    amplía en el plan 06, cuando la banda ya exista.
  </action>
  <verify>
    <automated>npm run e2e -- e2e/counter-band-height.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run e2e -- e2e/counter-band-height.spec.ts` termina con código 0 y 2 tests pasados
      (el build de `npm run generate` puede tardar hasta 180s la primera vez; es el
      `webServer` ya configurado, no un problema nuevo).
    - `grep -c 'RESERVED_BAND_HEIGHT = 96' e2e/counter-band-height.spec.ts` devuelve 1.
    - `grep -c 'width: 1024' e2e/counter-band-height.spec.ts` devuelve al menos 1.
    - `grep -c "'40px'" e2e/counter-band-height.spec.ts` devuelve al menos 1.
    - `grep -c 'CounterBand' e2e/counter-band-height.spec.ts` devuelve 0 (la spec no depende
      todavía del componente).
    - `grep -vc '^\s*//' e2e/counter-band-height.spec.ts` es mayor que 20 (el fichero tiene
      código real, no solo comentarios).
    - La spec cita literalmente `D-01` y `D-06` en su comentario de cabecera:
      `grep -c 'D-01' e2e/counter-band-height.spec.ts` y `grep -c 'D-06' …` devuelven al
      menos 1 cada uno.
  </acceptance_criteria>
  <done>
    El presupuesto de 96px está medido y registrado como test ejecutable contra 1024×768,
    con la banda todavía sin construir, y demuestra que quedan 512px para el texto del paso
    y que el texto grande mide 40px de partida.
  </done>
</task>

<task type="auto">
  <name>Task 2: Añadir `CounterState` y `SessionContext.counters?` sin bumpear `formatVersion`</name>
  <files>engine/types.ts</files>
  <read_first>
    - `engine/types.ts` — el fichero completo, y en particular `HeroSelection`
      (líneas ~63-71, con su comentario sobre viajar entera en `toPersistedPosition`),
      `SessionContext` (líneas ~115-124, con el comentario D-19 de la Fase 6 sobre campo
      aditivo) y los tipos del catálogo `VillainStage`/`CatalogueHero`/`CatalogueVillain`
      (líneas ~169-218). El campo `selection?` y su comentario son el analogo EXACTO a imitar.
    - `engine/persistence.ts` — confirmar leyendo (no modificar) que `toPersistedPosition`
      persiste `context` entero y que `resume()` lo restaura entero, que es lo que hace que
      este campo no necesite fontanería nueva (D-19).
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md`
      §D-09, §D-12, §D-19 y §«Claude's Discretion» (que obliga a `(number | null)[]`).
  </read_first>
  <action>
    En `engine/types.ts`, añadir la interfaz `CounterState` con exactamente dos campos:
    `villainHealth: number | null` y `heroHealth: (number | null)[]`. Colocarla junto a
    `HeroSelection`, siguiendo la misma disposición y el mismo tono de comentario.

    El `(number | null)` por entrada es obligatorio y es el único punto en el que NO se copia
    literalmente la forma de `HeroSelection.heroes`: `null` significa «nadie ha pulsado
    ▼/▲ en este contador» y se pinta con el valor calculado en vivo, mientras `0` significa
    «tocado y bajado hasta cero» y marca «SIN VIDA» (D-09, D-12). El comentario debe decir
    esa regla dura textualmente: `null` y `0` son estados DISTINTOS y no se colapsan nunca.
    Dejar escrito también, en ese mismo comentario, que `.planning/research/ARCHITECTURE.md`
    §a proponía `heroHealth: number[]` y que D-09 lo ensancha a propósito.

    En `SessionContext`, añadir `counters?: CounterState` inmediatamente después de
    `selection?: HeroSelection` y antes del index signature `[key: string]: unknown`, con un
    comentario de la misma forma retórica que el de `selection?`: campo ADITIVO, no bumpea
    `formatVersion` (que sigue en `1`) ni `contentVersion`, y `undefined` es un estado normal
    y permanente de la app (D-09/D-12: nadie ha tocado ningún contador todavía), no un caso
    de compatibilidad con partidas antiguas. Citar D-19 y dejar constancia de que
    `engine/persistence.ts` NO se modifica en esta fase.

    No tocar `formatVersion` en ningún fichero. No añadir `counters` a ningún esquema `zod`:
    `SessionContext` no se valida con `zod` (solo el contenido del juego lo hace), y `zod`
    sigue siendo Node/test-time.
  </action>
  <verify>
    <automated>npx vitest run --project engine</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'counters?: CounterState' engine/types.ts` devuelve 1.
    - `grep -c 'heroHealth: (number | null)\[\]' engine/types.ts` devuelve 1.
    - `grep -c 'villainHealth: number | null' engine/types.ts` devuelve 1.
    - `grep -n 'selection?: HeroSelection' -A 8 engine/types.ts` muestra `counters?:` DESPUÉS
      de `selection?:` y ANTES de `[key: string]: unknown`.
    - `git diff engine/types.ts | grep -c 'formatVersion'` devuelve 0 (no se toca).
    - `git diff --name-only` NO incluye `engine/persistence.ts` ni `engine/schema.ts`.
    - `grep -c 'zod' engine/types.ts` devuelve 0.
    - `npx vitest run --project engine` termina con código 0 y sin tests fallando (toda la
      suite del motor, incluida `voice-drift.test.ts`, sigue verde).
  </acceptance_criteria>
  <done>
    `CounterState` está exportada desde `engine/types.ts` con entradas nullable por contador,
    `SessionContext.counters?` existe como campo aditivo, `formatVersion` sigue en 1 y
    `engine/persistence.ts` está intacto.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → `resume()` → `SessionContext` | La única entrada no confiable de toda la app: `tga:progress:<gameId>` es editable a mano desde DevTools y corruptible por un fallo de escritura a medias. A partir de este plan ese objeto puede traer también `counters`. |
| Ninguna otra | App estática prerenderizada (`nuxt generate`), sin backend, sin auth, sin llamadas de red en ejecución, sin cuentas de usuario. No hay frontera cliente↔servidor que cruzar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `SessionContext.counters` leído de `localStorage` | mitigate | El tipo se declara `(number \| null)[]` para que el compilador nunca prometa un número donde puede no haberlo; la validación real por TIPO (no por presencia) la implementa `resolveCounters` en el plan 02. Este plan solo fija la forma; declarar el campo como NO opcional o como `number[]` sería la vía directa al `undefined`/`NaN` en pantalla. |
| T-07-02 | Denial of Service (denegación de los datos propios) | Partida real en curso guardada en la tablet del grupo | mitigate | `formatVersion` se queda en `1` y `engine/persistence.ts` no se toca (D-19). Bumpear la versión sería la ÚNICA acción de este plan capaz de descartar la partida guardada ahora mismo en el dispositivo real — es el daño de mayor severidad realista de toda la fase, y se evita por diseño, no por suerte. Gate automático: `git diff engine/types.ts \| grep -c formatVersion` = 0. |
| T-07-03 | Information Disclosure | — | accept | No hay dato sensible: los contadores son puntos de vida de una partida de mesa entre amigos, en un dispositivo compartido a propósito. Sin PII, sin secretos, sin cifrado que aplicar. |
| T-07-SC | Tampering (cadena de suministro) | Dependencias | accept | **Esta fase no instala ni actualiza ningún paquete** (confirmado por `07-RESEARCH.md` §Package Legitimacy Audit contra `package.json`): no hay `npm install` en el alcance, luego no hay superficie de cadena de suministro que auditar ni checkpoint de legitimidad que insertar. Se declara explícitamente en vez de omitirse. |
</threat_model>

<verification>
- `npm run e2e -- e2e/counter-band-height.spec.ts` en verde (2 tests) **sin que exista
  `app/components/CounterBand.vue`** — es la prueba de que la medición precedió a la
  construcción.
- `npx vitest run` completo en verde (proyectos `engine` y `app-logic`).
- `git status` no muestra cambios en `engine/persistence.ts`, `engine/schema.ts` ni en
  ningún fichero de `app/`.
</verification>

<success_criteria>
- El presupuesto de 96px está medido contra 1024×768 y registrado como test ejecutable, con
  el componente aún sin construir (criterio de éxito nº 1 de la fase).
- Queda escrito y comprobado que sobran 512px para el texto del paso y que el texto grande
  parte de 40px.
- `CounterState` y `SessionContext.counters?` existen, con `null` y `0` como estados
  distintos por tipo, y `formatVersion` sigue en 1.
</success_criteria>

<output>
Create `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-01-SUMMARY.md` when done
</output>
