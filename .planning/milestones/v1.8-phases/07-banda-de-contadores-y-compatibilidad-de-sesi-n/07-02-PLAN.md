---
phase: 07-banda-de-contadores-y-compatibilidad-de-sesi-n
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - engine/__tests__/counters.test.ts
  - engine/counters.ts
autonomous: true
requirements: [HP-03, HP-05, HP-06, HP-07, HP-08]

must_haves:
  truths:
    - "El valor precargado del villano es su etapa I × nº de jugadores × dificultad, y nada más — la banda no sabe de etapas II/III (D-11, HP-05)"
    - "El valor precargado del héroe es su vida plana, sin multiplicar (D-09, HP-05): Thor 14, Iron Man 9, She-Hulk 15"
    - "Un contador sin tocar vale `null` y se pinta con el valor calculado en vivo; el primer toque congela un número concreto en la sesión (D-09)"
    - "Cambiar de héroe con el contador ya tocado conserva la cifra que tenía, sin avisar ni preguntar; si no estaba tocado, se recalcula solo (D-10)"
    - "Un valor desconocido es `null` y NO es `0` — nunca se colapsan (D-12)"
    - "▲ sobre un contador sin dato conocido arranca en 1; ▼ sobre él no hace nada (D-12)"
    - "Un toque cambia el valor en exactamente ±1, sin repetición de ningún tipo en el motor (D-13, HP-04)"
    - "▼ sobre 0 deja el contador en 0 y ▲ vuelve a subir por encima de 0 (D-15, HP-06, HP-07)"
    - "Cada mutador devuelve una sesión NUEVA reasignando objeto nuevo en los cinco niveles que toca, para que el `watchDebounced` no profundo dispare el guardado (D-20, HP-08)"
    - "Ninguna función del módulo lanza nunca, y ninguna devuelve `undefined` ni `NaN` dentro del array de contadores (D-12, D-21)"
  artifacts:
    - path: "engine/counters.ts"
      provides: "Cálculo de precarga, resolución defensiva y los cuatro mutadores puros"
      exports: ["resolveCounters", "resolveCounterValues", "computeInitialVillainHealth", "computeInitialHeroHealth", "incrementVillain", "decrementVillain", "incrementHero", "decrementHero"]
      min_lines: 90
    - path: "engine/__tests__/counters.test.ts"
      provides: "Cobertura de precarga, desigualdad referencial, topes y normalización defensiva"
      min_lines: 120
  key_links:
    - from: "engine/counters.ts"
      to: "engine/selection.ts"
      via: "reutiliza resolvePlayerSlots/resolveVillainId en vez de releer selection a mano"
      pattern: "from '\\./selection'"
    - from: "engine/counters.ts"
      to: "engine/types.ts"
      via: "importa CounterState/CharacterCatalogue/Difficulty como tipos"
      pattern: "CounterState"
---

<objective>
Escribir el motor de contadores: precarga desde el catálogo, resolución defensiva de lo
persistido y los cuatro mutadores puros. Con test primero.

Purpose: aquí vive TODA la lógica de la fase. `useGameSession.ts` (plan 04) y
`CounterBand.vue` (plan 05) serán una costura y un componente tonto porque este módulo hace
el trabajo. Y se hace con el test delante porque el fallo de mayor coste de la fase —romper
la disciplina de reasignación de D-20— es invisible en `npm run dev` y solo se manifiesta al
recargar en producción: un test de desigualdad referencial lo caza en segundos.

Output: `engine/counters.ts` y `engine/__tests__/counters.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-RESEARCH.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-PATTERNS.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-01-SUMMARY.md

<interfaces>
<!-- Contrato que este plan ESTABLECE. Los planes 03 y 04 importan estos nombres tal cual. -->

Firmas exactas que debe exportar `engine/counters.ts`:

- `resolveCounters(context: SessionContext): CounterState`
  Normalización defensiva de lo PERSISTIDO. `null` por entrada = sin tocar.
- `computeInitialVillainHealth(villain: CatalogueVillain | null, playerCount: number, difficulty: Difficulty): number | null`
- `computeInitialHeroHealth(hero: CatalogueHero | null): number | null`
- `resolveCounterValues(context: SessionContext, catalogue: CharacterCatalogue | null): CounterState`
  Valores A PINTAR: el número congelado si existe, si no el calculado en vivo, si no `null`.
- `incrementVillain(session: EngineSession, catalogue: CharacterCatalogue | null): EngineSession`
- `decrementVillain(session: EngineSession, catalogue: CharacterCatalogue | null): EngineSession`
- `incrementHero(session: EngineSession, slot: number, catalogue: CharacterCatalogue | null): EngineSession`
- `decrementHero(session: EngineSession, slot: number, catalogue: CharacterCatalogue | null): EngineSession`

Tipos ya disponibles (plan 01 + Fase 5), importados de `./types`:
`CounterState { villainHealth: number | null, heroHealth: (number | null)[] }`,
`SessionContext { playerCount, difficulty, selection?, counters?, [key: string]: unknown }`,
`EngineSession { …, context: SessionContext }`, `Difficulty = 'normal' | 'expert'`,
`VillainStage { stage, health, healthPerHero, healthPerGroup, expert? }`,
`CatalogueHero { id, name, alterEgo, health, handSizeHero, handSizeAlterEgo }`,
`CatalogueVillain { id, name, stages }`,
`CharacterCatalogue { gameId, heroes, villains }`.

Funciones ya existentes que este módulo REUTILIZA de `./selection` (no reimplementar):
`resolvePlayerSlots(context)` → `{ heroId: string | null, playerName: string }[]` con
longitud siempre `playerCount`; `resolveVillainId(context)` → `string | null`.

Cifras de referencia verificadas contra `content/marvel-characters.json`:
Rhino etapa I `health: 14, healthPerHero: true, healthPerGroup: false` (sin `expert`);
Kang etapa I `health: 12, healthPerHero: true`, con `expert: { health: 15, healthPerHero: true }`;
Ultron etapa I `health: 17, healthPerHero: true` (sin `expert`);
Kang etapa II `health: 18` con `healthPerHero: false` (vida plana);
héroes: Thor 14, Iron Man 9, She-Hulk 15.
Con 3 jugadores: Rhino 42, Kang normal 36, Kang experto 45, Ultron 51.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir `engine/__tests__/counters.test.ts` primero (RED)</name>
  <files>engine/__tests__/counters.test.ts</files>
  <read_first>
    - `engine/__tests__/selection.test.ts` — el analogo estructural completo: cabecera de
      imports, carga del fixture `./fixtures/tiny-game.json` con `readFileSync` +
      `fileURLToPath`, `const context: SessionContext = { playerCount: 3, difficulty: 'normal' }`,
      `function baseSession()` con `expand(tinyGame, context)`, y el bloque
      `describe('desigualdad referencial (Pitfall 1 / SEL-08)')` con su cadena de `not.toBe`
      en cinco niveles. Es el patrón a replicar entero.
    - `engine/__tests__/persistence.test.ts` (líneas 1-24 y 141-200) — cómo se carga un JSON
      real de `content/` dentro de un test del proyecto `engine`, y el estilo del bloque
      `D-20` de normalización con `localStorage` manipulado.
    - `engine/selection.ts` — `resolvePlayerSlots`, `resolveVillainId`, `setVillain`,
      `setHero`: son las funciones con las que el test construye la selección antes de tocar
      contadores.
    - `engine/types.ts` — `CounterState` (añadido en el plan 01) y los tipos del catálogo.
    - `content/marvel-characters.json` — comprobar de primera mano las cifras de Rhino, Kang,
      Ultron, Thor, Iron Man y She-Hulk antes de escribirlas en las aserciones.
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md`
      §D-09 a §D-15 y §D-20.
  </read_first>
  <action>
    Crear `engine/__tests__/counters.test.ts` importando de `../counters` los ocho nombres
    del bloque `<interfaces>`. El fichero **debe fallar al ejecutarse ahora** porque
    `engine/counters.ts` todavía no existe: eso es el RED esperado, no un error a evitar.

    Fixtures: `tiny-game.json` vía `readFileSync` + `fileURLToPath` y `baseSession()` calcados
    de `selection.test.ts`; además el catálogo real, cargado igual que
    `persistence.test.ts` carga `marvel-champions.json`, desde
    `../../content/marvel-characters.json` y casteado a `CharacterCatalogue` (sin validador
    de esquema: eso ya lo cubre `engine/__tests__/catalogueSchema.test.ts`).

    Bloques `describe` obligatorios, con estas aserciones concretas:

    1. `precarga (D-09/D-11/HP-05)` — `computeInitialVillainHealth` con el villano `rhino` de
       `catalogue.villains`, `playerCount: 3`, `'normal'` devuelve `42`; Kang con `'normal'`
       devuelve `36` y con `'expert'` devuelve `45`; Ultron con `'normal'` devuelve `51` y con
       `'expert'` también `51` (Rhino y Ultron no traen `expert`: ausencia = hecho del
       dominio, no error). `computeInitialHeroHealth` del héroe `thor` devuelve `14`,
       `iron-man` devuelve `9`, `she-hulk` devuelve `15` — sin multiplicar por jugadores.
       `computeInitialVillainHealth(null, 3, 'normal')` devuelve `null` y
       `computeInitialHeroHealth(null)` devuelve `null`.
       Aserción explícita de D-11: el resultado con `playerCount: 3` para Kang NUNCA es `18`
       ni `54` (la etapa II es plana y no se usa jamás en la precarga).

    2. `resolveCounters — normalización defensiva (COMP-02/D-12)` — con `counters` ausente
       devuelve `{ villainHealth: null, heroHealth: [null, null, null] }` para
       `playerCount: 3`. Casos de entrada malformada, todos sin lanzar y todos con
       `heroHealth.length` igual a `playerCount` (nunca derivada de la longitud persistida):
       `counters: null`; `counters` no-objeto (un número, una cadena); `heroHealth` no-array;
       `heroHealth` más corto que `playerCount`; `heroHealth` más largo; entradas `'12'`
       (cadena), `undefined`, `null`, `NaN`, `Infinity`, `-5`, `2.7`;
       `villainHealth: 'muchos'` y `villainHealth: NaN`.
       Aserciones duras: ninguna entrada devuelta es `undefined`
       (`expect(result.heroHealth.every(v => v === null || Number.isInteger(v))).toBe(true)`),
       ninguna es `NaN`, y ninguna es negativa. `playerCount: 0` y `playerCount: -1` y
       `playerCount: 2.5` devuelven `heroHealth: []`.

    3. `resolveCounterValues (D-09/D-12)` — sin selección y sin `counters`, todos los valores
       son `null` (la banda pintará «—»). Con `setVillain(session, 'rhino')` y
       `setHero(session, 0, 'thor')`, `villainHealth` resuelve a `42` y `heroHealth[0]` a `14`
       mientras `heroHealth[1]`/`[2]` siguen `null`. Un valor congelado gana al calculado:
       con `counters.heroHealth[0] = 8` y el héroe `thor` seleccionado, resuelve `8`, no `14`.
       Un `heroId` inexistente en el catálogo resuelve `null` (defensa ante `localStorage`
       manipulado, no caso imposible). Con `catalogue` a `null`, todo resuelve `null` sin
       lanzar.

    4. `desigualdad referencial (D-20/HP-08)` — para cada uno de los cuatro mutadores, la
       cadena completa de `not.toBe`: `result`, `result.context`,
       `result.context.counters`, `result.context.counters.heroHealth` y
       `result.context.counters.heroHealth[slot]` no son la misma referencia que los de la
       sesión de entrada, y la sesión de entrada NO ha cambiado de valor
       (`expect(session.context.counters?.heroHealth[0]).toBe(<valor original>)`). Para los
       mutadores del villano, la cadena llega hasta `counters` y `villainHealth`.

    5. `topes y arranque (D-12/D-13/D-15/HP-06/HP-07)` — `incrementHero` sobre un contador en
       `null` SIN héroe seleccionado deja `1`; `decrementHero` sobre ese mismo `null` devuelve
       **la misma referencia de sesión** (`expect(result).toBe(session)`) porque ▼ sobre «—»
       no hace nada. Con Thor seleccionado (live 14): `incrementHero` congela `15` y
       `decrementHero` congela `13`. Bajando desde `1`: `decrementHero` deja `0`; otro
       `decrementHero` devuelve la misma referencia y el valor sigue `0`, nunca `-1`;
       `incrementHero` desde `0` deja `1`. Un toque cambia exactamente ±1, nunca ±5 (D-13).
       Mismos topes para el villano: `decrementVillain` en `0` no baja de `0`.

    6. `no-op de slot fuera de rango` — `incrementHero`/`decrementHero` con `slot` `-1`, `3`
       (con `playerCount: 3`), `1.5` y `NaN` devuelven la misma referencia de sesión y no
       lanzan.

    7. `D-10 (cambio de héroe a media ronda)` — tras `setHero(0, 'thor')` e
       `incrementHero(0)` (contador congelado en 15), un `setHero(0, 'iron-man')` deja
       `resolveCounterValues(...).heroHealth[0]` en `15`, NO en `9`. Y el caso benigno: un
       slot sin tocar (`null`) con el héroe cambiado de `thor` a `iron-man` pasa de resolver
       `14` a resolver `9` solo.

    Cada `describe` lleva en su nombre el identificador de decisión que fija, igual que hace
    `selection.test.ts`. No usar `try/catch` en el test: la aserción de que nada lanza se
    escribe como `expect(() => …).not.toThrow()`.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/counters.test.ts; test $? -ne 0 && echo "RED confirmado (el modulo ../counters aun no existe)"</automated>
  </verify>
  <acceptance_criteria>
    - `engine/__tests__/counters.test.ts` existe y tiene más de 120 líneas sin contar
      comentarios: `grep -vc '^\s*//' engine/__tests__/counters.test.ts` es mayor que 120.
    - `npx vitest run engine/__tests__/counters.test.ts` falla (código distinto de 0) por no
      resolver `../counters` — RED legítimo, el módulo llega en el Task 2.
    - El fichero declara los siete describe: `grep -c "describe(" engine/__tests__/counters.test.ts`
      devuelve al menos 7.
    - `grep -c 'toBe(42)' engine/__tests__/counters.test.ts` es al menos 1;
      `grep -c 'toBe(45)'` al menos 1; `grep -c 'toBe(51)'` al menos 1;
      `grep -c 'toBe(14)'` al menos 1.
    - `grep -c 'not.toBe' engine/__tests__/counters.test.ts` es al menos 16 (cinco niveles ×
      cuatro mutadores como mínimo).
    - `grep -c 'Number.isInteger\|Number.isNaN\|not.toThrow' engine/__tests__/counters.test.ts`
      es al menos 3.
    - `grep -c "from '../counters'" engine/__tests__/counters.test.ts` devuelve 1.
    - `npx vitest run --project engine` no rompe ningún test PREEXISTENTE: la única suite en
      rojo es `counters.test.ts`.
  </acceptance_criteria>
  <done>
    El test existe, está en rojo por la razón correcta (módulo ausente) y fija por escrito
    las cifras de precarga, los topes, la desigualdad referencial y la normalización
    defensiva antes de que exista una sola línea de implementación.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementar `engine/counters.ts` hasta poner el test en verde (GREEN)</name>
  <files>engine/counters.ts</files>
  <read_first>
    - `engine/selection.ts` — el fichero COMPLETO. Es el hermano directo: su comentario de
      cabecera (líneas 1-18, la fuente de D-20), `resolvePlayerSlots` (validar por TIPO,
      longitud derivada de `playerCount`, nunca lanzar), el no-op de `setHero` con `slot`
      fuera de rango devolviendo la MISMA referencia, y el spread anidado
      `{ ...session, context: { ...session.context, selection: { ...current, heroes } } }`.
      Copiar la estructura, no inventar otra.
    - `engine/__tests__/counters.test.ts` — el contrato exacto que hay que satisfacer (Task 1).
    - `engine/types.ts` — `CounterState`, `SessionContext`, `Difficulty`, `VillainStage`,
      `CatalogueHero`, `CatalogueVillain`, `CharacterCatalogue`.
    - `content/marvel-characters.json` — la forma real de `stages[0]` y de `expert`.
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-RESEARCH.md`
      §Code Examples — los borradores verificados de `computeInitialVillainHealth` y del
      esqueleto de `resolveCounters`; son el objetivo, no un boceto a rediseñar, con la
      excepción de la validación numérica descrita abajo.
  </read_first>
  <action>
    Crear `engine/counters.ts` con las ocho exportaciones del bloque `<interfaces>`, sin
    ninguna dependencia nueva y sin `zod`.

    Comentario de cabecera con la misma forma que el de `engine/selection.ts`: declarar la
    regla dura de D-20 (cada mutador devuelve objeto NUEVO en los cinco niveles que toca —
    `session`, `context`, `counters`, `heroHealth` y la entrada — porque el
    `watchDebounced(session, …, 300ms)` de `app/pages/[game]/index.vue` NO es profundo, y una
    mutación in situ actualizaría la pantalla pero perdería los contadores al recargar), y
    declarar que `engine/persistence.ts` no se toca (D-19).

    `computeInitialVillainHealth`: lee `villain.stages[0]` y **solo** `stages[0]` (D-11: la
    precarga es la etapa I y nada más; la banda no sabe de etapas). Si no hay etapa I,
    devuelve `null`. Si `difficulty === 'expert'` y la etapa trae `expert`, usa esas cifras;
    si no las trae, usa las normales (Rhino y Ultron: ausencia = hecho del dominio).
    Multiplica por `playerCount` cuando `healthPerHero` es `true`; devuelve la cifra plana en
    otro caso. Un `playerCount` que no sea entero positivo devuelve `null` en vez de propagar
    un `NaN`.

    `computeInitialHeroHealth`: la vida del héroe es plana, sin multiplicar por jugadores
    jamás. `null`/ausente devuelve `null`.

    `resolveCounters`: normalización de lo persistido con el contrato de `resolvePlayerSlots`.
    Longitud SIEMPRE `context.playerCount` (0 si no es entero positivo), **nunca**
    `context.counters.heroHealth.length`. Validar por TIPO y no por presencia. Punto en el que
    se MEJORA el borrador de RESEARCH.md, y hay que hacerlo así a propósito: `typeof x === 'number'`
    acepta `NaN` e `Infinity`, y ambos llegarían a la pantalla — usar `Number.isFinite`, y
    normalizar cada valor válido con `Math.trunc` y un suelo en `0` (`Math.max(0, …)`), de modo
    que un `-5` o un `2.7` manipulados a mano se saneen en vez de propagarse. Sin tope
    superior a propósito (D-22: no se gasta esfuerzo en defender más de lo necesario; el
    presupuesto de 3 dígitos de `07-UI-SPEC.md` es una nota de diseño, no una validación).
    Cualquier otro valor pasa a `null`. Nunca lanza, nunca devuelve `undefined` dentro del
    array.

    `resolveCounterValues`: para el villano, `resolveCounters(context).villainHealth` si es un
    número; si no, `computeInitialVillainHealth` con el villano resuelto por
    `resolveVillainId(context)` buscado en `catalogue.villains`; si no se conoce, `null`. Para
    cada slot, lo mismo con `resolvePlayerSlots(context)[i].heroId` buscado en
    `catalogue.heroes`. Un `catalogue` a `null` o un id ausente del catálogo dan `null`, no un
    error. Este es el punto donde D-09 se cumple sin ningún «instante de precarga»: no hay
    momento mágico que pueda perderse ni duplicarse.

    Los cuatro mutadores: firma y semántica exactas del bloque `<interfaces>`.
    Cada uno calcula su valor base con `resolveCounterValues(session.context, catalogue)` — es
    lo que hace que el primer toque congele el número que el grupo está VIENDO (D-09).
    Si el base es `null`: ▲ escribe `1` (D-12), ▼ devuelve la MISMA referencia de sesión sin
    clonar (no-op silencioso, igual que `setHero` con `slot` inválido).
    Si el base es un número: ▲ escribe `base + 1`; ▼ escribe `base - 1` salvo que `base` sea
    `0`, en cuyo caso devuelve la misma referencia (D-15: ▼ en 0 no hace nada, y no baja de 0).
    Ese tope en 0 aplica también al villano; lo que el villano NO tiene es marca de «SIN VIDA»
    (D-11), y eso se decide en la capa de presentación, no aquí.
    `incrementHero`/`decrementHero` validan `slot` con la misma guarda que `setHero`
    (`Number.isInteger(slot) && slot >= 0 && slot < playerCount`) y devuelven la misma
    referencia si falla.
    El objeto `counters` de partida se toma SIEMPRE de `resolveCounters(session.context)`,
    nunca de `session.context.counters` directamente y nunca con `{ ...session.context.counters }`:
    así el nuevo objeto se construye desde un literal saneado y ninguna clave desconocida ni
    heredada de un JSON manipulado se copia hacia adelante (ver `<threat_model>`).

    Prohibiciones explícitas de esta fase, que deben poder comprobarse leyendo el fichero:
    sin `throw` en ninguna línea; sin `try`/`catch`; sin `setInterval`/`setTimeout`/
    `requestAnimationFrame` (D-13: no hay repetición al mantener pulsado, ni aquí ni en el
    componente); sin escritura en propiedad anidada (`counters.heroHealth[i] =`, `.push`,
    `.splice`, `+=`); sin ninguna mención a etapas II/III, amenaza, aturdido, confundido ni
    duro (D-11 y `PITFALLS.md` §14).
  </action>
  <verify>
    <automated>npx vitest run --project engine</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run engine/__tests__/counters.test.ts` termina con código 0 y todos los
      tests del Task 1 en verde.
    - `npx vitest run --project engine` termina con código 0 (toda la suite del motor,
      incluida `voice-drift.test.ts`, sigue verde y no se regeneró ningún clip de voz).
    - `grep -c 'export function' engine/counters.ts` devuelve al menos 8.
    - `grep -c 'stages\[0\]' engine/counters.ts` es al menos 1 y
      `grep -c 'stages\[1\]\|stages\[2\]' engine/counters.ts` devuelve 0 (D-11).
    - `grep -c 'Number.isFinite' engine/counters.ts` es al menos 1 y
      `grep -c 'Math.max(0' engine/counters.ts` es al menos 1.
    - `grep -c 'throw\|try {\|catch' engine/counters.ts` devuelve 0.
    - `grep -c 'setInterval\|setTimeout\|requestAnimationFrame' engine/counters.ts` devuelve 0.
    - `grep -c "from './selection'" engine/counters.ts` devuelve 1 (reutiliza
      `resolvePlayerSlots`/`resolveVillainId` en vez de releer `selection` a mano).
    - `grep -cE '\.push\(|\.splice\(|heroHealth\[[a-z]+\] *=|\+=' engine/counters.ts` devuelve 0.
    - `git diff --name-only` NO incluye `engine/persistence.ts`, `engine/schema.ts` ni ningún
      fichero de `content/`.
  </acceptance_criteria>
  <done>
    `engine/counters.ts` existe, todo el test del Task 1 está en verde, las cifras de precarga
    salen del catálogo de la etapa I, los topes en 0 y el arranque en 1 se comportan como fija
    D-12/D-15, y ninguna función lanza ni muta in situ.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` → `resume()` → `context.counters` | Entrada no confiable: editable desde DevTools y corruptible por una escritura a medias. Todo lo que este módulo lee de ahí se valida antes de usarse. |
| `content/marvel-characters.json` → cálculo de precarga | Dato del propio repo, generado por `scripts/catalogue/fetch-marvelcdb.mjs` y validado en CI por `engine/__tests__/catalogueSchema.test.ts`. Frontera de confianza interna: se trata como confiable, pero un `stages[0]` ausente devuelve `null` en vez de reventar. |
| Ninguna otra | Sin backend, sin red en ejecución, sin auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | `resolveCounters` leyendo `context.counters` | mitigate | Validación por TIPO con `Number.isFinite` (no `typeof === 'number'`, que acepta `NaN`/`Infinity`), `Math.trunc` y suelo en `0`; longitud derivada de `context.playerCount`, nunca de la longitud persistida. Test dedicado con las 12 formas malformadas del describe 2. |
| T-07-05 | Tampering | Ensanchado del objeto persistido en los mutadores | mitigate | Los mutadores construyen el `counters` nuevo desde el literal saneado que devuelve `resolveCounters`, nunca desde `{ ...context.counters }`. Ninguna clave desconocida de un JSON manipulado (incluida una `__proto__` como propiedad propia, que `JSON.parse` sí crea) se propaga a la sesión nueva. Gate: `grep` de `...session.context.counters` debe ser 0. |
| T-07-06 | Denial of Service (denegación de los datos propios) | Un normalizador que lanzara con datos corruptos | mitigate | Contrato «nunca lanza» heredado de `resolvePlayerSlots`: sin `throw`, sin `try`/`catch` en todo el fichero (gate `grep`). Un normalizador que lanzase dejaría la partida real de la tablet irrecuperable en la pantalla de carga — el daño de mayor severidad realista de la fase. |
| T-07-07 | Tampering | `slot` fuera de rango escribiendo en un índice inesperado | mitigate | Guarda `Number.isInteger(slot) && slot >= 0 && slot < playerCount` idéntica a la de `setHero`, con no-op de misma referencia. Test dedicado (describe 6) con `-1`, `3`, `1.5` y `NaN`. |
| T-07-08 | Repudiation / Elevation of Privilege | — | accept | No aplica: no hay identidades, ni roles, ni auditoría que falsificar. Un dispositivo, un grupo de amigos, sin cuentas. |
| T-07-SC | Tampering (cadena de suministro) | Dependencias | accept | Este plan no instala ni actualiza ningún paquete: `engine/counters.ts` usa solo TypeScript plano e imports relativos del propio repo. No hay superficie de cadena de suministro que auditar. |
</threat_model>

<verification>
- `npx vitest run` completo en verde (proyectos `engine` y `app-logic`).
- `git diff --stat` limitado a `engine/counters.ts` y `engine/__tests__/counters.test.ts`.
- Los 35 clips de voz siguen intactos: `npx vitest run engine/__tests__/voice-drift.test.ts`
  en verde sin regenerar nada (esta fase no toca ningún `text` ni `speech`).
</verification>

<success_criteria>
- Las cuatro cifras de referencia de la fase salen del catálogo por cálculo, no por tabla
  copiada: Rhino 42, Kang normal 36, Kang experto 45, Ultron 51 con 3 jugadores; Thor 14.
- `null` (sin tocar) y `0` (tocado y a cero) son estados distintos en todo el módulo.
- ▼ en 0 y ▼ sobre «—» son no-ops de misma referencia; ▲ sobre «—» arranca en 1.
- Los cuatro mutadores pasan la cadena de desigualdad referencial en cinco niveles.
- Ninguna función lanza ni deja `undefined`/`NaN` en el array de contadores.
</success_criteria>

<output>
Create `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-02-SUMMARY.md` when done
</output>
