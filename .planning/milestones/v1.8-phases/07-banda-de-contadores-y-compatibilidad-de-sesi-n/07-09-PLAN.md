---
phase: 07-banda-de-contadores-y-compatibilidad-de-sesi-n
plan: 09
type: execute
wave: 7
gap_closure: true
depends_on: []  # los planes 07-01..07-07 están completos; esta ola continúa la numeración de la fase
files_modified:
  - engine/counters.ts
  - engine/__tests__/counters.test.ts
autonomous: true
requirements: [HP-05, HP-08]

must_haves:
  truths:
    - "Con un `playerCount` manipulado a mano en localStorage (2.5, NaN, '3', null), ningún mutador de contador produce NaN ni descarta los valores de vida ya congelados de los héroes (WR-05, confirmado por el verificador de forma independiente)"
    - "Una entrada de catálogo sin `health` utilizable devuelve `null` en vez de propagar un NaN hasta la pantalla — la banda nunca puede pintar «NaN» en la mesa (WR-07)"
    - "`resolveCounterValues` nunca devuelve `undefined` dentro de su array, ni siquiera si el índice consultado queda fuera de rango (el contrato que la cabecera del fichero ya promete)"
    - "La longitud del array de vidas y la guarda de rango de slot usan UNA sola definición de `playerCount` válido, no dos que puedan discrepar"
    - "Todo lo que ya funcionaba sigue igual: precarga de etapa I × jugadores × dificultad, topes en 0, arranque en 1 desde «—» y reasignación referencial de D-20"
  artifacts:
    - path: "engine/counters.ts"
      provides: "Una única normalización de playerCount y guardas por tipo en vez de por identidad con null"
      contains: "Number.isInteger"
    - path: "engine/__tests__/counters.test.ts"
      provides: "Casos de regresión de playerCount manipulado y de catálogo sin cifra de vida"
      contains: "it.each"
  key_links:
    - from: "engine/counters.ts"
      to: "engine/selection.ts"
      via: "resolvePlayerSlots / resolveVillainId — la longitud sigue derivándose de playerCount, nunca de counters.heroHealth.length"
      pattern: "resolvePlayerSlots"
---

<objective>
Cerrar WR-05 y WR-07 de `07-REVIEW.md`, los dos únicos avisos del motor que el verificador
confirmó de forma independiente como pérdida real de datos o como número inventado en
pantalla, sin tocar ninguna decisión bloqueada.

Purpose: WR-05 está reproducido con cifras (`[5,7]` → `[]` tras un solo toque en el villano
con `playerCount = 2.5`): un `localStorage` editado a mano hace que los mutadores del villano
**descarten las vidas ya congeladas de los héroes**. WR-07 es el mismo tipo de fallo por el
otro extremo: `figures.health * playerCount` sin guarda puede devolver NaN y
`resolveCounterValues` solo sanea lo persistido, no lo calculado, así que un catálogo
regenerado por `scripts/catalogue/fetch-marvelcdb.mjs` al que le faltase `health` pintaría
«NaN» en la banda. Los dos contradicen literalmente el contrato que la cabecera de
`engine/counters.ts` ya promete («nunca devuelve `undefined` dentro del array», «un
`playerCount` que no sea entero positivo devuelve null en vez de propagar un NaN»), y los dos
producen exactamente lo que `CLAUDE.md` §Constraints prohíbe: una cifra confiadamente
equivocada delante del grupo.

**Nota sobre `requirements`:** HP-05 y HP-08 ya están SATISFECHOS por los planes 07-02/07-04.
Este plan **no los re-reclama**: los endurece contra una entrada manipulada que el gate
original no cubría. La cobertura de la fase no cambia; cambia la confianza en ella.

**Por qué este plan NO adopta la otra mitad de la sugerencia de WR-06** (añadir
`typescript`/`vue-tsc` y un `typecheck` en CI, que es lo que habría detectado estos dos
fallos por análisis estático): (a) es un cambio de herramienta con radio de acción sobre todo
el repositorio, no sobre el hueco de esta fase; (b) instalar paquetes exige un Package
Legitimacy Audit y `07-RESEARCH.md` §Package Legitimacy Audit (A2) registra explícitamente
que esta fase necesita **cero** dependencias nuevas — abrir esa puerta aquí saltaría el gate;
y (c) la falta de tipos concreta que habría cazado se arregla aquí directamente, con tests de
ejecución que no dependen de que exista un comprobador. **Queda diferido de forma explícita y
recomendado como trabajo propio** (`/gsd:quick` o fase de endurecimiento), no descartado en
silencio.

Output: `engine/counters.ts` endurecido y `engine/__tests__/counters.test.ts` con los casos de
regresión que lo fijan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-REVIEW.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-VERIFICATION.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md
@engine/counters.ts
@engine/__tests__/counters.test.ts

<interfaces>
<!-- Firmas actuales, para no explorar el repo. -->

engine/counters.ts (exportaciones de hoy — ninguna cambia de firma en este plan):
  computeInitialVillainHealth(villain: CatalogueVillain | null, playerCount: number, difficulty: Difficulty): number | null
  computeInitialHeroHealth(hero: CatalogueHero | null): number | null
  resolveCounters(context: SessionContext): CounterState
  resolveCounterValues(context: SessionContext, catalogue: CharacterCatalogue | null): CounterState
  incrementVillain(session, catalogue): EngineSession
  decrementVillain(session, catalogue): EngineSession
  incrementHero(session, slot: number, catalogue): EngineSession
  decrementHero(session, slot: number, catalogue): EngineSession

CounterState = { villainHealth: number | null, heroHealth: (number | null)[] }

Definición de longitud válida que YA usa `resolveCounters`:
  Number.isInteger(context.playerCount) && context.playerCount > 0 ? context.playerCount : 0

Guarda de rango que hoy usan `incrementHero`/`decrementHero` (la que discrepa):
  !Number.isInteger(slot) || slot < 0 || slot >= session.context.playerCount  // playerCount CRUDO

Convenciones del fichero de tests: `describe` en español citando la decisión (D-09/D-12/D-20…),
`it.each([...])('… %s', …)` para tablas de entradas inválidas (ver los bloques
«playerCount %s devuelve heroHealth vacío» y «no-op de slot fuera de rango»).

Comando: `npm run test` (Vitest, 514 tests hoy en verde).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests de regresión que fijan el daño de un playerCount manipulado y de un catálogo sin cifra de vida</name>
  <files>engine/__tests__/counters.test.ts</files>
  <behavior>
    - Con `context.playerCount = 2.5` y dos vidas de héroe ya congeladas (p. ej. `[5, 7]`),
      un toque en `incrementVillain` deja `heroHealth` con esas dos vidas intactas — hoy las
      deja en `[]` (reproducido por el verificador).
    - Lo mismo para `decrementVillain`, y para `playerCount` valiendo `Number.NaN`, `'3'`
      (cadena) y `null` forzados con un cast, que son formas alcanzables desde un
      `localStorage` editado a mano.
    - Ningún mutador devuelve nunca un valor `NaN` dentro de `counters.heroHealth` ni en
      `counters.villainHealth`, para ninguna de esas entradas.
    - `resolveCounterValues` sobre un índice fuera de rango devuelve `null`, nunca
      `undefined` (comprobado con `Object.is(valor, undefined) === false`).
    - `computeInitialVillainHealth` con un villano cuya etapa I no trae una cifra de vida
      utilizable (`health` ausente o no finito, villano construido a mano en el test)
      devuelve `null`, no `NaN`.
    - Los casos que YA pasan siguen pasando: Rhino 14×3=42, Kang experto 15×3=45, Thor 14,
      tope en 0, arranque en 1 desde «—», y la desigualdad referencial de D-20.
  </behavior>
  <read_first>
    - `engine/__tests__/counters.test.ts` completo — estilo de `describe`/`it.each`, cómo se
      construyen las sesiones y el catálogo de prueba.
    - `engine/counters.ts` completo — el comportamiento actual que hay que fijar.
    - `07-REVIEW.md` §WR-05 y §WR-07 — las cifras exactas del fallo reproducido.
    - `07-CONTEXT.md` D-09, D-12, D-20, D-22 — lo que NO debe cambiar de comportamiento.
  </read_first>
  <action>
Añadir a `engine/__tests__/counters.test.ts` dos `describe` nuevos, sin tocar ni reescribir
ningún test existente:

1. `describe('playerCount manipulado a mano (WR-05)')` con un `it.each` sobre
   `[2.5, Number.NaN, '3', null]` (los tres últimos con el cast que el fichero ya usa para
   entradas inválidas) que, para cada entrada: construye una sesión con dos vidas de héroe ya
   congeladas, llama a `incrementVillain` y a `decrementVillain`, y afirma (a) que las vidas
   congeladas siguen ahí con su valor exacto, y (b) que ningún número del `CounterState`
   resultante es `NaN`. Añadir un caso análogo para `incrementHero`/`decrementHero` con un
   slot dentro del rango real de los datos congelados.

2. `describe('catálogo sin cifra de vida utilizable (WR-07)')` con un villano construido a
   mano cuya etapa I no trae `health` utilizable, afirmando que
   `computeInitialVillainHealth` devuelve `null` — y un caso extra que comprueba que ese
   `null` llega a `resolveCounterValues` como `null` (celda «—», D-12) y no como `NaN`.

Ejecutar `npm run test` y registrar en el SUMMARY qué tests nuevos fallan ANTES del arreglo
(evidencia RED). No modificar `engine/counters.ts` en esta tarea.
  </action>
  <acceptance_criteria>
    - `npm run test` ejecuta los tests nuevos y falla en ellos ANTES de la Task 2; la salida
      real de esos fallos queda copiada en el SUMMARY.
    - `git diff engine/__tests__/counters.test.ts | grep -c '^-[^-]'` devuelve 0: no se ha
      borrado ni reescrito ninguna línea de los tests existentes, solo se ha añadido.
    - Los 514 tests preexistentes siguen ejecutándose (el total sube, no baja).
    - Los `describe` nuevos citan WR-05 y WR-07 en su título, para que el motivo del test sea
      rastreable desde el informe de revisión.
  </acceptance_criteria>
  <verify>
    <automated>npm run test; echo "exit=$? (se espera != 0 en esta tarea: RED)"</automated>
  </verify>
  <done>Los dos fallos confirmados del motor están fijados por tests que hoy fallan.</done>
</task>

<task type="auto">
  <name>Task 2: Una sola definición de playerCount válido y guardas por tipo en engine/counters.ts</name>
  <files>engine/counters.ts</files>
  <read_first>
    - `engine/counters.ts` completo, con su cabecera de contrato.
    - `engine/selection.ts` — el contrato hermano (`resolvePlayerSlots`) del que este fichero
      hereda la disciplina de validar por TIPO y derivar la longitud de `playerCount`.
    - `07-REVIEW.md` §WR-05 y §WR-07.
    - `07-CONTEXT.md` D-20 — la reasignación referencial que NO se puede romper.
  </read_first>
  <action>
Tres cambios acotados, sin cambiar ninguna firma exportada ni el comportamiento observable en
uso normal:

1. Extraer una función interna (no exportada) que normalice `context.playerCount` con la
   MISMA regla que ya usa `resolveCounters` (entero > 0, si no 0), y usarla como única fuente
   de la longitud en todo el fichero: `resolveCounters` y las guardas de rango de
   `incrementHero`/`decrementHero`. Hoy la guarda de rango compara contra el valor CRUDO y la
   longitud del array contra el validado; esa discrepancia es la causa raíz de WR-05.

2. Sustituir las comparaciones por identidad con `null` que deciden el valor base
   (`resolveCounterValues` sobre el valor congelado, y el `base` de los cuatro mutadores) por
   comprobaciones de TIPO numérico finito, de forma que un `undefined` de un índice fuera de
   rango tome el mismo camino que un `null` (arranque en 1 para ▲, no-op para ▼, D-12) en
   lugar de convertirse en `NaN`. El comportamiento en uso normal no cambia: `null` sigue
   significando «sin tocar».

3. En `computeInitialVillainHealth`: guardar también la cifra del catálogo — si no es un
   número finito, devolver `null` (la promesa que la cabecera ya hace). Y colapsar la rama
   muerta de `healthPerGroup`, que hoy devuelve exactamente la misma expresión que el retorno
   siguiente, en una sola expresión con `healthPerHero`, para que ningún futuro colaborador
   cambie una de las dos ramas idénticas y no la otra.

Actualizar la cabecera del fichero solo si hace falta para que siga describiendo la verdad.
Cuidado con el precedente ya documentado en 07-04/07-05-SUMMARY.md: no citar en comentarios
literales que otro gate cuenta.
  </action>
  <acceptance_criteria>
    - `npm run test` pasa al 100%, incluidos los tests nuevos de la Task 1 y los 514 previos.
    - `npm run e2e` sigue en verde (el motor alimenta la banda; la precarga 42/14 del test de
      catálogo debe seguir exacta).
    - `npm run generate` termina en verde.
    - `grep -c 'session.context.playerCount\|context.playerCount' engine/counters.ts` sigue
      siendo > 0 pero **ninguna guarda de rango de slot** compara ya contra el valor sin
      normalizar: revisión de `git diff` + los tests de la Task 1 en verde lo demuestran.
    - Ninguna firma exportada cambia:
      `grep -c '^export function' engine/counters.ts` sigue devolviendo 8.
    - D-20 intacta: los tests de «desigualdad referencial» preexistentes siguen pasando sin
      modificarse.
    - `computeInitialVillainHealth` ya no contiene dos retornos idénticos consecutivos
      (revisión del diff).
  </acceptance_criteria>
  <verify>
    <automated>npm run test && npm run e2e && npm run generate</automated>
  </verify>
  <done>Un `playerCount` manipulado ya no descarta vidas congeladas ni produce NaN, y un catálogo sin cifra devuelve «—» en vez de «NaN».</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `localStorage` (`tga:progress`) → `engine/counters.ts` | **La única frontera de confianza real de esta app.** No hay backend, no hay red, no hay entrada de texto libre; pero el JSON de la sesión es editable a mano desde las devtools del propio dispositivo. `engine/counters.ts` ya declara en su cabecera que no confía en ningún campo persistido — este plan cierra los dos sitios donde esa promesa no se cumplía. |
| `content/marvel-champions.json` → motor | Contenido versionado en el repo, generado por `scripts/catalogue/fetch-marvelcdb.mjs` desde una API de terceros. Confiable en el commit, pero su forma depende de una fuente externa que puede cambiar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Tampering | `incrementVillain`/`decrementVillain` con `playerCount` manipulado | mitigate | WR-05: una sola normalización de `playerCount` en todo el fichero, más `it.each` de regresión sobre `[2.5, NaN, '3', null]` que fija que las vidas congeladas no se descartan. |
| T-07-09-02 | Tampering (datos de terceros) | `computeInitialVillainHealth` sobre el catálogo | mitigate | WR-07: guarda de número finito sobre la cifra del catálogo; devuelve `null` («—», D-12) en vez de propagar NaN a la pantalla. |
| T-07-09-03 | Denial of service | Mutadores puros | accept | Sin bucles, sin temporizadores, sin recursión; una entrada manipulada produce un no-op o un `null`, nunca un cuelgue. Los mutadores ya devuelven la misma referencia como no-op. |
| T-07-09-04 | Elevation of privilege / Spoofing / Repudiation | — | accept — no aplica | App local sin autenticación, sin cuentas, sin servidor y sin registro de auditoría. No hay privilegio que elevar ni identidad que suplantar. Decirlo así es más honesto que inventar una amenaza. |
| T-07-07-SC | Tampering (cadena de suministro) | instalaciones npm/pip/cargo | accept — no aplica | Este plan no instala nada. `07-RESEARCH.md` §Package Legitimacy Audit (A2): cero dependencias nuevas en toda la fase. La sugerencia de WR-06 de añadir `typescript`/`vue-tsc` queda diferida precisamente porque abriría este vector sin auditoría previa. |
</threat_model>

<verification>
- `npm run test` → 100% verde, con los tests nuevos incluidos.
- `npm run e2e` → 24 preexistentes + los del plan 07-08 (si ya está fusionado) en verde.
- `npm run generate` → build en verde.
- Evidencia RED de la Task 1 registrada en el SUMMARY.
</verification>

<success_criteria>
- Un `playerCount` no entero ya no puede descartar vidas de héroe congeladas.
- Ningún camino del motor puede poner un `NaN` ni un `undefined` delante del grupo.
- La longitud del array y la guarda de rango comparten una única definición.
- Precarga, topes, arranque en 1 y reasignación referencial siguen exactamente igual.
</success_criteria>

<output>
Crear `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-09-SUMMARY.md`
al terminar, con la evidencia RED y el recuento final de Vitest.
</output>
