---
phase: 07-banda-de-contadores-y-compatibilidad-de-sesi-n
plan: 05
type: execute
wave: 4
depends_on: ["07-01", "07-04"]
files_modified:
  - app/components/CounterBand.vue
  - app/pages/[game]/index.vue
autonomous: true
requirements: [HP-01, HP-03, HP-04, HP-08, HP-10]

must_haves:
  truths:
    - "La banda es un hijo flex de altura fija `h-24 shrink-0`, nunca `flex-1` y nunca sin tope de altura; `StepScreen` sigue siendo el único `flex-1` de la pila (D-01, HP-02)"
    - "Cada celda tiene la etiqueta encima a `text-label` y `▼ número ▲` en una línea con el número a `text-display`, y las flechas son zonas pulsables de los 96px completos de alto (D-02, HP-10)"
    - "La banda va justo bajo `AppHeader` y nunca pegada a `NavBand`, de modo que ningún ▼ queda junto al botón SIGUIENTE (D-03)"
    - "Por debajo de 640px la banda se parte en dos filas (villano arriba, jugadores abajo) y el número sigue a 40px (D-05)"
    - "La banda no se puede plegar ni ocultar a mano, y `AppHeader` no gana nada nuevo (D-08)"
    - "Un toque en ▼/▲ cambia el valor en ±1 y no existe repetición al mantener pulsado: no hay ni un temporizador en el componente (D-13, HP-04, HP-10)"
    - "El estado visual de pulsado va en `@touchstart`/`@touchend`/`@mousedown`/`@mouseup` y la acción va SOLO en `@click`, sin excepción (D-14)"
    - "Las flechas nunca se pintan desactivadas ni atenuadas: los topes son no-ops silenciosos (D-12, D-15)"
    - "No hay deshacer, ni destello de confirmación, ni pulsación larga: ▲ deshace ▼ y nada más (D-18)"
    - "El componente no importa `~~/engine/*` y pinta el nombre del jugador por interpolación, nunca con `v-html` (T-01-01)"
    - "Los contadores se persisten con la sesión sin una línea nueva de fontanería: `watchDebounced` y `pagehide` de la página quedan intactos (D-19, D-20, HP-08)"
  artifacts:
    - path: "app/components/CounterBand.vue"
      provides: "La banda de contadores: celdas, glifos, feedback de pulsado y partido en dos filas"
      min_lines: 45
    - path: "app/pages/[game]/index.vue"
      provides: "Montaje de la banda como hermano inmediato tras AppHeader"
      contains: "<CounterBand"
  key_links:
    - from: "app/pages/[game]/index.vue"
      to: "app/components/CounterBand.vue"
      via: "v-if showsCounterBand + :cells + @increment/@decrement"
      pattern: "<CounterBand\\s+v-if=\"showsCounterBand\""
    - from: "app/components/CounterBand.vue"
      to: "app/components/NavBand.vue"
      via: "mismo patrón h-24 shrink-0 bg-surface y misma separación pulsado/click"
      pattern: "h-24 shrink-0"
---

<objective>
Construir la banda y montarla en la pantalla de juego.

Purpose: el presupuesto ya está medido (plan 01), la lógica ya está en el motor (plan 02) y
las celdas ya llegan resueltas (plan 04). Aquí solo queda pintar y cablear — y hacerlo sin
reabrir ni una decisión visual: `07-UI-SPEC.md` §Layout §2 contiene la plantilla completa de
la celda, con sus clases, sus `aria-label` y su separación de eventos, y es el objetivo
literal, no un boceto.

Output: `app/components/CounterBand.vue` y el montaje en `app/pages/[game]/index.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-UI-SPEC.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-PATTERNS.md
@.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-04-SUMMARY.md

<interfaces>
<!-- Contrato ya fijado por el plan 04 y por 07-UI-SPEC.md §Component Inventory. -->

`CounterBand.vue`:
- props: `cells: { key: string, label: string, displayValue: string, defeated: boolean }[]`
  (el tipo se declara EN el componente, sin importar `CounterCell` de `~~/engine/*` ni del
  composable: componente tonto).
- emits: `increment: [key: string]`, `decrement: [key: string]`.
- estado interno único: `pressedKey = ref<string | null>(null)`, con valores
  `` `${cell.key}:down` `` y `` `${cell.key}:up` ``. Solo visual, nunca persistido.
- claves que llegan en `cells`: `'villano'` para la celda del villano, `'jugador-{i}'` para
  cada jugador (índice base 0).

Ya disponible de `useGameSession()` (plan 04): `showsCounterBand`, `counterCells`,
`incrementCounter(key)`, `decrementCounter(key)`.

Tokens de diseño ya existentes en `app/assets/css/main.css`, ninguno nuevo:
`--text-display: 2.5rem` (40px), `--text-heading: 1.75rem` (28px),
`--text-label: 1.125rem` (18px), `--color-surface`, `--color-background`,
`--color-primary-text`, `--color-accent`, `--color-warning`, `--spacing-xs: 4px`.

Orden final de la pila del `<div class="h-dvh flex flex-col">` de la página:
`AppHeader` (h-16) → `CounterBand` (h-24, `v-if`) → `VoiceUnavailableNotice` (`v-if`) →
`StepScreen` (flex-1, único) → `NavBand` (h-24) → bloque de overlays `fixed inset-0 z-50`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear `app/components/CounterBand.vue` según la plantilla ya fijada</name>
  <files>app/components/CounterBand.vue</files>
  <read_first>
    - `app/components/NavBand.vue` — el fichero completo (49 líneas). Es el modelo literal:
      `h-24 shrink-0 bg-surface flex` como cascarón de banda fija, y la separación entre estado
      visual de pulsado (`@mousedown`/`@mouseup`/`@touchstart`/`@touchend` sobre un `ref`, con
      `transition-transform duration-75` y `brightness-95 scale-[0.98]`) y acción
      (`@click` únicamente). Su comentario de cabecera sobre no declarar estados inactivos es
      también el precedente de la prohibición de `disabled` de esta fase.
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-UI-SPEC.md`
      §Layout §2 (la plantilla completa de la celda, con clases, `aria-label`s, la etiqueta
      como overlay `pointer-events-none` y el divisor `border-l border-background`),
      §Layout §3 (el partido en dos filas con `sm:contents` y el breakpoint `sm` = 640px),
      §Typography, §Color (la regla dura: el número NUNCA es `text-warning`, ni en 0) y
      §Component Inventory. **Es el contrato, no una sugerencia.**
    - `app/components/StepScreen.vue` — para ver el uso existente del breakpoint `sm`
      (`grid-cols-1 sm:grid-cols-2`), que es de donde sale el valor de 640px, y el uso de
      `text-warning` en la línea `⚠` (el token que la etiqueta derrotada reutiliza). **No
      modificar este fichero.**
    - `app/assets/css/main.css` — el bloque `@theme` con los tokens; comprobar que no hace
      falta ninguno nuevo.
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md`
      §D-01, §D-02, §D-05, §D-08, §D-13, §D-14, §D-15, §D-18.
  </read_first>
  <action>
    Crear `app/components/CounterBand.vue` como componente tonto con `<script setup lang="ts">`.

    Script: `import { computed, ref } from 'vue'` y nada más — **ningún** import de
    `~~/engine/*` ni de ningún composable. `defineProps` con el tipo `cells` del bloque
    `<interfaces>` declarado en línea. `defineEmits` con `increment: [key: string]` y
    `decrement: [key: string]`. Un único `const pressedKey = ref<string | null>(null)`. Dos
    computeds de partición por la clave: la celda del villano es la que tiene
    `key === 'villano'` (o `null` si no viniera), y las celdas de jugador son el resto en el
    orden en que llegan. Comentario de cabecera declarando que es un componente tonto, que
    recibe los valores ya resueltos («—» incluido) y que no distingue `null` de un número —
    eso ya lo hizo el composable (D-09/D-12).

    Template: cascarón `h-24 shrink-0 bg-surface` que en `sm:` y arriba es una sola fila y por
    debajo se apila en dos, siguiendo el enfoque de `07-UI-SPEC.md` §Layout §3 (dos `<div>`
    envoltorio, cada uno `h-24 shrink-0 flex`, con `sm:contents` para que desaparezcan del
    layout en el viewport objetivo y las celdas pasen a ser hijas directas de una única fila).
    En el viewport estrecho la banda mide 192px en total, y eso es una decisión explícita de
    D-05/D-06, no un incumplimiento de HP-02.

    Cada celda —la del villano y cada una de jugador, sin ninguna distinción visual— se pinta
    con la plantilla de `07-UI-SPEC.md` §Layout §2 tal cual: `relative flex-1 min-w-0 h-24`
    con `border-l border-background first:border-l-0`; dentro, una fila `h-24 flex items-stretch`
    con el botón `▼`, el `<span>` del número y el botón `▲`; y encima, la etiqueta como
    `<span>` absoluto con `pointer-events-none` para que no robe ni un píxel de zona pulsable.
    Los dos botones llevan `type="button"`, `flex-1 min-w-11 h-24` (los 96px completos de alto
    de D-02, y 44px de ancho mínimo como suelo táctil), el glifo a `text-heading` y en
    `text-accent`. El número va a `text-display` en `text-primary-text` **en todos los estados,
    incluido el 0** — la regla de color más importante de la fase: el aviso vive en la
    etiqueta, nunca en la cifra. La etiqueta va a `text-label font-bold` con `truncate`, y su
    color es `text-warning` cuando `cell.defeated` es `true` y `text-primary-text` en el resto.

    Eventos, regla dura de D-14 sin excepción: `@mousedown`/`@touchstart` fijan
    `pressedKey` a `` `${cell.key}:down` `` o `` `${cell.key}:up} ``, `@mouseup`/`@touchend` lo
    ponen a `null`, y **la única** vía de acción es `@click`, que emite `decrement`/`increment`
    con `cell.key`. Incrementar o decrementar desde `@touchstart` está prohibido: es el camino
    directo al doble conteo fantasma de Safari en iOS.

    `aria-label` de cada botón, con la etiqueta VIVA de la celda (no una cadena fija), en los
    literales exactos del Copywriting Contract: `Bajar vida de {label}` y
    `Subir vida de {label}`.

    Prohibiciones que deben poder comprobarse leyendo el fichero:
    sin `flex-1` en el cascarón de la banda ni ausencia de tope de altura (D-01 lo llama el
    síntoma nº 1 a vigilar); sin atributo `disabled` y sin `opacity-` en los botones (los topes
    son no-ops silenciosos, D-12/D-15, y `07-UI-SPEC.md` lo prohíbe explícitamente);
    sin `setInterval`/`setTimeout`/`requestAnimationFrame` ni ningún `@touchmove`/`pointerdown`
    para repetición al mantener pulsado (D-13); sin animación de destello del número (D-18);
    sin control de plegar/ocultar (D-08); sin `v-html` (T-01-01); sin ningún control ni mención
    de etapas del villano, amenaza, aturdido, confundido o duro (D-11, `PITFALLS.md` §14);
    sin ningún `<input>` (HP-04: no se escriben cifras).
  </action>
  <verify>
    <automated>npm run generate</automated>
  </verify>
  <acceptance_criteria>
    - `npm run generate` termina con código 0 (el componente compila dentro del build SSG real).
    - `grep -c 'h-24 shrink-0' app/components/CounterBand.vue` es al menos 1.
    - `grep -c 'flex-1' app/components/CounterBand.vue` es al menos 1 (las celdas y los
      botones lo usan) pero la RAÍZ del template no lo lleva — verificado leyendo la primera
      etiqueta del `<template>`, que debe empezar por `h-24 shrink-0`.
    - `grep -c 'text-display' app/components/CounterBand.vue` es al menos 1 y
      `grep -c 'text-label' app/components/CounterBand.vue` es al menos 1.
    - `grep -c 'text-warning' app/components/CounterBand.vue` devuelve exactamente 1, y esa
      línea es la del `:class` de la etiqueta, no la del número.
    - `grep -c '@click' app/components/CounterBand.vue` devuelve 2 y
      `grep -c '@touchstart' app/components/CounterBand.vue` devuelve 2 — y ninguna línea con
      `@touchstart` contiene `emit(`.
    - `grep -c 'emit(' app/components/CounterBand.vue` devuelve 2, y ambas ocurrencias están en
      un `@click`.
    - `grep -cE 'disabled|opacity-' app/components/CounterBand.vue` devuelve 0.
    - `grep -cE 'setInterval|setTimeout|requestAnimationFrame|@touchmove|pointerdown' app/components/CounterBand.vue`
      devuelve 0.
    - `grep -cE 'v-html|<input' app/components/CounterBand.vue` devuelve 0.
    - `grep -c '~~/engine' app/components/CounterBand.vue` devuelve 0.
    - `grep -c 'min-w-11' app/components/CounterBand.vue` es al menos 1 (suelo táctil de 44px).
    - `grep -c 'sm:contents\|sm:' app/components/CounterBand.vue` es al menos 1 (partido en dos
      filas de D-05).
    - `grep -c 'Bajar vida de' app/components/CounterBand.vue` es al menos 1 y
      `grep -c 'Subir vida de' app/components/CounterBand.vue` es al menos 1.
    - `grep -ciE 'etapa|amenaza|aturdid|confundid|\bduro\b' app/components/CounterBand.vue`
      devuelve 0.
  </acceptance_criteria>
  <done>
    `CounterBand.vue` existe como componente tonto de altura fija 96px, con celdas según la
    plantilla ya aprobada, glifos siempre activos, feedback de pulsado separado de la acción y
    partido en dos filas por debajo de 640px.
  </done>
</task>

<task type="auto">
  <name>Task 2: Montar la banda en `app/pages/[game]/index.vue` justo bajo `AppHeader`</name>
  <files>app/pages/[game]/index.vue</files>
  <read_first>
    - `app/pages/[game]/index.vue` — el fichero completo o, si es necesario acotar, estos
      tramos: el bloque de imports y la desestructuración de `useGameSession()` (líneas ~1-80),
      el `watchDebounced` y el `useEventListener('pagehide', …)` (líneas ~150-185, **que NO se
      tocan**), y la plantilla del `<div v-else class="h-dvh flex flex-col">` con la pila
      `AppHeader` → `VoiceUnavailableNotice` → `StepScreen` → `NavBand` → overlays
      (líneas ~666-755, incluido el comentario D-U3 sobre el orden en el DOM de los overlays).
    - `app/composables/useGameSession.ts` — los nombres exactos que expone tras el plan 04:
      `showsCounterBand`, `counterCells`, `incrementCounter`, `decrementCounter`.
    - `app/components/CounterBand.vue` — las props y los emits reales tras el Task 1.
    - `app/components/VoiceUnavailableNotice.vue` — el aviso preexistente que vive entre
      `AppHeader` y `StepScreen` y que **no se modifica ni se mueve**.
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-CONTEXT.md` §D-03,
      §D-07, §D-19, §D-20 y §«Integration Points».
    - `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-RESEARCH.md`
      §Open Questions nº 1 — la pregunta de colocación que este task cierra.
  </read_first>
  <action>
    Dos cambios mínimos, ninguno de lógica.

    (1) Añadir `showsCounterBand`, `counterCells`, `incrementCounter` y `decrementCounter` a la
    desestructuración existente de `useGameSession()`, en el mismo bloque donde ya aparecen
    `showsSelectionGrid`, `playerSlots`, `setVillain`, `setHero` y `setPlayerName`. No añadir
    ningún import de componente: los componentes de `app/components/` son de auto-import en
    este proyecto (`AppHeader`, `NavBand` y el resto tampoco se importan).

    (2) En la plantilla, insertar `<CounterBand v-if="showsCounterBand" :cells="counterCells"
    @increment="incrementCounter" @decrement="decrementCounter" />` como **hermano inmediato
    siguiente de `</AppHeader>` y ANTES de `<VoiceUnavailableNotice>`**.
    Esta colocación cierra la pregunta abierta nº 1 de la investigación y hay que dejarla
    escrita como decisión en un comentario junto a la inserción: D-03 dice «justo bajo
    `AppHeader`», y el aviso de voz es un elemento condicional, de altura no fija y descartable
    que precede a las Fases 6/7 y que ni `07-CONTEXT.md` ni `07-UI-SPEC.md` mencionan; poner la
    banda delante mantiene su posición estable con independencia de si hay voz disponible —un
    estado que no tiene nada que ver con los contadores— y no obliga a mover ni reestilar el
    aviso. Citar en el comentario que el bloque de overlays `fixed inset-0 z-50` del final
    (`IndexOverlay`, `ConfirmDialog`, `WarningDetailModal`, `VillainPickerModal`, `PlayerModal`)
    sigue pintando por encima de la banda sin tocar ningún `z-index`, por la misma disciplina
    D-U3 de orden en el DOM que ese fichero ya documenta.

    Prohibiciones: no modificar `watchDebounced` ni el `useEventListener('pagehide', …)` — HP-08
    sale gratis porque los mutadores del plan 04 reasignan `session.value` (D-19/D-20), y
    añadir `{ deep: true }` o un `save()` extra sería trabajo redundante sobre un mecanismo que
    ya cubre el caso. No tocar `StepScreen` (la banda es su hermano, nunca su hijo). No añadir
    nada a `AppHeader` (D-08). No introducir la cadena `'ronda'`: la condición ya viene resuelta
    en `showsCounterBand`. No añadir ningún control de plegado ni ningún `z-index`.
  </action>
  <verify>
    <automated>npm run generate</automated>
  </verify>
  <acceptance_criteria>
    - `npm run generate` termina con código 0.
    - `grep -c '<CounterBand' 'app/pages/[game]/index.vue'` devuelve 1.
    - El orden en la plantilla es el correcto: `grep -n 'AppHeader\|CounterBand\|VoiceUnavailableNotice\|StepScreen\|NavBand' 'app/pages/[game]/index.vue'`
      muestra, dentro del `<template>`, `CounterBand` con número de línea MAYOR que el de
      `<AppHeader` y MENOR que el de `<VoiceUnavailableNotice`.
    - `grep -c 'showsCounterBand' 'app/pages/[game]/index.vue'` es al menos 2 (desestructuración
      y `v-if`).
    - `grep -c 'incrementCounter\|decrementCounter' 'app/pages/[game]/index.vue'` es al menos 4.
    - `git diff 'app/pages/[game]/index.vue' | grep -c 'watchDebounced\|pagehide'` devuelve 0
      (el mecanismo de persistencia no se ha tocado).
    - `git diff 'app/pages/[game]/index.vue' | grep -cE "'ronda'|z-index|z-\[|deep: true"` devuelve 0.
    - `git diff --name-only` NO incluye `app/components/StepScreen.vue`,
      `app/components/AppHeader.vue`, `app/components/NavBand.vue`,
      `app/components/VoiceUnavailableNotice.vue` ni `engine/persistence.ts`.
    - `grep -rc 'v-html' app/ | grep -v ':0'` no devuelve ninguna línea (la convención T-01-01
      sigue intacta en todo `app/`).
    - `npx vitest run` completo termina con código 0.
  </acceptance_criteria>
  <done>
    La banda se pinta durante el bucle de rondas como hermano inmediato de `AppHeader`, recibe
    las celdas resueltas y emite hacia los mutadores del composable, con la persistencia, el
    aviso de voz, `StepScreen`, `NavBand` y los overlays intactos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nombre de jugador (texto arbitrario de hasta 14 caracteres) → DOM de la etiqueta | La única entrada de texto libre que este componente renderiza. |
| `cells` procedente de `useGameSession` (y por debajo, de `localStorage`) → DOM | Los valores llegan ya normalizados y como cadenas; el componente no vuelve a interpretarlos. |
| Ninguna otra | App estática prerenderizada, sin backend, sin auth, sin red en ejecución. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Tampering / Elevation of Privilege (XSS) | Etiqueta de celda con el nombre del jugador | mitigate | Interpolación de Vue (`{{ cell.label }}`) exclusivamente, nunca `v-html`. Gates: `grep -cE 'v-html' app/components/CounterBand.vue` = 0 y `grep -rc 'v-html' app/` sin ninguna coincidencia, el mismo gate que la Fase 6 ya dejó verde. |
| T-07-18 | Denial of Service (denegación de los datos propios) | `watchDebounced`/`pagehide` de la página | mitigate | No se tocan. Modificarlos —añadir `{ deep: true }`, un `save()` extra o reordenar el guardado— es la única forma de que este plan pierda los contadores del grupo al recargar. Gate: el diff de `index.vue` no menciona `watchDebounced` ni `pagehide`. |
| T-07-19 | Denial of Service (usabilidad) | Un ▼ pegado al borde de SIGUIENTE | mitigate | La banda va arriba, bajo `AppHeader`, nunca sobre `NavBand` (D-03): mantiene toda flecha a 96px como mínimo de la zona pulsable de SIGUIENTE. El toque accidental es el error más caro de un contador según `PITFALLS.md` §13. Gate: orden de líneas en la plantilla. |
| T-07-20 | Tampering | Doble conteo fantasma por acción en `@touchstart` (Safari iOS) | mitigate | La acción va SOLO en `@click`; `@touchstart`/`@touchend` solo mueven `pressedKey`. Gate: ninguna línea con `@touchstart` contiene `emit(`, y hay exactamente 2 `emit(`, ambos en `@click`. |
| T-07-21 | Denial of Service (fuga de temporizador) | Repetición al mantener pulsado | mitigate | No existe: D-13 elimina la repetición de raíz, así que no hay `setInterval` que limpiar en `pointercancel`/`pointerleave`/desmontaje. Gate: `grep` de temporizadores en el componente = 0. |
| T-07-22 | Information Disclosure | — | accept | No aplica: sin PII, sin secretos, sin red. Los nombres son motes elegidos por el propio grupo en su propio dispositivo. |
| T-07-SC | Tampering (cadena de suministro) | Dependencias | accept | Este plan no instala ni actualiza ningún paquete: el componente usa solo `vue` y clases de Tailwind sobre tokens ya existentes en `main.css`. No se añade ninguna librería de iconos (los glifos `▼`/`▲` son Unicode, como el resto de símbolos de la app). Sin superficie de cadena de suministro que auditar. |
</threat_model>

<verification>
- `npm run generate` en verde (build SSG real, que es la salida de producción).
- `npx vitest run` completo en verde.
- `git diff --name-only` limitado a `app/components/CounterBand.vue` y
  `app/pages/[game]/index.vue`.
- `npm run e2e -- e2e/counter-band-height.spec.ts` sigue en verde: la spec del plan 01 no
  depende de la banda y su aserción de 40px para el texto del paso debe seguir cumpliéndose
  con la banda ya montada (la comprobación con la banda presente la añade el plan 06).
</verification>

<success_criteria>
- La banda existe, mide 96px fijos, no lleva `flex-1` y `StepScreen` sigue siendo el único
  `flex-1`.
- Se pinta solo durante el bucle de rondas, justo bajo `AppHeader`, sin plegado y sin nada
  nuevo en la cabecera.
- Un toque en ▼/▲ emite exactamente un cambio; no hay temporizadores, ni `disabled`, ni
  atenuado, ni destello, ni `<input>`.
- El componente no importa el motor y no usa `v-html`.
- La persistencia de la página no se ha tocado.
</success_criteria>

<output>
Create `.planning/phases/07-banda-de-contadores-y-compatibilidad-de-sesi-n/07-05-SUMMARY.md` when done
</output>
