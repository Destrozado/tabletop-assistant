---
phase: 09-hist-rico-y-estad-sticas
plan: 18
type: execute
wave: 12
depends_on: ["09-16", "09-17"]
gap_closure: true
files_modified:
  - app/composables/usePersistedSession.ts
  - app/composables/__tests__/usePersistedSession.test.ts
autonomous: true
requirements: [HIST-06, HIST-09]

must_haves:
  truths:
    - "SC3: existe un valor de retorno real que dice si `tga:progress:<gameId>` quedó escrito — `save()` deja de tragarse el booleano de `writeRaw()`, que es el único dato capaz de sostener cualquier afirmación de la interfaz sobre el progreso del grupo"
    - "VOZ-06/D-51 intacto: el autoguardado de la partida (`watchDebounced` y `pagehide` de `[game]/index.vue`) sigue sin romperse ante un `localStorage` que lanza — devolver un booleano no obliga a nadie a consumirlo"
    - "El comentario de `writeRaw` deja de afirmar que `appendHistoryEntry` es el ÚNICO llamador que mira el resultado: esa frase dejó de ser cierta en 09-16 y es exactamente lo que leería quien fuera a arreglar CR-01 (09-REVIEW.md líneas 455-469)"
  artifacts:
    - path: "app/composables/usePersistedSession.ts"
      provides: "save() con firma booleana que propaga el resultado real de la escritura del progreso"
      contains: "function save(session: EngineSession): boolean"
    - path: "app/composables/__tests__/usePersistedSession.test.ts"
      provides: "tests que fijan el contrato nuevo: true al escribir, false cuando setItem lanza, y sin lanzar en ninguno de los dos casos"
      contains: "save("
  key_links:
    - from: "save()"
      to: "writeRaw()"
      via: "return directo del booleano, sin descartarlo"
      pattern: "return writeRaw\\(storageKey\\(session\\.gameId\\)"
---

<objective>
Cerrar la **causa raíz** del BLOCKER CR-01 (ronda 4) de `09-VERIFICATION.md`: hoy no existe en
todo el código ni un solo valor que diga si el progreso de la partida llegó a escribirse.
`save()` (`app/composables/usePersistedSession.ts:281-284`) tiene firma `: void` y descarta a
propósito el booleano que `writeRaw()` le devuelve, porque una decisión anterior (VOZ-06/D-51)
fijó que un fallo de almacenamiento nunca puede romper `next()`/`prev()`/`toggle()`.

Esa decisión sigue siendo correcta, pero desde 09-16 la interfaz **hace una promesa por escrito
al grupo sobre ese mismo dato** («la partida… sigue guardada en el dispositivo»,
`HistorySavedNotice.vue:56`). Un dato del que la app hace una promesa no puede seguir
escribiéndose con un fallo silencioso. Este plan crea el valor de retorno que falta; los planes
09-20 y 09-21 son los que lo consumen.

**Alcance deliberadamente mínimo:** solo cambia la firma y el comentario que la documenta. NO se
toca `saveVoicePreference`, ni `writeRaw`, ni `removeRaw`, ni ningún llamador de `save()`. Este
plan no cambia el comportamiento observable de nada: un `save()` que devuelve un booleano que
nadie mira todavía se comporta exactamente igual que uno que no lo devuelve.

Purpose: dotar a la app del único dato con el que puede dejar de mentirle al grupo sobre sus
partidas.
Output: `save(): boolean` + comentario de `writeRaw` reescrito + dos tests de contrato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@app/composables/usePersistedSession.ts
</context>

<scope_boundary>
**DENTRO:** `app/composables/usePersistedSession.ts` (solo `save` y el comentario de `writeRaw`)
y su fichero de tests.

**FUERA (lo tocan otros planes de este lote, no este):**
- `app/composables/useHistorySavedNotice.ts` → plan 09-20
- `app/pages/[game]/index.vue` y `app/components/HistorySavedNotice.vue` → plan 09-21
- `app/components/GameOutcomeDialog.vue` → plan 09-19
- `deferred-items.md` / documentos de planificación → plan 09-23

**FUERA y no lo toca nadie de este lote:** los WARNING de defensa en profundidad ante un
`localStorage` manipulado (`Number.isFinite` vs `Number.isInteger`, cadena vacía que pasa la
validación de tipo, `new Date(now).toISOString()` sin guarda en `engine/history.ts:121`). El
verificador los deja explícitamente fuera de esta ronda. No los arregles aquí.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: `save()` devuelve si el progreso quedó escrito de verdad</name>
  <files>app/composables/usePersistedSession.ts</files>
  <read_first>
    - `app/composables/usePersistedSession.ts` — el fichero a modificar. Leer COMPLETOS los tres
      bloques siguientes antes de tocar nada:
      - `:185-209` — el comentario de `writeRaw` y la propia función. El comentario es el que
        hay que reescribir en la Task 2 de este plan; leerlo ahora para entender por qué
        `save()` ignora el booleano hoy.
      - `:262-295` — `load` / `save` / `clear` dentro de `usePersistedSession()`. `save` es la
        función a modificar.
      - `:307-320` — `saveVoicePreference`, que NO se toca: es el otro llamador de `writeRaw`
        amparado por VOZ-06/D-51 y debe quedar exactamente igual.
    - `app/pages/[game]/index.vue:168-195` — los DOS llamadores actuales de `save()`
      (el `watchDebounced` del autoguardado y el `useEventListener('pagehide', …)`). Ninguno se
      toca en este plan; leerlos para confirmar con los propios ojos que ninguno de los dos usa
      el valor de retorno, así que el cambio de firma no puede romperlos.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el bloque `gaps:` al
      completo (líneas 16-59), en especial la fila de `usePersistedSession.ts` de `artifacts:`
      y el primer punto de `missing:`.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md` — D-03 (si falla, se dice; no
      se inventa un éxito) y la frase «Esto NO contradice VOZ-06/D-51 … la interacción no se
      rompe, se informa».
  </read_first>
  <action>
    En `app/composables/usePersistedSession.ts`, cambiar la firma de `save` de
    `function save(session: EngineSession): void` a
    `function save(session: EngineSession): boolean`, y hacer que devuelva directamente el
    resultado de la llamada a `writeRaw(storageKey(session.gameId), JSON.stringify(persisted))`
    en vez de descartarlo. El cuerpo no cambia en nada más: sigue construyendo `persisted` con
    `toPersistedPosition(session)` antes de escribir, y sigue sin `try/catch` propio (el `try`
    vive dentro de `writeRaw`, que nunca lanza).

    Añadir justo encima de `save` un comentario que fije el contrato nuevo en estos términos
    exactos, sin ambigüedad para quien lo lea dentro de un año:

    - `true` significa «`window.localStorage.setItem` completó sin lanzar para
      `tga:progress:<gameId>` con el contenido de ESTA sesión». Es lo único que el retorno
      afirma: no afirma que el dato siga ahí más tarde, ni que sea legible después.
    - `false` significa «no se pudo escribir» — sin `window` (SSR/prerender), o `setItem`
      lanzando (modo privado del navegador, cuota agotada).
    - Devolver el booleano **no obliga a nadie a mirarlo**: los dos llamadores del autoguardado
      (`watchDebounced` y `pagehide` en `app/pages/[game]/index.vue`) lo siguen ignorando, y eso
      es exactamente lo que VOZ-06/D-51 exigen — un fallo de almacenamiento no puede romper
      `next()`/`prev()`. Lo que cambia es que ahora EXISTE el dato para quien sí lo necesite.
    - Citar el motivo: CR-01 ronda 4 de `09-VERIFICATION.md` — desde 09-16 la interfaz afirma
      por escrito al grupo algo sobre `tga:progress:<gameId>`, y ninguna afirmación de la
      interfaz sobre los datos del grupo puede carecer de un valor de retorno real que la
      respalde.

    NO cambiar `saveVoicePreference` (sigue devolviendo `void`: nadie promete nada al usuario
    sobre la preferencia de voz, y es un dato reconstruible pulsando el botón otra vez). NO
    cambiar `writeRaw`, `removeRaw`, `readRaw`, `load` ni `clear`. NO tocar
    `app/pages/[game]/index.vue` en este plan.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -8 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function save(session: EngineSession): boolean" app/composables/usePersistedSession.ts` devuelve exactamente 1.
    - `grep -c "function save(session: EngineSession): void" app/composables/usePersistedSession.ts` devuelve exactamente 0.
    - `grep -n "function save" -A 4 app/composables/usePersistedSession.ts | grep -c "return writeRaw(storageKey(session.gameId)"` devuelve exactamente 1.
    - `grep -c "function saveVoicePreference(enabled: boolean): void" app/composables/usePersistedSession.ts` devuelve exactamente 1 — el otro llamador amparado por VOZ-06/D-51 queda intacto.
    - `grep -v '^ *//' app/composables/usePersistedSession.ts | grep -c "function writeRaw(key: string, value: string): boolean"` devuelve exactamente 1 — `writeRaw` no ha cambiado de firma.
    - `npm run build` termina con código 0.
    - `npx vitest run` termina con código 0 — en particular el test existente «save()/saveVoicePreference() sobreviven a un localStorage que lanza al escribir (cuota llena) sin romper la interacción» (`app/composables/__tests__/usePersistedSession.test.ts:158`) sigue en verde sin tocarlo: devolver un booleano no es lanzar.
  </acceptance_criteria>
  <done>
    Existe en el código un valor que responde a «¿quedó escrito el progreso de esta partida?».
    Ningún comportamiento observable ha cambiado todavía: nadie lo consume aún.
  </done>
</task>

<task type="auto">
  <name>Task 2: el comentario de `writeRaw` deja de desautorizar el arreglo de CR-01</name>
  <files>app/composables/usePersistedSession.ts</files>
  <read_first>
    - `app/composables/usePersistedSession.ts:185-196` — el comentario a reescribir, palabra por
      palabra. La frase concreta que hay que corregir es «El ÚNICO llamador de este fichero que
      MIRA el resultado es `appendHistoryEntry`».
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` — el hallazgo de las líneas
      455-469 (el que registra este comentario como defecto aparte de CR-01, con el argumento
      de que «es lo que va a leer quien intente arreglar CR-01, y hoy le dice explícitamente que
      no toque esta firma»).
    - `app/pages/[game]/index.vue:168-195` — para poder enumerar con exactitud, en el comentario
      nuevo, qué llamadores siguen ignorando el booleano.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md` — D-03 y la referencia a
      VOZ-06/D-51, para no contradecir ninguna de las dos al reescribir.
  </read_first>
  <action>
    Reescribir el comentario de bloque que precede a `writeRaw` en
    `app/composables/usePersistedSession.ts:185-195`. El texto nuevo debe decir, sin dejar
    ninguna de las tres cosas fuera:

    1. **Qué devuelve `writeRaw`** — se conserva tal cual el párrafo actual sobre que se propaga
       el resultado del `try` interno y por qué NO se compara `readRaw` antes/después (falso
       negativo si el contenido coincidiera, más una lectura y un parseo de más). Esa parte del
       comentario sigue siendo cierta y útil.
    2. **Quién mira el resultado, ahora que son dos y no uno.** Sustituir la frase «El ÚNICO
       llamador de este fichero que MIRA el resultado es `appendHistoryEntry`» por una
       enumeración explícita de los DOS que lo miran y por qué:
       - `appendHistoryEntry` — el histórico es el único dato de la app que no se puede
         reconstruir, así que el grupo tiene que enterarse si el dispositivo no dejó escribir.
       - `save` — desde 09-16 el progreso dejó de ser un dato meramente reconstruible en el
         camino del registro fallido: pasó a ser la red de seguridad explícita del reintento, y
         la interfaz lo declara por escrito al usuario. Un dato del que la app hace una promesa
         no puede escribirse con un fallo silencioso (CR-01 ronda 4, `09-VERIFICATION.md`).
    3. **Qué sigue ignorándolo, y que eso es deliberado.** Dejar escrito que
       `saveVoicePreference` sigue con firma `void`, y que los dos llamadores del autoguardado
       de la partida (el `watchDebounced` y el `pagehide` de `app/pages/[game]/index.vue`)
       siguen llamando a `save()` sin mirar lo que devuelve: VOZ-06/D-51 exigen que un fallo de
       almacenamiento nunca rompa `next()`/`prev()`/`toggle()`, y eso sigue intacto. La regla en
       una frase: **devolver el booleano no obliga a consumirlo; lo que ya no se permite es
       AFIRMARLE algo al grupo sobre ese dato sin haberlo mirado.**

    Escribir el comentario en español, con el mismo registro y la misma densidad que los
    comentarios vecinos de este fichero (ver el de `RawRead`, `:148-161`, como calibración).
    No añadir ningún `TODO`/`FIXME`.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -8 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "El ÚNICO llamador de este fichero que MIRA el resultado" app/composables/usePersistedSession.ts` devuelve exactamente 0 — la frase desautorizadora ya no está.
    - `grep -n "function writeRaw" -B 25 app/composables/usePersistedSession.ts | grep -c "appendHistoryEntry"` devuelve al menos 1 y la misma ventana contiene también la cadena `save` — el comentario enumera los dos llamadores que miran el resultado.
    - `grep -n "function writeRaw" -B 25 app/composables/usePersistedSession.ts | grep -c "VOZ-06"` devuelve al menos 1 — el comentario sigue citando la decisión que ampara a los llamadores que lo ignoran.
    - `grep -n "function writeRaw" -B 25 app/composables/usePersistedSession.ts | grep -c "CR-01"` devuelve al menos 1 — el comentario cita el hallazgo que motiva el cambio.
    - `grep -cE "TODO|FIXME|XXX" app/composables/usePersistedSession.ts` devuelve 0.
    - `npx vitest run` y `npm run build` terminan ambos con código 0.
  </acceptance_criteria>
  <done>
    Quien abra este fichero para entender por qué `save()` devuelve un booleano lee la razón
    correcta y no encuentra ninguna frase que le diga que no toque esa firma.
  </done>
</task>

<task type="auto">
  <name>Task 3: tests de contrato de `save()` — true al escribir, false cuando el dispositivo no deja</name>
  <files>app/composables/__tests__/usePersistedSession.test.ts</files>
  <read_first>
    - `app/composables/__tests__/usePersistedSession.test.ts` — el fichero a modificar. Leer
      COMPLETOS: el helper `createFakeLocalStorage()` (`:12-23`), el helper `makeSession()`
      (`:25-34`), el `beforeEach`/`afterEach` del bloque «funciones con estado» (`:88-110`) y el
      test existente de la línea 158 («save()/saveVoicePreference() sobreviven a un
      localStorage que lanza al escribir…»), que es el vecino directo de los tests nuevos y NO
      se borra.
    - `app/composables/usePersistedSession.ts` — `save` con la firma nueva de la Task 1, y
      `storageKey` (buscarlo por nombre) para conocer el formato exacto de la clave del
      progreso.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el primer punto de
      `missing:` del gap, que es lo que estos tests fijan.
  </read_first>
  <action>
    Añadir al bloque `describe('usePersistedSession — funciones con estado …')` de
    `app/composables/__tests__/usePersistedSession.test.ts` dos tests nuevos, justo después del
    test existente de la línea 158 (no sustituirlo: ese sigue fijando que `save` no lanza, que
    es la garantía de VOZ-06/D-51, y los nuevos fijan qué DEVUELVE, que es una propiedad
    distinta):

    - **Test A — «save() devuelve true cuando el progreso queda escrito de verdad (CR-01
      ronda 4)»:** con el `createFakeLocalStorage()` normal, `save(makeSession('marvel-champions'))`
      devuelve exactamente `true`, y `fakeStorage.setItem` ha sido invocado con la clave del
      progreso de ese juego.
    - **Test B — «save() devuelve false cuando setItem lanza (modo privado/cuota) — CR-01 ronda
      4: es el único dato capaz de sostener lo que el aviso le dice al grupo»:** con
      `fakeStorage.setItem.mockImplementation(() => { throw new Error('QuotaExceededError') })`,
      `save(makeSession('marvel-champions'))` devuelve exactamente `false` **y** no lanza
      (aserción doble: `expect(() => …).not.toThrow()` y `expect(save(…)).toBe(false)`), porque
      las dos propiedades tienen que cumplirse a la vez — informar sin romper la interacción es
      literalmente lo que dice D-03 de `09-CONTEXT.md`.

    Nombrar ambos tests en español, citando «CR-01 ronda 4» en el título, igual que los tests
    existentes del fichero citan «CR-01 (ronda 3)» o «WR-03». Usar `toBe(true)`/`toBe(false)`,
    nunca `toBeTruthy`/`toBeFalsy`: el contrato es un booleano exacto.

    NO tocar ningún otro test del fichero. NO añadir tests sobre el aviso ni sobre
    `notifyHistorySaved` aquí: eso es el plan 09-20.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/usePersistedSession.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/composables/__tests__/usePersistedSession.test.ts` termina con código 0.
    - `grep -c "CR-01 ronda 4" app/composables/__tests__/usePersistedSession.test.ts` devuelve al menos 2 — los dos tests nuevos están identificados por el hallazgo que cierran.
    - `grep -c "toBe(false)" app/composables/__tests__/usePersistedSession.test.ts` aumenta respecto al valor previo al plan y el bloque del Test B contiene tanto `toBe(false)` como `not.toThrow()`.
    - `grep -c "sobreviven a un localStorage que lanza al escribir" app/composables/__tests__/usePersistedSession.test.ts` sigue devolviendo 1 — el test previo de VOZ-06/D-51 no se ha borrado ni reescrito.
    - Prueba de que el test B falla con el código anterior: revertir mentalmente `save` a `: void` haría que devolviera `undefined` y `expect(undefined).toBe(false)` fallaría — verificarlo ejecutando `npx vitest run app/composables/__tests__/usePersistedSession.test.ts` tras un cambio temporal de `return writeRaw(...)` a `writeRaw(...)`, y deshacer el cambio temporal inmediatamente después (el test DEBE fallar en ese estado; si pasa, el test no vale).
    - `npx vitest run` completo termina con código 0.
  </acceptance_criteria>
  <done>
    El contrato booleano de `save()` está fijado por test y no puede volver a perderse en
    silencio en una refactorización futura.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` termina con código 0 (los tres proyectos: `engine`, `app-logic`).
- `npm run build` termina con código 0.
- `grep -rn "function save(session" app/composables/usePersistedSession.ts` muestra la firma
  `: boolean` y ninguna otra.
- `grep -rn "save(" app/pages/\[game\]/index.vue` sigue mostrando exactamente las dos llamadas
  del autoguardado, sin consumir el retorno — este plan no toca esa página.
</verification>

<success_criteria>
Existe un valor de retorno real que responde «¿se escribió el progreso de esta partida?», está
documentado sin contradecir VOZ-06/D-51, está fijado por dos tests, y el comentario que antes
prohibía este cambio ya no lo prohíbe. Ningún comportamiento visible para el grupo ha cambiado
todavía.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-18-SUMMARY.md` al terminar.
</output>
