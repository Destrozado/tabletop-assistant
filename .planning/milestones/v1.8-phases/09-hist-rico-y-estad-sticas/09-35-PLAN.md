---
phase: 09-hist-rico-y-estad-sticas
plan: 35
type: execute
wave: 28
depends_on: ["09-34"]
gap_closure: true
files_modified:
  - app/composables/__tests__/afirmacionesRespaldadas.test.ts
autonomous: true
requirements: [HIST-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Gate S deja de auto-verificar solo la EXTRACCIÓN de texto y pasa a ejercer la función de DECISIÓN: hay casos sintéticos que demuestran que `frasesSinAuditarDe` SE PONE ROJA ante una frase sin auditar"
    - "La instancia real que la ronda 7 encontró sin detectar —la promesa de conservación del diálogo de fin de partida— está fijada como caso sintético permanente: el gate nuevo la atrapa y queda demostrado por test que el criterio anterior no la atrapaba"
    - "Una copy partida en dos líneas por un formateador queda demostrada como detectada, no supuesta"
    - "La decisión de Gate B también es una función pura con casos sintéticos que la ponen roja: un titular nuevo sin variante respaldada no puede volver a pasar desapercibido"
    - "Un `respaldo` que apunte a un fichero inexistente pone rojo el gate, demostrado con un caso sintético"
  artifacts:
    - path: "app/composables/__tests__/afirmacionesRespaldadas.test.ts"
      provides: "Gate S ejerciendo las funciones de decisión de Gate A y Gate B, con casos rojos sintéticos"
      contains: "frasesSinAuditarDe"
  key_links:
    - from: "Gate S"
      to: "frasesSinAuditarDe (la decisión de Gate A)"
      via: "casos sintéticos que exigen resultado NO vacío ante una raíz sin auditar"
      pattern: "frasesSinAuditarDe\\("
    - from: "Gate S"
      to: "la decisión de Gate B"
      via: "casos sintéticos con registros de copy inventados que exigen detección"
      pattern: "variantesSinRespaldoDe\\("
  prohibitions:
    - "Ningún caso de Gate S lee el árbol real para demostrar la detección: los casos rojos son cadenas sintéticas construidas en el propio test, para que el gate no dependa de que el defecto siga existiendo en el repo"
    - "No se afloja ninguna comprobación para que un caso sintético pase: si un caso no se pone rojo, el arreglo va en la función de decisión, nunca en el caso"
    - "No se toca ningún fichero de producción"
---

<objective>
La quinta vía del gap del gate de clase, en palabras de `09-VERIFICATION.md`: *«Gate S (la
auto-verificación que se supone impide que este hueco se reabra en silencio) prueba
`regionVigilada` (la extracción de texto) y dos coberturas puntuales sobre `index.vue`, pero no
invoca ninguna función exportada que ejecute la DECISIÓN de Gate A — esa lógica vive inline dentro
de un `it.each` — así que un cambio futuro que invierta el filtro de `frasesSinAuditar` o cambie
`?? []` por `?? FRASES_SOBRE_LOS_DATOS_DEL_GRUPO` dejaría Gate S en verde mientras Gate A deja de
detectar nada.»*

El plan 09-34 sacó esa decisión a `frasesSinAuditarDe(ruta, contenido)`. Este plan la **ejerce**:
Gate S deja de demostrar que el gate extrae bien el texto y pasa a demostrar que el gate **se pone
rojo** cuando debe. Y lo hace también para la decisión de Gate B, que es donde vivía el hueco (b).

El caso más importante no es hipotético: la frase exacta que la ronda 7 encontró sin detectar en
el árbol —la promesa de que la app conservaría el progreso para poder reintentar el registro— se
fija como caso sintético permanente, junto con la demostración, por test, de que el criterio
cerrado anterior **no** la habría atrapado. Esa pareja de aserciones es la prueba ejecutable de
que este cierre no es otra promesa escrita.

Purpose: que el hueco del gate no pueda reabrirse en silencio.
Output: Gate S ejerciendo las dos funciones de decisión, con casos rojos sintéticos.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-34-SUMMARY.md
</context>

<scope_boundary>
**DENTRO:** `app/composables/__tests__/afirmacionesRespaldadas.test.ts` y nada más.

**FUERA:**
- Ficheros de producción: si un caso sintético revelara un defecto real en el árbol, se anota en
  el SUMMARY y se abre como hallazgo; no se arregla desde aquí.
- El criterio, las raíces y las excepciones auditadas → los fijó el plan **09-34**. Este plan no
  los cambia; los ejerce. Si al ejercitarlos apareciera que la decisión no se pone roja donde
  debe, el arreglo SÍ entra aquí (es el punto del plan) y se anota en el SUMMARY como corrección
  de 09-34.
- `.planning/**` → es el plan **09-36**.
</scope_boundary>

<flagged_assumptions>
**Cobertura de bordes sin resolver (no se descarta en silencio).** Las 14 filas de la sonda de
bordes (HIST-01..HIST-09, STAT-01..STAT-05) volvieron `unclassified`/`unresolved`; ninguna se ha
convertido en truth de respaldo.

**Sin `## UI Considerations` que levantar.** `09-UI-SPEC.md` no contiene esa sección.

**Comprobación humana pendiente.** El `human_verification` de `09-VERIFICATION.md` (solape visual
de bandas en tablet horizontal real, DEV-02) sigue abierto y este plan no lo automatiza.
</flagged_assumptions>

<artifacts_this_phase_produces>
| Símbolo | Tipo | Dónde |
|---|---|---|
| `variantesSinRespaldoDe(titulares, cuerpos, respaldadas): string[]` | función pura exportada (decisión de Gate B) | `app/composables/__tests__/afirmacionesRespaldadas.test.ts` |
| `FRASE_NO_DETECTADA_EN_LA_RONDA_7` | constante con la frase real que evadió el gate | ídem |
| Casos de Gate S que exigen resultado ROJO | tests | ídem |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Gate S ejerce la decisión de Gate A y demuestra que se pone roja</name>
  <files>app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` — COMPLETO tal como lo dejó el
      plan 09-34: `RAICES_SOBRE_LOS_DATOS_DEL_GRUPO`, `VOCABULARIO_CERRADO_HASTA_LA_RONDA_7`,
      `normalizarEspacios`, `contieneRaizSobreLosDatosDelGrupo`, `frasesSinAuditarDe`,
      `AFIRMACIONES_AUDITADAS` y el `describe` de Gate S actual (los cinco casos de
      `regionVigilada` y las dos coberturas reales sobre `index.vue`).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — la vía (e) del gap del
      gate, literal.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-32-SUMMARY.md` — el texto exacto que tenía
      `endGameBody` antes del cierre, que esta tarea necesita como fixture.
  </read_first>
  <behavior>
    - `frasesSinAuditarDe('app/components/FicheroQueNoExiste.vue', sfc)` con un `sfc` sintético que
      contiene una raíz vigilada devuelve un array NO vacío que contiene esa raíz.
    - El mismo `sfc` sobre una ruta que SÍ tiene esa raíz auditada devuelve `[]`.
    - Un fichero auditado para una raíz NO queda exento de las demás: con un `sfc` que contiene la
      raíz auditada y otra distinta, el resultado contiene solo la segunda.
    - Con la frase real que evadió el gate en la ronda 7 dentro de un `sfc` sintético,
      `frasesSinAuditarDe` sobre una ruta sin auditar devuelve un array NO vacío.
    - Esa misma frase **no** contiene ninguna de las 11 subcadenas de
      `VOCABULARIO_CERRADO_HASTA_LA_RONDA_7`: la demostración de que el criterio anterior estaba
      verde sobre un defecto real.
    - Una raíz partida en dos líneas dentro del `sfc` sintético también se detecta.
    - Una raíz que solo aparece dentro de un comentario NO se detecta (los comentarios no son
      copy) — la garantía complementaria, para que el gate no se ponga rojo por narrativa.
  </behavior>
  <action>
    1. **Declarar la constante `FRASE_NO_DETECTADA_EN_LA_RONDA_7`** con el texto literal de la
       segunda oración que `endGameBody` tenía antes del plan 09-32 (tomarlo de
       `09-32-SUMMARY.md` o de `git show` sobre la versión anterior del fichero, no reescribirlo de
       memoria). Comentario: es la instancia REAL que `09-VERIFICATION.md` (ronda 7, vía (a))
       encontró sin detectar en el árbol, viviendo en el fichero más vigilado del repo. Está aquí
       como fixture permanente para que el gate no dependa de que el defecto siga existiendo.

    2. **Añadir a Gate S los casos de `<behavior>`**, todos con cadenas sintéticas construidas en
       el propio test —nunca leyendo el árbol— y cada uno con un comentario de una línea que diga
       qué propiedad del gate demuestra.

       El caso central, escrito con las dos aserciones juntas para que se lean como una sola
       afirmación:
       - `frasesSinAuditarDe(<ruta inventada>, <sfc con FRASE_NO_DETECTADA_EN_LA_RONDA_7>)`
         devuelve un array NO vacío → el gate nuevo la atrapa.
       - ninguna de las 11 subcadenas de `VOCABULARIO_CERRADO_HASTA_LA_RONDA_7` aparece en
         `FRASE_NO_DETECTADA_EN_LA_RONDA_7` → el gate anterior estaba verde sobre ella.
       Comentario: esta pareja es la prueba ejecutable de que el cambio de criterio cierra un
       defecto real, no una hipótesis.

    3. **El caso de la auditoría parcial** (un fichero auditado para una raíz no queda exento de
       las demás) tiene que usar una ruta que exista de verdad en `AFIRMACIONES_AUDITADAS` tal
       como la dejó el plan 09-34, y una raíz que NO esté auditada para ella. Si esa combinación
       no existiera en la lista final, construir el caso con un `Record` sintético y —para poder
       hacerlo sin leer el módulo— aceptar en `frasesSinAuditarDe` un tercer parámetro OPCIONAL con
       la tabla de auditoría, cuyo valor por defecto es `AFIRMACIONES_AUDITADAS`. Ese parámetro
       existe para que Gate S pueda ejercer la decisión con tablas sintéticas; documentarlo con esa
       frase exacta, y comprobar que Gate A lo sigue llamando sin él.

    4. **El caso de la raíz en comentario** cierra la simetría: el gate no puede ponerse rojo por
       la narrativa histórica que estos ficheros llevan encima (es lo que ya justifica que Gate C
       aplique `quitarComentarios` en el test WR-03).

    Todo el código y los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts</automated>
    <fails_when>salida distinta de 0, o la línea de resumen contiene «0 passed», o cualquier línea «FAIL»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "FRASE_NO_DETECTADA_EN_LA_RONDA_7" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 3 (declaración y los dos usos del caso central).
    - `grep -c "frasesSinAuditarDe(" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 6 (la llamada de Gate A más al menos cinco casos de Gate S).
    - Existe al menos una aserción que exige resultado NO vacío: `grep -cE "frasesSinAuditarDe\(.*\)\)\.(toContain|not\.toHaveLength\(0\)|toHaveLength)" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 3; pegar en el SUMMARY las líneas encontradas.
    - **Prueba de mutación, a ejecutar y registrar en el SUMMARY sin dejar el cambio en el repo:** invertir a mano el filtro de `frasesSinAuditarDe` (devolver las auditadas en vez de las no auditadas), ejecutar el gate y comprobar que **Gate S se pone ROJO** — es exactamente el escenario que `09-VERIFICATION.md` describe como indetectable hoy. Pegar el mensaje de error obtenido y revertir.
    - **Segunda prueba de mutación, mismo trato:** cambiar el valor por defecto de las raíces auditadas de «lista vacía» a «todas las raíces» y comprobar que Gate S se pone rojo. Pegar el mensaje y revertir.
    - `npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts` sale 0; registrar el recuento de tests antes y después.
    - `npm test` sale 0 y `npm run typecheck` sale 0.
  </acceptance_criteria>
  <done>
    Un cambio que desactivara la detección de Gate A pone rojo Gate S, y está demostrado por
    ejecución real con las dos mutaciones que la verificación nombra.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: la decisión de Gate B sale a una función pura y Gate S también la pone roja</name>
  <files>app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` — el `describe` de Gate B tal
      como lo dejó el plan 09-34 (ya recorriendo las entradas de `NOTICE_HEADING` y de
      `NOTICE_BODY`), y el `describe` de Gate S con los casos de la Task 1.
    - `app/composables/useHistorySavedNotice.ts` — `NOTICE_HEADING`, `NOTICE_BODY` y
      `VARIANTES_RESPALDADAS_POR_LA_AUTORIDAD` en el test, para saber qué forma tienen los
      argumentos.
  </read_first>
  <behavior>
    - `variantesSinRespaldoDe(NOTICE_HEADING, NOTICE_BODY, VARIANTES_RESPALDADAS_POR_LA_AUTORIDAD)`
      devuelve `[]` sobre los registros reales de la app.
    - Con un registro de titulares sintético que añade una variante inventada cuyo titular contiene
      una raíz vigilada, y esa variante fuera de la lista de respaldadas, devuelve un array que
      contiene esa variante.
    - Lo mismo con un registro de cuerpos sintético.
    - Un cuerpo `null` nunca produce detección.
    - Una variante cuyo titular y cuerpo no contienen ninguna raíz no produce detección aunque no
      esté en la lista de respaldadas — es el caso de la variante de éxito, y queda escrito.
  </behavior>
  <action>
    1. **Extraer la decisión de Gate B** a
       `export function variantesSinRespaldoDe(titulares: Record<string, string | null>, cuerpos: Record<string, string | null>, respaldadas: string[]): string[]`,
       que concatena las entradas de los dos registros (nunca el spread: las claves coinciden y se
       perderían los titulares — el plan 09-34 ya dejó escrito por qué), busca raíces vigiladas en
       cada texto no nulo y devuelve las variantes con hallazgo que no estén en `respaldadas`.
       Comentario: mismo motivo que `frasesSinAuditarDe` — mientras la decisión viva inline dentro
       de un `it`, Gate S no puede demostrar que se pone roja, y un cambio que la desactive pasaría
       en verde (vía (e) de `09-VERIFICATION.md`, aplicada también a Gate B).

    2. **Gate B llama a la función** y afirma `toEqual([])` sobre los registros reales, conservando
       los otros dos tests del `describe` (la biyección de las cuatro variantes de fallo con los
       cuatro valores de `StoredProgress`, y que el registro con éxito produce siempre la variante
       de éxito) sin tocarlos.

    3. **Casos de Gate S** para cada viñeta de `<behavior>`, con registros sintéticos construidos
       en el propio test. El caso de la variante de éxito lleva su comentario: su titular no
       contiene ninguna raíz vigilada, así que Gate B no le exige nada; la garantía de que nadie
       lo pinta sin haber pasado por `record()` la da Gate C al vigilar ese literal.

    Todo el código y los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts</automated>
    <fails_when>salida distinta de 0, o la línea de resumen contiene «0 passed», o cualquier línea «FAIL»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function variantesSinRespaldoDe" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 1.
    - `grep -c "variantesSinRespaldoDe(" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 5 (la llamada de Gate B más los casos de Gate S).
    - **Prueba de mutación, a ejecutar y registrar en el SUMMARY sin dejar el cambio:** hacer que `variantesSinRespaldoDe` mire solo los cuerpos (el hueco (b) original), ejecutar y comprobar que Gate S se pone ROJO por el caso del titular sintético. Pegar el mensaje y revertir.
    - `npm test` sale 0 y `npm run typecheck` sale 0.
  </acceptance_criteria>
  <done>
    El hueco (b) —un titular nuevo que ningún gate mira— no puede reabrirse sin que Gate S lo note.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: el gate de respaldo también se demuestra rojo, y Gate S declara qué vía cierra cada caso</name>
  <files>app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` — `respaldoExiste` y el test que
      recorre `AFIRMACIONES_AUDITADAS`, tal como los dejó el plan 09-34, y los casos de Gate S de
      las tareas 1 y 2.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — la enumeración (a)-(e)
      del gap del gate, que esta tarea usa como índice.
  </read_first>
  <behavior>
    - `respaldoExiste('app/composables/__tests__/useProgressMountPlan.test.ts')` es `true`.
    - `respaldoExiste('app/composables/__tests__/esteFicheroNoExiste.test.ts')` es `false`.
    - `respaldoExiste('')` es `false`.
    - Toda entrada de `AFIRMACIONES_AUDITADAS` tiene `motivo` de al menos 40 caracteres — un motivo
      de una palabra no es un motivo.
    - Ningún `motivo` de `AFIRMACIONES_AUDITADAS` delega la garantía sin nombrar en qué consiste:
      ninguno contiene la frase «la garantía real la da» sin nombrar además una función o un
      fichero.
  </behavior>
  <action>
    1. **Casos de Gate S para `respaldoExiste`**, uno por viñeta: una ruta real que existe, una
       inventada, y la cadena vacía. Con un comentario que diga qué protege: la ronda 7 encontró
       una excepción auditada cuyo motivo escrito afirmaba lo contrario de lo verificado; un
       `respaldo` que no resuelve es la forma comprobable de ese mismo fallo.

    2. **Un test de calidad mínima de los motivos:** longitud mínima (40 caracteres) y la
       prohibición de la circularidad literal — si un motivo contiene la expresión que la ronda 7
       señaló como circular, tiene que nombrar además un identificador de función o una ruta de
       fichero (comprobarlo exigiendo que el motivo contenga, además, alguna de las dos cosas:
       una subcadena con `.ts`/`.vue`, o un identificador en `camelCase` de al menos 8
       caracteres). Es un listón bajo a propósito: no pretende juzgar la calidad de la prosa, solo
       impedir la forma exacta de motivo vacío que ya selló un hueco.

    3. **Un comentario de cierre en la cabecera de Gate S** que enumere, con una línea por vía, qué
       caso concreto cierra cada uno de los cinco huecos (a)-(e) de `09-VERIFICATION.md` — y, para
       (a), que diga explícitamente que el caso usa la instancia real encontrada en el árbol, no
       una inventada. Este comentario es la única forma de que la ronda siguiente pueda comprobar
       de un vistazo si el gate cubre lo que dice cubrir, en vez de tener que reconstruirlo.

    4. Comprobar y registrar en el SUMMARY el recuento final de tests del fichero y la lista de
       `describe` que contiene.

    Todo el código y los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts && npm test</automated>
    <fails_when>salida distinta de 0, o la línea de resumen contiene «0 passed», o cualquier línea «FAIL»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "respaldoExiste(" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 4.
    - `grep -c "motivo.length" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 1.
    - La cabecera de Gate S enumera las cinco vías: `grep -cE "\(a\)|\(b\)|\(c\)|\(d\)|\(e\)" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 5.
    - **Prueba de mutación, a ejecutar y registrar en el SUMMARY sin dejar el cambio:** cambiar el `respaldo` de una entrada auditada por una ruta inventada, ejecutar y comprobar que el gate se pone ROJO nombrando esa ruta. Pegar el mensaje y revertir.
    - `npm test` sale 0 y `npm run typecheck` sale 0.
    - El SUMMARY incluye el recuento final de tests del fichero y la lista de sus `describe`.
  </acceptance_criteria>
  <done>
    Las cinco vías que `09-VERIFICATION.md` enumera tienen, cada una, un caso de Gate S que la
    cierra, y las tres mutaciones ejecutadas dejan por escrito que el gate se pone rojo donde debe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate → futuro cambio del propio gate | Gate S es el único control sobre el control: si la decisión de Gate A/B se desactiva, nada más lo notaría |
| Excepción auditada → gate | Un `respaldo` inventado desactiva el control para un par (fichero, raíz) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-35-01 | Tampering | Un cambio futuro que invierta el filtro de `frasesSinAuditarDe` dejando Gate A ciego y Gate S verde | high | mitigate | Casos sintéticos que exigen resultado NO vacío + dos pruebas de mutación ejecutadas y registradas |
| T-09-35-02 | Tampering | Un cambio futuro que devuelva Gate B a mirar solo los cuerpos | high | mitigate | Decisión de Gate B extraída a función pura con caso sintético de titular + prueba de mutación registrada |
| T-09-35-03 | Repudiation | Un `respaldo` inventado o un motivo vacío pasando el gate | medium | mitigate | Casos de `respaldoExiste` + listón mínimo de motivo + prueba de mutación registrada |
| T-09-35-04 | Denial of service | Gate S poniéndose rojo por la narrativa histórica de los comentarios en vez de por copy real | low | mitigate | Caso explícito de raíz dentro de comentario que NO debe detectarse |
| T-09-35-SC | Tampering | Instalaciones de paquetes (npm) | low | accept | Este plan no instala ni actualiza ninguna dependencia: `git diff -- package.json package-lock.json` debe quedar vacío |
</threat_model>

<verification>
- `npm test` en verde y `npm run typecheck` en verde.
- `git diff --stat` muestra un único fichero modificado.
- `git diff -- package.json package-lock.json` vacío.
- Las cuatro pruebas de mutación de este plan (dos en la Task 1, una en la Task 2, una en la Task
  3) están registradas en el SUMMARY con su mensaje de error real, y ninguna ha quedado en el
  repo.
</verification>

<success_criteria>
- Gate S ejerce las funciones de DECISIÓN de Gate A y Gate B, no solo la extracción de texto.
- La instancia real que evadió el gate en la ronda 7 es un caso sintético permanente, con la
  demostración de que el criterio anterior estaba verde sobre ella.
- Cuatro mutaciones deliberadas ponen el gate rojo, y los mensajes están registrados.
- La cabecera de Gate S dice qué vía cierra cada caso.
</success_criteria>

<output>
Create `.planning/phases/09-hist-rico-y-estad-sticas/09-35-SUMMARY.md` when done
</output>
