---
phase: 09-hist-rico-y-estad-sticas
plan: 30
type: execute
wave: 23
depends_on: ["09-28", "09-29"]
gap_closure: true
files_modified:
  - app/composables/__tests__/afirmacionesRespaldadas.test.ts
  - app/pages/[game]/index.vue
autonomous: true
requirements: [HIST-06, HIST-09]

must_haves:
  truths:
    - "El gate de clase inspecciona TODO el fichero —plantilla completa, incluidos los `<template>` anidados, y `<script setup>`— menos comentarios y `<style>`, en vez del 1,9% del fichero donde han vivido cinco de las siete caras del defecto"
    - "El gate también barre `app/**/*.ts`, así que la copy que vive fuera de `NOTICE_BODY` deja de ser invisible"
    - "Existe un test que comprueba que el propio gate detecta una frase inyectada dentro de un `<template>` anidado: este hueco no puede reabrirse en silencio"
    - "Ninguna afirmación de la app sobre los datos del grupo pasa el gate sin estar o respaldada por la autoridad o auditada a mano CON SU MOTIVO ESCRITO junto a la frase concreta que se permite"
    - "La frase que la app le enseña al grupo antes de terminar una partida describe lo que la app hace de verdad en las dos ramas de `planGameEnd`, no solo en una"
    - "El gate no se puede evadir escribiendo el literal con comillas dobles o backticks, ni pasar en verde por no encontrar ficheros que mirar"
  artifacts:
    - path: "app/composables/__tests__/afirmacionesRespaldadas.test.ts"
      provides: "el gate de clase con región vigilada completa, barrido de .ts, lista de frases ampliada, excepciones por frase con motivo, y auto-verificación"
      contains: "regionVigilada"
    - path: "app/pages/[game]/index.vue"
      provides: "endGameBody honesto en las dos ramas de planGameEnd"
      contains: "endGameBody"
  key_links:
    - from: "app/composables/__tests__/afirmacionesRespaldadas.test.ts (regionVigilada)"
      to: "app/pages/[game]/index.vue (<script setup> y los `<template>` anidados)"
      via: "barrido del fichero completo menos comentarios y <style>"
      pattern: "regionVigilada"
    - from: "app/composables/__tests__/afirmacionesRespaldadas.test.ts (auto-verificación)"
      to: "el propio detector del gate"
      via: "SFC sintético con <template> anidado y frase inyectada después del primer cierre"
      pattern: "template #fallback"
---

<objective>
Cerrar el **Gap #2** de la ronda 6: el gate de clase que el plan 09-26 introdujo explícitamente
para «proteger la CLASE, no el caso concreto» inspecciona **761 de ~7.090+ caracteres** de
plantilla de `app/pages/[game]/index.vue` (1,9%), porque `extraerTemplate`
(`afirmacionesRespaldadas.test.ts:83-86`) usa un cuantificador perezoso que corta en el **primer**
`</template>` del fichero — el del `<template #fallback>` de `ClientOnly`. Deja fuera
`ResumePrompt`, `ConfirmDialog`, `ContentChangedNotice` y `GameOutcomeDialog`, que es exactamente
donde han vivido cinco de las siete caras del defecto en toda la fase. Y ninguno de los tres gates
mira `<script setup>`, así que `endGameBody` —un ejemplar vivo, abierto desde la ronda 4 como
IN-03/WR-04— sigue invisible por partida doble.

Este es **el hallazgo que explica mecánicamente por qué el Gap #1 no se detectó solo**. Sin él,
la octava cara tampoco se detectaría a tiempo por el mecanismo que existe para evitarlo.

El plan corre **después** de 09-28 y 09-29 a propósito: solo con el cuarto valor, la cuarta
variante y la copy nueva del montaje ya en el árbol se puede demostrar —midiendo, no deduciendo—
qué ve ahora el gate que antes no veía, y escribir las excepciones auditadas sobre frases que
existen de verdad.

Purpose: que la garantía de SC3 deje de depender de que alguien repita el barrido a mano cada
ronda.
Output: un gate que barre el fichero entero menos comentarios y `<style>`, incluye `app/**/*.ts`,
vigila la lista de frases ampliada, se autocomprueba, y un `endGameBody` que dice la verdad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-28-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-29-SUMMARY.md
</context>

<scope_boundary>
**DENTRO:** los dos ficheros de `files_modified`.

**FUERA:**
- Cualquier otro fichero de `app/` o `engine/`. Si el gate ampliado pone en rojo un fichero que no
  es `app/pages/[game]/index.vue`, la respuesta de este plan es **auditarlo con su motivo escrito**
  (Task 3), no reescribirlo — con **una única excepción nombrada**: `endGameBody`, que es un defecto
  conocido desde la ronda 4 y se arregla en la Task 2 precisamente para no auditar una frase falsa.
  Si apareciera un ejemplar NUEVO y genuinamente falso en otro fichero, se registra como hallazgo en
  el SUMMARY y se le abre entrada en `deferred-items.md` (plan 09-31) con evaluación de riesgo;
  nunca se audita en silencio.
- El texto de `discardBody` **salvo** que la medición de la Task 3 demuestre que es falso en alguna
  rama alcanzable. `onDiscardConfirm` llama a `clear(gameId)` incondicionalmente, así que la frase
  es cierta; entra en el mapa de auditadas con ese motivo escrito.
- `.planning/**` → es el plan **09-31**, incluida la actualización del contrato de copy de
  `09-UI-SPEC.md` y de la fila de auditoría de `GameOutcomeDialog.vue`.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: la región vigilada pasa a ser el fichero entero menos comentarios y `&lt;style&gt;`, y el gate se autocomprueba</name>
  <files>app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` — COMPLETO. Su cabecera (qué dice
      que protege y por qué), `quitarComentariosHtml`, `extraerTemplate`, `rutaRelativa`, y los tres
      `describe` (Gate A, Gate B, Gate C) con sus `import.meta.glob`. Importa entender por qué se
      usa `import.meta.glob` y no `node:fs` (este árbol pasa por `npm run typecheck` y no tiene
      `@types/node`).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §CR-03 COMPLETO — la tabla de
      cobertura medida por fichero, la demostración de la inyección que el gate no vio, y el fix
      propuesto (`quitarComentarios` + `regionVigilada`).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — Gap #2 completo, con sus
      tres puntos de `missing:`. El tercero (auto-verificación del gate) es obligatorio.
    - `app/pages/[game]/index.vue` — solo para localizar el `<template #fallback>` de `ClientOnly`
      (~735-740) y confirmar con los ojos dónde cortaba la regex vieja.
  </read_first>
  <action>
    1. **Sustituir `extraerTemplate` por dos funciones exportadas** (se exportan para que la
       auto-verificación pueda llamarlas directamente, no porque nadie fuera del fichero las use):

       - `export function quitarComentarios(sfc: string): string` — elimina comentarios HTML
         (`<!-- … -->`), comentarios de bloque JS (`/* … */`) y comentarios de línea JS
         (`^\s*//.*$`, con flags `gm`).
       - `export function regionVigilada(sfc: string): string` — aplica `quitarComentarios` y
         después elimina los bloques `<style … > … </style>`.

       Borrar `extraerTemplate` y `quitarComentariosHtml`: dejarlas vivas invita a que alguien las
       vuelva a usar. Si `quitarComentariosHtml` se conserva como paso interno, que sea privada y
       sin `export`.

    2. **Gate A pasa a usar `regionVigilada(contenido)`** en vez de `extraerTemplate(contenido)`, y
       su mensaje de error deja de decir «en su `<template>`» para decir «en su plantilla o en su
       `<script setup>`». Cambiar también el título del `describe` y del `it.each`.

    3. **Reescribir la cabecera del fichero** para que describa el gate que hay, no el que había:
       nombrar la ronda 6, el Gap #2, que la regex perezosa cubría el 1,9% del fichero de mayor
       riesgo, y la regla nueva: *no se intenta acotar el bloque; se barre el fichero entero menos
       lo que no es copy (comentarios y `<style>`). Acotar el bloque es lo que falló; es más barato
       barrer de más y auditar las excepciones una a una que confiar en un delimitador.*

    4. **Añadir un cuarto `describe`: «Gate S — auto-verificación del propio gate (09-30)»**, con
       estos tests, todos sobre cadenas sintéticas construidas en el propio test (nunca leyendo el
       árbol) salvo donde se indique:

       - Una SFC sintética con un `<template>` raíz que contiene un `<template #fallback>` anidado y
         **una frase de `FRASES_SOBRE_LOS_DATOS_DEL_GRUPO` colocada DESPUÉS del cierre de ese
         `<template>` anidado**: `regionVigilada(sfc)` debe contenerla. Añadir en el mismo test la
         aserción de contraste que demuestra el defecto que se cierra: la regex vieja
         (`/<template[^>]*>([\s\S]*?)<\/template>/`, escrita en línea dentro del test con un
         comentario que diga «esta es la que fallaba») **no** la captura.
       - Una SFC sintética cuya única aparición de la frase está dentro de un comentario HTML:
         `regionVigilada` **no** debe contenerla (los comentarios no son copy: si vigiláramos los
         comentarios, este mismo fichero y media fase se pondrían rojos).
       - Igual con un comentario de línea JS (`// … sigue guardada …`) dentro de un `<script setup>`
         sintético.
       - Igual con una frase dentro de un bloque `<style>`.
       - Una SFC sintética con la frase dentro de `<script setup>` (fuera de comentario):
         `regionVigilada` **sí** debe contenerla.
       - **Dos aserciones de cobertura sobre el árbol real**, que son las que fijan que el gate mira
         de verdad el fichero de mayor riesgo: `regionVigilada` de `app/pages/[game]/index.vue`
         contiene la cadena `'GameOutcomeDialog'` (solo existe DESPUÉS del primer `</template>`) y
         la cadena `'endGameBody'` (solo existe en `<script setup>`). Obtener el contenido por el
         mismo `import.meta.glob` que usa Gate A, no por una ruta tecleada.

    5. **IN-03 de la ronda 5**: el segundo test de Gate C no tiene la guarda de no-vacío que sí
       tiene el primero. Extraer la guarda (`expect(ficherosVigilados().length).toBeGreaterThan(0)`)
       a un `beforeAll` del `describe` de Gate C, o repetirla en el segundo test. Que ningún test de
       este fichero pueda pasar por no encontrar nada que mirar.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "extraerTemplate" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 0 fuera de comentarios: comprobar con `grep -v "^\s*//" app/composables/__tests__/afirmacionesRespaldadas.test.ts | grep -c "extraerTemplate"` → 0.
    - `grep -v "^\s*//" app/composables/__tests__/afirmacionesRespaldadas.test.ts | grep -c "export function regionVigilada"` devuelve 1.
    - `npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts` sale 0 e incluye un `describe` llamado «Gate S» con **al menos 6 tests**.
    - **Medición obligatoria, con las cifras pegadas en el SUMMARY:** con un `node -e` (o un test descartable) calcular, sobre el árbol actual, `longitud de regionVigilada(index.vue)` frente a `longitud de la captura de la regex vieja`. La primera debe ser **más de 10 veces** la segunda. Citar los dos números exactos; la ronda 6 midió 761 frente a ~7.090+.
    - `npm run typecheck` sale 0.
    - `npx vitest run` completo sale 0 **sin haber tocado todavía la lista de frases** (la Task 1 amplía la región, no el vocabulario: si algo se pone rojo aquí, es un hallazgo real y hay que nombrarlo en el SUMMARY antes de seguir).
  </acceptance_criteria>
  <done>
    El gate mira todo el fichero, no el 1,9%, y hay un test que se pone rojo si alguien vuelve a
    acotar la región con un delimitador.
  </done>
</task>

<task type="auto">
  <name>Task 2: `endGameBody` deja de afirmar un borrado que no siempre ocurre</name>
  <files>app/pages/[game]/index.vue</files>
  <read_first>
    - `app/pages/[game]/index.vue` — `savedSummary`, `discardBody` y `endGameBody` (~455-472), el
      uso de `endGameBody` como prop `warningBody` del `<GameOutcomeDialog>` en la plantilla,
      `onOutcomeRecorded` tal como lo dejó 09-28 y `finishGame(preserveProgress = false)` (~546-563).
    - `app/composables/useHistorySavedNotice.ts` — `planGameEnd` y su comentario (b): «
      `preserveProgress` NO depende de `stored` a propósito… solo mira `historyRecorded`». Es la
      fuente de verdad de cuándo se borra y cuándo no.
    - `app/components/GameOutcomeDialog.vue` — las cuatro salidas del diálogo (tres de registro +
      «Salir sin registrar») y qué prop pinta el texto. `onOutcomeDismiss` llama a `finishGame()`
      sin argumento, así que en esa salida el progreso **sí** se borra siempre.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-04 (ronda 5) — el diagnóstico
      exacto de por qué esta frase es falsa y por qué ningún gate la veía.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` — «Copywriting Contract», fila
      «Outcome dialog retained warning (D-02, unchanged text, only relocated)». Esta tarea **cambia**
      ese texto; el contrato se actualiza en el plan 09-31 y hay que decirlo en el SUMMARY para que
      ese plan lo recoja.
  </read_first>
  <action>
    1. **Sustituir el texto de `endGameBody`** por uno que sea cierto en las **cuatro** salidas del
       diálogo, usando el mismo `savedSummary` interpolado. Texto EXACTO:

       `El progreso guardado de esta partida (${savedSummary.value}) se borrará y volveréis a la pantalla de inicio. Si el registro en el histórico falla, la app conservará el progreso para que podáis reintentarlo.`

       Comprobación de veracidad, rama por rama, que hay que dejar escrita como comentario encima
       del `computed` y repetida en el SUMMARY:
       - «Salir sin registrar» → `onOutcomeDismiss` → `finishGame()` sin argumento →
         `preserveProgress` es `false` → se borra. **Primera frase cierta.**
       - Registro con éxito → `planGameEnd(true, …).preserveProgress === false` → se borra.
         **Primera frase cierta.**
       - Registro fallido (cualquiera de los cuatro estados del dispositivo) →
         `planGameEnd(false, …).preserveProgress === true` → NO se borra. **Segunda frase cierta,
         y es exactamente la que faltaba.**

       Se retira «Esta acción no se puede deshacer»: era la parte que convertía una descripción en
       una promesa absoluta, y además el registro del histórico sí se puede deshacer desde
       `/historico` (HIST-08). Dejarlo escrito en el comentario para que nadie lo restaure por
       parecer que falta.

    2. **`discardBody` no se toca.** Verificar —leyendo `onDiscardConfirm`— que llama a
       `clear(gameId)` de forma incondicional, así que su «Se borrará el progreso guardado de la
       partida en curso» es cierto. Dejar la verificación escrita en el SUMMARY: es lo que permite
       auditarlo en la Task 3 con un motivo real en vez de con una conjetura.

    3. No tocar `GameOutcomeDialog.vue`: el texto llega por la prop `warningBody` y el componente
       sigue siendo tonto.
  </action>
  <verify>
    <automated>npx vitest run && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Esta acción no se puede deshacer" "app/pages/[game]/index.vue"` devuelve 1 — queda solo la de `discardBody`. Registrar en el SUMMARY el valor previo (2).
    - `grep -c "Si el registro en el histórico falla, la app conservará el progreso" "app/pages/[game]/index.vue"` devuelve 1.
    - `grep -c "y volveréis a la pantalla de inicio" "app/pages/[game]/index.vue"` devuelve 1.
    - `npm run build` sale 0 y `npx vitest run` sale 0 (si algún test fijaba el texto anterior de `endGameBody`, actualizarlo y nombrarlo en el SUMMARY).
    - El SUMMARY contiene la tabla de veracidad rama por rama (cuatro salidas del diálogo → `preserveProgress` → qué frase la cubre).
  </acceptance_criteria>
  <done>
    La única afirmación conocida y falsa que quedaba abierta desde la ronda 4 describe ahora lo que
    la app hace en las dos ramas de `planGameEnd`.
  </done>
</task>

<task type="auto">
  <name>Task 3: vocabulario ampliado, excepciones auditadas por frase con motivo, y Gate C sin vías de evasión</name>
  <files>app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` tal como lo deja la Task 1 —
      `FRASES_SOBRE_LOS_DATOS_DEL_GRUPO`, `FICHEROS_CON_AFIRMACION_AUDITADA` (hoy vacía a propósito),
      `VARIANTES_RESPALDADAS_POR_LA_AUTORIDAD`, `FICHEROS_QUE_PUEDEN_NOMBRAR_EL_ESTADO_DEL_DISPOSITIVO`
      y los dos tests de Gate C.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-03 (los literales de
      `NoticeVariant` que el gate dice perseguir y no persigue), §WR-04 (la lista de frases
      propuesta y los tres ficheros que entran con ella) e §IN-05 (la evasión por comillas dobles /
      backticks y la ausencia de lint en CI).
    - `app/components/ResumePrompt.vue` y `app/components/ContentChangedNotice.vue` — las frases
      concretas que van a saltar y bajo qué condición se montan cada uno, para poder escribir el
      motivo de auditoría con un hecho verificable, no con una intuición.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` §2 — las filas de
      `ResumePrompt.vue` y `ContentChangedNotice.vue`, ya auditadas como RESPALDADA en la ronda 4
      con el valor de retorno que las sostiene. Los motivos que se escriban en el gate deben coincidir
      con lo que ese documento ya dice, no contradecirlo.
    - `app/composables/useHistorySavedNotice.ts` y `app/composables/useProgressMountPlan.ts` tal
      como los dejaron 09-28 y 09-29 — son los dos ficheros `.ts` que contienen copy a propósito.
  </read_first>
  <action>
    1. **MEDIR ANTES DE TOCAR NADA.** Ampliar primero solo el alcance y el vocabulario, ejecutar el
       gate, y **anotar la lista exacta de ficheros y frases que se ponen rojos**. Esa lista es la
       evidencia de que el gate ahora ve lo que antes no veía, y va pegada en el SUMMARY. Solo
       después se escriben las excepciones. (Ejecutar el gate con la lista ampliada y con
       `FICHEROS_CON_AFIRMACION_AUDITADA` todavía vacía para obtenerla.)

    2. **Ampliar el alcance de Gate A a `app/**/*.ts`**: añadir un segundo `import.meta.glob`
       (`'/app/**/*.ts'`) y unir ambos mapas, excluyendo `/__tests__/` con el mismo criterio que
       Gate C ya usa. El barrido pasa a llamarse «copy de la app», no «templates».

    3. **Ampliar `FRASES_SOBRE_LOS_DATOS_DEL_GRUPO`** con las cinco de WR-04:
       `'se borrará'`, `'progreso guardado'`, `'partida guardada'`, `'ya no está'`,
       `'no encontraréis'`. La comparación pasa a ser **insensible a mayúsculas**: comparar
       `region.toLowerCase().includes(frase.toLowerCase())`, para que «Partida guardada» (el `<h1>`
       de `ResumePrompt`) no se escape por la mayúscula.

    4. **Sustituir `FICHEROS_CON_AFIRMACION_AUDITADA: string[]` por un mapa por frase**:
       `const AFIRMACIONES_AUDITADAS: Record<string, string[]>` — ruta relativa → array de frases
       permitidas EN ESE fichero. Un fichero auditado deja de ser una puerta abierta: cualquier
       frase vigilada que no esté en su array sigue poniendo el gate rojo. Cada entrada lleva un
       comentario con su motivo, escrito como un hecho comprobable:

       - `app/components/ResumePrompt.vue` → `['partida guardada', 'progreso guardado']` — solo se
         monta cuando `planProgressMount` devuelve `action: 'resume-prompt'`, es decir cuando la
         autoridad ha leído el dispositivo y `resume()` ha resuelto una posición; el «borrará el
         progreso guardado» describe lo que `onDiscardConfirm` hace incondicionalmente.
       - `app/components/ContentChangedNotice.vue` → `['partida guardada']` — solo se monta con
         `outcome: 'content-changed'`, que `resume()` produce únicamente tras leer una posición
         válida.
       - `app/pages/[game]/index.vue` → `['se borrará', 'progreso guardado']` — `discardBody`
         (cierto: `onDiscardConfirm` llama a `clear()` sin condición) y `endGameBody` (reescrito en
         la Task 2 de este plan para ser cierto en las dos ramas de `planGameEnd`; citar la Task 2).
       - `app/composables/useHistorySavedNotice.ts` → las frases que su `NOTICE_BODY` contenga tras
         09-28 — motivo: es **la** casa de la copy respaldada, y Gate B ya la audita entrada por
         entrada contra las variantes que la autoridad puede producir. Auditarla aquí sin ese
         matiz sería circular; el comentario debe decir explícitamente que la garantía real de este
         fichero la da Gate B, no esta excepción.
       - `app/composables/useProgressMountPlan.ts` → `['partida guardada']` (o las que la medición
         del punto 1 arroje) — motivo: `UNVERIFIED_PROGRESS_NOTICE` solo se emite cuando
         `stored === 'unknown'`, y afirma exactamente eso (que no se ha podido comprobar), con un
         test puro que lo fija (`useProgressMountPlan.test.ts`).

       Si la medición del punto 1 arroja algún fichero o frase **no previsto en esta lista**, NO se
       añade sin más: se trata como hallazgo, se nombra en el SUMMARY y se decide a la vista del
       código si es legítimo (entra con motivo) o es un ejemplar nuevo del defecto (entrada en
       `deferred-items.md` vía el plan 09-31, con evaluación de riesgo).

    5. **Gate C — cerrar las tres vías de evasión:**
       - Añadir `'stale'` a `LITERALES` si 09-28 no lo añadió ya (comprobarlo, no asumirlo).
       - **WR-03:** añadir un tercer test a Gate C que vigile los literales de variante
         (`'failure-recoverable'`, `'failure-stale'`, `'failure-unrecoverable'`, `'failure-unknown'`)
         con la misma lista de ficheros permitidos —`useHistorySavedNotice.ts` los define— para que
         nadie pueda llamar a `notifyHistorySaved('failure-recoverable')` a mano sin pasar por
         `planGameEnd`. El mensaje de error debe citar por qué: es el gesto que el propio comentario
         de `useHistorySavedNotice.ts` dice que el gate persigue y hasta ahora no perseguía.
       - **IN-05:** normalizar las comillas antes de comparar en los tres tests de Gate C —
         sustituir en el contenido, antes de buscar, tanto la comilla doble como la comilla
         invertida por comilla simple— para que `stored === "absent"` y la versión con
         backticks no evadan el gate. Comentar por qué: no hay paso de lint en CI, así que nada del
         pipeline obliga a la comilla simple.

    6. **Actualizar la cabecera del fichero** con el recuento nuevo de gates (A, B, C, S) y una
       línea por cada uno.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts && npx vitest run && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^\s*//" app/composables/__tests__/afirmacionesRespaldadas.test.ts | grep -c "FICHEROS_CON_AFIRMACION_AUDITADA"` devuelve 0 — la lista plana ya no existe.
    - `grep -v "^\s*//" app/composables/__tests__/afirmacionesRespaldadas.test.ts | grep -c "AFIRMACIONES_AUDITADAS"` devuelve al menos 2 (declaración + uso).
    - `grep -c "'no encontraréis'" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 1 y `grep -c "'progreso guardado'" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 1 — la lista ampliada de WR-04 está puesta.
    - `grep -c "toLowerCase()" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 2 — la comparación es insensible a mayúsculas.
    - El `describe` de Gate A contiene dos llamadas a `import.meta.glob`, una de `.vue` y otra de `.ts`: comprobar con `grep -c "import.meta.glob" app/composables/__tests__/afirmacionesRespaldadas.test.ts`, que debe devolver al menos 4 (2 en Gate A + 2 en Gate C).
    - `grep -c "'failure-stale'" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 1 (Gate C vigila los literales de variante).
    - **Demostración obligatoria, con la salida pegada en el SUMMARY:** inyectar temporalmente la frase `Vuestra partida sigue guardada en el dispositivo` dentro del bloque de `<ResumePrompt>` en `app/pages/[game]/index.vue` (la misma región que la ronda 5 usó para demostrar el fallo) y confirmar que **el gate se pone ROJO** nombrando ese fichero y esa frase. Revertir la inyección. Repetir la inyección una segunda vez dentro de `<script setup>` (una constante con la frase) y confirmar que también se pone rojo. Sin estas dos comprobaciones el Gap #2 no puede darse por cerrado.
    - `npx vitest run` completo sale 0 con el árbol ya revertido, y `npm run typecheck` sale 0.
    - El SUMMARY contiene: (a) la lista medida en el punto 1 de ficheros/frases detectados al ampliar el vocabulario, (b) el motivo escrito de cada entrada de `AFIRMACIONES_AUDITADAS`, y (c) las dos salidas de la demostración de inyección.
  </acceptance_criteria>
  <done>
    Una frase nueva sobre los datos del grupo no puede entrar en `app/` —ni en una plantilla, ni en
    un `<template>` anidado, ni en un `<script setup>`, ni en un `.ts`— sin estar respaldada por la
    autoridad o auditada a mano con su motivo escrito junto a la frase concreta.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Código nuevo → copy visible | Toda frase que llegue a la pantalla es una afirmación sobre los datos del grupo. El gate es la frontera automática entre «alguien escribió una frase» y «esa frase está respaldada». |
| Autoridad de lectura → resto de `app/` | Solo tres ficheros pueden nombrar el estado del dispositivo. Cualquier otro que escriba `'resumable'`/`'stale'`/`'absent'`/`'unknown'` o una `NoticeVariant` se lo está inventando. |
| `planGameEnd` → lo que la app promete borrar | La app afirma un borrado antes de saber si va a ocurrir; la rama de fallo lo conserva. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-30-01 | Repudiation (el control existe pero no controla) | `extraerTemplate` cubriendo el 1,9% de `app/pages/[game]/index.vue` | mitigate | Task 1: `regionVigilada` barre el fichero entero menos comentarios y `<style>`. Verificación medible: la región vigilada de `index.vue` crece más de 10× y contiene `GameOutcomeDialog` y `endGameBody`, fijado por test. |
| T-09-30-02 | Repudiation (el control se rompe en silencio) | El propio gate, si alguien vuelve a acotar la región o el glob deja de resolver | mitigate | Task 1: «Gate S» comprueba el detector con SFC sintéticos (frase tras un `</template>` anidado, en `<script setup>`, en comentario, en `<style>`); Task 1 punto 5 añade la guarda de no-vacío a los tres tests de Gate C. |
| T-09-30-03 | Spoofing (afirmación falsa a la cara del grupo) | `endGameBody`, falso en la rama `preserveProgress: true` | mitigate | Task 2 reescribe la frase cubriendo las cuatro salidas del diálogo, con la tabla de veracidad rama por rama en el SUMMARY. Se arregla en vez de auditarse: auditar una frase conocida y falsa sería blanquearla. |
| T-09-30-04 | Elevation of privilege (saltarse la autoridad) | `notifyHistorySaved('failure-recoverable')` escrito a mano en cualquier fichero nuevo, sin leer el dispositivo | mitigate | Task 3, WR-03: Gate C vigila los cuatro literales de `NoticeVariant` fuera de `useHistorySavedNotice.ts`. |
| T-09-30-05 | Tampering (evasión del control) | Literales escritos con comillas dobles o backticks; CI sin paso de lint que lo impida | mitigate | Task 3, IN-05: normalización de comillas antes de comparar en los tres tests de Gate C. El paso de lint en CI queda fuera de este plan y no se necesita para cerrar la vía. |
| T-09-30-06 | Repudiation (excepción que se convierte en puerta abierta) | `FICHEROS_CON_AFIRMACION_AUDITADA` como lista plana de ficheros | mitigate | Task 3: se sustituye por `AFIRMACIONES_AUDITADAS`, mapa ruta → frases permitidas, con motivo escrito por entrada. Auditar un fichero deja de eximirlo de las frases futuras. |
| T-09-30-SC | Tampering (cadena de suministro) | `npm` / `package.json` | n/a | Este plan **no instala ningún paquete**. `git diff --stat` no debe incluir `package.json` ni `package-lock.json`. |
</threat_model>

<verification>
- `npx vitest run` sale 0, `npm run typecheck` sale 0, `npm run build` sale 0.
- Las dos inyecciones de la Task 3 (en el bloque de `<ResumePrompt>` y en `<script setup>`) ponen el
  gate en ROJO, con la salida pegada en el SUMMARY y el árbol revertido después.
- `git diff --stat` toca exclusivamente `app/composables/__tests__/afirmacionesRespaldadas.test.ts`
  y `app/pages/[game]/index.vue`.
- El SUMMARY deja escrito, para que el plan 09-31 lo recoja: que el texto de `endGameBody` ha
  cambiado y que la fila «Outcome dialog retained warning (D-02)» del «Copywriting Contract» de
  `09-UI-SPEC.md` queda pendiente de actualizar, y que la regla «excluido por acción futura» del §1
  de `09-AUDIT-AFIRMACIONES-UI.md` —la que dejó pasar `endGameBody`— necesita revisión.
</verification>

<success_criteria>
El gate de clase inspecciona el fichero entero de cada `.vue` y de cada `.ts` de `app/` menos
comentarios y `<style>`; detecta, demostrado por inyección real, una frase colocada tras un
`</template>` anidado y otra escrita en `<script setup>`; tiene un test que se pone rojo si alguien
vuelve a acotar la región; sus excepciones son por frase y con motivo escrito; no se puede evadir
con otro tipo de comillas ni pasar en vacío; y el único ejemplar vivo y conocido del defecto
(`endGameBody`) está arreglado, no auditado.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-30-SUMMARY.md` al terminar.
</output>
