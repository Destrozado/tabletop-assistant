---
phase: 09-hist-rico-y-estad-sticas
plan: 06
type: execute
wave: 4
depends_on: ["09-05"]
files_modified:
  - app/components/HistoryEntryCard.vue
  - app/pages/historico.vue
  - app/pages/estadisticas.vue
autonomous: true
requirements: [HIST-07, HIST-08, STAT-02, STAT-03, STAT-04, STAT-05]

must_haves:
  truths:
    - "HIST-07: /historico lista las partidas registradas de la más reciente a la más antigua"
    - "D-19: cada partida es una tarjeta con todo visible; nada escondido detrás de un toque salvo «Borrar»"
    - "HIST-08/D-20: «Borrar» abre el ConfirmDialog que ya existe con destructive: true y el cuerpo citando la partida concreta; cancelar no borra nada"
    - "D-16: son dos rutas separadas a pantalla completa, /historico y /estadisticas, cada una con su «Atrás»"
    - "D-18: las dos pantallas se enlazan entre sí además del «Atrás» al inicio"
    - "D-22 y su espejo: cada pantalla tiene un estado vacío propio, con encabezado y explicación"
    - "STAT-05: con el histórico vacío no se pinta ninguna cabecera de tabla ni ningún porcentaje"
    - "D-23/D-25: solo filas de quien ha jugado, sin umbral mínimo, con el recuento al mismo peso visual que el porcentaje"
    - "STAT-04/D-14: la única fuente de datos de las dos pantallas es useGameHistory, que solo lee localStorage"
  artifacts:
    - path: "app/components/HistoryEntryCard.vue"
      provides: "tarjeta tonta de una entrada ya formateada"
      contains: "Borrar"
    - path: "app/pages/historico.vue"
      provides: "ruta /historico con lista, estado vacío y borrado confirmado"
    - path: "app/pages/estadisticas.vue"
      provides: "ruta /estadisticas con las dos tablas, línea de muestra y estado vacío"
  key_links:
    - from: "app/pages/historico.vue"
      to: "app/composables/useGameHistory.ts"
      via: "useGameHistory()"
      pattern: "useGameHistory"
    - from: "app/pages/estadisticas.vue"
      to: "app/composables/useGameHistory.ts"
      via: "statisticsView"
      pattern: "statisticsView"
---

<objective>
Las dos pantallas nuevas de la fase: `/historico` (lista de partidas con borrado confirmado y
estado vacío) y `/estadisticas` (las dos tablas de % de victorias, línea de muestra y estado
vacío), más la tarjeta tonta que pinta una entrada.

Propósito: HIST-07, HIST-08, STAT-02, STAT-03 y STAT-05 se ven aquí por primera vez. Todo lo
que se pinta llega ya formateado del plan 09-05: estas tres piezas no calculan nada, no
ordenan nada y no importan nada de `~~/engine/*`.

Salida: `HistoryEntryCard.vue`, `app/pages/historico.vue` y `app/pages/estadisticas.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-PATTERNS.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-05-SUMMARY.md

<interfaces>
Del plan 09-05, en `app/composables/useGameHistory.ts`:

- `useGameHistory()` devuelve `{ entries, cardViews, statisticsView, isEmpty, reload, remove,
  record }`. `reload()` debe llamarse desde `onMounted` (no hay `localStorage` en prerender).
- `HistoryCardView { id; resultLabel; dateLabel; causeLabel: string | null; contextLine;
  playerLines: string[] | null; noSelectionLine: string | null; roundAndDurationLine;
  deleteAriaLabel; confirmTitle; confirmBody }`.
- `StatisticsView { heroRows: StatRowView[]; villainRows: StatRowView[]; sampleCaption: string
  | null; isEmpty: boolean }` con `StatRowView { id; name; valueLabel }`.

Del repo:
- `app/components/ConfirmDialog.vue` — props `{ title, body, confirmLabel, cancelLabel,
  destructive }`, emits `confirm`/`cancel`. Se reutiliza TAL CUAL, sin ningún cambio.
- `app/components/MesaListaScreen.vue` — precedente de cabecera propia:
  `div.h-dvh.flex.flex-col` > `header.h-16.shrink-0.bg-surface.flex.items-center.justify-between.px-lg.gap-md`
  > `main.flex-1.bg-background...overflow-y-auto`.
- `app/pages/index.vue` — precedente de página fina: composable + `navigateTo` en el script,
  un solo componente en la plantilla.
- `app/components/IndexOverlay.vue` — rótulos de bloque
  `h2.text-label.font-bold.uppercase.text-secondary-text` (mismo tratamiento que piden las
  cabeceras de sección de estadísticas).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HistoryEntryCard.vue</name>
  <files>app/components/HistoryEntryCard.vue</files>
  <read_first>
    - `app/components/CounterBand.vue` (patrón de fila de lista tonta con props ya resueltas)
    - `app/components/MesaListaScreen.vue` (uso de la escala tipográfica fija)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §4 (anatomía de la tarjeta)
    - `app/assets/css/main.css` (escala fija: display 40 / heading 28 / body 20 / label 18 y
      los tokens de espaciado — no se añade ninguno)
  </read_first>
  <action>
    Crear `app/components/HistoryEntryCard.vue`, componente TONTO: recibe una entrada YA
    formateada y no calcula nada (ni fechas, ni duraciones, ni etiquetas de jugador).

    Props, con los mismos nombres que `HistoryCardView` menos el `id`: `resultLabel: string`,
    `dateLabel: string`, `causeLabel: string | null`, `contextLine: string`,
    `playerLines: string[] | null`, `noSelectionLine: string | null`,
    `roundAndDurationLine: string`, `deleteAriaLabel: string`.
    Emits: `delete: []`.

    Estructura y clases (UI-SPEC §4):
    - Raíz `bg-surface p-lg flex flex-col gap-sm`.
    - Fila 1 `flex justify-between items-baseline`: `resultLabel` en
      `text-label font-bold text-primary-text` (NUNCA codificado por color — regla dura de
      UI-SPEC §Color: ni verde, ni rojo, ni acento) y `dateLabel` en
      `text-label font-normal text-secondary-text`.
    - `causeLabel` con `v-if`: `text-body font-normal text-secondary-text`. Ausente del DOM en
      una victoria, sin línea en blanco reservada.
    - `contextLine`: `text-body font-normal text-secondary-text`.
    - `playerLines` con `v-for` (`:key` por índice): cada línea en
      `text-body font-normal text-primary-text`.
    - `noSelectionLine` con `v-if`: `text-body font-normal text-secondary-text`.
    - `roundAndDurationLine`: `text-body font-normal text-secondary-text`.
    - Control `Borrar`: `button` alineado a la derecha (`self-end`), texto plano sin borde ni
      relleno, `text-label font-bold text-destructive`, `min-h-12 px-md`, con
      `:aria-label="deleteAriaLabel"` y `active:brightness-95`. Emite `delete`.

    Sin `v-html` en ninguna parte; toda la interpolación con `{{ }}`. Sin ningún tamaño de
    fuente, color ni token de espaciado nuevo. Sin import de `~~/engine/*` ni de ningún
    composable.
  </action>
  <verify>
    <automated>npx nuxt build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-label" app/components/HistoryEntryCard.vue` devuelve 1 (solo el de «Borrar»)
    - `grep -c "text-destructive" app/components/HistoryEntryCard.vue` devuelve 1
    - `grep -Ec "v-html|innerHTML" app/components/HistoryEntryCard.vue` devuelve 0
    - `grep -Ec "~~/engine|useGameHistory|localStorage|Date\(" app/components/HistoryEntryCard.vue` devuelve 0
    - `grep -Ec "text-display|bg-accent|bg-destructive" app/components/HistoryEntryCard.vue` devuelve 0
    - `npx nuxt build` termina con código 0
  </acceptance_criteria>
  <done>La tarjeta muestra resultado, fecha, causa, contexto, jugadores, ronda y duración, y un control «Borrar» que solo emite.</done>
</task>

<task type="auto">
  <name>Task 2: app/pages/historico.vue</name>
  <files>app/pages/historico.vue</files>
  <read_first>
    - `app/pages/index.vue` (página fina: composable + `navigateTo`)
    - `app/components/MesaListaScreen.vue` (cabecera propia `h-16` y cuerpo scrollable)
    - `app/pages/[game]/index.vue` (patrón de `onMounted` para todo lo que dependa de
      `localStorage`, y de cableado de `ConfirmDialog` con `v-if` + `@confirm`/`@cancel`)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §3, §4, §5 y
      §Copywriting Contract
  </read_first>
  <action>
    Crear `app/pages/historico.vue` (ruta `/historico`, D-16).

    Script: `useGameHistory()` para `cardViews`, `isEmpty`, `reload` y `remove`; un
    `ref` `cargado` puesto a `true` dentro de `onMounted` justo después de llamar a `reload()`
    — el prerender no tiene `localStorage`, así que hasta ese momento el cuerpo no pinta ni la
    lista ni el estado vacío (mismo criterio que `resumeResolved` en
    `app/pages/[game]/index.vue`, que evita afirmar «no hay nada» antes de haber mirado).
    Un `ref` `pendienteDeBorrar` con la `HistoryCardView` que abrió el diálogo, o `null`.
    Manejadores: abrir el diálogo, cancelar (poner a `null`, sin borrar), y confirmar (llamar
    a `remove(id)` y poner a `null`). Navegación con `navigateTo('/')` y
    `navigateTo('/estadisticas')` en el script, nunca dentro de un componente tonto.

    Plantilla (UI-SPEC §3):
    - Raíz `div.h-dvh.flex.flex-col`.
    - Cabecera propia `header.h-16.shrink-0.bg-surface.flex.items-center.justify-between.px-lg.gap-md`
      con tres zonas: a la izquierda un botón de texto `‹ Atrás`
      (`min-h-12 px-md text-label font-bold text-primary-text active:brightness-95`) que navega
      a `/`; en el centro el título `HISTÓRICO` en `text-heading font-bold text-primary-text`;
      a la derecha el enlace cruzado `Estadísticas ›` en
      `min-h-12 px-md text-label font-bold text-accent` que navega a `/estadisticas` (D-18:
      navegación de pantalla completa, NUNCA un salto filtrado por héroe o villano — eso no
      tiene requisito y está explícitamente fuera de alcance).
    - Cuerpo `main.flex-1.overflow-y-auto.bg-background.px-2xl.py-lg`.
      - Con entradas: `div.flex.flex-col.gap-md` con un `HistoryEntryCard` por elemento de
        `cardViews` (`:key` por `id`), pasando cada campo de la vista por prop y escuchando
        `@delete` para abrir el diálogo de borrado.
      - Vacío (D-22): bloque centrado
        `flex flex-col items-center justify-center text-center gap-md h-full` con encabezado
        `Todavía no hay partidas registradas` (`text-heading font-bold text-primary-text`) y
        cuerpo `Al terminar una partida y pulsar «Partida terminada», la app pregunta cómo
        acabó — así es como se rellena esta lista.` (`text-body font-normal
        text-secondary-text`). Sin botón, sin ilustración.
    - `ConfirmDialog` con `v-if="pendienteDeBorrar"` (D-20, componente reutilizado SIN
      cambios): `:title` y `:body` de `confirmTitle`/`confirmBody` de la vista,
      `confirm-label="Sí, borrar"`, `cancel-label="Cancelar"`, `:destructive="true"`.

    Sin `v-html`. Sin import de `~~/engine/*`. Sin ningún tamaño ni color nuevo.
  </action>
  <verify>
    <automated>npx nuxt build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Todavía no hay partidas registradas" app/pages/historico.vue` devuelve 1
    - `grep -c "navigateTo('/estadisticas')" app/pages/historico.vue` devuelve 1 y `grep -c "navigateTo('/')" app/pages/historico.vue` devuelve 1
    - `grep -c "ConfirmDialog" app/pages/historico.vue` devuelve al menos 1 y `grep -c ":destructive=\"true\"" app/pages/historico.vue` devuelve 1
    - `grep -c "onMounted" app/pages/historico.vue` devuelve 1 y `grep -c "reload()" app/pages/historico.vue` devuelve 1
    - `grep -Ec "~~/engine|v-html|aggregateStatistics|\.sort\(" app/pages/historico.vue` devuelve 0
    - `git diff --stat app/components/ConfirmDialog.vue` está vacío (el componente reutilizado no se toca)
    - `npx nuxt build` termina con código 0
  </acceptance_criteria>
  <done>`/historico` lista las partidas de la más reciente a la más antigua, permite borrar una tras confirmar y tiene su estado vacío.</done>
</task>

<task type="auto">
  <name>Task 3: app/pages/estadisticas.vue</name>
  <files>app/pages/estadisticas.vue</files>
  <read_first>
    - `app/pages/historico.vue` (recién creada: la cabecera de tres zonas y el patrón
      `cargado`/`onMounted` se replican tal cual)
    - `app/components/IndexOverlay.vue` (rótulo de bloque
      `h2.text-label.font-bold.uppercase.text-secondary-text`)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §3, §6, §7
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md` D-23, D-24, D-25
  </read_first>
  <action>
    Crear `app/pages/estadisticas.vue` (ruta `/estadisticas`, D-16), con la MISMA cabecera de
    tres zonas que `historico.vue`: `‹ Atrás` a `/`, título `ESTADÍSTICAS`, y el enlace cruzado
    `Histórico ›` (acento) a `/historico`.

    Script: `useGameHistory()` para `statisticsView` y `reload`; el mismo `ref` `cargado`
    puesto a `true` en `onMounted` tras `reload()`.

    Cuerpo `main.flex-1.overflow-y-auto.bg-background.px-2xl.py-lg`:
    - Estado vacío (`statisticsView.isEmpty`, STAT-05): SOLO el bloque centrado, con
      encabezado `Todavía no hay estadísticas` y cuerpo `En cuanto registréis vuestra primera
      partida en el histórico, aquí aparecerá el % de victorias por héroe y por villano.`
      Nunca se pinta la cabecera de una tabla sin filas debajo, y nunca aparece un `0 %` en
      esta pantalla.
    - Con datos:
      - `sampleCaption` con `v-if` (D-12): una línea
        `text-label font-bold text-secondary-text` encima de las dos tablas.
      - Dos secciones, `% DE VICTORIAS POR HÉROE` y `% DE VICTORIAS POR VILLANO`, cada una con
        cabecera `h2` en `text-label font-bold uppercase text-secondary-text border-b
        border-background pb-xs` y debajo un `v-for` sobre `heroRows` / `villainRows`.
      - Cada fila es un `div`, NUNCA un `button` (D-09 de la Fase 8 / UI-SPEC §6: sin
        afordancia falsa — nada de chevron, de estado de pulsación ni de `aria-label`):
        `flex justify-between items-baseline min-h-12 px-md`, con `name` a la izquierda
        (`flex-1 truncate`) y `valueLabel` a la derecha, AMBOS en
        `text-body font-normal text-primary-text` — mismo tamaño, mismo peso y mismo color,
        que es la defensa entera de D-25 contra un porcentaje engañoso.
      - Una sección cuyo array esté vacío no se pinta (por ejemplo, hay partidas registradas
        pero ninguna con villano anotado): tampoco entonces se enseña una cabecera sin filas.
    - El componente no ordena, no filtra y no calcula porcentajes: las filas llegan ya
      ordenadas por la cascada de D-24 desde el motor.

    Sin `v-html`, sin import de `~~/engine/*`, sin tokens nuevos.
  </action>
  <verify>
    <automated>npx nuxt build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Todavía no hay estadísticas" app/pages/estadisticas.vue` devuelve 1
    - `grep -c "% DE VICTORIAS POR HÉROE" app/pages/estadisticas.vue` devuelve 1 y `grep -c "% DE VICTORIAS POR VILLANO" app/pages/estadisticas.vue` devuelve 1
    - `grep -c "navigateTo('/historico')" app/pages/estadisticas.vue` devuelve 1
    - `grep -Ec "<button" app/pages/estadisticas.vue` devuelve 2 (solo «Atrás» y el enlace cruzado; ninguna fila de tabla es un botón)
    - `grep -Ec "~~/engine|\.sort\(|Math.round" app/pages/estadisticas.vue` devuelve 0
    - `npx nuxt build` termina con código 0
  </acceptance_criteria>
  <done>`/estadisticas` muestra el % de victorias por héroe y por villano con el recuento al mismo peso, la línea de muestra cuando procede, y un estado vacío que no pinta ninguna tabla.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entradas de `localStorage` → DOM | Nombres de jugador y nombres congelados de héroe/villano se pintan en pantalla; el único texto libre es el nombre de jugador que el propio grupo teclea |
| Página → almacenamiento | El borrado es la única escritura de estas pantallas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-18 | Tampering (XSS) | `HistoryEntryCard` pintando `playerLines` | mitigate | Interpolación con `{{ }}` siempre, `v-html` nunca (T-01-01); gate de grep en los criterios de aceptación de las tres tareas |
| T-09-19 | Destrucción de datos | Borrado accidental de una entrada del histórico | mitigate | D-20: «Borrar» nunca borra en su propio toque, abre el `ConfirmDialog` existente con `destructive: true` y cuerpo citando la partida concreta; cancelar deja la entrada intacta |
| T-09-20 | Information disclosure | Una pantalla leyendo de otra fuente que no sea `localStorage` | mitigate | Las dos páginas solo consumen `useGameHistory()`; grep de `~~/engine`, `fetch` y `firestore` en los criterios de aceptación (criterio de éxito nº 5 del ROADMAP) |
</threat_model>

<verification>
- `npx nuxt build` termina con código 0.
- `npm test` sigue en verde.
- `grep -rEi "firestore|firebase|fetch\(" app/pages/historico.vue app/pages/estadisticas.vue app/components/HistoryEntryCard.vue`
  no devuelve ninguna línea.
- `git diff --stat app/components/ConfirmDialog.vue` está vacío (D-20: se reutiliza sin tocarlo).
</verification>

<success_criteria>
- `/historico` lista, ordena y permite borrar con confirmación; su estado vacío explica cómo
  se rellena la lista.
- `/estadisticas` muestra las dos tablas ordenadas y su estado vacío; no existe ningún camino
  que pinte una tabla de cero filas ni un porcentaje sin su recuento al lado.
- Las dos pantallas se enlazan entre sí y vuelven al inicio.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-06-SUMMARY.md` al terminar.
</output>
