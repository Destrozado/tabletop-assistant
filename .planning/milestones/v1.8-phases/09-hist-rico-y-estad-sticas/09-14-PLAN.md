---
phase: 09-hist-rico-y-estad-sticas
plan: 14
type: execute
wave: 9
depends_on: ["09-01", "09-05", "09-12"]
gap_closure: true
files_modified:
  - app/composables/useGameHistory.ts
  - engine/history.ts
  - app/composables/useGameContent.ts
  - app/composables/useCharacterCatalogue.ts
  - engine/__tests__/history.test.ts
  - app/composables/__tests__/useGameHistory.test.ts
  - app/composables/__tests__/useGameContent.test.ts
autonomous: true
requirements: [HIST-04]

must_haves:
  truths:
    - "SC2: un `heroId` que coincide con una propiedad de `Object.prototype` (`constructor`, `toString`, `valueOf`, `hasOwnProperty`, `__proto__`, `isPrototypeOf`, `propertyIsEnumerable`, `toLocaleString`) produce `heroName: null`, nunca un valor de tipo `function` — la cadena de prototipos no participa en la búsqueda del nombre congelado"
    - "SC2: esa partida SE REGISTRA: `record()` devuelve `true` y `loadHistory()` la recupera, en vez de rechazarse con un aviso de fallo de almacenamiento falso"
    - "`buildHistoryEntry` nunca escribe en `heroName`/`villainName` nada que no sea `string` o `null` — el predicado de escritura `isHistoryPlayerEntry`/`isGameHistoryEntry` ya no puede rechazar una entrada que el propio motor acaba de construir"
    - "El camino feliz no se degrada: un `heroId` real presente en el mapa sigue produciendo su alias español congelado (D-11), y un `heroId` ausente del catálogo sigue produciendo `null`"
    - "Una ruta `/constructor` (o cualquier otra clave de `Object.prototype`) muestra la pantalla «No encontramos ese juego» en vez de quedarse en «Cargando…» para siempre: `getGame`/`getCatalogue` dejan de resolver por la cadena de prototipos"
    - "Ninguna regresión: los cierres de las rondas 1 y 2 siguen en verde y la suite completa (26 ficheros / 681 tests de línea base) sigue pasando"
  artifacts:
    - path: "app/composables/useGameHistory.ts"
      provides: "resolveFrozenNames con un mapa de nombres congelados SIN prototipo"
      contains: "Object.create(null)"
    - path: "engine/history.ts"
      provides: "buildHistoryEntry que lee el nombre congelado con Object.hasOwn y exige string, y que exige string en villainName"
      contains: "Object.hasOwn"
    - path: "app/composables/useGameContent.ts"
      provides: "getGame que no resuelve por la cadena de prototipos"
      contains: "Object.hasOwn(gamesById"
    - path: "app/composables/useCharacterCatalogue.ts"
      provides: "getCatalogue que no resuelve por la cadena de prototipos"
      contains: "Object.hasOwn(cataloguesById"
    - path: "app/composables/__tests__/useGameContent.test.ts"
      provides: "test parametrizado sobre las ocho claves de Object.prototype contra getGame y getCatalogue"
      contains: "constructor"
  key_links:
    - from: "resolveFrozenNames (useGameHistory.ts)"
      to: "buildHistoryEntry (engine/history.ts)"
      via: "FrozenNames.heroNames construido sin prototipo Y leído con Object.hasOwn — las dos puntas, no una"
      pattern: "Object\\.(create\\(null\\)|hasOwn)"
    - from: "buildHistoryEntry"
      to: "isHistoryPlayerEntry (usePersistedSession.ts)"
      via: "heroName siempre string|null, así que la frontera de escritura ya no puede rechazar lo que el motor produce"
      pattern: "typeof .* === 'string'"
    - from: "app/pages/[game]/index.vue (param de ruta)"
      to: "getGame / getCatalogue"
      via: "Object.hasOwn en el punto de lectura, no confianza en el ?? null"
      pattern: "Object\\.hasOwn\\((gamesById|cataloguesById), gameId\\)"
---

<objective>
Cerrar el BLOCKER **CR-02 (ronda 3)** de `09-VERIFICATION.md`, que hace fallar el criterio de
éxito SC2 del ROADMAP: `useGameHistory.ts:63` construye `heroNames` como objeto literal
(`const heroNames: Record<string, string> = {}`) y `engine/history.ts:41` lo indexa
directamente (`names.heroNames[slot.heroId] ?? null`), así que **la cadena de prototipos de
`Object` participa en la búsqueda**. Un `heroId` de valor `'constructor'` — alcanzable, porque
`resolvePlayerSlots` (`engine/selection.ts:63`) solo exige `typeof entry.heroId === 'string'
&& entry.heroId.length > 0`, sin contrastar contra el catálogo, y el propio proyecto declara
`localStorage` manipulado desde DevTools dentro de su modelo de amenaza
(`engine/__tests__/persistence.test.ts:181-183`) — devuelve la función `Object`, no
`undefined`, así que el `?? null` nunca entra.

La cadena completa, reproducida de forma independiente por el verificador: `heroName` queda
con `typeof === 'function'`; `isHistoryPlayerEntry` (`usePersistedSession.ts:99-100`) rechaza
la entrada entera; `appendHistoryEntry` devuelve `false`; `notifyHistorySaved(false)` pinta un
aviso que culpa al almacenamiento del dispositivo (modo privado, cuota) — **diagnóstico falso,
la causa real es un choque de nombre de propiedad**; y `finishGame()` se ejecuta igualmente,
así que la partida jugada es irrecuperable.

Propósito: es la misma familia de defecto que las dos rondas anteriores — un lado de un
contrato se endurece (la frontera de escritura, 09-12) y el productor que lo alimenta queda
sin revisar. Este plan cierra las DOS puntas a la vez (el mapa se construye sin prototipo Y se
lee con `Object.hasOwn`), porque cerrar solo una deja el defecto reintroducible por el otro
lado. Y cierra de paso **WR-08** de `09-REVIEW.md`, que es literalmente la misma línea
alimentada desde la URL: `getGame('constructor')` devuelve la función `Object`, la guarda
`v-if="!game"` no se dispara, `expand()` revienta y la pantalla se queda en «Cargando…» para
siempre.

Salida: ningún mapa indexado por dato no confiable de esta cadena resuelve por
`Object.prototype`, ni en el productor ni en el consumidor, y una batería de tests
parametrizada sobre las ocho claves heredadas fija la invariante en los cuatro ficheros.
</objective>

<scope_boundary>
DENTRO de este plan, y solo esto:

- Los tres `missing:` del gap SC2 de `09-VERIFICATION.md` (`Object.create(null)` en el
  productor, `Object.hasOwn` + exigencia de `string` en el consumidor, y el test que nombra
  las cinco claves heredadas).
- La misma guarda sobre `villainName` en `buildHistoryEntry`: no es un acceso indexado, pero
  es el campo HERMANO del mismo objeto `FrozenNames`, lo escribe la misma línea de código y
  `isGameHistoryEntry` le exige exactamente el mismo contrato (`null | string`). Dejarlo sin
  tocar sería repetir, dentro del mismo plan, el error de «endurecer un lado y olvidar el
  vecino» que lleva tres rondas costando esta fase.
- **WR-08** de `09-REVIEW.md` (`gamesById`/`cataloguesById` indexados por el parámetro de
  ruta): misma línea, misma familia, y su impacto —pantalla de juego colgada en «Cargando…»
  sin salida— es mayor que el de CR-02 en frecuencia, porque basta con teclear una URL.

FUERA de alcance, anotado para que la re-verificación no lo confunda con un descuido:

- **CR-01 (ronda 3)** (`readRaw` colapsando «no he podido leer» en «no hay nada»): vive en
  `usePersistedSession.ts`, fichero que este plan NO toca. Lo cierra **09-13**, en la misma
  ola y en paralelo. Este plan no debe editar `usePersistedSession.ts` ni su test.
- `spanishHeroAliases[heroId]` (`useHeroSearch.ts:66`) y `labelsByHeroId`/`result`
  (`useHeroSearch.ts:150-163`): mismo patrón, señalado por `09-REVIEW.md` al pie de CR-02.
  Hoy sus claves vienen SIEMPRE del catálogo (`buildHeroOptions` y el `if (hero)` de
  `resolveFrozenNames` filtran antes), así que no son alcanzables desde `localStorage`. Se
  cierran en **09-17** como parte del barrido exhaustivo, con el resto de la familia y con su
  propio test parametrizado — no aquí, para que este plan siga siendo el cierre acotado de un
  BLOCKER y no una refactorización de tres composables.
- `isHistoryPlayerEntry`/`isGameHistoryEntry` (`usePersistedSession.ts`): NO se relajan ni se
  endurecen. Su rechazo era CORRECTO; el defecto es que el motor le entregaba una función. Si
  algo de este plan tienta a tocar ese validador para que «pase», el fallo está en este plan.
- La copy del aviso de fallo y el cableado de `finishGame()` (el amplificador que convierte el
  rechazo en pérdida irreversible de la sesión): **09-16**.
</scope_boundary>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-12-PLAN.md

<interfaces>
Extraído del repo (no hay que explorar para averiguarlo):

`engine/history.ts` — `buildHistoryEntry(session, outcome, now, names)`:
- `FrozenNames` (`:18-21`): `{ villainName: string | null, heroNames: Record<string, string> }`.
- `:37-38`: `const villainId = resolveVillainId(context)` y
  `const villainName = villainId !== null ? names.villainName : null` — sin comprobar el TIPO
  de `names.villainName`.
- `:40-47`: `resolvePlayerSlots(context).map(slot => ...)` con
  `const heroName = slot.heroId !== null ? (names.heroNames[slot.heroId] ?? null) : null`.
  ESTA es la línea del BLOCKER.
- `:52-57` (`durationMs`) y `:67-80` (`difficulty`/`playerCount`/`round`, cierre 09-12) son
  el CRITERIO DEFENSIVO DE REFERENCIA del fichero: guarda explícita por tipo antes de
  aceptar un valor. Esta tarea aplica ese mismo criterio a los dos campos que faltaban.
- Cabecera del fichero: contrato de pureza — normalización defensiva, nunca muta, nunca lanza,
  cero Vue/DOM, cero imports de `app/`.

`app/composables/useGameHistory.ts` — `resolveFrozenNames(context, catalogue)` (`:54-71`):
- `:63`: `const heroNames: Record<string, string> = {}` — ESTA es la línea del BLOCKER.
- `:64-68`: recorre `resolvePlayerSlots(context)`, salta los `heroId === null`, busca el héroe
  en `catalogue?.heroes` y solo si lo encuentra hace
  `heroNames[slot.heroId] = resolveHeroSpanishName(slot.heroId, hero.name)`.
- `record(session, outcome)` (`:240-245`): `getCatalogue` → `resolveFrozenNames` →
  `buildHistoryEntry(session, outcome, Date.now(), names)` → `appendHistoryEntry(entry)`.

`app/composables/useGameContent.ts` (23 líneas):
- `:11-13`: `const gamesById: Record<string, GameDefinition> = { 'marvel-champions': marvelChampions as GameDefinition }`.
- `:18-20`: `function getGame(gameId: string): GameDefinition | null { return gamesById[gameId] ?? null }`.

`app/composables/useCharacterCatalogue.ts` (23 líneas): calcado del anterior —
`cataloguesById` (`:13-15`) y `getCatalogue(gameId)` (`:18-20`).

`engine/selection.ts:47-69` — `resolvePlayerSlots(context)`: el `heroId` de cada hueco se
acepta con `typeof entry.heroId === 'string' && entry.heroId.length > 0`, sin contrastar
contra ningún catálogo. Es la puerta por la que entra `'constructor'`.

`app/composables/usePersistedSession.ts:96-102` — `isHistoryPlayerEntry` exige
`(heroName === null || typeof heroName === 'string')`. NO se toca; es el contrato que el motor
debe cumplir, no al revés.

Arneses de test a reutilizar sin reescribir:
- `engine/__tests__/history.test.ts` (376 líneas): `NOON_UTC_MS` (`:14`),
  `baseContext(overrides)` (`:16`), `baseSession(overrides)` (`:20`), `emptyNames` (`:32`,
  que es literalmente `{ villainName: null, heroNames: {} }` — un objeto literal, así que
  sirve tal cual como arnés hostil). El `describe('D-11: heroName sale de names.heroNames,
  nunca del id')` (`:97`) es el hermano directo de los tests nuevos.
- `app/composables/__tests__/useGameHistory.test.ts` (459 líneas): `createFakeLocalStorage()`
  (`:18`), `makeEntry(overrides)` (`:34`), `makeSession(contextOverrides, round)` (`:56`),
  `describe('resolveFrozenNames')` (`:333`) y `describe('useGameHistory — ciclo
  record/reload/remove con localStorage falso')` (`:373`).
- `vitest.config.ts` declara dos proyectos: `engine` (`engine/**/*.test.ts`, entorno `node`,
  solo imports relativos) y `app-logic` (`app/**/*.test.ts`, entorno `node`, con los alias
  `~~` y `~` resueltos a mano). Un test nuevo bajo `app/composables/__tests__/` entra en el
  segundo automáticamente y puede importar `~~/engine/*` y `~~/content/*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: El mapa de nombres congelados deja de heredar — `Object.create(null)` en el productor y `Object.hasOwn` + exigencia de `string` en el consumidor</name>
  <files>app/composables/useGameHistory.ts, engine/history.ts</files>
  <read_first>
    - `app/composables/useGameHistory.ts` — el fichero a modificar. En concreto
      `resolveFrozenNames` (`:54-71`) con su comentario de cabecera (que ya documenta el
      contrato «un `heroId` que ya no exista en el catálogo simplemente no entra en el mapa»)
      y el `record()` del composable (`:240-245`), que es quien encadena productor y motor.
    - `engine/history.ts` — el otro fichero a modificar. Con atención a la cabecera (contrato
      de pureza), a `FrozenNames` (`:18-21`), a la línea 38 (`villainName`), a las líneas
      40-47 (el `map` de `players`) y sobre todo al bloque `durationMs` (`:52-57`) y al de
      `difficulty`/`playerCount`/`round` (`:59-80`, cierre 09-12): son el criterio defensivo
      que esta tarea replica, no un criterio nuevo.
    - `engine/selection.ts:47-69` (`resolvePlayerSlots`) — para ver con los propios ojos que
      `heroId` se acepta solo por tipo y longitud, sin catálogo: es la precondición de
      alcanzabilidad que el verificador usa.
    - `app/composables/usePersistedSession.ts:89-141` — `isHistoryPlayerEntry` e
      `isGameHistoryEntry`: el contrato que el motor debe cumplir. Leerlo para confirmar que
      la corrección va en el motor y NUNCA en el validador.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el gap SC2 del
      frontmatter: sus `artifacts`/`missing` son el estado final exacto de esta tarea.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §CR-02 — la reproducción
      ejecutada y la propuesta de fix («las dos, no una»).
  </read_first>
  <action>
    **1. `app/composables/useGameHistory.ts` — el mapa nace sin prototipo.**

    En `resolveFrozenNames`, sustituir la inicialización de `heroNames` por un objeto creado
    con `Object.create(null)`, conservando la anotación de tipo `Record<string, string>`. El
    resto del bucle no cambia: sigue saltando los `heroId === null`, sigue buscando el héroe
    en `catalogue?.heroes` y sigue asignando solo cuando lo encuentra.

    Añadir sobre esa línea un comentario que cite **CR-02 (ronda 3)** y explique la causa en
    una frase, en el mismo estilo cita-de-hallazgo del resto del fichero: *este mapa se indexa
    con un `heroId` que viene en última instancia de `localStorage` (editable a mano), así que
    un objeto literal convierte `constructor`/`toString`/`__proto__` en «claves que existen»
    heredadas de `Object.prototype`; un objeto sin prototipo solo puede contener lo que este
    bucle ha puesto.* Dejar escrito que el consumidor (`engine/history.ts`) aplica además su
    propia guarda, y que la pareja es deliberada: el motor no puede asumir que todo llamador
    le pase un mapa sin prototipo (su cabecera declara que nunca lanza y que normaliza por su
    cuenta).

    **2. `engine/history.ts` — el consumidor exige pertenencia propia y tipo.**

    Añadir en el fichero una función módulo-privada `resolveFrozenHeroName(heroNames: unknown,
    heroId: string): string | null` colocada justo encima de `buildHistoryEntry`, que devuelva
    el nombre congelado solo cuando se cumplan las cuatro condiciones, y `null` en cualquier
    otro caso: `heroNames` es un objeto no nulo; `heroId` es una cadena no vacía;
    `Object.hasOwn(heroNames, heroId)` es verdadero; y el valor leído es de tipo `string`.
    Nunca lanza. Documentarla con el mismo tono que `durationMs`: la guarda existe porque la
    clave es dato no confiable Y porque el valor asociado tampoco está garantizado por el
    tipo TypeScript (que es una promesa de compilación, no de ejecución).

    Usar esa función en el `map` de `players`, sustituyendo
    `names.heroNames[slot.heroId] ?? null`. El resto del objeto devuelto por el `map`
    (`heroId`, `playerName`) no cambia.

    Aplicar la MISMA disciplina al campo hermano `villainName` de la línea 38: aceptar
    `names.villainName` solo si `villainId !== null` **y** `typeof names.villainName ===
    'string'`; `null` en cualquier otro caso. Comentar por qué va junto con lo anterior: es el
    otro campo de `FrozenNames`, lo produce el mismo llamador y `isGameHistoryEntry` le exige
    el mismo contrato `null | string` — endurecer solo uno de los dos es exactamente el
    patrón que lleva tres rondas reabriendo esta fase.

    NO cambiar la firma pública de `buildHistoryEntry`, ni el tipo `FrozenNames` (sigue siendo
    `Record<string, string>`: `Object.create(null)` produce un valor perfectamente asignable a
    ese tipo, y cambiarlo a `Map` obligaría a tocar consumidores que no participan del
    defecto). NO tocar `describeLossCause`, `formatEntryDate`, `formatEntryDuration`,
    `sortEntriesByRecency` ni el bloque de normalización de 09-12.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/history.test.ts app/composables/__tests__/useGameHistory.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *//' app/composables/useGameHistory.ts | grep -c "Object.create(null)"` devuelve 1.
    - `grep -v '^ *//' app/composables/useGameHistory.ts | grep -c "heroNames: Record<string, string> = {}"` devuelve 0.
    - `grep -v '^ *//' engine/history.ts | grep -c "Object.hasOwn"` devuelve al menos 1.
    - `grep -v '^ *//' engine/history.ts | grep -c "names.heroNames\[slot.heroId\] ?? null"` devuelve 0.
    - `grep -v '^ *//' engine/history.ts | grep -c "typeof names.villainName === 'string'"` devuelve 1.
    - `grep -c "CR-02 (ronda 3)" app/composables/useGameHistory.ts engine/history.ts` devuelve un total de al menos 2 (uno por fichero como mínimo).
    - Comportamiento: `buildHistoryEntry` sobre una sesión con `context.playerCount: 1` y `context.selection.heroes[0].heroId: 'constructor'`, con `names = { villainName: null, heroNames: {} }` (objeto literal, el arnés hostil), devuelve `entry.players[0].heroName === null` y `typeof entry.players[0].heroName !== 'function'`.
    - Comportamiento: lo mismo para `toString`, `valueOf`, `hasOwnProperty`, `__proto__`, `isPrototypeOf`, `propertyIsEnumerable` y `toLocaleString`.
    - Comportamiento: con `heroNames` que SÍ contiene `{ 'spider-man': 'Spider-Man' }`, `entry.players[0].heroName === 'Spider-Man'` — el camino feliz no se degrada.
    - Comportamiento: con `heroNames = { 'spider-man': 7 }` (valor no-string), `heroName === null`.
    - Comportamiento: con `villainId` resuelto y `names.villainName = 7`, `entry.villainName === null`; con `names.villainName = 'Rhino'`, `entry.villainName === 'Rhino'`.
    - `npx vitest run engine/__tests__/history.test.ts app/composables/__tests__/useGameHistory.test.ts` termina con código 0 sin haber editado ningún test existente (los tests nuevos llegan en la Task 3).
  </acceptance_criteria>
  <done>
    Ningún `heroId` ni `villainName`, venga de donde venga, puede producir un valor que no sea
    `string` o `null` en la entrada del histórico, así que la frontera de escritura ya no
    puede rechazar —ni perder— una partida que el motor acaba de construir.
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-08 — el parámetro de ruta deja de resolver por la cadena de prototipos</name>
  <files>app/composables/useGameContent.ts, app/composables/useCharacterCatalogue.ts</files>
  <read_first>
    - `app/composables/useGameContent.ts` COMPLETO (23 líneas) — el fichero a modificar,
      incluida su cabecera (import estático en build, sin red en ejecución).
    - `app/composables/useCharacterCatalogue.ts` COMPLETO (23 líneas) — calcado del anterior
      por decisión explícita («Calcado literalmente de `app/composables/useGameContent.ts`»),
      así que los dos cambian igual o el calco deja de serlo.
    - `app/pages/[game]/index.vue` líneas 40-60 y 140-170 — los consumidores: `getGame(gameId)`
      con `gameId` sacado del parámetro de ruta, la guarda `v-if="!game"` y el `onMounted` que
      llama a `expand(game, ...)`. Leerlo para confirmar con los propios ojos que un valor
      truthy devuelto por `getGame` esquiva la guarda y revienta en `expand`.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-08 — la cadena completa
      y la lista de las ocho claves afectadas.
  </read_first>
  <action>
    En `app/composables/useGameContent.ts`, cambiar el cuerpo de `getGame(gameId)` para que
    devuelva la definición solo cuando `Object.hasOwn(gamesById, gameId)` sea verdadero, y
    `null` en cualquier otro caso. Mantener el tipo de retorno `GameDefinition | null` y el
    objeto `gamesById` tal como está (objeto literal con su única clave
    `'marvel-champions'`): la guarda va en el PUNTO DE LECTURA, que es donde entra el dato no
    confiable, no en la declaración — así el mapa sigue leyéndose de un vistazo y la
    corrección no depende de que quien añada el segundo juego recuerde una forma de
    construcción poco habitual.

    Aplicar exactamente el mismo cambio a `getCatalogue(gameId)` en
    `app/composables/useCharacterCatalogue.ts` sobre `cataloguesById`.

    Añadir en cada uno de los dos ficheros un comentario que cite **WR-08 (`09-REVIEW.md`,
    ronda 3)** y nombre la consecuencia concreta observada: `gamesById['constructor']` devuelve
    la función `Object`, que es truthy, así que `?? null` nunca entra, la guarda `v-if="!game"`
    de la pantalla de juego no se dispara, `expand()` revienta sobre `game.sections` y la
    pantalla se queda en «Cargando…» sin salida. Dejar escrito que es la misma familia de
    defecto que CR-02 (ronda 3) — un objeto literal indexado por dato no confiable — y que el
    dato no confiable aquí es el parámetro de ruta, es decir, la URL.

    NO tocar `games` (el índice de juegos), ni los imports estáticos, ni la firma de ninguno
    de los dos composables, ni `app/pages/[game]/index.vue` (su guarda `v-if="!game"` ya es
    correcta y empieza a funcionar sola en cuanto `getGame` deja de mentir).
  </action>
  <verify>
    <automated>npx vitest run app/composables 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *//' app/composables/useGameContent.ts | grep -c "Object.hasOwn(gamesById, gameId)"` devuelve 1.
    - `grep -v '^ *//' app/composables/useCharacterCatalogue.ts | grep -c "Object.hasOwn(cataloguesById, gameId)"` devuelve 1.
    - `grep -v '^ *//' app/composables/useGameContent.ts | grep -c "gamesById\[gameId\] ?? null"` devuelve 0.
    - `grep -v '^ *//' app/composables/useCharacterCatalogue.ts | grep -c "cataloguesById\[gameId\] ?? null"` devuelve 0.
    - `grep -c "WR-08" app/composables/useGameContent.ts app/composables/useCharacterCatalogue.ts` devuelve un total de 2 como mínimo.
    - Comportamiento: `getGame('constructor')`, `getGame('toString')`, `getGame('__proto__')`, `getGame('hasOwnProperty')`, `getGame('valueOf')`, `getGame('isPrototypeOf')`, `getGame('propertyIsEnumerable')` y `getGame('toLocaleString')` devuelven `null` los ocho.
    - Comportamiento: `getGame('marvel-champions')` sigue devolviendo la definición con `gameId === 'marvel-champions'`, y `getCatalogue('marvel-champions')` sigue devolviendo un catálogo con `heroes` no vacío.
    - `npm run build` termina con código 0.
  </acceptance_criteria>
  <done>
    Una URL con cualquier nombre de propiedad heredada de `Object.prototype` cae en la
    pantalla «No encontramos ese juego» en lugar de dejar la pantalla de juego colgada en
    «Cargando…» para siempre.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests de regresión — la batería parametrizada sobre las ocho claves heredadas, en los cuatro ficheros</name>
  <files>engine/__tests__/history.test.ts, app/composables/__tests__/useGameHistory.test.ts, app/composables/__tests__/useGameContent.test.ts</files>
  <read_first>
    - `engine/__tests__/history.test.ts` — reutilizar `NOON_UTC_MS`, `baseContext`,
      `baseSession`, `emptyNames`. El `describe('D-11: heroName sale de names.heroNames, nunca
      del id')` (`:97`) y el `describe('CR-01 (ronda 2): buildHistoryEntry normaliza en origen
      y nunca propaga un hueco')` (`:298`) son los dos modelos de redacción: cada `it` nombra
      el caso hostil y afirma el valor exacto.
    - `app/composables/__tests__/useGameHistory.test.ts` — reutilizar
      `createFakeLocalStorage()`, `makeSession(contextOverrides, round)`, y los `describe` de
      `resolveFrozenNames` (`:333`) y del ciclo `record/reload/remove` (`:373`). Este último
      es el que cruza los tres ficheros de producción y donde va el test de ida y vuelta.
    - `app/composables/__tests__/usePersistedSession.test.ts` — NO se modifica; leerlo solo
      para copiar el arnés `createFakeLocalStorage()` + `globalThis.window` si hiciera falta
      replicarlo, y para no duplicar cobertura que ya existe allí.
    - `vitest.config.ts` — confirmar los dos proyectos y los alias `~~`/`~` del proyecto
      `app-logic` antes de crear el fichero de test nuevo.
    - `engine/history.ts`, `app/composables/useGameHistory.ts`,
      `app/composables/useGameContent.ts` y `app/composables/useCharacterCatalogue.ts` YA
      CORREGIDOS por las Tasks 1 y 2.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el tercer `missing:`
      del gap SC2, que nombra literalmente el test de esta tarea y exige que cubra «tanto
      `useGameHistory.ts` como `engine/history.ts`».
  </read_first>
  <action>
    Añadir los tests de regresión que hoy no existen. Cada uno debe FALLAR contra el código
    previo a este plan y PASAR tras él. Nombrar cada `it` citando el hallazgo que cubre
    («CR-02 ronda 3», «WR-08»), igual que los `describe` existentes citan su decisión D-xx. No
    editar, reordenar ni borrar ningún test existente.

    Declarar en cada fichero de test una constante compartida con las ocho claves heredadas
    —`constructor`, `toString`, `valueOf`, `hasOwnProperty`, `__proto__`, `isPrototypeOf`,
    `propertyIsEnumerable`, `toLocaleString`— y recorrerla con `it.each` (o un `for` alrededor
    de los `it`, si eso encaja mejor con el estilo del fichero), para que añadir una clave
    nueva en el futuro sea una sola línea.

    **A. `engine/__tests__/history.test.ts`** — `describe` nuevo, «CR-02 (ronda 3): el mapa de
    nombres congelados se indexa con dato no confiable — la cadena de prototipos no participa»:

    1. Parametrizado sobre las ocho claves: con una sesión cuyo
       `context.selection.heroes[0].heroId` sea la clave y `context.playerCount` sea 1, y con
       `emptyNames` (el objeto literal del propio fichero, que es justamente el arnés hostil),
       la entrada devuelve `entry.players[0].heroName === null` y, explícitamente,
       `typeof entry.players[0].heroName !== 'function'`. La segunda aserción es la que
       describe el síntoma real que el verificador observó; sin ella el test podría pasar por
       el motivo equivocado.
    2. La entrada resultante supera el contrato de escritura: afirmar sobre esa misma entrada
       que `entry.players.every(p => p.heroName === null || typeof p.heroName === 'string')`
       es `true`. Es la conexión explícita con `isHistoryPlayerEntry`, expresada como
       invariante del motor en vez de como acoplamiento de import.
    3. Camino feliz intacto: con `heroNames` construido con `Object.create(null)` y la clave
       `'spider-man'` puesta a `'Spider-Man'`, el `heroName` es `'Spider-Man'`; con un
       `heroNames` literal `{}` y un `heroId` normal ausente del mapa, sigue siendo `null`
       (D-11).
    4. Valor presente pero de tipo equivocado: con `heroNames` que contiene `'spider-man'`
       asignado a un número, el `heroName` es `null`, no el número.
    5. `heroNames` que no es un objeto en absoluto (`null` y una cadena, forzados con `as
       never`): `buildHistoryEntry` no lanza y devuelve `heroName === null` — el motor declara
       en su cabecera que nunca lanza, y ese contrato no puede depender del llamador.
    6. `villainName` no-string (un número y un objeto): `entry.villainName === null`; con
       `'Rhino'`, sigue siendo `'Rhino'`.

    **B. `app/composables/__tests__/useGameHistory.test.ts`** — dos bloques:

    7. Dentro del `describe('resolveFrozenNames')`: el mapa devuelto no tiene prototipo —
       `Object.getPrototypeOf(names.heroNames)` es `null` — y, parametrizado sobre las ocho
       claves, `names.heroNames[clave]` es `undefined` (no una función) cuando ese héroe no
       está en el catálogo.
    8. Dentro del `describe` del ciclo con `localStorage` falso: **el test de ida y vuelta que
       cierra el círculo**. Construir una sesión con `playerCount: 1` y
       `selection.heroes[0] = { heroId: 'constructor', playerName: 'Ana' }`, llamar a
       `record(session, 'won')` y afirmar que devuelve `true`; después `reload()` y afirmar
       que `entries.value` tiene longitud 1, que su `players[0].heroId === 'constructor'` y
       que su `players[0].heroName === null`. Contra el código previo a este plan, `record()`
       devolvía `false` y `entries.value` quedaba vacío: la partida se perdía. Añadir la
       variante con un `heroId` real (`'spider-man'`) para confirmar que el camino feliz sigue
       recuperando el alias congelado.

    **C. `app/composables/__tests__/useGameContent.test.ts`** — fichero NUEVO, con dos
    `describe` (uno por composable) porque el defecto, la línea corregida y el test son los
    mismos y separarlos en dos ficheros duplicaría la constante de claves sin ganar nada.
    Cabecera del fichero explicando eso en dos líneas.

    9. `describe('useGameContent — WR-08: el parámetro de ruta no resuelve por la cadena de
       prototipos')`: parametrizado sobre las ocho claves, `getGame(clave)` es `null` y, de
       forma explícita, `typeof getGame(clave) !== 'function'`.
    10. `getGame('marvel-champions')` devuelve una definición con
        `gameId === 'marvel-champions'`; `getGame('no-existe')` y `getGame('')` devuelven
        `null`.
    11. `describe('useCharacterCatalogue — WR-08: ídem')`: las mismas dos comprobaciones sobre
        `getCatalogue`, afirmando además que `getCatalogue('marvel-champions').heroes` tiene
        longitud mayor que 0.

    Tras escribirlos, ejecutar la prueba anti-tautología y dejar constancia del resultado en el
    SUMMARY: `git stash push -- app/composables/useGameHistory.ts engine/history.ts
    app/composables/useGameContent.ts app/composables/useCharacterCatalogue.ts` (pathspec
    explícito; un `git stash` a secas se llevaría también los tests y la prueba no valdría
    nada), `npx vitest run` para confirmar que los tests nuevos FALLAN, y `git stash pop` para
    confirmar que vuelven a pasar.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` termina con código 0, con al menos 27 ficheros (el fichero de test nuevo) y estrictamente más de 681 tests (línea base reproducida por `09-VERIFICATION.md` en la ronda 3).
    - El fichero `app/composables/__tests__/useGameContent.test.ts` existe y contiene exactamente dos `describe`.
    - `grep -c "CR-02 (ronda 3)" engine/__tests__/history.test.ts app/composables/__tests__/useGameHistory.test.ts` devuelve un total de al menos 2.
    - `grep -c "WR-08" app/composables/__tests__/useGameContent.test.ts` devuelve al menos 1.
    - `grep -c "toLocaleString" engine/__tests__/history.test.ts app/composables/__tests__/useGameHistory.test.ts app/composables/__tests__/useGameContent.test.ts` devuelve un total de al menos 3 (la lista de ocho claves está completa en los tres ficheros, no recortada a las cinco que nombra la verificación).
    - Al menos un `it` afirma explícitamente `typeof ... !== 'function'` sobre un `heroName`, y al menos uno lo afirma sobre el retorno de `getGame`.
    - `grep -c "Object.getPrototypeOf" app/composables/__tests__/useGameHistory.test.ts` devuelve al menos 1.
    - `git diff --stat` no muestra ninguna línea ELIMINADA en `engine/__tests__/history.test.ts` ni en `app/composables/__tests__/useGameHistory.test.ts`: solo adiciones y, como mucho, `import` ampliados.
    - Prueba anti-tautología ejecutada y anotada en el SUMMARY: con el `git stash push` de los cuatro ficheros de producción aplicado, `npx vitest run` FALLA; tras `git stash pop`, pasa.
  </acceptance_criteria>
  <done>
    Los dos escenarios que `09-VERIFICATION.md` y `09-REVIEW.md` reprodujeron con tests
    desechables están cubiertos por tests permanentes, parametrizados sobre las ocho claves
    heredadas y no solo sobre las cinco nombradas, en los tres puntos donde el dato no fiable
    entra: el motor, la costura reactiva y el parámetro de ruta.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `window.localStorage` (`tga:progress:*`) → `resolvePlayerSlots` → `buildHistoryEntry` | **Entrada no fiable**, declarada así por el propio proyecto (`engine/__tests__/persistence.test.ts:181-183`: «`localStorage` es editable desde DevTools»). El `heroId` que llega aquí solo ha pasado por «es una cadena no vacía», nunca por «existe en el catálogo». |
| Parámetro de ruta (`/[game]`) → `getGame` / `getCatalogue` | **Entrada no fiable**: la escribe quien teclea la URL. Es el único dato de la app que llega del exterior sin pasar por ninguna pantalla. |
| `buildHistoryEntry` → `isGameHistoryEntry` (frontera de escritura) | El motor es el productor y el validador es el guardián. Un productor que entrega algo que su propio guardián rechaza convierte una partida jugada en una pérdida con diagnóstico falso. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14-01 | Tampering (contaminación de búsqueda por cadena de prototipos) | `names.heroNames[slot.heroId]` en `engine/history.ts`, mapa producido en `useGameHistory.ts` | mitigate | Task 1: `Object.create(null)` en el productor **y** `Object.hasOwn` + exigencia de `string` en el consumidor. Tests 1-5 de la Task 3, parametrizados sobre las ocho claves heredadas. Severidad `high`: pérdida irrecuperable de la partida jugada. |
| T-09-14-02 | Repudiation (diagnóstico de fallo falso) | `notifyHistorySaved(false)` disparado por un choque de nombre de propiedad, pintando «el dispositivo no permitió escribir» | mitigate | Task 1 elimina la CAUSA (la entrada ya no se rechaza), que es la mitigación que de verdad importa. La copy que atribuye mal los `false` restantes se corrige en 09-16. Test 8 de la Task 3 lo comprueba de extremo a extremo: `record()` devuelve `true` y la entrada se recupera. Severidad `high`. |
| T-09-14-03 | Denial of Service (pantalla colgada sin salida) | `getGame`/`getCatalogue` devolviendo la función `Object` para una URL como `/constructor` | mitigate | Task 2: `Object.hasOwn` en el punto de lectura de los dos composables. Tests 9-11 de la Task 3. Severidad `medium`: no hay pérdida de datos, pero la pantalla queda en «Cargando…» sin ninguna afordancia de salida. |
| T-09-14-04 | Tampering (misma familia, no alcanzable hoy) | `spanishHeroAliases[heroId]` y `labelsByHeroId` en `useHeroSearch.ts` | transfer | Fuera de alcance declarado: sus claves vienen hoy siempre del catálogo. Se cierra en **09-17** (barrido exhaustivo) con su propio test parametrizado. Se registra aquí para que no se pierda entre planes. |
| T-09-14-05 | Information disclosure | `tga:history` | accept | Nombres de jugador del propio grupo y resultados de partidas, solo en el dispositivo, sin red ni backend. Firestore no existe todavía en el repo (SC5/STAT-04). |
| T-09-14-06 | Spoofing / Elevation of privilege | — | accept | No aplican: la app no tiene identidades, sesiones autenticadas ni privilegios que escalar. |
| T-09-14-SC | Tampering (supply chain) | Instalaciones npm/pip/cargo | accept | Ninguna tarea de este plan instala ni actualiza paquetes: se modifican cuatro ficheros TypeScript existentes y se crea un fichero de test. El gate de legitimidad de paquetes no aplica. Si durante la ejecución surgiera la necesidad de instalar algo, este plan queda bloqueado hasta que un checkpoint humano lo apruebe. |
</threat_model>

<verification>
1. `npx vitest run` — suite completa en código 0, al menos 27 ficheros, estrictamente más de
   681 tests. Ningún test preexistente editado para que pase.
2. `grep -v '^ *//' app/composables/useGameHistory.ts | grep -c "Object.create(null)"` devuelve 1.
3. `grep -v '^ *//' engine/history.ts | grep -c "Object.hasOwn"` devuelve al menos 1.
4. `grep -v '^ *//' engine/history.ts | grep -c "names.heroNames\[slot.heroId\] ?? null"` devuelve 0.
5. `grep -v '^ *//' app/composables/useGameContent.ts app/composables/useCharacterCatalogue.ts | grep -c "Object.hasOwn"` devuelve 2.
6. `grep -rn "import" engine/history.ts | grep -c "app/"` devuelve 0 — el motor sigue sin
   alcanzar la capa de interfaz (contrato de su cabecera).
7. `grep -ric "firestore\|firebase" app/ engine/` devuelve 0 — SC5 y STAT-04 siguen
   trivialmente ciertos.
8. `npm run build` termina con código 0.
9. Prueba anti-tautología con `git stash push` de los cuatro ficheros de producción ejecutada
   y su resultado anotado en el SUMMARY.
</verification>

<success_criteria>
- Un `heroId` igual a cualquiera de las ocho claves de `Object.prototype` produce
  `heroName: null`, y `typeof heroName` nunca es `'function'`.
- Esa partida se registra: `record()` devuelve `true` y `loadHistory()`/`reload()` la
  recuperan con `heroId` conservado y `heroName` a `null`.
- Un valor presente en el mapa pero no-string, y un `heroNames` que no es un objeto, producen
  `null` sin que `buildHistoryEntry` lance.
- `villainName` no-string produce `null`; un `villainName` real sigue viajando intacto.
- `getGame`/`getCatalogue` devuelven `null` para las ocho claves heredadas y siguen
  devolviendo el contenido real para `'marvel-champions'`.
- La suite completa sigue en verde con los cierres de las rondas 1 y 2 intactos, y los tests
  nuevos fallan si se revierten los cambios de producción.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-14-SUMMARY.md` al terminar.
</output>
