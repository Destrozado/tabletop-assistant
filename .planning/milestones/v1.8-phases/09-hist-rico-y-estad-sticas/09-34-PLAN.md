---
phase: 09-hist-rico-y-estad-sticas
plan: 34
type: execute
wave: 27
depends_on: ["09-32", "09-33"]
gap_closure: true
files_modified:
  - app/composables/__tests__/afirmacionesRespaldadas.test.ts
autonomous: true
requirements: [HIST-06]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "El vocabulario vigilado deja de ser una lista cerrada de subcadenas que cada ronda demuestra incapaz de alcanzar a la copy nueva: pasa a ser un conjunto de RAÍCES léxicas que cubre por construcción las flexiones («conservar», «conservará», «sigue guardado», «está guardada») sin enumerarlas"
    - "El criterio nuevo es demostrablemente MÁS ancho que el viejo: cada una de las 11 subcadenas de la ronda 7 queda cubierta por al menos una raíz, y hay un test que lo comprueba"
    - "Una copy partida en dos líneas por un formateador ya no evade el gate: la comparación normaliza los espacios en blanco antes de mirar"
    - "La DECISIÓN de Gate A vive en una función pura exportada, no inline dentro de un `it.each` — para que el plan 09-35 pueda ejercerla y demostrar que se pone roja"
    - "Cada excepción auditada nombra un fichero real del repo que respalda la afirmación, y un gate comprueba que ese fichero existe: un motivo circular o inventado ya no puede sellar un hueco"
    - "Gate B audita `NOTICE_HEADING` además de `NOTICE_BODY`: un titular nuevo ya no pasa sin que ningún gate lo mire"
    - "Gate C vigila también el literal `'success'`, el más peligroso de escribir a mano porque pinta «✓ Partida registrada» sin haber pasado por `record()`"
    - "`app/pages/[game]/index.vue` —el fichero donde han vivido cinco de las ocho caras del defecto— no necesita ninguna excepción auditada, porque ya no contiene ninguna afirmación sobre los datos guardados del grupo"
  artifacts:
    - path: "app/composables/__tests__/afirmacionesRespaldadas.test.ts"
      provides: "el criterio por raíces, la normalización de espacios, `frasesSinAuditarDe`, las excepciones con respaldo comprobable, Gate B sobre titulares y Gate C con 'success'"
      contains: "frasesSinAuditarDe"
  key_links:
    - from: "RAICES_SOBRE_LOS_DATOS_DEL_GRUPO"
      to: "el vocabulario cerrado de la ronda 7"
      via: "un test de subsunción comprueba que cada subcadena vieja está cubierta por alguna raíz"
      pattern: "VOCABULARIO_CERRADO_HASTA_LA_RONDA_7"
    - from: "AFIRMACIONES_AUDITADAS[*].respaldo"
      to: "un fichero real de `app/` o `engine/`"
      via: "un gate resuelve cada ruta contra el árbol y falla si no existe"
      pattern: "respaldo"
  prohibitions:
    - "No se añade ninguna excepción auditada cuyo motivo sea circular (del tipo «la garantía real la da otro gate») ni cuyo `respaldo` apunte a un fichero que no existe: es exactamente el mecanismo que selló el hueco de `endGameBody` con un motivo falso"
    - "No se estrecha la región vigilada para reducir hits: acotar la región es lo que falló en la ronda 6; si un hit es ruido, se arregla el código (renombrar un identificador, sacar la copy a un `.ts`), no el gate"
    - "No se toca ningún fichero de producción desde este plan: solo el fichero de test del gate"
    - "No se elimina ninguna de las comprobaciones existentes de Gate A/B/C al reescribirlas: el criterio nuevo tiene que ser un superconjunto, y el test de subsunción es la prueba"
---

<objective>
El gate de clase existe para «proteger la CLASE, no el caso concreto». Los tres hallazgos de la
ronda 7 pasaron sus tres gates en verde. `09-VERIFICATION.md` documenta cinco vías concretas, y
subraya que ninguna es hipotética: la primera tiene una instancia real y sin detectar hoy mismo en
el árbol.

Este plan cierra cuatro de las cinco (la quinta —que Gate S ejerza la decisión de Gate A— es el
plan 09-35, que necesita que esa decisión exista antes como función):

- **(a) Vocabulario cerrado.** `FRASES_SOBRE_LOS_DATOS_DEL_GRUPO` es una lista de 11 subcadenas
  literales; ni «conservará» ni «reintentarlo» están en ella, así que la frase exacta que hacía
  daño en `endGameBody` ni siquiera entraba en el barrido, pese a vivir en el fichero más vigilado
  del repo. La `Gaps Summary` de la verificación va más lejos: *«una lista de subcadenas que el
  propio patrón demuestra, ronda tras ronda, que no puede alcanzar a la copy nueva a tiempo»*. La
  respuesta de este plan no es añadir seis subcadenas más; es **cambiar el criterio a raíces
  léxicas**, que cubren las flexiones por construcción, y **demostrar por test que el criterio
  nuevo es más ancho que el viejo**.
- **(b) `NOTICE_HEADING` fuera de los dos gates** que auditan copy.
- **(c) `'success'` sin vigilar en Gate C.**
- **(d) Comparador sin normalizar espacios.**

Y prepara (e) extrayendo la decisión de Gate A a una función pura exportada.

Además cierra el agravante que hace falso el sello: una excepción auditada cuyo motivo escrito
afirma lo contrario de lo verificado. Desde este plan, cada excepción no lleva solo un motivo en
prosa, sino **la ruta de un fichero del repo que respalda la afirmación**, y un gate comprueba que
esa ruta existe.

Purpose: que la novena cara del defecto se encuentre con un gate que de verdad la mire.
Output: el gate reescrito por raíces, con decisión extraíble, excepciones con respaldo comprobable
y los dos huecos de Gate B/Gate C cerrados.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-32-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-33-SUMMARY.md
</context>

<scope_boundary>
**DENTRO:** `app/composables/__tests__/afirmacionesRespaldadas.test.ts` y nada más.

**FUERA:**
- Cualquier fichero de producción. Si el criterio nuevo deja el gate rojo sobre un fichero de
  producción, la salida correcta **no** es auditar la frase sin más: ver la Task 2, que ordena
  primero intentar el arreglo en el código y solo después auditar, dejando constancia en el
  SUMMARY. Si el arreglo en el código es imprescindible, se ejecuta ahí mismo y se anota como
  desviación explícita del `files_modified` de este plan, con fichero, línea y motivo.
- Gate S (la auto-verificación) → es el plan **09-35**. Este plan deja `frasesSinAuditarDe`
  exportada y lista para que 09-35 la ejerza; no escribe todavía los casos sintéticos rojos.
- `.planning/**` → es el plan **09-36**.
</scope_boundary>

<flagged_assumptions>
**Cobertura de bordes sin resolver (no se descarta en silencio).** Las 14 filas de la sonda de
bordes (HIST-01..HIST-09, STAT-01..STAT-05) volvieron `unclassified`/`unresolved`; ninguna se ha
convertido en truth de respaldo. Quedan como suposición explícita del planificador.

**Sin `## UI Considerations` que levantar.** `09-UI-SPEC.md` no contiene esa sección.

**Medición previa del planificador, a re-medir por el ejecutor.** Sobre el árbol anterior a los
planes 09-32/09-33, el conjunto de raíces propuesto produce hits en exactamente estos ficheros:
`AppHeader.vue` (dispositivo), `ContentChangedNotice.vue` (guardad), `ResumePrompt.vue` (guardad,
progreso), `VoiceUnavailableNotice.vue` (dispositivo), `useHistorySavedNotice.ts` (guardad,
guardar, dispositivo, reintent, no se ha perdido, no encontraréis), `useProgressMountPlan.ts`
(guardad, guardar, dispositivo) y `[game]/index.vue` (conserv, guardad, progreso, reintent, se
borrar). Tras 09-32 y 09-33 se espera que `[game]/index.vue` caiga a cero y aparezcan
`useGameEndCopy.ts` y `useProgressMismatchMark.ts`. **Esta lista es una expectativa, no un
resultado:** el ejecutor la re-mide y registra la salida real en el SUMMARY.
</flagged_assumptions>

<artifacts_this_phase_produces>
| Símbolo | Tipo | Dónde |
|---|---|---|
| `RAICES_SOBRE_LOS_DATOS_DEL_GRUPO` | constante | `app/composables/__tests__/afirmacionesRespaldadas.test.ts` |
| `VOCABULARIO_CERRADO_HASTA_LA_RONDA_7` | constante | ídem |
| `normalizarEspacios(texto: string): string` | función exportada | ídem |
| `contieneRaizSobreLosDatosDelGrupo(region, raiz): boolean` | función exportada (renombre de `contieneFraseSobreLosDatosDelGrupo`) | ídem |
| `frasesSinAuditarDe(ruta: string, contenido: string): string[]` | función pura exportada | ídem |
| `AfirmacionAuditada` (`{ raiz, motivo, respaldo }`) | interfaz | ídem |
| `respaldoExiste(ruta: string): boolean` | función exportada | ídem |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: el criterio pasa de subcadenas cerradas a raíces léxicas, y la decisión sale a una función pura</name>
  <files>app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` — COMPLETO. Especialmente:
      la cabecera (líneas 1-55), el vocabulario cerrado (líneas 76-88), el comparador (líneas
      94-98), `quitarComentarios`/`regionVigilada` (líneas 200-215), los globs de Gate A y el
      `it.each` de Gate A donde vive hoy la decisión inline.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el gap del gate de clase
      completo, con sus cinco vías (a)-(e) y su lista `missing:`.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-04 — la propuesta de invertir
      el criterio, que este plan adopta en su versión practicable (raíces, no lista blanca de
      rótulos: ver `<alternativa_rechazada>` más abajo).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` §1 — el criterio
      de inclusión del que este fichero es la versión ejecutable.
  </read_first>
  <behavior>
    - Cada una de las 11 subcadenas del vocabulario cerrado de la ronda 7 contiene al menos una de
      las raíces nuevas — el criterio nuevo es un superconjunto estricto del viejo.
    - `contieneRaizSobreLosDatosDelGrupo('la partida\n  sigue guardada aquí', 'sigue guardad')` es
      `true`: una copy partida en dos líneas por un formateador ya no evade la comparación.
    - `contieneRaizSobreLosDatosDelGrupo('SIGUE GUARDADA', 'sigue guardad')` es `true`: la
      comparación sigue siendo insensible a mayúsculas, como antes.
    - `frasesSinAuditarDe(ruta, contenido)` devuelve `[]` para un contenido sin ninguna raíz.
    - `frasesSinAuditarDe(ruta, contenido)` devuelve la raíz encontrada cuando el contenido la
      tiene y la ruta no la lleva auditada.
    - `frasesSinAuditarDe(ruta, contenido)` devuelve `[]` cuando la raíz encontrada SÍ está
      auditada para esa ruta exacta.
    - `frasesSinAuditarDe` no mira comentarios ni bloques `<style>`: usa `regionVigilada`.
    - Gate A sigue barriendo todos los `.vue` y `.ts` de `app/` menos `/__tests__/`, y sigue
      teniendo su test de «el gate no está vacío por accidente».
  </behavior>
  <action>
    1. **Sustituir el vocabulario cerrado por raíces.** Declarar
       `const RAICES_SOBRE_LOS_DATOS_DEL_GRUPO = ['conserv', 'guardad', 'guardar', 'dispositivo', 'progreso', 'reintent', 'no se ha perdido', 'ya no está', 'no encontraréis', 'se borrar']`,
       con un comentario de cabecera que explique el cambio de criterio: la lista de subcadenas
       era cerrada y cada ronda demostró que no alcanzaba a la copy nueva a tiempo; una raíz cubre
       todas las flexiones de su familia por construcción (`conserv` alcanza «conservar»,
       «conservará», «conservado»; `guardad` alcanza «guardada», «guardado», «sigue guardado»,
       «está guardada», «no se ha borrado» no —para eso está `se borrar`—), así que ampliar la
       copy deja de exigir ampliar la lista. Citar `09-VERIFICATION.md` ronda 7, vía (a), y su
       instancia real sin detectar (`endGameBody`).

    2. **Conservar el vocabulario viejo como constante de comprobación, no como criterio.**
       Declarar `const VOCABULARIO_CERRADO_HASTA_LA_RONDA_7` con las 11 subcadenas exactas que
       hoy están en `FRASES_SOBRE_LOS_DATOS_DEL_GRUPO` (copiarlas literalmente) y añadir un test:
       **cada una de las 11 contiene al menos una raíz**. Ese test es la prueba mecánica de que el
       criterio nuevo no pierde nada del viejo — sin él, «hemos cambiado a raíces» sería
       exactamente el tipo de afirmación sin respaldo que esta fase persigue.

    3. **Normalizar espacios antes de comparar** (vía (d)). Exportar
       `normalizarEspacios(texto: string): string` que aplique `.replace(/\s+/g, ' ')`, y
       renombrar `contieneFraseSobreLosDatosDelGrupo` a
       `contieneRaizSobreLosDatosDelGrupo(region: string, raiz: string): boolean`, que ahora hace
       `normalizarEspacios(region.toLowerCase()).includes(normalizarEspacios(raiz.toLowerCase()))`.
       Comentario de dos líneas con el motivo: una copy que un formateador parte en dos líneas
       evade el `includes` sin mala fe, y eso convierte al gate en algo que depende del formateo.

    4. **Extraer la decisión de Gate A a una función pura exportada** (preparación de la vía (e)):

       `export function frasesSinAuditarDe(ruta: string, contenido: string): string[]`

       Cuerpo: calcula `regionVigilada(contenido)`, filtra
       `RAICES_SOBRE_LOS_DATOS_DEL_GRUPO` por `contieneRaizSobreLosDatosDelGrupo`, obtiene las
       raíces auditadas para `ruta` a partir de `AFIRMACIONES_AUDITADAS` y devuelve las
       encontradas que no estén auditadas. Comentario explicando por qué se exporta: hasta ahora
       esta lógica vivía inline dentro de un `it.each`, así que Gate S no podía ejercerla — un
       cambio futuro que invirtiera el filtro o cambiara el valor por defecto de las auditadas
       dejaría Gate S en verde mientras Gate A dejaba de detectar nada (vía (e) de
       `09-VERIFICATION.md`). El plan 09-35 es quien la ejerce.

    5. **Gate A llama a la función**, sin duplicar la lógica: el `it.each` se queda con la llamada,
       el `throw` con el mensaje de diagnóstico (conservar el texto del mensaje actual, adaptando
       «frases» a «raíces») y la aserción final. No dejar ninguna copia de la lógica dentro del
       `it.each`.

    6. Conservar intactos: `quitarComentarios`, `regionVigilada`, `rutaRelativa`, los dos globs de
       Gate A y su filtro de `/__tests__/`, y el test de «barre al menos un fichero real».

    Todo el código y los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts</automated>
    <fails_when>salida distinta de 0, o la línea de resumen contiene «0 passed», o cualquier línea «FAIL»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function frasesSinAuditarDe" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 1.
    - `grep -c "export function normalizarEspacios" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 1.
    - `grep -c "RAICES_SOBRE_LOS_DATOS_DEL_GRUPO" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 3 (declaración, uso en `frasesSinAuditarDe`, uso en el test de subsunción).
    - `grep -c "VOCABULARIO_CERRADO_HASTA_LA_RONDA_7" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 2.
    - <!-- planner-discipline-allow: FRASES_SOBRE_LOS_DATOS_DEL_GRUPO --> `grep -c "FRASES_SOBRE_LOS_DATOS_DEL_GRUPO" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 0 — el criterio viejo ya no existe como criterio (sobrevive con otro nombre, solo como constante de comprobación).
    - El test de subsunción existe y recorre las 11: `grep -c "VOCABULARIO_CERRADO_HASTA_LA_RONDA_7.length" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 1, **o** el test itera la constante con `it.each`/`for…of` — dejar en el SUMMARY la línea exacta que lo recorre.
    - `npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts` sale 0; registrar en el SUMMARY el recuento de tests antes y después.
    - `npm test` sale 0 y `npm run typecheck` sale 0.
    - El SUMMARY incluye la salida real de la medición: qué ficheros y qué raíces produce el
      criterio nuevo sobre el árbol actual.
  </acceptance_criteria>
  <reversibility rating="costly">
    Cambiar el criterio del gate afecta a todo lo que se escriba después en `app/`: revertirlo es
    un `git revert` limpio, pero el coste real es que la copy escrita bajo el criterio nuevo se
    habrá acostumbrado a él. Se acepta: el criterio viejo ya demostró ocho veces que no alcanza.
  </reversibility>
  <done>
    El gate mira la clase por raíz léxica en vez de por subcadena tecleada, su decisión es una
    función pura que otro plan puede ejercer, y hay un test que demuestra que no ha perdido
    cobertura respecto al criterio anterior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cada excepción auditada nombra un respaldo real, y un gate comprueba que existe</name>
  <files>app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` líneas 100-140 —
      `AFIRMACIONES_AUDITADAS` completa, con los cinco motivos escritos. El de
      `app/pages/[game]/index.vue` (líneas 118-125) es el que `09-VERIFICATION.md` señala como
      motivo falso; el de `useHistorySavedNotice.ts` (líneas 130-139) es el que señala como
      circular.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-32-SUMMARY.md` y `09-33-SUMMARY.md` — qué
      copy se movió a `useGameEndCopy.ts` y `useProgressMismatchMark.ts`, y con qué test se fijó.
    - Los ficheros de producción que aparezcan en la medición, para poder escribir un motivo que
      sea un hecho comprobable y no una impresión.
  </read_first>
  <behavior>
    - Toda entrada de `AFIRMACIONES_AUDITADAS` tiene `raiz`, `motivo` no vacío y `respaldo` no
      vacío.
    - Todo `respaldo` resuelve a un fichero que existe en `app/` o en `engine/`.
    - `AFIRMACIONES_AUDITADAS` no tiene ninguna clave para `app/pages/[game]/index.vue`.
    - `frasesSinAuditarDe('app/pages/[game]/index.vue', <contenido real del fichero>)` devuelve
      `[]` — y no porque esté auditado, sino porque el fichero no contiene ninguna raíz.
    - Gate A queda en verde sobre todo el árbol.
  </behavior>
  <action>
    1. **Cambiar la forma de `AFIRMACIONES_AUDITADAS`** a
       `Record<string, AfirmacionAuditada[]>` con
       `interface AfirmacionAuditada { raiz: string, motivo: string, respaldo: string }`.
       `respaldo` es la ruta relativa de un fichero del repo (bajo `app/` o `engine/`) que
       **ejerce** la comprobación que el motivo describe — normalmente el test puro que fija la
       copy o la decisión.

       Escribir en el comentario de la constante por qué se añade `respaldo`: la ronda 7 encontró
       una excepción cuyo motivo escrito afirmaba que `endGameBody` «es cierto en las cuatro
       salidas», lo cual era falso, y otra cuyo motivo era explícitamente circular («la garantía
       real la da Gate B»). Un motivo en prosa no se puede comprobar; una ruta sí, y obligar a
       nombrarla fuerza a que exista algo que ejerza la afirmación.

    2. **Añadir el gate de respaldo:** exportar `respaldoExiste(ruta: string): boolean`, apoyada en
       un glob propio —`import.meta.glob` sobre `/app/**/*.ts`, `/app/**/*.vue` y `/engine/**/*.ts`
       **sin** filtrar `/__tests__/`, porque el respaldo típico es justamente un test— y un test
       que recorra todas las entradas y falle nombrando la ruta que no resuelve. Añadir también la
       comprobación de que `motivo` y `respaldo` no son cadenas vacías.

    3. **Re-medir el árbol y reescribir la lista de excepciones entera.** Ejecutar el gate,
       recoger los hits reales, y para cada par (fichero, raíz):
       - Si el hit es **ruido de código** (un identificador que casualmente contiene la raíz, no
         una frase que el grupo lea), el arreglo correcto es **renombrar el identificador** en el
         fichero de producción, no auditarlo. Si eso obliga a salir del `files_modified` de este
         plan, se hace igualmente y se anota como desviación explícita en el SUMMARY (fichero,
         línea, motivo).
       - Si el hit es **copy real**, se audita con un motivo que sea un hecho comprobable (qué
         función o qué rama garantiza la frase) y un `respaldo` que apunte al fichero que lo
         ejerce.
       Expectativa medida por el planificador —a contrastar, no a copiar a ciegas—:
       `AppHeader.vue` y `VoiceUnavailableNotice.vue` (raíz `dispositivo`, respaldo
       `app/composables/__tests__/useVoiceAnnouncer.test.ts`); `ContentChangedNotice.vue` y
       `ResumePrompt.vue` (raíces `guardad`/`progreso`, respaldo
       `app/composables/__tests__/useProgressMountPlan.test.ts`, que es quien fija que esos dos
       componentes solo se montan tras una lectura real de la autoridad);
       `useProgressMountPlan.ts` (respaldo su propio test); `useHistorySavedNotice.ts` (respaldo
       `app/composables/__tests__/avisoTrasRegistroFallido.test.ts`, el test de costura que
       recorre almacenamiento → autoridad → decisión → copy, y que **no** es circular a diferencia
       del motivo actual); `useGameEndCopy.ts` y `useProgressMismatchMark.ts` (respaldo sus
       propios tests, creados en 09-32/09-33).

    4. **Eliminar la entrada de `app/pages/[game]/index.vue`.** El `missing:` de SC3 pide
       «corregir el motivo escrito»; este plan hace algo más fuerte: como el plan 09-32 sacó del
       fichero toda la copy sobre los datos del grupo y renombró los identificadores que producían
       ruido, **la excepción deja de ser necesaria** y se borra. Un motivo que no existe no puede
       ser falso.
       Añadir un test explícito que lo fije: `frasesSinAuditarDe` sobre el contenido real de
       `app/pages/[game]/index.vue` devuelve `[]` **y** `AFIRMACIONES_AUDITADAS` no tiene clave
       para esa ruta, con un comentario que diga por qué esa propiedad importa (es el fichero
       donde han vivido cinco de las ocho caras del defecto).
       Si al medir quedara algún hit residual en ese fichero: si es identificador, renombrarlo; si
       es copy, sacarla a un `.ts` con test puro siguiendo el patrón de `useGameEndCopy.ts`. Solo
       si ninguna de las dos cosas fuera posible se audita — y entonces el SUMMARY tiene que
       explicar, con la línea concreta, por qué no lo era.

    Todo el código y los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts</automated>
    <fails_when>salida distinta de 0, o la línea de resumen contiene «0 passed», o cualquier línea «FAIL»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface AfirmacionAuditada" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 1 y `grep -c "respaldo:" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 7 (una por entrada auditada; registrar el número real en el SUMMARY).
    - `grep -c "export function respaldoExiste" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 1.
    - <!-- planner-discipline-allow: 'app/pages/[game]/index.vue': --> `grep -c "'app/pages/\[game\]/index.vue':" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 0 — no hay clave de excepción para ese fichero. Mostrar además en el SUMMARY el bloque completo de `AFIRMACIONES_AUDITADAS` tal como queda.
    - Existe un test que afirma que `frasesSinAuditarDe` devuelve `[]` para `app/pages/[game]/index.vue` leído del glob real: `grep -c "pages/\[game\]/index.vue" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 1.
    - `npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts` sale 0.
    - `npm test` sale 0.
    - El SUMMARY incluye, íntegra, la lista de (fichero, raíz, motivo, respaldo) tal como queda, y
      nombra cualquier renombre hecho en ficheros de producción como desviación explícita.
  </acceptance_criteria>
  <done>
    Ninguna excepción auditada puede sellar un hueco con un motivo circular o falso sin que un
    gate lo note, y el fichero que más caras del defecto ha alojado ya no necesita excepción
    alguna.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate B mira también los titulares, Gate C vigila `'success'`, y la lista de ficheros deja de contradecirse</name>
  <files>app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` líneas 145-160
      (`VARIANTES_RESPALDADAS_POR_LA_AUTORIDAD`), 157-194
      (`FICHEROS_QUE_PUEDEN_NOMBRAR_EL_ESTADO_DEL_DISPOSITIVO`, con su cabecera que dice «los
      cuatro» mientras lista cinco y su `TODO(09-30)` sin cerrar) y 266-295 (el bucle de Gate B) y
      370-395 (el test WR-03 de Gate C con `LITERALES_VARIANTE`).
    - `app/composables/useHistorySavedNotice.ts` — `NOTICE_HEADING` y `NOTICE_BODY` tal como los
      dejó el plan 09-32, e `isSuccessVariant`.
    - `app/components/HistorySavedNotice.vue` tal como lo dejó el plan 09-32 (ya sin el literal de
      variante).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-10 — la contradicción entre
      la cabecera de la lista y su contenido.
  </read_first>
  <behavior>
    - Gate B recorre las entradas de `NOTICE_HEADING` **y** las de `NOTICE_BODY`; un titular que
      afirmara algo sobre los datos del grupo desde una variante no respaldada pondría el gate
      rojo.
    - El titular de la variante de éxito («✓ Partida registrada») no contiene ninguna raíz
      vigilada, así que no exige estar en la lista de variantes respaldadas — y eso queda escrito.
    - Los cuatro titulares de fallo sí contienen una raíz (`guardar`) y sus cuatro variantes están
      en la lista de respaldadas.
    - Ningún fichero fuera de `useHistorySavedNotice.ts` nombra el literal `'success'`.
    - La cabecera de `FICHEROS_QUE_PUEDEN_NOMBRAR_EL_ESTADO_DEL_DISPOSITIVO` dice el mismo número
      de ficheros que la lista contiene, y no queda ningún marcador `TODO` sin cerrar en el
      fichero.
  </behavior>
  <action>
    1. **Gate B recorre los dos registros.** Sustituir `Object.entries(NOTICE_BODY)` por
       `[...Object.entries(NOTICE_HEADING), ...Object.entries(NOTICE_BODY)]`.

       **Atención — el `missing:` de la verificación propone literalmente
       `{ ...NOTICE_HEADING, ...NOTICE_BODY }`, y eso sería un error:** los dos registros tienen
       exactamente las mismas cinco claves (`NoticeVariant`), así que el spread descartaría los
       cinco titulares y Gate B seguiría mirando solo los cuerpos — el mismo hueco con otra
       forma. Implementar la concatenación de entradas y **dejar escrito en un comentario por qué
       no se usa el spread**, citando la verificación: es la clase de detalle que convierte un
       cierre en la cara siguiente del defecto.

       Importar `NOTICE_HEADING` en el fichero de test (hoy solo importa `NOTICE_BODY` y
       `resolveNoticeVariant`).

    2. **Documentar el caso del titular de éxito.** Comprobar al escribirlo y dejarlo por escrito:
       «✓ Partida registrada» no contiene ninguna raíz vigilada, así que Gate B no le exige nada —
       y la garantía de que nadie lo pinta sin haber pasado por `record()` la da Gate C al vigilar
       el literal `'success'` (punto 3). Las dos piezas se citan mutuamente **nombrando qué
       comprueba cada una**, que es lo contrario de la circularidad que la ronda 7 señaló (un
       motivo que delega la garantía sin decir en qué consiste).

    3. **Gate C vigila `'success'`:** añadir ese literal a `LITERALES_VARIANTE` del test WR-03, con
       un comentario que recoja el motivo de `09-VERIFICATION.md` vía (c): es el más peligroso de
       escribir a mano porque pinta «✓ Partida registrada» sin haber pasado por `record()`. El
       único fichero autorizado sigue siendo `app/composables/useHistorySavedNotice.ts`, que es
       quien lo define; el plan 09-32 ya retiró la única aparición que existía fuera
       (`HistorySavedNotice.vue`), sustituida por `isSuccessVariant`.

    4. **Cerrar WR-10** en la misma pasada, porque son las mismas líneas: corregir la cabecera de
       `FICHEROS_QUE_PUEDEN_NOMBRAR_EL_ESTADO_DEL_DISPOSITIVO` para que el número que anuncia
       coincida con el número de entradas, y sustituir el `TODO(09-30)` de la entrada de
       `useProgressMountPlan.ts` por su motivo definitivo: traduce `StoredProgress` a la decisión
       de montaje (`MountAction`), el mismo papel que `useHistorySavedNotice.ts` tiene para la
       copy; su `switch (stored)` nombra los cuatro valores porque **es** la decisión total sobre
       ellos, y su totalidad está fijada por `app/composables/__tests__/useProgressMountPlan.test.ts`.
       Que no quede en el fichero ningún marcador `TODO`/`FIXME`/`TBD`/`XXX`/`HACK`.

    Todo el código y los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts && npm test</automated>
    <fails_when>salida distinta de 0, o la línea de resumen contiene «0 passed», o cualquier línea «FAIL»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "Object.entries(NOTICE_HEADING)" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 1 y `grep -c "import { NOTICE_BODY, NOTICE_HEADING" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 1 (o el orden de importación que ESLint acepte; registrar la línea exacta en el SUMMARY).
    - <!-- planner-discipline-allow: { ...NOTICE_HEADING, ...NOTICE_BODY } --> `grep -v "^\s*//" app/composables/__tests__/afirmacionesRespaldadas.test.ts | grep -c "\.\.\.NOTICE_HEADING, \.\.\.NOTICE_BODY }"` devuelve 0 — no se ha usado el spread que perdería los titulares.
    - `grep -c "'success'" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 1 dentro de `LITERALES_VARIANTE`; mostrar la línea completa en el SUMMARY.
    - <!-- planner-discipline-allow: TODO, FIXME, TBD, XXX, HACK --> `grep -cE "TODO|FIXME|TBD|XXX|HACK" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 0.
    - Prueba manual de que Gate C funciona, a ejecutar y registrar en el SUMMARY sin dejar el
      cambio en el repo: introducir temporalmente el literal de la variante de éxito en un `.vue`
      de `app/`, ejecutar el gate, comprobar que se pone ROJO con el mensaje de WR-03, y revertir.
      Pegar en el SUMMARY el mensaje de error obtenido.
    - `npm test` sale 0 y `npm run typecheck` sale 0.
  </acceptance_criteria>
  <done>
    Los dos registros de copy del composable están auditados por Gate B, el literal de la variante
    de éxito está vigilado por Gate C con una demostración registrada de que se pone roja, y la
    lista de ficheros autorizados dice la verdad sobre sí misma.
  </done>
</task>

</tasks>

<alternativa_rechazada>
`09-REVIEW.md` §WR-04 y la `Gaps Summary` de `09-VERIFICATION.md` proponen **invertir el criterio
por completo**: ningún `.vue` puede llevar texto literal en un nodo de plantilla salvo una lista
blanca de rótulos, y toda copy sobre datos persistidos entra por prop/computed desde un `.ts` con
test puro.

**Este plan adopta la segunda mitad de esa propuesta y rechaza la primera, por dos razones
comprobables:**

1. **La segunda mitad ya se está aplicando donde importa.** Los planes 09-32 y 09-33 sacan a un
   `.ts` con test puro toda la copy sobre datos persistidos que quedaba en plantilla o en un
   `computed` del SFC (`endGameBody`, `discardBody`, el aviso de discrepancia). El resultado
   —comprobado por test en la Task 2— es que `app/pages/[game]/index.vue` deja de necesitar
   excepción.
2. **La primera mitad (lista blanca de TODO rótulo literal en los 24 `.vue` de la app) tendría un
   coste que el propio patrón desaconseja.** Obligaría a auditar cientos de literales que no
   afirman nada sobre los datos del grupo («ENTENDIDO ›», «Empezar nueva», «Perdida»), y una lista
   de excepciones de ese tamaño es precisamente donde un motivo escrito se rellena sin mirar —el
   mecanismo exacto que produjo el motivo falso de `endGameBody`. El criterio por raíces consigue
   la cobertura que faltaba (una raíz alcanza las flexiones futuras) manteniendo la lista de
   excepciones en un tamaño que se puede auditar de verdad: 7-8 entradas, medidas, no estimadas.

Queda registrado para que la ronda siguiente pueda reevaluarlo con datos: si aparece una novena
cara en una frase que ninguna raíz alcanza, la inversión completa vuelve a la mesa.
</alternativa_rechazada>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Copy de la app → grupo | Toda frase que el grupo lee sobre sus datos guardados; el gate es el único control automático sobre esa frontera |
| Excepción auditada → gate | Una entrada en `AFIRMACIONES_AUDITADAS` desactiva el control para un par (fichero, raíz): es el punto donde un motivo falso puede sellar un hueco |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-34-01 | Repudiation | Una excepción auditada con motivo falso o circular sellando un hueco con sello de verificado | high | mitigate | `respaldo` obligatorio + gate que comprueba que la ruta existe + eliminación de la excepción de `index.vue` en vez de su corrección |
| T-09-34-02 | Spoofing | Copy nueva que afirma sobre los datos del grupo y evade el gate por flexión no enumerada | high | mitigate | Criterio por raíces + test de subsunción del vocabulario cerrado anterior |
| T-09-34-03 | Spoofing | Copy nueva que evade el gate por estar partida en dos líneas por un formateador | medium | mitigate | `normalizarEspacios` aplicado a los dos lados de la comparación |
| T-09-34-04 | Spoofing | Un titular nuevo en `NOTICE_HEADING` que ningún gate mira | medium | mitigate | Gate B recorre las entradas de los dos registros, con la concatenación correcta (no el spread, que perdería los titulares) |
| T-09-34-05 | Elevation of privilege | Un fichero cualquiera pintando «✓ Partida registrada» sin haber pasado por `record()` | high | mitigate | `'success'` añadido a `LITERALES_VARIANTE` de Gate C, con demostración registrada de que el gate se pone rojo |
| T-09-34-SC | Tampering | Instalaciones de paquetes (npm) | low | accept | Este plan no instala ni actualiza ninguna dependencia: `git diff -- package.json package-lock.json` debe quedar vacío |
</threat_model>

<verification>
- `npm test` en verde y `npm run typecheck` en verde.
- `git diff -- package.json package-lock.json` vacío.
- `git diff --stat -- engine/` vacío.
- `git diff --stat` muestra `app/composables/__tests__/afirmacionesRespaldadas.test.ts` y, si los
  hubo, los renombres de identificador que la Task 2 autoriza — cada uno nombrado en el SUMMARY.
</verification>

<success_criteria>
- El criterio del gate es por raíz léxica y hay un test que demuestra que cubre todo lo que cubría
  el criterio anterior.
- La decisión de Gate A es una función pura exportada, lista para que el plan 09-35 la ejerza.
- Cada excepción auditada nombra un fichero real que respalda la afirmación, comprobado por gate.
- `app/pages/[game]/index.vue` no tiene excepción porque no la necesita, y un test lo fija.
- Gate B audita titulares y cuerpos; Gate C vigila el literal de la variante de éxito.
</success_criteria>

<output>
Create `.planning/phases/09-hist-rico-y-estad-sticas/09-34-SUMMARY.md` when done
</output>
