---
phase: 09-hist-rico-y-estad-sticas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/types.ts
  - engine/history.ts
  - engine/__tests__/history.test.ts
autonomous: true
requirements: [HIST-02, HIST-03, HIST-04, HIST-05]

must_haves:
  truths:
    - "D-06: el enum guardado es 'mainSchemeCompleted' | 'heroesEliminated' y la redacción española vive en una función del motor, no en el dato"
    - "D-08/D-09: la duración es Date.now() - startedAt sin tope y la ronda se guarda tal cual, nunca round - 1"
    - "D-10: sin startedAt la entrada guarda durationMs: null y el formateador devuelve «—», nunca 0"
    - "D-11: heroName/villainName son nombres congelados que llegan como parámetro; engine/ no resuelve alias"
    - "D-12: una entrada sin villano ni héroes se construye igual, con villainId null y heroId null en cada hueco"
    - "D-15: cada entrada guarda su gameId"
    - "D-21: fecha y duración las formatea una función pura del motor, nunca toLocaleDateString"
    - "HIST-04: la entrada contiene resultado, causa, villano, héroe y nombre por jugador, fecha, dificultad, nº de jugadores, duración y ronda"
  artifacts:
    - path: "engine/types.ts"
      provides: "SessionContext.startedAt, LossCause, GameOutcome, HistoryPlayerEntry, GameHistoryEntry"
      contains: "export interface GameHistoryEntry"
    - path: "engine/history.ts"
      provides: "buildHistoryEntry, describeLossCause, formatEntryDate, formatEntryDuration, sortEntriesByRecency"
      exports: ["buildHistoryEntry", "describeLossCause", "formatEntryDate", "formatEntryDuration", "sortEntriesByRecency"]
      min_lines: 90
    - path: "engine/__tests__/history.test.ts"
      provides: "cobertura de D-06/D-08/D-09/D-10/D-11/D-12/D-15/D-21"
  key_links:
    - from: "engine/history.ts"
      to: "engine/selection.ts"
      via: "resolvePlayerSlots / resolveVillainId"
      pattern: "from './selection'"
---

<objective>
Levantar la base pura de toda la fase: los tipos aditivos del histórico en `engine/types.ts`
y el módulo `engine/history.ts` que construye una entrada del histórico a partir de una
sesión viva y la formatea para pantalla.

Propósito: `ARCHITECTURE.md` §Chunk 5 fija este orden — las funciones puras se construyen y
se prueban contra una sesión y un histórico hechos a mano, sin interfaz y sin navegador,
antes de que exista una sola pantalla. Todos los planes siguientes de la fase dependen de
estos tipos.

Salida: `engine/types.ts` extendido de forma aditiva, `engine/history.ts` nuevo y su test
hermano en verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-RESEARCH.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-PATTERNS.md

<interfaces>
Contratos ya existentes que este plan consume (extraídos del repo; no hace falta explorar):

- `engine/types.ts` — `Difficulty = 'normal' | 'expert'`; `SessionContext { playerCount:
  number; difficulty: Difficulty; selection?: HeroSelection; counters?: CounterState;
  [key: string]: unknown }`; `EngineSession { gameId: string; contentVersion: number;
  sequence: RuntimeStepNode[]; cursor: number; round: number; context: SessionContext;
  loopStartIndex?: number; loopEndIndex?: number }`.
- `engine/selection.ts` — `resolvePlayerSlots(context: SessionContext): { heroId: string |
  null, playerName: string }[]` (longitud derivada de `context.playerCount`, nunca lanza) y
  `resolveVillainId(context: SessionContext): string | null`.
- `engine/header.ts` — `describeHeader(session)`: el precedente de «función pura del motor
  que devuelve cadenas ya listas para pintar».
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos aditivos del histórico en engine/types.ts</name>
  <files>engine/types.ts</files>
  <read_first>
    - `engine/types.ts` (fichero a modificar; leer el bloque `SessionContext` con
      `selection?`/`counters?` y su comentario de campo aditivo, y la cabecera del bloque de
      catálogo que explica por qué este fichero NUNCA importa zod)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-RESEARCH.md` §«Extensión de
      `engine/types.ts`» (forma completa ya especificada)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-PATTERNS.md` §`engine/types.ts`
  </read_first>
  <action>
    Añadir a `engine/types.ts`, de forma ADITIVA y sin tocar ni una línea existente:

    1. En `SessionContext`, el campo opcional `startedAt?: number` (epoch ms) justo después
       de `counters?: CounterState` y antes del índice `[key: string]: unknown`. Comentario
       obligatorio citando D-07: se fija UNA sola vez en `start()` de
       `app/composables/useGameSession.ts`, nunca se reescribe al reanudar; es campo aditivo
       igual que `selection` (D-19 Fase 6) y `counters` (D-19 Fase 7), así que NO se bumpea
       `formatVersion` ni `contentVersion` y `engine/persistence.ts` no se toca.
       `undefined` es el estado real de una sesión guardada por v1.7 (D-10).
    2. `export type LossCause = 'mainSchemeCompleted' | 'heroesEliminated'` — comentario
       citando D-06: enum plano, la clave del dato NO es la etiqueta de pantalla (misma
       disciplina que `handSizeAlterEgo`, D-07 de la Fase 8); la redacción española vive en
       `describeLossCause` de `engine/history.ts`.
    3. `export type GameOutcome = 'won' | LossCause` — los tres valores que el diálogo de
       resultado puede emitir (D-01): elegir la derrota YA elige su causa, así que no existe
       el estado «perdida sin causa».
    4. `export interface HistoryPlayerEntry { heroId: string | null; heroName: string |
       null; playerName: string }` — comentario citando D-11: `heroName` es el nombre
       CONGELADO que el grupo vio en pantalla (alias español), resuelto por la capa `app/`
       antes de llamar al motor; `null` cuando ese hueco no tenía héroe (SEL-09/D-12).
    5. `export interface GameHistoryEntry` con exactamente estos campos y tipos:
       `id: string`, `gameId: string`, `result: 'won' | 'lost'`,
       `lossCause: LossCause | null`, `villainId: string | null`,
       `villainName: string | null`, `players: HistoryPlayerEntry[]`,
       `difficulty: Difficulty`, `playerCount: number`, `round: number`,
       `durationMs: number | null`, `recordedAt: string`.
       Comentarios por campo citando la decisión: `gameId` D-15 (un solo histórico para toda
       la app), `lossCause` D-06 (`null` cuando `result === 'won'`), `round` D-09 (valor del
       motor tal cual, se lee «hasta la ronda N», nunca `round - 1`), `durationMs` D-08/D-10
       (reloj de pared sin tope; `null` cuando no se puede saber), `recordedAt` ISO.

    Prohibido en este fichero: importar zod, importar cualquier cosa de `app/`, y añadir un
    campo `syncedToFirestore` o cualquier mención a Firestore/Firebase/sync (D-14).
  </action>
  <verify>
    <automated>npx vitest run engine 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "startedAt?: number" engine/types.ts` devuelve 1
    - `grep -c "export type LossCause = 'mainSchemeCompleted' | 'heroesEliminated'" engine/types.ts` devuelve 1
    - `grep -c "export type GameOutcome" engine/types.ts` devuelve 1
    - `grep -c "export interface GameHistoryEntry" engine/types.ts` devuelve 1
    - `grep -c "export interface HistoryPlayerEntry" engine/types.ts` devuelve 1
    - `grep -Ei "zod|firestore|firebase|syncedToFirestore" engine/types.ts` no devuelve ninguna línea
    - `npx vitest run engine` termina con código 0 (ningún test existente se rompe)
  </acceptance_criteria>
  <done>`engine/types.ts` exporta los cuatro símbolos nuevos y `SessionContext` acepta `startedAt`, sin bump de versión y sin ningún import nuevo.</done>
</task>

<task type="auto">
  <name>Task 2: engine/history.ts — construcción y formateo puros</name>
  <files>engine/history.ts</files>
  <read_first>
    - `engine/selection.ts` (de dónde salen `resolvePlayerSlots`/`resolveVillainId` y el
      estilo de normalización defensiva por tipo que nunca lanza)
    - `engine/header.ts` (precedente de función pura que devuelve cadenas listas para pintar)
    - `engine/types.ts` (ya modificado en la Task 1)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-RESEARCH.md` §Pattern 1 y §Pattern 2
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §Copywriting Contract
  </read_first>
  <action>
    Crear `engine/history.ts` con cabecera de comentario al estilo de `engine/selection.ts`
    (qué contrato cumple: puro, sin Vue, sin DOM, sin zod, sin acceso a `app/`), importando
    `resolvePlayerSlots` y `resolveVillainId` de `./selection` y los tipos de `./types`.
    Exporta exactamente:

    1. `export interface FrozenNames { villainName: string | null; heroNames:
       Record<string, string> }` — resuelve la Open Question 2 de `09-RESEARCH.md`: los
       nombres congelados (alias español, `app/data/spanish-hero-aliases.ts`) los resuelve el
       llamador (`app/composables/useGameHistory.ts`, plan 09-05) y llegan como DATO PLANO;
       `engine/` no puede importar `app/`. `heroNames` es un mapa `heroId → nombre
       congelado`.

    2. `export function buildHistoryEntry(session: EngineSession, outcome: GameOutcome,
       now: number, names: FrozenNames): GameHistoryEntry`.
       - `now` se inyecta, nunca se llama a `Date.now()` dentro (determinismo en test).
       - `id`: plantilla `${now}-${Math.random().toString(36).slice(2, 8)}` (Claude's
         Discretion de D; sin `crypto.randomUUID`, que exige contexto seguro).
       - `gameId`, `round` y `context` se leen de `session` tal cual; `round` sin restar nada
         (D-09).
       - Villano: `resolveVillainId(session.context)` — NUNCA `context.selection.villainId`
         en crudo. `villainName` es `names.villainName` cuando hay `villainId`, si no `null`.
       - Jugadores: `resolvePlayerSlots(session.context)` mapeado a `HistoryPlayerEntry`;
         `heroName` es `names.heroNames[slot.heroId] ?? null` cuando `slot.heroId` no es
         `null`, y `null` en cualquier otro caso.
       - `result`: `'won'` cuando `outcome === 'won'`, si no `'lost'`. `lossCause`: `null`
         cuando `outcome === 'won'`, si no el propio `outcome` (D-06).
       - `difficulty` y `playerCount` de `session.context`.
       - `durationMs` (D-08/D-10): se calcula `now - context.startedAt` SOLO si
         `typeof context.startedAt === 'number' && Number.isFinite(context.startedAt) &&
         now >= context.startedAt`; en cualquier otro caso `null`. Nunca 0 de relleno, nunca
         un negativo (un `startedAt` manipulado en el futuro cae a `null`).
       - `recordedAt`: `new Date(now).toISOString()`.
       - No muta `session` ni `names`: devuelve un objeto nuevo (disciplina de todo el motor).

    3. `export function describeLossCause(cause: LossCause): string` — devuelve exactamente
       `'Se completó el Plan Principal'` para `'mainSchemeCompleted'` y
       `'Todos los héroes eliminados'` para `'heroesEliminated'` (D-06, contrastado con el
       Rules Reference v1.7 p. 46 y «Eliminated»). Comentario obligatorio: estas dos cadenas
       son las MISMAS que `app/components/GameOutcomeDialog.vue` pinta en sus botones tras el
       prefijo `PERDIDA · ` (UI-SPEC §Copywriting Contract) — si una cambia, la otra también.

    4. `export const MONTHS_ES` con los doce meses abreviados en minúscula: ene, feb, mar,
       abr, may, jun, jul, ago, sep, oct, nov, dic.

    5. `export function formatEntryDate(recordedAt: string): string` — construye
       `new Date(recordedAt)` y devuelve `${d.getDate()} ${MONTHS_ES[d.getMonth()]}
       ${d.getFullYear()}` (formato `12 sep 2026`, D-21). Nunca `toLocaleDateString` ni
       `Intl` (los tests deben comparar cadenas exactas sin depender del locale del runner).
       Si la fecha no es parseable (`Number.isNaN(d.getTime())`) devuelve `'—'`: un dato
       ilegible se trata como ausencia, nunca como error.

    6. `export function formatEntryDuration(durationMs: number | null): string` — `'—'` para
       `null` o para cualquier valor no finito (D-10: nunca 0, nunca inventado). Si no,
       redondea a minutos (`Math.round(durationMs / 60000)`) y devuelve `${minutos} min`
       cuando hay menos de una hora, o `${horas} h ${minutos} min` cuando hay al menos una
       (formato `1 h 40 min`, D-21).

    7. `export function sortEntriesByRecency(entries: GameHistoryEntry[]):
       GameHistoryEntry[]` — devuelve un array NUEVO ordenado por `recordedAt` descendente
       (comparación de cadenas ISO, que es cronológica), sin mutar la entrada. Es la garantía
       mecánica de HIST-07 («de la más reciente a la más antigua») incluso si el array
       almacenado llegara desordenado por edición manual.

    Prohibido en este fichero: `Date.now()`, `toLocaleDateString`, `Intl`, cualquier import
    de `app/` o de zod, y cualquier mención a Firestore/Firebase/sync (D-14).
  </action>
  <verify>
    <automated>npx vitest run engine 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `engine/history.ts` existe y contiene `export function buildHistoryEntry(`
    - `grep -c "from './selection'" engine/history.ts` devuelve 1
    - `grep -Ec "Date\.now\(\)|toLocaleDateString|Intl\." engine/history.ts` devuelve 0
    - `grep -c "Se completó el Plan Principal" engine/history.ts` devuelve al menos 1 y `grep -c "Todos los héroes eliminados" engine/history.ts` devuelve al menos 1
    - `grep -Ei "firestore|firebase|~/|app/data" engine/history.ts` no devuelve ninguna línea
    - `grep -c "export function sortEntriesByRecency" engine/history.ts` devuelve 1
    - `npx vitest run engine` termina con código 0
  </acceptance_criteria>
  <done>El módulo construye una entrada completa desde una sesión y formatea fecha y duración con cadenas exactas, sin tocar `app/` ni el reloj real.</done>
</task>

<task type="auto">
  <name>Task 3: engine/__tests__/history.test.ts</name>
  <files>engine/__tests__/history.test.ts</files>
  <read_first>
    - `engine/__tests__/selection.test.ts` (estilo de fixtures, `describe` nombrado por el
      invariante en español, aserciones de no-mutación)
    - `engine/__tests__/header.test.ts` (aserciones de cadena exacta)
    - `engine/history.ts` (recién creado)
  </read_first>
  <action>
    Crear el test hermano. Construye a mano un `EngineSession` mínimo (no hace falta el
    fixture `tiny-game.json`: `buildHistoryEntry` solo lee `gameId`, `round` y `context`), con
    `sequence: []`, `cursor: 0` y el `context` que cada caso necesite.

    Bloques `describe` nombrados por la decisión que verifican, cubriendo como mínimo:
    - `D-06`: `outcome 'won'` produce `result 'won'` y `lossCause null`; cada una de las dos
      derrotas produce `result 'lost'` y su `lossCause` exacto. `describeLossCause` devuelve
      literalmente `'Se completó el Plan Principal'` y `'Todos los héroes eliminados'`.
    - `D-08/D-10`: con `startedAt` presente, `durationMs` es exactamente `now - startedAt`;
      sin `startedAt`, es `null`; con un `startedAt` posterior a `now` (dato manipulado), es
      `null`; con `startedAt` no numérico, es `null`.
    - `D-09`: `round` se copia tal cual (una sesión en `round: 7` guarda `7`, nunca `6`).
    - `D-11`: `heroName` sale de `names.heroNames`, no del id; un `heroId` ausente del mapa
      deja `heroName` en `null`.
    - `D-12`: un `context` sin `selection` produce `villainId: null`, `villainName: null` y
      `playerCount` huecos con `heroId: null` — y no lanza.
    - `D-15`: `gameId` se copia de la sesión.
    - `D-21`: `formatEntryDate` sobre un ISO conocido devuelve `'12 sep 2026'`;
      `formatEntryDuration(6000000)` devuelve `'1 h 40 min'`; `formatEntryDuration(120000)`
      devuelve `'2 min'`; `formatEntryDuration(null)` devuelve `'—'`; una cadena no parseable
      en `formatEntryDate` devuelve `'—'`.
    - `sortEntriesByRecency`: ordena descendente por `recordedAt` y devuelve un array nuevo
      (`expect(resultado).not.toBe(entrada)`), sin mutar el original.
    - No-mutación: `buildHistoryEntry` no modifica `session` ni `session.context`.

    Regla obligatoria para las fechas de los tests: usar instantes ISO a mediodía UTC (por
    ejemplo `2026-09-12T12:00:00.000Z`) para que el desplazamiento horario del entorno de CI
    no cambie el día. Documentarlo en un comentario del fichero.

    El `id` es aleatorio: no se compara con una cadena fija, se comprueba con la expresión
    regular `/^\d+-[a-z0-9]+$/` y que dos llamadas seguidas con el mismo `now` produzcan ids
    distintos no es obligatorio comprobarlo.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/history.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run engine/__tests__/history.test.ts` termina con código 0 y ejecuta al menos 15 tests
    - `grep -c "describe(" engine/__tests__/history.test.ts` devuelve al menos 6
    - `grep -c "D-10" engine/__tests__/history.test.ts` devuelve al menos 1 (los bloques citan la decisión)
    - `npm test` termina con código 0
  </acceptance_criteria>
  <done>Cada decisión de dato de la fase (D-06, D-08, D-09, D-10, D-11, D-12, D-15, D-21) tiene al menos un test que la fija.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → `SessionContext` | El `context` de una sesión reanudada viene de `localStorage`, editable a mano desde DevTools; `buildHistoryEntry` lo lee entero |
| engine/ → app/ | Frontera de pureza: `engine/` no puede alcanzar `app/data/spanish-hero-aliases.ts` ni ningún módulo que importe zod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `buildHistoryEntry` leyendo `context.selection` manipulado | mitigate | Leer SIEMPRE por `resolvePlayerSlots`/`resolveVillainId` de `engine/selection.ts`, ya normalizados por tipo y con test propio; nunca acceder a `context.selection` en crudo |
| T-09-02 | Tampering | `context.startedAt` manipulado (no numérico, o en el futuro) | mitigate | Guarda explícita `Number.isFinite` + `now >= startedAt`, si no `durationMs: null`; nunca se pinta un negativo ni un 0 inventado |
| T-09-03 | Information disclosure | `engine/types.ts` arrastrando zod al bundle del navegador | mitigate | Prohibición explícita de importar zod en `engine/types.ts` (precedente DC-03 de la Fase 5), con gate de grep en los criterios de aceptación |
| T-09-04 | Tampering | `recordedAt` ilegible haciendo que la tarjeta pinte `Invalid Date` | mitigate | `formatEntryDate` devuelve `'—'` ante una fecha no parseable — un dato ilegible se trata como ausencia, nunca como error |
</threat_model>

<verification>
- `npm test` en verde (los 3 proyectos de Vitest).
- `grep -rEi "firestore|firebase" engine/` no devuelve ninguna línea (criterio de éxito nº 5
  del ROADMAP).
- `git diff engine/persistence.ts` está vacío (D-07: la persistencia no se toca).
</verification>

<success_criteria>
- `engine/types.ts` exporta `LossCause`, `GameOutcome`, `HistoryPlayerEntry` y
  `GameHistoryEntry`, y `SessionContext` admite `startedAt`, sin bump de `formatVersion` ni
  de `contentVersion`.
- `engine/history.ts` construye una entrada completa (HIST-04) desde una sesión viva y
  formatea fecha y duración con cadenas deterministas.
- `engine/__tests__/history.test.ts` fija las ocho decisiones de dato listadas arriba.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-01-SUMMARY.md` al terminar.
</output>
