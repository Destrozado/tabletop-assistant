---
phase: 09-hist-rico-y-estad-sticas
plan: 08
type: execute
wave: 5
depends_on: ["09-06", "09-07"]
files_modified:
  - app/components/GameSelectorScreen.vue
  - app/pages/index.vue
  - nuxt.config.ts
  - e2e/offline-flow.spec.ts
autonomous: false
requirements: [STAT-01, HIST-07, HIST-08, HIST-09, STAT-04, STAT-05]

must_haves:
  truths:
    - "STAT-01: hay una pantalla de estadísticas accesible desde el inicio"
    - "D-17: los dos accesos son botones secundarios bajo las tarjetas de juego, sin reestructurar el bloque centrado"
    - "D-16/Pitfall 4: /historico y /estadisticas están enumeradas en nitro.prerender.routes y su HTML aparece en .output/public — crawlLinks es false y nadie las descubriría solas"
    - "Las dos rutas nuevas se abren con la red cortada tras instalar la PWA, verificado sobre una build real y no supuesto"
    - "TECH-04: GameSelectorScreen sigue sin mencionar ningún juego concreto"
    - "HIST-09: tras «Partida terminada» el histórico sigue completo, comprobado a mano en una partida real"
  artifacts:
    - path: "app/components/GameSelectorScreen.vue"
      provides: "dos accesos secundarios Histórico / Estadísticas"
      contains: "Estadísticas"
    - path: "nuxt.config.ts"
      provides: "las cuatro rutas prerenderizadas"
      contains: "'/historico'"
    - path: "e2e/offline-flow.spec.ts"
      provides: "prueba de que las dos rutas nuevas se abren sin red"
  key_links:
    - from: "app/pages/index.vue"
      to: "/historico y /estadisticas"
      via: "navigateTo en el manejador de los emits nuevos"
      pattern: "navigateTo\\('/historico'\\)"
    - from: "nuxt.config.ts"
      to: ".output/public/historico/index.html"
      via: "nitro.prerender.routes"
      pattern: "'/historico'"
---

<objective>
Cerrar la fase: los dos accesos desde la pantalla de inicio, el registro de las dos rutas
nuevas en el prerender (sin el cual fallarían offline), y la verificación humana del ciclo
completo jugando de verdad.

Propósito: STAT-01 pide explícitamente que las estadísticas sean accesibles «desde el inicio»,
y `PITFALLS.md` §9 documenta que una ruta nueva no enumerada funciona en desarrollo y falla la
primera vez que se abre sin red — el fallo más caro de esta fase, porque solo aparece en la
mesa.

Salida: `GameSelectorScreen.vue` y `app/pages/index.vue` con los accesos, `nuxt.config.ts` con
las cuatro rutas, una prueba de Playwright que abre las dos rutas nuevas sin red, y el visto
bueno humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-RESEARCH.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-06-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-07-SUMMARY.md

<interfaces>
Del repo (leído):
- `app/components/GameSelectorScreen.vue` — props `{ games: { id, title, status }[] }`, emit
  `select: [id]`, ref `pressedId` con press-feedback
  `mousedown`/`touchstart`/`mouseup`/`touchend` + `brightness-95 scale-[0.98]`; raíz
  `div.h-dvh.bg-background.flex.flex-col.items-center.justify-center.gap-2xl.px-2xl`, con el
  bloque de texto y la fila de tarjetas `div.flex.flex-wrap.items-center.justify-center.gap-lg`.
- `app/pages/index.vue` — `const { games } = useGameContent()`, `onSelect(gameId)` con
  `navigateTo('/' + gameId)`, plantilla de una sola línea.
- `nuxt.config.ts` — `nitro: { prerender: { crawlLinks: false, routes: ['/',
  '/marvel-champions'] } }`; el bloque `pwa.workbox.globPatterns` ya incluye
  `'**/*.{js,css,html}'` y NO necesita cambios.
- `e2e/offline-flow.spec.ts` — ayudante `waitForServiceWorkerControl(page)` y el test «la ruta
  /marvel-champions se puede abrir directamente sin red (page.goto)», que es la plantilla
  literal del test nuevo. `playwright.config.ts` levanta
  `npm run generate && npx nuxi preview` en el puerto 4173.
- Botón secundario de referencia (`ConfirmDialog.vue`, botón no destructivo):
  `min-h-12 px-lg bg-surface text-primary-text text-label font-bold transition-transform
  duration-75` + `brightness-95 scale-[0.98]` al pulsar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: accesos «Histórico» y «Estadísticas» en la pantalla de inicio (D-17/STAT-01)</name>
  <files>app/components/GameSelectorScreen.vue, app/pages/index.vue</files>
  <read_first>
    - `app/components/GameSelectorScreen.vue` (fichero a modificar; copiar su mecanismo de
      `pressedId` y su cabecera que declara la disciplina TECH-04)
    - `app/pages/index.vue` (fichero a modificar)
    - `app/components/ConfirmDialog.vue` (cromado del botón secundario a reutilizar)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §Layout 1 y
      §Copywriting Contract
  </read_first>
  <action>
    En `app/components/GameSelectorScreen.vue`, añadir DEBAJO de la fila de tarjetas de juego
    —sin reestructurar el bloque centrado, que sigue siendo `flex flex-col items-center
    justify-center gap-2xl` con «¿A qué juego vas a jugar?» como centro de la pantalla (D-17)—
    una fila propia `div.flex.gap-md` con dos botones de texto exacto `Histórico` y
    `Estadísticas`.

    Cromado idéntico al botón NO destructivo de `ConfirmDialog.vue`
    (`min-h-12 px-lg bg-surface text-primary-text text-label font-bold transition-transform
    duration-75` más `brightness-95 scale-[0.98]` al pulsar), que es la forma «secundaria/
    neutra» ya establecida en este código: así se leen como secundarios frente a las tarjetas
    grandes sin introducir ningún color ni tamaño nuevo. Reutilizar el mismo mecanismo de
    press-feedback que ya usan las tarjetas (un ref propio, o extender `pressedId` con las
    claves de estos dos controles).

    Emits nuevos: `open-history: []` y `open-statistics: []`. Ninguno de los dos botones se
    pinta nunca deshabilitado: las dos pantallas tienen estado vacío definido, así que no hay
    motivo para bloquear la entrada. El componente sigue sin mencionar ningún juego concreto
    (TECH-04).

    En `app/pages/index.vue`, escuchar los dos emits y navegar con `navigateTo('/historico')`
    y `navigateTo('/estadisticas')` — la navegación vive en la página, nunca dentro del
    componente tonto.
  </action>
  <verify>
    <automated>npx nuxt build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Histórico" app/components/GameSelectorScreen.vue` devuelve 1 y `grep -c "Estadísticas" app/components/GameSelectorScreen.vue` devuelve 1
    - `grep -c "open-history" app/components/GameSelectorScreen.vue` devuelve al menos 1 y `grep -c "open-statistics" app/components/GameSelectorScreen.vue` devuelve al menos 1
    - `grep -Eci "marvel|warhammer" app/components/GameSelectorScreen.vue` devuelve 0 (TECH-04 intacto)
    - `grep -c "navigateTo('/historico')" app/pages/index.vue` devuelve 1 y `grep -c "navigateTo('/estadisticas')" app/pages/index.vue` devuelve 1
    - `grep -Ec "disabled|aria-disabled" app/components/GameSelectorScreen.vue` devuelve 1 (solo el de la tarjeta «PRÓXIMAMENTE» que ya existía)
    - `npx nuxt build` termina con código 0
  </acceptance_criteria>
  <done>Desde el inicio se llega a las dos pantallas con un toque, sin cambiar la jerarquía de la pregunta principal.</done>
</task>

<task type="auto">
  <name>Task 2: prerender de las dos rutas nuevas y prueba offline (D-16/Pitfall 4)</name>
  <files>nuxt.config.ts, e2e/offline-flow.spec.ts</files>
  <read_first>
    - `nuxt.config.ts` (fichero a modificar; leer el comentario de `nitro.prerender` que
      explica por qué la lista es manual, y el bloque `pwa.workbox` para confirmar que NO
      necesita cambios)
    - `e2e/offline-flow.spec.ts` (fichero a modificar; el test «la ruta /marvel-champions se
      puede abrir directamente sin red» es la plantilla literal)
    - `playwright.config.ts` (cómo se levanta el servidor: `npm run generate && npx nuxi
      preview` en el puerto 4173)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-RESEARCH.md` §Pitfall 4
  </read_first>
  <action>
    1. En `nuxt.config.ts`, añadir `'/historico'` y `'/estadisticas'` al array
       `nitro.prerender.routes`, que pasa a ser `['/', '/marvel-champions', '/historico',
       '/estadisticas']`. Ampliar el comentario existente citando D-16: la navegación de la
       app usa `navigateTo()` en manejadores de click y `crawlLinks` sigue en `false`, así que
       cada ruta prerenderizable se enumera a mano; sin esto, `/historico` y `/estadisticas`
       no tendrían HTML que precachear y fallarían la primera vez que se abrieran sin red. NO
       tocar `routeRules`, ni el bloque `pwa`, ni `workbox.globPatterns` (el patrón
       `**/*.{js,css,html}` ya cubre el HTML nuevo) — y NO añadir `navigateFallback`.

    2. Ejecutar una build real (`npm run generate`) y comprobar que existen
       `.output/public/historico/index.html` y `.output/public/estadisticas/index.html`, y que
       ambas rutas aparecen en el manifiesto de precache que genera Workbox dentro de
       `.output/public/sw.js`. Esto se comprueba, no se supone.

    3. En `e2e/offline-flow.spec.ts`, añadir un test hermano del de `/marvel-champions`: tras
       `waitForServiceWorkerControl(page)`, con `context.setOffline(true)`, hacer `page.goto`
       de `/historico` y comprobar que se ve el título `HISTÓRICO` o el encabezado del estado
       vacío; y lo mismo con `/estadisticas`. Nombrar el test citando D-16/Pitfall 4. No
       relajar ni desactivar ninguna aserción existente del fichero.
  </action>
  <verify>
    <automated>npm run generate && ls .output/public/historico/index.html .output/public/estadisticas/index.html && grep -c "historico" .output/public/sw.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'/historico'" nuxt.config.ts` devuelve 1 y `grep -c "'/estadisticas'" nuxt.config.ts` devuelve 1
    - `git diff nuxt.config.ts | grep -Ec "^[-+].*(routeRules|globPatterns|navigateFallback|registerType)"` devuelve 0 (solo cambian `prerender.routes` y su comentario)
    - `npm run generate` termina con código 0 y `.output/public/historico/index.html` y `.output/public/estadisticas/index.html` existen
    - `grep -c "historico" .output/public/sw.js` devuelve al menos 1 (el HTML nuevo entra en el precache)
    - `npx playwright test e2e/offline-flow.spec.ts` termina con código 0 (si falta el navegador, ejecutar antes `npm run e2e:install`)
  </acceptance_criteria>
  <done>Las dos rutas nuevas se prerenderizan, se precachean y se abren con la red cortada, comprobado sobre una build real.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: verificación humana del ciclo completo</name>
  <files>—</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §Copywriting Contract (para
      contrastar los literales que se ven en pantalla)
    - `.planning/ROADMAP.md` §Phase 9 (los cinco criterios de éxito)
  </read_first>
  <action>
    Detener aquí y pedir al usuario la verificación humana descrita abajo. No continuar, no
    marcar la fase como completa y no escribir el SUMMARY hasta recibir su respuesta.
    Antes de pedirla, dejar la build real levantada (`npm run generate && npx nuxi preview`,
    puerto 4173) y pegar en el mensaje la lista numerada de pasos tal cual aparece en
    `<how-to-verify>`, para que el usuario no tenga que abrir el plan.
  </action>

  <what-built>
    El ciclo completo de la fase: registrar el resultado al terminar una partida, el histórico
    con borrado confirmado, las estadísticas con sus dos tablas, los accesos desde el inicio y
    el aviso de guardado. Todo en `localStorage`, sin una sola línea de Firestore.
  </what-built>
  <how-to-verify>
    Levantar la build real (`npm run generate && npx nuxi preview`, puerto 4173) y seguir estos
    pasos en orden, en un navegador cualquiera (no hace falta la tablet de mesa; el ROADMAP
    marca esta verificación como recomendable, no bloqueante por dispositivo):

    1. En `/`, comprobar que bajo las tarjetas de juego hay dos botones secundarios,
       `Histórico` y `Estadísticas`, y que la pregunta «¿A qué juego vas a jugar?» sigue
       siendo el centro de la pantalla.
    2. Abrir `Histórico` con el histórico vacío: debe verse «Todavía no hay partidas
       registradas» con su explicación, nunca un error ni una lista vacía sin texto. Probar el
       enlace cruzado `Estadísticas ›` y comprobar que la pantalla de estadísticas muestra
       «Todavía no hay estadísticas» y NINGUNA cabecera de tabla ni ningún `0 %`.
    3. Empezar una partida de Marvel Champions, elegir villano, héroes y al menos un nombre de
       jugador (dejar otro hueco sin nombre), avanzar unos pasos y entrar en el bucle de
       rondas hasta que la cabecera diga al menos «RONDA 2».
    4. Abrir el índice y pulsar «Partida terminada»: debe aparecer el diálogo con la pregunta
       «¿Cómo terminó la partida?», la línea de contexto (villano · N jug · dificultad · ronda
       N), el aviso de que se borrará el progreso guardado, y CUATRO opciones: `GANADA`,
       `PERDIDA · Se completó el Plan Principal`, `PERDIDA · Todos los héroes eliminados` y
       `Salir sin registrar`. Ninguna debe ir coloreada de verde ni de rojo.
    5. Pulsar `GANADA`: debe volverse al inicio y verse la banda `✓ Partida registrada`, que
       desaparece sola a los pocos segundos y también se puede cerrar con la `✕`.
    6. Abrir `Histórico`: la partida debe aparecer con resultado, fecha en formato `12 sep
       2026`, villano · dificultad · nº de jugadores, una línea por jugador (con «Jugador N»
       en el hueco sin nombre), y «Hasta la ronda N · X min». Confirmar que la ronda que pone
       es la MISMA que mostraba la cabecera al terminar.
    7. Jugar una segunda partida corta y terminarla con una de las dos derrotas: la tarjeta
       nueva debe aparecer LA PRIMERA y llevar debajo del resultado la causa exacta elegida.
    8. Terminar una tercera partida con `Salir sin registrar`: la partida debe cerrarse y
       volver al inicio SIN añadir ninguna entrada y SIN banda de aviso — y las dos entradas
       anteriores deben seguir en el histórico (HIST-09).
    9. En `Histórico`, pulsar `Borrar` en una tarjeta: debe abrirse el diálogo rojo citando esa
       partida concreta. Cancelar no debe borrar nada; confirmar debe quitar solo esa entrada.
    10. En `Estadísticas`, comprobar que los porcentajes cuadran con lo jugado, que el recuento
        («2 de 3») se lee con el mismo peso que el porcentaje, y que el orden es de mayor a
        menor porcentaje.
    11. Con las herramientas de desarrollo, cortar la red y recargar `/historico` y
        `/estadisticas`: las dos deben abrirse con sus datos intactos.
  </how-to-verify>
  <verify>
    <human-check>Los once pasos se comportan como se describe; cualquier discrepancia se anota
    literalmente antes de aprobar.</human-check>
  </verify>
  <acceptance_criteria>
    - El usuario responde «aprobado» o enumera las discrepancias observadas
    - Toda discrepancia queda registrada en el SUMMARY del plan, con la decisión de si se
      corrige en esta fase o se difiere
  </acceptance_criteria>
  <resume-signal>Escribe «aprobado» o describe los problemas encontrados</resume-signal>
  <done>El grupo ha visto funcionar el ciclo completo con datos reales y ha dado el visto bueno.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build → dispositivo offline | Lo que no entra en el precache no existe cuando se cae la wifi a media partida |
| Pantalla de inicio → rutas nuevas | Único punto de entrada a las dos pantallas desde fuera de ellas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-25 | Denial of service | Ruta nueva no prerenderizada, inaccesible sin red | mitigate | Enumerar las dos rutas en `nitro.prerender.routes` y COMPROBARLO sobre una build real (`.output/public/*/index.html` + `sw.js`) más un test de Playwright con `setOffline(true)` — no se da por hecho |
| T-09-26 | Tampering | Cabeceras de caché tocadas por error al editar `nuxt.config.ts` | mitigate | Criterio de aceptación con `git diff` que exige que solo cambien `prerender.routes` y su comentario; `routeRules` (incluido el `no-cache` de `/sw.js`) no se toca |
| T-09-27 | Repudiation | Fase dada por buena sin haber visto el ciclo completo con datos reales | mitigate | Checkpoint humano bloqueante de once pasos que ejercita registrar, no registrar, listar, borrar, agregar y abrir sin red |
</threat_model>

<verification>
- `npm test` en verde.
- `npm run generate` con código 0 y las cuatro rutas presentes en `.output/public`.
- `npx playwright test e2e/offline-flow.spec.ts` con código 0.
- `grep -rEi "firestore|firebase" app/ engine/ nuxt.config.ts` no devuelve ninguna línea — la
  comprobación literal del criterio de éxito nº 5 del ROADMAP.
- Checkpoint humano aprobado.
</verification>

<success_criteria>
- Las estadísticas y el histórico son accesibles desde el inicio con un toque (STAT-01).
- Las cuatro rutas se prerenderizan y las dos nuevas se abren sin red.
- El ciclo completo queda verificado a mano por el usuario, con las discrepancias (si las hay)
  registradas.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-08-SUMMARY.md` al terminar.
</output>
