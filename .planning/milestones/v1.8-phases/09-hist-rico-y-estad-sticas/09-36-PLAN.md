---
phase: 09-hist-rico-y-estad-sticas
plan: 36
type: execute
wave: 29
depends_on: ["09-32", "09-33", "09-34", "09-35"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
  - .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
autonomous: true
requirements: [HIST-04, HIST-06, HIST-09]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`REQUIREMENTS.md` registra que la ronda 7 encontró una OCTAVA variante del mismo patrón, dentro de la propia copy escrita para cerrar la séptima, y HIST-06 sigue `[ ]` — el hueco no se da por cerrado en ningún sitio"
    - "El contrato de copy de la fase describe el texto que la app enseña de verdad tras este lote, incluidas las tres frases que cambian y la que nace"
    - "Lo que este lote deja sin cerrar está registrado con una evaluación de RIESGO que se sostiene sola —por qué la marca no se escribe en el dispositivo y qué pasa si el navegador recarga— nunca con un motivo de alcance de plan"
    - "`ROADMAP.md` cuenta los planes reales de la fase y cita la verificación vigente, sin marcar la fase como completa"
    - "La comprobación visual humana en tablet (DEV-02) sigue nombrada como ABIERTA, en ningún sitio como hecha"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "el párrafo de la ronda 7 y la tabla de trazabilidad sincronizada a mano"
      contains: "ronda 7"
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md"
      provides: "el contrato de copy con los textos reales tras el lote 09-32..09-35"
      contains: "no coincide con el punto en el que habéis terminado"
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md"
      provides: "la deuda de la marca en memoria, evaluada por riesgo, y el repaso de lo que sigue abierto"
      contains: "(ronda 7)"
    - path: ".planning/ROADMAP.md"
      provides: "la entrada de la Fase 9 con el recuento real de planes y la nota del lote de cierre"
      contains: "09-36"
  key_links:
    - from: ".planning/REQUIREMENTS.md (nota de HIST-06)"
      to: "app/composables/__tests__/afirmacionesRespaldadas.test.ts (criterio por raíces)"
      via: "la nota nombra el mecanismo concreto que cierra la octava cara, no una intención"
      pattern: "RAICES_SOBRE_LOS_DATOS_DEL_GRUPO"
  prohibitions:
    - "No se marca HIST-06 como `[x]` ni se escribe en ningún sitio que el hueco quede cerrado: ocho caras en siete rondas han enseñado que solo una verificación independiente puede decir eso, y cinco notas anteriores de este mismo documento dieron por cerrado lo que no lo estaba"
    - "No se escribe que la comprobación visual humana en tablet (DEV-02) esté hecha ni resuelta"
    - "Ninguna entrada de `deferred-items.md` se justifica por «estaba fuera del alcance del plan»: la justificación tiene que ser una evaluación de riesgo que se sostenga sin conocer los planes"
    - "No se toca ningún fichero de código desde este plan"
---

<objective>
Poner la documentación de la fase al día con lo que el código hace de verdad **después** de que el
código exista — nunca antes, y nunca prometiendo más de lo verificado.

El quinto `missing:` del gap de SC3 lo pide de forma explícita: *«Sincronizar `REQUIREMENTS.md`: la
nota de HIST-06 debe registrar que esta ronda (7ª) encontró una octava variante del mismo patrón,
dentro de la propia copy escrita para cerrar la séptima — no dar el hueco por cerrado.»*

Este plan añade además tres cosas que el lote deja pendientes de escribir: el contrato de copy con
los textos reales (tres cambian, uno nace), la deuda explícita de la marca en memoria (qué protege,
qué no, y por qué no se escribe en el dispositivo), y el recuento real de planes en el ROADMAP.

Purpose: que la próxima ronda de verificación encuentre la documentación diciendo exactamente lo
que el árbol hace, ni una palabra más.
Output: `REQUIREMENTS.md`, `ROADMAP.md`, `deferred-items.md` y `09-UI-SPEC.md` al día.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-32-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-33-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-34-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-35-SUMMARY.md
</context>

<scope_boundary>
**DENTRO:** los cuatro ficheros de `files_modified`.

**FUERA:**
- Todo el código. Si al escribir la documentación se descubre que el código no hace lo que un
  SUMMARY dice, **no se arregla el código aquí**: se anota como hallazgo en el SUMMARY de este
  plan, con fichero y línea, para que la verificación siguiente lo vea.
- `09-AUDIT-AFIRMACIONES-UI.md` → no se toca en este lote: el criterio ejecutable vive ahora en el
  gate (plan 09-34) y duplicarlo en prosa es precisamente lo que produjo un método auditado que no
  coincidía con el gate. Se deja constancia de esta decisión en el SUMMARY.
</scope_boundary>

<tracer_deviation>
`TRACER_MODE` está activo y este plan **no** lleva una tarea `type="tracer"`. El motivo, explícito:
un tracer es la rebanada más fina que atraviesa todas las capas de ejecución, y este plan no toca
ninguna capa de ejecución — modifica cuatro documentos de `.planning/`. Forzar una tarea etiquetada
como tracer aquí sería una etiqueta sin contenido, que es la misma clase de afirmación sin respaldo
que toda la fase persigue. Las tres tareas son `type="auto"`.
</tracer_deviation>

<flagged_assumptions>
**Cobertura de bordes sin resolver (no se descarta en silencio).** Las 14 filas de la sonda de
bordes (HIST-01..HIST-09, STAT-01..STAT-05) volvieron `unclassified`/`unresolved`; ninguna se ha
convertido en truth de respaldo. Quedan como suposición explícita del planificador y este plan las
deja registradas en `deferred-items.md` (Task 2, punto 4) para que la ronda siguiente sepa que la
sonda no clasificó nada, en vez de suponer que sí.

**Sin `## UI Considerations` que levantar.** `09-UI-SPEC.md` no contiene esa sección; sus
encabezados son Design System, Spacing Scale, Typography, Color, Layout, Component Inventory,
Interaction & State Coverage, Copywriting Contract, Registry Safety, Decisions Made Without User
Input, Forward-Compatibility Notes y Checker Sign-Off. Lo que este plan actualiza es el
**Copywriting Contract**.
</flagged_assumptions>

<artifacts_this_phase_produces>
Este plan no crea símbolos de código. Produce:

| Artefacto | Tipo | Dónde |
|---|---|---|
| Párrafo «Ronda 7» de la nota de HIST-06 | sección nueva de documento | `.planning/REQUIREMENTS.md` |
| Fila «Save-result notice — failure, `failure-stale` (ronda 7, plan 09-32)» | fila de tabla nueva/reescrita | `09-UI-SPEC.md` |
| Fila «Resume prompt — aviso de progreso que no coincide (ronda 7, plan 09-33)» | fila de tabla nueva | `09-UI-SPEC.md` |
| Entrada «La marca de progreso que no coincide es en memoria (ronda 7)» | entrada nueva | `deferred-items.md` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: `REQUIREMENTS.md` registra la octava cara y no da nada por cerrado</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` líneas 75-165 — los checkboxes de HIST-01..HIST-09 y la nota de
      cierre de hueco completa, con sus párrafos de las rondas 2-3, 4, 5 y 6. El párrafo de la
      ronda 6 termina con la frase que hay que honrar: *«Este cierre no se da por bueno hasta que
      una ronda de verificación independiente lo confirme: cuatro notas anteriores de este mismo
      documento dieron por cerrado lo que no lo estaba.»*
    - `.planning/REQUIREMENTS.md` líneas 290-305 — la tabla de trazabilidad (filas HIST-04,
      HIST-06, HIST-09) y la línea 223 (DEV-02).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el gap de SC3 y el del
      gate de clase, completos.
    - Los cuatro SUMMARY del lote (`09-32`..`09-35`) — para escribir qué se hizo de verdad, no qué
      se planeó.
  </read_first>
  <action>
    1. **Añadir el párrafo «Ronda 7»** al final de la nota de cierre de hueco de HIST-06,
       siguiendo el formato de los párrafos de las rondas 4, 5 y 6. Tiene que decir, sin
       suavizarlo:
       - **El hallazgo en una frase:** el cierre de la ronda 6 sustituyó la pregunta «¿hay alguna
         partida reanudable?» por «¿es ESTA?» —eso quedó genuinamente cerrado—, pero la copy
         escrita para anunciar la respuesta nueva afirmaba tres hechos que la comparación no
         establece: anterioridad temporal (imposible, `updatedAt` queda excluido por escrito),
         identidad de partida (`runtimeId` identifica un paso, no una instancia) y diferencia de
         ronda — esta última **literalmente falsa** en el escenario canónico que el propio plan
         09-28 construyó como test de regresión, donde la diferencia real está en `runtimeId` y las
         dos rondas valen 1.
       - **El agravante, sin suavizarlo:** `endGameBody` prometía un reintento que 2 de los 4
         estados del dispositivo contradicen, y el motivo escrito que lo auditaba afirmaba por
         escrito que era «cierto en las cuatro salidas». Un motivo falso sellando un hueco es peor
         que no tener excepción.
       - **Qué se ha hecho** (con los planes): 09-32 reescribe la copy y saca la del diálogo a
         `app/composables/useGameEndCopy.ts` con test puro; 09-33 transporta la discrepancia
         comprobada hasta el modal de reanudación con una marca en memoria; 09-34 cambia el
         criterio del gate de subcadenas cerradas a raíces léxicas, obliga a que cada excepción
         nombre un fichero que la respalde y cierra los huecos de Gate B y Gate C; 09-35 hace que
         Gate S ejerza las funciones de decisión y demuestre por mutación que se ponen rojas.
       - **Qué NO se ha hecho**, con su motivo: no se ha convertido `esLaMismaPartida` en un
         resultado con motivo (ver el razonamiento de 09-32, que el párrafo resume en dos líneas:
         no arreglaría la anterioridad, multiplicaría por seis la superficie de afirmación, y no
         cambiaría la acción del grupo porque `buildHistoryEntry` solo lee `round` y `context`); y
         la marca de discrepancia no se escribe en el dispositivo (ver `deferred-items.md`).
       - **La frase de cierre, repetida a propósito:** este cierre no se da por bueno hasta que una
         ronda de verificación independiente lo confirme; cinco notas anteriores de este documento
         dieron por cerrado lo que no lo estaba.

    2. **HIST-06 se queda `[ ]`.** Comprobarlo explícitamente y dejarlo dicho en el SUMMARY.

    3. **Sincronizar la tabla de trazabilidad a mano:**
       - `HIST-06`: rango de planes a `Fase 9 (09-13..09-36)`; estado, que la octava variante se
         encontró en la ronda 7 y sigue pendiente de confirmación independiente.
       - `HIST-04`: añadir que la rama de reintento sobre un snapshot que no coincide queda además
         señalizada al reentrar (plan 09-33), sin afirmar que el riesgo desaparezca — la marca es
         en memoria.
       - `HIST-09`: actualizar el rango de planes a `09-13..09-36`, manteniendo `Satisfecho`.

    4. **DEV-02** (línea ~223): añadir al guion de pruebas pendientes los dos puntos nuevos de este
       lote (el texto nuevo de la variante `failure-stale`, 20 s, y el aviso de progreso que no
       coincide dentro del modal de reanudación), dejando la comprobación marcada como pendiente.
       No escribir en ningún sitio que esté hecha.
  </action>
  <verify>
    <automated>grep -c "Ronda 7" .planning/REQUIREMENTS.md</automated>
    <fails_when>devuelve 0, o el comando sale con código distinto de 0 porque el patrón no aparece</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*HIST-06\*\*" .planning/REQUIREMENTS.md` devuelve 1 — el requisito sigue sin marcar.
    - `grep -c "09-13..09-36" .planning/REQUIREMENTS.md` devuelve 2 (filas HIST-06 y HIST-09).
    - `grep -c "useGameEndCopy" .planning/REQUIREMENTS.md` devuelve al menos 1 — la nota nombra el mecanismo concreto, no una intención.
    - `grep -c "ronda de verificación independiente" .planning/REQUIREMENTS.md` devuelve al menos 2 (la frase de la ronda 6 y la de la ronda 7).
    - `grep -c "DEV-02" .planning/REQUIREMENTS.md` devuelve al menos 1 y la línea correspondiente sigue describiendo la comprobación como pendiente — pegar la línea completa en el SUMMARY.
    - El SUMMARY cita, textualmente, el párrafo «Ronda 7» tal como queda escrito.
  </acceptance_criteria>
  <done>
    La nota de HIST-06 cuenta la octava cara con la misma franqueza que las siete anteriores, y no
    hay ni una línea en el documento que dé el hueco por cerrado.
  </done>
</task>

<task type="auto">
  <name>Task 2: la deuda que este lote deja, evaluada por riesgo y no por alcance de plan</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` — COMPLETO, con atención al
      repaso final («Lo que sigue ABIERTO») y al formato de las entradas anteriores
      (Encontrado durante / Descripción / Por qué no se corrige aquí / Acción sugerida).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-33-SUMMARY.md` — cómo quedó la marca en
      memoria y qué comprobó el ejecutor sobre la navegación de cliente.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el `human_verification` y
      el cuarto `missing:` de SC3, que admite explícitamente documentar esto como deuda.
  </read_first>
  <action>
    1. **Entrada nueva: «La marca de progreso que no coincide vive en memoria (ronda 7)».**
       Formato de las entradas existentes, y en «Por qué no se corrige aquí» una **evaluación de
       riesgo que se sostenga sin conocer ningún plan**:
       - `'stale'` solo es alcanzable cuando el registro del histórico **y** el guardado de cierre
         han fallado los dos; escribir la marca en `localStorage` en ese instante es la operación
         menos fiable del sistema, y una marca que no se puede escribir en su propio escenario no
         es una mitigación.
       - Lo que la marca en memoria SÍ cubre: el recorrido real del grupo, `/{juego}` → `/` →
         `/{juego}`, que es navegación de cliente y no recarga la página (registrar cómo se
         comprobó, según `09-33-SUMMARY.md`).
       - Lo que NO cubre: una recarga completa, cerrar la pestaña, o reabrir la app al día
         siguiente. En esos casos el modal de reanudación vuelve a ofrecer el snapshot sin aviso.
       - **Por qué el riesgo residual es aceptable hoy, dicho como riesgo y no como excusa:**
         cuando la marca no está, la app no afirma nada (no dice algo falso), y la copy del aviso
         de fin de partida ya no ordena el reintento en esa variante. Lo que se pierde es la
         advertencia, no la corrección.
       - **Acción sugerida** si algún día deja de ser aceptable: una clave hermana
         (`tga:progress-mismatch:<gameId>`) escrita **en el siguiente arranque con éxito** —cuando
         el almacenamiento vuelve a funcionar— en vez de en el instante del fallo.

    2. **Actualizar el repaso final** («Lo que sigue ABIERTO») con el estado tras este lote: qué
       cierra cada plan 09-32..09-35 y qué sigue abierto. Nombrar uno a uno, con su plan, y sin
       dar por cerrado nada que no lo esté.

    3. **DEV-02 sigue ABIERTA**, con sus dos puntos nuevos de guion. La entrada existente ya lo
       dice; ampliarla, nunca sustituirla por una versión que suene a resuelta.

    4. **Entrada nueva: «La sonda de cobertura de bordes no clasificó ninguna fila (ronda 7)».**
       Registrar como hecho, no como problema resuelto: la sonda determinista se ejecutó sobre los
       14 IDs de la fase y devolvió las 14 como `unclassified`/`unresolved`, así que ninguna
       garantía de este lote se apoya en ella. Es información que la ronda siguiente necesita para
       no confundir «la sonda no dijo nada» con «la sonda dijo que estaba bien».
  </action>
  <verify>
    <automated>grep -c "(ronda 7)" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md</automated>
    <fails_when>devuelve 0, o el comando sale con código distinto de 0 porque el patrón no aparece</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "tga:progress-mismatch" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1 — la acción sugerida es concreta, no genérica.
    - `grep -c "DEV-02" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1 y ninguna de esas líneas la describe como hecha — pegar las líneas en el SUMMARY.
    - `grep -ci "fuera del alcance de este plan" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` no aumenta respecto al valor previo: registrar en el SUMMARY el recuento antes y después, y comprobar que ninguna de las **entradas nuevas** usa esa justificación.
    - `grep -c "unclassified" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 1.
    - El SUMMARY cita textualmente la sección «Por qué no se corrige aquí» de la entrada nueva de la marca.
  </acceptance_criteria>
  <done>
    Lo que este lote deja abierto está escrito con una evaluación de riesgo que se lee sin conocer
    ningún plan, y nada de lo que sigue abierto aparece como cerrado.
  </done>
</task>

<task type="auto">
  <name>Task 3: el contrato de copy dice el texto que la app enseña, y el ROADMAP cuenta los planes reales</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §Copywriting Contract completa
      (a partir de la línea 525), con atención a la fila «Outcome dialog retained warning» y a la
      fila «Save-result notice — failure, `failure-stale`», que son las dos que este lote deja
      obsoletas, y a la nota al pie de la ronda 6 que explica el formato de citación literal.
    - `app/composables/useHistorySavedNotice.ts`, `app/composables/useGameEndCopy.ts` y
      `app/composables/useProgressMismatchMark.ts` **en el árbol, tras el lote** — los textos se
      copian del código, nunca de los planes: es la única forma de que la tabla diga lo que la app
      enseña de verdad.
    - `.planning/ROADMAP.md` — la entrada de la Fase 9 en la lista de fases y su bloque de detalle.
  </read_first>
  <action>
    1. **`09-UI-SPEC.md` §Copywriting Contract**, cuatro cambios:
       - Reescribir la fila «Outcome dialog retained warning» citando literalmente
         `buildEndGameBody` de `app/composables/useGameEndCopy.ts`, y explicando en la misma celda
         qué se retiró y por qué: la promesa de reintento se emitía antes de conocer el estado del
         dispositivo y era falsa en 2 de los 4 estados; lo que queda es lo que
         `preserveProgress = !historyRecorded` garantiza en las cuatro salidas. Citar
         `09-VERIFICATION.md` ronda 7.
       - Reescribir la fila de `failure-stale` citando literalmente el `NOTICE_BODY` actual, y
         nombrar las dos afirmaciones retiradas (anterioridad y ronda) con el motivo de cada una.
       - Añadir una fila nueva: «Resume prompt — aviso de progreso que no coincide (ronda 7, plan
         09-33)», citando literalmente `PROGRESS_MISMATCH_WARNING`, y diciendo cuándo se muestra
         (solo cuando el último cierre de partida de ese juego encontró discrepancia y el progreso
         que dejó sigue ahí) y qué no garantiza (una recarga completa la pierde).
       - Añadir una nota al pie de la ronda 7, en el mismo formato que la de la ronda 6,
         explicando que las filas citan el texto literal del código tras el lote 09-32..09-35.

    2. **`.planning/ROADMAP.md`**, entrada de la Fase 9:
       - Actualizar el recuento de planes al número real tras este lote (36) en la línea de la lista
         de fases y, si el bloque de detalle de la fase lleva lista de planes, añadir las entradas
         `09-32`..`09-36` con su objetivo en una línea.
       - Actualizar la nota para que describa el lote de cierre de la ronda 7 y lo que cierra, sin
         afirmar que la fase esté verificada.
       - **La casilla de la Fase 9 sigue `[ ]`.** Comprobarlo explícitamente.
       - Usar `Edit` con reemplazos acotados, nunca reescribir el fichero entero: el ROADMAP
         contiene todas las demás fases del hito.

    3. Registrar en el SUMMARY la línea del ROADMAP tal como queda, entera.
  </action>
  <verify>
    <automated>grep -c "PROGRESS_MISMATCH_WARNING" .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md</automated>
    <fails_when>devuelve 0, o el comando sale con código distinto de 0 porque el patrón no aparece</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "no coincide con el punto en el que habéis terminado" .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` devuelve al menos 1 — la tabla cita el texto real de `failure-stale`.
    - `grep -c "el progreso no se borrará y la app os avisará" .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` devuelve al menos 1 — la tabla cita el texto real del diálogo.
    - `grep -c "ronda 7" .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` devuelve al menos 3.
    - `grep -c "09-36" .planning/ROADMAP.md` devuelve al menos 1 y `grep -c "36/36" .planning/ROADMAP.md` devuelve 1.
    - `grep -c "^- \[ \] \*\*Phase 9:" .planning/ROADMAP.md` devuelve 1 — la fase sigue sin marcar.
    - `git diff --stat -- .planning/ROADMAP.md` muestra un número de líneas cambiadas compatible con un reemplazo acotado (pegar la cifra en el SUMMARY); si superara las 20 líneas, explicar por qué.
    - Comprobación cruzada, a registrar en el SUMMARY: para cada uno de los tres textos citados en la tabla, la cadena citada aparece literalmente en el fichero `.ts` correspondiente (`grep -F` con la cadena completa). Si alguna no coincide carácter a carácter, corregir la tabla, nunca el código.
  </acceptance_criteria>
  <done>
    El contrato de copy cita el texto que la app enseña de verdad, comprobado carácter a carácter
    contra el código, y el ROADMAP cuenta los planes reales sin declarar verificada la fase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentación → ronda de verificación siguiente | Lo que estos documentos afirman es la base sobre la que la ronda siguiente decide qué re-comprobar; una afirmación falsa aquí desvía esa ronda |
| SUMMARY de un plan → documentación | Un SUMMARY puede afirmar más de lo que su plan hizo; copiar de él sin contrastar propaga el error |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-36-01 | Repudiation | La documentación dando por cerrado un hueco que no lo está (ha ocurrido cinco veces en este mismo documento) | high | mitigate | Prohibición explícita de marcar HIST-06, criterio de aceptación por `grep` sobre la casilla, y repetición de la frase de confirmación independiente |
| T-09-36-02 | Spoofing | El contrato de copy citando un texto que el código no tiene | medium | mitigate | Los textos se copian del `.ts` en el árbol y se comprueban con `grep -F` carácter a carácter |
| T-09-36-03 | Tampering | Reescribir `ROADMAP.md` entero y perder las entradas de las demás fases del hito | medium | mitigate | Instrucción explícita de usar `Edit` acotado y criterio de aceptación sobre el tamaño del diff |
| T-09-36-04 | Repudiation | Una deuda justificada por «alcance del plan» en vez de por riesgo | medium | mitigate | Prohibición explícita + criterio de aceptación que compara el recuento antes/después de esa fórmula |
| T-09-36-SC | Tampering | Instalaciones de paquetes (npm) | low | accept | Este plan no toca código ni dependencias: `git diff --stat -- package.json package-lock.json app/ engine/` debe quedar vacío |
</threat_model>

<verification>
- `git diff --stat -- app/ engine/ package.json package-lock.json` vacío: este plan no toca código.
- `git diff --name-only` lista exactamente los cuatro ficheros de `files_modified`.
- `npm test` sale 0 (regresión rápida: la documentación no puede haber roto nada, pero se ejecuta
  para dejar constancia del estado del árbol al cerrar el lote).
</verification>

<success_criteria>
- La nota de HIST-06 registra la octava cara con sus dos hallazgos y su agravante, y no hay
  ninguna línea que dé el hueco por cerrado.
- El contrato de copy cita los textos reales, comprobados contra el código.
- La deuda de la marca en memoria está escrita como riesgo evaluado, con una acción sugerida
  concreta.
- El ROADMAP cuenta 36 planes y la Fase 9 sigue sin marcar.
- DEV-02 sigue abierta en los dos documentos donde aparece.
</success_criteria>

<output>
Create `.planning/phases/09-hist-rico-y-estad-sticas/09-36-SUMMARY.md` when done
</output>
