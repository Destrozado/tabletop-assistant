---
phase: 09-hist-rico-y-estad-sticas
plan: 29
type: execute
wave: 22
depends_on: ["09-28"]
gap_closure: true
files_modified:
  - app/composables/useProgressMountPlan.ts
  - app/composables/__tests__/useProgressMountPlan.test.ts
  - app/components/MiniSetupScreen.vue
  - app/pages/[game]/index.vue
  - app/composables/__tests__/avisoTrasRegistroFallido.test.ts
autonomous: true
requirements: [HIST-06, HIST-09]

must_haves:
  truths:
    - "El montaje de la pantalla de juego consume lo que la app puede AFIRMAR (`informe.stored`), no solo lo que puede HACER (`informe.outcome`): «he comprobado el dispositivo y no hay nada» y «no he podido comprobarlo» dejan de ser el mismo camino"
    - "Cuando la lectura del progreso ha fallado, la app se lo dice al grupo con una frase que afirma exactamente eso y nada más, en vez de presentar el mini-setup como si hubiera comprobado el dispositivo"
    - "El grupo sabe, antes de empezar una partida nueva sobre una lectura fallida, que al guardar la nueva podría sustituir una anterior que la app no consigue ver"
    - "Ningún test de la suite fija en verde el comportamiento que la ronda 6 declaró defectuoso: el escenario de snapshot desactualizado exige ahora la variante que no promete identidad de partida"
    - "Existe un test que siembra un autoguardado ANTERIOR con posición distinta de la partida que termina y falla si la app vuelve a decir «sigue guardada»"
  artifacts:
    - path: "app/composables/useProgressMountPlan.ts"
      provides: "la decisión de montaje como función pura y total sobre los cuatro valores de StoredProgress, con su copy como dato"
      contains: "planProgressMount"
    - path: "app/composables/__tests__/useProgressMountPlan.test.ts"
      provides: "tabla de verdad de la decisión de montaje"
      contains: "planProgressMount"
    - path: "app/pages/[game]/index.vue"
      provides: "onMounted consumiendo informe.stored a través de la función pura"
      contains: "planProgressMount"
    - path: "app/composables/__tests__/avisoTrasRegistroFallido.test.ts"
      provides: "el test 5 corregido y el test nuevo del snapshot desactualizado"
      contains: "failure-stale"
  key_links:
    - from: "app/pages/[game]/index.vue (onMounted)"
      to: "app/composables/useProgressMountPlan.ts (planProgressMount)"
      via: "el montaje pasa informe.stored y informe.outcome a la decisión pura"
      pattern: "planProgressMount\\(informe\\.stored, informe\\.outcome\\)"
    - from: "app/composables/useProgressMountPlan.ts"
      to: "app/components/MiniSetupScreen.vue"
      via: "la copy del aviso de lectura fallida viaja como prop, nunca escrita en la plantilla"
      pattern: "unverifiedProgressNotice"
---

<objective>
Cerrar la mitad de montaje del Gap #1 y el hallazgo CR-02 de `09-REVIEW.md`, y desactivar el test
que fija el defecto de la ronda 6 como comportamiento esperado.

`onMounted` (`app/pages/[game]/index.vue:158-163`) consume `informe.outcome`, no `informe.stored`.
Como `'absent'` y `'unknown'` producen los dos `outcome: 'fresh'`, el montaje **no puede
distinguir** «he comprobado el dispositivo y no hay nada» de «no he podido comprobarlo» — que es
literalmente la distinción que `StoredProgress` se creó para imponer. Y el aviso de fallo le pedía
al grupo que usara la aparición del mini-setup como oráculo de ausencia («si os pide jugadores y
dificultad, esa partida ya no está»), justo la pantalla que la app enseña **cuando no sabe**. El
plan 09-28 ya retiró esa frase del aviso; este plan cierra la otra mitad: que el montaje deje de
presentar el mini-setup como si hubiera comprobado el dispositivo.

Y cierra un agravante confirmado por la verificación: el test 5 de `avisoTrasRegistroFallido.test.ts`
—el propio test de regresión que el plan 09-26 escribió para cerrar el BLOCKER de la ronda 5—
siembra el escenario exacto de la séptima cara (tres autoguardados válidos con `cursor: 0/1/2` y un
cierre fallido con `cursor: 3`) y **afirma en verde** que la variante correcta es
`failure-recoverable`, sin comprobar que lo que hay en el dispositivo sea la partida que terminó.
Protege la regresión de la ronda 5 mientras consolida la de la ronda 6.

Purpose: que ninguna pantalla de la app —ni por lo que dice ni por lo que enseña— dé por comprobado
algo que no se ha comprobado, y que ningún test siga certificando lo contrario.
Output: `planProgressMount` como decisión pura y total, el aviso de lectura fallida en el
mini-setup, y `avisoTrasRegistroFallido.test.ts` sin el test contraproducente y con el de la
séptima cara.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-28-SUMMARY.md
</context>

<scope_boundary>
**DENTRO:** los cinco ficheros de `files_modified`.

**FUERA:**
- `onOutcomeRecorded` y todo lo que 09-28 dejó cerrado (la firma de `readStoredProgress`, la
  variante `failure-stale`, el `try/catch` de WR-06, el comentario de WR-05) → ya está hecho. Si
  algo de eso parece mal, se dice en el SUMMARY; no se rehace aquí.
- `app/composables/__tests__/afirmacionesRespaldadas.test.ts` (el gate de clase) → es el plan
  **09-30**, que corre después precisamente para poder demostrar que detecta lo que antes no
  detectaba. **Única excepción:** si la copy nueva de este plan pone el gate en rojo, NO se
  silencia aquí ni se retoca el gate: se deja constancia en el SUMMARY de qué frase lo dispara y
  se le pasa a 09-30, que es el plan que decide qué se audita y con qué motivo escrito. Si eso
  dejara la suite en rojo entre olas, se añade a `FICHEROS_CON_AFIRMACION_AUDITADA` la ruta del
  fichero nuevo **con un `TODO(09-30)` explícito al lado**, y 09-30 lo sustituye por la entrada
  definitiva.
- **Cortar o condicionar el autoguardado** (`watchDebounced`/`pagehide`) cuando la lectura falló →
  FUERA. Es la consecuencia nº 1 que WR-02 describe (el primer autoguardado de una partida nueva
  machaca una posición que quizá seguía ahí) y arreglarla bien exige decidir qué pasa con una
  partida que el grupo ya ha empezado, lo que es diseño de UX, no un cierre de huecos. Este plan
  cubre la parte honesta y barata —decírselo al grupo antes de que empiece— y el resto se registra
  como riesgo evaluado en `deferred-items.md` (plan 09-31). No dejarlo en el limbo: si al ejecutar
  se decide no hacerlo, tiene que quedar escrito ahí.
- `engine/**` → no se toca.
- `.planning/**` → es el plan **09-31**.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: la decisión de montaje deja de ser cuatro líneas en `onMounted` y pasa a ser una función pura y total</name>
  <files>app/composables/useProgressMountPlan.ts, app/composables/__tests__/useProgressMountPlan.test.ts</files>
  <read_first>
    - `app/composables/useStoredProgress.ts` tal como lo dejó 09-28 — los CUATRO valores de
      `StoredProgress`, la firma con `esperada` opcional y el invariante reescrito
      («`(stored === 'resumable' || stored === 'stale')` ⟺ `outcome !== 'fresh'`»). Importa
      entender que **sin `esperada` la función nunca devuelve `'stale'`**, y el montaje no le pasa
      `esperada` porque no tiene ninguna sesión con la que comparar.
    - `app/composables/useHistorySavedNotice.ts` tal como lo dejó 09-28 — es el modelo a imitar:
      decisión pura (`planGameEnd`), copy como dato exportado (`NOTICE_BODY`), y el comentario que
      explica por qué la copy no vive en la plantilla.
    - `app/pages/[game]/index.vue` — el `onMounted` completo (~146-170) y sus comentarios, incluido
      el de «Primer consumidor de la autoridad (plan 09-26)» y la referencia a Pitfall 7 (nada de
      esto ocurre durante el prerender).
    - `engine/persistence.ts` — el tipo `ResumeOutcome` (`'fresh' | 'resumed' | 'content-changed'`).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §CR-02 y §WR-02 completos — la
      enumeración de los TRES caminos que llevan al mini-setup y por qué el montaje no puede
      distinguirlos hoy.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — Gap #1, tercer punto de
      su lista `missing:`, y la fila «`[game]/index.vue` `onMounted` → `readStoredProgress().stored`»
      de la tabla de Key Links (`NOT_WIRED`).
  </read_first>
  <behavior>
    - `planProgressMount('resumable', 'resumed')` → `{ action: 'resume-prompt', unverifiedNotice: null }`.
    - `planProgressMount('resumable', 'content-changed')` → `{ action: 'content-changed-notice', unverifiedNotice: null }`.
    - `planProgressMount('stale', 'resumed')` → `{ action: 'resume-prompt', unverifiedNotice: null }` — `'stale'` no cambia lo que la app puede HACER al montar (la partida está ahí y se ofrece), y al montar no hay ninguna sesión que pudiera contradecirla.
    - `planProgressMount('absent', 'fresh')` → `{ action: 'mini-setup', unverifiedNotice: null }`.
    - `planProgressMount('unknown', 'fresh')` → `{ action: 'mini-setup', unverifiedNotice: UNVERIFIED_PROGRESS_NOTICE }` — **la única entrada que produce aviso**, y la que rompe el colapso con `'absent'`.
    - La función es total: recorrer los cuatro valores de `StoredProgress` produce cuatro resultados y ninguna combinación lanza ni devuelve `undefined`.
    - `'absent'` y `'unknown'` producen resultados DISTINTOS (aserción explícita, es el hueco que se cierra).
    - `UNVERIFIED_PROGRESS_NOTICE` no contiene `'ya no está'`, ni `'sigue guardada'`, ni `'no hay'`.
  </behavior>
  <action>
    1. **Crear `app/composables/useProgressMountPlan.ts`** con esta superficie exacta:

       - `export type MountAction = 'resume-prompt' | 'content-changed-notice' | 'mini-setup'`
       - `export interface ProgressMountPlan { action: MountAction, unverifiedNotice: string | null }`
       - `export const UNVERIFIED_PROGRESS_NOTICE: string`
       - `export function planProgressMount(stored: StoredProgress, outcome: ResumeOutcome): ProgressMountPlan`

       `StoredProgress` se importa **con `import type`** desde `~/composables/useStoredProgress` y
       `ResumeOutcome` con `import type` desde `~~/engine/persistence`. La dependencia va en una
       sola dirección, igual que en `useHistorySavedNotice.ts`: este fichero conoce la forma que
       produce la autoridad; la autoridad no sabe que existe ninguna decisión de montaje.

    2. **Texto EXACTO de `UNVERIFIED_PROGRESS_NOTICE`:**

       `No hemos podido comprobar si este dispositivo tiene una partida guardada de este juego. Podéis empezar una nueva, pero si había alguna, al guardar la nueva podríais sustituirla.`

       Comentario encima con la regla que cumple, palabra por palabra: *afirma exactamente lo que
       `stored === 'unknown'` respalda —que la lectura ha fallado— y nada más. No dice que no haya
       partida (eso es lo que la ronda 6 encontró que la app no podía afirmar), no promete que la
       haya, y avisa de la consecuencia real de seguir adelante, que es la única acción que el grupo
       puede tomar desde aquí. La copy vive en este fichero y no en la plantilla por la misma razón
       que `NOTICE_BODY`: una afirmación sobre los datos del grupo tiene que ser comprobable por un
       test puro.*

    3. **Cuerpo de `planProgressMount`**: implementarlo como un `switch (stored)` con rama para los
       cuatro valores y un `default` inalcanzable que haga `const _exhaustivo: never = stored` — así
       añadir un quinto valor a `StoredProgress` sin decidir qué hace el montaje hace fallar
       `npm run typecheck`, en vez de colarse por una rama `else`. Dentro de `'resumable'`/`'stale'`,
       `action` se deriva de `outcome` (`'content-changed'` → `'content-changed-notice'`, cualquier
       otro → `'resume-prompt'`).

    4. **Comentario de cabecera del fichero** explicando por qué existe, con los tres caminos que
       CR-02 enumera: el mini-setup es lo que la app enseña cuando no hay nada (`'absent'`), cuando
       no ha podido leer (`'unknown'`) y cuando la posición existe pero es inservible (que
       `resume()` ya degrada a `'fresh'`/`'absent'`). Mientras el montaje consumiera `outcome`, los
       tres eran indistinguibles y el aviso de fallo le pedía al grupo que los distinguiera con sus
       propios ojos. Nombrar el plan (09-29), la ronda (6) y el hallazgo (CR-02/WR-02).

    5. **Crear `app/composables/__tests__/useProgressMountPlan.test.ts`** con un caso por viñeta de
       `<behavior>`. Es un test puro: no necesita `window`, ni jsdom, ni contexto de Nuxt (mismo
       criterio que `useHistorySavedNotice.test.ts`). Incluir explícitamente el test
       `expect(planProgressMount('absent', 'fresh')).not.toEqual(planProgressMount('unknown', 'fresh'))`
       con un comentario que diga qué gap cierra.

    Todo el código y los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/useProgressMountPlan.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/composables/useProgressMountPlan.ts` y `test -f app/composables/__tests__/useProgressMountPlan.test.ts` salen 0.
    - `grep -v "^\s*//" app/composables/useProgressMountPlan.ts | grep -c "export function planProgressMount(stored: StoredProgress, outcome: ResumeOutcome)"` devuelve 1.
    - `grep -c "const _exhaustivo: never = stored" app/composables/useProgressMountPlan.ts` devuelve 1.
    - `grep -c "al guardar la nueva podríais sustituirla" app/composables/useProgressMountPlan.ts` devuelve 1 — el texto exacto está puesto.
    - `grep -c "ya no está" app/composables/useProgressMountPlan.ts` devuelve 0.
    - `npx vitest run app/composables/__tests__/useProgressMountPlan.test.ts` sale 0 con **al menos 8 tests**.
    - Comprobación de la barrera de tipos, a ejecutar y registrar en el SUMMARY: añadir temporalmente un quinto valor a `StoredProgress` en `useStoredProgress.ts` y confirmar que `npm run typecheck` FALLA señalando el `never` de `planProgressMount`. Revertir el cambio después; el SUMMARY debe citar el mensaje de error obtenido.
    - `npm run typecheck` sale 0 con el árbol ya revertido.
  </acceptance_criteria>
  <done>
    La decisión de montaje es una función pura, total sobre los cuatro estados del dispositivo, con
    su copy como dato y con `'absent'` y `'unknown'` produciendo resultados distintos.
  </done>
</task>

<task type="auto">
  <name>Task 2: cablear el montaje y enseñarle al grupo, en el mini-setup, que la lectura falló</name>
  <files>app/pages/[game]/index.vue, app/components/MiniSetupScreen.vue</files>
  <read_first>
    - `app/pages/[game]/index.vue` — el `onMounted` completo con sus comentarios (~146-170), los
      refs `resumeResolved`/`awaitingResumeChoice`/`awaitingContentChangedAck` (~140-144), el uso
      de `MiniSetupScreen` en la plantilla (~774-787) y la cadena `v-if`/`v-else-if` completa dentro
      de `<ClientOnly>` (~735-790), para no romperla al añadir una prop.
    - `app/components/MiniSetupScreen.vue` — COMPLETO. Sus `defineProps`, el `<main>` y el `<h1>`
      (`{{ gameTitle }} — Preparar partida`), y su comentario de cabecera («Componente tonto: no
      importa nada del motor ni de los composables»), que esta tarea debe respetar: la copy llega
      por prop, no se escribe aquí.
    - `app/composables/useProgressMountPlan.ts` tal como lo deja la Task 1.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` — «Design System», «Typography»
      y «Color» (las clases reales: `text-body`, `font-normal`, `text-secondary-text`,
      `text-warning` si existe, y la escala de espaciado `gap-*`/`px-*`). No inventar clases: usar
      las que el repo ya usa en `VoiceUnavailableNotice.vue` para un aviso no bloqueante.
  </read_first>
  <action>
    1. **`MiniSetupScreen.vue`** gana una prop opcional en su `defineProps`:
       `unverifiedProgressNotice?: string | null`. Se renderiza como un único `<p>` dentro del
       `<main>`, **inmediatamente debajo del `<h1>`**, con `v-if="unverifiedProgressNotice"`,
       `role="status"` (no bloquea ni roba foco: mismo patrón que la banda de voz no disponible) y
       las clases tipográficas de cuerpo secundario que ya usa el repo, con `max-w-[640px]` y
       `text-center` para respetar el eje central de la pantalla. El contenido es
       `{{ unverifiedProgressNotice }}`, interpolado — **ni una palabra de copy escrita en esta
       plantilla**. Añadir dos líneas de comentario diciendo exactamente eso y por qué (el gate de
       clase, `afirmacionesRespaldadas.test.ts`).

    2. **`onMounted`** pasa a esta forma, conservando la guarda `if (!game)` y el orden:
       - `const informe = readStoredProgress(game)` (sin `esperada`: no hay nada que comparar al
         montar — dejarlo escrito en un comentario, porque es la diferencia con `onOutcomeRecorded`).
       - `const plan = planProgressMount(informe.stored, informe.outcome)`.
       - `avisoProgresoNoComprobado.value = plan.unverifiedNotice` (ref nuevo,
         `ref<string | null>(null)`, declarado junto a `resumeResolved`).
       - `if (plan.action === 'mini-setup') { resumeResolved.value = true; return }`.
       - En otro caso: `session.value = informe.session`,
         `awaitingResumeChoice.value = plan.action === 'resume-prompt'`,
         `awaitingContentChangedAck.value = plan.action === 'content-changed-notice'`,
         `resumeResolved.value = true`.

       Ninguna de las tres asignaciones vuelve a mirar `informe.outcome`: la única lectura de
       `outcome` que queda en `onMounted` es la que se le pasa a `planProgressMount`.

    3. **Reescribir el comentario** de `onMounted` que hoy dice «Primer consumidor de la autoridad
       (plan 09-26)». Debe decir qué cambió y por qué: el montaje consumía `informe.outcome`, que
       colapsa `'absent'` y `'unknown'` en el mismo camino observable, así que no podía distinguir
       «he comprobado y no hay nada» de «no he podido comprobarlo» — la distinción exacta para la
       que `StoredProgress` existe (CR-02/WR-02 de `09-REVIEW.md`, Gap #1 de la ronda 6). Ahora la
       decisión vive en `planProgressMount`, que es total y testeable, y el estado «no comprobado»
       tiene superficie propia en pantalla en vez de delegarse en los ojos del grupo. Conservar la
       referencia a Pitfall 7 (nada de esto ocurre durante el prerender), que sigue siendo cierta.

    4. **Plantilla:** añadir `:unverified-progress-notice="avisoProgresoNoComprobado"` al
       `<MiniSetupScreen>` existente. No tocar nada más de la cadena `v-else-if`.

    5. **Comprobación de no regresión del atajo de teclado:** `shortcutsEnabled` recibe
       `resumeResolved`, `hasSession`, `awaitingResumeChoice`, `awaitingContentChangedAck`, … — este
       cambio no altera ninguno de esos valores en ninguna rama. Verificarlo leyendo
       `atajosActivos` (~697-715) y dejarlo dicho en el SUMMARY; no tocar `useStepShortcuts.ts`.
  </action>
  <verify>
    <automated>npx vitest run && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "planProgressMount(informe.stored, informe.outcome)" "app/pages/[game]/index.vue"` devuelve 1.
    - `grep -c "informe.outcome === 'fresh'" "app/pages/[game]/index.vue"` devuelve 0 — la comparación que colapsaba los dos estados ya no existe.
    - `grep -c "informe.outcome" "app/pages/[game]/index.vue"` devuelve exactamente 1 (la única aparición es el argumento que se le pasa a `planProgressMount`).
    - `grep -c "unverified-progress-notice" "app/pages/[game]/index.vue"` devuelve 1 y `grep -c "unverifiedProgressNotice" app/components/MiniSetupScreen.vue` devuelve al menos 2 (la prop y su interpolación).
    - `grep -c "No hemos podido comprobar" app/components/MiniSetupScreen.vue` devuelve 0 y `grep -c "No hemos podido comprobar" "app/pages/[game]/index.vue"` devuelve 0 — la copy no se ha filtrado a ninguna plantilla.
    - `npm run build` sale 0 y `npx vitest run` sale 0.
    - Comprobación manual del ejecutor, con resultado escrito en el SUMMARY: con `npx vitest` o un `node -e`, simular `planProgressMount('unknown', 'fresh')` y confirmar que `unverifiedNotice` no es `null`; y confirmar leyendo el diff que el `<p>` nuevo está dentro del `<main>` y después del `<h1>`, no en la cabecera.
    - Si el gate de clase (`afirmacionesRespaldadas.test.ts`) se pone rojo por la copy nueva, el SUMMARY nombra la frase concreta que lo dispara y el fichero, y deja constancia de que la entrada definitiva de auditoría la escribe 09-30.
  </acceptance_criteria>
  <done>
    El montaje distingue en código y en pantalla «no hay nada» de «no he podido mirar», y el grupo
    se entera de lo segundo antes de empezar una partida que podría sustituir a otra.
  </done>
</task>

<task type="auto">
  <name>Task 3: retirar el test que fija el defecto en verde y sembrar el que lo caza</name>
  <files>app/composables/__tests__/avisoTrasRegistroFallido.test.ts</files>
  <read_first>
    - `app/composables/__tests__/avisoTrasRegistroFallido.test.ts` — COMPLETO. Los tres dobles de
      `localStorage` (`createFakeLocalStorage`, `createFakeLocalStorageWithBrokenHistoryKey`,
      `createFakeLocalStorageFailingAfter`), `makeSession`/`makeEntry`, y los SIETE tests, en
      especial el 4 («contraste: solo la clave del histórico está rota…») y el 5 («EL TEST DE LA
      RONDA 5…», líneas ~187-224).
    - `engine/__tests__/fixtures/tiny-game.json` — tiene **6 pasos** (`intro.prep.01..03` +
      `loop.turno.01..03`), así que `cursor: 2` y `cursor: 3` apuntan a `runtimeId` distintos: el
      escenario del snapshot desactualizado es representable sin tocar el fixture. Comprobarlo antes
      de escribir el test, no darlo por bueno.
    - `app/composables/useStoredProgress.ts` tal como lo dejó 09-28 — la firma con `esperada` y la
      semántica exacta de `'stale'`.
    - `app/composables/useHistorySavedNotice.ts` tal como lo dejó 09-28 — `'failure-stale'` y su
      copy.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — Gap #1: el `artifacts:`
      de este fichero (`⚠ CONTRAPRODUCENTE`) y el cuarto punto de su lista `missing:`, que es
      literalmente el encargo de esta tarea.
  </read_first>
  <behavior>
    - **Test 5 reescrito:** mismo montaje (tres autoguardados con `cursor: 0/1/2` que escriben de
      verdad, cierre con `cursor: 3` que falla), pero la lectura pasa a ser
      `readStoredProgress(tinyGame, { ...base, cursor: 3 })` y la aserción pasa a ser
      `planGameEnd(historyRecorded, informe.stored).variant === 'failure-stale'`. Se añade la
      aserción que faltaba y que da nombre al hallazgo: `expect(informe.stored).toBe('stale')`.
    - El test 5 conserva la mitad que SÍ protegía: `informe.outcome` sigue siendo `'resumed'` — la
      app seguirá ofreciendo «Continuar», así que el aviso no puede decir «no hay nada que
      reintentar». Esa aserción se mantiene tal cual.
    - El texto que leería el grupo en ese escenario NO contiene `'sigue guardada'` ni
      `'no se ha perdido'`, y NO contiene la instrucción de reintento `'Partida terminada'`.
    - **Test nuevo:** con el MISMO montaje pero llamando a `readStoredProgress(tinyGame)` **sin**
      `esperada`, la variante sigue siendo `'failure-recoverable'` — la prueba de que el segundo
      argumento es lo que cambia la respuesta, y de que el defecto era exactamente su ausencia.
    - **Test nuevo:** escenario del test 4 (solo la clave del histórico rota, `save()` sí escribe)
      con `esperada` = la MISMA sesión que se guardó → `informe.stored` es `'resumable'` y la
      variante vuelve a ser `'failure-recoverable'` con su promesa intacta. Es el contraste que
      demuestra que `'stale'` no se dispara de más.
  </behavior>
  <action>
    1. **Reescribir el test 5** según `<behavior>`, conservando su montaje línea por línea (no
       reconstruirlo: el doble `createFakeLocalStorageFailingAfter(3)` y los tres autoguardados son
       lo que hace el escenario real). Cambiar su nombre a algo que diga qué fija ahora, p. ej.
       `'5. EL TEST DE LA RONDA 6: el dispositivo conserva una versión ANTERIOR, no la partida que terminó — la variante no puede prometer identidad de partida'`.

    2. **Sustituir su bloque de comentario interno.** Hoy explica que «la variante correcta es la
       recuperable» porque la clave sigue en el dispositivo. Ese razonamiento es exactamente el
       defecto. El comentario nuevo debe decir: la clave sigue en el dispositivo (cierto), pero lo
       que hay bajo ella es `cursor: 2` y la partida que terminó estaba en `cursor: 3` — afirmar
       «sigue guardada» sobre eso es hablar de otra partida, y el reintento que esa frase ordena
       escribiría en el histórico —irreconstruible— la ronda y la selección de héroes/villano del
       autoguardado anterior. Citar la ronda 6 y el Gap #1 de `09-VERIFICATION.md`.

    3. **Añadir los dos tests nuevos** de `<behavior>` (el de «sin `esperada` sigue dando
       recuperable» y el contraste del histórico roto con `esperada`), con comentario propio
       diciendo qué demuestra cada uno.

    4. **Actualizar la cabecera del fichero**: hoy dice que el test fue «reescrito por el plan 09-26
       contra el BLOCKER de la ronda 5». Añadir el párrafo de la ronda 6 sin borrar el de la 5 —
       este fichero es el registro cruzado de las tres piezas (almacenamiento, autoridad, aviso) y
       su historia es parte de su valor. Nombrar explícitamente que el test 5 anterior fijaba en
       verde el comportamiento defectuoso, para que nadie lo restaure por parecer «el que ya
       estaba».

    5. **No tocar los tests 1, 2, 3, 4, 6 y 7.** Comprobar que siguen en verde sin cambios y dejarlo
       dicho en el SUMMARY. El test 4, en particular, es el que fija que `failure-recoverable`
       sigue existiendo y alcanzándose: si hubiera que tocarlo, es señal de que `'stale'` se está
       disparando de más y hay que revisar `esLaMismaPartida`, no el test.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/avisoTrasRegistroFallido.test.ts && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run app/composables/__tests__/avisoTrasRegistroFallido.test.ts` sale 0 con **9 tests** (los 7 previos, uno de ellos reescrito, más los 2 nuevos).
    - `grep -c "'failure-stale'" app/composables/__tests__/avisoTrasRegistroFallido.test.ts` devuelve al menos 1.
    - `grep -c "toBe('stale')" app/composables/__tests__/avisoTrasRegistroFallido.test.ts` devuelve al menos 1.
    - `grep -c "readStoredProgress(tinyGame, " app/composables/__tests__/avisoTrasRegistroFallido.test.ts` devuelve al menos 2.
    - `grep -c "ronda 6" app/composables/__tests__/avisoTrasRegistroFallido.test.ts` devuelve al menos 2 (cabecera del fichero + comentario del test 5).
    - Comprobación negativa OBLIGATORIA, con salida pegada en el SUMMARY: revertir temporalmente el cableado de 09-28 (dejar `readStoredProgress(game)` sin `session.value` en `onOutcomeRecorded`) **no** basta para probar nada aquí porque el test llama a la autoridad directamente; en su lugar, cambiar temporalmente la llamada del test 5 a `readStoredProgress(tinyGame)` (sin `esperada`) y confirmar que el test se pone **ROJO**. Restaurarlo después. Sin esta comprobación el test podría estar pasando por el motivo equivocado.
    - `npx vitest run` completo sale 0.
  </acceptance_criteria>
  <done>
    Ningún test de la suite afirma ya que «sigue guardada» sea correcto sobre un snapshot anterior, y
    existe uno que se pone rojo si alguien lo reintroduce.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `window.localStorage` → app (lectura de montaje) | Entrada NO fiable y, además, **no siempre legible**: modo privado, cuota, `SecurityError`. Lo que la app no consigue leer no puede convertirse en una afirmación. |
| app → `tga:progress:<gameId>` (autoguardado) | Frontera de ESCRITURA destructiva: el primer autoguardado de una partida nueva sobrescribe lo que hubiera antes, sin leerlo. |
| Copy de la interfaz → grupo | Una pantalla puede afirmar sin decir palabra: enseñar el mini-setup «como si» se hubiera comprobado el dispositivo es una afirmación por omisión. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-29-01 | Spoofing (de estado, hacia el grupo) | `onMounted` presentando el mini-setup tras un `stored: 'unknown'` sin distinguirlo de `'absent'` | mitigate | Task 1 + Task 2: la decisión pasa por `planProgressMount`, total sobre los cuatro valores con guarda `never`, y `'unknown'` produce un aviso propio en pantalla. Criterio verificable: `grep -c "informe.outcome === 'fresh'"` devuelve 0 y `planProgressMount('absent','fresh') !== planProgressMount('unknown','fresh')` está fijado por test. |
| T-09-29-02 | Tampering / pérdida de datos locales | El primer autoguardado de una partida nueva sobrescribe `tga:progress:<gameId>` después de una lectura fallida, destruyendo una posición que quizá seguía ahí | accept (parcialmente mitigado) | Mitigación parcial y barata: el aviso nuevo se lo dice al grupo **antes** de empezar («si había alguna, al guardar la nueva podríais sustituirla»), que es la única decisión que puede tomar. El corte del autoguardado queda FUERA por diseño (ver `<scope_boundary>`) y se registra como riesgo evaluado en `deferred-items.md` (plan 09-31), nunca como olvido. Radio de impacto: el PROGRESO de una partida, no el histórico (`tga:history` es otra clave y no se toca); en la causa más probable (modo privado) la escritura falla igual que la lectura, así que no hay nada que destruir. |
| T-09-29-03 | Repudiation (la suite certifica lo falso) | `avisoTrasRegistroFallido.test.ts` test 5 fijando en verde la respuesta defectuosa | mitigate | Task 3 lo reescribe y añade la comprobación negativa obligatoria (el test debe ponerse ROJO si se le quita el segundo argumento), para que no pueda pasar por el motivo equivocado. |
| T-09-29-04 | Elevation of privilege (de la plantilla sobre la autoridad) | Copy nueva escrita directamente en `MiniSetupScreen.vue` | mitigate | La frase vive en `useProgressMountPlan.ts` como dato exportado y viaja por prop; criterio verificable: `grep -c "No hemos podido comprobar"` devuelve 0 en los dos `.vue`. El gate de clase ampliado (plan 09-30) la vigilará también en `.ts`. |
| T-09-29-05 | Denial of service (prerender) | `planProgressMount` ejecutándose fuera del cliente | accept | La llamada vive dentro de `onMounted` y bajo `<ClientOnly>` (Pitfall 7), exactamente como hoy; la función es pura y no toca `window`. `npm run build` (prerender real) es criterio de aceptación de la Task 2. |
| T-09-29-SC | Tampering (cadena de suministro) | `npm` / `package.json` | n/a | Este plan **no instala ningún paquete**. `git diff --stat` no debe incluir `package.json` ni `package-lock.json`. |
</threat_model>

<verification>
- `npx vitest run` sale 0. Registrar en el SUMMARY el recuento antes y después (debe subir en al
  menos 10: los ≥8 de `useProgressMountPlan.test.ts` más los 2 nuevos de
  `avisoTrasRegistroFallido.test.ts`).
- `npm run typecheck` sale 0.
- `npm run build` sale 0.
- `grep -rn "informe.outcome" app/` devuelve exactamente una coincidencia, la del argumento de
  `planProgressMount`.
- `git diff --stat` toca exclusivamente los cinco ficheros de `files_modified` (más
  `afirmacionesRespaldadas.test.ts` solo si hubo que ponerle la entrada temporal con `TODO(09-30)`,
  que el SUMMARY debe nombrar).
</verification>

<success_criteria>
Con la lectura del progreso caída, la app enseña el mini-setup **y le dice al grupo que no ha podido
comprobar el dispositivo**, en vez de dejar que lo deduzca de una pantalla que significa tres cosas
distintas. La decisión de montaje es pura, total y testeada sobre los cuatro estados. Y el escenario
del snapshot desactualizado —tres autoguardados válidos y un cierre fallido— produce en el test la
variante que no promete identidad de partida, con una comprobación negativa que demuestra que el
test no pasa por casualidad.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-29-SUMMARY.md` al terminar.
</output>
