---
phase: 09-hist-rico-y-estad-sticas
plan: 16
type: execute
wave: 10
depends_on: ["09-13", "09-14"]
gap_closure: true
files_modified:
  - app/pages/[game]/index.vue
  - app/components/HistorySavedNotice.vue
autonomous: true
requirements: [HIST-02, HIST-06, HIST-09]

must_haves:
  truths:
    - "SC3: cuando `record()` devuelve `false`, la partida jugada deja de ser irrecuperable — el progreso sigue en `tga:progress:<gameId>`, así que el grupo puede volver a entrar, ver «Partida guardada … CONTINUAR» y reintentar «Partida terminada»"
    - "SC3: el aviso de fallo deja de afirmar una causa que la app no conoce («el dispositivo no permitió escribir») y dice en su lugar qué ha pasado con la partida y qué hacer a continuación"
    - "HIST-02: «Salir sin registrar» sigue terminando la partida SIN registrar y SIN condiciones — esa salida no depende de ningún resultado de escritura y borra el progreso igual que hoy"
    - "HIST-09: ninguna de las dos salidas toca `tga:history`; `clear(gameId)` sigue borrando solo la clave de progreso del juego"
    - "D-U4: el orden `silence()` → `record()` → `notifyHistorySaved()` → cierre de partida se conserva intacto — `record()` sigue ejecutándose ANTES de que nada destruya `session.value`"
    - "El aviso es perceptible por un lector de pantalla: la región viva existe en el DOM antes de que aparezca el texto, no se inserta junto con él"
  artifacts:
    - path: "app/pages/[game]/index.vue"
      provides: "finishGame con un parámetro explícito que decide si se borra el progreso, y onOutcomeRecorded que lo pasa según el resultado de record()"
      contains: "preserveProgress"
    - path: "app/components/HistorySavedNotice.vue"
      provides: "aviso de fallo con copy que no atribuye causa y sí describe la vía de recuperación, dentro de una región viva permanente"
      contains: "aria-live=\"polite\""
  key_links:
    - from: "record() devolviendo false"
      to: "finishGame(preserveProgress = true)"
      via: "el resultado de la escritura decide si se borra el progreso, no solo qué aviso se pinta"
      pattern: "finishGame\\(!?guardado\\)|finishGame\\(true\\)"
    - from: "finishGame"
      to: "clear(gameId)"
      via: "el borrado del progreso queda detrás de la guarda del parámetro; HIST-09 intacto (nunca toca tga:history)"
      pattern: "if \\(!preserveProgress\\) clear\\(gameId\\)"
    - from: "onOutcomeDismiss"
      to: "finishGame()"
      via: "sin argumento — salir sin registrar sigue siendo terminal e incondicional (HIST-02)"
      pattern: "finishGame\\(\\)"
---

<objective>
Cerrar el ⚠️ WARNING que `09-VERIFICATION.md` clasifica como **amplificador del impacto de
CR-02** (`app/pages/[game]/index.vue:556-574`, tabla de anti-patrones y tabla de «Verificación
de enlaces clave»): `finishGame()` se invoca incondicionalmente tras `record()`, sin distinguir
«registrado» de «rechazado». Su primer paso es `session.value = null` y el segundo
`clear(gameId)`, así que **cualquier `false` de `record()` convierte la partida jugada en
pérdida irreversible** — el grupo lee «⚠ No se pudo guardar la partida» mientras la sesión ya
se ha destruido y no hay nada que reintentar.

El verificador lo marcó como WARNING y no como BLOCKER porque no CREA ningún defecto: es lo que
convierte los defectos de otros en catástrofes. Los planes 09-13 y 09-14 cierran las dos causas
conocidas por las que `record()` devolvía `false` indebidamente; este plan cierra el
amplificador, que es lo que hace que la CUARTA causa —la que nadie ha encontrado todavía— no
vuelva a costar una partida entera.

Y cierra de paso la mitad honesta de **WR-02**: el aviso de fallo afirma hoy, palabra por
palabra, que «el dispositivo no permitió escribir en su almacenamiento (modo privado, cuota
agotada u otro bloqueo similar)». Con el cierre de 09-13 hay al menos tres motivos distintos
detrás de ese mismo `false` (no se pudo leer, no se supo interpretar el blob guardado, no se
pudo escribir), y la app no sabe cuál es. Un aviso que nombra una causa que no conoce manda al
grupo a revisar ajustes del navegador por nada — es exactamente el tipo de afirmación falsa que
esta fase lleva tres rondas intentando erradicar.

Salida: una salida de partida que preserva lo que el registro no consiguió guardar, y un aviso
que dice lo que sí sabe (la partida no se ha perdido, se puede reintentar) en vez de lo que
supone.
</objective>

<scope_boundary>
DENTRO de este plan, y solo esto:

- El cableado condicional de la salida de partida en `onOutcomeRecorded`
  (`app/pages/[game]/index.vue`), con el mínimo cambio que lo consigue: un parámetro explícito
  en `finishGame` que decide si se borra el progreso.
- La copy del aviso de fallo en `app/components/HistorySavedNotice.vue`, que deja de atribuir
  causa y pasa a describir el estado real y la vía de recuperación.
- `role="status"`/`aria-live="polite"` sobre una región viva PERMANENTE (mitad (a) de WR-05 de
  `09-REVIEW.md`): el aviso es hoy el único resultado observable del gesto «terminar partida»,
  y es invisible para un lector de pantalla. Va aquí porque esta tarea ya reescribe ese mismo
  elemento y porque el texto nuevo es una instrucción de recuperación: un aviso que dice «podéis
  reintentar» y que nadie oye no cumple su función.

FUERA de alcance, anotado para que la re-verificación no lo confunda con un descuido:

- **La mitad (b) de WR-05** (la banda empuja fuera del viewport las pantallas `h-dvh` porque se
  monta como hermano encima de `<NuxtPage/>`): es un cambio de maquetación que afecta también a
  `UpdateBanner` y a las cuatro pantallas de la app, ninguna de las cuales pertenece a esta
  fase. No se agrava aquí: el aviso sigue apareciendo exactamente donde aparecía, en la
  pantalla de destino tras `navigateTo('/')`. Queda registrado en `deferred-items.md` por
  09-17.
- **WR-04** (`GameOutcomeDialog` sin `Escape`, sin gestión de foco, sin `aria-labelledby`):
  accesibilidad heredada de la ronda 1, sin pérdida de datos asociada, y su fix requiere
  decidir qué significa `Escape` frente a D-01 (que fija exactamente cuatro botones). Queda
  registrado en `deferred-items.md` por 09-17.
- **La otra mitad de WR-02** (no hay ninguna vía en la interfaz para salir del bloqueo que
  produce un blob `unreadable` permanente: archivar la clave y empezar limpio): es una
  superficie de interfaz nueva que `09-UI-SPEC.md` no cubre, y construirla a ciegas en una
  ronda de cierre de huecos es exactamente cómo han entrado los defectos de las tres rondas
  anteriores. Queda registrado en `deferred-items.md` por 09-17, con su propuesta concreta.
- El tipo de retorno de `appendHistoryEntry`/`record` sigue siendo `boolean`. Este plan NO lo
  convierte en un resultado discriminado: la copy nueva no necesita distinguir los tres
  motivos (deja de nombrarlos), y el cambio de firma arrastraría ~12 aserciones de test en dos
  ficheros que 09-13 y 09-14 acaban de tocar.
- `GameOutcomeDialog.vue`, `useHistorySavedNotice.ts`, `app/app.vue`, `usePersistedSession.ts`,
  `useGameHistory.ts`, `engine/*`: NO se tocan. Los dos ficheros de `files_modified` son los
  únicos que cambian.
</scope_boundary>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md

<interfaces>
Extraído del repo (no hay que explorar para averiguarlo):

`app/pages/[game]/index.vue`, estado ACTUAL de la cola de fin de partida:
- `onEndGameRequest()` (`:524-526`): pone `awaitingEndConfirm.value = true` — abre
  `GameOutcomeDialog`.
- `finishGame()` (`:533-547`): tres pasos, en este orden y con su comentario D-U4 explicando
  por qué: `session.value = null` (ANTES de `clear`, porque el autoguardado es un
  `watchDebounced` de 300 ms y una escritura pendiente resucitaría la clave), `clear(gameId)`,
  `navigateTo('/')`. No libera el wake lock a propósito (`navigateTo` desmonta la página y el
  `tryOnScopeDispose` de `useWakeLock` ya lo hace).
- `onOutcomeRecorded(outcome)` (`:556-575`): `awaitingEndConfirm = false`, `isIndexOpen = false`,
  `silence()`, y después —con la guarda WR-04 `if (session.value)`—
  `const guardado = record(session.value, outcome)` + `notifyHistorySaved(guardado)`. Termina
  con `finishGame()` en la línea 574, FUERA del `if` y sin condicionar.
- `onOutcomeDismiss()` (`:583-588`): `awaitingEndConfirm = false`, `isIndexOpen = false`,
  `silence()`, `finishGame()`. Su comentario de cabecera (NOTA DE RECONCILIACIÓN, HIST-02)
  explica que «Salir sin registrar» TERMINA la partida y no es «cancelar y seguir jugando».
- `clear` viene de `usePersistedSession()` y borra SOLO `tga:progress:<gameId>` — nunca
  `tga:history` (D-13/HIST-09, comprobado en `usePersistedSession.ts:41-47` y `:250-255`).
- `record` viene de `useGameHistory()` y devuelve `boolean`.
- `notifyHistorySaved(saved: boolean)` viene de `useHistorySavedNotice.ts:49`.

`app/components/HistorySavedNotice.vue` (43 líneas), estado ACTUAL:
- `const { variant, dismiss } = useHistorySavedNotice()`; `variant` es
  `'success' | 'failure' | null`.
- Plantilla: un único `<div v-if="variant !== null" class="bg-surface border-b border-background px-2xl py-lg flex items-start justify-between gap-md">`
  con dos ramas hermanas (`v-if="variant === 'success'"` / `v-else`) y el botón `✕`
  (`aria-label="Cerrar aviso"`, `@click="dismiss"`).
- Rama de éxito: `<h2>` con `✓ Partida registrada`, sin párrafo.
- Rama de fallo (`:26-33`): `<h2>` con `⚠ No se pudo guardar la partida` y un `<p>` con el
  texto que este plan sustituye.
- Autocierre: 6 s en éxito, 20 s en fallo (`SUCCESS_AUTO_DISMISS_MS`/`FAILURE_AUTO_DISMISS_MS`
  en `useHistorySavedNotice.ts:26-27`). No cambia.
- Punto de montaje: `app/app.vue:32`, dentro de `<ClientOnly>` y encima de `<NuxtPage/>`, para
  que el aviso disparado en `/[game]` ya se vea en la pantalla de destino.

`app/components/WarningDetailModal.vue` es el patrón «bueno» de accesibilidad del repo
(foco al abrir, `Escape`, `aria-labelledby`) — se cita aquí como referencia de estilo, pero
este plan NO lo replica: el aviso no es un diálogo modal y no captura el foco.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un registro que no se pudo guardar deja de llevarse la partida por delante</name>
  <files>app/pages/[game]/index.vue</files>
  <read_first>
    - `app/pages/[game]/index.vue` — el fichero a modificar. Leer COMPLETO el bloque `:520-600`
      (desde `onEndGameRequest` hasta `onContentChangedAcknowledge`), prestando especial
      atención a: el comentario D-U4 de `finishGame` (`:528-532`), que explica por qué
      `session.value = null` va ANTES de `clear(gameId)` y por qué no se libera el wake lock
      aquí; el comentario D-U4/Pitfall 1 de `onOutcomeRecorded` (`:550-555`), que fija el orden
      EXACTO y advierte de que mover `record()` después de `finishGame()` produciría un
      histórico vacío en silencio; y la NOTA DE RECONCILIACIÓN de `onOutcomeDismiss`
      (`:577-582`).
    - También de ese fichero: `onDiscardConfirm` (buscarlo por nombre) — es el OTRO llamador de
      `clear(gameId)` de la página, y no se toca; leerlo para no confundirse de sitio.
    - `app/composables/usePersistedSession.ts:41-47` y `:250-255` — la constante `HISTORY_KEY` y
      `clear(gameId)`, para confirmar con los propios ojos que `clear` no puede tocar el
      histórico (HIST-09) y que preservar el progreso no tiene ningún efecto sobre él.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — la fila de
      `app/pages/[game]/index.vue` de la tabla «Anti-patrones encontrados» y la fila
      `onOutcomeRecorded` de la tabla «Verificación de enlaces clave» («⚠️ WIRED sin
      condicionar»).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md` — D-01 (cuatro botones,
      ninguno más), D-03 (si falla, se dice) y D-05 (la otra salida, la que no registra).
  </read_first>
  <action>
    Modificar `app/pages/[game]/index.vue`. Dos cambios, ninguno más.

    **1. `finishGame` recibe un parámetro explícito que decide si se borra el progreso.**

    Cambiar su firma a un único parámetro booleano con valor por defecto `false`, nombrado
    `preserveProgress`, y poner `clear(gameId)` detrás de una guarda que solo lo ejecute cuando
    `preserveProgress` sea falso. Los otros dos pasos —`session.value = null` primero y
    `navigateTo('/')` al final— se conservan intactos, en el mismo orden y con su comentario
    D-U4 palabra por palabra: la razón por la que `session.value = null` va antes (cancelar la
    escritura pendiente del `watchDebounced`) sigue siendo válida y es justamente lo que hace
    que preservar el progreso funcione — la clave queda tal como la dejó el último autoguardado,
    sin que una escritura tardía la reescriba.

    Añadir sobre la guarda un comentario que cite el WARNING de `09-VERIFICATION.md`
    (`app/pages/[game]/index.vue:556-574`, «amplificador del impacto de CR-02») y fije la regla
    en una frase: *el progreso solo se borra cuando ya no hace falta; si el registro no se pudo
    guardar, el progreso es lo único que permite reintentarlo, así que borrarlo convierte un
    fallo recuperable en una pérdida definitiva.* Dejar escrito además que esto NO toca
    `tga:history` en ninguna de las dos ramas (HIST-09 sigue intacto: `clear` solo borra
    `tga:progress:<gameId>`), y que el valor por defecto `false` mantiene el comportamiento
    histórico para todos los llamadores que no digan nada.

    **2. `onOutcomeRecorded` decide con el resultado de `record()`.**

    Dentro de la guarda `if (session.value)` que ya existe, tras `notifyHistorySaved(guardado)`,
    llamar a `finishGame` pasando la preservación cuando el registro NO se guardó. La llamada
    incondicional de la línea 574, la que hoy vive fuera del `if`, desaparece: la rama sin
    sesión (WR-04: no hubo ningún intento de escritura, así que no se avisa de nada) debe seguir
    llamando a `finishGame` sin argumento, es decir, borrando el progreso como hasta ahora. Las
    dos ramas terminan la partida; lo único que cambia entre ellas es si se conserva lo que
    permitiría reintentar.

    Ampliar el comentario D-U4/Pitfall 1 de la función con la razón del cambio, citando el
    mismo WARNING: el orden sigue siendo el mismo y sigue siendo obligatorio (`record()` lee
    `session.value`, así que va antes de que nada lo destruya); lo que se añade es que el
    RESULTADO de `record()` decide también qué se destruye. Describir en dos líneas la vía de
    recuperación que esto abre, para que quien lea el código entienda la copy del aviso: el
    grupo vuelve a `/`, entra otra vez en el juego, ve «Partida guardada … CONTINUAR» y puede
    volver a pulsar «Partida terminada» para reintentar el registro.

    NO tocar `onOutcomeDismiss`: «Salir sin registrar» (HIST-02, D-05) es una decisión
    deliberada de no guardar nada, no un fallo, así que sigue llamando a `finishGame()` sin
    argumento y sigue borrando el progreso. Esa es además la salida garantizada del bucle si el
    registro fallara siempre — dejarla incondicional es parte del cierre, no un olvido.

    NO tocar `onDiscardConfirm`, ni `onEndGameRequest`, ni la plantilla, ni ningún otro
    llamador de `clear`, ni añadir un quinto botón al diálogo (D-01 fija exactamente cuatro).
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -8 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "preserveProgress" "app/pages/[game]/index.vue"` devuelve al menos 3 (firma, guarda y comentario).
    - `grep -v '^ *//' "app/pages/[game]/index.vue" | grep -c "clear(gameId)"` devuelve exactamente 2 (el de `finishGame`, ahora tras la guarda, y el de `onDiscardConfirm`, intacto).
    - En la salida de `grep -n "function finishGame" -A 20 "app/pages/[game]/index.vue"`, la línea de `session.value = null` tiene número MENOR que la de `clear(gameId)`, y la de `clear(gameId)` está dentro de una guarda que menciona `preserveProgress`.
    - En la salida de `grep -n "function onOutcomeRecorded" -A 25 "app/pages/[game]/index.vue"`, aparece exactamente una llamada a `record(` y exactamente una a `notifyHistorySaved(`, la de `record(` tiene número MENOR que la de `notifyHistorySaved(`, y ambas tienen número MENOR que toda llamada a `finishGame(`.
    - `grep -n "function onOutcomeDismiss" -A 12 "app/pages/[game]/index.vue" | grep -c "finishGame()"` devuelve 1 — la salida sin registrar sigue siendo incondicional y sin argumento (HIST-02).
    - `grep -c "tga:history" "app/pages/[game]/index.vue"` devuelve 0 — la página sigue sin conocer la clave del histórico (HIST-09 por construcción).
    - `npm run build` termina con código 0 (es la única comprobación automática de tipos y de plantilla que cubre una página `.vue` en este proyecto).
    - `npx vitest run` termina con código 0 — ningún test existente se rompe.
  </acceptance_criteria>
  <done>
    Un `record()` que devuelve `false` deja la partida donde estaba: el progreso sobrevive en
    `localStorage`, el grupo vuelve a entrar y puede reintentar el registro. «Salir sin
    registrar» sigue siendo la salida incondicional y terminal que HIST-02 exige.
  </done>
</task>

<task type="auto">
  <name>Task 2: El aviso deja de atribuir una causa que la app no conoce, y se puede oír</name>
  <files>app/components/HistorySavedNotice.vue</files>
  <read_first>
    - `app/components/HistorySavedNotice.vue` COMPLETO (43 líneas) — el fichero a modificar,
      incluido su comentario de cabecera (por qué se monta en `app.vue` y no en la pantalla de
      juego).
    - `app/composables/useHistorySavedNotice.ts` COMPLETO (76 líneas) — para confirmar que este
      plan NO necesita tocarlo: las dos variantes (`'success'`/`'failure'`) y los dos tiempos
      de autocierre (6 s / 20 s) siguen siendo los correctos. El comentario de `:22-25` explica
      por qué el de fallo dura mucho más («dos frases que hay que poder leer a un brazo de
      distancia desde la mesa») — la copy nueva sigue siendo de dos frases largas, así que los
      20 s se conservan.
    - `app/pages/[game]/index.vue` YA CORREGIDO por la Task 1 — la copy nueva describe
      exactamente la vía de recuperación que esa tarea acaba de abrir; si la copy promete algo
      que el código no hace, el fallo está aquí.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §8 — la especificación
      original del aviso (estructura, jerarquía tipográfica, botón de cierre). La ESTRUCTURA no
      cambia; cambia el texto del párrafo de la rama de fallo, y ese cambio debe anotarse en el
      SUMMARY como desviación consciente de la especificación, con su motivo (WR-02).
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-02 (el diagnóstico falso)
      y §WR-05 (la región viva y la maquetación).
    - `app/components/WarningDetailModal.vue` — referencia del repo para atributos ARIA; leerlo
      para seguir su estilo de anotación, NO para copiar su gestión de foco (este aviso no es
      un diálogo modal).
  </read_first>
  <action>
    Modificar `app/components/HistorySavedNotice.vue`. Dos cambios, ninguno más.

    **1. La región viva es permanente; lo que aparece y desaparece es su contenido.**

    Envolver la banda actual en un contenedor exterior que se renderice SIEMPRE (sin `v-if`),
    con `role="status"` y `aria-live="polite"`, y mover el `v-if="variant !== null"` al
    elemento interior —el que hoy lleva las clases de la banda—, que no cambia ni una clase. El
    contenedor exterior no lleva clases de maquetación: sin contenido ocupa cero altura, así
    que la disposición visual es idéntica a la de hoy.

    Añadir un comentario explicando por qué el orden importa y no es intercambiable: un lector
    de pantalla solo anuncia los cambios de una región viva que YA estaba en el DOM cuando el
    contenido cambió. Poner `aria-live` sobre el mismo elemento que lleva el `v-if` —la forma
    aparentemente equivalente— no anuncia nada, porque la región y el texto aparecen a la vez.
    Citar **WR-05 (`09-REVIEW.md`, ronda 3)** y dejar escrito que este aviso es el ÚNICO
    resultado observable del gesto «terminar partida».

    **2. La copy de la rama de fallo deja de nombrar una causa.**

    Conservar el `<h2>` tal cual (`⚠ No se pudo guardar la partida`, con sus clases). Sustituir
    el `<p>` —que hoy afirma que «El dispositivo no permitió escribir en su almacenamiento (modo
    privado, cuota agotada u otro bloqueo similar)»— por un texto que diga, en este orden y en
    español de tú a tú con el grupo, como el resto de la copy de la app:

    1. Qué ha pasado con la partida: NO se ha perdido, sigue guardada en el dispositivo.
    2. Qué hacer: volver a entrar en la partida y pulsar «Partida terminada» otra vez para
       reintentar el registro. El nombre del botón debe coincidir LITERALMENTE con el que se
       pinta en el índice de la pantalla de juego, para que la instrucción sea seguible sin
       interpretar.
    3. Qué mirar si vuelve a fallar, en condicional y sin afirmar ninguna causa: puede ser el
       modo privado del navegador, la memoria llena, o un histórico anterior que la app no
       consigue leer.

    El punto 3 conserva la información útil del texto viejo sin su defecto: antes se AFIRMABA
    una causa que la app no conoce (con el cierre de 09-13 hay al menos tres motivos distintos
    detrás del mismo `false`), y ahora se enumera como posibilidades, después de lo que sí es
    seguro. Añadir un comentario que cite **WR-02** y explique justo eso, y que apunte a la
    Task 1 de este plan como la razón por la que la primera frase es cierta: la copy y el
    cableado son una sola corrección en dos ficheros.

    NO tocar la rama de éxito, ni el botón `✕` y su `aria-label`, ni las clases de la banda, ni
    los dos tiempos de autocierre, ni `useHistorySavedNotice.ts`. NO sacar la banda del flujo
    (`fixed`/`z-40`): es la mitad (b) de WR-05, afecta también a `UpdateBanner` y a las cuatro
    pantallas `h-dvh`, y queda registrada en `deferred-items.md` por 09-17.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'role="status"' app/components/HistorySavedNotice.vue` devuelve 1.
    - `grep -c 'aria-live="polite"' app/components/HistorySavedNotice.vue` devuelve 1.
    - En la salida de `grep -n 'aria-live\|v-if="variant !== null"' app/components/HistorySavedNotice.vue`, la línea de `aria-live` tiene número MENOR que la de `v-if="variant !== null"` (la región viva envuelve al contenido, no coincide con él).
    - `grep -c "El dispositivo no permitió escribir" app/components/HistorySavedNotice.vue` devuelve 0.
    - `grep -c "Partida terminada" app/components/HistorySavedNotice.vue` devuelve al menos 1 — la instrucción nombra el botón real.
    - `grep -c "WR-02" app/components/HistorySavedNotice.vue` devuelve al menos 1 y `grep -c "WR-05" app/components/HistorySavedNotice.vue` devuelve al menos 1.
    - `grep -c "✓ Partida registrada" app/components/HistorySavedNotice.vue` devuelve 1 — la rama de éxito no se ha tocado.
    - `grep -c 'aria-label="Cerrar aviso"' app/components/HistorySavedNotice.vue` devuelve 1 — el botón de cierre sigue igual.
    - `npm run build` termina con código 0.
    - `npx vitest run` termina con código 0 (incluido `app/composables/__tests__/useHistorySavedNotice.test.ts`, que no debe necesitar ninguna edición).
  </acceptance_criteria>
  <done>
    El aviso de fallo dice lo que la app sabe —la partida sigue ahí y se puede reintentar— y
    enumera como posibilidades lo que solo supone, dentro de una región viva que un lector de
    pantalla sí anuncia.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `record()` (resultado de la escritura) → cola de fin de partida | El booleano que cruza aquí decide qué se le dice al grupo Y qué se destruye. Tratarlo solo como «qué aviso pinto» es lo que convierte un fallo recuperable en una pérdida definitiva. |
| App → `window.localStorage` (`tga:progress:<gameId>`) | `clear(gameId)` es irreversible y no pide confirmación. Es correcto cuando la partida ya está registrada o cuando el grupo ha decidido no registrarla; nunca cuando el registro ha fallado. |
| App → grupo (copy del aviso) | Un texto que afirma una causa técnica que la app no ha comprobado es una afirmación falsa con efecto real: manda a revisar ajustes del navegador por nada, y oculta la acción que sí resuelve. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16-01 | Tampering (destrucción de datos por el propio código) | `finishGame()` invocado incondicionalmente tras `record()` en `app/pages/[game]/index.vue` | mitigate | Task 1: el borrado de `tga:progress:<gameId>` queda detrás de un parámetro explícito que `onOutcomeRecorded` fija según el resultado de `record()`. Severidad `high`: es el amplificador que convierte cualquier rechazo de escritura —conocido o futuro— en pérdida irreversible de la partida jugada. |
| T-09-16-02 | Repudiation (la app afirma una causa que no ha comprobado) | copy de la rama de fallo en `HistorySavedNotice.vue` | mitigate | Task 2: el texto enuncia primero lo que es seguro (la partida no se ha perdido) y después las posibilidades en condicional. Severidad `medium`: no destruye datos, pero dirige mal la única acción correctiva del grupo. |
| T-09-16-03 | Denial of Service (bucle de reintento infinito) | Un fallo de escritura PERMANENTE con el progreso preservado en cada intento | accept | Aceptado y con salida garantizada por diseño: «Salir sin registrar» (`onOutcomeDismiss`) sigue siendo incondicional y borra el progreso, así que el grupo nunca queda atrapado. La copy de la Task 2 nombra el reintento, no lo impone. |
| T-09-16-04 | Tampering (una partida vieja resucitada por error) | Progreso preservado que el grupo no reintenta y encuentra días después | accept | El progreso preservado es exactamente el que había un segundo antes, y la pantalla de reanudación («Partida guardada … CONTINUAR», con su opción de descartar) es la misma que la app ya usa para cualquier partida a medias. No se introduce ningún estado nuevo que el grupo no sepa interpretar. |
| T-09-16-05 | Information disclosure | copy del aviso | accept | El texto nuevo no revela nada que el grupo no sepa ya: nombra su propio botón y el estado de su propia partida. |
| T-09-16-06 | Spoofing / Elevation of privilege | — | accept | No aplican: la app no tiene identidades, sesiones autenticadas ni privilegios que escalar. |
| T-09-16-SC | Tampering (supply chain) | Instalaciones npm/pip/cargo | accept | Ninguna tarea de este plan instala ni actualiza paquetes: se modifican una página `.vue` y un componente `.vue`, ambos existentes. Si durante la ejecución surgiera la necesidad de instalar algo, este plan queda bloqueado hasta que un checkpoint humano lo apruebe. |
</threat_model>

<verification>
1. `npm run build` termina con código 0 — comprobación de tipos y de plantilla de los dos
   ficheros `.vue` modificados.
2. `npx vitest run` termina con código 0, sin editar ningún test existente.
3. `grep -v '^ *//' "app/pages/[game]/index.vue" | grep -c "clear(gameId)"` devuelve 2.
4. `grep -c "El dispositivo no permitió escribir" app/components/HistorySavedNotice.vue`
   devuelve 0.
5. `grep -c 'role="status"' app/components/HistorySavedNotice.vue` devuelve 1.
6. `grep -c "tga:history" "app/pages/[game]/index.vue"` devuelve 0 — HIST-09 sigue siendo
   cierto por construcción: la página no conoce la clave del histórico.
7. `grep -ric "firestore\|firebase" app/ engine/` devuelve 0 — SC5 y STAT-04 intactos.

**Verificación humana recomendada (no bloqueante, ROADMAP §Fase 9).** Ninguna de las dos
tareas tiene arnés de test unitario (el proyecto no monta componentes ni páginas en Vitest:
`vitest.config.ts` usa entorno `node` en los dos proyectos, a propósito), así que esta
comprobación es la que cierra el círculo. Anotar el resultado en el SUMMARY:

1. Empezar una partida y avanzar tres o cuatro pasos.
2. En la consola de DevTools, forzar el fallo de escritura del histórico:
   redefinir `localStorage.setItem` con una función que lance cuando la clave sea
   `'tga:history'` y delegue en la original en cualquier otro caso.
3. Pulsar «Partida terminada» → «GANADA». Debe verse el aviso de fallo con el texto nuevo, y
   debe volverse al selector de juegos.
4. Entrar otra vez en Marvel Champions: debe aparecer «Partida guardada … CONTINUAR» en el
   paso donde se dejó. **Esto es lo que antes era imposible.**
5. Restaurar `localStorage.setItem`, continuar, pulsar «Partida terminada» → «GANADA» otra
   vez: debe verse «✓ Partida registrada» y la partida debe aparecer en `/historico`.
6. Repetir desde el paso 1 pero eligiendo «Salir sin registrar»: debe volverse al selector y,
   al entrar de nuevo, NO debe haber partida guardada (HIST-02/D-05 intactos).
7. Con un lector de pantalla activo (VoiceOver en iPad, TalkBack en Android), confirmar que el
   aviso se anuncia solo al aparecer.
</verification>

<success_criteria>
- Con `record()` devolviendo `false`, `tga:progress:<gameId>` sobrevive a la salida de partida
  y la sesión se puede reanudar y reintentar.
- Con `record()` devolviendo `true`, el comportamiento es idéntico al de hoy: progreso borrado,
  vuelta al selector, aviso de éxito.
- «Salir sin registrar» sigue borrando el progreso sin condiciones y sigue siendo la salida
  garantizada del bucle (HIST-02).
- Ninguna de las dos rutas toca `tga:history` (HIST-09).
- El aviso de fallo no afirma ninguna causa técnica; enuncia el estado real de la partida y la
  acción de reintento nombrando el botón literal.
- El aviso vive dentro de una región `role="status" aria-live="polite"` presente en el DOM
  antes de que aparezca su contenido.
- `npm run build` y `npx vitest run` terminan en verde sin editar ningún test.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-16-SUMMARY.md` al terminar.
</output>
