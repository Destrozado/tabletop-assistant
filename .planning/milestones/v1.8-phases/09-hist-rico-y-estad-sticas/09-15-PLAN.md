---
phase: 09-hist-rico-y-estad-sticas
plan: 15
type: execute
wave: 9
depends_on: ["09-12"]
gap_closure: true
files_modified:
  - engine/persistence.ts
  - engine/__tests__/persistence.test.ts
autonomous: true
requirements: [HIST-04]

must_haves:
  truths:
    - "SC2: una sesión cuyo `context` persistido no se puede validar NO se reanuda: `resume()` devuelve `outcome: 'fresh'` y el mini-setup vuelve a preguntar nº de jugadores y dificultad, en vez de continuar con el relleno `{ playerCount: 1, difficulty: 'normal' }` de la página bajo la etiqueta «reanudada»"
    - "SC2: la entrada del histórico de una partida así ya no puede quedar con datos INCORRECTOS (1 jugador, Normal) haciéndose pasar por correctos — o la configuración es la real, o no hay reanudación"
    - "`isValidContext` exige un `playerCount` entero positivo y una `difficulty` de las dos que el mini-setup puede producir, no solo `typeof number`/`typeof string` — un `playerCount: NaN` es configuración perdida, no configuración válida"
    - "El camino feliz no se degrada: una sesión con `context` real se sigue reanudando con `outcome: 'resumed'`, su cursor, su ronda y su `context` persistido intactos"
    - "Las dos ramas que deciden qué `context` entra en la sesión viva (`resumed` y `contentChangedFallback`) comparten un único criterio, y ese criterio se aplica UNA sola vez, antes de las tres comprobaciones de versión"
    - "Ninguna regresión: los tests de D-20 y D-21 (`selection` ausente, forma v1.7, `counters` corruptos) siguen en verde sin editarlos, y la suite completa sigue pasando"
  artifacts:
    - path: "engine/persistence.ts"
      provides: "resume() con una guarda única de context al principio que degrada a 'fresh', e isValidContext que valida por rango y por valor, no solo por typeof"
      contains: "outcome: 'fresh'"
    - path: "engine/__tests__/persistence.test.ts"
      provides: "tests que fijan la degradación a 'fresh' y el camino feliz intacto, sustituyendo a los dos que fijaban el comportamiento antiguo"
      contains: "WR-03"
  key_links:
    - from: "resume()"
      to: "isValidContext"
      via: "guarda única al principio de la función, antes de formatVersion/contentVersion/runtimeId"
      pattern: "if \\(!isValidContext\\(persisted\\.context\\)\\)"
    - from: "resume() con context inválido"
      to: "app/pages/[game]/index.vue (onMounted)"
      via: "outcome 'fresh' deja session.value en null y la plantilla muestra el mini-setup"
      pattern: "outcome: 'fresh'"
---

<objective>
Cerrar **WR-03 (ronda 3)** de `09-REVIEW.md`, recogido por `09-VERIFICATION.md` en su tabla de
anti-patrones (`engine/persistence.ts:94`, severidad ⚠️ WARNING): ante un `context` persistido
que no supera `isValidContext`, la rama `resumed` de `resume()` adopta `fresh.context` —que la
página construye como relleno explícito, `expand(game, { playerCount: 1, difficulty: 'normal' })`,
`app/pages/[game]/index.vue:149`— pero conserva `outcome: 'resumed'`.

Consecuencia concreta, no teórica: el grupo ve «Partida guardada … CONTINUAR», continúa en el
paso correcto, y a partir de ahí toda la sesión miente. La banda de contadores pinta 1 héroe
en lugar de 4, la cabecera dice «1 jug · Normal», y **al terminar, `buildHistoryEntry` escribe
esa mentira en el histórico** (`playerCount: 1`, `difficulty: 'normal'`) sin ninguna marca de
degradación. Es el caso peor de la fase: no una partida ausente, sino una partida presente con
datos incorrectos, indistinguible de una correcta.

Propósito: el cierre de la ronda 2 (plan 09-12) hizo que la rama `resumed` dejara de propagar
un `context` sin forma — arregló el DATO y dejó intacta la ETIQUETA. Un `context` que no se
puede validar no es una partida reanudable: es una partida cuya configuración se ha perdido, y
lo único honesto es que el mini-setup vuelva a preguntar dos cosas que se contestan en cinco
segundos. Este plan es también, deliberadamente, el tercer frente del mismo barrido que
09-13 y 09-14: los tres cierran casos donde un lado de un contrato se endureció y el vecino
quedó afirmando algo que ya no era cierto.

Salida: una guarda única al principio de `resume()` que degrada a `'fresh'`, un `isValidContext`
que valida por rango y por valor en vez de por `typeof`, y tres tests que sustituyen a los dos
que fijaban el comportamiento antiguo como correcto.
</objective>

<scope_boundary>
DENTRO de este plan, y solo esto:

- La guarda de `context` en `resume()` y su degradación a `outcome: 'fresh'` (el fix que
  `09-REVIEW.md` §WR-03 propone literalmente).
- El endurecimiento de `isValidContext` a «entero positivo» + «una de las dos dificultades».
  Va DENTRO porque es el caso adyacente exacto: sin él, un `playerCount: NaN` seguiría
  superando la validación, se seguiría reanudando bajo la etiqueta «resumed» y seguiría
  produciendo una entrada de histórico con `playerCount` derivado de otra cosa. El plan 09-12
  descartó a propósito este endurecimiento porque entonces habría convertido sesiones
  reanudables en reinicios SILENCIOSOS; ahora el reinicio es explícito (el mini-setup
  pregunta), así que la objeción de entonces ya no aplica. Un `context` legítimo, escrito por
  el mini-setup, no puede fallar estas comprobaciones: `playerCount` sale de un selector de
  1 a 4 y `difficulty` de un par de botones.
- La actualización de los DOS tests que fijan el comportamiento antiguo
  (`engine/__tests__/persistence.test.ts:104` y `:291`), que es una edición legítima y
  necesaria: fijan como correcto justo lo que este plan declara defectuoso.

FUERA de alcance, anotado para que la re-verificación no lo confunda con un descuido:

- Los dos BLOCKER de la ronda 3: **CR-01** lo cierra 09-13 (`usePersistedSession.ts`),
  **CR-02** lo cierra 09-14 (`useGameHistory.ts` + `engine/history.ts`). Este plan no toca
  ninguno de esos ficheros y corre en paralelo con ambos en la ola 9.
- La normalización de `difficulty`/`playerCount`/`round` en `buildHistoryEntry` (cierre 09-12)
  se queda EXACTAMENTE como está. Sigue siendo necesaria: `resume()` no es el único camino por
  el que se construye una sesión, y el motor no puede depender de que su llamador haya
  validado antes (es el contrato escrito en su propia cabecera).
- `toPersistedPosition`, `contentChangedFallback` (más allá del comentario que explica por qué
  su guarda pasa a ser inalcanzable por construcción), `expand`, `next` y `engine/counters.ts`:
  no se tocan.
- El relleno `expand(game, { playerCount: 1, difficulty: 'normal' })` de
  `app/pages/[game]/index.vue:149` se queda como está. Es correcto para lo que hace
  (construir la SECUENCIA, que no depende del `context`), y su problema era exclusivamente
  que `resume()` lo ascendía a verdad. `index.vue` no se modifica en este plan — lo modifica
  09-16, en la ola siguiente.
</scope_boundary>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-12-PLAN.md

<interfaces>
Extraído del repo (no hay que explorar para averiguarlo):

`engine/persistence.ts` (104 líneas), estado ACTUAL tras el cierre 09-12:
- `PersistedPosition` (`:9-17`): `{ formatVersion: 1, gameId, contentVersion, runtimeId, round, context, updatedAt }`.
- `ResumeOutcome` (`:19`): `'fresh' | 'resumed' | 'content-changed'`. `ResumeResult` (`:21-24`):
  `{ session, outcome }`.
- `isValidContext(value): value is SessionContext` (`:45-49`) — privada. HOY exige objeto no
  nulo + `typeof playerCount === 'number'` + `typeof difficulty === 'string'`. Nada más:
  `NaN`, `-3`, `2.5` e `'imposible'` la superan.
- `contentChangedFallback(persisted, fresh)` (`:62-65`) — `const context = isValidContext(persisted.context) ? persisted.context : fresh.context`
  y devuelve `{ ...fresh, cursor: 0, round: 1, context }`.
- `resume(persisted, fresh)` (`:67-104`) — orden actual de decisiones:
  1. `persisted === null` ⇒ `{ session: fresh, outcome: 'fresh' }`.
  2. `persisted.formatVersion !== 1` ⇒ `contentChangedFallback`, `'content-changed'`.
  3. `persisted.contentVersion !== fresh.contentVersion` ⇒ ídem.
  4. `cursor === -1` (runtimeId no encontrado) ⇒ ídem.
  5. `return` final (`:94-103`): `context = isValidContext(...) ? persisted.context : fresh.context`,
     `round = Number.isInteger(persisted.round) && persisted.round >= 1 ? persisted.round : fresh.round`,
     `{ session: { ...fresh, cursor, round, context }, outcome: 'resumed' }`.

`app/pages/[game]/index.vue:140-166` — el consumidor, que YA sabe tratar las tres salidas:
`outcome === 'fresh'` ⇒ `resumeResolved.value = true` y `return` con `session.value` en `null`
(la plantilla muestra el mini-setup); `'resumed'` ⇒ `awaitingResumeChoice`; cualquier otra ⇒
`awaitingContentChangedAck`. NO hay que tocar nada ahí para que este plan funcione.

`engine/__tests__/persistence.test.ts` (316 líneas), arnés a reutilizar:
- `tinyGame` (fixture `./fixtures/tiny-game.json`), `marvelChampions` (contenido real
  validado), `catalogue`, `context` (`:17`, `{ playerCount: 3, difficulty: 'normal' }`),
  `basePersisted(overrides)` (`:33-44`, con `context: { playerCount: 2, difficulty: 'expert' }`
  y `runtimeId: 'intro.prep.02'`).
- Tests que este plan DEBE actualizar (fijan el comportamiento antiguo):
  `:104` («CR-01: con persisted parcial … nunca deja session.context indefinido», que afirma
  `outcome === 'content-changed'`) y `:291` («con context: {} persistido, resume() devuelve
  "resumed" pero adopta el context de la sesión FRESCA»).
- Tests que DEBEN seguir en verde SIN editarlos: `:47`, `:56`, `:66`, `:76`, `:86`, `:95`,
  `:118`, todo el `describe` de D-20 (`:150-220`), todo el de D-21 (`:221-289`), `:299` y
  `:307`. Todos usan un `context` con forma legítima, así que la guarda nueva no los alcanza.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un `context` que no se puede validar deja de ser una reanudación — guarda única en `resume()` e `isValidContext` por rango y por valor</name>
  <files>engine/persistence.ts</files>
  <read_first>
    - `engine/persistence.ts` COMPLETO (104 líneas) — el fichero a modificar. Con atención a:
      el comentario de `isValidContext` (`:39-44`, que ya declara que es «la última línea de
      defensa del motor ante un `persisted` cuya forma no se puede dar por buena»), el de
      `contentChangedFallback` (`:51-61`) y el bloque `:87-98` que el cierre 09-12 dejó
      escrito sobre la rama `resumed`, incluida la frase «esta rama y `contentChangedFallback`
      comparten criterio A PROPÓSITO — que nadie vuelva a endurecer solo una de las dos». Este
      plan cumple esa frase llevando el criterio a un único sitio.
    - `app/pages/[game]/index.vue` líneas 140-170 — el consumidor de `ResumeOutcome`, para
      confirmar con los propios ojos que la rama `'fresh'` ya existe, deja `session.value` en
      `null` y hace que la plantilla muestre el mini-setup. Este plan NO lo modifica, pero su
      corrección depende de que esa rama haga exactamente eso.
    - `engine/selection.ts:30-45` (`emptySelection`) y `:47-69` (`resolvePlayerSlots`) — el
      criterio `Number.isInteger(playerCount) && playerCount > 0` que ya usa todo el motor y
      que esta tarea replica en `isValidContext`, en vez de inventar uno nuevo.
    - `engine/history.ts:59-80` — la normalización de `difficulty`/`playerCount`/`round` del
      cierre 09-12, para confirmar que sigue en pie y que este plan NO la sustituye: son dos
      defensas a distinta altura, no una duplicada.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-03 — la cadena completa
      y el fix propuesto.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-12-PLAN.md` §Task 1, parte 2 — donde se
      descartó explícitamente endurecer `isValidContext` y por qué; esta tarea revierte esa
      decisión y debe dejar escrito el motivo del cambio de criterio.
  </read_first>
  <action>
    Modificar `engine/persistence.ts`. Dos cambios, ninguno más.

    **1. `isValidContext` valida por rango y por valor, no por `typeof`.**

    Sustituir las dos comprobaciones actuales por:
    - `playerCount`: `Number.isInteger(...)` **y** mayor que 0 — literalmente el mismo criterio
      que `resolvePlayerSlots` (`engine/selection.ts:50`) y `emptySelection` ya usan, para que
      el motor tenga un solo criterio de «cuántos juegan» y no dos.
    - `difficulty`: exactamente la cadena `'normal'` o la cadena `'expert'`. Son las dos únicas
      que el mini-setup puede producir y las dos únicas que `isGameHistoryEntry`
      (`app/composables/usePersistedSession.ts:137`) acepta al escribir en el histórico: un
      valor que la frontera de almacenamiento va a rechazar no puede ser «un context válido»
      aquí.

    Actualizar su comentario de bloque citando **WR-03 (`09-REVIEW.md`, ronda 3)** y dejando
    escrito el cambio de criterio respecto al plan 09-12: entonces no se endureció porque
    habría convertido sesiones reanudables en reinicios SILENCIOSOS; ahora el reinicio es
    explícito (el mini-setup pregunta de nuevo), así que la objeción ya no aplica. Añadir que
    un `context` escrito por el mini-setup no puede fallar estas comprobaciones, así que lo
    único que esta guarda puede rechazar es un dato manipulado, corrupto o de otra versión.

    **2. Una sola guarda de `context`, al principio de `resume()`, que degrada a `'fresh'`.**

    Insertar, inmediatamente después de la comprobación `persisted === null` y ANTES de la de
    `formatVersion`, una guarda que devuelva `{ session: fresh, outcome: 'fresh' }` cuando
    `isValidContext(persisted.context)` sea falso. Comentarla citando **WR-03** y nombrando la
    consecuencia que evita: sin configuración fiable no hay partida que reanudar, y la
    alternativa —seguir jugando con el relleno de la página bajo la etiqueta «reanudada»— hace
    que la banda de contadores, la cabecera y la entrada final del histórico afirmen datos que
    nadie ha elegido. Dejar explícito que `'fresh'` se elige frente a `'content-changed'`
    porque el contenido NO ha cambiado: decirlo sería otra afirmación falsa, y la pantalla de
    «el contenido ha cambiado» ofrece un CTA que abre partida, que es justo lo que aquí no se
    puede ofrecer.

    Retirar de la rama `resumed` (el `return` final) la expresión
    `isValidContext(persisted.context) ? persisted.context : fresh.context`, que pasa a ser
    código muerto, y dejar `context: persisted.context`. Sustituir el comentario de esa rama
    por uno que explique dónde vive ahora el criterio y por qué está arriba: para que las dos
    ramas no puedan volver a divergir, que es literalmente lo que el comentario anterior pedía
    y no conseguía. La normalización de `round` de esa rama (`Number.isInteger(persisted.round)
    && persisted.round >= 1 ? persisted.round : fresh.round`, cierre 09-12) se conserva tal
    cual: `round` no forma parte de `isValidContext` y sigue necesitando su propia guarda.

    En `contentChangedFallback`, conservar su expresión con `isValidContext` como defensa en
    profundidad, y añadir una línea de comentario diciendo que tras la guarda de arriba es
    inalcanzable desde `resume()` — su valor es impedir que un llamador futuro la invoque
    directamente con un `persisted` sin validar. NO borrarla: borrarla es exactamente el gesto
    que ha reabierto esta fase tres rondas seguidas.

    NO modificar `toPersistedPosition`, ni el tipo `ResumeOutcome` (las tres salidas siguen
    siendo las mismas), ni la firma de `resume`, ni ningún otro fichero del motor.
  </action>
  <verify>
    <automated>npx vitest run engine/__tests__/persistence.test.ts 2>&1 | tail -14</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^ *//' engine/persistence.ts | grep -c "if (!isValidContext(persisted.context))"` devuelve 1.
    - En la salida de `grep -n "export function resume" -A 8 engine/persistence.ts`, la línea de esa guarda tiene número MENOR que la de `persisted.formatVersion !== 1`.
    - `grep -v '^ *//' engine/persistence.ts | grep -c "Number.isInteger((value as SessionContext).playerCount)"` devuelve 1 (o la variante equivalente que el ejecutor elija, siempre con `Number.isInteger` dentro de `isValidContext`).
    - `grep -v '^ *//' engine/persistence.ts | grep -c "typeof (value as SessionContext).playerCount === 'number'"` devuelve 0.
    - `grep -v '^ *//' engine/persistence.ts | grep -c "typeof (value as SessionContext).difficulty === 'string'"` devuelve 0.
    - `grep -v '^ *//' engine/persistence.ts | grep -c "=== 'expert'"` devuelve al menos 1.
    - `grep -c "WR-03" engine/persistence.ts` devuelve al menos 2 (la guarda y el comentario de `isValidContext`).
    - `grep -v '^ *//' engine/persistence.ts | grep -c "isValidContext(persisted.context) ? persisted.context : fresh.context"` devuelve 1 (solo queda la de `contentChangedFallback`, la de la rama `resumed` ha desaparecido).
    - Comportamiento: `resume(basePersisted({ context: {} }), fresh)` devuelve `outcome === 'fresh'`, `session.cursor === 0`, `session.round === 1` y `session.context` igual a `fresh.context`.
    - Comportamiento: `resume(basePersisted({ context: { playerCount: Number.NaN, difficulty: 'normal' } }), fresh)` y `resume(basePersisted({ context: { playerCount: 2, difficulty: 'imposible' } }), fresh)` devuelven ambos `outcome === 'fresh'`.
    - Comportamiento: `resume(basePersisted(), fresh)` (context `{ playerCount: 2, difficulty: 'expert' }`) sigue devolviendo `outcome === 'resumed'` con `session.context` igual al persistido y `session.cursor` apuntando a `'intro.prep.02'`.
    - `npx vitest run engine/__tests__/persistence.test.ts` puede fallar EXACTAMENTE en los dos tests que la Task 2 actualiza (`:104` y `:291`) y en ningún otro; cualquier otro fallo indica que la guarda alcanza un caso legítimo.
  </acceptance_criteria>
  <done>
    Una sesión guardada cuya configuración no se puede dar por buena deja de continuar bajo la
    etiqueta «reanudada» con el relleno de la página: `resume()` devuelve `'fresh'`, el
    mini-setup vuelve a preguntar, y ninguna entrada del histórico puede quedar con un
    `playerCount`/`difficulty` que nadie eligió.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tests — la degradación a `'fresh'` sustituye a los dos que fijaban el comportamiento antiguo</name>
  <files>engine/__tests__/persistence.test.ts</files>
  <read_first>
    - `engine/__tests__/persistence.test.ts` COMPLETO — el fichero a modificar. En concreto:
      `basePersisted` (`:33-44`), el test `:104` y el `describe('CR-01 (ronda 2): la rama
      `resumed` valida el context igual que `content-changed`')` (`:290-316`), que son los dos
      únicos bloques que este plan puede editar. Y los `describe` de D-20 (`:150`) y D-21
      (`:221`), que son los que NO pueden romperse: si alguno se vuelve rojo, la guarda de la
      Task 1 está alcanzando un `context` legítimo.
    - `engine/persistence.ts` YA CORREGIDO por la Task 1.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` §WR-03 — su párrafo final
      dice literalmente «Actualizar los dos tests que fijan el comportamiento actual»; esta
      tarea es ese trabajo.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-12-PLAN.md` §Task 3 — el estilo de
      nombrado de los `it` (cada uno cita el hallazgo) y la prueba anti-tautología con
      pathspec explícito.
  </read_first>
  <action>
    **1. Actualizar los dos tests que fijan el comportamiento antiguo.** No borrarlos: son
    cobertura real de un caso real, y lo único que cambia es la expectativa. En ambos, añadir
    al nombre del `it` la cita «WR-03 (ronda 3)» junto a la que ya lleva, y un comentario de
    dos líneas explicando el cambio de expectativa — que la re-verificación pueda distinguir
    de un vistazo «test actualizado con motivo» de «test aflojado para que pase».

    - `:104` («CR-01: con persisted parcial (context ausente…) nunca deja session.context
      indefinido»): la expectativa pasa de `outcome === 'content-changed'` a
      `outcome === 'fresh'`. Las otras tres aserciones (`session.context` definido, con
      `playerCount` numérico y `difficulty` de tipo cadena) se conservan sin tocar: siguen
      siendo ciertas y siguen siendo el punto del test.
    - El `describe` de `:290`: su primer `it` pasa a afirmar que con `context: {}` persistido
      `resume()` devuelve `outcome === 'fresh'`, que `session.context` es igual a
      `fresh.context`, y —añadido nuevo— que `session.cursor` es 0 y `session.round` es 1, es
      decir, que la sesión devuelta es la fresca ENTERA y no una mezcla. Su segundo `it` (el
      camino feliz con `context` válido) no se toca ni una letra: es el anti-regresión. Su
      tercer `it` (`round: NaN`) tampoco: `basePersisted` le da un `context` válido, así que la
      guarda nueva no lo alcanza y sigue probando lo que probaba.

    **2. Añadir un `describe` nuevo**, titulado citando **WR-03 (ronda 3)**: «un context que no
    se puede validar no es una partida reanudable». Cinco `it`:

    1. `context: { playerCount: Number.NaN, difficulty: 'normal' }` ⇒ `outcome === 'fresh'`.
    2. `context: { playerCount: 0, difficulty: 'normal' }` y `context: { playerCount: 2.5,
       difficulty: 'normal' }` ⇒ `'fresh'` los dos.
    3. `context: { playerCount: 2, difficulty: 'imposible' }` y
       `context: { playerCount: 2, difficulty: 7 }` ⇒ `'fresh'` los dos.
    4. `context: null` y `context: []` (los dos superan `isPersistedPosition` en la capa de
       storage, que solo exige «objeto no nulo»… y `[]` lo es) ⇒ `'fresh'`, sin lanzar.
    5. Anti-regresión explícita del rango legítimo: para `playerCount` de 1, 2, 3 y 4 con
       `difficulty` `'normal'` y `'expert'`, `resume()` devuelve `'resumed'` y conserva el
       `context` persistido intacto. Es el test que impide que un endurecimiento futuro se
       coma el camino feliz.

    **3. Comprobar explícitamente que D-20 y D-21 siguen en verde** ejecutando el fichero
    completo, y anotarlo en el SUMMARY: son las dos baterías que prueban la reanudación de
    sesiones guardadas por versiones anteriores de la app, y son la señal de alarma de que
    esta guarda se ha pasado de estricta.

    Tras escribirlos, ejecutar la prueba anti-tautología y dejar constancia en el SUMMARY:
    `git stash push -- engine/persistence.ts` (pathspec explícito), `npx vitest run
    engine/__tests__/persistence.test.ts` para confirmar que los tests nuevos y los dos
    actualizados FALLAN contra el código previo, y `git stash pop`.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` termina con código 0 y con estrictamente más tests que la línea base de este fichero (21 `it` antes de este plan en `engine/__tests__/persistence.test.ts`; `grep -c "  it(" engine/__tests__/persistence.test.ts` debe devolver al menos 26).
    - `grep -c "WR-03" engine/__tests__/persistence.test.ts` devuelve al menos 3.
    - `grep -c "toBe('content-changed')" engine/__tests__/persistence.test.ts` devuelve exactamente 4 (los cuatro casos legítimos de cambio de contenido: formatVersion, contentVersion, runtimeId inexistente y el de `:66`), y ninguno de ellos es el del `context` parcial.
    - `npx vitest run engine/__tests__/persistence.test.ts -t "D-20"` termina con código 0.
    - `npx vitest run engine/__tests__/persistence.test.ts -t "D-21"` termina con código 0.
    - `git diff engine/__tests__/persistence.test.ts` no muestra ningún `it` eliminado: solo dos expectativas cambiadas (con su comentario de motivo) y adiciones.
    - Prueba anti-tautología ejecutada y anotada en el SUMMARY: con `git stash push -- engine/persistence.ts` aplicado, `npx vitest run engine/__tests__/persistence.test.ts` FALLA en los tests nuevos; tras `git stash pop`, pasa.
    - `npm run build` termina con código 0.
  </acceptance_criteria>
  <done>
    El comportamiento nuevo está fijado por tests que cubren los cuatro sabores de `context`
    inválido y, sobre todo, por un anti-regresión que recorre las ocho combinaciones legítimas
    de `playerCount`/`difficulty` — así un endurecimiento futuro no puede llevarse por delante
    una reanudación real sin ponerse rojo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `window.localStorage` (`tga:progress:<gameId>`) → `resume()` | **Entrada no fiable**: residuo de una build anterior con otra forma de `context`, edición manual en DevTools, escritura interrumpida por cuota. `isPersistedPosition` (capa de storage) solo comprueba que `context` sea un objeto no nulo — ni `[]` ni `{}` se filtran allí. |
| `resume()` → sesión viva → banda de contadores, cabecera y `buildHistoryEntry` | Lo que esta función deja en `session.context` se pinta como verdad en tres sitios y acaba escrito en el histórico. Una degradación no señalizada aquí es una afirmación falsa río abajo. |
| `fresh.context` (relleno de `app/pages/[game]/index.vue:149`) | Dato de relleno DECLARADO como tal en el propio código (`{ playerCount: 1, difficulty: 'normal' }`, solo sirve para construir la secuencia). Ascenderlo a configuración de partida es el defecto que este plan cierra. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15-01 | Repudiation (la app afirma una configuración que nadie eligió) | rama `resumed` de `resume()` en `engine/persistence.ts` | mitigate | Task 1: guarda única al principio que degrada a `outcome: 'fresh'` cuando el `context` no se puede validar. Tests 1-4 del `describe` nuevo. Severidad `high`: produce una entrada de histórico con datos INCORRECTOS, indistinguible de una correcta — peor que una entrada ausente. |
| T-09-15-02 | Tampering (type confusion que supera la validación) | `isValidContext` aceptando `NaN`, `0`, `2.5` e `'imposible'` por `typeof` | mitigate | Task 1: validación por rango (`Number.isInteger` y `> 0`) y por valor (`'normal'`/`'expert'`), alineada con `resolvePlayerSlots` y con `isGameHistoryEntry`. Tests 1-3 del `describe` nuevo. Severidad `medium`. |
| T-09-15-03 | Denial of Service (una guarda demasiado estricta borra partidas legítimas) | La propia guarda nueva, si rechazara un `context` real | mitigate | Riesgo INTRODUCIDO por este plan, no preexistente: se mitiga con el test 5 (anti-regresión sobre las ocho combinaciones legítimas de `playerCount`×`difficulty`) y con la exigencia de que D-20 y D-21 sigan en verde sin editarlos. Además, `resume()` devolviendo `'fresh'` no borra nada del disco: el blob de progreso sigue ahí hasta que el mini-setup escriba encima. |
| T-09-15-04 | Information disclosure | `tga:progress:<gameId>` | accept | Nº de jugadores, dificultad y posición en la partida, solo en el dispositivo, sin red ni backend. Nada que mitigar. |
| T-09-15-05 | Spoofing / Elevation of privilege | — | accept | No aplican: la app no tiene identidades, sesiones autenticadas ni privilegios que escalar. |
| T-09-15-SC | Tampering (supply chain) | Instalaciones npm/pip/cargo | accept | Ninguna tarea de este plan instala ni actualiza paquetes: se modifican un fichero TypeScript y uno de test, ambos existentes. Si durante la ejecución surgiera la necesidad de instalar algo, este plan queda bloqueado hasta que un checkpoint humano lo apruebe. |
</threat_model>

<verification>
1. `npx vitest run` — suite completa en código 0. Ningún test de D-20/D-21 editado.
2. `grep -v '^ *//' engine/persistence.ts | grep -c "if (!isValidContext(persisted.context))"` devuelve 1.
3. `grep -v '^ *//' engine/persistence.ts | grep -c "typeof (value as SessionContext).playerCount === 'number'"` devuelve 0.
4. `npx vitest run engine/__tests__/persistence.test.ts -t "D-20"` y `-t "D-21"` terminan
   ambos con código 0.
5. `grep -ric "firestore\|firebase" app/ engine/` devuelve 0 — SC5 y STAT-04 intactos.
6. `npm run build` termina con código 0.
7. Prueba anti-tautología con `git stash push -- engine/persistence.ts` ejecutada y anotada en
   el SUMMARY.

**Verificación humana recomendada (no bloqueante, ROADMAP §Fase 9):** con la app abierta en
una partida en curso, editar `tga:progress:marvel-champions` en DevTools poniendo
`"context": {}` y recargar. Debe aparecer el mini-setup preguntando jugadores y dificultad, no
la pantalla «Partida guardada … CONTINUAR». Anotar el resultado en el SUMMARY.
</verification>

<success_criteria>
- Un `context` persistido que es `{}`, `null`, `[]`, o que tiene `playerCount` no entero /
  no positivo o `difficulty` distinta de `'normal'`/`'expert'`, produce `outcome: 'fresh'` y
  la sesión fresca entera (cursor 0, round 1, `fresh.context`).
- Las ocho combinaciones legítimas de `playerCount` (1-4) × `difficulty`
  (`normal`/`expert`) siguen produciendo `outcome: 'resumed'` con el `context` persistido
  intacto y el cursor apuntando al paso guardado.
- Las baterías D-20 y D-21 (reanudación de sesiones de versiones anteriores) siguen en verde
  sin editarlas.
- El criterio de «qué `context` puede entrar en la sesión viva» se aplica en UN solo punto de
  `resume()`, y el de `contentChangedFallback` queda documentado como inalcanzable por
  construcción en lugar de como una segunda fuente de verdad.
- Los tests nuevos fallan si se revierte `engine/persistence.ts`.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-15-SUMMARY.md` al terminar.
</output>
