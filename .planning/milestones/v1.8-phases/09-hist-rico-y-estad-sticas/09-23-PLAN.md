---
phase: 09-hist-rico-y-estad-sticas
plan: 23
type: execute
wave: 16
depends_on: ["09-19", "09-21", "09-22"]
gap_closure: true
files_modified:
  - .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md
  - .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md
  - .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HIST-06, HIST-09]

must_haves:
  truths:
    - "El agujero de proceso queda cerrado, no solo el bug: la pregunta Q3 del barrido de fronteras («¿puede el valor que devuelve esta función afirmar algo que no ha ocurrido?») se extiende a la superficie que 09-AUDIT-FRONTERAS.md §5 excluyó por diseño — lo que la interfaz le afirma al grupo sobre sus datos"
    - "El barrido nuevo es acotado y comprobable: cubre exactamente los ficheros .vue que `find app -name '*.vue'` enumera, ni uno más ni uno menos, con veredicto explícito por fichero"
    - "Los dos diferidos que el verificador reclasificó (WR-04, WR-05b) figuran como CERRADOS en deferred-items.md, con el plan que los cerró — ninguno vuelve a aplazarse por motivo de alcance"
    - "Cualquier hallazgo nuevo del barrido queda registrado con veredicto de RIESGO evaluado, nunca con «fuera del perímetro de este plan»"
  artifacts:
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md"
      provides: "inventario de las afirmaciones de estado que la interfaz hace al grupo, con el valor de retorno que respalda cada una"
      min_lines: 60
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md"
      provides: "WR-04 y WR-05(b) marcados como cerrados, con el plan y la fecha"
      contains: "CERRADO"
  key_links:
    - from: "09-AUDIT-FRONTERAS.md §5 (fuera de perímetro: los .vue)"
      to: "09-AUDIT-AFIRMACIONES-UI.md"
      via: "nota de continuación que nombra el documento que cubre lo que §5 dejó fuera y por qué hizo falta"
      pattern: "09-AUDIT-AFIRMACIONES-UI"
---

<objective>
Cerrar el **agujero de proceso**, no solo el defecto. `09-AUDIT-FRONTERAS.md` (plan 09-17) hizo un
barrido de 54 funciones con tres preguntas, una de ellas —Q3— formulada precisamente para cazar
el patrón de fondo de esta fase: *«¿puede el valor que devuelve esta función afirmar algo que no
ha ocurrido?»*. Y aun así la ronda 4 encontró un BLOCKER nuevo, porque §5 de ese mismo documento
declaró **fuera de perímetro los ficheros `.vue`** — y la afirmación sin respaldo se había mudado
justo allí: al texto que el aviso le muestra al grupo.

Q3 mira lo que una función le devuelve a otra función. Lo que faltaba es **Q4: ¿puede lo que esta
pantalla le afirma al grupo sobre sus datos no ser cierto?** Este plan escribe ese barrido, de
forma acotada y verificable: exactamente los ficheros `.vue` que `find app -name "*.vue"`
enumera (24 a fecha de este plan), un veredicto por fichero, y ni una función más de las 54 ya
auditadas.

Además deja formalmente cerrados en `deferred-items.md` los dos diferidos que el verificador
reclasificó (WR-04, WR-05b), y sincroniza las dos filas de `REQUIREMENTS.md` que esta ronda toca.

Purpose: que la quinta ronda no encuentre el mismo patrón en una tercera superficie.
Output: `09-AUDIT-AFIRMACIONES-UI.md`, nota de continuación en `09-AUDIT-FRONTERAS.md`,
`deferred-items.md` al día, `REQUIREMENTS.md` sincronizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md
@.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
</context>

<scope_boundary>
**DENTRO:** documentos de planificación, y solo los cuatro nombrados en `files_modified`.

**FUERA:**
- **Cualquier fichero de código.** Este plan no modifica ni una línea de `app/`, `engine/`,
  `e2e/` ni `scripts/`. Si el barrido encuentra un defecto nuevo, se REGISTRA con su veredicto;
  no se arregla aquí.
- Re-auditar las 54 funciones de `09-AUDIT-FRONTERAS.md` §2 — ya están auditadas, no se repiten.
- Ampliar el barrido a `e2e/`, `scripts/` o `nuxt.config.ts`.
- Los WARNING de defensa en profundidad ante un `localStorage` manipulado (`Number.isFinite` vs
  `Number.isInteger`, cadena vacía, `new Date(now).toISOString()` sin guarda): el verificador los
  deja explícitamente fuera de esta ronda. Ya están registrados; no se reabren.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: barrido Q4 — qué le afirma la interfaz al grupo sobre sus datos, y qué lo respalda</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` — §1 «Método»
      (líneas 26-85) COMPLETA: es la plantilla de la que este barrido hereda forma, vocabulario
      de veredictos (OK / DEFECTO / ACEPTADO) y densidad. Leer también §5 «Fuera de perímetro»
      (líneas 315-347), que es el agujero que este documento tapa.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el bloque `gaps:`
      completo y los dos párrafos del «Resumen ejecutivo» que explican por qué el defecto
      reapareció en la superficie que §5 excluyó.
    - El índice del barrido: ejecutar `find app -name "*.vue" | sort` y usar esa salida como
      lista cerrada. No auditar nada que no esté en ella.
    - Cada uno de esos ficheros, leídos de UNA sola pasada. Para los que no muestren ninguna
      afirmación sobre datos persistidos basta con confirmarlo; NO releer ninguno.
    - `app/composables/useHistorySavedNotice.ts` (con `NOTICE_HEADING`/`NOTICE_BODY` del plan
      09-20) y `app/composables/useGameHistory.ts` (`emptyTitle`/`emptyBody`,
      `buildHistoryCardView`): el texto visible que no vive en el `.vue` también entra en el
      barrido cuando es lo que la pantalla acaba mostrando.
  </read_first>
  <action>
    Escribir `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md`, en
    español, con esta estructura de cinco secciones:

    **§1 Método.** Enunciar Q4 y por qué no la cubría ninguna de las tres preguntas de
    `09-AUDIT-FRONTERAS.md`: Q1 (fallo a medias), Q2 (clave no confiable) y Q3 (retorno que
    miente) miran lo que una función le pasa a otra función. Q4 mira **la última costura**, la
    que llega a los ojos del grupo: *¿esta frase que la pantalla muestra afirma algo sobre los
    datos del grupo, y hay un valor de retorno real que lo respalde?* Documentar que este barrido
    nace de CR-01 ronda 4, que reapareció exactamente ahí.

    Fijar el criterio de inclusión, para que el barrido sea reproducible y no una lista a ojo:
    entra en el inventario **toda frase que la interfaz muestre y que afirme un hecho sobre el
    estado de los datos persistidos del grupo** (histórico, progreso de partida, preferencia de
    voz): que existen, que se guardaron, que se borraron, que siguen ahí, que no hay ninguno. NO
    entran las etiquetas de control («GANADA», «Cerrar aviso»), ni las preguntas, ni los textos
    que describen una acción futura sin afirmar un estado presente.

    **§2 Inventario.** Una fila por **cada** fichero de la salida de
    `find app -name "*.vue" | sort`, en ese mismo orden. Columnas: `Fichero` | `Afirmación (texto
    literal o su origen)` | `Valor de retorno que la respalda` | `Veredicto`. Los ficheros sin
    ninguna afirmación llevan una sola fila con `— (sin afirmación de estado)` y veredicto
    `NO APLICA`. Los que tienen varias llevan una fila por afirmación.

    Veredictos permitidos, exactamente tres:
    - `RESPALDADA` — existe un valor de retorno concreto que la sostiene; nombrarlo (función y
      fichero).
    - `DEFECTO UI-xx` — la afirmación no tiene respaldo; identificador correlativo, con el
      escenario concreto en que resulta falsa.
    - `ACEPTADO` — la afirmación es imprecisa pero el riesgo está evaluado y es bajo. **La
      justificación tiene que ser de RIESGO, nunca de alcance de plan.** Escribir esa regla
      dentro del documento, citando el dictamen del verificador sobre los dos diferidos
      reclasificados («es una razón de ALCANCE DE PLAN, no una evaluación de riesgo aceptado»).

    Prestar atención especial —son los candidatos evidentes— a: la banda de
    `HistorySavedNotice.vue` (ya corregida por 09-20/09-21: debe salir `RESPALDADA`, nombrando
    `record()` y `save()`); el estado vacío de `historico.vue` («Todavía no hay partidas
    registradas», que con un `tga:history` ilegible afirma algo que la app no sabe — ya está
    registrado como WR-02 en `deferred-items.md`: comprobar si sigue vigente y ENLAZARLO en vez
    de duplicarlo); el estado vacío de `estadisticas.vue`; el «Partida guardada · CONTINUAR» de
    `ResumePrompt.vue` / `GameSelectorScreen.vue`; el aviso de progreso que se borrará de
    `GameOutcomeDialog.vue`; y `ContentChangedNotice.vue`.

    **§3 Defectos.** Un apartado por cada `DEFECTO UI-xx`, con fichero, línea, escenario
    reproducible y propuesta de arreglo. Si no hay ninguno, decirlo con esa misma frase explícita
    en vez de omitir la sección.

    **§4 Recuento.** Número de ficheros auditados (debe coincidir exactamente con el de la salida
    de `find`), número de afirmaciones inventariadas, y desglose por veredicto.

    **§5 La regla que este barrido deja escrita para el futuro.** Una sola frase, destacada:
    *ninguna frase que la interfaz muestre al grupo sobre el estado de sus datos puede carecer de
    un valor de retorno real que la respalde; si hace falta una frase nueva, primero el booleano
    que la sostiene y el test que lo fija, después el texto.*

    NO arreglar nada de código en esta tarea. NO inventar afirmaciones que no estén en el código:
    cada fila del inventario tiene que poder contrastarse abriendo el fichero que nombra.
  </action>
  <verify>
    <automated>bash -c 'D=.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md; test -f "$D" || exit 1; F=0; for f in $(find app -name "*.vue" | sort); do grep -q "$(basename "$f")" "$D" || { echo "FALTA $f"; F=1; }; done; exit $F'</automated>
  </verify>
  <acceptance_criteria>
    - El comando de `verify` sale 0: cada fichero devuelto por `find app -name "*.vue" | sort` aparece nombrado en el documento. Ninguna línea `FALTA …` en su salida.
    - `grep -c "^| " .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos tantas filas como ficheros liste `find app -name "*.vue" | wc -l` (24 a fecha de este plan), cabeceras de tabla aparte.
    - `grep -cE "RESPALDADA|DEFECTO UI-|ACEPTADO|NO APLICA" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 24 — hay veredicto explícito por fila, ninguna casilla en blanco.
    - `grep -c "fuera del perímetro" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve 0 salvo dentro de la cita del dictamen del verificador — ningún veredicto se justifica por alcance.
    - `grep -c "HistorySavedNotice.vue" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 1 y su fila lleva veredicto `RESPALDADA`, nombrando `record()` y `save()`.
    - `grep -c "Q4" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 2 (enunciado + método).
    - `git status --porcelain app engine e2e scripts` no devuelve ninguna línea — esta tarea no toca código.
  </acceptance_criteria>
  <done>
    Existe un inventario cerrado y contrastable de todo lo que la interfaz de esta app le afirma
    al grupo sobre sus datos, con el valor de retorno que respalda cada afirmación.
  </done>
</task>

<task type="auto">
  <name>Task 2: cerrar los dos diferidos reclasificados, enlazar el barrido nuevo y sincronizar la trazabilidad</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md, .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` — COMPLETO. Las dos
      entradas a cerrar son «WR-04: `GameOutcomeDialog` sin `Escape`, sin gestión de foco y sin
      salida no terminal» y «WR-05 (b): `HistorySavedNotice`/`UpdateBanner` empujan fuera del
      viewport las pantallas `h-dvh`». Fijarse en el formato de las entradas anteriores que ya
      llevan «Actualización (09-17)» — se sigue ese mismo patrón.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` §5 (líneas 315-347) —
      es donde va la nota de continuación; en concreto el punto que dice «**WR-04** … y **WR-05
      (b)** …: viven en ficheros `.vue`, explícitamente fuera del perímetro de este plan».
    - Los SUMMARY de este lote que ya existan: `09-19-SUMMARY.md` (WR-04), `09-21-SUMMARY.md` y
      `09-22-SUMMARY.md` (WR-05b y el cierre del BLOCKER). De ellos salen los hechos concretos
      que se citan al cerrar cada entrada — no inventarlos.
    - `.planning/REQUIREMENTS.md` — las líneas 81 y 84 (checkboxes de HIST-06 y HIST-09), la nota
      de cierre de hueco de las líneas 86-99, y las filas 209 y 212 de la tabla de trazabilidad.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — la sección «Sobre los
      dos diferidos reclasificados» (líneas 176-187) y la fila HIST-06 de «Requirements
      Coverage», que dice «⚠ SATISFIED con reserva».
  </read_first>
  <action>
    **A. `deferred-items.md`.** Añadir al final de las entradas WR-04 y WR-05(b),
    respectivamente, un bloque `**Actualización (ronda 4, plan 09-19)** — CERRADO:` y
    `**Actualización (ronda 4, plan 09-22)** — CERRADO:` que diga, en cada caso:
    - Que `09-VERIFICATION.md` (ronda 4) reclasificó la entrada de «diferido» a «hallazgo real»
      porque su motivo de aplazamiento era de ALCANCE DE PLAN, no una evaluación de riesgo.
    - Qué se hizo exactamente, tomándolo del SUMMARY correspondiente: en WR-04,
      `aria-labelledby`, foco al panel al abrir y devolución al cerrar, y `Escape` resuelto como
      decisión de diseño documentada contra `09-UI-SPEC.md` §Layout 2 (no se implementa a
      propósito: las cuatro salidas terminan la partida). En WR-05(b), las dos bandas fuera del
      flujo con `pointer-events-none` y controles en `pointer-events-auto`, sin tocar ninguna
      pantalla `h-dvh`.
    - En WR-04, dejar escrito que la parte de `Escape` queda **resuelta, no pendiente**, con la
      razón — para que una ronda futura no la reabra como olvido.

    NO borrar el texto original de ninguna de las dos entradas: el historial de por qué se
    aplazaron mal es parte de lo que esta fase aprende.

    Las demás entradas (WR-02, WR-07, los INFO IN-04/05/07/11/12/13, la sincronización de
    `REQUIREMENTS.md`) **no se tocan**: siguen aplazadas con motivos que sí son de riesgo o de
    contrato, no de alcance.

    **B. `09-AUDIT-FRONTERAS.md` §5.** Añadir al final de la sección una nota de continuación,
    sin reescribir nada de lo que ya hay:
    - Que el punto de §5 que dejó WR-04 y WR-05(b) fuera «por vivir en ficheros `.vue`» fue,
      exactamente, el agujero por el que se coló CR-01 de la ronda 4: la afirmación sin respaldo
      se había mudado a la superficie de interfaz que este barrido excluyó por diseño.
    - Que el perímetro queda extendido por `09-AUDIT-AFIRMACIONES-UI.md` (plan 09-23) con una
      cuarta pregunta, Q4, sobre lo que la interfaz le afirma al grupo, y que las dos entradas
      quedan cerradas por los planes 09-19 y 09-22.
    - La regla de proceso en una frase: **un veredicto de «fuera de perímetro» es válido cuando
      el perímetro es un límite de RIESGO; no lo es cuando es solo el encargo de un plan.**

    **C. `.planning/REQUIREMENTS.md`.** Dos cambios, ninguno más:
    - En la nota de cierre de hueco (líneas 86-99), añadir un párrafo de ronda 4 que registre que
      la reserva que `09-VERIFICATION.md` puso sobre HIST-06 —el mecanismo de recuperación del
      PROGRESO que 09-16 añadió y cuya promesa no se sostenía— queda cerrada por los planes
      09-18 (`save()` devuelve el resultado real), 09-20 (`onOutcomeRecorded` lo produce y lo
      comprueba; test de regresión) y 09-21 (la superficie solo pinta lo decidido).
    - En la tabla de trazabilidad, actualizar la columna de fase/plan de las filas HIST-06
      (línea 209) y HIST-09 (línea 212) para incluir los planes de este lote
      (`09-18..09-23`), manteniendo el texto de estado existente y la nota que ya enlaza.

    NO marcar ni desmarcar ningún checkbox: los dos requisitos ya están en `[x]` y esta ronda no
    cambia eso. NO tocar ninguna otra fila (la sincronización íntegra de HIST-01/02/03/05/07/08
    sigue siendo el diferido abierto que `deferred-items.md` registra desde 09-03).
  </action>
  <verify>
    <automated>bash -c 'grep -c "CERRADO" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md | grep -qE "^[2-9]" && grep -q "09-AUDIT-AFIRMACIONES-UI" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md && git status --porcelain app engine e2e scripts | wc -l | grep -q "^ *0$"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CERRADO" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 2.
    - `grep -n "WR-04" -A 40 .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md | grep -c "09-19"` devuelve al menos 1, y la misma ventana contiene la cadena `Escape` junto a `09-UI-SPEC` — el cierre de la parte de `Escape` está justificado, no pendiente.
    - `grep -n "WR-05 (b)" -A 30 .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md | grep -c "09-22"` devuelve al menos 1.
    - `grep -c "WR-02" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` sigue devolviendo el mismo valor que antes del plan — las entradas legítimamente aplazadas no se han tocado.
    - `grep -c "09-AUDIT-AFIRMACIONES-UI" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md` devuelve al menos 1.
    - `grep -n "^## 5. Fuera de perímetro" -A 60 .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-FRONTERAS.md | grep -c "Q4"` devuelve al menos 1.
    - `grep -c "09-18" .planning/REQUIREMENTS.md` devuelve al menos 1 y `grep -n "| HIST-06 |" .planning/REQUIREMENTS.md | grep -c "09-2"` devuelve 1 — la fila de trazabilidad nombra los planes de este lote.
    - `grep -c "^- \[x\] \*\*HIST-06\*\*" .planning/REQUIREMENTS.md` devuelve 1 y `grep -c "^- \[x\] \*\*HIST-09\*\*" .planning/REQUIREMENTS.md` devuelve 1 — ningún checkbox ha cambiado de estado.
    - `git status --porcelain app engine e2e scripts` no devuelve ninguna línea.
    - `git diff --name-only` lista exactamente los tres ficheros de esta tarea más el documento creado en la Task 1.
  </acceptance_criteria>
  <done>
    Los dos diferidos reclasificados están cerrados con la razón y el plan que los cerró; el
    documento que excluyó los `.vue` de su perímetro enlaza al que lo extiende; y la trazabilidad
    de HIST-06/HIST-09 refleja este lote.
  </done>
</task>

</tasks>

<verification>
- El comando de `verify` de la Task 1 sale 0: cobertura completa del índice de `.vue`.
- `git status --porcelain app engine e2e scripts` vacío tras las dos tareas — este plan no toca
  código.
- `npx vitest run` y `npm run build` siguen terminando con código 0 (no deberían verse
  afectados; comprobarlo es barato y descarta un cambio accidental de código).
</verification>

<success_criteria>
La cuarta ronda no se cierra solo arreglando el defecto: queda escrito, de forma acotada y
contrastable, qué le afirma esta interfaz al grupo sobre sus datos y qué respalda cada
afirmación; los dos diferidos con motivo de alcance quedan cerrados; y el documento que creó el
agujero de perímetro apunta ahora al que lo tapa.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-23-SUMMARY.md` al terminar.
</output>
