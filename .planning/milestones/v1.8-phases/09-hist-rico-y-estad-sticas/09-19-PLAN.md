---
phase: 09-hist-rico-y-estad-sticas
plan: 19
type: execute
wave: 12
depends_on: ["09-16", "09-17"]
gap_closure: true
files_modified:
  - app/components/GameOutcomeDialog.vue
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03]

must_haves:
  truths:
    - "El diálogo de fin de partida tiene nombre accesible: un lector de pantalla lo anuncia con la pregunta «¿Cómo terminó la partida?» en vez de con un «diálogo» anónimo"
    - "Al abrirse, el foco entra en el diálogo y deja de estar en el botón «Partida terminada» que quedó debajo del velo opaco — un usuario de teclado no puede seguir tabulando por la pantalla de juego oculta"
    - "El foco no se deposita en ninguno de los tres botones de registro: D-02 prohíbe empujar al grupo hacia un resultado, y un foco inicial sobre «GANADA» es exactamente eso"
    - "La ausencia de `Escape` queda documentada en el propio componente como decisión de diseño contrastada con 09-UI-SPEC.md §Layout 2, no como olvido — el diferido WR-04 deja de apoyarse en un motivo de alcance"
  artifacts:
    - path: "app/components/GameOutcomeDialog.vue"
      provides: "diálogo con aria-labelledby, foco gestionado al abrir y al cerrar, y nota de reconciliación escrita sobre Escape"
      contains: "aria-labelledby"
  key_links:
    - from: "el atributo aria-labelledby del contenedor role=dialog"
      to: "el id del <h1> «¿Cómo terminó la partida?»"
      via: "misma cadena literal en los dos sitios"
      pattern: "aria-labelledby=\"game-outcome-heading\""
    - from: "onMounted del diálogo"
      to: "el panel con tabindex=\"-1\""
      via: "focus() sobre el panel, nunca sobre un botón de resultado (D-02)"
      pattern: "tabindex=\"-1\""
---

<objective>
Cerrar **WR-04**, uno de los dos diferidos que `09-VERIFICATION.md` reclasifica de «aplazado» a
«hallazgo real, no bloqueante» en la ronda 4. El motivo que `deferred-items.md:61-80` registró
para aplazarlo fue literalmente «vive en un fichero `.vue` de pantalla, explícitamente fuera del
perímetro de este plan» — y el verificador dictamina que eso es **alcance de plan, no evaluación
de riesgo**, así que no es un aplazamiento válido: «deben entrar en el próximo lote de cierre
igual que CR-01, no quedar aplazados de nuevo con el mismo motivo de alcance».

`app/components/GameOutcomeDialog.vue:37-97` declara `role="dialog" aria-modal="true"` sin
`aria-labelledby`, sin mover el foco al abrir y sin devolverlo al cerrar, mientras que
`WarningDetailModal.vue` —el patrón bueno del repo— sí hace las tres cosas.

**La tercera pieza del hallazgo (`Escape`) se resuelve NO implementándola, y por escrito.**
`09-UI-SPEC.md` §Layout 2 fija explícitamente, como contrato de diseño de esta fase, «no
backdrop-tap-dismiss and no Escape-to-dismiss on this dialog … a diálogo this consequential
requires an explicit tap on one of its four options, never an accidental one». Las cuatro
salidas terminan la partida (D-01/HIST-02): un `Escape` accidental terminaría una partida en
curso. Eso es una evaluación de riesgo, la única que el diferido original no llegó a hacer, y es
lo que este plan deja escrito en el componente para que la próxima revisión no lo reabra como
olvido.

Purpose: que el diálogo más consecuente de la app tenga nombre accesible y foco gestionado, sin
tocar ni una de las cuatro salidas que HIST-01/HIST-02/HIST-03 fijan.
Output: `GameOutcomeDialog.vue` con `aria-labelledby`, foco de entrada y de salida, y nota de
reconciliación sobre `Escape`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@app/components/GameOutcomeDialog.vue
@app/components/WarningDetailModal.vue
</context>

<scope_boundary>
**DENTRO:** `app/components/GameOutcomeDialog.vue`, y nada más.

**FUERA:**
- Añadir, quitar o renombrar cualquiera de los cuatro botones (D-01 fija exactamente cuatro).
- Cambiar el texto visible de cualquiera de ellos (D-06 fija la redacción contrastada con el
  Rules Reference; `09-UI-SPEC.md` §Copywriting Contract exige que coincida con la línea de
  causa de la tarjeta del histórico).
- Cambiar qué emite cada botón, o hacer que alguna salida deje de terminar la partida.
- `app/pages/[game]/index.vue` — no hace falta tocarlo: el diálogo se monta con `v-if` y se
  desmonta solo (`:810-816`), así que el foco de salida se gestiona desde el propio componente.
- `ConfirmDialog.vue` — es un hueco conocido y registrado, NO se retrofita (ver la nota de
  cabecera de `WarningDetailModal.vue`, que lo dice literalmente).
- `deferred-items.md` — el cierre formal de la entrada WR-04 lo escribe el plan 09-23.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: nombre accesible y foco gestionado, sin empujar hacia ningún resultado</name>
  <files>app/components/GameOutcomeDialog.vue</files>
  <read_first>
    - `app/components/GameOutcomeDialog.vue` — el fichero a modificar, COMPLETO. Prestar
      atención a la NOTA DE RECONCILIACIÓN de la cabecera (D-01/D-02/HIST-02: «las cuatro
      terminan la partida», «quien vaya a añadir un quinto botón debe leer D-01 primero») y a
      los tres `ref` de press-feedback, que no se tocan.
    - `app/components/WarningDetailModal.vue` — COMPLETO. Es el patrón bueno del repo y de él
      se copia la FORMA: `ref` al elemento, `onMounted` → `focus()`, `onUnmounted` →
      limpieza, `aria-labelledby` apuntando al `id` del `<h1>`. Fijarse también en su comentario
      de cabecera, que declara que sus tres vías de cierre son propias suyas y NO se retrofitan
      a `ConfirmDialog`.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` — §Layout 2 completa (líneas
      135-207), en especial el punto de `Salir sin registrar` que fija «no backdrop-tap-dismiss
      and no Escape-to-dismiss on this dialog», y §Component Inventory → «Accessible names»,
      que dice que los cuatro controles no necesitan `aria-label` porque su texto visible ya es
      una frase completa.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — la fila
      `GameOutcomeDialog.vue | 37-97` de la tabla de Anti-Patrones (WARNING reclasificado desde
      «diferido») y la sección «Sobre los dos diferidos reclasificados».
    - `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` — la entrada §WR-04
      completa, incluido su «Acción sugerida», que ya avisa de que hay que decidir qué significa
      `Escape` frente a D-01 ANTES de implementarlo.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md` — D-01 (cuatro botones, un
      toque = un registro) y D-02 (ningún resultado se destaca sobre los otros).
  </read_first>
  <action>
    Modificar `app/components/GameOutcomeDialog.vue`. Tres cambios, ninguno más.

    **1. Nombre accesible.** Dar al `<h1>` que contiene `¿Cómo terminó la partida?` el atributo
    `id="game-outcome-heading"`, y añadir al `<div role="dialog" aria-modal="true">` el atributo
    `aria-labelledby="game-outcome-heading"`. Es la misma mecánica exacta que
    `WarningDetailModal.vue` usa con `warning-detail-heading`. No añadir `aria-describedby`: la
    línea de contexto y el aviso retenido son dos párrafos distintos y `09-UI-SPEC.md` no fija
    ninguna descripción accesible para ellos.

    **2. El foco entra en el diálogo al abrirse, y vuelve de donde vino al cerrarse.** Importar
    `onMounted`, `onUnmounted` y `ref` de `vue` (el fichero ya importa `ref`). Poner
    `tabindex="-1"` en el **panel interior** (el `<div class="w-full max-w-[640px] …">`, no en
    el contenedor `fixed inset-0`) y un `ref` de plantilla sobre él; en `onMounted`, guardar
    `document.activeElement` en una variable de módulo-del-componente tipada como
    `HTMLElement | null` y llamar a `focus()` sobre el panel; en `onUnmounted`, llamar a
    `focus()` sobre el elemento guardado si sigue existiendo, dentro de un `if` que compruebe
    que no es `null` y que tiene método `focus`.

    **El foco inicial va al PANEL, no a ningún botón, y esa es la diferencia deliberada con
    `WarningDetailModal.vue`** (que sí enfoca su único botón «Entendido» porque no hay elección
    que sesgar). Escribirlo como comentario junto al `onMounted`, citando D-02: los tres botones
    de registro son deliberadamente iguales en peso visual porque D-02 prohíbe empujar al grupo
    hacia un resultado; depositar el foco sobre «GANADA» sería exactamente ese empujón, en la
    capa de accesibilidad en vez de en la visual. Enfocar el panel mete al usuario de teclado
    dentro del diálogo (el siguiente `Tab` cae en «GANADA», el `Shift+Tab` en «Salir sin
    registrar») sin preseleccionar nada.

    **3. Nota de reconciliación escrita sobre `Escape`.** Añadir al comentario de cabecera del
    `<script setup>`, debajo de la NOTA DE RECONCILIACIÓN que ya existe, un párrafo que deje
    cerrado el asunto para la próxima revisión:
    - Este diálogo NO escucha `Escape` y NO se cierra al tocar el velo, **a propósito**:
      `09-UI-SPEC.md` §Layout 2 lo fija como contrato de diseño de la fase, y la razón es que
      las cuatro salidas terminan la partida (D-01/HIST-02), así que un cierre accidental
      terminaría una partida en curso. Es la misma postura de «sin descarte accidental» que
      `ConfirmDialog.vue` ya tiene.
    - `WarningDetailModal.vue` sí lo hace y no es contradicción: ese modal es informativo y se
      vuelve exactamente al mismo sitio; este no.
    - La salida por teclado existe igualmente: `Tab` hasta «Salir sin registrar» y `Enter`.
    - Citar `deferred-items.md` §WR-04 y `09-VERIFICATION.md` (ronda 4) para que quede claro que
      esto es una decisión contrastada, no un olvido pendiente.

    NO tocar la plantilla más allá de los dos atributos, el `tabindex` y el `ref`. NO cambiar
    clases de Tailwind. NO añadir trampa de foco (focus trap) cíclica: `09-UI-SPEC.md` no la
    pide, el diálogo es `fixed inset-0` opaco sobre toda la pantalla, y añadirla supondría un
    bucle de teclado hecho a mano que ningún otro modal del repo tiene.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aria-labelledby="game-outcome-heading"' app/components/GameOutcomeDialog.vue` devuelve exactamente 1.
    - `grep -c 'id="game-outcome-heading"' app/components/GameOutcomeDialog.vue` devuelve exactamente 1, y está en la misma línea que contiene `text-heading font-bold` (el `<h1>` de la pregunta).
    - `grep -c 'tabindex="-1"' app/components/GameOutcomeDialog.vue` devuelve exactamente 1, y la línea que lo contiene incluye también `max-w-[640px]` — el `tabindex` está en el panel interior, no en el contenedor `fixed inset-0`.
    - `grep -c "onMounted" app/components/GameOutcomeDialog.vue` y `grep -c "onUnmounted" app/components/GameOutcomeDialog.vue` devuelven cada uno al menos 2 (import + uso).
    - `grep -c "document.activeElement" app/components/GameOutcomeDialog.vue` devuelve exactamente 1.
    - `grep -c "Escape" app/components/GameOutcomeDialog.vue` devuelve al menos 1 **y** `grep -c "addEventListener" app/components/GameOutcomeDialog.vue` devuelve exactamente 0 — `Escape` se menciona solo en la nota de reconciliación, nunca se escucha.
    - `grep -c "09-UI-SPEC" app/components/GameOutcomeDialog.vue` devuelve al menos 1 — la nota cita el contrato de diseño que sostiene la decisión.
    - `grep -c "GANADA" app/components/GameOutcomeDialog.vue` devuelve exactamente 1, `grep -c "Se completó el Plan Principal"` devuelve exactamente 1, `grep -c "Todos los héroes eliminados"` devuelve exactamente 1 y `grep -c "Salir sin registrar"` devuelve exactamente 1 — los cuatro textos visibles siguen intactos (D-01/D-06).
    - `grep -c "emit('record'" app/components/GameOutcomeDialog.vue` devuelve exactamente 3 y `grep -c "emit('dismiss')" app/components/GameOutcomeDialog.vue` devuelve exactamente 1 — ninguna salida ha cambiado de contrato.
    - `npm run build` termina con código 0 (es la única comprobación automática de tipos y plantilla que cubre un `.vue` en este proyecto).
    - `npx vitest run` termina con código 0.
  </acceptance_criteria>
  <done>
    Un lector de pantalla anuncia el diálogo por su pregunta; el foco de teclado entra en él al
    abrirse y vuelve al botón «Partida terminada» al cerrarse; y la ausencia de `Escape` está
    justificada por escrito contra el contrato de diseño de la fase, no pendiente.
  </done>
</task>

</tasks>

<verification>
- `npm run build` termina con código 0.
- `npx vitest run` termina con código 0.
- `git diff --stat` muestra exactamente un fichero modificado: `app/components/GameOutcomeDialog.vue`.
- `grep -rn "GameOutcomeDialog" app/pages/\[game\]/index.vue` sigue mostrando el mismo bloque
  `:810-816` sin cambios: el componente no ha ganado ni perdido props ni eventos.
</verification>

<success_criteria>
WR-04 deja de ser un diferido con motivo de alcance: las dos piezas accionables (nombre
accesible, gestión de foco) están implementadas, y la tercera (`Escape`) está resuelta como
decisión de diseño documentada y contrastada contra `09-UI-SPEC.md` §Layout 2 y D-01/D-02.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-19-SUMMARY.md` al terminar.
</output>
