---
phase: 09-hist-rico-y-estad-sticas
plan: 27
type: execute
wave: 20
depends_on: ["09-26"]
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
  - .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md
autonomous: true
requirements: [HIST-06]

must_haves:
  truths:
    - "La nota de `REQUIREMENTS.md` sobre HIST-06 deja de dar por cerrado lo que la ronda 5 demostró que seguía abierto, y no lo sustituye por otra afirmación de cierre que nadie ha verificado"
    - "Lo que este lote NO cierra queda registrado con una evaluación de RIESGO que se sostiene sola — nunca con un motivo de alcance de plan, que la ronda 4 dictaminó inválido"
    - "El método de auditoría de afirmaciones incorpora la pregunta que habría cazado la ronda 5 durante el barrido de la ronda 4"
    - "La comprobación humana en tablet pendiente desde 09-22 sigue nombrada como ABIERTA, en ningún sitio como hecha"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "la corrección de la nota de cierre prematura sobre HIST-06 y el registro honesto de la ronda 5"
      contains: "ronda 5"
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md"
      provides: "cuatro entradas nuevas con justificación de riesgo medida"
      contains: "(ronda 4)"
    - path: ".planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md"
      provides: "la pregunta Q5 y la fila de HistorySavedNotice corregida"
      contains: "Q5"
  key_links:
    - from: ".planning/REQUIREMENTS.md (nota HIST-06)"
      to: "app/composables/useStoredProgress.ts"
      via: "la nota nombra la autoridad concreta que cierra el hueco, no una intención"
      pattern: "readStoredProgress"
---

<objective>
Poner la documentación de la fase al día con lo que el código hace de verdad — y hacerlo **después**
de que los planes 09-25 y 09-26 estén verdes, no antes.

El tercer punto de `missing:` del gap lo pide literalmente: *«la nota de cierre de hueco sobre
HIST-06 (ronda 4) da por cerrado un problema que sigue abierto en una variante distinta; debe
reflejar que la ronda 5 encontró una regresión de signo invertido, no una confirmación de cierre»*.

**Por qué este trabajo va en su propio plan y al final.** Por dos motivos independientes:

1. **Orden.** Una nota escrita antes que el código es una intención disfrazada de hecho, que es
   exactamente el defecto que esta fase persigue. La nota de la ronda 4 se escribió declarando un
   cierre y la ronda 5 demostró que ese cierre no existía; repetir la secuencia sería cómico.
2. **Contexto.** Este plan no toca ni un fichero de `app/` ni de `engine/`, así que no comparte
   superficie con el plan 09-26 — que es el que hace el trabajo peligroso, en la costura donde
   nacieron los cinco BLOCKERs. Sacar de ahí una tarea de documentación es quitarle presión de
   contexto al ejecutor que menos margen puede permitirse.

Purpose: que quien lea `REQUIREMENTS.md` sepa en qué estado real está HIST-06, y que lo que se deja
sin cerrar esté registrado con un motivo que aguante una lectura crítica.
Output: nota de HIST-06 corregida y ampliada con la ronda 5, cuatro diferidos con justificación de
riesgo, y la auditoría de afirmaciones con una pregunta más.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md
</context>

<scope_boundary>
**DENTRO:** los tres ficheros de `files_modified`. Ninguno es código.

**FUERA:**
- Cualquier fichero bajo `app/` o `engine/` → si esta tarea necesitara tocar código, es que algo
  salió mal en 09-26 y hay que decirlo, no arreglarlo aquí a escondidas.
- Las filas de trazabilidad de los demás requisitos (`SEL-*`, `HIST-01..05`, `HIST-07/08`,
  `STAT-*`) → siguen desincronizadas por el problema conocido de
  `requirements.mark-complete`, ya registrado como deuda en `deferred-items.md` desde el plan
  09-03. Corregirlas a ciegas aquí es justo lo que esa entrada desaconseja: hace falta cotejar
  cada fila contra los `requirements-completed` reales de cada SUMMARY, y eso es un barrido
  propio, no un añadido a un cierre de huecos.
- Marcar la fase como completa en `ROADMAP.md` → no le corresponde a este plan. Lo decide una
  ronda de verificación.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: `REQUIREMENTS.md` deja de afirmar un cierre que la ronda 5 desmintió</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — el bloque de checkboxes `HIST-01..HIST-09` (`:75-83`), la nota
      de cierre de hueco completa (`:85-113`, incluido el párrafo «Ronda 4 … queda cerrada por el
      lote 09-18/09-20/09-21») y la tabla de trazabilidad (`:219-232`), en especial las filas
      HIST-04, HIST-06 y HIST-09.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — COMPLETO. El tercer punto
      de `missing:` es literalmente el encargo de esta tarea; el bloque `human_verification:` fija
      qué NO se puede dar por cerrado.
    - Los SUMMARY de los planes `09-24`, `09-25` y `09-26` — qué se construyó de verdad, con qué
      nombres de fichero y función. La nota tiene que nombrar código que existe.
    - La memoria del usuario `gsd-sdk-requirements-tabla-espanol.md` — la tabla de trazabilidad se
      sincroniza a mano; ninguna herramienta lo hace sola.
  </read_first>
  <action>
    1. **Checkbox de HIST-06**: pasar de `[x]` a `[ ]`. La ronda 5 adjudicó por trazado de código
       independiente que la reserva seguía abierta; un requisito no vuelve a marcarse porque un
       plan diga haberlo cerrado, sino cuando una ronda de verificación lo confirme. No tocar los
       checkboxes de los demás requisitos.

    2. **Párrafo «Ronda 4» de la nota de cierre de hueco**: reescribir su afirmación final. Hoy
       dice que la reserva «queda cerrada por el lote 09-18/09-20/09-21»; debe decir que ese lote
       cerró **la mitad** del problema —`save()` devuelve ya el booleano real de la escritura— y
       que la ronda 5 demostró que ese booleano **no basta** para sostener la afirmación, porque
       responde a una pregunta distinta de la que la copy plantea. Conservar el resto del párrafo
       (qué hizo cada plan) sin tocarlo: es cierto y sigue siendo útil.

    3. **Añadir un párrafo «Ronda 5 (`09-VERIFICATION.md`, planes 09-24/09-25/09-26)»** con, como
       mínimo:
       - **El hallazgo**, en una frase: `save()` certifica la escritura de AHORA, y la interfaz
         usaba ese booleano para afirmar algo sobre TODO el estado del dispositivo («no hay nada
         que reintentar»), afirmación falsa en un escenario alcanzable —autoguardados previos con
         éxito y cuota agotada al final— en el que la app ofrece «Continuar» justo después.
       - **El cierre elegido** (opción A, causa raíz; decisión explícita del usuario frente a la
         mínima de retirar la frase): una autoridad de lectura única, `readStoredProgress`
         (`app/composables/useStoredProgress.ts`), que hace una lectura real de
         `tga:progress:<gameId>` y aplica la misma regla `resume()` que el montaje de la página, de
         la que beben los DOS consumidores —el aviso de fin de partida y el `ResumePrompt`—; el
         tipo `StoredProgress` de tres valores (incluido «no he podido comprobarlo»), que impide
         pasar un booleano de escritura; `planGameEnd` como única decisión de fin de partida; y un
         gate automatizado (`app/composables/__tests__/afirmacionesRespaldadas.test.ts`) que se
         pone rojo ante cualquier afirmación nueva sin respaldo.
       - **Un hallazgo colateral que merece quedar escrito**: la barrera de tipos en la que se
         apoyaba el diseño **no existía**. El proyecto no tenía `typescript` ni `vue-tsc`
         instalados, `nuxt build` transpila con esbuild sin comprobar tipos y CI no ejecutaba
         ninguna comprobación de tipos, así que «esto no compila» era una figura retórica. El plan
         09-24 instaló el comprobador, añadió `npm run typecheck` y lo colgó de CI. Es el mismo
         patrón que esta fase lleva cinco rondas cerrando, esta vez en el utillaje en vez de en el
         código.
       - **El cierre de WR-01**: guarda de reentrada en `onOutcomeRecorded`/`onOutcomeDismiss`, que
         impedía que una segunda invocación borrara el progreso preservado por la primera.
       - **Una frase final que este lote no puede omitir**: *este cierre no se da por bueno hasta
         que una ronda de verificación independiente lo confirme; cuatro notas anteriores de este
         mismo documento dieron por cerrado lo que no lo estaba.*

    4. **Tabla de trazabilidad**:
       - Fila `HIST-06` → `| HIST-06 | Fase 9 (09-13..09-27) | Reabierto en la ronda 5 — ver nota de cierre de hueco abajo |`
       - Fila `HIST-09` → actualizar el rango de planes a `(09-13..09-27)`, conservando su estado
         `Satisfecho`: el requisito literal —«Partida terminada» nunca borra el histórico— sigue
         cumpliéndose y la ronda 5 lo verificó de forma independiente.
       - Ninguna otra fila se toca (ver `<scope_boundary>`).

    Todo en español. No tocar ningún fichero de código.
  </action>
  <verify>
    <automated>grep -c "queda cerrada por el lote 09-18/09-20/09-21" .planning/REQUIREMENTS.md; grep -c "readStoredProgress" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "queda cerrada por el lote 09-18/09-20/09-21" .planning/REQUIREMENTS.md` devuelve 0 — la afirmación de cierre prematura ya no existe.
    - `grep -n "HIST-06" .planning/REQUIREMENTS.md | grep -c "\[ \]"` devuelve 1 — el checkbox de HIST-06 está sin marcar.
    - `grep "^| HIST-06 |" .planning/REQUIREMENTS.md | grep -c "Reabierto"` devuelve 1 — la fila de trazabilidad lo refleja. (Se comprueba sobre la fila de la tabla y no sobre todo el fichero, para que la misma palabra usada en la prosa de la nota no falsee el recuento.)
    - `grep "^| HIST-09 |" .planning/REQUIREMENTS.md | grep -c "09-27"` devuelve 1.
    - `grep -c "readStoredProgress" .planning/REQUIREMENTS.md` devuelve al menos 1 — la nota nombra la autoridad concreta, no una intención genérica.
    - `grep -c "no se da por bueno hasta que" .planning/REQUIREMENTS.md` devuelve al menos 1 — la nota nueva no vuelve a reclamar un cierre que no le corresponde declarar.
    - `grep -c "npm run typecheck" .planning/REQUIREMENTS.md` devuelve al menos 1 — el hallazgo colateral queda registrado, no se pierde.
    - `git diff --stat` de esta tarea toca exclusivamente `.planning/REQUIREMENTS.md`.
  </acceptance_criteria>
  <done>
    `REQUIREMENTS.md` dice la verdad sobre en qué estado está HIST-06, nombra el código concreto que
    lo aborda, y no declara un cierre que no le toca declarar.
  </done>
</task>

<task type="auto">
  <name>Task 2: registrar lo aplazado con justificación de riesgo, y añadir al método de auditoría la pregunta que faltaba</name>
  <files>.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md, .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md</files>
  <read_first>
    - `.planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` — COMPLETO. El formato de
      cada entrada (título, «Encontrado durante», «Descripción», «Por qué no se corrige aquí»,
      «Acción sugerida») y, sobre todo, el dictamen de la ronda 4 citado en varias entradas: un
      aplazamiento justificado por ALCANCE DE PLAN no es válido; la justificación tiene que ser una
      evaluación de RIESGO. Ojo: el fichero YA tiene una entrada `WR-04` de la ronda 1 (cerrada),
      así que las entradas nuevas deben llevar la ronda en el título para no confundirse con ella.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md` — WR-02 (`:171-205`), WR-03
      (`:206-232`), WR-04 (`:233-263`), WR-05 (`:264-286`) y WR-06 (`:287-312`) completos.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el bloque
      `human_verification:` y la tabla de anti-patrones, para no cerrar por escrito nada que siga
      abierto.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` — §1 «Método»
      completo (Q1-Q4, el criterio de inclusión y los tres veredictos) y la fila de
      `app/components/HistorySavedNotice.vue` de §2, marcada `RESPALDADA`, que la ronda 5 declaró
      incompleta.
    - `CLAUDE.md` — la sección de `@vite-pwa/nuxt` y `registerType: 'prompt'`, que sostiene la
      evaluación de riesgo de la entrada de las dos bandas.
    - `engine/statistics.ts` — qué agrega de verdad `aggregateStatistics` (% de victorias por héroe
      y villano), que sostiene la evaluación de riesgo de la entrada de la duración inflada.
  </read_first>
  <action>
    **A. `deferred-items.md` — cuatro entradas nuevas.** Cada título con el formato exacto
    `## WR-xx (ronda 4) — <resumen>`, para que se distingan de la entrada `WR-04` de la ronda 1 que
    ya vive en el fichero. Cada una con el bloque «Por qué no se corrige aquí» redactado como
    **evaluación de riesgo**: qué hace falta para alcanzarlo, qué pasa como mucho si ocurre, y por
    qué eso es asumible. Ninguna puede decir «fuera del perímetro/alcance de este plan»: ese motivo
    es exactamente el que la ronda 4 invalidó, y el verificador ya rechazó un aplazamiento por esa
    vía.

    1. **`## WR-04 (ronda 4) — `UpdateBanner` e `HistorySavedNotice` comparten posición y capa, y la
       segunda tapa por completo a la primera`.** Riesgo: ambas bandas son transitorias y ninguna
       afirma nada sobre los datos del grupo; la de actualización vuelve a ofrecerse en cada carga
       por diseño (`registerType: 'prompt'`, elegido en `CLAUDE.md` precisamente para no
       interrumpir una partida), así que el peor caso es retrasar una actualización hasta la
       siguiente apertura de la app — nada se pierde de forma irreversible. Acción sugerida:
       apilarlas en un contenedor común en `app/app.vue` en vez de superponerlas.
    2. **`## WR-05 (ronda 4) — `pointer-events-none` deja toques invisibles sobre la cabecera de
       `/historico``.** Riesgo: la ventana dura 20 s como máximo y los dos controles afectados
       («Volver» y «Estadísticas») son navegaciones no destructivas y reversibles con un toque; no
       hay pérdida de datos ni acción irreversible detrás. Queda además dentro del guion de la
       comprobación humana pendiente (DEV-02), que es quien mejor puede juzgar el impacto real en
       la tablet.
    3. **`## WR-06 (ronda 4) — el reintento que anuncia la variante recuperable registra una
       duración inflada`.** Riesgo: solo se alcanza tras un fallo de escritura del histórico Y un
       reintento muy posterior; D-08 define la duración como reloj de pared sin tope y se decidió
       con un hecho conocido del grupo (las partidas se terminan de una sentada), y la duración no
       se agrega en `/estadisticas` —que solo calcula % de victorias por héroe y villano—, así que
       el radio de impacto es la etiqueta de duración de UNA tarjeta del histórico. Acción
       sugerida: congelar el instante de referencia al reanudar, o acumular la duración en vez de
       derivarla de `startedAt` en el momento del registro.
    4. **`## WR-02 (ronda 4) — `GameOutcomeDialog` declara `aria-modal` sin atrapar el foco (y
       WR-03, la restauración de foco inalcanzable)`.** Riesgo: el dispositivo objetivo del
       proyecto es una tablet apoyada en la mesa y el uso real es táctil; la navegación por `Tab`
       no es un modo de uso de este grupo, y el diálogo es opaco, así que el escenario exige un
       teclado físico Y tabular a ciegas más allá del último botón. Acción sugerida: el ciclo de
       foco de `WarningDetailModal.vue`, que ya es el patrón bueno del repo.

    Añadir además, al final de la entrada de WR-05, una línea recordando que **la comprobación
    visual humana en tablet horizontal sigue ABIERTA** bajo DEV-02 desde `09-22-SUMMARY.md`: no se
    abre una entrada nueva por ella, pero no puede desaparecer del radar.

    **B. `09-AUDIT-AFIRMACIONES-UI.md` — cerrar el agujero de método que dejó pasar la ronda 5.**

    1. En §1 «Método», añadir **Q5 — *¿el dato que respalda esta frase responde a la MISMA pregunta
       que la frase plantea?*** Explicar de dónde nace: la ronda 4 introdujo Q4 («¿puede esta frase
       afirmar algo que no ha ocurrido?») y el barrido marcó `HistorySavedNotice.vue` como
       RESPALDADA por comprobar que su texto dependía de un booleano real — sin auditar si ESE
       booleano contestaba la pregunta del texto. Una escritura contesta «¿ha funcionado esto que
       acabo de hacer?»; el texto afirmaba algo sobre el estado del dispositivo. Q5 es la pregunta
       que habría cazado la ronda 5 dentro del barrido de la ronda 4, y la que convierte
       «RESPALDADA» en un veredicto que significa algo.
    2. Actualizar la fila de `app/components/HistorySavedNotice.vue` en §2: dejar constancia de que
       su veredicto `RESPALDADA` de la ronda 4 era incompleto bajo Q5, y de cuál es la situación
       tras este lote — la variante la decide `planGameEnd` a partir de un `StoredProgress`
       procedente de una lectura real (`readStoredProgress`), y existe un gate automatizado
       (`app/composables/__tests__/afirmacionesRespaldadas.test.ts`) que impide que una frase nueva
       vuelva a entrar sin respaldo.
    3. **No rehacer el barrido de los 24 `.vue`.** No ha cambiado ningún otro componente, y afirmar
       un barrido que no se ha hecho sería, literalmente, el defecto que este documento audita. Si
       hace falta, decir por escrito que las demás filas se mantienen tal cual y por qué.

    Todo en español. No tocar ningún fichero de código.
  </action>
  <verify>
    <automated>grep -cE "^## WR-[0-9]+ \(ronda 4\)" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md; grep -c "Q5" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## WR-[0-9]+ \(ronda 4\)" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve exactamente 4 — las cuatro entradas nuevas existen y llevan la ronda en el título, así que no se confunden con la entrada `WR-04` de la ronda 1.
    - `grep -c "Encontrado durante" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` aumenta en 4 respecto al valor previo (registrar ambas cifras en el SUMMARY, tomando la de partida ANTES de editar).
    - Ninguna de las cuatro entradas nuevas contiene las expresiones «fuera del perímetro de este plan», «fuera del alcance de este plan» ni «no entraba en el encargo». Comprobarlo con `grep -n "fuera del perímetro\|fuera del alcance\|no entraba en el encargo" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` y verificar a mano que las líneas que aparezcan pertenecen todas a entradas ANTIGUAS, no a las cuatro nuevas. Dejarlo dicho en el SUMMARY, citando el dictamen de la ronda 4.
    - `grep -c "DEV-02" .planning/phases/09-hist-rico-y-estad-sticas/deferred-items.md` devuelve al menos 2 — la comprobación humana en tablet sigue nombrada como ABIERTA, nunca como cerrada.
    - `grep -c "Q5" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 3 (enunciado en §1, justificación, y la fila actualizada de §2).
    - `grep -c "afirmacionesRespaldadas" .planning/phases/09-hist-rico-y-estad-sticas/09-AUDIT-AFIRMACIONES-UI.md` devuelve al menos 1 — la fila corregida apunta al gate concreto.
    - `git diff --stat` de esta tarea no toca ningún fichero bajo `app/` ni `engine/`.
  </acceptance_criteria>
  <done>
    Lo que se deja sin cerrar está escrito con un motivo que aguanta una lectura crítica, y el
    método de auditoría incorpora la pregunta que habría evitado esta ronda.
  </done>
</task>

</tasks>

<verification>
- `git diff --stat` del plan completo toca exclusivamente los tres ficheros de `.planning/`.
- `npx vitest run` y `npm run typecheck` siguen en verde — este plan no toca código, así que
  cualquier cambio de resultado sería señal de que algo se coló.
- El SUMMARY deja escrito que la comprobación visual humana en tablet horizontal (DEV-02) sigue
  pendiente y que este lote no la cierra.
</verification>

<success_criteria>
La documentación de la fase describe el estado real de HIST-06, nombra el código concreto que
aborda el hallazgo de la ronda 5 sin declarar un cierre que no le corresponde, registra con
evaluación de riesgo lo que queda fuera, y la auditoría de afirmaciones incorpora la pregunta que
faltaba.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-27-SUMMARY.md` al terminar.
</output>
