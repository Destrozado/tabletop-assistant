---
phase: 09-hist-rico-y-estad-sticas
plan: 37
type: execute
wave: 30
depends_on: ["09-36"]
gap_closure: true
files_modified:
  - app/composables/__tests__/vocabularioDeAfirmaciones.ts
  - app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts
  - app/composables/__tests__/afirmacionesRespaldadas.test.ts
autonomous: true
requirements: [HIST-04, HIST-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existe un gate que audita INVARIANTES DE CICLO DE VIDA DE ESTADO, no texto: descubre por glob toda marca de estado de módulo de `app/composables/` que afirme sobre los datos guardados del grupo, y exige de cada una que valide su referente en el momento de LEER"
    - "El gate se pone ROJO hoy, contra el árbol sin arreglar, por las dos caras confirmadas en la ronda 8 (CR-01 y WR-01), y el mensaje de fallo real queda pegado en el SUMMARY — no es una promesa escrita, es una ejecución registrada"
    - "El descubrimiento de marcas es un glob, nunca una lista tecleada: una marca DÉCIMA añadida en una ronda futura queda sometida a las mismas comprobaciones sin que nadie edite ninguna lista — esto es lo que atrapa automáticamente la próxima cara del defecto"
    - "El vocabulario de raíces léxicas tiene UNA sola implementación compartida por los dos gates: nunca dos copias que puedan divergir en silencio"
    - "El gate de clase reconoce POR ESCRITO, en su propia cabecera, que su alcance es texto-contra-respaldo y que NO cubre invariantes de estado de módulo, y nombra el fichero que sí los cubre"
    - "Cada función de decisión del gate nuevo es una función pura exportada con casos sintéticos que la ponen roja — la lección del plan 09-35: una decisión que vive dentro de un `it` no se puede demostrar que detecte nada"
  artifacts:
    - path: "app/composables/__tests__/vocabularioDeAfirmaciones.ts"
      provides: "vocabulario de raíces, normalización, región vigilada y respaldoExiste compartidos por los dos gates"
      contains: "RAICES_SOBRE_LOS_DATOS_DEL_GRUPO"
    - path: "app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts"
      provides: "el gate de invariantes de ciclo de vida de estado de módulo, con sus cuatro patas y su auto-verificación"
      contains: "marcasDeEstadoDeModuloDe"
    - path: "app/composables/__tests__/afirmacionesRespaldadas.test.ts"
      provides: "el gate de clase importando el vocabulario compartido y declarando por escrito su alcance"
      contains: "invariantesDeMarcaDeEstado.test.ts"
  key_links:
    - from: "invariantesDeMarcaDeEstado.test.ts (descubrimiento)"
      to: "app/composables/**/*.ts (glob, nunca lista)"
      via: "import.meta.glob + detección de estado de módulo a columna 0"
      pattern: "import\\.meta\\.glob\\('/app/composables"
    - from: "invariantesDeMarcaDeEstado.test.ts (pata 2)"
      to: "app/pages/[game]/index.vue (punto de lectura)"
      via: "toda llamada al lector descubierto tiene que pasar el testigo del referente"
      pattern: "lecturasSinTestigoDe\\("
    - from: "afirmacionesRespaldadas.test.ts"
      to: "vocabularioDeAfirmaciones.ts"
      via: "import del vocabulario único, sin copia local"
      pattern: "from '\\./vocabularioDeAfirmaciones'"
  prohibitions:
    - "No se toca ningún fichero de producción en este plan: el gate se escribe contra el árbol tal como está, y tiene que ponerse rojo contra él"
    - "No se debilita ninguna pata del gate nuevo para que pase en verde hoy: si una pata está roja, es porque el defecto existe, y el arreglo es el plan 09-38"
    - "No se crea una segunda copia de las raíces léxicas ni de `regionVigilada`: el gate nuevo importa del módulo compartido o no existe"
    - "No se importa `afirmacionesRespaldadas.test.ts` desde otro fichero de test: importarlo registraría sus `describe` por segunda vez en la ejecución del importador"
    - "No se escribe en ningún sitio que HIST-06 quede cerrado ni que la comprobación visual humana en tablet (DEV-02) esté hecha"
    - "No se modifica, renumera ni borra ninguno de los planes 09-01..09-36 ni sus SUMMARY"
    - "Ningún gate nuevo se apoya en una lista cerrada de subcadenas literales como único criterio: es exactamente el patrón que WR-03 encontró un nivel más arriba"
---

<objective>
Nueve rondas de verificación de esta fase han encontrado nueve caras del MISMO defecto. La ronda 8
nombra por primera vez, con precisión, por qué el gate que existe no puede atraparlas todas:

> *«estructuralmente, el gate audita texto (`NOTICE_BODY`/`NOTICE_HEADING`/literales de variante)
> contra su respaldo, no invariantes de ciclo de vida de estado — CR-01 no es una frase sin
> respaldo en el momento en que se escribe, es una frase que TENÍA respaldo y lo pierde por un
> camino de código que nunca la invalida; ningún gate de este fichero mira los puntos de
> invalidación de `useProgressMismatchMark`.»*

Este plan construye el gate que falta. No audita frases: audita **marcas de estado de módulo que
transportan una afirmación sobre los datos guardados del grupo**, y les exige la propiedad que
CR-01 viola — que la marca no pueda sobrevivir a la destrucción de su propio referente.

El plan se escribe **contra el árbol sin arreglar**, a propósito: el gate tiene que ponerse ROJO
hoy por CR-01 y por WR-01, y el mensaje de fallo real tiene que quedar registrado. El arreglo es
el plan 09-38, que lo pone verde. Es el paso RED de un RED→GREEN que cruza dos planes, igual que
el plan 09-35 hizo con sus cuatro mutaciones deliberadas — con la diferencia de que aquí la
«mutación» no hace falta inventarla: es el estado real del repositorio.

Purpose: que la DÉCIMA cara de este defecto la encuentre una ejecución de `npm test`, no otra
ronda de revisión humana.
Output: un gate de invariantes de ciclo de vida, rojo hoy, y un vocabulario compartido sin copias.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-REVIEW.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-35-SUMMARY.md
</context>

<scope_boundary>
**DENTRO:** los tres ficheros de `files_modified` y nada más.

**FUERA:**
- Cualquier fichero de producción (`app/**` que no esté bajo `__tests__/`). El gate se escribe
  contra el árbol tal como está. Si al escribirlo aparece un defecto adicional no previsto, se
  anota en el SUMMARY como hallazgo; no se arregla desde aquí.
- Las dos evasiones del propio gate de clase (WR-02 `respaldoExiste` sin relevancia de contenido,
  WR-03 detector de circularidad tras una subcadena literal) → son el plan **09-39**.
- `.planning/**` → es el plan **09-40**.
</scope_boundary>

<flagged_assumptions>
**Cobertura de bordes sin resolver (no se descarta en silencio).** La sonda determinista de
cobertura de bordes se ejecutó sobre las 14 filas de requisitos de la fase (HIST-01..HIST-09,
STAT-01..STAT-05) y devolvió las 14 como `unclassified`/`unresolved`. Ninguna se convierte en
truth de respaldo de este plan, ninguna se descarta: se registran como información ausente, igual
que hizo el plan 09-35 y que `deferred-items.md` ya documenta («La sonda de cobertura de bordes no
clasificó ninguna fila»).

**`09-UI-SPEC.md` NO contiene ninguna sección `## UI Considerations`.** Medido, no supuesto:
`grep -n "^## " .planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` devuelve
`Design System`, `Spacing Scale`, `Typography`, `Color`, `Layout`, `Component Inventory`,
`Interaction & State Coverage`, `Copywriting Contract`, `Registry Safety`, `Decisions Made Without
User Input`, `Forward-Compatibility Notes` y `Checker Sign-Off`, y ninguna más. No hay por tanto
ningún ítem de esa sección que levantar a `must_haves`, y no se inventa ninguno.

**Comprobación visual humana pendiente.** El `human_verification` de `09-VERIFICATION.md` (solape
de bandas y desbordamiento de texto en tablet horizontal real, DEV-02) sigue ABIERTO y este plan
**no** lo automatiza ni lo da por hecho en ningún sitio.

**Este plan termina con la suite ROJA, a propósito.** Ver `<verification>`.
</flagged_assumptions>

<requirements_and_decisions_scope>
**Qué IDs re-aborda este lote y cuáles NO.** Los gaps de la ronda 8 tocan exactamente dos
requisitos de la fase:

- **HIST-06** (el histórico vive en localStorage y es la fuente de verdad, y ninguna afirmación de
  la interfaz sobre esos datos puede carecer de comprobación real) — **REABIERTO**, marcado `[ ]`
  en `REQUIREMENTS.md`, y este lote NO lo cierra: lo trabaja y deja la confirmación a una ronda de
  verificación independiente.
- **HIST-04** (el registro se construye a partir de una sesión que puede estar desactualizada) — la
  señal que avisaba de ese riesgo en el reintento es justo la que podía sobrevivir falsa.

Los otros doce IDs de la fase —HIST-01, HIST-02, HIST-03, HIST-05, HIST-07, HIST-08, HIST-09,
STAT-01, STAT-02, STAT-03, STAT-04 y STAT-05— **NO se re-abordan en este lote**, y la razón es
explícita: `09-VERIFICATION.md` (ronda 8) los verifica como SATISFECHOS por los planes 09-01..09-31,
sin reserva y sin cambios desde rondas anteriores. Reabrirlos aquí sería trabajo sin hallazgo que lo
motive. Ninguno de los cuatro planes de este lote toca el código que los satisface, y
`git diff` lo demuestra en cada uno.

**Decisiones de `09-CONTEXT.md` implicadas.** Este lote no revisa ninguna decisión: cambia cuándo y
dónde se muestra un aviso, nunca qué se decidió. Las decisiones que rozan el código tocado y que
siguen vigentes sin modificación son **D-03** (el aviso de resultado guardado nunca bloquea),
**D-43** (los CTA de reanudación locutan el paso recuperado) y **D-09**/**D-51** (dispara-y-olvida
de precarga y bloqueo de pantalla en esos mismos CTA): los tres comentarios que las citan en
`app/pages/[game]/index.vue` se conservan literalmente al añadir las llamadas nuevas.
</requirements_and_decisions_scope>

<artifacts_this_phase_produces>
| Símbolo | Tipo | Dónde |
|---|---|---|
| `RAICES_SOBRE_LOS_DATOS_DEL_GRUPO` | constante exportada (movida, no copiada) | `app/composables/__tests__/vocabularioDeAfirmaciones.ts` |
| `normalizarEspacios(texto)` | función pura exportada (movida) | ídem |
| `contieneRaizSobreLosDatosDelGrupo(region, raiz)` | función pura exportada (movida) | ídem |
| `quitarComentarios(sfc)` / `regionVigilada(sfc)` | funciones puras exportadas (movidas) | ídem |
| `rutaRelativa(clave)` | función pura exportada (movida) | ídem |
| `ficherosVueDelArbol` / `ficherosTsDelArbol` / `ficherosEngineDelArbol` | mapas de `import.meta.glob` exportados (movidos) | ídem |
| `respaldoExiste(ruta)` | función pura exportada (movida) | ídem |
| `marcasDeEstadoDeModuloDe(ficheros)` | función pura exportada (descubrimiento) | `app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` |
| `contratoDeLaMarcaDe(fuente)` | función pura exportada (ponedor/retirador/lector y aridad) | ídem |
| `lecturasSinTestigoDe(fuente, nombreDelLector)` | función pura exportada (pata 2) | ídem |
| `MARCAS_CON_REFERENTE_NO_PERSISTENTE` | tabla de excepciones auditadas con motivo y respaldo | ídem |
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: vocabulario compartido, descubrimiento de marcas y contrato del lector — el gate entero de punta a punta, en rojo</name>
  <files>app/composables/__tests__/vocabularioDeAfirmaciones.ts, app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts, app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` — COMPLETO. En concreto:
      `RAICES_SOBRE_LOS_DATOS_DEL_GRUPO` (~línea 100), `normalizarEspacios` (~520),
      `contieneRaizSobreLosDatosDelGrupo` (~536), `AfirmacionAuditada`/`respaldoExiste` (~150-176),
      `quitarComentarios`/`regionVigilada` (~455-463), `rutaRelativa` (~468), y los tres
      `import.meta.glob` con `rutasConRespaldoPosible` (~483-497).
    - `app/composables/useProgressMismatchMark.ts` — COMPLETO: el `Set` de estado de módulo
      (línea 47), las tres funciones exportadas (`markProgressMismatch`, `clearProgressMismatch`,
      `readProgressMismatchWarning`) y la cabecera de comentarios.
    - `app/composables/useHistorySavedNotice.ts` — solo las líneas 125-140: es la SEGUNDA marca de
      estado de módulo que el descubrimiento va a encontrar (`activeVariant`, `timeoutId`), y esta
      tarea tiene que decidir por escrito qué hacer con ella.
    - `app/pages/[game]/index.vue` — líneas 160-210 (el `onMounted` y la única llamada a
      `readProgressMismatchWarning`) y líneas 520-545 (`onResumeContinue`).
    - `vitest.config.ts` — el proyecto `app-logic` incluye `app/**/*.test.ts`: un `.ts` sin
      `.test.` bajo `__tests__/` NO se recoge como fichero de test, que es exactamente por qué el
      vocabulario compartido puede vivir ahí.
  </read_first>
  <behavior>
    - `marcasDeEstadoDeModuloDe(ficheros)` sobre el árbol real devuelve exactamente dos rutas:
      `app/composables/useHistorySavedNotice.ts` y `app/composables/useProgressMismatchMark.ts`.
      `app/composables/usePreloadedAudio.ts` NO sale (tiene estado de módulo pero ninguna raíz).
    - `marcasDeEstadoDeModuloDe` sobre un mapa sintético con un fichero que tiene estado de módulo
      y ninguna raíz devuelve `[]`.
    - `marcasDeEstadoDeModuloDe` sobre un mapa sintético con un fichero que tiene raíces y NINGÚN
      estado de módulo a columna 0 devuelve `[]`.
    - `contratoDeLaMarcaDe(<fuente de useProgressMismatchMark.ts>)` identifica `ponedor`
      = `markProgressMismatch`, `retirador` = `clearProgressMismatch`, `lector`
      = `readProgressMismatchWarning`, y `aridadDelLector` = 1 **hoy**.
    - La pata 1 exige `aridadDelLector >= 2` y que el segundo parámetro no sea opcional: **ROJA
      hoy**.
    - `lecturasSinTestigoDe(<fuente de index.vue>, 'readProgressMismatchWarning')` devuelve un
      array NO vacío hoy (la llamada de la línea ~202 pasa un solo argumento): **ROJA hoy**.
    - `lecturasSinTestigoDe` sobre una fuente sintética con una llamada de dos argumentos devuelve
      `[]`.
    - `lecturasSinTestigoDe` no confunde una llamada con un argumento que contiene una coma dentro
      de paréntesis anidados (`lector(f(a, b))`) con una llamada de dos argumentos.
    - El gate de clase (`afirmacionesRespaldadas.test.ts`) sigue en VERDE con el MISMO número de
      tests que antes de mover el vocabulario.
  </behavior>
  <action>
    1. **Crear `app/composables/__tests__/vocabularioDeAfirmaciones.ts`** y MOVER a él —cortar, no
       copiar— desde `afirmacionesRespaldadas.test.ts`: `RAICES_SOBRE_LOS_DATOS_DEL_GRUPO`,
       `normalizarEspacios`, `contieneRaizSobreLosDatosDelGrupo`, `quitarComentarios`,
       `regionVigilada`, `rutaRelativa`, los tres `import.meta.glob` (renombrados a
       `ficherosVueDelArbol`, `ficherosTsDelArbol`, `ficherosEngineDelArbol`),
       `rutasConRespaldoPosible` y `respaldoExiste`. Todos exportados. Los comentarios que
       acompañan a cada uno viajan con él, literalmente: explican por qué el criterio es por raíces
       y no por subcadenas, y esa explicación es lo que impide que alguien lo revierta sin querer.
       Cabecera del fichero nuevo: por qué existe (un vocabulario, dos gates; dos copias que
       divergen en silencio serían una cara nueva del mismo defecto) y por qué vive bajo
       `__tests__/` sin ser un `.test.ts` (el proyecto `app-logic` de `vitest.config.ts` solo
       recoge `app/**/*.test.ts`, y Gate A excluye por diseño las rutas que contienen
       `/__tests__/`, así que este fichero no se audita a sí mismo ni se ejecuta como suite).

    2. **`afirmacionesRespaldadas.test.ts` importa de ese módulo** (`from './vocabularioDeAfirmaciones'`)
       y conserva LOCALES solo las cosas que son suyas: `AfirmacionAuditada`,
       `AFIRMACIONES_AUDITADAS`, `frasesSinAuditarDe`, `variantesSinRespaldoDe`,
       `VOCABULARIO_CERRADO_HASTA_LA_RONDA_7`, `FRASE_NO_DETECTADA_EN_LA_RONDA_7`,
       `LITERALES_VARIANTE`, `VARIANTES_RESPALDADAS_POR_LA_AUTORIDAD`, `copyDeLaAppGateA` y los
       cuatro `describe` de Gate A/B/C/S. `copyDeLaAppGateA` se sigue construyendo aquí, ahora a
       partir de `ficherosVueDelArbol`/`ficherosTsDelArbol` importados. Ni una comprobación
       cambia de sentido: es un movimiento mecánico y el recuento de tests tiene que ser idéntico.

    3. **Crear `app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts`** con cabecera que
       diga, literalmente, qué clase de defecto audita y por qué no cabe en el gate de clase: el
       gate de clase compara TEXTO contra RESPALDO en el momento en que el texto se escribe; este
       audita el CICLO DE VIDA de un estado de módulo que transporta una afirmación sobre datos
       guardados — una frase que tenía respaldo y lo pierde porque nadie la invalida cuando su
       referente deja de existir. Citar `09-VERIFICATION.md` ronda 8 y CR-01/WR-01 de
       `09-REVIEW.md` con nombre.

    4. **Descubrimiento — `export function marcasDeEstadoDeModuloDe(ficheros: Record<string, string>): string[]`.**
       Para cada entrada: es marca si (a) su contenido tiene al menos una línea a COLUMNA 0 que
       case con `/^(const [A-Za-z_$][\w$]* = (new (Set|Map)\b|ref[<(])|let [A-Za-z_$])/m`
       —estado de módulo mutable, la forma exacta que `useProgressMismatchMark.ts` y
       `useHistorySavedNotice.ts` tienen hoy— **y** (b) `regionVigilada(contenido)` contiene alguna
       raíz de `RAICES_SOBRE_LOS_DATOS_DEL_GRUPO` (importada, nunca recopiada). Devuelve rutas
       relativas ordenadas. Esta función es el corazón anti-recurrencia del plan: el criterio es un
       glob más dos predicados, así que una marca DÉCIMA en una ronda futura entra sola.
       Comentario obligatorio diciendo exactamente eso.

    5. **Aplicarlo al árbol real**: un `it` que afirma que el descubrimiento sobre
       `ficherosTsDelArbol` filtrado a `app/composables/` y sin `/__tests__/` devuelve
       exactamente las dos rutas medidas (`useHistorySavedNotice.ts`, `useProgressMismatchMark.ts`)
       y otro que afirma que NO contiene `usePreloadedAudio.ts`. Si la medición real no coincidiera
       con estas dos rutas, el arreglo va en el gate y la discrepancia se anota en el SUMMARY —
       nunca al revés.

    6. **`MARCAS_CON_REFERENTE_NO_PERSISTENTE`**: tabla `Record<string, { motivo: string; respaldo: string }>`
       con la MISMA forma y el mismo rigor que `AFIRMACIONES_AUDITADAS`. Una sola entrada hoy:
       `app/composables/useHistorySavedNotice.ts`, cuyo motivo tiene que decir el hecho comprobable
       —su estado de módulo es un aviso transitorio en pantalla, decidido por
       `planGameEnd(historyRecorded, stored)` a partir de una lectura fresca de la autoridad en el
       mismo instante en que se pinta, y nunca transportado entre montajes— y cuyo `respaldo` es
       `app/composables/__tests__/avisoTrasRegistroFallido.test.ts`. Un `it` exige que toda entrada
       de esta tabla tenga motivo no vacío y `respaldo` que pase `respaldoExiste`.

    7. **Pata 1 — `export function contratoDeLaMarcaDe(fuente: string)`** devuelve
       `{ ponedor, retirador, lector, aridadDelLector, segundoParametroOpcional }`. Extrae cada
       `export function <nombre>(<params>)<:tipo>? {<cuerpo>}` y los clasifica POR LO QUE HACEN, no
       por cómo se llaman: cuerpo con `.add(`/`.set(` → ponedor; cuerpo con `.delete(` →
       retirador; tipo de retorno distinto de `void` y cuerpo que consulta el estado (`.has(`,
       `.get(`) → lector. Comentario: clasificar por nombre sería un vocabulario cerrado, que es
       justo lo que WR-03 encontró un nivel más arriba. `aridadDelLector` cuenta los parámetros
       declarados del lector; `segundoParametroOpcional` es cierto si ese segundo parámetro lleva
       `?:`. El `it` de la pata 1 exige, para toda marca descubierta y no auditada,
       `aridadDelLector >= 2` y `segundoParametroOpcional === false`, con mensaje de fallo que
       nombre la ruta y el lector. **Hoy esto se pone rojo.**

    8. **Pata 2 — `export function lecturasSinTestigoDe(fuente: string, nombreDelLector: string): string[]`.**
       Busca en `regionVigilada(fuente)` cada llamada `<nombreDelLector>(` y cuenta los argumentos
       de nivel superior recorriendo caracteres y llevando la profundidad de paréntesis/corchetes/
       llaves —nunca un `split(',')`, que rompería con `lector(f(a, b))`—. Devuelve el texto de las
       llamadas con menos de dos argumentos. El `it` de la pata 2 recorre todos los ficheros de
       `app/**` (`.vue` y `.ts`, excluyendo `/__tests__/`) y exige `[]` para cada marca descubierta
       y no auditada. **Hoy esto se pone rojo por `app/pages/[game]/index.vue`.**

    9. **Auto-verificación de las dos patas** (lección del plan 09-35): casos sintéticos, cadenas
       construidas en el propio test y nunca leídas del árbol, que demuestren que
       `marcasDeEstadoDeModuloDe`, `contratoDeLaMarcaDe` y `lecturasSinTestigoDe` devuelven
       resultado NO vacío / el valor esperado cuando deben, incluido el caso de la coma anidada.

    Todo el código y todos los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts</automated>
    <fails_when>salida distinta de 0, o cualquier línea «FAIL», o un recuento de tests distinto del que tenía antes de mover el vocabulario</fails_when>
    <automated>npx vitest run app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts</automated>
    <fails_when>salida 0 —el gate pasaría entero contra un árbol donde CR-01 y WR-01 están confirmados, y eso significa que el gate es vacuo—, o una salida cuyos mensajes de fallo no nombren `readProgressMismatchWarning`</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>salida distinta de 0, o cualquier línea que contenga «error TS»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "RAICES_SOBRE_LOS_DATOS_DEL_GRUPO" app/composables/__tests__/vocabularioDeAfirmaciones.ts` devuelve al menos 1, y el mismo `grep` sobre `afirmacionesRespaldadas.test.ts` NO encuentra ninguna declaración propia: `grep -c "^const RAICES_SOBRE_LOS_DATOS_DEL_GRUPO" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 0.
    - `grep -c "from './vocabularioDeAfirmaciones'" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve 1 y el mismo `grep` sobre `invariantesDeMarcaDeEstado.test.ts` devuelve 1.
    - `grep -c "afirmacionesRespaldadas" app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` cuenta solo menciones en comentarios: `grep -v '^\s*//' app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts | grep -c "from '.*afirmacionesRespaldadas'"` devuelve 0 (no se importa un fichero de test desde otro).
    - `grep -cE "^export function (marcasDeEstadoDeModuloDe|contratoDeLaMarcaDe|lecturasSinTestigoDe)" app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` devuelve 3.
    - La ejecución del gate nuevo sale distinta de 0 y su salida nombra `readProgressMismatchWarning`; **pegar en el SUMMARY el mensaje de fallo literal de cada pata roja**, sin resumirlo.
    - El recuento de tests de `afirmacionesRespaldadas.test.ts` antes y después del movimiento queda registrado en el SUMMARY y es el mismo número.
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>
  <done>
    El gate de invariantes existe, descubre las marcas por glob, y está ROJO contra el árbol de hoy
    por la llamada de lectura sin testigo — con el mensaje real registrado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: la prueba de ciclo de vida obligatoria y la rama de montaje que lee sin pintar</name>
  <files>app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts</files>
  <read_first>
    - `app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` — tal como lo deja la Task 1.
    - `app/composables/__tests__/useProgressMismatchMark.test.ts` — COMPLETO: los quince `it`
      actuales y el `afterEach` de limpieza. Es el fichero sobre el que la pata 3 mide.
    - `app/pages/[game]/index.vue` — líneas 195-210 (las dos asignaciones desde comparación y la
      lectura de la marca) y líneas 818-850 (la cadena `v-if`/`v-else-if` de la plantilla y el
      `:mismatch-warning` de `ResumePrompt`).
    - `app/components/ContentChangedNotice.vue` — COMPLETO: su `defineProps` de dos claves, sin
      ninguna prop de aviso.
  </read_first>
  <behavior>
    - `faltaPruebaDeCicloDeVidaEn(<fuente de useProgressMismatchMark.test.ts>, 'markProgressMismatch', 'readProgressMismatchWarning')`
      devuelve `true` **hoy**: ningún `it` del fichero pone la marca y luego la lee con un testigo
      distinto esperando `null`.
    - Sobre una fuente sintética que sí contiene ese `it`, devuelve `false`.
    - Sobre una fuente sintética donde el `it` lee con el MISMO segundo argumento con el que puso,
      devuelve `true` (poner y leer con el mismo testigo no demuestra nada sobre la sustitución del
      referente).
    - `ramasQueLeenSinPintarDe(<fuente de index.vue>, 'readProgressMismatchWarning')` devuelve un
      array que contiene `awaitingContentChangedAck` **hoy**, y no contiene `awaitingResumeChoice`.
    - Sobre una fuente sintética donde las dos ramas llevan el binding del aviso, devuelve `[]`.
    - El descubrimiento de ramas no incluye ningún ref asignado desde un literal
      (`resumeResolved.value = true` no es una rama de decisión de montaje).
  </behavior>
  <action>
    1. **Pata 3 — `export function faltaPruebaDeCicloDeVidaEn(fuenteDelTest: string, ponedor: string, lector: string): boolean`.**
       Trocea la fuente en bloques por cada aparición de `it(` (desde una hasta la siguiente o el
       final). Un bloque demuestra el ciclo de vida si: contiene una llamada al `ponedor`, contiene
       DESPUÉS (índice mayor) una llamada al `lector`, el segundo argumento textual de las dos
       llamadas es DISTINTO, y el bloque contiene `toBe(null)` después de la llamada al lector.
       Devuelve `true` (falta la prueba) si ningún bloque lo cumple. El `it` de la pata 3 exige,
       para toda marca descubierta y no auditada, que exista su fichero hermano de test
       (`app/composables/__tests__/<nombreDelMódulo>.test.ts`, resuelto sobre `ficherosTsDelArbol`)
       y que `faltaPruebaDeCicloDeVidaEn` devuelva `false`. Comentario: esta pata es la forma
       ejecutable del cuarto `missing:` del gap de SC3 — *«un test de extremo a extremo sobre
       useProgressMismatchMark que reproduzca el camino completo … para que la novena cara no
       dependa de que otra ronda de verificación la vuelva a encontrar a mano»*. **Hoy se pone
       roja.**

    2. **Pata 4 — `export function ramasQueLeenSinPintarDe(fuenteDelConsumidor: string, lector: string): string[]`.**
       Mecánica, medida sobre el árbol real antes de escribirla (la medición ya está hecha: en todo
       `app/` hay exactamente dos asignaciones de la forma `<ref>.value = <expresión con ===>`, las
       dos en `app/pages/[game]/index.vue`):
       a. Identificar el ref del aviso: la asignación `<X>.value = <lector>(`.
       b. Identificar las ramas de decisión de montaje: todo `<R>.value = <expresión que contiene
          `===`>`. Un ref asignado desde un literal NO es una rama (por eso `resumeResolved` queda
          fuera, y el comentario tiene que decirlo).
       c. Para cada `R`, localizar en la plantilla la aparición de `v-if="R"` o `v-else-if="R"` y
          tomar el segmento que va desde ahí hasta la siguiente aparición de `v-if=`, `v-else-if=`,
          `v-else` o el final de la plantilla.
       d. `R` está en falta si ese segmento no contiene `="X"` (el ref del aviso, enlazado como
          prop). Devolver las `R` en falta.
       El `it` de la pata 4 exige `[]` sobre el consumidor real. **Hoy devuelve
       `awaitingContentChangedAck` y se pone roja** — es exactamente WR-01: la rama calcula el
       aviso y no tiene destino de render.

    3. **Auto-verificación de las dos patas** con fuentes sintéticas construidas en el propio test,
       una por viñeta de `<behavior>`, cada una con un comentario de una línea diciendo qué
       propiedad demuestra. Incluir el caso negativo de la pata 3 (leer con el mismo testigo con
       el que se puso NO cuenta): sin él, la pata sería satisfacible con un test que no prueba
       nada.

    Todo el código y todos los comentarios en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts</automated>
    <fails_when>salida 0 —cuatro patas verdes contra un árbol con CR-01 y WR-01 confirmados significa que el gate no mide nada—, o unos mensajes de fallo que no nombren a la vez `readProgressMismatchWarning` y `awaitingContentChangedAck`</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>salida distinta de 0, o cualquier línea que contenga «error TS»</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^export function (faltaPruebaDeCicloDeVidaEn|ramasQueLeenSinPintarDe)" app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` devuelve 2.
    - La salida del gate contiene un mensaje de fallo que nombra `awaitingContentChangedAck` y otro que nombra `useProgressMismatchMark.test.ts`; **pegar los dos literales en el SUMMARY**.
    - Los casos sintéticos de auto-verificación de las dos patas están todos en VERDE: `npx vitest run app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts -t "sintétic"` sale 0.
    - `grep -c "resumeResolved" app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` devuelve al menos 1 (el comentario que explica por qué un ref asignado desde un literal no es una rama).
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>
  <done>
    Las cuatro patas del gate están escritas, las cuatro tienen auto-verificación sintética en
    verde, y las cuatro comprobaciones sobre el árbol real están rojas por CR-01 y WR-01, con los
    mensajes literales registrados.
  </done>
</task>

<task type="auto">
  <name>Task 3: cobertura del descubrimiento y declaración escrita del alcance de cada gate</name>
  <files>app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts, app/composables/__tests__/afirmacionesRespaldadas.test.ts</files>
  <read_first>
    - `app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` — tal como lo dejan las
      Tasks 1 y 2.
    - `app/composables/__tests__/afirmacionesRespaldadas.test.ts` — la cabecera completa (líneas
      1-60), que es donde el fichero declara hoy sus cuatro gates y su premisa.
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-VERIFICATION.md` — el tercer `missing:` del
      segundo gap, literal: *«Reconocer por escrito, en el propio fichero del gate, que su alcance
      es texto-contra-respaldo y que NO cubre invariantes de estado de módulo…»*.
  </read_first>
  <action>
    1. **Pata 5 — cobertura del descubrimiento.** Un `it` que exige
       `marcasDeEstadoDeModuloDe(...)` NO vacío sobre el árbol real (un gate que no descubre nada
       está verde por accidente, no por ausencia de defecto), y otro que exige que TODA marca
       descubierta esté o bien sometida a las cuatro patas o bien listada en
       `MARCAS_CON_REFERENTE_NO_PERSISTENTE` — nunca ignorada en silencio. El mensaje de fallo tiene
       que decir la ruta de la marca huérfana y qué se espera de ella.

    2. **Comentario de anti-recurrencia, obligatorio y nominal**, junto a la pata 5: qué tendría que
       pasar para que la DÉCIMA cara de este defecto la encuentre esta suite y no otra ronda de
       revisión — a saber, que cualquier composable nuevo de `app/composables/` con estado de módulo
       mutable y una raíz vigilada entra solo en el descubrimiento (es un glob, no una lista), y a
       partir de ahí tiene que probar por construcción que su lector valida el referente, que su
       test hermano recorre el ciclo completo y que todas las ramas que lo leen lo pintan. Sin
       editar ninguna lista.

    3. **Declaración de alcance en `afirmacionesRespaldadas.test.ts`**, en su cabecera: que el
       alcance de esos cuatro gates es texto-contra-respaldo EN EL MOMENTO EN QUE EL TEXTO SE
       ESCRIBE; que NO cubre invariantes de ciclo de vida de estado de módulo; que la novena cara
       (una frase que tenía respaldo y lo pierde por un camino que nunca la invalida) es
       estructuralmente invisible aquí; y que quien la cubre es
       `app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts`, citado por ruta. Un `it`
       nuevo en Gate S comprueba por `grep` interno (sobre el propio contenido del fichero, leído
       del glob) que esa ruta aparece citada — para que borrar la declaración rompa el test en vez
       de pasar desapercibido.

    4. **Declaración simétrica** en la cabecera de `invariantesDeMarcaDeEstado.test.ts`: qué NO
       cubre este gate (no mira el contenido de ninguna frase; una copy sin respaldo sigue siendo
       trabajo de Gate A/B/C) y cita a `afirmacionesRespaldadas.test.ts` por ruta.

    Todo en español.
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts</automated>
    <fails_when>salida distinta de 0, o cualquier línea «FAIL»</fails_when>
    <automated>npx vitest run app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts</automated>
    <fails_when>salida 0, o una salida cuyo recuento de tests fallidos sea distinto del enumerado en el SUMMARY de la Task 2</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "invariantesDeMarcaDeEstado.test.ts" app/composables/__tests__/afirmacionesRespaldadas.test.ts` devuelve al menos 2 (la declaración de alcance y el `it` que la vigila).
    - `grep -c "afirmacionesRespaldadas.test.ts" app/composables/__tests__/invariantesDeMarcaDeEstado.test.ts` devuelve al menos 1.
    - `npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts` sale 0 y su recuento de tests es el de la Task 1 más los tests nuevos de esta tarea; registrar los dos números en el SUMMARY.
    - El SUMMARY enumera, uno a uno y con su nombre exacto, los tests del gate nuevo que quedan ROJOS al cerrar este plan, y afirma que ningún otro fichero de la suite está rojo: pegar la salida de `npm test 2>&1 | tail -40`.
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>
  <done>
    Cada gate declara por escrito su alcance y cita al otro, y el descubrimiento no puede quedarse
    vacío ni dejar una marca huérfana sin que la suite lo diga.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grupo (tablet compartida) → `localStorage` del navegador | Único almacén de la app: no hay backend, ni autenticación, ni red. Cualquiera con la tablet en la mano lee y escribe el histórico y el progreso |
| Datos guardados → afirmaciones de la interfaz | La frontera que esta fase lleva nueve rondas defendiendo: lo que la pantalla dice sobre esos datos tiene que estar respaldado por una lectura real |
| Estado de módulo (memoria) → montaje posterior de la misma página | Un hecho comprobado en un instante se transporta a otro instante; entre los dos, su referente puede haber dejado de existir |
| Gate automatizado → rondas de verificación futuras | Un gate en verde se lee como «esta clase de defecto está cubierta»; un gate vacuo es peor que no tener gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-37-01 | Repudiation | Un gate que pasa en verde sin medir nada (descubrimiento vacío, patas satisfacibles trivialmente) | high | mitigate | Pata 5 exige descubrimiento no vacío; auto-verificación sintética por pata; el plan exige observar el gate ROJO contra el árbol real y pegar el mensaje literal |
| T-09-37-02 | Tampering | Dos copias divergentes del vocabulario de raíces (una por gate) | high | mitigate | El vocabulario se MUEVE a un módulo compartido; criterio de aceptación por `grep` que prohíbe una declaración local en el gate de clase |
| T-09-37-03 | Information disclosure | El gate lee ficheros del repo vía `import.meta.glob` | low | accept | Solo se ejecuta en Vitest sobre el árbol local; nada de esto entra en el bundle del cliente (el proyecto `app-logic` de `vitest.config.ts` no construye nada) |
| T-09-37-04 | Repudiation | Aflojar una pata para que la suite quede verde hoy, dando el hueco por cerrado | high | mitigate | Prohibición explícita en `must_haves.prohibitions`; el `fails_when` de cada verify declara que una salida 0 ES el fallo de este plan |
| T-09-37-SC | Tampering | Instalaciones de paquetes (npm) | low | accept | Este plan no añade ninguna dependencia: `git diff --stat -- package.json package-lock.json` tiene que quedar vacío |
</threat_model>

<verification>
- `git diff --name-only` lista exactamente los tres ficheros de `files_modified`.
- `git diff --stat -- package.json package-lock.json` vacío.
- `git diff --stat -- app/components/ app/pages/ app/composables/*.ts engine/` **vacío**: este plan
  no toca producción. (`app/composables/__tests__/` sí cambia, y es lo único de `app/composables/`
  que puede cambiar.)
- `npx tsc --noEmit` sale 0.
- `npx vitest run app/composables/__tests__/afirmacionesRespaldadas.test.ts` sale 0.
- **`npm test` sale DISTINTO DE 0 al cerrar este plan, y eso es el resultado esperado.** Los únicos
  tests rojos permitidos son los del gate nuevo que miden el árbol real (patas 1-4). El SUMMARY
  tiene que enumerarlos por nombre y demostrar con la salida real que ningún otro fichero está
  rojo. El plan 09-38 los pone verdes. Si al cerrar este plan hubiera un test rojo FUERA del gate
  nuevo, es una regresión y hay que arreglarla antes de cerrar.
- Commit del paso RED con mensaje `test(09-37): …`, nunca `fix(...)`.
</verification>

<success_criteria>
- Existe un gate que audita invariantes de ciclo de vida de estado de módulo, con cinco patas y
  auto-verificación sintética por pata.
- El descubrimiento de marcas es un glob más dos predicados; no hay ninguna lista de módulos
  tecleada a mano.
- El gate está rojo por CR-01 (lector sin testigo) y por WR-01 (rama que lee y no pinta), con los
  mensajes de fallo literales registrados en el SUMMARY.
- El vocabulario de raíces tiene una sola implementación, compartida por los dos gates, y el gate
  de clase sigue verde con el mismo recuento de tests.
- Los dos gates declaran por escrito su alcance y se citan mutuamente por ruta, con un test que
  vigila esa cita.
</success_criteria>

<output>
Create `.planning/phases/09-hist-rico-y-estad-sticas/09-37-SUMMARY.md` when done
</output>
