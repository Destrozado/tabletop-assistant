---
phase: 09-hist-rico-y-estad-sticas
plan: 07
type: execute
wave: 4
depends_on: ["09-02", "09-05"]
files_modified:
  - app/composables/useGameSession.ts
  - app/composables/__tests__/useGameSession.test.ts
  - app/pages/[game]/index.vue
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03, HIST-05, HIST-09]

must_haves:
  truths:
    - "D-07/HIST-05: startedAt se fija UNA sola vez en start(); la ruta de reanudación nunca lo reescribe y engine/persistence.ts no se toca"
    - "HIST-01: pulsar «Partida terminada» abre el diálogo de resultado en vez del ConfirmDialog anterior"
    - "D-U4/Pitfall 1: el registro ocurre entre silence() y session.value = null; los pasos session=null → clear(gameId) → navigateTo('/') no cambian ni una línea"
    - "HIST-02: «Salir sin registrar» cierra la partida igual que las otras tres opciones, pero sin escribir en el histórico"
    - "D-04: el mismo diálogo aparece siempre, también a medio montar la mesa; cero ramas condicionales"
    - "D-05: onDiscardConfirm («Empezar partida nueva») no registra nada y no se toca"
    - "D-02: el aviso de que se borrará el progreso guardado (endGameBody) sigue mostrándose, ahora dentro del diálogo nuevo"
    - "D-03: el resultado de la escritura decide la variante del aviso; nunca se finge un éxito"
    - "D-12 (Fase 6)/D-17 (Fase 7): los atajos de teclado siguen suprimidos con el diálogo abierto, por la misma bandera awaitingEndConfirm de siempre"
  artifacts:
    - path: "app/composables/useGameSession.ts"
      provides: "withStartedAt + start() que sella el instante de inicio"
      contains: "startedAt"
    - path: "app/pages/[game]/index.vue"
      provides: "GameOutcomeDialog cableado con onOutcomeRecorded/onOutcomeDismiss"
      contains: "GameOutcomeDialog"
  key_links:
    - from: "app/pages/[game]/index.vue"
      to: "app/composables/useGameHistory.ts"
      via: "record(session.value, outcome)"
      pattern: "record\\("
    - from: "app/pages/[game]/index.vue"
      to: "app/composables/useHistorySavedNotice.ts"
      via: "notifyHistorySaved(saved)"
      pattern: "notifyHistorySaved"
---

<objective>
El punto de enganche de toda la fase: sellar el instante de inicio de la partida y sustituir
el `ConfirmDialog` de «Partida terminada» por el diálogo de resultado, registrando la entrada
justo antes de que la sesión se destruya.

Propósito: `onEndGameConfirm` en `app/pages/[game]/index.vue` tiene un orden marcado como
«EXACTO, no cosmético» (D-U4) y sus pasos 4-6 destruyen precisamente los datos que la entrada
necesita. Este es el único sitio de la fase donde equivocarse de línea produce un histórico
vacío en silencio.

Salida: `useGameSession.ts` con `startedAt`, su test ampliado, y `index.vue` cableado al
diálogo nuevo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-RESEARCH.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-PATTERNS.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-02-SUMMARY.md
@.planning/phases/09-hist-rico-y-estad-sticas/09-05-SUMMARY.md

<interfaces>
De los planes 09-02 y 09-05:
- `app/components/GameOutcomeDialog.vue` — props `contextLine: string`, `warningBody: string`;
  emits `record: ['won' | 'mainSchemeCompleted' | 'heroesEliminated']` y `dismiss: []`.
- `app/composables/useHistorySavedNotice.ts` — `notifyHistorySaved(saved: boolean): void`.
- `app/composables/useGameHistory.ts` — `useGameHistory()` devuelve entre otras cosas
  `record(session: EngineSession, outcome: GameOutcome): boolean`.

Del repo (`app/pages/[game]/index.vue`, leído):
- `awaitingEndConfirm` (ref), `onEndGameRequest()`, `onEndGameCancel()`, `onEndGameConfirm()`
  con el orden D-U4: `awaitingEndConfirm=false` → `isIndexOpen=false` → `silence()` →
  `session.value = null` → `clear(gameId)` → `navigateTo('/')`.
- `sessionContextLabel` (computed de `useGameSession`) produce `3 jug · Normal`.
- `endGameBody` (computed) produce `Se borrará el progreso guardado ({savedSummary}) y
  volveréis a la pantalla de inicio. Esta acción no se puede deshacer.`
- `selectedVillainId` (computed), `villainOptions` (constante de página) y
  `findVillainOption(options, villainId): VillainOption | null` ya importado en el fichero.
- `shortcutsEnabled({... awaitingEndConfirm ...})` ya suprime los atajos con esa bandera
  activa: no hay que añadir ninguna rama nueva, solo comprobarlo.
- `app/composables/useGameSession.ts` — `start(gameId, context)` hace
  `session.value = game ? expand(game, context) : null`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: startedAt se sella en start() (D-07/HIST-05)</name>
  <files>app/composables/useGameSession.ts, app/composables/__tests__/useGameSession.test.ts</files>
  <read_first>
    - `app/composables/useGameSession.ts` (fichero a modificar; ver `start()` y el estilo de
      las funciones puras exportadas a nivel de módulo `buildCounterCells`,
      `buildStepValueSuffix`, `buildStepValueCells`)
    - `app/composables/__tests__/useGameSession.test.ts` (fichero a modificar; su cabecera
      declara que solo se fijan las funciones puras exportadas, nunca el cableado reactivo —
      hay que respetarlo)
    - `engine/persistence.ts` (para confirmar que NO hace falta tocarlo: `toPersistedPosition`
      ya persiste `context` entero y `resume()` ya lo restaura entero)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md` D-07, D-08, D-10
  </read_first>
  <action>
    En `app/composables/useGameSession.ts`:

    1. Añadir la función pura exportada a nivel de módulo
       `export function withStartedAt(context: SessionContext, now: number): SessionContext`,
       que devuelve un objeto NUEVO `{ ...context, startedAt: now }` sin mutar su argumento.
       Se exporta precisamente para poder fijarla con un test sin montar el composable, igual
       que `buildCounterCells` y sus hermanas.
    2. `start(gameId, context)` pasa `withStartedAt(context, Date.now())` a `expand(...)` en
       vez de `context` a secas.

    Comentario obligatorio junto a la función, citando las decisiones: D-07 —`startedAt` es
    campo ADITIVO de `SessionContext`, se fija UNA sola vez aquí y la ruta de reanudación
    (`resume()` en `onMounted` de `app/pages/[game]/index.vue`) NUNCA lo reescribe, porque
    `resume()` restaura el `context` persistido entero y no pasa por `start()`. Por eso
    `engine/persistence.ts` no se toca y no se bumpea `formatVersion` ni `contentVersion`.
    D-08 — la duración es reloj de pared sin tope (`Date.now() - startedAt`), sin
    temporizadores ni pausas: este grupo termina de una sentada lo que empieza. D-10 — una
    partida guardada por la versión desplegada de v1.7 se reanuda SIN `startedAt` y eso es un
    estado válido: se guardará `durationMs: null` y el histórico pintará «—», nunca un cero ni
    una cifra inventada rellenando el reloj al reanudar.

    En el test, añadir un `describe('withStartedAt (D-07)')` con: sella `startedAt` con el
    valor recibido; devuelve un objeto nuevo (`not.toBe`) y no muta el original (el `context`
    de entrada sigue sin la clave); conserva `playerCount`, `difficulty` y cualquier campo
    aditivo previo (`selection`, `counters`).
  </action>
  <verify>
    <automated>npx vitest run app/composables/__tests__/useGameSession.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function withStartedAt" app/composables/useGameSession.ts` devuelve 1
    - `grep -c "withStartedAt(context, Date.now())" app/composables/useGameSession.ts` devuelve 1
    - `grep -rln "startedAt" app/ --include=*.ts --include=*.vue | grep -v __tests__` devuelve únicamente `app/composables/useGameSession.ts` (ningún otro fichero de `app/` escribe ni reescribe el campo)
    - `git diff --stat engine/persistence.ts` está vacío
    - `npx vitest run app/composables/__tests__/useGameSession.test.ts` termina con código 0
  </acceptance_criteria>
  <done>Toda partida nueva nace con su instante de inicio; reanudar una partida vieja no lo inventa ni lo reescribe.</done>
</task>

<task type="auto">
  <name>Task 2: manejadores de fin de partida en index.vue</name>
  <files>app/pages/[game]/index.vue</files>
  <read_first>
    - `app/pages/[game]/index.vue` (fichero a modificar; leer entero el bloque de
      `savedSummary`/`discardBody`/`endGameBody`, `onDiscardConfirm`, `onEndGameRequest`,
      `onEndGameCancel`, `onEndGameConfirm` con su comentario D-U4, y el bloque de
      `atajosActivos`/`shortcutsEnabled`)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-RESEARCH.md` §Pitfall 1 (el fallo que
      esta tarea existe para evitar)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-UI-SPEC.md` §Layout 2 y
      §Copywriting Contract (formato exacto de la línea de contexto)
    - `.planning/phases/09-hist-rico-y-estad-sticas/09-CONTEXT.md` D-02, D-03, D-04, D-05
  </read_first>
  <action>
    En el `<script setup>` de `app/pages/[game]/index.vue`:

    1. Importar `useGameHistory` de `~/composables/useGameHistory` y `notifyHistorySaved` de
       `~/composables/useHistorySavedNotice`, y obtener `const { record } = useGameHistory()`
       junto al resto de composables de la parte alta del fichero.

    2. Añadir la computed `outcomeContextLine`, con el formato exacto
       `{villano} · {n} jug · {dificultad} · ronda {N}`: componer un array con el nombre del
       villano —`findVillainOption(villainOptions, selectedVillainId.value)?.name`, que ya
       está importado y en uso en este fichero— solo si existe, después
       `sessionContextLabel.value` (que ya produce `3 jug · Normal`) y por último
       `ronda ${session.value?.round}` si hay sesión; unir con ` · `. Cuando no hay villano
       elegido, el segmento se OMITE por completo, no se sustituye por un marcador (SEL-09/
       D-12: elegir es opcional de principio a fin).

    3. Extraer la cola de limpieza a `function finishGame(): void` con EXACTAMENTE las tres
       sentencias finales del `onEndGameConfirm` actual, en el mismo orden y con sus
       comentarios intactos: `session.value = null`, `clear(gameId)`, `navigateTo('/')`.
       Conservar también el comentario que explica por qué NO se llama a `releaseWakeLock()`
       aquí.

    4. Sustituir `onEndGameConfirm` y `onEndGameCancel` por estos dos manejadores, ambos con
       el orden de D-U4 y el registro insertado ENTRE `silence()` y `finishGame()`
       (Pitfall 1: después de `session.value = null` los datos ya no existen):
       - `function onOutcomeRecorded(outcome: GameOutcome): void` —
         `awaitingEndConfirm.value = false`; `isIndexOpen.value = false`; `silence()`;
         después `const guardado = session.value ? record(session.value, outcome) : false`;
         después `notifyHistorySaved(guardado)`; y por último `finishGame()`.
       - `function onOutcomeDismiss(): void` — mismos tres primeros pasos y `finishGame()`,
         SIN llamar a `record` y SIN avisar de nada.
       Nada de `async`/`await` en este camino: la escritura de `localStorage` es síncrona y en
       esta fase no existe ninguna operación de red.

    5. `onEndGameRequest()` no cambia (sigue poniendo `awaitingEndConfirm` a `true` sin cerrar
       el índice, D-U3). `onDiscardConfirm()` no se toca NI UNA LÍNEA (D-05: descartar una
       partida para empezar otra es abandonarla, no terminarla — no hay resultado que
       registrar).

    NOTA DE RECONCILIACIÓN obligatoria como comentario junto a `onOutcomeDismiss`: «Salir sin
    registrar» TERMINA la partida sin escribir en el histórico; no es «cancelar y seguir
    jugando». Lo exigen HIST-02 y el criterio de éxito nº 1 del ROADMAP («o cerrar la partida
    sin registrar nada»), y D-01 fija exactamente cuatro opciones, así que con este diálogo
    desaparece la opción «Cancelar» del `ConfirmDialog` anterior. Es deliberado, no un olvido.

    Verificar (no rediseñar) que `atajosActivos` sigue pasando `awaitingEndConfirm` a
    `shortcutsEnabled`: como el diálogo nuevo se monta con la MISMA bandera, la supresión de
    atajos con overlay abierto (D-12 de la Fase 6, D-17 de la Fase 7) sigue funcionando sin
    ninguna rama nueva.
  </action>
  <verify>
    <automated>npx nuxt build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function onOutcomeRecorded" app/pages/\[game\]/index.vue` devuelve 1 y `grep -c "function onOutcomeDismiss" app/pages/\[game\]/index.vue` devuelve 1
    - `grep -c "function onEndGameConfirm" app/pages/\[game\]/index.vue` devuelve 0 y `grep -c "function onEndGameCancel" app/pages/\[game\]/index.vue` devuelve 0
    - En el cuerpo de `onOutcomeRecorded`, la llamada a `record(` aparece DESPUÉS de `silence()` y ANTES de `finishGame()` (comprobar leyendo la función completa; `sed -n '/function onOutcomeRecorded/,/^}/p' app/pages/\[game\]/index.vue` muestra ese orden)
    - `grep -c "notifyHistorySaved" app/pages/\[game\]/index.vue` devuelve 1
    - `sed -n '/function finishGame/,/^}/p' app/pages/\[game\]/index.vue` contiene `session.value = null`, `clear(gameId)` y `navigateTo('/')` en ese orden
    - `git diff app/pages/\[game\]/index.vue | grep -c "^-.*onDiscardConfirm"` devuelve 0 (la ruta de descarte no se toca)
    - `grep -Ec "async function onOutcome|await record" app/pages/\[game\]/index.vue` devuelve 0
    - `npx nuxt build` termina con código 0
  </acceptance_criteria>
  <done>Registrar y salir sin registrar terminan la partida por el mismo camino de limpieza, y la entrada se construye mientras la sesión todavía existe.</done>
</task>

<task type="auto">
  <name>Task 3: sustituir el ConfirmDialog por GameOutcomeDialog en la plantilla</name>
  <files>app/pages/[game]/index.vue</files>
  <read_first>
    - `app/pages/[game]/index.vue` (ya modificado en la Task 2; leer el bloque de plantilla con
      el comentario D-U3 y la instancia actual de `ConfirmDialog` atada a `awaitingEndConfirm`,
      hermana justo después de `IndexOverlay`)
    - `app/components/GameOutcomeDialog.vue` (props y emits exactos)
  </read_first>
  <action>
    En la plantilla de `app/pages/[game]/index.vue`, sustituir ÍNTEGRAMENTE la instancia de
    `ConfirmDialog` que hoy lleva `v-if="awaitingEndConfirm"` por
    `<GameOutcomeDialog v-if="awaitingEndConfirm" :context-line="outcomeContextLine"
    :warning-body="endGameBody" @record="onOutcomeRecorded" @dismiss="onOutcomeDismiss" />`.

    Conservar la posición exacta y el comentario D-U3 que la precede: hermana JUSTO DESPUÉS de
    `IndexOverlay`, para que se apile encima sin tocar ningún `z-index`. La otra instancia de
    `ConfirmDialog` del fichero —la del descarte, atada a `awaitingDiscardConfirm`— NO se toca
    (D-05), así que `ConfirmDialog` sigue importándose y usándose en esta página.

    `endGameBody` se sigue usando tal cual (D-02: el aviso de que se borrará el progreso
    guardado no desaparece, cambia de sitio); su computed no se modifica.
  </action>
  <verify>
    <automated>npx nuxt build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GameOutcomeDialog" app/pages/\[game\]/index.vue` devuelve 1
    - `grep -c "v-if=\"awaitingEndConfirm\"" app/pages/\[game\]/index.vue` devuelve 1 y la línea siguiente pertenece al `GameOutcomeDialog`, no a un `ConfirmDialog`
    - `grep -c "ConfirmDialog" app/pages/\[game\]/index.vue` devuelve al menos 2 (import + instancia del descarte, que sigue viva)
    - `grep -c "Sí, terminar" app/pages/\[game\]/index.vue` devuelve 0 (el copy del diálogo anterior desaparece)
    - `grep -c ":warning-body=\"endGameBody\"" app/pages/\[game\]/index.vue` devuelve 1
    - `npx nuxt build` termina con código 0 y `npm test` termina con código 0
  </acceptance_criteria>
  <done>Pulsar «Partida terminada» abre el diálogo de cuatro opciones, apilado sobre el índice como hasta ahora.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sesión en memoria → histórico persistido | El único momento en que datos de partida cruzan al almacenamiento duradero del histórico |
| Almacenamiento del navegador → grupo | Una escritura fallida en silencio haría creer al grupo que la partida quedó guardada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-21 | Repudiation | Registro perdido por insertar la escritura después de `session.value = null` | mitigate | El registro ocurre entre `silence()` y `finishGame()`; criterio de aceptación que comprueba el orden dentro de la función, y `finishGame()` aísla los pasos 4-6 para que no se puedan reordenar por accidente |
| T-09-22 | Repudiation | Éxito fingido cuando `appendHistoryEntry` devolvió `false` | mitigate | `notifyHistorySaved` recibe exactamente el booleano devuelto por `record`, sin valor por defecto ni `|| true` |
| T-09-23 | Destrucción de datos | Que el flujo de fin de partida borre también el histórico | mitigate | `finishGame()` solo llama a `clear(gameId)`, que por construcción (plan 09-04) no puede tocar `tga:history`; hay un test unitario de HIST-09 que lo fija |
| T-09-24 | Elevation of privilege (UI) | Atajos de teclado actuando con el diálogo abierto | mitigate | El diálogo se monta con la misma bandera `awaitingEndConfirm` que `shortcutsEnabled` ya consulta; se verifica, no se rediseña |
</threat_model>

<verification>
- `npm test` en verde y `npx nuxt build` con código 0.
- `git diff engine/persistence.ts` vacío (D-07).
- `git diff app/components/ConfirmDialog.vue` vacío (el componente genérico no cambia).
- `grep -rEi "firestore|firebase" app/pages/\[game\]/index.vue` no devuelve ninguna línea.
</verification>

<success_criteria>
- Toda partida nueva guarda su instante de inicio y ninguna reanudación lo reescribe.
- Las cuatro opciones del diálogo terminan la partida; tres de ellas registran y una no.
- El aviso posterior refleja el resultado real de la escritura.
</success_criteria>

<output>
Crear `.planning/phases/09-hist-rico-y-estad-sticas/09-07-SUMMARY.md` al terminar.
</output>
