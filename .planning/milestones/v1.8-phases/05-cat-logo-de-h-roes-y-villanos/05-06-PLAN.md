---
phase: 05-cat-logo-de-h-roes-y-villanos
plan: 06
type: execute
wave: 6
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - engine/types.ts
  - engine/catalogueSchema.ts
  - engine/__tests__/catalogueSchema.test.ts
  - scripts/catalogue/fetch-marvelcdb.mjs
  - content/marvel-characters.json
  - engine/__tests__/characters.test.ts
  - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md
autonomous: true
requirements: [CAT-02, CAT-03, CAT-05]
gap_closure: true
closes_gaps: ["truth-08-kang-experto"]

must_haves:
  truths:
    - "El catálogo committeado representa la salud de Kang en modo Experto: sus tres etapas llevan un sub-objeto `expert` con 15, 22 y 25 puntos de vida (cartas del set `exp_kang`), además de los 12/18/20 del set estándar que ya guardaban"
    - "Las banderas de escalado del modo Experto se guardan por etapa y coinciden con la API: healthPerHero true en la etapa I, false en la II y true en la III; healthPerGroup false en las tres"
    - "Rhino y Ultron —villanos del Core Set cuyo escenario no trae ningún set de villano alternativo— salen del catálogo sin ninguna clave `expert` en ninguna etapa, y un test ejecutable lo fija"
    - "El contrato de datos (engine/types.ts + engine/catalogueSchema.ts) declara `expert` como sub-objeto OPCIONAL, de modo que un consumidor de la Fase 6/7 en dificultad `expert` puede leer la cifra correcta en vez de asumir que `stages` es universal"
    - "El esquema Zod rechaza cualquier clave desconocida dentro de `expert` (z.strictObject en el quinto nivel de anidamiento) y rechaza un `expert` al que le falte cualquiera de sus tres campos"
    - "El comentario del generador ya no presenta la exclusión de los códigos exp_kang como una protección: describe que 11034/11035/11039 se declaran vía `expertCode` en cada fila de Kang, y qué puertas los validan"
    - "Re-ejecutar `npm run catalogue:generate` sobre el mismo origen sigue produciendo un fichero byte a byte idéntico (D-10 intacto) con la forma nueva"
    - "El guardarraíl anti-copyright y el gate de aislamiento siguen en verde con su estructura de acceso perezoso de CR-02 intacta: ninguna lectura, parseo ni validación en ámbito de módulo ni de describe()"
  artifacts:
    - path: "engine/types.ts"
      provides: "VillainStage con expert opcional (health/healthPerHero/healthPerGroup) y el comentario que explica que los cuatro campos planos son el modo estándar"
      contains: "expert?"
    - path: "engine/catalogueSchema.ts"
      provides: "VillainStageSchema con expert como z.strictObject de tres campos y .optional() — quinto z.strictObject del fichero"
      contains: "exp_kang"
    - path: "scripts/catalogue/fetch-marvelcdb.mjs"
      provides: "Filas de Kang con expertCode 11034/11035/11039 y expectedExpertSetCode exp_kang, más las puertas que validan la carta Experto igual que la estándar"
      contains: "expertCode"
    - path: "content/marvel-characters.json"
      provides: "Kang con expert 15/22/25 por etapa; Rhino y Ultron sin clave expert"
      contains: "expert"
    - path: "engine/__tests__/characters.test.ts"
      provides: "Tests que fijan 15/22/25 con sus banderas para Kang y la ausencia de expert en Rhino/Ultron, más el gate D-11 actualizado a la forma nueva"
      contains: "expert"
    - path: "engine/__tests__/catalogueSchema.test.ts"
      provides: "Tests unitarios de expert: acepta ausente, presente y mixto por etapa; rechaza clave desconocida dentro, campo faltante y tipos inválidos"
      contains: "expert"
  key_links:
    - from: "scripts/catalogue/fetch-marvelcdb.mjs"
      to: "content/marvel-characters.json"
      via: "extractVillainStage devuelve el sub-objeto expert cuando la fila declara expertCode"
      pattern: "expertCode"
    - from: "engine/catalogueSchema.ts"
      to: "engine/types.ts"
      via: "VillainStageSchema y VillainStage declaran el mismo campo opcional expert, campo a campo"
      pattern: "expert"
    - from: "engine/__tests__/characters.test.ts"
      to: "content/marvel-characters.json"
      via: "lector perezoso loadValidatedCatalogue() dentro de it(), aserciones sobre villain.stages[n].expert"
      pattern: "expert"
---

<objective>
Cerrar el único gap que queda en `05-VERIFICATION.md` (truth #8, requisito CAT-02 en
⚠️ PARCIAL): el catálogo de villanos modela **una sola** tabla de etapas por villano, sin
ninguna dimensión de dificultad, mientras `content/marvel-champions.json` ya instruye al
grupo —en la variante `expert` de `setup.escenario.04`— *"Sustituid las cartas de villano
numeradas por las del modo Experto de este escenario"*. El pack `toafk` trae ese segundo
conjunto de Kang bajo `card_set_code: exp_kang` con 15/22/25 puntos de vida en vez de los
12/18/20 committeados, y el generador lo excluye a propósito con un comentario que
presenta la exclusión como una protección en lugar de como un hueco del contrato.

Purpose: la app ya pide la dificultad al usuario desde el mini-setup
(`Difficulty = 'normal' | 'expert'`, `engine/types.ts:6`). En cuanto la Fase 6/7 lea este
catálogo en una partida Experta narrará 12/18/20 mientras la mesa tiene físicamente las
cartas de 15/22/25 — exactamente "un asistente que guía mal", el fallo que `CLAUDE.md`
§Constraints declara peor que no tener asistente. Y el coste solo crece: la forma que se
congele en esta fase es la que la Fase 6 va a importar.

**Decisión del usuario, ya tomada y no reabrible:** se implementa la "salida buena" del
verificador —modelar la dificultad **en la etapa**, con un sub-objeto `expert` opcional—,
no la alternativa de renombrar el campo a `standardStages` y diferir. Los campos
existentes `health` / `healthPerHero` / `healthPerGroup` **conservan su nombre y su
significado** (modo estándar); no hay ningún renombrado.

Output: contrato (`engine/types.ts`, `engine/catalogueSchema.ts`) y generador ampliados
con la dimensión de dificultad, `content/marvel-characters.json` regenerado con las tres
etapas de Kang llevando su bloque `expert`, Rhino y Ultron sin ninguna clave `expert`,
determinismo D-10 reconfirmado, y las cifras fijadas por tests ejecutables en vez de
documentadas en prosa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-VERIFICATION.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-REVIEW.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-02-SUMMARY.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-04-SUMMARY.md

<interfaces>
<!-- Forma ACTUAL en el repo y forma OBJETIVO. El ejecutor no necesita explorar el -->
<!-- código para conocer los contratos: están aquí. Los datos de la API de abajo -->
<!-- están VERIFICADOS EN VIVO durante esta planificación (2026-09-07), no asumidos. -->

ACTUAL — `engine/types.ts` líneas 124-129:
  interface VillainStage { stage: number; health: number; healthPerHero: boolean; healthPerGroup: boolean }

ACTUAL — `engine/catalogueSchema.ts` líneas 38-43:
  const VillainStageSchema = z.strictObject({ stage, health, healthPerHero, healthPerGroup })
  (los cuatro con z.number().int().positive() / z.boolean(). Un grep de `z.strictObject(`
  sobre las líneas no comentadas devuelve HOY 4: VillainStageSchema, HeroSchema,
  VillainSchema, CharacterCatalogueSchema)

OBJETIVO — `VillainStage` gana un quinto campo, ÚLTIMO y OPCIONAL, sin tocar los cuatro
existentes ni sus nombres:
  expert?: { health: number; healthPerHero: boolean; healthPerGroup: boolean }

OBJETIVO — `VillainStageSchema` gana `expert` como z.strictObject de esos tres campos con
`.optional()`, lo que eleva el recuento de `z.strictObject(` a 5. Ningún `.default()` en
ninguna parte: el objeto que sale de `parse()` debe seguir siendo idéntico al crudo del
fichero (evita la divergencia parse/crudo señalada como IN-06 en `05-REVIEW.md`).

ACTUAL — `scripts/catalogue/fetch-marvelcdb.mjs` (294 líneas):
  const STAGE_MAP = { I: 1, II: 2, III: 3 }                       -- línea 65
  const REQUEST_DELAY_MS = 1500                                   -- línea 68
  const VILLAIN_STAGE_CARDS = [ ... 9 filas ... ]                 -- líneas 111-130
  el comentario a corregir (estilo D-09)                          -- líneas 123-126
  function extractVillainStage({ villainName, code, stage, expectedSetCode })  -- líneas 219-252
  function writeCatalogue(heroes, villains)                       -- líneas 254-259
  async function main()                                           -- líneas 261-288 (dice "los 32 códigos")
  cabecera CAT-04: "sólo los cinco campos de héroe y los cuatro de etapa de villano" -- líneas 33-34
  bloque "CÓMO AÑADIR UN HÉROE O VILLANO NUEVO (CAT-07)"          -- líneas 36-52

DATOS DE LA API — verificados en vivo con `curl https://marvelcdb.com/api/public/card/{code}.json`
durante esta planificación. Estas son las cifras que el ejecutor debe obtener; si la API
devolviera otra cosa, es un cambio del origen: hay que PARAR y reportarlo, nunca ajustar
el test a lo que salga.

| code  | name                       | type_code | card_set_code | card_set_name | stage | health | health_per_hero | health_per_group |
|-------|----------------------------|-----------|---------------|---------------|-------|--------|-----------------|------------------|
| 11001 | (estándar, ya committeado) | villain   | kang          | Kang          | I     | 12     | true            | false            |
| 11002 | (estándar, ya committeado) | villain   | kang          | Kang          | II    | 18     | false           | false            |
| 11006 | (estándar, ya committeado) | villain   | kang          | Kang          | III   | 20     | true            | false            |
| 11034 | Kang (The Conqueror)       | villain   | exp_kang      | Expert Kang   | I     | 15     | true            | false            |
| 11035 | Kang (Immortus)            | villain   | exp_kang      | Expert Kang   | II    | 22     | false           | false            |
| 11039 | Kang (The Conqueror)       | villain   | exp_kang      | Expert Kang   | III   | 25     | true            | false            |

Nota verificada en vivo también: igual que el set estándar, `exp_kang` tiene **cuatro
alternativas narrativas de etapa II** (`11035` Immortus, `11036` Iron Lad, `11037`
Rama-Tut, `11038` Scarlet Centurion), las cuatro con `22 / false / false` idénticos. Se
declara `11035` como representante (la primera por `position`), espejo exacto de la
elección de `11002` en el set estándar; a efectos numéricos —todo lo que este catálogo
guarda— da igual cuál de las cuatro sea.

Nota verificada: `health_per_hero` de la etapa II es **false** también en Experto. Es la
misma asimetría por etapa que ya justifica que las banderas vivan en la etapa y no en el
villano (D-11) — no es un error de extracción.

Contenido de la Fase 2 que este gap hace narrable de verdad (`content/marvel-champions.json`,
paso `setup.escenario.04`, líneas ~198-219):
  variants.difficulty.normal -> "Dejad las cartas de villano numeradas (etapas) tal como vienen con el escenario."
  variants.difficulty.expert -> "Sustituid las cartas de villano numeradas por las del modo Experto de este escenario."
  Este plan NO toca ese fichero: la Fase 5 no toca `content/marvel-champions.json`.

Gates existentes que este plan debe dejar en verde SIN degradar:
  engine/__tests__/characters.test.ts:197-205  -- gate D-11 de claves exactas por etapa
      (`expectedKeys = ['stage','health','healthPerHero','healthPerGroup'].sort()` con
      `toEqual` — ESTE TEST FALLARÁ al añadir `expert` y hay que actualizarlo en la
      Task 3, nunca borrarlo)
  engine/__tests__/characters.test.ts:60-99    -- blocklist de 20 claves prohibidas sobre el crudo
  engine/__tests__/catalogue-isolation.test.ts:133-141 -- el crudo no contiene 'http'/'marvelcdb'/'api'
      (verificado durante la planificación: la subcadena "expert" no contiene ninguna de
      las tres, así que este gate no necesita ningún cambio — solo confirmarlo en verde)

Baseline medido durante esta planificación con `npx vitest run --project engine`:
  `Test Files 12 passed (12)`, `Tests 239 passed (239)`. Este plan no añade ficheros de
  test nuevos: al terminar deben seguir siendo 12 ficheros y el total de tests debe ser
  estrictamente mayor que 239.
</interfaces>
</context>

<source_audit>
## Auditoría de cobertura de fuentes (alcance: el gap de truth #8)

| Fuente | Ítem | Estado | Tarea |
|--------|------|--------|-------|
| GOAL (ROADMAP Fase 5) | "catálogo fiable... listo para alimentar la selección y los contadores de las fases siguientes" | COVERED | Tasks 1-3: la forma que la Fase 6/7 importará ya incluye la dimensión de dificultad |
| REQ CAT-02 | "vida por etapa, indicando explícitamente si esa cifra es por jugador o total" (⚠️ PARCIAL) | COVERED | Task 2 (datos) + Task 3 (tests que fijan las cifras) |
| REQ CAT-03 | script regenera, uso documentado, procedimiento de una fila | COVERED | Task 2: filas nuevas, procedimiento CAT-07 ampliado con `expertCode`, determinismo D-10 recomprobado |
| REQ CAT-05 | esquema Zod en test de Vitest que falla la build | COVERED | Task 1: `expert` bajo `z.strictObject` con tests unitarios propios |
| REQ CAT-01/04/06/07 | ya SATISFECHO en `05-VERIFICATION.md` | COVERED (preservación) | Criterios de aceptación de las tres tareas los re-comprueban; ninguna tarea los modifica |
| RESEARCH `05-RESEARCH.md:445` | "el script debe declarar únicamente códigos de `card_set_code: kang`... salvo que una decisión futura explícita quiera modelar también el modo experto" | COVERED | Esta ES esa decisión explícita. Task 2 la implementa; Task 3 la registra en `05-CONTEXT.md` para que el research no quede contradicho sin rastro |
| CONTEXT D-02 | 3 villanos, Klaw fuera a propósito | RESPETADO | Sigue habiendo 3 villanos; `exp_kang` no añade un cuarto villano, añade una dimensión a Kang |
| CONTEXT D-06 | Kang aborta si no mapea limpiamente | RESPETADO | Task 2: las tres cartas Experto pasan por las mismas puertas de aborto que las estándar |
| CONTEXT D-09 | nadie edita el JSON a mano; el fix va en el script | RESPETADO | Task 2 regenera con `npm run catalogue:generate`; edición manual del JSON prohibida y comprobada por el gate de determinismo |
| CONTEXT D-10 | fichero determinista puro | COVERED | Task 2, criterio de aceptación de doble ejecución más `cmp` |
| CONTEXT D-11 | base + banderas por etapa, sin precomputar por jugadores | RESPETADO Y AMPLIADO | `expert` es también base + banderas, nunca una tabla `healthByPlayerCount`; el gate D-11 actualizado en Task 3 lo sigue prohibiendo también dentro de `expert` |

**Sin ítems MISSING.** Fuera de alcance por clasificación explícita de la verificación
(warnings no bloqueantes): WR-01 (cruce `villainName`/`expectedSetCode`), WR-02
(`alterEgo` sin fijar), WR-03 (`STAGE_MAP` sin `IV`), WR-04 (duplicación de
`findForbiddenKey`), WR-05 (sin `typecheck` en CI), WR-06 (`engine/schema.ts`, fase
anterior). CR-01 y CR-02 están cerrados y re-verificados: no se reabren.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Contrato — VillainStage gana un sub-objeto expert opcional en tipos y esquema</name>
  <files>engine/types.ts, engine/catalogueSchema.ts, engine/__tests__/catalogueSchema.test.ts</files>
  <read_first>
    - engine/types.ts líneas 111-167 (bloque completo "Catálogo de personajes (Fase 5)": comentario de cabecera del bloque, el comentario que cita D-11 justo antes de VillainStage, VillainStage, CatalogueHero, CatalogueVillain y CharacterCatalogue — hace falta ver el registro exacto de esos comentarios para escribir el nuevo en el mismo tono, y confirmar que el fichero no importa zod)
    - engine/catalogueSchema.ts completo (123 líneas: cabecera CR-01 sobre z.strictObject vs z.object, characterIdPattern, slugifyCharacterName, los cuatro esquemas y el superRefine con sus tres invariantes; el cambio va SOLO en VillainStageSchema, ninguna rama del superRefine se toca)
    - engine/__tests__/catalogueSchema.test.ts completo (197 líneas: baseCatalogue() en 7-25, el describe "claves desconocidas rechazadas (CR-01 / CAT-04)" en 36-68 con su test de illustrator en una etapa, el describe "tipos y rangos" en 70-106, y el describe "DC-01: etapas de villano" en 122-174 con sus literales de etapa)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-VERIFICATION.md (bloque `gaps:` del frontmatter: el `reason` y las dos entradas de `missing` — se implementa la primera, la "salida buena")
    - engine/schema.ts líneas 1-40 (la cabecera de "los dos únicos ficheros que importan zod" y la disciplina de comentarios del esquema hermano; sirve de referencia de registro, NO se modifica en este plan)
  </read_first>
  <action>
Ampliar el contrato de la etapa de villano con una dimensión de dificultad, sin renombrar
ni cambiar el significado de ningún campo existente.

**`engine/types.ts`** — en la interfaz `VillainStage` (líneas ~124-129), añadir un quinto
campo, el ÚLTIMO y OPCIONAL, con esta forma exacta: `expert?: { health: number;
healthPerHero: boolean; healthPerGroup: boolean }`. Tipo anidado en línea, sin crear una
interfaz exportada nueva: es la misma tripleta base+banderas de D-11 y no necesita nombre
propio. Los cuatro campos actuales (`stage`, `health`, `healthPerHero`, `healthPerGroup`)
se quedan tal cual, con su nombre y su significado — pasan a ser explícitamente los del
**modo estándar**.

Ampliar el comentario de bloque que precede a `VillainStage` (líneas ~121-123, el que
cita D-11 y el numeral romano). Debe decir, en el mismo registro que el resto del fichero:
  - que los cuatro campos planos son las cifras del set de villano **estándar** del
    escenario;
  - que `expert` lleva las del set de villano de **modo Experto** cuando el escenario trae
    uno (Kang: `card_set_code` `exp_kang`, 15/22/25 frente a 12/18/20), y que su ausencia
    significa que el modo Experto de ese escenario **no sustituye** las cartas de villano
    numeradas (Rhino y Ultron, del Core Set) — la ausencia es un hecho del dominio, no un
    dato pendiente;
  - por qué vive en la etapa y no en el villano: las banderas difieren por etapa dentro
    del mismo villano en los dos sets (Kang I true / II false / III true);
  - que quien consuma esto en la Fase 6/7 debe leer `expert` cuando la sesión está en
    `difficulty: 'expert'` y hay `expert` disponible, porque
    `content/marvel-champions.json` (paso `setup.escenario.04`, variante `expert`) ya
    instruye al grupo sustituir esas cartas físicas en la mesa;
  - que la multiplicación por número de jugadores sigue siendo de la Fase 7, nunca de
    aquí (D-11 intacto), también para `expert`.

**`engine/catalogueSchema.ts`** — declarar el esquema del sub-objeto Experto como una
constante propia con `z.strictObject` (nunca `z.object`), con los tres campos
`health: z.number().int().positive()`, `healthPerHero: z.boolean()` y
`healthPerGroup: z.boolean()`, y referenciarlo dentro de `VillainStageSchema` como
`expert` con `.optional()`, en último lugar. Prohibido usar `.default()` en cualquier
punto: el objeto que devuelve `parse()` tiene que seguir siendo idéntico al crudo del
fichero (la divergencia parse/crudo es el defecto IN-06 de `05-REVIEW.md` y no se replica
aquí). No tocar `characterIdPattern`, `slugifyCharacterName`, `HeroSchema`,
`VillainSchema`, `CharacterCatalogueSchema` ni ninguna rama del `superRefine`.

Añadir junto a `expert` un comentario de una o dos líneas con la consecuencia buscada: al
ser `z.strictObject` también en este quinto nivel de anidamiento, una clave
`text`/`flavor`/`imagesrc` colada dentro de `expert` lanza en vez de descartarse en
silencio (CAT-04), y un `expert` incompleto también lanza. El comentario debe nombrar
`exp_kang` como el set real que motiva el campo.

Revisar la cabecera del fichero y actualizarla si —y solo si— alguna de sus afirmaciones
queda falsa con este cambio (por ejemplo si menciona un número concreto de objetos o de
niveles de anidamiento). Comprobarlo leyendo la cabecera, no asumirlo.

**`engine/__tests__/catalogueSchema.test.ts`** — `baseCatalogue()` se queda como está: su
Rhino de tres etapas SIN `expert` es ya la prueba positiva de que el campo es opcional.
Añadir un describe nuevo —con la subcadena `expert` en el nombre— con estos tests, todos
sobre `CharacterCatalogueSchema.parse()`:
  1. no lanza con una etapa que lleve un `expert` válido (`health: 15`, `healthPerHero:
     true`, `healthPerGroup: false`);
  2. no lanza con un villano MIXTO: etapa 1 con `expert`, etapa 2 sin él — fija que la
     opcionalidad es por etapa, no por villano;
  3. lanza con una clave desconocida DENTRO de `expert` (usar `flavor` o `illustrator`,
     nombres reales de la API de MarvelCDB, no genéricos, siguiendo la disciplina del
     describe "claves desconocidas rechazadas");
  4. lanza si a `expert` le falta uno de sus tres campos (borrar `healthPerGroup`);
  5. lanza con `expert.health` a 0, y lanza con `expert.health` no entero;
  6. lanza con `expert.healthPerHero` como string `'true'` en vez de booleano;
  7. lanza con un `expert` anidado dentro de `expert` (queda cubierto por strictObject;
     el test lo hace explícito para que se lea qué protege).
Los literales de etapa de los describes "DC-01" y "tipos y rangos" no se tocan: siguen
siendo prueba viva de que `expert` es opcional.
  </action>
  <verify>
    <automated>npx vitest run --project engine engine/__tests__/catalogueSchema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project engine engine/__tests__/catalogueSchema.test.ts` termina con exit code 0
    - `grep -c "expert?" engine/types.ts` devuelve al menos 1
    - `grep -c "healthPerGroup" engine/types.ts` devuelve al menos 2 (el campo estándar y el de dentro de `expert`)
    - `grep -cE "from 'zod'|require\\('zod'\\)" engine/types.ts` devuelve 0 (T-01-19: el fichero de tipos sigue sin importar zod; ojo, un `grep -c "zod"` a secas devuelve 1 por la mención de T-01-19 en el comentario de la línea 118 y no sirve como puerta)
    - `grep -v "^\s*//" engine/catalogueSchema.ts | grep -c "z.strictObject("` devuelve exactamente 5
    - `grep -c "\.optional()" engine/catalogueSchema.ts` devuelve al menos 1 y `grep -c "\.default(" engine/catalogueSchema.ts` devuelve 0
    - `grep -c "exp_kang" engine/catalogueSchema.ts` devuelve al menos 1
    - `grep -ic "standardStages" engine/types.ts` devuelve 0 y `grep -ic "standardStages" engine/catalogueSchema.ts` devuelve 0 (se implementó la salida buena, no la alternativa diferida)
    - `grep -c "it(" engine/__tests__/catalogueSchema.test.ts` devuelve al menos 29 (baseline medido en planificación: 22, más los 7 tests nuevos)
    - En `engine/__tests__/catalogueSchema.test.ts` existe un describe cuyo nombre contiene `expert`, y dentro de él al menos un test que inserta una clave real de la API (`flavor` o `illustrator`) dentro de `expert` y afirma `.toThrow()`
    - `node -e "const {CharacterCatalogueSchema}=await import('./engine/catalogueSchema.ts')"` NO se usa como comprobación: este repo no tiene typecheck ni carga TS desde node (WR-05). La prueba de que tipo y esquema coinciden es que los tests de Vitest de este fichero pasan con literales que llevan `expert`
  </acceptance_criteria>
  <done>VillainStage y VillainStageSchema declaran expert como sub-objeto opcional de tres campos, bajo z.strictObject en el quinto nivel de anidamiento y sin ningún .default(); el comentario de tipos explica que los cuatro campos planos son el modo estándar y que la ausencia de expert es un hecho del dominio; los tests unitarios del esquema cubren aceptación (ausente, presente, mixto por etapa) y rechazo (clave desconocida dentro, campo faltante, tipos y rangos inválidos).</done>
</task>

<task type="auto">
  <name>Task 2: Generador — declarar los códigos exp_kang, validarlos con las mismas puertas y regenerar el catálogo</name>
  <files>scripts/catalogue/fetch-marvelcdb.mjs, content/marvel-characters.json</files>
  <read_first>
    - scripts/catalogue/fetch-marvelcdb.mjs completo (294 líneas: la cabecera D-06/D-09/D-10/CAT-04 y el bloque "CÓMO AÑADIR UN HÉROE O VILLANO NUEVO" marcan el registro; STAGE_MAP y REQUEST_DELAY_MS en 65-68; VILLAIN_STAGE_CARDS en 111-130 con el comentario a corregir en 123-126; fetchCard en 149-167; extractVillainStage en 219-252; writeCatalogue en 254-259; main en 261-288 con su comentario de "los 32 códigos". extractHero NO se toca)
    - engine/catalogueSchema.ts tal como queda tras la Task 1 (la forma exacta —orden de campos y opcionalidad de expert— que el fichero regenerado tiene que satisfacer)
    - content/marvel-characters.json líneas 189-260 (el array `villains` completo: Rhino, Kang y Ultron con sus tres etapas cada uno, para ver el formato de indentación y el orden de claves que el diff debe conservar)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md §"Comportamiento ante fallo del script" (D-05 y D-06 completos) y §"Corrección de cifras y reproducibilidad" (D-09 y D-10 completos)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-02-SUMMARY.md (el pacing de 1500 ms y el coste real de una ejecución completa: ~48 s para 32 códigos; con 35 pasa a ~53 s)
  </read_first>
  <action>
Declarar las tres cartas del set `exp_kang` y proyectar sus cifras al sub-objeto `expert`,
por el mismo camino de "una fila" que exige CAT-07 y con las mismas puertas de aborto de
D-05/D-06 que ya protegen las cartas estándar.

**Filas de `VILLAIN_STAGE_CARDS`** — las tres filas de Kang ganan dos campos nuevos, y
solo ellas. Valores exactos, ya verificados en vivo contra la API (tabla de
`<interfaces>`):
  - fila `code: '11001'`, stage 1 -> `expertCode: '11034'`, `expectedExpertSetCode: 'exp_kang'`
  - fila `code: '11002'`, stage 2 -> `expertCode: '11035'`, `expectedExpertSetCode: 'exp_kang'`
  - fila `code: '11006'`, stage 3 -> `expertCode: '11039'`, `expectedExpertSetCode: 'exp_kang'`
Las seis filas de Rhino y Ultron NO se tocan: son villanos del Core Set sin
`card_set_code` alternativo conocido, y deben seguir sin `expertCode` para que salgan del
catálogo sin ninguna clave `expert`. **Prohibido inventarles cifras de Experto.**

**Comentario de las líneas 123-126** — sustituirlo por completo. El texto actual dice que
los códigos `exp_kang` "quedan FUERA a propósito" y que "la puerta de expectedSetCode los
atraparía si alguien los colara por error": después de este cambio eso describe lo
contrario de lo que hace el script. El comentario nuevo debe decir:
  - que `11034`/`11035`/`11039` (set `exp_kang`, "Expert Kang") **sí** están declarados,
    como `expertCode` de cada fila estándar de Kang, porque
    `content/marvel-champions.json` ya instruye sustituir las cartas de villano numeradas
    en dificultad Experta y narrar 12/18/20 en esa partida sería guiar mal (`CLAUDE.md`
    §Constraints);
  - las cifras concretas: 15/22/25 frente a 12/18/20, con `health_per_hero`
    true/false/true por etapa en los dos sets;
  - que la etapa II Experta también tiene cuatro alternativas narrativas (`11035`
    Immortus, `11036` Iron Lad, `11037` Rama-Tut, `11038` Scarlet Centurion) con
    `22/false/false` idénticos, y que se declara `11035` como representante igual que
    `11002` en el set estándar, sin impacto numérico;
  - que un villano sin `expertCode` sale sin clave `expert`, y que eso significa "el modo
    Experto de este escenario no sustituye sus cartas numeradas" (Rhino, Ultron), no
    "dato pendiente";
  - que la referencia de esta decisión es el cierre del gap de truth #8 de
    `05-VERIFICATION.md`, que resuelve con decisión explícita del usuario la nota de
    alcance que `05-RESEARCH.md` había dejado abierta sobre `exp_kang`.

**`extractVillainStage`** — refactorizar para no duplicar puertas entre la carta estándar
y la Experto. Extraer las comprobaciones que hoy viven en el cuerpo de la función a un
helper puro y síncrono que reciba la carta ya descargada más el contexto declarado
(`villainName`, el código concreto, `stage`, el `card_set_code` esperado y una etiqueta
legible del set — `'estándar'` o `'Experto'`) y devuelva la tripleta
`{ health, healthPerHero, healthPerGroup }` o lance. Las puertas, aplicadas **a las dos
cartas por igual**: `type_code === 'villain'`; `card_set_code` igual al esperado;
`STAGE_MAP[card.stage]` definido; `STAGE_MAP[card.stage]` igual al `stage` declarado en la
fila (esta es la que ata la carta Experto a la etapa correcta); `health` entero mayor que
0; `health_per_hero` y `health_per_group` booleanos. Todos los mensajes de
`throw new Error` deben incluir el código, el nombre del villano, el número de etapa y la
etiqueta del set, siguiendo el patrón literal de los mensajes que ya hay en el fichero,
para que el desarrollador sepa qué descarga concreta falló.

Añadir dos puertas de aborto nuevas, propias de la dimensión Experta (D-05: fallar alto
antes que escribir un dato dudoso):
  - `expertCode` y `expectedExpertSetCode` van juntos o no van: si una fila declara uno
    sin el otro, lanzar nombrando la fila;
  - el `expertCode` no puede ser igual al `code` estándar de su fila: si coinciden, la
    fila apunta la carta Experto a la carta estándar y hay que lanzar.

La función solo descarga la carta Experto cuando la fila declara `expertCode`. Construir
el objeto de retorno de forma que la clave `expert` **no exista** cuando no hay
`expertCode` —asignándola condicionalmente después de construir la base, nunca dejándola
como `undefined` ni introduciéndola con un spread—, y con este orden de claves exacto:
`stage`, `health`, `healthPerHero`, `healthPerGroup`, y `expert` al final cuando aplica;
dentro de `expert`: `health`, `healthPerHero`, `healthPerGroup`. Sigue siendo lista blanca
campo a campo (CAT-04): prohibido cualquier spread del objeto `card` de la API.

**Cabecera y contadores** — actualizar todas las afirmaciones que este cambio vuelve
falsas, comprobándolas por lectura y no de memoria: la frase de CAT-04 sobre "los cinco
campos de héroe y los cuatro de etapa de villano" (los de héroe ya son seis desde el plan
05-04, y los de etapa pasan a cuatro más el sub-objeto opcional `expert` de tres), y el
comentario de `main()` que habla de "los 32 códigos" (pasan a 35: 23 héroes + 9 etapas
estándar + 3 etapas Experto; ~53 s con el pacing de 1500 ms). Ampliar el bloque "CÓMO
AÑADIR UN HÉROE O VILLANO NUEVO" (CAT-07) con el paso correspondiente: si el escenario del
villano nuevo trae un set de villano de modo Experto, la misma fila de etapa gana
`expertCode` y `expectedExpertSetCode`; si no lo trae, se dejan fuera y la etapa sale sin
clave `expert`. El procedimiento debe seguir siendo de una sola fila por etapa.

**Regenerar** con `npm run catalogue:generate`. Tarda ~53 s y aborta sin escribir nada si
cualquiera de los 35 códigos falla (D-05). **No editar `content/marvel-characters.json` a
mano bajo ningún concepto** (D-09): si un número sale distinto de la tabla verificada de
`<interfaces>`, PARAR y reportarlo en el SUMMARY como un cambio del origen — no ajustar
nada para que cuadre.

**Comprobar el determinismo D-10 explícitamente**, porque este plan cambia la forma de la
salida: copiar el fichero recién generado a un fichero temporal fuera del repo, volver a
ejecutar `npm run catalogue:generate`, y comparar los dos con `cmp` (deben ser byte a byte
idénticos). Registrar en el SUMMARY el resultado del `cmp` y los seis valores Experto
obtenidos (health y las dos banderas de cada etapa).
  </action>
  <verify>
    <automated>node -e "const c=require('./content/marvel-characters.json');const k=c.villains.find(v=>v.id==='kang');const got=k.stages.map(s=>[s.health,s.expert.health,s.expert.healthPerHero,s.expert.healthPerGroup]);const want=[[12,15,true,false],[18,22,false,false],[20,25,true,false]];if(JSON.stringify(got)!==JSON.stringify(want))throw new Error('Kang mal: '+JSON.stringify(got));for(const id of ['rhino','ultron'])for(const s of c.villains.find(v=>v.id===id).stages)if('expert' in s)throw new Error(id+' no debe llevar expert');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run catalogue:generate` termina con exit code 0 e imprime `Escritos 23 héroes y 3 villanos en content/marvel-characters.json`
    - El comando de `<verify>` imprime `OK` (fija 12/18/20 estándar, 15/22/25 Experto, banderas true/false/true por etapa y per-group false, y la ausencia de `expert` en Rhino y Ultron)
    - `node -e "const c=require('./content/marvel-characters.json');const k=c.villains.find(v=>v.id==='kang');console.log(JSON.stringify(Object.keys(k.stages[0])),JSON.stringify(Object.keys(k.stages[0].expert)))"` imprime exactamente `["stage","health","healthPerHero","healthPerGroup","expert"] ["health","healthPerHero","healthPerGroup"]`
    - `node -e "const c=require('./content/marvel-characters.json');console.log(JSON.stringify(Object.keys(c.villains.find(v=>v.id==='rhino').stages[0])))"` imprime exactamente `["stage","health","healthPerHero","healthPerGroup"]`
    - `grep -c '"expert"' content/marvel-characters.json` devuelve exactamente 3 (una por etapa de Kang, ninguna en Rhino ni Ultron)
    - Determinismo D-10: `cp content/marvel-characters.json "$TMPDIR/cat-run1.json" && npm run catalogue:generate && cmp "$TMPDIR/cat-run1.json" content/marvel-characters.json` termina con exit code 0 y sin salida
    - `git diff --stat -- content/marvel-characters.json` tras la segunda ejecución muestra el mismo diff que tras la primera (la segunda ejecución no añade ningún cambio propio)
    - `grep -c "expertCode" scripts/catalogue/fetch-marvelcdb.mjs` devuelve al menos 4 (las tres filas de Kang más el uso en la extracción)
    - `grep -c "11034" scripts/catalogue/fetch-marvelcdb.mjs` es al menos 1, e igual para `11035` y `11039`
    - `grep -c "los atraparía si alguien" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0 (el comentario que enmarcaba la exclusión como protección ya no existe)
    - `grep -c "quedan FUERA a propósito" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0
    - `grep -c "los 32 códigos" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0 y `grep -c "35" scripts/catalogue/fetch-marvelcdb.mjs` devuelve al menos 1
    - `grep -c "\.\.\.card" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0 (lista blanca CAT-04 intacta)
    - `grep -Ec '"(text|real_text|flavor|traits|imagesrc|illustrator|url|octgn_id|generatedAt)"' content/marvel-characters.json` devuelve 0
    - `grep -Ec "http|marvelcdb|api" content/marvel-characters.json` devuelve 0 (CAT-06 intacto con la forma nueva: verificado en planificación que la subcadena `expert` no contiene ninguna de las tres)
    - `npx vitest run --project engine engine/__tests__/catalogue-isolation.test.ts` termina con exit code 0 (el gate CAT-06/D-06 sigue en verde sin haber sido modificado)
    - `git diff --exit-code -- package.json package-lock.json` termina con exit code 0 (cero dependencias nuevas)
  </acceptance_criteria>
  <done>Las tres filas de Kang declaran expertCode 11034/11035/11039 con expectedExpertSetCode exp_kang; un único helper de puertas valida por igual la carta estándar y la Experto (type_code, card_set_code, mapeo de etapa, rangos y booleanos) y hay puertas nuevas para expertCode/expectedExpertSetCode incompletos y para expertCode igual al code; el catálogo regenerado lleva expert 15/22/25 en Kang y ninguna clave expert en Rhino ni Ultron; el comentario de exclusión está sustituido por la descripción real; el procedimiento CAT-07 documenta el caso; el determinismo D-10 está recomprobado con cmp.</done>
</task>

<task type="auto">
  <name>Task 3: Gates del catálogo real — fijar 15/22/25 por test, actualizar el gate D-11 y registrar la decisión en CONTEXT</name>
  <files>engine/__tests__/characters.test.ts, .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md</files>
  <read_first>
    - engine/__tests__/characters.test.ts completo (219 líneas: el bloque de comentario CR-02 de las líneas 16-37 explica por qué TODA lectura/parseo/validación vive dentro de un it() y nunca en ámbito de módulo ni de describe — esa estructura es intocable; readRawCatalogueText y loadValidatedCatalogue en 38-54; FORBIDDEN_KEYS en 56-81; findForbiddenKey en 86-88; el describe "invariantes de forma sobre villanos" en 151-190; el gate D-11 de claves exactas en 192-205; el gate CAT-07 de "ningún recuento" en 207-218)
    - content/marvel-characters.json líneas 189-275 tal como queda tras la Task 2 (los tres villanos con Kang llevando expert, para escribir las aserciones contra los datos reales)
    - engine/types.ts líneas 118-135 tal como queda tras la Task 1 (la forma de VillainStage con expert)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md §"Vida de villano" (D-11 completo, incluida la "Nota para el planner" y la frase "No reabrir esta disyuntiva") y §"Qué entra en el catálogo" (D-02 y D-06)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-04-SUMMARY.md (el precedente exacto de cómo el plan 05-04 anotó una corrección de gap dentro de 05-CONTEXT.md sin borrar la decisión original: bloque "Corrección (gap CR-01 de 05-VERIFICATION.md)" añadido bajo D-09)
  </read_first>
  <action>
Convertir las cifras del modo Experto en aserciones ejecutables y dejar la decisión
registrada en el artefacto de contexto, para que ni la Fase 6/7 ni una regeneración futura
puedan perderlas en silencio.

**`engine/__tests__/characters.test.ts`** — respetar sin excepción la estructura CR-02:
toda lectura de fichero, `JSON.parse` y llamada a `validateCharacterCatalogue()` debe
seguir ocurriendo dentro del cuerpo de un `it()`, vía `loadValidatedCatalogue()` /
`readRawCatalogueText()`. Nada de mover cargas a ámbito de módulo, a un `describe()`, a un
`beforeAll` ni a la fuente de datos de un `it.each`.

1. **Actualizar el gate D-11 de claves exactas por etapa** (líneas 192-205). Hoy exige
   `toEqual(['stage','health','healthPerHero','healthPerGroup'].sort())` y fallará al
   añadir `expert`. Reescribirlo conservando su propósito íntegro —que nadie cuele una
   tabla precomputada por número de jugadores— con esta forma: las claves de cada etapa
   deben contener siempre las cuatro obligatorias y no contener ninguna fuera del conjunto
   permitido `stage`/`health`/`healthPerHero`/`healthPerGroup`/`expert`; y cuando `expert`
   está presente, sus propias claves deben ser exactamente
   `health`/`healthPerHero`/`healthPerGroup`, ni una más. El mensaje de fallo debe nombrar
   el id del villano y el número de etapa, como el actual. Ampliar el comentario que lo
   precede para decir que la prohibición de precomputar (por ejemplo
   `healthByPlayerCount`) aplica también DENTRO de `expert`, y que la multiplicación por
   jugadores sigue siendo de la Fase 7 en los dos modos.

2. **Añadir un describe nuevo** —nombre que mencione CAT-02 y el modo Experto— con tres
   tests:
   - **Fidelidad de Kang (pin de cifras):** el villano con id `kang` tiene tres etapas;
     sus `health` estándar son 12, 18 y 20; sus `expert.health` son 15, 22 y 25; sus
     `expert.healthPerHero` son true, false y true; y sus tres `expert.healthPerGroup` son
     false. El test debe fallar con un mensaje que nombre la etapa y el valor obtenido si
     alguno se desvía. Comentario obligatorio encima: estas cifras vienen del set
     `card_set_code: exp_kang` del pack `toafk` (códigos 11034/11035/11039), y son las que
     `content/marvel-champions.json` manda poner en mesa en la variante `expert` de
     `setup.escenario.04`; si una regeneración las cambia, es un cambio del origen que hay
     que revisar a mano, no un test que ajustar.
   - **Ausencia deliberada en Rhino y Ultron:** para los ids `rhino` y `ultron`, ninguna
     etapa tiene la clave `expert` (comprobado con `'expert' in stage`, no con
     `=== undefined`, para que un `expert: null` también falle). Comentario obligatorio:
     son villanos del Core Set cuyo escenario no trae set de villano de modo Experto, así
     que la ausencia es un hecho del dominio y no un dato pendiente; y esta lista de dos
     ids no rompe CAT-07 porque un villano futuro que SÍ traiga set Experto simplemente no
     estará en ella (lo que este test prohíbe es fabricar cifras de Experto para estos
     dos, no que otros las tengan).
   - **Invariante de forma general sobre `expert`** (sin enumerar ids): para todo villano
     y toda etapa que tenga `expert`, `expert.health` es entero mayor que 0 y las dos
     banderas son booleanas. Mismo estilo de mensajes que las invariantes de forma que ya
     existen en el fichero.

3. No tocar `FORBIDDEN_KEYS`, `findForbiddenKey`, el describe del guardarraíl
   anti-copyright, ni el gate CAT-07 de "ningún recuento de personajes": ninguno se ve
   afectado por este cambio y los tres deben seguir pasando exactamente igual.

**`.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md`** — siguiendo el
precedente literal del plan 05-04 (que añadió un bloque "Corrección (gap CR-01 de
05-VERIFICATION.md)" bajo D-09 sin borrar la decisión original), añadir bajo **D-11** un
bloque titulado como ampliación por el gap de truth #8 de `05-VERIFICATION.md`, que diga:
  - que la vida de villano gana una dimensión de dificultad: sub-objeto `expert` opcional
    por etapa, con la misma forma base+banderas;
  - que esto **no reabre** la disyuntiva que D-11 cerró: `expert` sigue siendo base +
    banderas y sigue prohibido precomputar `healthByPlayerCount`, dentro y fuera de
    `expert`; la multiplicación por jugadores sigue siendo de la Fase 7;
  - que es la **decisión explícita del usuario** que `05-RESEARCH.md` dejaba abierta al
    calificar `exp_kang` de "fuera de alcance de D-02/D-03 salvo decisión futura
    explícita", y el motivo: `content/marvel-champions.json` ya instruye sustituir las
    cartas de villano numeradas en dificultad Experta;
  - que Rhino y Ultron salen sin `expert` a propósito, y que la ausencia es un hecho del
    dominio;
  - que D-02 sigue intacto: siguen siendo 3 villanos, no 4 — `exp_kang` no es un villano
    nuevo sino una segunda tabla de cifras del mismo Kang.
No borrar ni reescribir el texto original de D-11: se añade debajo, igual que hizo 05-04.
  </action>
  <verify>
    <automated>npx vitest run --project engine</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project engine` termina con exit code 0, reporta `Test Files 12 passed (12)` y un total de tests estrictamente mayor que 239 (baseline medido en planificación)
    - `npm run test` termina con exit code 0 (suite completa del repo, no solo el proyecto engine)
    - `grep -c "expert" engine/__tests__/characters.test.ts` devuelve al menos 8
    - `grep -c "healthByPlayerCount" engine/__tests__/characters.test.ts` devuelve al menos 1 (el comentario del gate D-11 sigue nombrando qué prohíbe)
    - En `engine/__tests__/characters.test.ts` existe un test cuyo cuerpo contiene los literales `15`, `22` y `25` y los ids `kang`, `rhino` y `ultron` aparecen en el fichero: `grep -c "kang" engine/__tests__/characters.test.ts` devuelve al menos 1, e igual para `rhino` y `ultron`
    - `grep -c "'expert' in" engine/__tests__/characters.test.ts` devuelve al menos 1 (la ausencia se comprueba con `in`, no comparando contra undefined)
    - Estructura CR-02 intacta: `grep -n "readFileSync\|JSON.parse\|validateCharacterCatalogue(" engine/__tests__/characters.test.ts` no devuelve ninguna línea fuera del cuerpo de `readRawCatalogueText`, `loadValidatedCatalogue` o un `it(` — verificarlo leyendo las líneas que devuelva el grep y anotar la comprobación en el SUMMARY
    - Prueba de que el gate nuevo muerde: borrar temporalmente el bloque `expert` de la primera etapa de Kang en `content/marvel-characters.json`, ejecutar `npx vitest run --project engine engine/__tests__/characters.test.ts`, y confirmar que (a) el fichero se reporta como `failed`, (b) el test de fidelidad de Kang falla **individualmente y por su nombre**, (c) NO aparece `no tests` en la salida y el recuento de tests colectados sigue siendo el mismo que en verde. Restaurar el fichero después y confirmar `git diff --exit-code -- content/marvel-characters.json` en 0. Registrar la salida real en el SUMMARY
    - `grep -c "truth #8\|exp_kang" .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md` devuelve al menos 1
    - `grep -c "No reabrir esta disyuntiva" .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md` devuelve al menos 1 (el texto original de D-11 sigue presente, no se reescribió)
  </acceptance_criteria>
  <done>El gate D-11 acepta expert como única clave extra permitida y sigue prohibiendo cualquier tabla precomputada, dentro y fuera de expert; tres tests nuevos fijan las cifras 15/22/25 de Kang con sus banderas por etapa, la ausencia de expert en Rhino y Ultron, y la forma de todo expert presente; la estructura de acceso perezoso de CR-02 está intacta y comprobada con una mutación real que falla de forma nombrada sin colapsar la colección; 05-CONTEXT.md registra la ampliación de D-11 sin borrar el texto original.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API pública de MarvelCDB -> generador en la máquina del desarrollador | Respuesta JSON de un tercero, no autenticada, cruzando a un proceso que escribe un fichero versionado. Es la única frontera con datos no confiables de toda la fase |
| Generador -> `content/marvel-characters.json` committeado | Un fichero que entra en el repo público y viaja después dentro del bundle del cliente; lo que pase esta frontera es permanente y visible |
| Catálogo committeado -> mesa de juego (Fase 6/7) | Cifras que se narrarán en voz alta como instrucción de reglas; un número equivocado aquí es el fallo que `CLAUDE.md` §Constraints declara peor que no tener asistente |

Sin superficie de usuario final, sin autenticación, sin sesión, sin entrada de usuario y
sin ninguna red en tiempo de ejecución: el generador nunca corre en build ni en CI (D-06,
con gate propio en `catalogue-isolation.test.ts`).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Information Disclosure | `extractVillainStage` -> sub-objeto `expert` de `content/marvel-characters.json` | mitigate | El nivel de anidamiento nuevo se proyecta como lista blanca de tres campos (`health`, `healthPerHero`, `healthPerGroup`), asignados uno a uno; prohibido cualquier spread del `card` de la API (Task 2, criterio `grep -c "\.\.\.card"` = 0). Segunda barrera: `expert` es `z.strictObject` (Task 1, criterio de recuento = 5) — una clave `text`/`flavor`/`imagesrc` colada dentro lanza. Tercera barrera, independiente de Zod: la blocklist de 20 claves sobre el texto crudo sigue corriendo (criterio `grep -Ec` = 0 en Task 2) |
| T-05-06-02 | Tampering | filas `expertCode`/`expectedExpertSetCode` de `VILLAIN_STAGE_CARDS` | mitigate | Los códigos son texto teclelado a mano cuyo error se narraría en mesa como cifra de reglas. Puertas de la Task 2, aplicadas a la carta Experto por el mismo helper que a la estándar: `type_code === 'villain'`, `card_set_code` igual al `expectedExpertSetCode` declarado, `STAGE_MAP[card.stage]` igual al `stage` de la fila, `health` entero > 0, banderas booleanas; más dos puertas propias (los dos campos van juntos o no van; `expertCode` distinto del `code` estándar). Aborto antes de escribir nada (D-05). Cuarta barrera: las cifras quedan fijadas por test en la Task 3, así que una regeneración que las cambie pone CI en rojo en vez de deslizarse en un diff |
| T-05-06-03 | Tampering (integridad legal/de contenido) | `content/marvel-characters.json` como artefacto público | mitigate | La restricción legal de `CLAUDE.md` §Constraints (no reproducir cartas, arte ni textos con copyright) actúa como control de integridad de contenido: el bloque `expert` solo puede contener tres números/booleanos, comprobado por el criterio de claves exactas de la Task 2 y por el gate D-11 reescrito de la Task 3 |
| T-05-06-04 | Denial of Service | disponibilidad de marvelcdb.com | accept | Riesgo aceptado y ya cerrado por diseño: el script no corre en `build`, `generate`, `preview`, `dev`, `postinstall`, `test` ni CI, y el gate de aislamiento lo comprueba (criterio de la Task 2 que ejecuta `catalogue-isolation.test.ts`). Una caída de MarvelCDB retrasa una regeneración manual, nunca un despliegue ni una partida |
| T-05-06-05 | Repudiation | trazabilidad de un cambio de cifras | accept | D-10 (fichero sin marca temporal) desplaza la trazabilidad al commit de git, que es la fuente de autoridad. El gate de determinismo con `cmp` de la Task 2 mantiene esa propiedad comprobable en vez de afirmada |
| T-05-06-SC | Tampering (cadena de suministro) | instalaciones npm/pip/cargo | mitigate | **Este plan no instala ningún paquete**: no hay dependencias nuevas, ni de producción ni de desarrollo, así que no aplica el protocolo de legitimidad de paquetes ni hace falta ningún checkpoint humano bloqueante. Comprobado por el criterio `git diff --exit-code -- package.json package-lock.json` de la Task 2 |
</threat_model>

<verification>
Comprobaciones de fase, ejecutadas tras la última tarea:

1. `npm run test` termina con exit code 0 (suite completa del repo).
2. `npx vitest run --project engine` reporta `Test Files 12 passed (12)` y un total de
   tests estrictamente mayor que 239.
3. Las cifras del modo Experto de Kang están en el fichero committeado y fijadas por
   test: 15/22/25 con banderas true/false/true por etapa y per-group false en las tres.
4. Rhino y Ultron no llevan ninguna clave `expert`, comprobado con `'expert' in stage`.
5. `grep -Ec '"(text|real_text|flavor|traits|imagesrc|illustrator|url|octgn_id|generatedAt)"' content/marvel-characters.json`
   devuelve 0, y `grep -Ec "http|marvelcdb|api" content/marvel-characters.json` devuelve 0
   (CAT-04 y CAT-06 intactos con la forma nueva).
6. `grep -v "^\s*//" engine/catalogueSchema.ts | grep -c "z.strictObject("` devuelve 5 y
   `grep -c "\.default(" engine/catalogueSchema.ts` devuelve 0.
7. Determinismo D-10: dos ejecuciones consecutivas de `npm run catalogue:generate`
   producen ficheros byte a byte idénticos (`cmp` en 0).
8. El comentario de exclusión de `exp_kang` ya no existe en el generador
   (`grep -c "los atraparía si alguien"` y `grep -c "quedan FUERA a propósito"` en 0) y el
   procedimiento CAT-07 documenta cómo declarar un set Experto para un villano futuro.
9. Estructura CR-02 intacta y probada con una mutación real: el test de fidelidad de Kang
   falla individualmente por su nombre, sin `no tests` y sin caída del recuento de tests
   colectados; fichero restaurado y `git diff --exit-code` en 0.
10. `git diff --exit-code -- package.json package-lock.json` en 0 (cero dependencias
    nuevas).
11. Ningún fichero de `content/marvel-champions.json`, `app/` ni `engine/schema.ts` ha
    sido modificado: `git status --porcelain` solo lista los siete ficheros de
    `files_modified`.
</verification>

<success_criteria>
- El truth #8 de `05-VERIFICATION.md` puede darse por VERIFICADO: el catálogo committeado
  representa las cifras del modo Experto de Kang (15/22/25 con sus banderas por etapa) que
  `content/marvel-champions.json` ya instruye poner en mesa, y las representa en el
  contrato, no solo en prosa de research.
- CAT-02 pasa de ⚠️ PARCIAL a SATISFECHO: vida por etapa, con indicación explícita de si
  es por jugador o total, en los dos modos de dificultad.
- El contrato que la Fase 6 va a importar (`engine/types.ts`) declara `expert` como campo
  opcional y documenta qué significa su ausencia, de modo que ningún consumidor pueda
  asumir que `stages` es universal sin tropezar con el comentario ni con el tipo.
- Rhino y Ultron salen sin cifras de Experto inventadas, y un test lo garantiza.
- CAT-01, CAT-03, CAT-04, CAT-05, CAT-06 y CAT-07 siguen satisfechos: nada de lo cerrado
  en 05-01..05-05 se degrada, y en particular los cierres de CR-01 (`handSizeHero` /
  `handSizeAlterEgo`) y CR-02 (acceso perezoso en los gates) quedan intactos y
  recomprobados.
- Determinismo D-10 recomprobado con la forma nueva del fichero.
</success_criteria>

<output>
Create `.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-06-SUMMARY.md` when done.

El SUMMARY debe registrar, además de lo habitual: los seis valores Experto obtenidos de la
API (health y las dos banderas de cada etapa) frente a los esperados de este plan; el
resultado literal del `cmp` de determinismo; la salida real de la mutación que prueba que
el gate nuevo muerde (recuento de tests colectados y nombre del test que falló); y los
recuentos de tests antes y después.
</output>
