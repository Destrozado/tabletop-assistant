---
phase: 05-cat-logo-de-h-roes-y-villanos
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - engine/__tests__/characters.test.ts
  - engine/__tests__/catalogue-isolation.test.ts
autonomous: true
requirements: [CAT-04, CAT-05, CAT-06, CAT-07]
must_haves:
  truths:
    - "`npm run test` falla si content/marvel-characters.json está malformado o no cumple el esquema (CAT-05)"
    - "`npm run test` falla si el catálogo committeado contiene una clave con texto de carta, cita de sabor o referencia a arte de MarvelCDB (CAT-04)"
    - "El guardarraíl anti-copyright lee el string crudo del fichero, no el objeto ya validado por Zod, para ser estructuralmente capaz de fallar"
    - "Los gates comprueban invariantes de forma, nunca un recuento literal de 23 héroes o 3 villanos: comprar una caja no obliga a tocar ningún test (D-05/CAT-07)"
    - "`npm run test` falla si alguien cablea el script de generación en build, generate, postinstall, test o CI (D-06 heredado, CAT-06)"
    - "El catálogo es un fichero estático sin ninguna URL dentro: nada que pedir por red en ejecución (CAT-06)"
    - "El procedimiento de una fila de CAT-07 está escrito en el script y un gate comprueba que sigue ahí"
    - "Los gates no hacen ninguna petición de red ni lanzan procesos hijos: corren en CI sin acceso a marvelcdb.com"
  artifacts:
    - path: "engine/__tests__/characters.test.ts"
      provides: "Validación del catálogo real contra CharacterCatalogueSchema + guardarraíl anti-copyright sobre el crudo + invariantes de forma"
      min_lines: 90
    - path: "engine/__tests__/catalogue-isolation.test.ts"
      provides: "Gate de aislamiento: el script de generación nunca entra en build/CI, el catálogo no lleva URLs, el procedimiento CAT-07 sigue documentado"
      min_lines: 70
  key_links:
    - from: "engine/__tests__/characters.test.ts"
      to: "engine/catalogueSchema.ts"
      via: "import { validateCharacterCatalogue }"
      pattern: "from '\\.\\./catalogueSchema'"
    - from: "engine/__tests__/characters.test.ts"
      to: "content/marvel-characters.json"
      via: "readFileSync del crudo + JSON.parse"
      pattern: "marvel-characters\\.json"
    - from: "engine/__tests__/catalogue-isolation.test.ts"
      to: "package.json y .github/workflows/ci.yml"
      via: "readFileSync + aserción de ausencia"
      pattern: "ci\\.yml"
---

<objective>
Instalar los gates permanentes de CI que hacen del catálogo un artefacto de confianza:
validación del fichero real contra el esquema, guardarraíl anti-copyright sobre el
string crudo, invariantes de forma sin recuentos fijos, y el gate de aislamiento que
impide que el script de generación acabe cableado en el build o en CI.

Purpose: hasta aquí el catálogo es correcto porque el desarrollador lo miró. Este plan
lo convierte en correcto porque la build lo comprueba en cada push. Es lo que cierra
CAT-05, la segunda barrera de CAT-04, y la lectura honesta de CAT-06 en una fase que
todavía no tiene consumidores en `app/`.

Output: `engine/__tests__/characters.test.ts` y
`engine/__tests__/catalogue-isolation.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-RESEARCH.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-PATTERNS.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-01-SUMMARY.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-02-SUMMARY.md

<interfaces>
<!-- Superficie que estos gates consumen. Todo existe ya al llegar aquí. -->

De `engine/catalogueSchema.ts` (plan 05-01):
  export function validateCharacterCatalogue(json: unknown): CharacterCatalogue
  export const CharacterCatalogueSchema

De `engine/types.ts` (plan 05-01):
  CharacterCatalogue { gameId, heroes: CatalogueHero[], villains: CatalogueVillain[] }
  CatalogueHero { id, name, alterEgo, health, handSize }
  CatalogueVillain { id, name, stages: VillainStage[] }
  VillainStage { stage, health, healthPerHero, healthPerGroup }

De `content/marvel-characters.json` (plan 05-02): el catálogo real, 23 héroes y
3 villanos, escrito exclusivamente por `scripts/catalogue/fetch-marvelcdb.mjs`.

De `package.json` (plan 05-02): entrada `"catalogue:generate": "node scripts/catalogue/fetch-marvelcdb.mjs"`.

Configuración de test ya vigente (`vitest.config.ts`): el proyecto `engine` incluye
`engine/**/*.test.ts` con `environment: 'node'`. Los dos ficheros nuevos entran solos,
sin tocar la configuración.

Blocklist de claves de copyright, enumerada a partir de las claves reales que devuelve
la API de MarvelCDB (05-RESEARCH.md §"Code Examples"):
  text, real_text, flavor, traits, real_traits, back_text, back_flavor, back_name,
  imagesrc, backimagesrc, illustrator, octgn_id, url, meta, subname, boost,
  deck_requirements, deck_options, restrictions, errata
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate del catálogo real — esquema, forma y guardarraíl anti-copyright</name>
  <files>engine/__tests__/characters.test.ts</files>
  <read_first>
    - engine/__tests__/content.test.ts líneas 1-72 (patrón exacto a replicar: imports, `contentPath` con `fileURLToPath(new URL('../../content/...', import.meta.url))`, la separación entre el crudo `rawMarvelChampions` y el validado, el comentario CR-01 sobre `findRawStep` que explica por qué un gate de AUSENCIA debe leer el crudo, y el primer test "valida contra GameDefinitionSchema")
    - engine/catalogueSchema.ts (firma de `validateCharacterCatalogue` y las invariantes que ya cubre el esquema, para no duplicarlas aquí)
    - content/marvel-characters.json (el fichero real que este gate valida)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-PATTERNS.md §`engine/__tests__/characters.test.ts` (qué copiar del análogo y, sobre todo, qué NO copiar: el test de recuento literal)
  </read_first>
  <action>
Crear `engine/__tests__/characters.test.ts` como espejo estructural de
`engine/__tests__/content.test.ts`, pero sin `expand` ni `resolveText` (aquí no hay
pasos ni dificultad).

Cabecera de comentario: este fichero es SOLO LECTOR — nunca hace peticiones de red,
nunca lanza procesos hijos, nunca escribe en disco; el script de generación no corre
en CI (D-06 heredado) y este gate solo lee ficheros ya versionados.

Carga, siguiendo el análogo: `contentPath` construido con
`fileURLToPath(new URL('../../content/marvel-characters.json', import.meta.url))`,
`rawText` con `readFileSync(contentPath, 'utf-8')`, `rawCatalogue` con
`JSON.parse(rawText)`, y `catalogue` con `validateCharacterCatalogue(rawCatalogue)`.

Tests exigidos:

1. `describe('content/marvel-characters.json')` con el test de entrada:
   `expect(() => validateCharacterCatalogue(rawCatalogue)).not.toThrow()` (CAT-05).

2. Guardarraíl anti-copyright (CAT-04), con un comentario que explique —igual que el
   comentario CR-01 de `findRawStep` en el análogo— que se opera sobre el **string
   crudo** y no sobre el objeto validado, porque un gate que afirma la AUSENCIA de una
   clave sobre el objeto ya validado sería estructuralmente incapaz de fallar. Declarar
   una constante `FORBIDDEN_KEYS` con las 20 claves de la blocklist del bloque
   `<interfaces>`, cada una en su forma de clave JSON (con comillas y dos puntos, por
   ejemplo la cadena que representa `"text":`), y un test que recorra la lista
   comprobando `expect(rawText).not.toContain(key)` con un mensaje que nombre la clave.
   Añadir un segundo test que demuestre que **el gate muerde**: aplicar la misma
   función de detección a un string sintético que sí contiene una de esas claves y
   comprobar que la detecta. Extraer la comprobación a una función pura local para que
   el gate real y el test de "el gate muerde" compartan lógica, como hace
   `engine/__tests__/voice-drift.test.ts` con `findStaleAudio`.

3. Invariantes de forma sobre héroes, recorriendo el array sin enumerar ids a mano:
   todo héroe tiene `name` y `alterEgo` no vacíos tras `trim()`; `health` y `handSize`
   son enteros mayores que 0; los `id` son únicos; `heroes` no está vacío.

4. Invariantes de forma sobre villanos: todo villano tiene al menos una etapa; las
   etapas son consecutivas desde 1 y en orden; toda etapa tiene `health` entero mayor
   que 0 y `healthPerHero` / `healthPerGroup` booleanos; los `id` son únicos;
   `villains` no está vacío.

5. Invariante D-11 explícita, con nombre de test que la nombre: ninguna etapa lleva
   una tabla precomputada por número de jugadores — comprobar que `Object.keys()` de
   cada etapa es exactamente el conjunto `stage`, `health`, `healthPerHero`,
   `healthPerGroup`, ni una clave más ni una menos.

6. Un test explícitamente titulado en torno a CAT-07 que documente la ausencia
   deliberada de recuento: comprobar que el fichero **no** impone 23 ni 3 en ningún
   sitio de este gate. Materializarlo como un comentario de bloque más un test que
   afirme únicamente `heroes.length > 0` y `villains.length > 0`.

**Prohibido en este fichero**: cualquier aserción del tipo
`expect(catalogue.heroes).toHaveLength(23)` o `toHaveLength(3)`, cualquier lista
literal de nombres de héroe o de villano, y cualquier aserción sobre la ausencia de
Klaw. D-01/D-02/D-05 son decisiones del script, no del test: endurecerlas aquí
convertiría "comprar una caja" en tocar el test, contra CAT-07.
  </action>
  <verify>
    <automated>npx vitest run --project engine engine/__tests__/characters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project engine engine/__tests__/characters.test.ts` termina con exit code 0
    - `grep -Ec "toHaveLength\(23\)|toHaveLength\(3\)|length === 23|length === 3" engine/__tests__/characters.test.ts` devuelve 0
    - `grep -Ec "Klaw|Spider-Man|Rhino|Ultron|Kang" engine/__tests__/characters.test.ts` devuelve 0
    - `grep -c "readFileSync" engine/__tests__/characters.test.ts` devuelve al menos 1 y `grep -c "fetch(" engine/__tests__/characters.test.ts` devuelve 0
    - `grep -c "FORBIDDEN_KEYS" engine/__tests__/characters.test.ts` devuelve al menos 2 (declaración + uso), y el array declara 20 claves
    - `grep -c "not.toContain" engine/__tests__/characters.test.ts` devuelve al menos 1
    - Mutación de comprobación (ejecutar y revertir): insertar temporalmente la subcadena de clave `"flavor":` dentro de `content/marvel-characters.json` hace que `npx vitest run --project engine engine/__tests__/characters.test.ts` termine con exit code distinto de 0; tras `git checkout -- content/marvel-characters.json` vuelve a 0
    - Mutación de comprobación (ejecutar y revertir): cambiar a mano el `handSize` de un héroe a `0` en `content/marvel-characters.json` hace fallar el gate; tras revertir vuelve a pasar
    - `npm run test` termina con exit code 0
  </acceptance_criteria>
  <done>El catálogo real se valida contra el esquema en cada `npm run test`, el guardarraíl anti-copyright opera sobre el crudo y está demostrado que muerde, las invariantes de forma cubren héroes y villanos, y ningún test fija un recuento de personajes.</done>
</task>

<task type="auto">
  <name>Task 2: Gate de aislamiento — el script nunca entra en build ni en CI, y el catálogo no pide red</name>
  <files>engine/__tests__/catalogue-isolation.test.ts</files>
  <read_first>
    - engine/__tests__/voice-drift.test.ts líneas 1-60 (patrón del gate SOLO LECTOR: cabecera que declara qué NO hace, rutas resueltas con `fileURLToPath(new URL(...))`, helpers puros compartidos entre el gate real y el test de "el gate muerde")
    - package.json (bloque `scripts` completo, con la entrada `catalogue:generate` ya añadida por el plan 05-02)
    - .github/workflows/ci.yml (los pasos que CI ejecuta hoy: `npm ci`, `npm run test`, Playwright — ninguno debe invocar el script de catálogo)
    - scripts/catalogue/fetch-marvelcdb.mjs (la cabecera, para comprobar el marcador del procedimiento CAT-07)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md §"Established Patterns" (D-06 heredado de la Fase 3.1: los scripts de generación nunca corren desde el build de despliegue)
  </read_first>
  <action>
Crear `engine/__tests__/catalogue-isolation.test.ts`, un gate SOLO LECTOR que
convierte en comprobable lo que hoy solo está escrito en comentarios.

Cabecera de comentario: qué protege este fichero (D-06 heredado de la Fase 3.1 +
CAT-06 + CAT-03/CAT-07) y qué no hace nunca (red, procesos hijos, escritura en disco).

Rutas, resueltas con `fileURLToPath(new URL('../../<ruta>', import.meta.url))`:
`package.json`, `.github/workflows/ci.yml`, `scripts/catalogue/fetch-marvelcdb.mjs`,
`content/marvel-characters.json`.

Definir una función pura local `referencesCatalogueScript(text)` que devuelva true si
el texto contiene `catalogue:generate` o `fetch-marvelcdb`. El gate real y el test de
"el gate muerde" deben usar esa misma función, siguiendo el patrón de
`voice-drift.test.ts`.

Tests exigidos:

1. D-06 heredado: leer `package.json`, y para cada una de las entradas `build`,
   `generate`, `preview`, `dev`, `postinstall` y `test`, comprobar que
   `referencesCatalogueScript()` devuelve false. Mensaje de fallo que nombre la
   entrada culpable. Motivo escrito en el test: meter una llamada de red al build
   rompería "sin backend" y haría el despliegue dependiente de que marvelcdb.com esté
   levantado.
2. D-06 heredado en CI: comprobar que `.github/workflows/ci.yml` existe y que
   `referencesCatalogueScript()` sobre su contenido devuelve false. CI corre en
   ubuntu-latest y no debe depender de una API de terceros para pasar.
3. El gate muerde: `referencesCatalogueScript('npm run catalogue:generate && nuxt build')`
   devuelve true, y `referencesCatalogueScript('nuxt build')` devuelve false.
4. CAT-03 — punto de entrada documentado: `package.json` declara
   `scripts["catalogue:generate"]` con el valor exacto
   `node scripts/catalogue/fetch-marvelcdb.mjs`, y el fichero de script existe en esa
   ruta.
5. CAT-07 — procedimiento de una fila documentado: el contenido de
   `scripts/catalogue/fetch-marvelcdb.mjs` contiene el marcador de sección
   `CÓMO AÑADIR UN HÉROE O VILLANO NUEVO` y menciona `npm run catalogue:generate`.
6. CAT-06 — el catálogo no pide nada por red: el string crudo de
   `content/marvel-characters.json` no contiene la subcadena `http`, ni
   `marvelcdb`, ni `api`. Comentario que explique la lectura: el catálogo viaja como
   fichero estático dentro del bundle y no lleva ninguna referencia remota que
   resolver en ejecución; su importación estática llega en la Fase 6.
7. CAT-06 / T-01-19 — el script de generación no es alcanzable desde la app: recorrer
   los ficheros de `app/` (lectura recursiva del directorio, sin procesos hijos) y
   comprobar que ninguno contiene `fetch-marvelcdb`, `marvelcdb.com` ni
   `catalogue:generate`. Si `app/` creciera mucho, limitar la lectura a ficheros con
   extensión `.vue`, `.ts` y `.js`.

Ningún test de este fichero debe hacer red, escribir en disco ni lanzar procesos hijos.
  </action>
  <verify>
    <automated>npx vitest run --project engine engine/__tests__/catalogue-isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project engine engine/__tests__/catalogue-isolation.test.ts` termina con exit code 0
    - `grep -c "referencesCatalogueScript" engine/__tests__/catalogue-isolation.test.ts` devuelve al menos 4 (declaración + gate de package.json + gate de ci.yml + test de "el gate muerde")
    - `grep -Ec "execSync|execFileSync|spawn|fetch\(" engine/__tests__/catalogue-isolation.test.ts` devuelve 0
    - `grep -Ec "writeFileSync|mkdirSync|unlinkSync" engine/__tests__/catalogue-isolation.test.ts` devuelve 0
    - `grep -c "ci.yml" engine/__tests__/catalogue-isolation.test.ts` devuelve al menos 1
    - `grep -c "CÓMO AÑADIR UN HÉROE" engine/__tests__/catalogue-isolation.test.ts` devuelve al menos 1
    - Mutación de comprobación (ejecutar y revertir): añadir temporalmente `npm run catalogue:generate && ` al principio del script `build` de `package.json` hace que el gate termine con exit code distinto de 0; tras `git checkout -- package.json` vuelve a 0
    - Mutación de comprobación (ejecutar y revertir): añadir temporalmente una línea con `run: npm run catalogue:generate` a `.github/workflows/ci.yml` hace fallar el gate; tras revertir vuelve a pasar
    - `npm run test` termina con exit code 0 con la suite completa
  </acceptance_criteria>
  <done>Un gate automatizado impide que el script de generación entre en build, generate, postinstall, test o CI; comprueba que el punto de entrada y el procedimiento de una fila siguen documentados; y afirma que el catálogo committeado no contiene ninguna referencia remota.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo público ↔ mundo | El catálogo committeado es visible; los gates de CI son la barrera que detecta una filtración de texto o arte antes de que se acumule |
| repo ↔ build de despliegue (Vercel) y CI | Un cambio futuro que cablee el script en `build` o en el workflow convertiría una app "sin backend" en una dependiente de marvelcdb.com en tiempo de build |
| tablet ↔ red | CAT-06: en ejecución no puede haber ninguna petición para obtener el catálogo, ni con wifi ni sin ella |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `content/marvel-characters.json` | mitigate | Segunda barrera independiente del esquema: `FORBIDDEN_KEYS` con las 20 claves reales de la API, comprobadas contra el **string crudo** del fichero (Task 1). Se demuestra que muerde con un test de gate sobre string sintético y con una mutación real revertida |
| T-05-03 | Tampering | `package.json` y `.github/workflows/ci.yml` | mitigate | Gate automatizado que falla `npm run test` si `catalogue:generate` o `fetch-marvelcdb` aparecen en `build`, `generate`, `preview`, `dev`, `postinstall`, `test` o en el workflow de CI (Task 2), con mutación revertida como prueba de que muerde |
| T-05-07 | Denial of Service | build de despliegue dependiente de marvelcdb.com | mitigate | Mismo gate de la Task 2: si la API de un tercero se cayera, el despliegue no puede verse afectado porque nada del build la consulta |
| T-05-08 | Tampering | catálogo alterado a mano tras generarlo | mitigate | El gate de la Task 1 valida el fichero real contra el esquema estricto, incluida la invariante DC-02 (`id === slug(name)`) del plan 05-01, que hace fallar CI ante una edición manual incoherente |
| T-05-04 | Information Disclosure | zod en el bundle del navegador | mitigate | Los dos ficheros nuevos viven en `engine/__tests__/` y solo corren bajo Vitest; el gate de la Task 2 comprueba además que ningún fichero de `app/` referencia el script ni la API |
| T-05-SC | Tampering | instalaciones npm/pip/cargo | accept | Esta fase no instala ningún paquete nuevo; `vitest` y `zod` ya son devDependencies verificadas. El gate de legitimidad se aplicaría si alguna tarea añadiera uno |
</threat_model>

<verification>
1. `npm run test` termina con exit code 0 e incluye los ficheros `characters.test.ts` y `catalogue-isolation.test.ts` en su salida.
2. Cada uno de los cuatro ensayos de mutación descritos en los criterios de aceptación hace fallar la suite, y tras `git checkout --` del fichero mutado la suite vuelve a verde. Registrar los cuatro resultados en el SUMMARY.
3. `grep -Ec "execSync|spawn|fetch\(" engine/__tests__/characters.test.ts engine/__tests__/catalogue-isolation.test.ts` devuelve 0 en ambos: los gates corren en CI sin red.
4. `git status --porcelain` lista exactamente los dos ficheros de test nuevos; nada bajo `app/`, nada en `nuxt.config.ts`, nada en `content/marvel-champions.json`.
5. `npx vitest run --project engine` sigue en verde con la suite completa del motor.
</verification>

<success_criteria>
- CAT-05 satisfecho: un catálogo malformado hace fallar `npm run test`, y por tanto la CI.
- CAT-04 satisfecho con doble barrera: `z.strictObject` en el esquema (plan 05-01) y la blocklist sobre el crudo aquí, con prueba de que la segunda muerde por sí sola.
- CAT-06 satisfecho en lo que esta fase puede satisfacer: el catálogo es un fichero estático sin ninguna referencia remota, y hay un gate que impide que su generación se cuele en build o CI.
- CAT-07 satisfecho de forma comprobable: ningún test fija un recuento de personajes, y el procedimiento de una fila sigue documentado en el script, comprobado por un gate.
</success_criteria>

<output>
Create `.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-03-SUMMARY.md` when done
</output>
