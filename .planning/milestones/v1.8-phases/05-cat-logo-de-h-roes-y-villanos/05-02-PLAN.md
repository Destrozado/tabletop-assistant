---
phase: 05-cat-logo-de-h-roes-y-villanos
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/catalogue/fetch-marvelcdb.mjs
  - package.json
  - content/marvel-characters.json
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-07]
must_haves:
  truths:
    - "Ejecutar `npm run catalogue:generate` regenera content/marvel-characters.json desde la API pública de MarvelCDB (CAT-03)"
    - "El catálogo committeado contiene exactamente los 23 héroes dictados en D-03 y los 3 villanos de D-02, con Klaw ausente a propósito (D-01/D-02)"
    - "Qué entra se lee de un vistazo: dos listas literales de códigos de carta al principio del script, una fila por héroe (D-04/CAT-07)"
    - "Si un código declarado falla —HTTP no-ok, nombre distinto del esperado, tipo de carta equivocado, conjunto equivocado, etapa no mapeable, campo obligatorio ausente— el script aborta sin escribir absolutamente nada (D-05/D-06)"
    - "El script proyecta campo a campo una lista blanca; ninguna clave con texto de carta, cita de sabor o referencia a arte entra al fichero (CAT-04)"
    - "alterEgo, health y handSize salen siempre de la carta de alter ego (linked_card), nunca del lado héroe (D-09)"
    - "La vida de villano se guarda por etapa como base + banderas, sin multiplicar por jugadores (D-11)"
    - "Re-ejecutar el script sin cambios en MarvelCDB deja `git diff` vacío: no hay generatedAt ni ninguna marca temporal (D-10)"
    - "Añadir un héroe cuando se compre una caja es añadir una fila a HERO_CARDS y re-ejecutar, y ese procedimiento está escrito en el propio script (CAT-07/CAT-03)"
  artifacts:
    - path: "scripts/catalogue/fetch-marvelcdb.mjs"
      provides: "Único escritor de content/marvel-characters.json; listas literales de 32 códigos, puertas de aborto, proyección de lista blanca y escritura determinista"
      min_lines: 150
    - path: "content/marvel-characters.json"
      provides: "Catálogo committeado: 23 héroes + 3 villanos con 3 etapas cada uno"
      contains: "\"gameId\": \"marvel-champions\""
    - path: "package.json"
      provides: "Entrada npm catalogue:generate"
      contains: "catalogue:generate"
  key_links:
    - from: "package.json"
      to: "scripts/catalogue/fetch-marvelcdb.mjs"
      via: "script npm catalogue:generate"
      pattern: "node scripts/catalogue/fetch-marvelcdb.mjs"
    - from: "scripts/catalogue/fetch-marvelcdb.mjs"
      to: "https://marvelcdb.com/api/public/card/{code}.json"
      via: "fetch global de Node, sin retry"
      pattern: "marvelcdb\\.com/api/public"
    - from: "scripts/catalogue/fetch-marvelcdb.mjs"
      to: "content/marvel-characters.json"
      via: "un único writeFileSync al final, todo o nada"
      pattern: "writeFileSync"
---

<objective>
Escribir el script de generación del catálogo y producir con él el fichero
`content/marvel-characters.json` committeado.

Purpose: es el corazón de la fase — CAT-03 (regenerable), CAT-04 (lista blanca),
CAT-07 (una fila por héroe) y los datos de CAT-01/CAT-02 salen todos de aquí. El
plan 05-01 ya fijó la forma exacta que este script debe producir; este plan la
rellena con datos reales verificados y demuestra que la puerta de aborto muerde.

Output: `scripts/catalogue/fetch-marvelcdb.mjs`, entrada `catalogue:generate` en
`package.json`, y `content/marvel-characters.json` generado y committeado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-RESEARCH.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-PATTERNS.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-01-SUMMARY.md

<interfaces>
<!-- Forma de salida fijada por el plan 05-01. El script DEBE producir exactamente
     esto; cualquier desviación la caza el gate del plan 05-03. -->

Raíz: `gameId` (string), `heroes` (array), `villains` (array). Nada más.
Héroe: `id`, `name`, `alterEgo`, `health`, `handSize`. Nada más.
Villano: `id`, `name`, `stages`. Nada más.
Etapa: `stage`, `health`, `healthPerHero`, `healthPerGroup`. Nada más.

`id` = slug kebab-case del `name` (minúsculas, todo lo que no sea `[a-z0-9]` pasa a
`-`, guiones de los extremos recortados). El esquema `engine/catalogueSchema.ts`
comprueba esa igualdad (DC-02), así que la función de slug del script debe coincidir
exactamente con `slugifyCharacterName()` de ese fichero.

Orden de `heroes` = orden literal de `HERO_CARDS`. Orden de `villains` = orden de
primera aparición en `VILLAIN_STAGE_CARDS`. Orden de `stages` = 1, 2, 3.
Formato de escritura: `JSON.stringify(catalogue, null, 2)` + un `\n` final.
Sin `generatedAt` ni ningún otro campo temporal (D-10).
</interfaces>

<data_reference>
<!-- Los 32 códigos de carta, verificados en vivo contra marvelcdb.com el 2026-09-07
     (05-RESEARCH.md). Se transcriben aquí para que el ejecutor no tenga que
     re-resolverlos. Los valores de health/handSize/alterEgo son la referencia de
     contraste del muestreo de la Task 2, NO se escriben a mano en el script. -->

HERO_CARDS — 23 filas, en este orden exacto (los 5 del Core Set primero, luego los
18 de packs sueltos en el orden dictado por D-03):

| # | code | expectedName | alter ego esperado | health | handSize |
|---|------|--------------|--------------------|--------|----------|
| 1 | 01001a | Spider-Man | Peter Parker | 10 | 6 |
| 2 | 01010a | Captain Marvel | Carol Danvers | 12 | 6 |
| 3 | 01019a | She-Hulk | Jennifer Walters | 15 | 6 |
| 4 | 01029a | Iron Man | Tony Stark | 9 | 6 |
| 5 | 01040a | Black Panther | T'Challa | 11 | 6 |
| 6 | 03001a | Captain America | Steve Rogers | 11 | 6 |
| 7 | 05001a | Ms. Marvel | Kamala Khan | 10 | 6 |
| 8 | 06001a | Thor | Odinson | 14 | 5 |
| 9 | 08001a | Black Widow | Natasha Romanoff | 9 | 6 |
| 10 | 09001a | Doctor Strange | Stephen Strange | 10 | 6 |
| 11 | 10001a | Hulk | Bruce Banner | 18 | 5 |
| 12 | 12001a | Ant-Man | Scott Lang | 12 | 6 |
| 13 | 13001a | Wasp | Nadia Van Dyne | 11 | 6 |
| 14 | 14001a | Quicksilver | Pietro Maximoff | 9 | 6 |
| 15 | 15001a | Scarlet Witch | Wanda Maximoff | 10 | 6 |
| 16 | 19001a | Drax | Drax | 14 | 6 |
| 17 | 25001a | Valkyrie | Brunnhilde | 12 | 6 |
| 18 | 26001a | Vision | Vision | 11 | 5 |
| 19 | 28001a | Nova | Sam Alexander | 10 | 6 |
| 20 | 36001a | Storm | Ororo Munroe | 10 | 6 |
| 21 | 44001a | Deadpool | Wade Wilson | 9 | 6 |
| 22 | 46001a | Iceman | Bobby Drake | 11 | 6 |
| 23 | 47001a | Jubilee | Jubilation Lee | 9 | 6 |

Trampas confirmadas sobre esta tabla:
- `12001c` y `13001c` (formas alternativas Giant-Man / Wasp, sin `linked_card`) NO
  entran: la lista declara `12001a` y `13001a`. No barrer packs.
- Drax y Vision tienen el mismo `name` en héroe y alter ego. Es dato real de la API,
  no un fallo de extracción; el script no debe avisar ni corregir nada.
- `handSize` NO es universalmente 6 (Thor, Hulk y Vision valen 5): leer siempre el
  valor real de `linked_card.hand_size`, nunca asumir una constante.

VILLAIN_STAGE_CARDS — 9 filas, en este orden exacto:

| # | villainName | code | stage declarada | card_set_code esperado | health | healthPerHero | healthPerGroup |
|---|-------------|------|-----------------|------------------------|--------|---------------|----------------|
| 1 | Rhino | 01094 | 1 | (comprobar en la primera ejecución) | 14 | true | false |
| 2 | Rhino | 01095 | 2 | (idem) | 15 | true | false |
| 3 | Rhino | 01096 | 3 | (idem) | 16 | true | false |
| 4 | Kang | 11001 | 1 | kang | 12 | true | false |
| 5 | Kang | 11002 | 2 | kang | 18 | **false** | false |
| 6 | Kang | 11006 | 3 | kang | 20 | true | false |
| 7 | Ultron | 01134 | 1 | (comprobar en la primera ejecución) | 17 | true | false |
| 8 | Ultron | 01135 | 2 | (idem) | 22 | true | false |
| 9 | Ultron | 01136 | 3 | (idem) | 27 | true | false |

Notas confirmadas sobre esta tabla:
- Klaw (Core Set) NO entra: D-02, exclusión deliberada del usuario.
- La etapa II de Kang tiene cuatro alternativas narrativas (`11002` Immortus,
  `11003` Iron Lad, `11004` Rama-Tut, `11005` Scarlet Centurion) con `health` y
  banderas IDÉNTICOS (18/false/false). Se declara `11002` como representante y el
  motivo se documenta en el propio script.
- Los códigos de `card_set_code: exp_kang` (`11034`, `11035`, `11039`, salud
  15/22/25, modo Experto) NO entran. Ese es el error que la puerta de conjunto
  esperado existe para atrapar.
- `healthPerHero` cambia dentro del mismo villano (Kang I true, II false, III true):
  confirma que la bandera vive en la etapa, no en el villano.
- MarvelCDB sirve `stage` como numeral romano en string (`"I"`, `"II"`, `"III"`) en
  las cartas `type_code: villain`. En el mismo pack `toafk` las cartas
  `type_code: main_scheme` usan un formato arábigo distinto (`"1"`, `"1A"`) — por eso
  el script solo toca los 9 códigos declarados y nunca recorre packs.
- Un código inexistente devuelve **HTTP 500**, no 404. `!response.ok` ya lo captura.
</data_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir scripts/catalogue/fetch-marvelcdb.mjs y registrar catalogue:generate</name>
  <files>scripts/catalogue/fetch-marvelcdb.mjs, package.json</files>
  <read_first>
    - scripts/voice/generate.mjs (fichero completo: es el análogo exacto — cabecera larga con el bloque D-06 "nunca desde build/generate/CI/Vercel", bloque de constantes declaradas arriba, `synthesize()` como forma del fetch con manejo de error, `saveManifest()` como forma de escritura de JSON, y el `catch` final con `process.exit(1)`)
    - engine/catalogueSchema.ts (creado en 05-01: la función `slugifyCharacterName()` que este script debe replicar carácter a carácter, y la forma exacta de cada objeto)
    - package.json (bloque `scripts`: convención `<dominio>:<verbo>` de `voice:generate`, `voice:probe`, `icons:generate`)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-PATTERNS.md §`scripts/catalogue/fetch-marvelcdb.mjs` (qué copiar del análogo y qué NO copiar)
  </read_first>
  <action>
Crear el directorio `scripts/catalogue/` y dentro el fichero `fetch-marvelcdb.mjs`
con shebang `#!/usr/bin/env node`, siguiendo la estructura de
`scripts/voice/generate.mjs` pero **más simple** (sin `execFileSync`, sin módulos
hermanos, sin modos ni flags de línea de comandos, sin `RETRY_BACKOFFS_MS`).

**Cabecera de comentario**, tan explícita como la del análogo, con estos bloques:
- Qué es: CLI invocado A MANO por el desarrollador para regenerar
  `content/marvel-characters.json` desde la API pública de MarvelCDB.
- D-06 heredado de la Fase 3.1: este script NUNCA se invoca desde `build`,
  `generate`, `postinstall` ni CI ni Vercel. Meter una llamada de red al build
  rompería "sin backend" (CLAUDE.md §Constraints).
- D-09: este fichero es el ÚNICO escritor de `content/marvel-characters.json`. Nadie
  edita ese JSON a mano, nunca. Un número equivocado significa que la regla de
  extracción de aquí está mal.
- D-10: la salida es determinista pura — sin `generatedAt` ni ninguna marca temporal.
  Re-ejecutar sin cambios en MarvelCDB debe dejar `git diff` vacío.
- CAT-04: la proyección es una lista blanca campo a campo. Prohibido `{ ...card }`:
  arrastraría `text`, `real_text`, `flavor`, `traits`, `imagesrc`, `illustrator`,
  `url` y `octgn_id` al repo público.
- Un bloque titulado **CÓMO AÑADIR UN HÉROE O VILLANO NUEVO (CAT-07)** con el
  procedimiento numerado literal: (1) añadir una fila a `HERO_CARDS` (o las filas de
  etapa a `VILLAIN_STAGE_CARDS`) con su `code` y su `expectedName`; (2) ejecutar
  `npm run catalogue:generate`; (3) revisar el diff de
  `content/marvel-characters.json`; (4) commitear script y JSON juntos. Añadir que el
  `code` se encuentra en la URL de la carta en marvelcdb.com y que para héroes es
  siempre el sufijo `a` (lado héroe), nunca el `b` (alter ego) ni el `c` (formas
  alternativas sin `linked_card`).
- Endpoints usados, escritos literalmente, como sustituto de la marca de versión que
  D-10 descarta: `GET https://marvelcdb.com/api/public/card/{code}.json`.

**Imports:** solo `writeFileSync` de `node:fs`. Nada más.

**Constantes al principio, antes de cualquier función** (mismo sitio que el bloque de
constantes del análogo):
- `API_BASE = 'https://marvelcdb.com/api/public'`
- `CATALOGUE_PATH = 'content/marvel-characters.json'`
- `GAME_ID = 'marvel-champions'`
- `STAGE_MAP = { I: 1, II: 2, III: 3 }`
- `HERO_CARDS`: las 23 filas `{ code, expectedName }` de la tabla del
  `<data_reference>`, en ese orden exacto. Comentarios en línea sobre `12001a` y
  `13001a` (no usar `12001c`/`13001c`).
- `VILLAIN_STAGE_CARDS`: las 9 filas `{ villainName, code, stage, expectedSetCode }`
  de la tabla del `<data_reference>`, en ese orden exacto. Comentario sobre por qué
  se toma `11002` entre las cuatro alternativas de la etapa II de Kang, y comentario
  de que los códigos `exp_kang` (`11034`/`11035`/`11039`) quedan fuera a propósito.
  Para `expectedSetCode` de Rhino y Ultron: dejar el valor que la primera ejecución
  revele (la Task 2 lo fija con el dato real); para Kang el valor es `kang`.
- Comentario explícito de que NO existe `RETRY_BACKOFFS_MS` aquí, con el motivo:
  MarvelCDB no tiene cuota ni 429 documentado, y un 500 significa "código inválido",
  que es información para abortar, no ruido transitorio (a diferencia de Gemini en
  `scripts/voice/generate.mjs`).

**Funciones:**

- `slugify(name)` — copia exacta de `slugifyCharacterName()` de
  `engine/catalogueSchema.ts`, con comentario cruzado que nombre ese fichero y avise
  de que una deriva entre ambas hace fallar CI a propósito (DC-02 del plan 05-01).

- `fetchCard(code)` — `await fetch(`${API_BASE}/card/${code}.json`)`, sin retry. Si
  `!response.ok`, lanza un `Error` cuyo mensaje incluya el `code`, el
  `response.status` y los primeros 200 caracteres del cuerpo. Comentario: MarvelCDB
  devuelve 500 (no 404) para un código inexistente y `!response.ok` ya lo captura.

- `extractHero({ code, expectedName })` — puertas de aborto antes de proyectar, cada
  una lanzando un `Error` con un mensaje que nombre el `code` y qué se esperaba:
  (a) `card.name !== expectedName`; (b) `card.type_code !== 'hero'`;
  (c) `card.linked_card` ausente; (d) `linked_card.health` o `linked_card.hand_size`
  no son enteros positivos. Devuelve el objeto proyectado campo a campo:
  `id` = `slugify(card.name)`, `name` = `card.name`,
  `alterEgo` = `card.linked_card.name`, `health` = `card.linked_card.health`,
  `handSize` = `card.linked_card.hand_size`. Comentario obligatorio: `health` y
  `handSize` se toman del `linked_card` como fuente única declarada — el `hand_size`
  del lado héroe es un modificador de habilidad, universal en las 23 cartas, no el
  tamaño de mano real (D-09).

- `extractVillainStage({ villainName, code, stage, expectedSetCode })` — puertas de
  aborto: (a) `card.type_code !== 'villain'`; (b) `card.card_set_code !==
  expectedSetCode` (esta es la que atrapa un código de `exp_kang` colado por error);
  (c) `STAGE_MAP[card.stage]` indefinido — mensaje que diga qué valor de `stage`
  devolvió la API; (d) `STAGE_MAP[card.stage] !== stage`; (e) `card.health` no es
  entero positivo o `card.health_per_hero` / `card.health_per_group` no son
  booleanos. Devuelve `{ stage, health: card.health, healthPerHero:
  card.health_per_hero, healthPerGroup: card.health_per_group }`. Comentario: el
  `name` del villano NUNCA sale de esta carta (la etapa II de Kang varía entre cuatro
  nombres narrativos); sale de `villainName`, declarado arriba (D-02).

- `writeCatalogue(heroes, villains)` — construye
  `{ gameId: GAME_ID, heroes, villains }` y hace UN solo
  `writeFileSync(CATALOGUE_PATH, JSON.stringify(catalogue, null, 2) + '\n')`.
  Sin ordenación alfabética (el orden es el declarado, D-04) y sin ningún campo de
  fecha (D-10).

- `main()` — resuelve los 32 códigos **en memoria completa antes de escribir nada**:
  recorre `HERO_CARDS` en orden acumulando héroes, luego `VILLAIN_STAGE_CARDS` en
  orden agrupando etapas por `villainName` (preservando el orden de primera
  aparición del villano y añadiendo `id: slugify(villainName)` y `name: villainName`),
  y solo si los 32 tuvieron éxito llama a `writeCatalogue`. Al final imprime por
  consola cuántos héroes y cuántos villanos se escribieron y la ruta del fichero.
  Diferencia deliberada respecto al análogo, que escribe incrementalmente: aquí es
  todo o nada (D-05/D-09) — ninguna escritura parcial de
  `content/marvel-characters.json` puede ocurrir jamás.

- Envoltorio final: `main().catch(error => { console.error(...); console.error('No se
  ha escrito nada en content/marvel-characters.json (D-05).'); process.exit(1) })`,
  siguiendo la forma del `catch` + `process.exit(1)` del análogo.

En `package.json`, añadir dentro de `scripts`, junto a las entradas `voice:*` e
`icons:generate` y siguiendo la misma convención:
`"catalogue:generate": "node scripts/catalogue/fetch-marvelcdb.mjs"`.
No tocar ninguna otra entrada; en particular `build`, `generate`, `postinstall` y
`test` deben quedar exactamente como están (D-06 heredado).
  </action>
  <verify>
    <automated>node --check scripts/catalogue/fetch-marvelcdb.mjs && node -e "const p=require('./package.json');if(p.scripts['catalogue:generate']!=='node scripts/catalogue/fetch-marvelcdb.mjs')process.exit(1);for(const k of ['build','generate','postinstall','test'])if(/catalogue/.test(p.scripts[k]))process.exit(1);console.log('package.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/catalogue/fetch-marvelcdb.mjs` termina con exit code 0
    - `grep -c "code: '" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 32 (23 filas de héroe + 9 de etapa de villano)
    - `grep -c "expectedName" scripts/catalogue/fetch-marvelcdb.mjs` devuelve al menos 24 (23 filas + al menos un uso en la puerta de aborto)
    - `grep -c "RETRY_BACKOFFS_MS" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0
    - `grep -c "generatedAt" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0
    - `grep -c "\.\.\.card" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0 (sin spread del objeto crudo — CAT-04)
    - `grep -c "writeFileSync" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 1
    - `grep -c "linked_card" scripts/catalogue/fetch-marvelcdb.mjs` devuelve al menos 4 (name, health, hand_size y la puerta de presencia)
    - `grep -c "11002" scripts/catalogue/fetch-marvelcdb.mjs` devuelve al menos 1 y `grep -Ec "11003|11004|11005|11034|11035|11039|klaw|Klaw" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0 fuera de las líneas de comentario que explican por qué quedan fuera
    - `grep -vE '^\s*(//|\*|/\*)' scripts/catalogue/fetch-marvelcdb.mjs | grep -c "12001c\|13001c"` devuelve 0
    - `grep -c "CÓMO AÑADIR UN HÉROE" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 1
    - `node -e "const p=require('./package.json');console.log(p.scripts['catalogue:generate'])"` imprime `node scripts/catalogue/fetch-marvelcdb.mjs`
    - `node -e "const p=require('./package.json');console.log(['build','generate','postinstall','test'].map(k=>p.scripts[k]).join('|'))"` no contiene la subcadena `catalogue`
  </acceptance_criteria>
  <done>El script existe, pasa el chequeo de sintaxis de Node, declara las 32 filas literales con sus puertas de aborto y su lista blanca, documenta el procedimiento de una fila de CAT-07, y `npm run catalogue:generate` está registrado sin contaminar build/generate/postinstall/test.</done>
</task>

<task type="auto">
  <name>Task 2: Ejecutar el script, contrastar los datos y verificar el determinismo</name>
  <files>content/marvel-characters.json, scripts/catalogue/fetch-marvelcdb.mjs</files>
  <read_first>
    - scripts/catalogue/fetch-marvelcdb.mjs (el fichero recién escrito, para poder corregir `expectedSetCode` de Rhino/Ultron con el valor real y para diagnosticar cualquier aborto)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-RESEARCH.md §"Confirmación completa — tabla de 23 héroes" y §"Confirmación completa — 3 villanos, 9 cartas de etapa" (las cifras de contraste)
    - engine/catalogueSchema.ts (para poder validar la salida contra el esquema)
  </read_first>
  <action>
Ejecutar `npm run catalogue:generate`.

Si aborta por `expectedSetCode` en Rhino o Ultron —esperado, porque ese valor se dejó
por confirmar en la Task 1—, leer del mensaje de error el `card_set_code` real que
devuelve la API, fijarlo literalmente en las filas correspondientes de
`VILLAIN_STAGE_CARDS` y volver a ejecutar. Ese valor es un hecho de la API que se
registra una sola vez, no una expectativa que se relaja: si el aborto fuera por
cualquier otro motivo (nombre inesperado, tipo de carta, etapa no mapeable, campo
ausente), NO se debe ablandar la puerta — hay que diagnosticar la causa real.

Contrastar la salida contra las tablas de `05-RESEARCH.md`:
- `heroes` tiene 23 entradas, en el orden declarado, empezando por `spider-man` y
  terminando en `jubilee`; ningún héroe fuera de la lista de D-03.
- Muestreo de cifras, comprobando al menos: Iron Man `health` 9 y `handSize` 6 (no 1
  — sería el `hand_size` del lado héroe, la trampa de D-09); Thor, Hulk y Vision con
  `handSize` 5; Hulk `health` 18; Drax y Vision con `alterEgo` igual a su `name`;
  Ms. Marvel con `id` `ms-marvel`.
- `villains` tiene 3 entradas —`rhino`, `kang`, `ultron`— y no aparece Klaw.
- Kang tiene `healthPerHero` false en la etapa 2 y true en las etapas 1 y 3, con
  `health` 12/18/20. Ultron 17/22/27. Rhino 14/15/16.
- El fichero no contiene ninguna de estas subcadenas: `"text"`, `"flavor"`,
  `"traits"`, `"imagesrc"`, `"illustrator"`, `"url"`, `"octgn_id"`, `"generatedAt"`.

Validar la salida contra el esquema del plan 05-01 sin escribir todavía el fichero de
test definitivo (eso es del plan 05-03): basta con una comprobación puntual desde
Vitest o con un fichero temporal que se borre después. Si el esquema rechaza la
salida, el fallo está en el script o en el esquema — corregir el que corresponda, no
relajar el esquema para acomodar una salida equivocada.

Verificar el determinismo de D-10 de forma observable: con el fichero ya en el
índice de git (`git add content/marvel-characters.json`), volver a ejecutar
`npm run catalogue:generate` y comprobar que `git diff -- content/marvel-characters.json`
no produce ninguna línea. Registrar en el SUMMARY que esta comprobación se hizo y su
resultado.
  </action>
  <verify>
    <automated>npm run catalogue:generate && git add content/marvel-characters.json && npm run catalogue:generate && git diff --quiet -- content/marvel-characters.json && node -e "const c=require('./content/marvel-characters.json');const im=c.heroes.find(h=>h.id==='iron-man');const kang=c.villains.find(v=>v.id==='kang');const ok=c.gameId==='marvel-champions'&&c.heroes.length===23&&c.villains.length===3&&im.handSize===6&&im.health===9&&kang.stages[1].healthPerHero===false&&kang.stages.map(s=>s.health).join(',')==='12,18,20'&&!c.villains.some(v=>v.id==='klaw');if(!ok)process.exit(1);console.log('catalogo OK: 23 heroes, 3 villanos, determinista')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run catalogue:generate` termina con exit code 0 y crea/actualiza `content/marvel-characters.json`
    - `node -e "const c=require('./content/marvel-characters.json');console.log(c.heroes.length,c.villains.length)"` imprime `23 3`
    - `node -e "const c=require('./content/marvel-characters.json');console.log(c.heroes[0].id,c.heroes[22].id)"` imprime `spider-man jubilee`
    - `node -e "const c=require('./content/marvel-characters.json');const h=c.heroes.find(x=>x.id==='iron-man');console.log(h.health,h.handSize)"` imprime `9 6`
    - `node -e "const c=require('./content/marvel-characters.json');console.log(c.heroes.filter(h=>h.handSize===5).map(h=>h.id).sort().join(','))"` imprime `hulk,thor,vision`
    - `node -e "const c=require('./content/marvel-characters.json');console.log(c.villains.map(v=>v.id).join(','))"` imprime `rhino,kang,ultron`
    - `node -e "const c=require('./content/marvel-characters.json');const k=c.villains.find(v=>v.id==='kang');console.log(k.stages.map(s=>s.health+':'+s.healthPerHero).join(' '))"` imprime `12:true 18:false 20:true`
    - `grep -Ec '"(text|real_text|flavor|traits|imagesrc|illustrator|url|octgn_id|generatedAt)"' content/marvel-characters.json` devuelve 0
    - `tail -c 1 content/marvel-characters.json | xxd -p` devuelve `0a` (salto de línea final)
    - Segunda ejecución consecutiva de `npm run catalogue:generate` deja `git diff --quiet -- content/marvel-characters.json` con exit code 0 (D-10)
    - `git status --porcelain` no muestra ningún fichero bajo `app/` ni `content/marvel-champions.json` modificado
  </acceptance_criteria>
  <done>content/marvel-characters.json existe con los 23 héroes y 3 villanos correctos, las cifras contrastadas contra las tablas de RESEARCH.md, sin ninguna clave de copyright, y una segunda ejecución del script deja el diff vacío.</done>
</task>

<task type="auto">
  <name>Task 3: Demostrar que la puerta de aborto muerde sin dejar escritura parcial</name>
  <files>scripts/catalogue/fetch-marvelcdb.mjs, content/marvel-characters.json</files>
  <read_first>
    - scripts/catalogue/fetch-marvelcdb.mjs (para saber dónde está la primera fila de `HERO_CARDS` y cómo se restaura tras la prueba)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-CONTEXT.md §D-05 y §D-06 (qué comportamiento exacto hay que demostrar)
  </read_first>
  <action>
Demostrar empíricamente, y dejar constancia en el SUMMARY, que las dos puertas de
aborto de D-05/D-06 funcionan y que ninguna de ellas deja el catálogo a medias.
Prerrequisito: `content/marvel-characters.json` ya generado y sin cambios pendientes
respecto al índice de git (así `git diff --quiet` es una prueba válida de "no se
escribió nada").

Prueba A — código inexistente (HTTP 500, D-05): copiar el script a
`scripts/catalogue/fetch-marvelcdb.mjs.bak`, sustituir en la copia de trabajo el
código `01001a` por `99999z`, ejecutar `npm run catalogue:generate`, y comprobar tres
cosas: el proceso termina con exit code distinto de 0; el mensaje de error nombra el
código `99999z` y el estado HTTP; y `git diff --quiet -- content/marvel-characters.json`
sigue devolviendo 0, es decir el fichero no se tocó. Restaurar el script desde el
`.bak` y borrar el `.bak`.

Prueba B — nombre inesperado (la puerta que resuelve la Open Question 2 de
RESEARCH.md a favor de abortar): mismo procedimiento, pero esta vez cambiando en la
copia de trabajo el `expectedName` de la primera fila de `Spider-Man` a
`Spider-Ham`. Comprobar exit code distinto de 0, mensaje que nombre ambos nombres, y
de nuevo `git diff --quiet -- content/marvel-characters.json` en 0. Restaurar.

Al terminar, `git status --porcelain scripts/catalogue/` debe listar únicamente
`scripts/catalogue/fetch-marvelcdb.mjs` y ningún `.bak`, y `git diff` del script debe
estar vacío respecto a lo escrito en la Task 1 (la restauración fue exacta).

Estas pruebas son de ejecución única del desarrollador, no tests de CI: requieren red
y el script nunca corre en CI (D-06 heredado). El gate permanente equivalente lo
aporta el plan 05-03 sobre el fichero ya generado.
  </action>
  <verify>
    <automated>cp scripts/catalogue/fetch-marvelcdb.mjs scripts/catalogue/fetch-marvelcdb.mjs.bak && sed -i '' "0,/'01001a'/s//'99999z'/" scripts/catalogue/fetch-marvelcdb.mjs && npm run catalogue:generate; RC=$?; git diff --quiet -- content/marvel-characters.json; DIFF=$?; mv scripts/catalogue/fetch-marvelcdb.mjs.bak scripts/catalogue/fetch-marvelcdb.mjs; test "$RC" -ne 0 && test "$DIFF" -eq 0 && echo "D-05 OK: abortó con rc=$RC y no escribió nada"</automated>
  </verify>
  <acceptance_criteria>
    - Prueba A: `npm run catalogue:generate` con `99999z` en la lista termina con exit code distinto de 0, y su salida de error contiene la subcadena `99999z` y un estado HTTP (`500`)
    - Prueba A: tras el aborto, `git diff --quiet -- content/marvel-characters.json` termina con exit code 0 (el fichero no cambió)
    - Prueba B: `npm run catalogue:generate` con `expectedName` `Spider-Ham` termina con exit code distinto de 0, y su salida contiene tanto `Spider-Ham` como `Spider-Man`
    - Prueba B: tras el aborto, `git diff --quiet -- content/marvel-characters.json` termina con exit code 0
    - Tras restaurar: `ls scripts/catalogue/` no lista ningún fichero `.bak`
    - Tras restaurar: `node --check scripts/catalogue/fetch-marvelcdb.mjs` termina con exit code 0 y `grep -c "99999z\|Spider-Ham" scripts/catalogue/fetch-marvelcdb.mjs` devuelve 0
    - Tras restaurar: `npm run catalogue:generate` vuelve a terminar con exit code 0 y `git diff --quiet -- content/marvel-characters.json` sigue en 0
    - El SUMMARY registra los exit codes observados en ambas pruebas
  </acceptance_criteria>
  <done>Está demostrado con exit codes y diffs vacíos que un código inválido y un nombre inesperado abortan el script sin escribir nada en content/marvel-characters.json, y el script queda restaurado byte a byte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marvelcdb.com → máquina del desarrollador | Respuesta JSON de un tercero, no controlada, que se convierte en artefacto committeado del repo |
| máquina del desarrollador → repo público en GitHub | Todo lo que el script escriba queda publicado; el filtro es la proyección de lista blanca |
| repo → build de despliegue (Vercel) y CI | El script no debe cruzar esta frontera nunca: la app es "sin backend" y no puede depender de una llamada de red en build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `extractHero` / `extractVillainStage` en `scripts/catalogue/fetch-marvelcdb.mjs` | mitigate | Proyección campo a campo de exactamente 5 claves de héroe y 4 de etapa; `{ ...card }` prohibido y verificado con `grep -c "\.\.\.card"` en 0. Segunda barrera independiente en el plan 05-03 |
| T-05-02 | Tampering | filas de `HERO_CARDS` / `VILLAIN_STAGE_CARDS` | mitigate | Puertas de aborto por `expectedName`, `type_code` y `card_set_code`: un código mal transcrito que por azar exista pertenece a otra carta y no pasa. La puerta de `card_set_code` es la que impide que entren los códigos `exp_kang` del modo Experto. Probado en la Task 3 |
| T-05-05 | Tampering | `content/marvel-characters.json` escrito a medias | mitigate | Los 32 códigos se resuelven en memoria antes de cualquier escritura; un único `writeFileSync` al final. Verificado con `grep -c writeFileSync` en 1 y demostrado con diff vacío tras aborto (Task 3) |
| T-05-03 | Tampering | `package.json` scripts `build`/`generate`/`postinstall`/`test` | mitigate | La Task 1 solo añade `catalogue:generate` y su criterio de aceptación comprueba que las cuatro entradas de build no contienen la subcadena `catalogue`. El gate permanente y automatizado lo instala el plan 05-03 |
| T-05-06 | Repudiation | JSON editado a mano tras generarlo | mitigate | Cabecera que declara este script como único escritor (D-09) + determinismo comprobable (`git diff` vacío en la segunda ejecución) que hace visible cualquier edición manual |
| T-05-SC | Tampering | instalaciones npm/pip/cargo | accept | Esta fase no instala ningún paquete: el script usa solo el `fetch` global de Node 22 y `node:fs`. `grep -c "^import" scripts/catalogue/fetch-marvelcdb.mjs` no debe mostrar ninguna dependencia de terceros |
</threat_model>

<verification>
1. `npm run catalogue:generate` termina en 0 y produce un fichero con 23 héroes y 3 villanos.
2. Una segunda ejecución consecutiva deja `git diff --quiet -- content/marvel-characters.json` en 0 (D-10).
3. `grep -Ec '"(text|real_text|flavor|traits|imagesrc|illustrator|url|octgn_id|generatedAt)"' content/marvel-characters.json` devuelve 0 (CAT-04 / D-10).
4. Un código inválido y un `expectedName` erróneo abortan con exit code distinto de 0 sin modificar el fichero (D-05/D-06).
5. `npm run test` sigue en 0 — los tests preexistentes no se rompen.
6. `git status --porcelain` no lista nada bajo `app/`, ni `content/marvel-champions.json`, ni `nuxt.config.ts` (scope fence).
</verification>

<success_criteria>
- El catálogo committeado cubre CAT-01 (23 héroes con nombre, alter ego, vida y tamaño de mano) y CAT-02 (3 villanos con vida por etapa y sus banderas por jugador / por grupo).
- CAT-03 está satisfecho de forma comprobable: el script está committeado, su uso documentado en la cabecera y registrado como `npm run catalogue:generate`, y re-ejecutarlo produce el mismo resultado byte a byte.
- CAT-04 está satisfecho en la fuente: la proyección es una lista blanca y el fichero no contiene ninguna clave de texto ni de arte.
- CAT-07 está satisfecho: el bloque "CÓMO AÑADIR UN HÉROE O VILLANO NUEVO" del script describe el procedimiento de una fila.
</success_criteria>

<output>
Create `.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-02-SUMMARY.md` when done
</output>
