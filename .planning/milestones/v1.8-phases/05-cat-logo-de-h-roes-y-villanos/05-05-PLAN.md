---
phase: 05-cat-logo-de-h-roes-y-villanos
plan: 05
type: execute
wave: 5
depends_on: ["05-03", "05-04"]
files_modified:
  - engine/__tests__/characters.test.ts
  - engine/__tests__/catalogue-isolation.test.ts
autonomous: true
requirements: [CAT-04, CAT-05]
gap_closure: true
closes_gaps: ["CR-02"]

must_haves:
  truths:
    - "Con el catálogo violando el esquema, los 20 tests it.each(FORBIDDEN_KEYS) se siguen colectando y ejecutando: Vitest nunca reporta 'no tests' para characters.test.ts"
    - "Una clave prohibida insertada a la vez que una violación de esquema no relacionada (id duplicado) se reporta como fallo individual que nombra la clave encontrada"
    - "Ninguna lectura de fichero, JSON.parse ni validación Zod ocurre en ámbito de módulo ni en cuerpo de describe() en los dos ficheros de gate: todas viven dentro de un it()"
    - "Un package.json ilegible no impide que los tests de catalogue-isolation que no dependen de él (CAT-06, CAT-07, barrido de app/) se ejecuten y reporten por separado"
    - "El comentario de cabecera de characters.test.ts describe el mecanismo real que hace independiente al guardarraíl, no una independencia que el fichero no tenía"
  artifacts:
    - path: "engine/__tests__/characters.test.ts"
      provides: "Guardarraíl anti-copyright sobre el crudo con acceso perezoso, estructuralmente capaz de fallar por sí solo"
      contains: "FORBIDDEN_KEYS"
      min_lines: 90
    - path: "engine/__tests__/catalogue-isolation.test.ts"
      provides: "Gate de aislamiento con lecturas de fichero perezosas dentro de cada it()"
      contains: "readFileSync"
      min_lines: 70
  key_links:
    - from: "engine/__tests__/characters.test.ts"
      to: "content/marvel-characters.json"
      via: "lector perezoso del crudo invocado dentro de cada it(), nunca en ámbito de módulo"
      pattern: "marvel-characters\\.json"
    - from: "engine/__tests__/characters.test.ts"
      to: "engine/catalogueSchema.ts"
      via: "validateCharacterCatalogue invocado dentro de un it(), no al evaluar el módulo"
      pattern: "validateCharacterCatalogue"
    - from: "engine/__tests__/catalogue-isolation.test.ts"
      to: "package.json y .github/workflows/ci.yml"
      via: "lectores perezosos invocados dentro de cada it()"
      pattern: "ci\\.yml"
---

<objective>
Cerrar el gap CR-02 de `05-VERIFICATION.md`: `engine/__tests__/characters.test.ts` llama a
`validateCharacterCatalogue(rawCatalogue)` en ámbito de módulo (línea 17). El verificador
reprodujo empíricamente que, cuando esa llamada lanza, Vitest reporta `Test Files 1
failed` pero `Tests no tests` — ninguno de los 20 tests `it.each(FORBIDDEN_KEYS)` ni
ninguna invariante de forma llega siquiera a colectarse. El mismo acoplamiento existe en
`engine/__tests__/catalogue-isolation.test.ts` (líneas 26-30).

Purpose: la build global sigue fallando (exit code distinto de cero), así que el criterio
de éxito nº 1 del ROADMAP se sostiene de forma gruesa. Lo que NO se sostiene es el
must-have explícito del plan 05-03 y la afirmación literal de la cabecera del propio
fichero: "una segunda barrera INDEPENDIENTE de z.strictObject". Es falsa hoy — y falla
justo en el escenario para el que se escribió: una fuga de clave con copyright que
coincida con cualquier otra violación de esquema hace que CI reporte un error genérico de
colección y que la clave prohibida no se nombre nunca. Un guardarraíl que miente sobre lo
que garantiza es peor que no tenerlo, porque nadie vuelve a mirarlo.

Este plan no añade gates nuevos ni endurece ninguno: reestructura cómo se cargan los datos
en los dos ficheros para que cada aserción pueda fallar sola, y reproduce la prueba de
mutación exacta que el verificador exige para dar el gap por cerrado.

Output: los dos ficheros de gate con acceso perezoso a fichero/parseo/validación, más la
evidencia de las dos pruebas de mutación registrada en el SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-VERIFICATION.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-REVIEW.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-03-PLAN.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-03-SUMMARY.md
@.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-04-SUMMARY.md

<interfaces>
<!-- Lo que ya existe y NO cambia de comportamiento en este plan. -->

De `engine/catalogueSchema.ts`:
  export function validateCharacterCatalogue(json: unknown): CharacterCatalogue   // lanza ZodError
  export const CharacterCatalogueSchema

De `engine/types.ts`, tras el plan 05-04:
  CatalogueHero { id, name, alterEgo, health, handSizeHero, handSizeAlterEgo }

Estado ACTUAL de `engine/__tests__/characters.test.ts` (lo que este plan reestructura):
  línea 14  const contentPath = fileURLToPath(new URL('../../content/marvel-characters.json', import.meta.url))
  línea 15  const rawText = readFileSync(contentPath, 'utf-8')
  línea 16  const rawCatalogue: unknown = JSON.parse(rawText)
  línea 17  const catalogue = validateCharacterCatalogue(rawCatalogue)     <- causa raíz del gap
  líneas 33-54  const FORBIDDEN_KEYS = [...]   (20 claves, se mantiene tal cual)
  líneas 59-61  function findForbiddenKey(text, keys)   (se mantiene tal cual)
  líneas 63-167 los describe/it que consumen `rawText` y `catalogue`

Estado ACTUAL de `engine/__tests__/catalogue-isolation.test.ts`:
  líneas 20-24  las cinco constantes de ruta (fileURLToPath sobre URLs estáticas)
  líneas 26-30  readFileSync x4 + JSON.parse x1 en ámbito de módulo   <- mismo acoplamiento
  línea 63      const guardedEntries = ['build','generate','preview','dev','postinstall','test']
  líneas 36-56  referencesCatalogueScript() y listSourceFiles()   (se mantienen tal cual)

Comportamiento de Vitest que fija el diseño (verificado por el verificador con una sonda
aislada, y la razón por la que el boceto de `05-REVIEW.md` no basta):
  - Un `throw` mientras se EVALÚA el módulo del fichero de test = fallo de COLECCIÓN.
    Vitest reporta `Test Files 1 failed` / `Tests no tests`: ningún `it()` se registra.
  - El callback de un `describe()` TAMBIÉN se ejecuta en tiempo de colección. Mover la
    llamada al cuerpo de un `describe` NO arregla nada: reproduce el mismo fallo. El boceto
    de `05-REVIEW.md` la coloca dentro de un `describe`; ignorar ese detalle del boceto.
  - El único sitio seguro es el cuerpo de un `it()`, que se ejecuta en tiempo de test y
    convierte un throw en el fallo de ESE test, con su nombre y su mensaje.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acceso perezoso — que cada aserción de los dos gates pueda fallar sola</name>
  <files>engine/__tests__/characters.test.ts, engine/__tests__/catalogue-isolation.test.ts</files>
  <read_first>
    - engine/__tests__/characters.test.ts completo, tal como queda tras el plan 05-04 (las líneas 14-17 son la causa raíz; las líneas 19-27 son el comentario de cabecera cuya afirmación el gap declara falsa; los describe de invariantes consumen `catalogue`)
    - engine/__tests__/catalogue-isolation.test.ts completo (las líneas 26-30 replican el patrón; ojo a la línea 78, `expect(ciWorkflowText.length).toBeGreaterThan(0)`, que hoy es una aserción muerta porque el readFileSync de módulo ya habría lanzado)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-VERIFICATION.md (gap CR-02: bloques `reason`, `artifacts` y `missing` — son el suelo de aceptación)
    - engine/__tests__/voice-drift.test.ts (gate hermano del repo: patrón de helper puro compartido entre el gate real y el test de "el gate muerde"; el estilo de cabecera y de mensajes se conserva)
    - engine/catalogueSchema.ts (firma de validateCharacterCatalogue: lanza, no devuelve resultado)
  </read_first>
  <action>
Reestructurar los DOS ficheros para que ninguna operación capaz de lanzar se ejecute en
tiempo de colección. Ningún gate se añade, se quita ni se relaja: lo único que cambia es
CUÁNDO se leen y validan los datos.

Regla que gobierna toda la tarea, y que hay que respetar literalmente: las llamadas
perezosas van dentro del cuerpo de cada `it()`. NO dentro del cuerpo de un `describe()`,
porque el callback de `describe` se ejecuta en tiempo de colección y reproduciría el
mismo defecto (el boceto de `05-REVIEW.md` se equivoca en esto: no seguirlo).

En `engine/__tests__/characters.test.ts`:
- Lo que puede quedarse en ámbito de módulo, porque no lee nada ni puede lanzar de forma
  dependiente de datos: los `import`, la constante `contentPath` (es `fileURLToPath` sobre
  una URL estática), el array `FORBIDDEN_KEYS` y la función pura `findForbiddenKey`.
- Eliminar del ámbito de módulo las constantes `rawText`, `rawCatalogue` y `catalogue`.
- Añadir un lector perezoso memoizado del crudo, con nombre explícito (por ejemplo
  `readRawCatalogueText()`), que haga el `readFileSync(contentPath, 'utf-8')` la primera
  vez que se le llama y cachee el string en una variable de módulo declarada con `let`.
  El guardarraíl anti-copyright depende SÓLO de este lector: nunca de `JSON.parse`, nunca
  del esquema.
- Añadir un cargador perezoso `loadValidatedCatalogue()` que haga
  `validateCharacterCatalogue(JSON.parse(readRawCatalogueText()))` y devuelva el objeto.
  No memoizar el resultado ni cachear el error: validar en cada llamada es barato (26
  entradas) y hace que cada test que necesite el objeto reporte su propio fallo con su
  propio mensaje.
- Los 20 tests `it.each(FORBIDDEN_KEYS)` pasan a llamar `readRawCatalogueText()` dentro de
  su cuerpo y a asertar sobre ese string, con el mismo mensaje de fallo que ya nombra la
  clave encontrada. El test "el gate muerde" no cambia (opera sobre strings sintéticos).
- El test `'valida contra CharacterCatalogueSchema'` pasa a afirmar que
  `loadValidatedCatalogue()` no lanza. Con este cambio deja de ser un test muerto: ahora
  es el único sitio donde se reporta un fallo de esquema como fallo de test.
- Cada `it()` de invariantes de forma (héroes, villanos, D-11, CAT-07) obtiene su objeto
  llamando a `loadValidatedCatalogue()` en su propia primera línea. Las aserciones de
  dentro no cambian ni una: siguen siendo exactamente las que dejó el plan 05-04,
  incluidas las de `handSizeHero` / `handSizeAlterEgo` y la de ausencia del campo legado.
- Reescribir el párrafo de cabecera que hoy afirma "segunda barrera INDEPENDIENTE de
  z.strictObject" (líneas 19-27). El texto nuevo debe (a) mantener la razón original —un
  gate que afirma la AUSENCIA de una clave debe leer el string crudo, no el objeto ya
  validado—; (b) describir el mecanismo concreto que hace real esa independencia: fichero
  leído perezosamente dentro de cada `it()`, sin `JSON.parse` ni validación de por medio;
  (c) explicar por qué no puede volver a ámbito de módulo ni a cuerpo de `describe`, con
  el síntoma exacto y citable (`Tests no tests`, la suite entera desaparece de la
  colección); y (d) citar el gap CR-02 de `05-VERIFICATION.md` como origen del cambio.

En `engine/__tests__/catalogue-isolation.test.ts`:
- Mantener en ámbito de módulo las cinco constantes de ruta, el array `guardedEntries`, y
  las funciones puras `referencesCatalogueScript()` y `listSourceFiles()`.
- Eliminar del ámbito de módulo las cinco constantes que leen o parsean:
  `packageJsonText`, `packageJson`, `ciWorkflowText`, `catalogueScriptText`,
  `catalogueContentText`. Sustituirlas por lectores perezosos con nombres explícitos, uno
  por fichero, más un cargador que parsee el `package.json`. Cada `it()` llama a los que
  necesita, y sólo a ésos.
- Consecuencia buscada, que hay que dejar anotada en la cabecera: un `package.json`
  ilegible o inválido hace fallar únicamente los tests que lo usan; los de CAT-06
  (catálogo sin referencias remotas), CAT-07 (marcador documentado en el script) y el
  barrido de `app/` se siguen colectando y ejecutando por separado.
- La aserción `expect(ciWorkflowText.length).toBeGreaterThan(0)` de la línea 78 deja de
  ser muerta al hacerse perezosa la lectura: mantenerla, ahora sí comprueba de verdad la
  mitad "existe" del test.
- Los `it.each(guardedEntries)` no cambian de forma: siguen recorriendo la misma lista de
  seis nombres. El paso a lista negra sobre todas las entradas y las mejoras de
  insensibilidad a mayúsculas del gate CAT-06 están clasificados como WARNING no
  bloqueante en `05-VERIFICATION.md` y quedan FUERA de alcance de este plan de cierre de
  gaps: no tocarlos.
  </action>
  <verify>
    <automated>npx vitest run --project engine</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project engine` termina con exit code 0 y el recuento de tests no ha bajado respecto a la ejecución previa a esta tarea (los gates se reestructuran, no se eliminan)
    - `npm run test` termina con exit code 0
    - En `engine/__tests__/characters.test.ts` no queda ninguna asignación de ámbito de módulo que lea o valide: `grep -nE "^(const|let) .*(readFileSync|JSON\.parse|validateCharacterCatalogue)" engine/__tests__/characters.test.ts` no devuelve ninguna línea
    - En `engine/__tests__/catalogue-isolation.test.ts`: `grep -nE "^(const|let) .*(readFileSync|JSON\.parse)" engine/__tests__/catalogue-isolation.test.ts` no devuelve ninguna línea
    - `grep -c "readFileSync" engine/__tests__/characters.test.ts` devuelve al menos 1 (la lectura sigue existiendo, dentro de la función perezosa)
    - `grep -c "FORBIDDEN_KEYS" engine/__tests__/characters.test.ts` devuelve al menos 2 y el array sigue declarando 20 claves
    - `grep -c "fetch(" engine/__tests__/characters.test.ts engine/__tests__/catalogue-isolation.test.ts` devuelve 0 en ambos (los gates siguen siendo sólo lectores, sin red ni procesos hijos)
    - Ninguna llamada a `readRawCatalogueText`, `loadValidatedCatalogue` ni a los lectores perezosos del fichero de aislamiento aparece dentro del cuerpo de un `describe(` fuera de un `it(` — comprobación manual documentada en el SUMMARY citando los números de línea de cada invocación
    - `grep -c "CR-02" engine/__tests__/characters.test.ts` devuelve al menos 1 (la cabecera cita el gap que originó el cambio)
    - `grep -ic "no tests" engine/__tests__/characters.test.ts` devuelve al menos 1 (el síntoma queda documentado para que nadie revierta el patrón)
  </acceptance_criteria>
  <done>Los dos ficheros de gate cargan fichero, parseo y validación de forma perezosa dentro de cada it(); ninguna operación capaz de lanzar corre en tiempo de colección; los gates son exactamente los mismos que antes, sólo que ahora cada uno puede fallar por su cuenta.</done>
</task>

<task type="auto">
  <name>Task 2: Prueba de mutación — reproducir la evidencia del verificador y demostrar que ahora reporta individualmente</name>
  <files>(ninguno modificado — tarea de verificación con mutaciones temporales que se revierten)</files>
  <read_first>
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-VERIFICATION.md (gap CR-02, segundo punto de `missing`: es la definición literal de esta prueba — clave prohibida MÁS algo no relacionado roto a la vez)
    - .planning/phases/05-cat-logo-de-h-roes-y-villanos/05-03-SUMMARY.md (las cuatro pruebas de mutación que documentó el plan 05-03: mismo formato de registro de evidencia a replicar)
    - engine/__tests__/characters.test.ts tal como queda tras la Task 1
    - engine/__tests__/catalogue-isolation.test.ts tal como queda tras la Task 1
    - content/marvel-characters.json (el fichero que se muta temporalmente)
  </read_first>
  <action>
Dos pruebas de mutación, ejecutadas a mano y revertidas siempre. Regla de seguridad
innegociable: mientras haya una mutación aplicada NO se commitea nada, NO se ejecuta
`npm ci` ni `npm install`, y la reversión se hace con `git checkout --` sobre el fichero
mutado antes de pasar a lo siguiente. Si el ejecutor se interrumpe a mitad, lo primero al
retomar es comprobar `git status --porcelain` y revertir.

Prueba A — la que exige `05-VERIFICATION.md` para dar CR-02 por cerrado:
1. Baseline: ejecutar `npx vitest run --project engine engine/__tests__/characters.test.ts`
   y anotar el recuento de tests que pasan.
2. Mutar `content/marvel-characters.json` con DOS defectos simultáneos, uno de copyright
   y otro no relacionado: (a) añadir una clave `"flavor"` con un texto cualquiera dentro
   del primer héroe, y (b) duplicar entero ese primer objeto de héroe dentro del array
   `heroes`, lo que dispara la rama de ids duplicados del `superRefine` del esquema. El
   fichero debe seguir siendo JSON sintácticamente válido, para que el defecto sea de
   esquema y no de parseo.
3. Volver a ejecutar el mismo comando y capturar la salida completa. Lo que debe verse, y
   que es la prueba de que el gap está cerrado: la salida NO contiene `no tests`; el
   recuento de tests colectados es el mismo que el del baseline; el test de la clave
   `"flavor":` aparece nombrado individualmente como fallo, con su mensaje "Se encontró la
   clave prohibida ..."; y el test de validación de esquema falla por separado, con el
   error de id duplicado. Es decir: dos fallos con nombre, no un error genérico de
   colección.
4. Revertir con `git checkout -- content/marvel-characters.json`, re-ejecutar y confirmar
   exit code 0.

Prueba B — el acoplamiento hermano de `catalogue-isolation.test.ts`:
1. Mutar `package.json` para que sea ilegible por `JSON.parse` (por ejemplo, quitar una
   llave de cierre). No ejecutar ningún comando de npm mientras esté así: invocar Vitest
   directamente con `npx vitest run --project engine
   engine/__tests__/catalogue-isolation.test.ts`.
2. Confirmar en la salida que los tests que NO dependen de `package.json` —CAT-06 (el
   catálogo no contiene referencias remotas), CAT-07 (el script documenta el
   procedimiento de una fila) y el barrido de `app/`— se colectan y PASAN, mientras que
   fallan sólo los que sí lo leen. La salida no debe contener `no tests`.
3. Revertir con `git checkout -- package.json`, re-ejecutar y confirmar exit code 0.

Registrar en el SUMMARY, para cada prueba: el comando exacto, las líneas relevantes de la
salida de Vitest (recuento de ficheros y de tests, nombres de los tests que fallaron), y
la confirmación de reversión limpia. Ésa es la evidencia que la re-verificación de la fase
va a leer.

No crear ningún fichero de sonda permanente ni ningún script en el repo: estas mutaciones
son manuales y efímeras, igual que las cuatro que documentó el plan 05-03.
  </action>
  <verify>
    <automated>npx vitest run --project engine && git status --porcelain content/marvel-characters.json package.json</automated>
  </verify>
  <acceptance_criteria>
    - Prueba A: con la doble mutación aplicada, la salida de `npx vitest run --project engine engine/__tests__/characters.test.ts` NO contiene la subcadena `no tests`
    - Prueba A: esa misma salida nombra individualmente el test de la clave `"flavor":` como fallido e incluye el mensaje que nombra la clave prohibida
    - Prueba A: esa misma salida muestra además un fallo separado del test de validación de esquema (id duplicado), y el número de tests colectados coincide con el del baseline
    - Prueba B: con `package.json` ilegible, la salida de `npx vitest run --project engine engine/__tests__/catalogue-isolation.test.ts` NO contiene `no tests`, y los tests de CAT-06, CAT-07 y del barrido de `app/` aparecen como pasados
    - Tras revertir ambas mutaciones, `git status --porcelain content/marvel-characters.json package.json` no imprime nada
    - `npx vitest run --project engine` termina con exit code 0 y `npm run test` termina con exit code 0
    - No se ha creado ningún fichero nuevo en el repo: `git status --porcelain` no muestra ninguna entrada `??` atribuible a esta tarea
    - El SUMMARY contiene, transcritas, las líneas de recuento de Vitest de las dos pruebas y los nombres de los tests que fallaron individualmente
  </acceptance_criteria>
  <done>La evidencia con la que el verificador declaró el gap CR-02 está reproducida sobre el código reestructurado y ahora da el resultado contrario: los tests se colectan, la clave prohibida se nombra en su propio fallo, y el acoplamiento hermano del gate de aislamiento también está roto. El repo queda sin mutaciones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `content/marvel-characters.json` -> repo público / bundle desplegado | El guardarraíl anti-copyright de estos ficheros es lo único que se interpone entre una fuga de texto de carta y un commit público permanente. La frontera no la cruza un atacante: la cruza un descuido del desarrollador o un cambio de forma de la API de MarvelCDB. |
| Mutación temporal durante la prueba -> historial de git | Durante la Task 2 el repo contiene deliberadamente una clave prohibida y un `package.json` roto. Si eso se commitea, el guardarraíl habría sido derrotado por su propia prueba. |

No hay servidor, ni sesión, ni entrada de usuario: modelar spoofing, repudio o elevación
de privilegios aquí sería inventar superficie.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | Guardarraíl anti-copyright de `characters.test.ts` que falla abierto | mitigate | Es el gap que cierra este plan. Lectura perezosa del crudo dentro de cada `it()`, sin `JSON.parse` ni Zod de por medio, de modo que ninguna otra violación pueda cancelar la colección de los 20 tests. Demostrado por la prueba de mutación doble de la Task 2, no sólo afirmado en un comentario. |
| T-05-07 | Information disclosure | Commit accidental de la mutación de la Task 2 | mitigate | Prohibición explícita de commitear con mutación aplicada; reversión obligatoria con `git checkout --` tras cada prueba; criterio de aceptación que exige `git status --porcelain` vacío para los dos ficheros mutados; y el propio guardarraíl reestructurado, que ahora sí nombra la clave si sobreviviera. |
| T-05-08 | Denial of service | `package.json` roto durante la Prueba B seguido de un `npm ci` | mitigate | Instrucción explícita de no ejecutar ningún comando de npm mientras la mutación está aplicada e invocar Vitest directamente con `npx`. El daño posible es local y reversible con `git checkout --`. |
| T-05-09 | Tampering | Regresión futura: alguien devuelve la carga a ámbito de módulo | accept (mitigado con documentación) | El comentario de cabecera reescrito documenta el síntoma citable (`Tests no tests`) y el gap CR-02 que lo originó. Un gate automático que detectara el patrón sería un meta-test frágil sobre el propio texto fuente; se acepta el riesgo residual a cambio de no añadir esa fragilidad. |
| T-05-SC | Tampering | npm / instalación de paquetes | n/a | Este plan no instala ningún paquete ni modifica dependencias; `package.json` sólo se muta y se revierte dentro de la Prueba B. La puerta de legitimidad de paquetes no se activa. |
</threat_model>

<verification>
1. `npm run test` termina con exit code 0.
2. El número total de tests del proyecto `engine` es mayor o igual que antes de este plan: ningún gate desapareció al reestructurar.
3. `git diff --stat` toca exactamente los dos ficheros de `files_modified`, ni uno más — en particular, `content/marvel-characters.json` y `package.json` quedan sin cambios.
4. Las dos pruebas de mutación están registradas en el SUMMARY con salida transcrita, no resumida en prosa.
</verification>

<success_criteria>
- El gap CR-02 de `05-VERIFICATION.md` queda cerrado: el guardarraíl anti-copyright es estructuralmente independiente del éxito del esquema, y está demostrado con la prueba de mutación que el propio verificador especificó.
- La afirmación de la cabecera de `characters.test.ts` vuelve a ser cierta, y ahora describe el mecanismo que la sostiene.
- El acoplamiento hermano de `catalogue-isolation.test.ts` queda roto también.
- Ningún gate se ha relajado, eliminado ni endurecido: sólo ha cambiado cuándo se cargan los datos.
- El repo queda limpio, sin mutaciones ni ficheros de sonda.
</success_criteria>

<output>
Create `.planning/phases/05-cat-logo-de-h-roes-y-villanos/05-05-SUMMARY.md` when done
</output>
